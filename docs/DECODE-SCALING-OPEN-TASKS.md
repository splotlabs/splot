# Decode Scaling Open Tasks

This is the live checklist for the work described in
[`DECODE-SCALING-MISSION.md`](DECODE-SCALING-MISSION.md). The mission records
measurements, rejected experiments, and architectural conclusions. This file
records what remains to be done.

## Update rule

When an investigation or implementation discovers another bottleneck,
correctness requirement, or prerequisite:

1. Add it here before continuing implementation.
2. Give it a stable ID, owner, status, dependencies, evidence, and next gate.
3. Keep only one implementation task active at a time. Read-only audits may run
   in parallel.
4. Do not delete rejected or completed work. Move its evidence to the mission
   ledger and update dependent tasks here.
5. A task is complete only when its stated correctness and measurement gates
   pass. Source code existing on a branch is not completion.

Statuses are `Open`, `Audit active`, `Investigation active`, `Fix active`,
`Implementation active`, `Blocked`, `Completed`, `Rejected`, and
`Rejected standalone`.

## Current checklist

| ID | Status | Owner | Task | Dependencies | Evidence | Next gate |
|---|---|---|---|---|---|---|
| SCALE-011-A | Completed | `/root` | Admit entropy work from multiple frame contexts into the shared queue, guarded by exact CDF, CCSO, and reference progress. | Integrated row ownership; pending reference products | Merged in `#1140` through SCALE-011-H with byte-identical 1T/10T output and the serial-reference-storage peak regression. | Preserve the merged capability and regression coverage. |
| SCALE-011-A1 | Completed | `/root` | Reintroduce the proven three-context entropy admission design without restoring obsolete whole-frame ownership. | SCALE-011-D; SCALE-011-A | Merged in `#1140` through SCALE-011-H with exact pending-product conditions, ordered promotion, and failure settlement. | Preserve the merged three-context path. |
| SCALE-011-A2 | Rejected standalone | `/root` | Recheck dav2d-style context depth now that entropy, reconstruction rows, and filter publication coexist in one candidate. | SCALE-011-A1; SCALE-011-D | Ten contexts remained byte-exact, but seven alternating pairs regressed 8T from about 0.260s to 0.264s and 10T from about 0.261s to 0.265s versus three contexts. Extra whole-frame entropy contexts still add pressure without enough new row-progress work. | Reverted to three contexts. Do not raise entropy depth again without a post-SCALE-011-H profile showing admission starvation; the tested deeper configurations added pressure and regressed. |
| SCALE-011-A3 | Rejected standalone | `/root` | Recheck dav2d-style full frame-context depth instead of entropy depth alone. | SCALE-011-A1; SCALE-011-D/E | A depth-aware recon/filter lane plus the same number of reusable temporal-scratch slots is ownership- and failure-safe. The full frame-delay-sized candidate was byte-exact for all 30 frames at 1T/10T, but nine alternating pairs were neutral at 8T/10T. A depth-four variant, matching the plateau in both decoders' frame-delay sweeps, was also neutral. Extra live reconstruction workspaces and shadows add pressure without removing the universal motion/resolve/copy barriers. | Reverted to three recon/filter contexts, one temporal baton, and three entropy contexts. Do not raise full frame-context depth again without post-SCALE-011-H profiler evidence that additional live contexts unlock ready row work. |
| SCALE-011-B | Completed | `/root` | Replace the all-at-once reconstruction task dump with progress-driven row advancement, ordered publication, and requeuing. | SCALE-011-B3; SCALE-011-F1 | Merged in `#1140` through SCALE-011-H; SCALE-011-B1/B2 remain evidence that queue-only variants around shadow storage are not useful. | Preserve the merged row continuation; do not benchmark another queue-only variant. |
| SCALE-011-B1 | Rejected standalone | `/root` | Bound existing batch precompute with a pool-sized sliding window replenished by ordered commits. | SCALE-011-B | The exact candidate kept 1T neutral but regressed 8T from about 0.27s to 0.43s and 10T from about 0.27s to 0.43s, roughly 60%. With three in-flight frames and window `max(1, (threads - 2) / 3)`, commit-driven replenishment starved useful shadow precompute rather than prioritizing publication. | Reverted completely. Preserve the result as evidence that queue repair must change task granularity/continuation ownership, not only admission count. |
| SCALE-011-B2 | Rejected standalone | `/root` | Replace the shadow-precompute/serial-commit pair with one resumable canonical row continuation. | SCALE-011-A1; SCALE-011-D | The candidate retained batch-four `OwnedFrameRect` precompute, seeded `pool_width - 1` self-replenishing producers, and kept exactly one canonical continuation. Full 30-frame raw output remained byte-identical at 1T/10T. Seven alternating pairs were neutral: about 0.26s versus 0.26s at both 8T and 10T, with 1T about 1.00s for both. | Reverted the topology. Do not tune FIFO ordering again without removing a whole-frame dependency or the shadow-surface copy. |
| SCALE-011-B3 | Completed | `/root` | Replace temporary `OwnedFrameRect` reconstruction plus serial publish/replay with direct canonical row-band ownership. | SCALE-011-F1; exact current-frame and IntraBC liveness | Merged in `#1140` through SCALE-011-H for bounded `ReferenceOnly` rows. | Preserve conservative fallback for GeneralIntra, IntraBC, current-frame inter, unbounded inter, partial tiles, and disabled filters. Keep B5's parse-time containment gate. |
| SCALE-011-B4 | Rejected | `b3_general_intra_surface` + `/root` | Reconstruct GeneralIntra directly into the current canonical row band while reading the sealed predecessor band. | SCALE-011-B3 | The target abstraction became byte-exact after admission retained leaf-level dependency and rejected hidden current-frame inter in mixed superblocks. Performance still failed architecturally: admitting GeneralIntra made each band reconstruct all ReferenceOnly work after its first intra dependency on the serial canonical continuation. At 8T the integrated path was about 0.271 s versus 0.251 s with the B3 fallback; at 10T it was about 0.273 s versus 0.246 s. | Reverted completely. Do not retry without a horizontal wavefront or disjoint canonical sub-band design that preserves ReferenceOnly fanout after an intra dependency. |
| SCALE-011-B5 | Completed | `/root` | Prove owned-band write containment for unresolved `ReferenceOnly` leaves before frame admission. | SCALE-011-B3 | `ReconRowEntry` retains its pending-inter index and admission evaluates the exact parse-time luma, chroma, and residual write footprint through the shared `prepass_write_is_contained` check. Unresolved leaves no longer bypass containment. Timing diagnostics confirm the contained wide fixture selects `owned_bands`; the multirow fixtures conservatively select legacy storage for current-frame reads. | Preserve the unresolved-leaf containment check and conservative whole-frame fallback. |
| SCALE-011-C | Completed | `scale011_raw_band_handoff_audit` | Define the earliest safe canonical raw-band handoff in the presence of current-frame reads and IntraBC. | None | If the latest canonical row ends at MI row `F`, ordinary current-frame reads permit § 7.17 deblock only through `F - sb_h4`; terminal commit permits `mi_rows`. Any actual local or global `ReconCommand::Intrabc` makes the simple row frontier unsafe, so those frames retain whole-frame deblock. `prime_vertical_pass` may not run early. | Preserve this rule in focused frontier tests. Do not add a whole-frame copy; optimize IntraBC only after deriving exact raw-source liveness. |
| SCALE-011-D | Completed | `/root` | Move one canonical row through raw-band ownership, incremental deblock, owned filter stripes, and exact publication. | SCALE-011-C; SCALE-011-D1 through SCALE-011-D12 | The integrated owner now advances canonical reconstruction, performs only positive safe deblock frontiers, extracts owned windows, schedules filter stripes, and publishes exact progress. Focused overlap tests pass; full 30-frame 1T/10T raw output is 186,624,000 bytes with SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`. Nine alternating pairs show no 1T regression, 4.5% at 8T, and 3.6% at 10T versus exact base. | Retain D inside SCALE-011-H. The historical D-only candidate was below threshold; the cohesive H candidate clears it after removing the adjacent B/A barriers. |
| SCALE-011-D1 | Completed | `scale006_filter_handoff` | Add a committed multirow active-deblock inter oracle with no IntraBC. | None | `syn-2frame-multirow-deblock-inter-64x256-10bit-q158.ivf` has all four deblock passes active, no IntraBC, and byte-identical 98,304-byte AVM/Splot raw output. The committed scheduled-frame test compares serial and 8T hashes; frontier unit tests cover one-row lag, terminal completion, and conservative fallback. Both multirow fixtures select legacy storage for current-frame reads. | Keep both fixtures in the decoder oracle and run them after every SCALE-011-D change. |
| SCALE-011-D2 | Completed | `scale006_filter_handoff` | Make the early owned-filter sink and terminal freeze scheduler-owned. | SCALE-011-D1 | `OwnedFilterSetup` owns `Arc<FrameProgress>`, immutable filter inputs, exactly-once stripe lifecycle, and one terminal `Arc::try_unwrap` freeze. Terminal-created and early-created scheduler fanout are exact; focused missing, duplicate, out-of-order, and shared-owner tests pass. | Preserve the ownership contract while changing admission policy. Keep SCALE-012's outcome ordering repair separate. |
| SCALE-011-D3 | Completed | `/root` | Eliminate real-file nondeterminism in early filter extraction, execution, and publication. | SCALE-011-D2 | Two frontier bugs were fixed: superblock units are counted only after a complete row, and a zero safe frontier is represented as `None` so `FrameDeblock::advance(0)` cannot run its four-MI vertical lead over unreconstructed columns. Full 30-frame raw output is exact at 1T and 10T. | Retain wide-unit, zero-frontier, conservative-fallback, and active-overlap coverage. |
| SCALE-011-D4 | Rejected standalone | `scale006_filter_handoff` | Make progressive dependent-frame reference consumption byte-exact with early filter publication. | SCALE-011-D3 | Forced settled reference gates did not change the mismatch. The earlier “disabled deblock” control still called `advance(0)`, which mutates four MI rows; a genuine terminal-only control was exact. The frame-2 failure was therefore current-frame raw corruption, not progressive dependent-reference admission. | Do not widen reference gates without new evidence; continue using exact published-row conditions. |
| SCALE-011-D5 | Completed | `/root` | Preserve lossless-grid derivation when deblock records move into the incremental owner. | SCALE-011-D2 | The owned setup now derives lossless state from complete records before `detach_deblock_records` transfers the vectors to incremental deblock. The focused regression proves the lossless grid remains populated after the single-owner move. Full non-lossless real-file output remains exact. | Keep the lossless ownership regression and restore moved records exactly once at finish. |
| SCALE-011-D6 | Completed | `/root` | Make wide-frame scheduled superblock reconstruction byte-exact before filters without discarding useful intra-row fanout. | SCALE-011-D3 | The whole-row diagnostic was exact only because it masked the zero-frontier deblock mutation, and it regressed 8T/10T by about 12%. Restored superblock units batched four per task are exact once zero performs no deblock work. Focused multirow and wide-frame serial/parallel exactness fixtures pass. | Keep batch-four superblock fanout; do not restore whole-row tasking. |
| SCALE-011-D7 | Completed | `scale006_filter_handoff` | Isolate the owned-filter lifecycle component that makes scheduled superblock reconstruction diverge. | SCALE-011-D2; SCALE-011-D6 | Scheduled superblock parsing, batch sizes 1/4, detached filter records, per-commit deblock, whole-reference admission, ordered scratch resets, and lazy `FrameProgress.begin` were individually non-causal. Returning `workspace + records` to the old terminal finish is exact. Terminal-owned setup with serial filtering is exact, and terminal-owned setup with the same scheduler stripe fanout/publication is also exact. The 2x2 isolates the remaining mismatch to state created before early owned-filter execution; scheduler fanout, sink opening, and publication are non-causal. | Do not serialize superblock reconstruction or disable scheduler stripe fanout. Continue with the surviving setup-state hypotheses. |
| SCALE-011-D8 | Rejected standalone | `scale006_filter_handoff` | Make early owned-window readiness include every source sample and halo copied by `DeblockedWindow::extract`. | SCALE-011-D7 | Every `DeblockedWindow` extracted before terminal commit was re-extracted after terminal reconstruction and complete deblock, then compared plane-by-plane and sample-by-sample. All early windows were byte-identical to their terminal counterparts while frame 2 still diverged. Copied source rows, chroma rows, horizontal padding, and vertical halo are therefore not causal. | Keep the diagnostic result in the mission ledger; do not widen or serialize the ready-window frontier without new evidence. |
| SCALE-011-D9 | Rejected standalone | `/root` | Separate concurrent early filter execution/publication from early immutable setup and window capture. | SCALE-011-D7; SCALE-011-D8 | Setup creation, incremental deblock, and early window capture were left unchanged, but every captured filter job was withheld until terminal reconstruction. Frame 2 retained the same divergent hash `30abcb68827921f85c90c03b0b0f6d703f34be41e39a5400c4fb9349da8dff4e`, so overlap between filter execution/publication and reconstruction is not causal. | Compare the early-detached per-row setup records with the terminal-replayed records, beginning with `tx_skip_records`; do not serialize filter jobs. |
| SCALE-011-D10 | Rejected standalone | `/root` | Prove parity between early-detached and terminal-replayed immutable filter records. | SCALE-011-D9 | Early-detached and independently terminal-replayed `tx_skip_records` matched element-for-element on all six admitted frames in the divergent three-output chain, with counts 510, 1,667, 1,229, 705, 510, and 617. Frame 2 retained the same divergent hash. CDEF, CCSO, GDF, and LR products are the same entropy-pass owners in both paths. | Do not change immutable record derivation without new evidence. Recheck whether incremental deblock mutates raw pixels still needed by later canonical reconstruction. |
| SCALE-011-D11 | Completed | `/root` | Make a zero safe frontier perform no deblock work. | SCALE-011-D3; SCALE-011-D8; SCALE-011-D10 | A genuine terminal-only deblock control restored frame 2 to the expected hash `f6343e5dac10026444f2d58a4d970e87d6415bff78e3cb90d7c57f18127583a4`. `safe_deblock_mi_end` now returns `Option<NonZeroUsize>`, so a zero frontier cannot call `FrameDeblock::advance(0)` and trigger its four-MI vertical lead. | Preserve the nonzero type boundary and exact full-file gate. |
| SCALE-011-D12 | Completed | `/root` | Restore focused scheduled-frame exactness after rejecting whole-row tasking. | SCALE-011-D6 | Focused multirow, active-deblock, and wide-frame fixtures prove serial/parallel output equality. Process-global scheduler telemetry assertions were removed because concurrent tests made them racy; batch-four remains the accepted legacy implementation. | Keep exactness coverage aligned with the accepted batch-four design; do not restore process-global telemetry assertions. |
| SCALE-011-E | Completed | `scale005_motion_bands` | Publish immutable reference bands and exact reconstruction/filter watermarks to dependent frames. | SCALE-011-D | Merged in `#1140` through SCALE-011-H with monotonic stripe progress and fail-closed reference reads. | Do not add a second reference-band abstraction; retain the exact sample-watermark path. |
| SCALE-011-F | Completed | `/root` | Replace whole-frame motion-field/temporal resolve with an ordered row/band producer-consumer baton. | SCALE-011-A; SCALE-011-E; SCALE-011-F1 | Merged in `#1140` through SCALE-011-H with band settlement and terminal whole-field compatibility. | Preserve the merged baton and failure settlement. |
| SCALE-011-F1 | Completed | `/root` | Publish immutable source motion bands and consume them through one ordered temporal-projection/resolve continuation. | SCALE-011-E | Merged in `#1140` through SCALE-011-H with separate metadata publication and ordered dependent-row resolution. | Preserve failure settlement and terminal-token coverage. |
| SCALE-011-G | Open | Unassigned | Move hot row, coefficient, motion, and filter scratch into reusable frame/task contexts. | Stable integrated task boundaries | SCALE-009 found individual recycler locks too small for standalone PRs; dav2d keeps hot scratch in task/frame contexts. | Remove hot process-global recycling from the integrated row path without increasing peak live reference storage. |
| SCALE-011-H | Completed | `/root` | Complete one cohesive dav2d-style vertical row graph: publish a source motion band, resolve the dependent row, reconstruct directly into canonical row-band ownership, publish deblock/filter/reference progress, and admit the next frame's row. | SCALE-011-A; SCALE-011-B3; SCALE-011-D/E; SCALE-011-F1 | Merged in `#1140`. Exact raw output matches base at 1T/10T. The final post-CI matrix measured +0.04% at 1T, +12.91% at 8T, and +13.67% at 10T versus exact base. | Preserve the merged graph while later candidates close the remaining dav2d gap. |
| SCALE-011-I | Paused by user | `/root` | Continue from merged `#1140` to the final dav2d target. | SCALE-011-H | No implementation leaf is active. Exact main is about 0.240 seconds at 10T versus dav2d 0.155 seconds; the 50%-faster target is at most about 0.077 seconds. K10.12–K10.57 are recorded; K10.58 stopped before selecting a successor. | Resume by re-ranking the existing exact profile and advance only an evidence-backed distinct candidate. |
| SCALE-011-K10.7 | Rejected | `/root` + subagents | Persistent row cursors, horizontal leaves, and row-scoped reference leases. | SCALE-011-H | Rejected; measurements are retained in the mission ledger. | Do not carry segmented reference storage or permanent halos forward. |
| SCALE-011-K10.8 | Rejected | `/root` + subagents | Vertical readiness graph over the packed/contiguous path. | SCALE-011-K10.7 | Rejected; measurements are retained in the mission ledger. | Do not retry whole-frame claims or lock-order/context-depth patches. |
| SCALE-011-K10.9 | Rejected | `/root` + subagents | Hybrid row bands for the remaining current-dependent frames. | SCALE-011-K10.8 | Rejected; measurements are retained in the mission ledger. | Do not retry segmented whole-frame kernels. |
| SCALE-011-K10.10 | Rejected | `/root` + subagents | Leaf-DAG row continuations with contiguous band interiors. | SCALE-011-K10.9 | Rejected; measurements are retained in the mission ledger. | Do not retry leaf/SB granularity when observable progress remains row-wide. |
| SCALE-011-K10.11 | Rejected | `/root` + subagents | Scoped zero-copy horizontal row wavefront. | SCALE-011-K10.10 | Rejected; measurements are retained in the mission ledger. | Reconstruction ownership variants are evidence-closed under safe Rust plus one contiguous mutable workspace. |
| SCALE-011-K10.12 | Rejected | `/root` + subagents | Build one fused owned `PostFilterGraph` that reduces actual filter work and removes progressive publication lock inversion. | SCALE-011-K10.11 | The integrated 10-bit path was byte-exact at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and passed the decoder all-target gate before timing. Five-pair 10T screens rejected every shape: 256-pixel slabs were +51.29% versus exact base, 512-pixel slabs +48.42%, slab expansion disabled +45.37%, and full-width publication with expansion disabled +46.16%. A safe four-segment reference view restored cross-stripe reads and remained exact, but the integrated 512-pixel graph regressed +54.94% (0.377700 s versus 0.246013 s); TIP motion-grid attribution rose from about 88 ms on base to 301 ms. | Do not retry immutable segmented reference storage or globally scheduled x-slabs. Preserve the merged contiguous progressive workspace and full-width filter-stripe task until a design removes kernel work without changing the hot reference representation. |
| SCALE-011-K10.12A | Rejected | `/root` + subagents | Fuse CDEF, CCSO, LR, and GDF into reusable typed stripe arenas without whole-stripe LR cloning or a full padded GDF source. | SCALE-011-K10.12 | Exact typed LR/GDF slab APIs removed redundant full-stripe outputs locally, but the cohesive graph did not reduce aggregate filter time enough to repay scheduling/publication costs. | Retain only the source-lifetime finding: GDF needs immutable pre-LR CDEF plus post-LR base. Do not publish the experimental APIs. |
| SCALE-011-K10.12B | Rejected | `/root` + subagents | Split each filter stripe into globally visible cache-sized x-slab nodes as part of the arena graph. | SCALE-011-K10.12A | Independent LR/GDF leaves, nonblocking completion joins, exact halos, tails, tile boundaries, and child-failure settlement were correct. Halving width granularity from 256 to 512 recovered only about three percentage points; disabling expansion still left a 45% regression, proving task count was not the primary loss. | Keep full-width filter stripes. Do not retry 128/256/512 x-slab scheduling. |
| SCALE-011-K10.12C | Rejected | `/root` + subagents | Replace whole-frame output writer locking/backlog with fixed immutable stripe slots and terminal one-copy assembly. | SCALE-011-K10.12B | Fixed `OnceLock` stripe ownership and terminal assembly were exact. Reads crossing immutable slots either waited for settled contiguous storage (+45% with the filter graph disabled) or used a safe four-segment reference view that forced hot kernels off their contiguous fast representation (+54.94% integrated). | Preserve the current contiguous progressive workspace. Address any demonstrated writer contention without changing reference-plane storage. |
| SCALE-011-K10.13 | Rejected | `/root` + sequential implementation agents | Reuse the existing typed scheduled-reconstruction context across frames, install its MC/TIP buffers once per admitted row, and localize coefficient-quant storage to the row/task lifetime. | SCALE-011-G; SCALE-011-K10.12 rejection | The full two-tier context was byte-exact at 1T/10T. Five alternating pairs improved 8T by 1.91% (0.237016 s versus 0.243152 s) and 10T by 2.50% (0.236977 s versus 0.243055 s), far below the 10% gate; dav2d measured 0.160695/0.155624 s. | Fully revert the runtime candidate. Preserve the conclusion that allocator/recycler and TLS-install overhead are not the remaining barrier; do not pursue standalone context-pool, quant-pool, residual-pool, or filter-pool edits. |
| SCALE-011-K10.14 | Rejected | `/root` + sequential implementation agents | Replace the dominant full-map multi-class Wiener-NS luma classifier-plus-filter traversal with one scalar rolling class-row/filter pass. | SCALE-011-K10.13 rejection | The candidate was byte-exact and passed focused u8/u16 composition parity, but five-pair medians regressed 8T by 3.07% and 10T by 1.66%. Rolling removed classification time (56.160 ms to 7.328 ms) yet raised Wiener-NS luma from 74.037 ms to 190.727 ms and LR from 178.704 ms to 241.881 ms. | Fully revert. Preserve the full-grid bulk traversal; do not retry scalar rolling classification without a measured faster scalar feature/filter kernel. |
| SCALE-011-K10.15 | Rejected | `/root` + sequential implementation agents | Fuse TIP motion refinement, final prediction, temporal-record emission, and output publication into one owned command/traversal. | SCALE-011-K10.14 rejection | The failure-atomic carried-plan/grid/u16-luma candidate was byte-exact and passed focused parity. Five-pair medians regressed 8T by 1.59% and 10T by 0.39%. Motion-grid attribution rose from 79.207 ms to 120.650 ms while prediction fell from 118.241 ms to 84.094 ms, leaving the integrated path neutral-to-worse. | Fully revert. Preserve the current bulk plane traversal and split motion/prediction boundary; do not retry per-cell final TIP MC, direct TIP publication, or owned luma sidecars without a measured faster scalar final-MC kernel. |
| SCALE-011-K10.16 | Rejected | `/root` | Remove the measured occupancy gap through driver placement, context depth, or cooperative admission execution without changing exact codec work. | SCALE-011-K10.15 rejection | Timeline tracing found the three-context reconstruction gate saturated for 155.9 ms but only 11.64 ms of strict single-job execution. Depth ten regressed 1.73%; frame-delay four/five moved only about 2%. An exact external-caller driver regressed 149% and remained about 160% slower after wait donation because off-pool execution lost current nested semantics. Exact cooperative global runners regressed 7.48% in the three-pair 10T screen. | Runtime edits are fully reverted. Do not retry context-depth, external-caller, or cooperative-runner scheduling in isolation. |
| SCALE-011-K10.17 | Rejected | `/root` | Give admission/driver work a distinct lane while preserving installed-pool semantics and exactly the requested compute-worker budget. | SCALE-011-K10.16 rejection | Explicit pool re-entry around every direct whole-frame walk recovered the caller-side driver from about 0.62 s to 0.29 s and kept raw output exact, but the three-pair 10T screen still regressed 18.46%. Whole-wait pool re-entry preserved work-first assistance and still regressed 18.34%. | Runtime edits are fully reverted. Do not retry caller separation under the current mixed driver/direct-work API. |
| SCALE-011-K10.18 | Rejected | `/root` + one sequential implementation agent | Decouple independent temporal-band projection from the frame-depth and ordered neighbor-grid resolve batons. | SCALE-011-K10.17 rejection | The exact after-gate split improved 10T only 1.46%. Moving preparation/projection before the N−3 gate plus removing ordered scratch recycling remained exact but regressed 2.28%. | Runtime edits are fully reverted. Preserve the evidence that projection eligibility alone does not move the wall-time critical path. |
| SCALE-011-K10.19 | Rejected | `/root` + one sequential implementation agent | Replace the whole-frame depth-three reconstruction gate with bounded cross-frame row-progress credits. | SCALE-011-K10.18 rejection | The exact complete/touched-row watermark candidate preserved all semantic gates and was byte-identical at 1T/10T, but the three-pair 10T screen regressed 0.59% (0.240336 s versus 0.239840 s); dav2d was 0.154996 s. | Runtime is fully reverted. Do not retry frame-depth gates, early projection/preparation, or cross-frame row credits. |
| SCALE-011-K10.20 | Rejected | `/root` + read-only audit agents | Re-synthesize a materially distinct candidate from scheduler transition cost, working-set pressure, and the scalar small-win ledger. | SCALE-011-K10.19 rejection | Immediately runnable commit/finish transition gaps are below 15 ms aggregate, cache-only local changes cannot recover the 24 ms threshold, and the remaining unclosed scalar ledger reaches 10% only at the optimistic edge before overlap. | No implementation. Do not replay local fallthrough, persistent runners, allocation slabs, completion coalescing, or a blind cross-domain scalar bundle. |
| SCALE-011-K10.21 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction in entropy-symbol arithmetic and CDF dispatch. | SCALE-011-K10.20 rejection | All symbol decode/update/selection/literal primitives total only about 43 worker-ms; binary specialization cannot credibly save the required 24 ms and cross-frame overlap lowers its wall ceiling further. | No implementation. Retain trusted validation; ledger fixed-arity binary decoding as a later small scalar candidate. |
| SCALE-011-K10.22 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction across coefficient parse, staged transform state, dequant/inverse transform, and residual addition. | SCALE-011-K10.21 rejection | The large coefficient stacks are inclusive. Dense coefficient-state construction is the only large exclusive materialization, but K10.13 already removed its pool ownership/search and improved 10T by only 2.50% (about 6 ms). Dequantization and scan generation account for only 15 and 12 worker-ms respectively, before overlap. | No implementation. Keep scan-table, DC-only inverse-transform, and sparse-dequant ideas in the small scalar ledger; do not add a cross-crate sparse coefficient representation for this gate. |
| SCALE-011-K10.23 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction across inter prediction, warp/subpel MC, compound output, and their intermediate materialization. | SCALE-011-K10.22 rejection | The roughly 630 worker-ms MC family is overwhelmingly required convolution and copy arithmetic. Warp's 114-sample prepared pass and 57-sample output pass are its normative horizontal and vertical filters. Translational compound already fuses the second predictor with uniform blending and specializes full-pixel, horizontal, and small two-axis cases. At the observed 5.7-core occupancy, a 24 ms wall saving needs about 137 avoidable worker-ms, which the remaining intermediates do not contain. | No implementation. Retain direct single-reference warp output and wider fused two-axis compound coverage as small-ledger ideas; do not replay per-cell TIP fusion or replace exact convolution with an approximation. |
| SCALE-011-K10.24 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction in actual CDEF, Wiener-NS/LR, GDF, or deblock kernel work. | SCALE-011-K10.23 rejection | CDEF contributes about 380 worker samples, LR about 280, GDF about 142, and deblock about 190, but nearly all of those buckets are normative tap/class/filter work. Existing code already shares CDEF luma gather data, pairs chroma, caches PC features, rolls GDF gradients, and writes deblock directly. No removable subset reaches the roughly 137 worker-ms gate. | No implementation. Keep compact prederived deblock edge descriptors as a 40–90 worker-ms small-ledger item; do not replay filter graphs, rolling Wiener, or approximations. |
| SCALE-011-K10.25 | Rejected | `/root` + one read-only audit agent + one sequential implementation agent | Preserve ready-heap priority across Rayon's queue by bounding scheduler-issued top-level jobs. | SCALE-011-K10.24 rejection | Telemetry found real inversions, and the exact POC retained excess work in the ordered heap under a pool-width issue cap. Its three-pair 10T screen improved only 1.91% median (0.240802 s versus 0.245491 s), with one pair regressing; dav2d was 0.157779 s. | Fully revert runtime and telemetry code. Queue-age union was mostly hidden latency; do not retry issue caps, urgent overflow, or admission priority without a different measured critical path. |
| SCALE-011-K10.26 | Rejected | `/root` + one read-only audit agent | Attribute the 198-worker-ms global memory-copy bucket and find a cohesive copy elimination that preserves contiguous storage and task fanout. | SCALE-011-K10.25 rejection | Exhaustive exclusive attribution splits the bucket into CDEF/filter windows 56, LR/GDF materialization/publication 36, canonical owned-rectangle commit 34, MC/prediction writes 23, coefficient/residual movement 16, frame allocation/output 4, and other metadata growth 29. The largest family is 56; even all filter copies are only 92 before required work. | No implementation. Preserve contiguous publication and deblock/filter overlap. Keep rolling CDEF, direct LR stripe output, exact-capacity metadata, and coefficient-container movement as small-ledger ideas; do not replay direct-row ownership or segmented filter storage. |
| SCALE-011-K10.27 | Rejected | `/root` + one read-only audit agent | Find a cohesive exact scalar reduction across motion-metadata construction and updates. | SCALE-011-K10.26 rejection | Temporal projection including trajectory children is 209 worker-ms, trajectory-only work 147, all neighbour-grid construction/publication 129, and motion-field folding 88 including 53 lock-wait samples. Normative projection/intersection/cell writes dominate. Dav2d's compact maps and SB-row ring do not remove Splot's two-phase readiness, warp/TIP cell values, or flag replay; the neighbour-grid hard ceiling is only 22.6 ms at 5.7 cores. | No implementation. Keep compact trajectory/owner maps, bounded neighbour rows, and exclusive motion-band segments in the small ledger; do not replay K10.18 ordering or bundle independent motion structures. |
| SCALE-011-K10.28 | Rejected | `/root` + one read-only audit agent | Attribute blocking mutex waits and find one cohesive ownership change that removes a wall-visible lock family. | SCALE-011-K10.27 rejection | Of 178 mutex-wait samples, 18 are Rayon sleep/wake and project sites total 160. The largest family is coefficient quant at 74, already calibrated by K10.13 to about 6 ms wall; motion folding is 53, admission 13 (7 inside Rayon wake), filter scratch 11, residual pools 4, frame precompute 4, and tile conditions 1. No family reaches the gate. | No implementation. Do not combine unrelated locks or replay coefficient contexts, motion segmentation, ready-queue issue caps, or standalone scratch pools. |
| SCALE-011-K10.29 | Rejected | `/root` + one read-only audit agent | Source-attribute the profile's 110-sample `<deduplicated_symbol>` bucket. | SCALE-011-K10.28 rejection | Exclusive call-tree reconstruction assigns 98 samples to compound-subpel intermediate output, 6 to deferred motion derivation, 4 to temporal resolve, and 2 to Wiener class-vector extension. One extra raw occurrence is in `libsystem_malloc`. The dominant 98 are required MC output work already included in K10.23. | No implementation. Do not treat linker-folded addresses as one hotspot or replay compound-MC, temporal-motion, or Wiener families. |
| SCALE-011-K10.30 | Rejected | `/root` + one read-only audit agent | Find a cohesive frame/row/scratch arena reuse that removes measured allocation and initialization. | SCALE-011-K10.29 rejection | Constructors/validation total 60 worker samples, allocator self time 43, and zero-init 59, but these cross incompatible lifetimes. Reconstruction planes are already pooled; filtered output remains live reference storage; owned row bands are required canonical scheduler storage; invariant validation is not removable; and the largest zero-fill owner is only 10. | No implementation. Keep exact-capacity vectors and terminal output-plane recycling in the small ledger; do not replay context pools, direct-row ownership, or segmented reference storage. |
| SCALE-011-K10.31 | Rejected | `/root` + one read-only audit agent | Find a wall-visible, removable pipeline completion stall rather than another compute or queue-local change. | SCALE-011-K10.30 rejection | All 477 sampled inflight-harvest observations are on the capacity-reserve path: 241 execute queued Rayon jobs and 230 park only after the pool reports no runnable job. Entropy promotion similarly has 37 assisted executions and 45 idle parks. No terminal-drain wait was sampled. | No implementation. These waits are on running or dependency-gated producers; do not replay deeper admission, caller/pool re-entry, cooperative runners, row credits, or ready-queue issue caps. |
| SCALE-011-K10.32 | Rejected | `/root` + one read-only audit agent | Find a cohesive reference-sample ownership change that removes wall-visible lock contention without delaying progressive reads. | SCALE-011-K10.31 rejection | All 75 semaphore waits use one progressive-workspace RwLock: 69 reader acquisitions and only 6 terminal writer acquisitions. Nonterminal publication cannot park, and the wait total includes concurrent readers behind one writer. Dav2d's direct final-picture rows require the segmented/disjoint ownership already rejected by K10.12. | No implementation. Keep terminal backlog-copy shortening and freeze-validation reduction in the small ledger; preserve progressive row conditions and contiguous hot-path references. |
| SCALE-011-K10.33 | Rejected | `/root` + one read-only audit agent | Classify every remaining exclusive top-of-stack entry of at least five samples and find any unclosed cohesive domain above the gate. | SCALE-011-K10.32 rejection | All 130 leaves partition exactly: 5,823 of 6,013 samples were already covered, and the remaining 190 split into general-intra kernels 51, sample validation 41, intra metadata 41, partition/frontier 30, runtime 22, and row-commit bookkeeping 5. | No implementation. No remaining cohesive domain reaches 137 worker samples; retain the exact residual partition as the small-win ledger. |
| SCALE-011-K10.34 | Rejected | `/root` + one read-only audit agent | Find a parallelizable wall-critical subset inside ordered commit rather than another self-time reduction. | SCALE-011-K10.33 rejection | The commit subtree is normative intra/deblock/inter work. Prediction-only extraction has a 24.94 worker-ms upper bound before eligibility or side-buffer cost; the current prepass already moves the safe leading run, and K10.9–K10.11 measured the remaining ownership shapes negative. | No implementation. Do not retry later-entry prediction sidecars or within-row leaf graphs without a new eligible 24 ms wall interval. |
| SCALE-011-K10.35 | Rejected | `/root` + one sequential implementation agent | Measure the exact performance ceiling from host-native compiler scheduling/code generation without source changes. | SCALE-011-K10.34 rejection | Raw output was exact. Native codegen regressed 8T by 1.071%, improved 10T by only 0.271%, and regressed 1T by 0.550%. | No implementation. Keep generic AArch64 code generation; no CI or simplification gate was warranted. |
| SCALE-011-K10.36 | Rejected | `/root` + one sequential implementation agent | Preserve installed-pool semantics while giving `N` requested compute workers a distinct caller-side driver lane. | SCALE-011-K10.35 rejection | Exact output and focused tests passed. The joined caller improved 8T only 0.369%, regressed 10T by 0.766%, and Rayon cannot durably unregister the caller for reusable contexts. | Source/tests fully reverted. Do not retry spare caller lanes or internal `N + 1` registries. |
| SCALE-011-K10.37 | Rejected | `/root` + one sequential implementation agent | Eliminate the second clamp/pack traversal after single-reference 8x8 block-warp output. | SCALE-011-K10.36 rejection; K10.23 ledger | Both implementations were exact. The dedicated kernel initially improved five-pair medians, but 11-pair 10T confirmation regressed 1.163% with 8 of 11 candidate runs slower. | Source/tests fully reverted. Do not retain a noisy second-traversal deletion or reintroduce a generic row-sink abstraction. |
| SCALE-011-K10.38 | Rejected | `/root` + one read-only audit agent | Replace deblock candidate-mask walks with compact prederived edge descriptors. | SCALE-011-K10.24 ledger | Every oriented edge is consumed once, so descriptor construction repeats rather than amortizes adjacency, transform, sub-PU, strength, and applicability work. The strict 40–90-worker-ms ceiling is only about 7–16 ms wall before construction, while a 1080p edge list adds roughly 3–6 MB per in-flight frame. | No implementation. Preserve compact candidate masks and retain only zero-storage exact-mask or tile-boundary-bit specializations as micro-ledger ideas. |
| SCALE-011-K10.39 | Rejected | `/root` + one read-only audit agent | Remove repeated active-bit-depth validation from decoder-owned sample paths. | SCALE-011-K10.33 ledger | Workspace output contributes 31 samples and Wiener-NS sources 10, but public range/fail-atomic validation cannot be removed. The whole family has only an optimistic 2.9% wall ceiling. | No implementation. Retain private clipped-MC direct writes or prevalidated LR windows only as later small-ledger options; do not weaken public boundaries or bundle the domains. |
| SCALE-011-K10.40 | Rejected | `/root` + one read-only audit agent | Centralize coefficient scan order as immutable static slices. | SCALE-011-K10.22 ledger | The complete scan-generation family is only 12 worker samples; a static design adds about 16 KiB and spans four or five production files. | No implementation. Revisit only in a later combined ledger if a fresh profile still shows the family. |
| SCALE-011-K10.41 | Rejected | `/root` + one sequential implementation agent | Skip redundant default-valued intra-context writes for ordinary non-intra leaves. | SCALE-011-K10.33 ledger | Focused tests and exact output passed, but a cleaner 11-pair 10T confirmation regressed in all pairs, with a 0.709% median loss. | Source/tests fully reverted. Do not retain the lazy-default deletion. |
| SCALE-011-K10.42 | Rejected | `/root` + one sequential implementation agent | Skip redundant range validation for proven-clipped scheduled MC output. | SCALE-011-K10.39 audit | Focused checks and exact output passed. Five-pair medians improved 1T by 1.601%, were noise at 8T, and regressed 10T by 0.243%. | Source/tests fully reverted. Do not bypass the range rescan without new multithread evidence. |
| SCALE-011-K10.43 | Rejected | `/root` + one sequential implementation agent | Replace the 29-entry inverse block-size search with exact O(1) geometry mapping. | SCALE-011-K10.33 ledger | Exhaustive mapping/partition tests and exact output passed, but 1T/8T regressed and the 10T gain was only 0.796% with scattered pairs. | Source/tests fully reverted. Do not retain the O(1) mapping. |
| SCALE-011-K10.44 | Rejected | `/root` + one read-only audit agent | Use deblock candidate masks as a zero-storage exact luma edge fast path. | SCALE-011-K10.38 audit | Masks retain superseded boundaries and do not encode default sub-PU edges; proving one safe transform-edge comparison needs a new construction invariant for an approximately one-sample ceiling. | No implementation. Preserve current mask/grid semantics. |
| SCALE-011-K10.45 | Rejected | `/root` + one read-only audit agent | Reuse odd-direction first-distance CDEF tap aliases. | SCALE-011-K10.24 ledger | The exact alias affects only both-strength odd-direction blocks and has a realistic ceiling below 18.75 worker samples. | No implementation. Retain only behind larger measured scalar candidates. |
| SCALE-011-K10.46 | Rejected | `/root` + one sequential implementation agent | Reuse duplicated neighbor loads/checks in two-row GDF kernels. | SCALE-011-K10.24 ledger | The full design was disproportionate; the compact fallback increased code size, stack frames, vector loads, and stores in all affected AArch64 kernels. | Reverted before exact-output/timing. Preserve the simple reload loops. |
| SCALE-011-K10.47 | Rejected | `/root` + one read-only audit agent | Remove redundant work from general-intra scalar kernels. | SCALE-011-K10.33 ledger | The only new deletion is CfL's constant-DC fill/read, bounded by an eight-sample leaf; other kernels are already hoisted or preserve validation. | No implementation. Retain direct CfL construction only for a future combined ledger. |
| SCALE-011-K10.48 | Rejected | `/root` + one read-only audit agent | Fuse Wiener-NS luma rows to reuse overlapping source vectors. | SCALE-011-K10.24 ledger | The current kernel is spill-free; multirow fusion's register demand would force narrower lanes or spills for a generous ceiling below 35 worker samples. | No implementation. Preserve the bulk full-grid kernel. |
| SCALE-011-K10.49 | Rejected | `/root` + one sequential implementation agent | Train and measure an exact workload-specific LLVM PGO build. | SCALE-011-K10.35 rejection; K10.48 close | Exact PGO improved 1T by 1.821% but regressed 8T by 29.794% and 10T by 33.802%, with every high-thread pair slower. | No source change. Do not use the workload profile for concurrent decode. |
| SCALE-011-K10.50 | Rejected | `/root` + one sequential implementation agent | Measure fat cross-crate LTO alone with default codegen units. | SCALE-011-K10.49 rejection | Exact fat LTO was smaller and improved 8T only 2.38%, while 10T regressed 0.19%. | No source change. Keep the default release profile. |
| SCALE-011-K10.51 | Rejected | `/root` + one read-only audit agent | Remove disabled timing/diagnostic hook overhead. | SCALE-011-K10.33 ledger | Disabled hot hooks execute only branches; concrete unconditional leaks are a few thousand relaxed loads, ~31 formats, and one counter snapshot. | No implementation. Preserve runtime timing diagnostics. |
| SCALE-011-K10.52 | Rejected | `/root` + one read-only audit agent | Add local row-band parallelism inside CDEF stripe tasks. | SCALE-011-K10.24 ledger | Ideal eight-way fanout misses the 10% gate before thousands of tiny tasks/joins and replays earlier staged banding. | No implementation. Preserve serial CDEF inside globally parallel stripes. |
| SCALE-011-K10.53 | Rejected | `/root` + one sequential implementation agent | Split only hot GDF output rows into four coarse local children. | SCALE-011-K10.24 ledger; K10.52 close | Focused parity and exact output passed, but nested fanout regressed 8T by 0.992% and 10T by 1.377%. | Source/tests fully reverted. Preserve serial GDF inside globally parallel stripes. |
| SCALE-011-K10.54 | Rejected | `/root` + one read-only audit agent | Defer deblock-record grid construction out of ordered commit. | SCALE-011-K10.34 evidence | Grid construction already happens before scheduled reconstruction; the commit bucket is actual filtering/walking. | No implementation. Record deferral deletes zero critical-path work. |
| SCALE-011-K10.55 | Rejected | `/root` + one read-only audit agent | Remove inverse-transform/residual materialization. | SCALE-011-K10.22 ledger | The full transform-block subtree is only 79 worker samples and already skips zero work/reuses scratch; the largest remaining traversal is 18. | No implementation. Retain general-intra DC/fused residual only as micro-ledger ideas. |
| SCALE-011-K10.56 | Rejected | `/root` + one sequential implementation agent | Store internal trajectory MVs as exact i16 pairs. | SCALE-011-K10.27 ledger | Exact tests/output and codegen passed, but medians improved only 0.791%/0.925% at 8T/10T with mixed pairs and 1T was neutral/slightly slower by 0.025%. | Source/tests fully reverted. Preserve the existing i32 trajectory representation. |
| SCALE-011-K10.57 | Rejected | `/root` + one read-only audit agent | Compact or split retained projected temporal-motion cells. | SCALE-011-K10.27 ledger; K10.56 storage audit | A lossless 12-byte cell saves 25% storage, but the generous ceiling is only 1.55% at 1T and 3.3% at 4T; dynamic 12-byte indexing may regress AArch64. | No implementation. Preserve the current 16-byte cell. |
| SCALE-011-K10.58 | Completed | `/root` + read-only audit and diagnostics agents | Re-rank unclosed profile families after K10.12–K10.57. | K10.56/K10.57 rejection | Completed with a settle census (0 of 954 batches settle-collapsed; hypothesis closed) and a frame-lifecycle timeline that decomposed the 7.26 ms/frame steady pace into the lane recurrence L/3 = 7.11 ms and the publication recurrence W = 7.48 ms, with 54% pool utilization and negligible scheduler cost. Full evidence in the mission ledger. | Successor SCALE-013 selected from the timeline arithmetic. |
| SCALE-013 | Completed | `/root` + one sequential implementation agent | Shorten the first reference-row publication recurrence via the exact six-mode-info-row deblock frontier lead; lane-cap lift measured and dropped. | SCALE-011-K10.58 | Committed in `d1eb9ad0e`: 10T +5.64% and 8T +5.52% over eleven alternating pairs (11/11 positive at both), 4T +1.36%, 1T/2T neutral, byte-exact, corpus oracle and `cargo xtask ci` green. First publication latency 13.1 to 6.9 ms. Full evidence in the mission ledger. | Retained under the 2026-07-31 stacking policy. Preserve the six-row lead derivation and its tests. |
| SCALE-014 | Completed | `/root` + one sequential implementation agent | Narrow the subpel MC intermediate from i32 to i16 lanes where the normative value ranges allow it, preserving the active-tap-span pruning. | SCALE-013; 2026-07-31 dav2d review | Committed in `de4d4d6df`: consistently positive at all five thread levels (paired medians -0.55% to -1.13%), byte-exact, oracle and gates green. Actual magnitude ~1% — the 1T profile shows subpel MC is only ~13% of CPU on this stream. Full evidence in the mission ledger. | Retained. Re-rank the remaining scalar queue against the fresh 1T profile before each next pick. |
| SCALE-022 | Completed | `/root` + one sequential implementation agent | Enable the scheduled split parse/reconstruction path at two and three workers. | SCALE-013 | Committed in `851bf8816`: 2T +20.07% (11/11 pairs), 3T +39.39% (5/5), other levels noise, byte-exact at 1/2/3/10T, `cargo xtask ci` green. The storage objection from SCALE-002 dissolved: the split-path peak is serial peak plus resolved-frame-delay frames at every width, already paid by main at four or more workers; the retargeted `pipelined_hash_decode_bounds_reference_storage_by_the_resolved_frame_delay` asserts that invariant at 2/3/8 workers. Full evidence in the mission ledger. | Retained. Preserve the depth-proportional storage test. |
| SCALE-015 | Open | Unassigned | Vectorize directional intra prediction (including IDIF) over x with shifted loads of the contiguous padded edge buffer. | SCALE-013 | `intra_directional_angle.rs` is fully scalar and feeds the 214 worker-ms ordered-commit bucket; the fractional offset is loop-invariant per row, and padded edges are already materialized. Estimated 3–5% at 1T. | Byte-exact; consistent positive pairs; focused intra tests; standard gates. |
| SCALE-016 | Completed | `/root` + one sequential implementation agent | Remove redundant LR staging traffic; rolling CDEF and post-LR bulk-copy elimination measured and closed. | SCALE-013 | Committed in `aaab561b3`: LR and PC-Wiener blocks now filter directly into the post-LR stripe rectangle, deleting the per-block staging fill/copy (~4.2 ms/1T run). Dataflow proof: post-LR is destination-only until GDF; GDF's dual pre-LR/post-LR sources (K10.12A) make the two stripe planes structurally required, and measured ceilings for the surviving bulk copies (CDEF copy 3.73 ms, post-LR copy 2.05 ms per 1008 ms run, before fallback machinery) close the in-place options as disproportionate. Paired medians: 1T +0.48% (8/11), 2T +0.82%, 4T +0.00%, 8T +0.27% (5/5), 10T +0.96% (7/11) — consistent positive, no regression. Byte-exact 1T/10T; full `cargo xtask ci` green. | Retained. Do not revisit rolling CDEF output or post-LR copy coverage without new evidence. |
| SCALE-017 | Open | Unassigned | Carry the parser's eob into dequant/inverse-transform to remove the zero-rescan, add a DC-only fast path, and evaluate the recursive even/odd DCT decomposition. | SCALE-013 | Splot rediscovers emptiness by scanning every dequant block (`inverse_transform_2d.rs:185`) although `scan_walk.rs:225` knows eob; AV2's DCT factors into an N/2 recursion plus a dense odd part (roughly 344 versus 1024 MACs at N=32) while ADST/DDT stay dense. Estimated 1–2% at 1T. | Byte-exact; consistent positive pairs; transform unit tests; standard gates. |
| SCALE-018 | Open | Unassigned | Vectorize MSAC symbol decode: the linear CDF scan and the two-sided `update_cdf`. | SCALE-013 | The renorm path already matches the reference shape (clz single shift, 64-bit window). The scan and adaptation are lane-parallel over at most 16 `u16` entries. Estimated 1–2% overall but it shortens the serial parse spine that feeds the lane recurrence. | Byte-exact; consistent positive pairs including 1T; parser unit tests; standard gates. |
| SCALE-019 | Open | Unassigned | Re-evaluate the frame-delay auto policy against a bounded frame-context count. | SCALE-013 | dav2d runs `min(8, ceil(sqrt(threads)))` frame contexts (four at 10T); splot's auto frame delay equals the thread count. K10.16 measured about 2% for delay four/five under the old gate. Re-measure on the stacked tree before changing the default. | Byte-exact; consistent positive pairs at 8T/10T without 2T/4T regression; standard gates. |
| SCALE-020 | Open | Unassigned | Micro batch: vectorize CCSO, remove the per-block GDF `Vec` allocation, and the per-call FSC scan-order allocation. | SCALE-013 | `filters/ccso.rs` is scalar and lane-parallel over x; `gdf.rs:545` and `scan_walk.rs:152` allocate on hot paths. Individually small; measure as one batch, retain the members that survive paired confirmation. | Byte-exact; consistent positive direction for the batch; standard gates. |
| SCALE-021 | Rejected without implementation | `/root` | Never-block lane ownership: let a frame waiting on cross-frame rows occupy zero recon lanes. | SCALE-023 depth sweep | Lane yielding is arithmetically an effective depth increase, and the SCALE-023 sweep bounds it on the stacked tree: L inflates near-proportionally with depth because the added frame's head is dependency-gated, not pool-gated (L/D 6.75 at depth 3 versus 6.62 at depth 4), depth 5 is −1.395% head-to-head, and the residual gap between L/4 and the first-publication segment is 0.89 ms/frame. | Closed. Revisit only after the first-publication segment S falls materially. |
| SCALE-023 | Completed | `/root` + one sequential diagnostics/implementation agent | Admit a fourth reconstruction frame context. | SCALE-013/014/016/022 stack | Committed in `541eb1ffc`: 10T +1.506% (9/11), 8T +0.814% (9/11), byte-exact, storage and `cargo xtask ci` green; depth 5 dropped. Full evidence in the mission ledger. | Retained. Depth re-sweep only after S falls. |
| SCALE-024 | Rejected; no commit | `/root` + one sequential implementation agent | Re-key the admission scheduler so the pace-setting first-band publication chain is not the lowest-priority work in the pool. | SCALE-023 | Prerequisite held (the order key is a pure hint; every dependency is a condition), and three byte-exact re-keys were measured: a minimal head-only class −0.72%, a full publication class −0.04% at 10T over 11 pairs (9/22 positive pooled with 8T), and a blanket newest-first inversion probe +0.885% — bounding the entire order-key lever at about ±1%. Mechanism data: S fell only 0.3–0.5 ms because scheduler-attributable spine idle is just 1.6–1.7 ms/frame; the dominant 6.2 ms `spine_prewait` is the spine waiting on its own cross-frame row-gated precompute, which no priority can remove; and `admit_ready` drains the ready heap completely at every publication, so cross-frame reordering has no material. Fully reverted. Successor: SCALE-025. | Do not retry order-key variants. |
| SCALE-025 | Open | Unassigned | De-quantise the deblock/stripe frontier below whole-superblock-row granularity so stripe 0 stops waiting for 128 committed rows when it needs 64. | SCALE-024 mechanism data | `commit0 → filt_first` is 3.79 ms because `safe_deblock_mi_end` counts only complete superblock rows: on 128-row superblocks stripe 0 (rows 0..56) waits for the full first row (commits 0..3). Advancing the § 7.17 frontier at sub-superblock-row granularity shortens the publication chain execution itself — what the SCALE-024 measurement says is needed. Complexity warning: a partial-width row cannot be deblocked full-width; this needs a careful left-prefix or per-pass sub-row design honoring the D-series invariants and the six-row read lead. Host noise is ±3.9% per pair; claim direction only at 11+ pairs. | Byte-exact 1T/10T; consistent positive at 8T/10T; no regression elsewhere; D-series invariant tests; standard gates. | SCALE-023 | `order_base = frame_index * ORDER_KEY_FRAME_STRIDE` is strictly frame-major, so frame N's final filter stripes outrank frame N+1's resolve[0]/row-0 precompute/first commit links/stripe-0 chain — the chain that now sets the pace. Measured: S = pre0-to-first-publication is 5.73 ms mean with only ~2.0 ms of execution, i.e. ~3.7 ms/frame of pure queueing in a 42%-idle pool. Ceiling: capturing a third to a half of it makes R/D binding again and lands 10T near 0.152–0.160 s. Prerequisite check: the order key must be a pure priority hint with correctness carried entirely by conditions — verify before editing. Distinct from K10.25 (which preserved the frame-major key), K10.16/K10.19 (depth/credits, pre-SCALE-013). | Byte-exact 1T/10T; consistent positive pairs at 8T/10T with no 1T/2T/4T regression; storage test; standard gates. |
| SCALE-012 | Open | Unassigned | Make deferred finish reporting happen-before in-flight harvest. | Keep separate from SCALE-011-D | `FrameSlotWriter` may settle before `FinishOutcome` publishes records or the filter error, allowing harvest to observe an empty outcome. | Cover success, failure, and exactly-once settlement; pass focused in-flight tests and CI. |
| SCALE-010 | Open | Unassigned | Reduce scalar CPU work after the scaling barriers move. | SCALE-011-I architecture measurement | The 10-thread target is at most 0.076443 seconds; even ideal scaling of the current 0.996527-second 1T result requires about 23.3% additional scalar reduction. | Reprofile the merged row architecture, rank scalar hotspots, and accept only exact wins that do not regress one thread. |

## Attachment reconciliation

The two architectural findings in the July 29 investigation remain required:

- Whole-frame synchronous entropy admission is `SCALE-011-A`. SCALE-003 rejected
  only the standalone POC, not multi-frame entropy admission in the cohesive
  row DAG.
- FIFO reconstruction backlog delaying publication is `SCALE-011-B`.
  SCALE-004 rejected only bounded scheduling around shadow surfaces, not
  dav2d-style row advancement around canonical ownership and real progress
  publication.

`SCALE-011-A` and `SCALE-011-B` were delivered together through the cohesive
SCALE-011-H graph in `#1140`. Their rejected standalone variants remain
evidence against repeating queue-only or context-depth tuning.
