# Decode Scaling Open Tasks

This is the live checklist for the work described in [`DECODE-SCALING-MISSION.md`](DECODE-SCALING-MISSION.md). The mission records measurements, rejected experiments, and architectural conclusions. This file records what remains to be done.

## Update rule

When an investigation or implementation discovers another bottleneck,
correctness requirement, or prerequisite:

1. Add it here before continuing implementation.
2. Give it a stable ID, owner, status, dependencies, evidence, and next gate.
3. Keep only one implementation task active at a time. Read-only audits may run
   in parallel.
4. Do not delete rejected or completed work. Move its evidence to the mission
   ledger and update dependent tasks here.
5. A task is complete only when its stated correctness and measurement gates
   pass. Source code existing on a branch is not completion.

## Current checklist

| ID | Status | Owner | Task | Dependencies | Evidence | Next gate |
|---|---|---|---|---|---|---|
| SCALE-011-A | Completed | `/root` | Admit entropy work from multiple frame contexts into the shared queue, guarded by exact CDF, CCSO, and reference progress. | Integrated row ownership; pending reference products | Merged in `#1140` through SCALE-011-H with byte-identical 1T/10T output and the serial-reference-storage peak regression. | Preserve the merged capability and regression coverage. |
| SCALE-011-A1 | Completed | `/root` | Reintroduce the proven three-context entropy admission design without restoring obsolete whole-frame ownership. | SCALE-011-D; SCALE-011-A | Merged in `#1140` through SCALE-011-H with exact pending-product conditions, ordered promotion, and failure settlement. | Preserve the merged three-context path. |
| SCALE-011-A2 | Rejected standalone | `/root` | Recheck dav2d-style context depth now that entropy, reconstruction rows, and filter publication coexist in one candidate. | SCALE-011-A1; SCALE-011-D | Ten contexts remained byte-exact, but seven alternating pairs regressed 8T from about 0.260s to 0.264s and 10T from about 0.261s to 0.265s versus three contexts. Extra whole-frame entropy contexts still add pressure without enough new row-progress work. | Reverted to three contexts. Do not raise entropy depth again without a post-SCALE-011-H profile showing admission starvation; the tested deeper configurations added pressure and regressed. |
| SCALE-011-A3 | Rejected standalone | `/root` | Recheck dav2d-style full frame-context depth instead of entropy depth alone. | SCALE-011-A1; SCALE-011-D/E | A depth-aware recon/filter lane plus the same number of reusable temporal-scratch slots is ownership- and failure-safe. The full frame-delay-sized candidate was byte-exact for all 30 frames at 1T/10T, but nine alternating pairs were neutral at 8T/10T. A depth-four variant, matching the plateau in both decoders' frame-delay sweeps, was also neutral. Extra live reconstruction workspaces and shadows add pressure without removing the universal motion/resolve/copy barriers. | Reverted to three recon/filter contexts, one temporal baton, and three entropy contexts. Do not raise full frame-context depth again without post-SCALE-011-H profiler evidence that additional live contexts unlock ready row work. |
| SCALE-011-B | Completed | `/root` | Replace the all-at-once reconstruction task dump with progress-driven row advancement, ordered publication, and requeuing. | SCALE-011-B3; SCALE-011-F1 | Merged in `#1140` through SCALE-011-H; SCALE-011-B1/B2 remain evidence that queue-only variants around shadow storage are not useful. | Preserve the merged row continuation; do not benchmark another queue-only variant. |
| SCALE-011-B1 | Rejected standalone | `/root` | Bound existing batch precompute with a pool-sized sliding window replenished by ordered commits. | SCALE-011-B | The exact candidate kept 1T neutral but regressed 8T from about 0.27s to 0.43s and 10T from about 0.27s to 0.43s, roughly 60%. With three in-flight frames and window `max(1, (threads - 2) / 3)`, commit-driven replenishment starved useful shadow precompute rather than prioritizing publication. | Reverted completely. Preserve the result as evidence that queue repair must change task granularity/continuation ownership, not only admission count. |
| SCALE-011-B2 | Rejected standalone | `/root` | Replace the shadow-precompute/serial-commit pair with one resumable canonical row continuation. | SCALE-011-A1; SCALE-011-D | The candidate retained batch-four `OwnedFrameRect` precompute, seeded `pool_width - 1` self-replenishing producers, and kept exactly one canonical continuation. Full 30-frame raw output remained byte-identical at 1T/10T. Seven alternating pairs were neutral: about 0.26s versus 0.26s at both 8T and 10T, with 1T about 1.00s for both. | Reverted the topology. Do not tune FIFO ordering again without removing a whole-frame dependency or the shadow-surface copy. |
| SCALE-011-B3 | Completed | `/root` | Replace temporary `OwnedFrameRect` reconstruction plus serial publish/replay with direct canonical row-band ownership. | SCALE-011-F1; exact current-frame and IntraBC liveness | Merged in `#1140` through SCALE-011-H for bounded `ReferenceOnly` rows. | Preserve conservative fallback for GeneralIntra, IntraBC, current-frame inter, unbounded inter, partial tiles, and disabled filters. Keep B5's parse-time containment gate. |
| SCALE-011-B4 | Rejected | `b3_general_intra_surface` + `/root` | Reconstruct GeneralIntra directly into the current canonical row band while reading the sealed predecessor band. | SCALE-011-B3 | The target abstraction became byte-exact after admission retained leaf-level dependency and rejected hidden current-frame inter in mixed superblocks. Performance still failed architecturally: admitting GeneralIntra made each band reconstruct all ReferenceOnly work after its first intra dependency on the serial canonical continuation. At 8T the integrated path was about 0.271 s versus 0.251 s with the B3 fallback; at 10T it was about 0.273 s versus 0.246 s. | Reverted completely. Do not retry without a horizontal wavefront or disjoint canonical sub-band design that preserves ReferenceOnly fanout after an intra dependency. |
| SCALE-011-B5 | Completed | `/root` | Prove owned-band write containment for unresolved `ReferenceOnly` leaves before frame admission. | SCALE-011-B3 | `ReconRowEntry` retains its pending-inter index and admission evaluates the exact parse-time luma, chroma, and residual write footprint through the shared `prepass_write_is_contained` check. Unresolved leaves no longer bypass containment. Timing diagnostics confirm the contained wide fixture selects `owned_bands`; the multirow fixtures conservatively select legacy storage for current-frame reads. | Preserve the unresolved-leaf containment check and conservative whole-frame fallback. |
| SCALE-011-C | Completed; IntraBC clause superseded | `scale011_raw_band_handoff_audit` | Define the earliest safe canonical raw-band handoff in the presence of current-frame reads and IntraBC. | None | If the latest canonical row ends at MI row `F`, ordinary current-frame reads permit § 7.17 deblock only through `F - sb_h4` (tightened to `F - 6` by SCALE-013); terminal commit permits `mi_rows`. `prime_vertical_pass` may not run early. **The IntraBC clause is retired by SCALE-045**: SCALE-028 gave the frontier a sealed per-frame copy, so no deblock pass writes the raw workspace and IntraBC source liveness constrains nothing. | Preserve the read lead and the sealing rule in focused frontier tests. Do not reintroduce a command-shape term in `seals_filter_copy` without evidence that some reader other than the frontier writes the sealed copy. |
| SCALE-011-D | Completed | `/root` | Move one canonical row through raw-band ownership, incremental deblock, owned filter stripes, and exact publication. | SCALE-011-C; SCALE-011-D1 through SCALE-011-D12 | The integrated owner now advances canonical reconstruction, performs only positive safe deblock frontiers, extracts owned windows, schedules filter stripes, and publishes exact progress. Focused overlap tests pass; full 30-frame 1T/10T raw output is 186,624,000 bytes with SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`. Nine alternating pairs show no 1T regression, 4.5% at 8T, and 3.6% at 10T versus exact base. | Retain D inside SCALE-011-H. The historical D-only candidate was below threshold; the cohesive H candidate clears it after removing the adjacent B/A barriers. |
| SCALE-011-D1 | Completed | `scale006_filter_handoff` | Add a committed multirow active-deblock inter oracle with no IntraBC. | None | `syn-2frame-multirow-deblock-inter-64x256-10bit-q158.ivf` has all four deblock passes active, no IntraBC, and byte-identical 98,304-byte AVM/Splot raw output. The committed scheduled-frame test compares serial and 8T hashes; frontier unit tests cover one-row lag, terminal completion, and conservative fallback. Both multirow fixtures select legacy storage for current-frame reads. | Keep both fixtures in the decoder oracle and run them after every SCALE-011-D change. |
| SCALE-011-D2 | Completed | `scale006_filter_handoff` | Make the early owned-filter sink and terminal freeze scheduler-owned. | SCALE-011-D1 | `OwnedFilterSetup` owns `Arc<FrameProgress>`, immutable filter inputs, exactly-once stripe lifecycle, and one terminal `Arc::try_unwrap` freeze. Terminal-created and early-created scheduler fanout are exact; focused missing, duplicate, out-of-order, and shared-owner tests pass. | Preserve the ownership contract while changing admission policy. Keep SCALE-012's outcome ordering repair separate. |
| SCALE-011-D3 | Completed | `/root` | Eliminate real-file nondeterminism in early filter extraction, execution, and publication. | SCALE-011-D2 | Two frontier bugs were fixed: superblock units are counted only after a complete row, and a zero safe frontier is represented as `None` so `FrameDeblock::advance(0)` cannot run its four-MI vertical lead over unreconstructed columns. Full 30-frame raw output is exact at 1T and 10T. | Retain wide-unit, zero-frontier, conservative-fallback, and active-overlap coverage. |
| SCALE-011-D4 | Rejected standalone | `scale006_filter_handoff` | Make progressive dependent-frame reference consumption byte-exact with early filter publication. | SCALE-011-D3 | Forced settled reference gates did not change the mismatch. The earlier “disabled deblock” control still called `advance(0)`, which mutates four MI rows; a genuine terminal-only control was exact. The frame-2 failure was therefore current-frame raw corruption, not progressive dependent-reference admission. | Do not widen reference gates without new evidence; continue using exact published-row conditions. |
| SCALE-011-D5 | Completed | `/root` | Preserve lossless-grid derivation when deblock records move into the incremental owner. | SCALE-011-D2 | The owned setup now derives lossless state from complete records before `detach_deblock_records` transfers the vectors to incremental deblock. The focused regression proves the lossless grid remains populated after the single-owner move. Full non-lossless real-file output remains exact. | Keep the lossless ownership regression and restore moved records exactly once at finish. |
| SCALE-011-D6 | Completed | `/root` | Make wide-frame scheduled superblock reconstruction byte-exact before filters without discarding useful intra-row fanout. | SCALE-011-D3 | The whole-row diagnostic was exact only because it masked the zero-frontier deblock mutation, and it regressed 8T/10T by about 12%. Restored superblock units batched four per task are exact once zero performs no deblock work. Focused multirow and wide-frame serial/parallel exactness fixtures pass. | Keep batch-four superblock fanout; do not restore whole-row tasking. |
| SCALE-011-D7 | Completed | `scale006_filter_handoff` | Isolate the owned-filter lifecycle component that makes scheduled superblock reconstruction diverge. | SCALE-011-D2; SCALE-011-D6 | Scheduled superblock parsing, batch sizes 1/4, detached filter records, per-commit deblock, whole-reference admission, ordered scratch resets, and lazy `FrameProgress.begin` were individually non-causal. Returning `workspace + records` to the old terminal finish is exact. Terminal-owned setup with serial filtering is exact, and terminal-owned setup with the same scheduler stripe fanout/publication is also exact. The 2x2 isolates the remaining mismatch to state created before early owned-filter execution; scheduler fanout, sink opening, and publication are non-causal. | Do not serialize superblock reconstruction or disable scheduler stripe fanout. Continue with the surviving setup-state hypotheses. |
| SCALE-011-D8 | Rejected standalone | `scale006_filter_handoff` | Make early owned-window readiness include every source sample and halo copied by `DeblockedWindow::extract`. | SCALE-011-D7 | Every `DeblockedWindow` extracted before terminal commit was re-extracted after terminal reconstruction and complete deblock, then compared plane-by-plane and sample-by-sample. All early windows were byte-identical to their terminal counterparts while frame 2 still diverged. Copied source rows, chroma rows, horizontal padding, and vertical halo are therefore not causal. | Keep the diagnostic result in the mission ledger; do not widen or serialize the ready-window frontier without new evidence. |
| SCALE-011-D9 | Rejected standalone | `/root` | Separate concurrent early filter execution/publication from early immutable setup and window capture. | SCALE-011-D7; SCALE-011-D8 | Setup creation, incremental deblock, and early window capture were left unchanged, but every captured filter job was withheld until terminal reconstruction. Frame 2 retained the same divergent hash `30abcb68827921f85c90c03b0b0f6d703f34be41e39a5400c4fb9349da8dff4e`, so overlap between filter execution/publication and reconstruction is not causal. | Compare the early-detached per-row setup records with the terminal-replayed records, beginning with `tx_skip_records`; do not serialize filter jobs. |
| SCALE-011-D10 | Rejected standalone | `/root` | Prove parity between early-detached and terminal-replayed immutable filter records. | SCALE-011-D9 | Early-detached and independently terminal-replayed `tx_skip_records` matched element-for-element on all six admitted frames in the divergent three-output chain, with counts 510, 1,667, 1,229, 705, 510, and 617. Frame 2 retained the same divergent hash. CDEF, CCSO, GDF, and LR products are the same entropy-pass owners in both paths. | Do not change immutable record derivation without new evidence. Recheck whether incremental deblock mutates raw pixels still needed by later canonical reconstruction. |
| SCALE-011-D11 | Completed | `/root` | Make a zero safe frontier perform no deblock work. | SCALE-011-D3; SCALE-011-D8; SCALE-011-D10 | A genuine terminal-only deblock control restored frame 2 to the expected hash `f6343e5dac10026444f2d58a4d970e87d6415bff78e3cb90d7c57f18127583a4`. `safe_deblock_mi_end` now returns `Option<NonZeroUsize>`, so a zero frontier cannot call `FrameDeblock::advance(0)` and trigger its four-MI vertical lead. | Preserve the nonzero type boundary and exact full-file gate. |
| SCALE-011-D12 | Completed | `/root` | Restore focused scheduled-frame exactness after rejecting whole-row tasking. | SCALE-011-D6 | Focused multirow, active-deblock, and wide-frame fixtures prove serial/parallel output equality. Process-global scheduler telemetry assertions were removed because concurrent tests made them racy; batch-four remains the accepted legacy implementation. | Keep exactness coverage aligned with the accepted batch-four design; do not restore process-global telemetry assertions. |
| SCALE-011-E | Completed | `scale005_motion_bands` | Publish immutable reference bands and exact reconstruction/filter watermarks to dependent frames. | SCALE-011-D | Merged in `#1140` through SCALE-011-H with monotonic stripe progress and fail-closed reference reads. | Do not add a second reference-band abstraction; retain the exact sample-watermark path. |
| SCALE-011-F | Completed | `/root` | Replace whole-frame motion-field/temporal resolve with an ordered row/band producer-consumer baton. | SCALE-011-A; SCALE-011-E; SCALE-011-F1 | Merged in `#1140` through SCALE-011-H with band settlement and terminal whole-field compatibility. | Preserve the merged baton and failure settlement. |
| SCALE-011-F1 | Completed | `/root` | Publish immutable source motion bands and consume them through one ordered temporal-projection/resolve continuation. | SCALE-011-E | Merged in `#1140` through SCALE-011-H with separate metadata publication and ordered dependent-row resolution. | Preserve failure settlement and terminal-token coverage. |
| SCALE-011-G | Rejected; no source change | `/root` + one sequential investigation agent | Move hot row, coefficient, motion, and filter scratch into reusable frame/task contexts. | Stable integrated task boundaries; SCALE-056 | **Closed by SCALE-057.** An env-gated per-pool census identifies SCALE-056's 87.1 MB of pooled scratch exactly (86.04 MB retained across the eight process-global pools at ten workers), and an env-gated cap scale over all eight prices the whole family: retaining nothing returns only 38.5 MB of the 532.3 MB peak resident set at ten workers, costs +0.728% marginal steady CPU there, and regresses the 30-frame wall at every width. Full evidence in the mission ledger. | Closed. Process-global recycling is not the footprint, and the footprint is not the inflation driver; do not re-open pool ownership, cap right-sizing, or per-pool free lists for this gate. |
| SCALE-011-H | Completed | `/root` | Complete one cohesive dav2d-style vertical row graph: publish a source motion band, resolve the dependent row, reconstruct directly into canonical row-band ownership, publish deblock/filter/reference progress, and admit the next frame's row. | SCALE-011-A; SCALE-011-B3; SCALE-011-D/E; SCALE-011-F1 | Merged in `#1140`. Exact raw output matches base at 1T/10T. The final post-CI matrix measured +0.04% at 1T, +12.91% at 8T, and +13.67% at 10T versus exact base. | Preserve the merged graph while later candidates close the remaining dav2d gap. |
| SCALE-011-I | Paused by user | `/root` | Continue from merged `#1140` to the final dav2d target. | SCALE-011-H | No implementation leaf is active. Exact main is about 0.240 seconds at 10T versus dav2d 0.155 seconds; the 50%-faster target is at most about 0.077 seconds. K10.12–K10.57 are recorded; K10.58 stopped before selecting a successor. | Resume by re-ranking the existing exact profile and advance only an evidence-backed distinct candidate. |
| SCALE-011-K10.7 | Rejected | `/root` + subagents | Persistent row cursors, horizontal leaves, and row-scoped reference leases. | SCALE-011-H | Rejected; measurements are retained in the mission ledger. | Do not carry segmented reference storage or permanent halos forward. |
| SCALE-011-K10.8 | Rejected | `/root` + subagents | Vertical readiness graph over the packed/contiguous path. | SCALE-011-K10.7 | Rejected; measurements are retained in the mission ledger. | Do not retry whole-frame claims or lock-order/context-depth patches. |
| SCALE-011-K10.9 | Rejected | `/root` + subagents | Hybrid row bands for the remaining current-dependent frames. | SCALE-011-K10.8 | Rejected; measurements are retained in the mission ledger. | Do not retry segmented whole-frame kernels. |
| SCALE-011-K10.10 | Rejected | `/root` + subagents | Leaf-DAG row continuations with contiguous band interiors. | SCALE-011-K10.9 | Rejected; measurements are retained in the mission ledger. | Do not retry leaf/SB granularity when observable progress remains row-wide. |
| SCALE-011-K10.11 | Rejected | `/root` + subagents | Scoped zero-copy horizontal row wavefront. | SCALE-011-K10.10 | Rejected; measurements are retained in the mission ledger. | Reconstruction ownership variants are evidence-closed under safe Rust plus one contiguous mutable workspace. |
| SCALE-011-K10.12 | Rejected | `/root` + subagents | Build one fused owned `PostFilterGraph` that reduces actual filter work and removes progressive publication lock inversion. | SCALE-011-K10.11 | The integrated 10-bit path was byte-exact at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and passed the decoder all-target gate before timing. Five-pair 10T screens rejected every shape: 256-pixel slabs were +51.29% versus exact base, 512-pixel slabs +48.42%, slab expansion disabled +45.37%, and full-width publication with expansion disabled +46.16%. A safe four-segment reference view restored cross-stripe reads and remained exact, but the integrated 512-pixel graph regressed +54.94% (0.377700 s versus 0.246013 s); TIP motion-grid attribution rose from about 88 ms on base to 301 ms. | Do not retry immutable segmented reference storage or globally scheduled x-slabs. Preserve the merged contiguous progressive workspace and full-width filter-stripe task until a design removes kernel work without changing the hot reference representation. |
| SCALE-011-K10.12A | Rejected | `/root` + subagents | Fuse CDEF, CCSO, LR, and GDF into reusable typed stripe arenas without whole-stripe LR cloning or a full padded GDF source. | SCALE-011-K10.12 | Exact typed LR/GDF slab APIs removed redundant full-stripe outputs locally, but the cohesive graph did not reduce aggregate filter time enough to repay scheduling/publication costs. | Retain only the source-lifetime finding: GDF needs immutable pre-LR CDEF plus post-LR base. Do not publish the experimental APIs. |
| SCALE-011-K10.12B | Rejected | `/root` + subagents | Split each filter stripe into globally visible cache-sized x-slab nodes as part of the arena graph. | SCALE-011-K10.12A | Independent LR/GDF leaves, nonblocking completion joins, exact halos, tails, tile boundaries, and child-failure settlement were correct. Halving width granularity from 256 to 512 recovered only about three percentage points; disabling expansion still left a 45% regression, proving task count was not the primary loss. | Keep full-width filter stripes. Do not retry 128/256/512 x-slab scheduling. |
| SCALE-011-K10.12C | Rejected | `/root` + subagents | Replace whole-frame output writer locking/backlog with fixed immutable stripe slots and terminal one-copy assembly. | SCALE-011-K10.12B | Fixed `OnceLock` stripe ownership and terminal assembly were exact. Reads crossing immutable slots either waited for settled contiguous storage (+45% with the filter graph disabled) or used a safe four-segment reference view that forced hot kernels off their contiguous fast representation (+54.94% integrated). | Preserve the current contiguous progressive workspace. Address any demonstrated writer contention without changing reference-plane storage. |
| SCALE-011-K10.13 | Rejected | `/root` + sequential implementation agents | Reuse the existing typed scheduled-reconstruction context across frames, install its MC/TIP buffers once per admitted row, and localize coefficient-quant storage to the row/task lifetime. | SCALE-011-G; SCALE-011-K10.12 rejection | The full two-tier context was byte-exact at 1T/10T. Five alternating pairs improved 8T by 1.91% (0.237016 s versus 0.243152 s) and 10T by 2.50% (0.236977 s versus 0.243055 s), far below the 10% gate; dav2d measured 0.160695/0.155624 s. | Fully revert the runtime candidate. Preserve the conclusion that allocator/recycler and TLS-install overhead are not the remaining barrier; do not pursue standalone context-pool, quant-pool, residual-pool, or filter-pool edits. |
| SCALE-011-K10.14 | Rejected | `/root` + sequential implementation agents | Replace the dominant full-map multi-class Wiener-NS luma classifier-plus-filter traversal with one scalar rolling class-row/filter pass. | SCALE-011-K10.13 rejection | The candidate was byte-exact and passed focused u8/u16 composition parity, but five-pair medians regressed 8T by 3.07% and 10T by 1.66%. Rolling removed classification time (56.160 ms to 7.328 ms) yet raised Wiener-NS luma from 74.037 ms to 190.727 ms and LR from 178.704 ms to 241.881 ms. | Fully revert. Preserve the full-grid bulk traversal; do not retry scalar rolling classification without a measured faster scalar feature/filter kernel. |
| SCALE-011-K10.15 | Rejected | `/root` + sequential implementation agents | Fuse TIP motion refinement, final prediction, temporal-record emission, and output publication into one owned command/traversal. | SCALE-011-K10.14 rejection | The failure-atomic carried-plan/grid/u16-luma candidate was byte-exact and passed focused parity. Five-pair medians regressed 8T by 1.59% and 10T by 0.39%. Motion-grid attribution rose from 79.207 ms to 120.650 ms while prediction fell from 118.241 ms to 84.094 ms, leaving the integrated path neutral-to-worse. | Fully revert. Preserve the current bulk plane traversal and split motion/prediction boundary; do not retry per-cell final TIP MC, direct TIP publication, or owned luma sidecars without a measured faster scalar final-MC kernel. |
| SCALE-011-K10.16 | Rejected | `/root` | Remove the measured occupancy gap through driver placement, context depth, or cooperative admission execution without changing exact codec work. | SCALE-011-K10.15 rejection | Timeline tracing found the three-context reconstruction gate saturated for 155.9 ms but only 11.64 ms of strict single-job execution. Depth ten regressed 1.73%; frame-delay four/five moved only about 2%. An exact external-caller driver regressed 149% and remained about 160% slower after wait donation because off-pool execution lost current nested semantics. Exact cooperative global runners regressed 7.48% in the three-pair 10T screen. | Runtime edits are fully reverted. Do not retry context-depth, external-caller, or cooperative-runner scheduling in isolation. |
| SCALE-011-K10.17 | Rejected | `/root` | Give admission/driver work a distinct lane while preserving installed-pool semantics and exactly the requested compute-worker budget. | SCALE-011-K10.16 rejection | Explicit pool re-entry around every direct whole-frame walk recovered the caller-side driver from about 0.62 s to 0.29 s and kept raw output exact, but the three-pair 10T screen still regressed 18.46%. Whole-wait pool re-entry preserved work-first assistance and still regressed 18.34%. | Runtime edits are fully reverted. Do not retry caller separation under the current mixed driver/direct-work API. |
| SCALE-011-K10.18 | Rejected | `/root` + one sequential implementation agent | Decouple independent temporal-band projection from the frame-depth and ordered neighbor-grid resolve batons. | SCALE-011-K10.17 rejection | The exact after-gate split improved 10T only 1.46%. Moving preparation/projection before the N−3 gate plus removing ordered scratch recycling remained exact but regressed 2.28%. | Runtime edits are fully reverted. Preserve the evidence that projection eligibility alone does not move the wall-time critical path. |
| SCALE-011-K10.19 | Rejected | `/root` + one sequential implementation agent | Replace the whole-frame depth-three reconstruction gate with bounded cross-frame row-progress credits. | SCALE-011-K10.18 rejection | The exact complete/touched-row watermark candidate preserved all semantic gates and was byte-identical at 1T/10T, but the three-pair 10T screen regressed 0.59% (0.240336 s versus 0.239840 s); dav2d was 0.154996 s. | Runtime is fully reverted. Do not retry frame-depth gates, early projection/preparation, or cross-frame row credits. |
| SCALE-011-K10.20 | Rejected | `/root` + read-only audit agents | Re-synthesize a materially distinct candidate from scheduler transition cost, working-set pressure, and the scalar small-win ledger. | SCALE-011-K10.19 rejection | Immediately runnable commit/finish transition gaps are below 15 ms aggregate, cache-only local changes cannot recover the 24 ms threshold, and the remaining unclosed scalar ledger reaches 10% only at the optimistic edge before overlap. | No implementation. Do not replay local fallthrough, persistent runners, allocation slabs, completion coalescing, or a blind cross-domain scalar bundle. |
| SCALE-011-K10.21 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction in entropy-symbol arithmetic and CDF dispatch. | SCALE-011-K10.20 rejection | All symbol decode/update/selection/literal primitives total only about 43 worker-ms; binary specialization cannot credibly save the required 24 ms and cross-frame overlap lowers its wall ceiling further. | No implementation. Retain trusted validation; ledger fixed-arity binary decoding as a later small scalar candidate. |
| SCALE-011-K10.22 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction across coefficient parse, staged transform state, dequant/inverse transform, and residual addition. | SCALE-011-K10.21 rejection | The large coefficient stacks are inclusive. Dense coefficient-state construction is the only large exclusive materialization, but K10.13 already removed its pool ownership/search and improved 10T by only 2.50% (about 6 ms). Dequantization and scan generation account for only 15 and 12 worker-ms respectively, before overlap. | No implementation. Keep scan-table, DC-only inverse-transform, and sparse-dequant ideas in the small scalar ledger; do not add a cross-crate sparse coefficient representation for this gate. |
| SCALE-011-K10.23 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction across inter prediction, warp/subpel MC, compound output, and their intermediate materialization. | SCALE-011-K10.22 rejection | The roughly 630 worker-ms MC family is overwhelmingly required convolution and copy arithmetic. Warp's 114-sample prepared pass and 57-sample output pass are its normative horizontal and vertical filters. Translational compound already fuses the second predictor with uniform blending and specializes full-pixel, horizontal, and small two-axis cases. At the observed 5.7-core occupancy, a 24 ms wall saving needs about 137 avoidable worker-ms, which the remaining intermediates do not contain. | No implementation. Retain direct single-reference warp output and wider fused two-axis compound coverage as small-ledger ideas; do not replay per-cell TIP fusion or replace exact convolution with an approximation. |
| SCALE-011-K10.24 | Rejected | `/root` + one read-only audit agent | Find a cohesive safe scalar reduction in actual CDEF, Wiener-NS/LR, GDF, or deblock kernel work. | SCALE-011-K10.23 rejection | CDEF contributes about 380 worker samples, LR about 280, GDF about 142, and deblock about 190, but nearly all of those buckets are normative tap/class/filter work. Existing code already shares CDEF luma gather data, pairs chroma, caches PC features, rolls GDF gradients, and writes deblock directly. No removable subset reaches the roughly 137 worker-ms gate. | No implementation. Keep compact prederived deblock edge descriptors as a 40–90 worker-ms small-ledger item; do not replay filter graphs, rolling Wiener, or approximations. |
| SCALE-011-K10.25 | Rejected | `/root` + one read-only audit agent + one sequential implementation agent | Preserve ready-heap priority across Rayon's queue by bounding scheduler-issued top-level jobs. | SCALE-011-K10.24 rejection | Telemetry found real inversions, and the exact POC retained excess work in the ordered heap under a pool-width issue cap. Its three-pair 10T screen improved only 1.91% median (0.240802 s versus 0.245491 s), with one pair regressing; dav2d was 0.157779 s. | Fully revert runtime and telemetry code. Queue-age union was mostly hidden latency; do not retry issue caps, urgent overflow, or admission priority without a different measured critical path. |
| SCALE-011-K10.26 | Rejected | `/root` + one read-only audit agent | Attribute the 198-worker-ms global memory-copy bucket and find a cohesive copy elimination that preserves contiguous storage and task fanout. | SCALE-011-K10.25 rejection | Exhaustive exclusive attribution splits the bucket into CDEF/filter windows 56, LR/GDF materialization/publication 36, canonical owned-rectangle commit 34, MC/prediction writes 23, coefficient/residual movement 16, frame allocation/output 4, and other metadata growth 29. The largest family is 56; even all filter copies are only 92 before required work. | No implementation. Preserve contiguous publication and deblock/filter overlap. Keep rolling CDEF, direct LR stripe output, exact-capacity metadata, and coefficient-container movement as small-ledger ideas; do not replay direct-row ownership or segmented filter storage. |
| SCALE-011-K10.27 | Rejected | `/root` + one read-only audit agent | Find a cohesive exact scalar reduction across motion-metadata construction and updates. | SCALE-011-K10.26 rejection | Temporal projection including trajectory children is 209 worker-ms, trajectory-only work 147, all neighbour-grid construction/publication 129, and motion-field folding 88 including 53 lock-wait samples. Normative projection/intersection/cell writes dominate. Dav2d's compact maps and SB-row ring do not remove Splot's two-phase readiness, warp/TIP cell values, or flag replay; the neighbour-grid hard ceiling is only 22.6 ms at 5.7 cores. | No implementation. Keep compact trajectory/owner maps, bounded neighbour rows, and exclusive motion-band segments in the small ledger; do not replay K10.18 ordering or bundle independent motion structures. |
| SCALE-011-K10.28 | Rejected | `/root` + one read-only audit agent | Attribute blocking mutex waits and find one cohesive ownership change that removes a wall-visible lock family. | SCALE-011-K10.27 rejection | Of 178 mutex-wait samples, 18 are Rayon sleep/wake and project sites total 160. The largest family is coefficient quant at 74, already calibrated by K10.13 to about 6 ms wall; motion folding is 53, admission 13 (7 inside Rayon wake), filter scratch 11, residual pools 4, frame precompute 4, and tile conditions 1. No family reaches the gate. | No implementation. Do not combine unrelated locks or replay coefficient contexts, motion segmentation, ready-queue issue caps, or standalone scratch pools. |
| SCALE-011-K10.29 | Rejected | `/root` + one read-only audit agent | Source-attribute the profile's 110-sample `<deduplicated_symbol>` bucket. | SCALE-011-K10.28 rejection | Exclusive call-tree reconstruction assigns 98 samples to compound-subpel intermediate output, 6 to deferred motion derivation, 4 to temporal resolve, and 2 to Wiener class-vector extension. One extra raw occurrence is in `libsystem_malloc`. The dominant 98 are required MC output work already included in K10.23. | No implementation. Do not treat linker-folded addresses as one hotspot or replay compound-MC, temporal-motion, or Wiener families. |
| SCALE-011-K10.30 | Rejected | `/root` + one read-only audit agent | Find a cohesive frame/row/scratch arena reuse that removes measured allocation and initialization. | SCALE-011-K10.29 rejection | Constructors/validation total 60 worker samples, allocator self time 43, and zero-init 59, but these cross incompatible lifetimes. Reconstruction planes are already pooled; filtered output remains live reference storage; owned row bands are required canonical scheduler storage; invariant validation is not removable; and the largest zero-fill owner is only 10. | No implementation. Keep exact-capacity vectors and terminal output-plane recycling in the small ledger; do not replay context pools, direct-row ownership, or segmented reference storage. |
| SCALE-011-K10.31 | Rejected | `/root` + one read-only audit agent | Find a wall-visible, removable pipeline completion stall rather than another compute or queue-local change. | SCALE-011-K10.30 rejection | All 477 sampled inflight-harvest observations are on the capacity-reserve path: 241 execute queued Rayon jobs and 230 park only after the pool reports no runnable job. Entropy promotion similarly has 37 assisted executions and 45 idle parks. No terminal-drain wait was sampled. | No implementation. These waits are on running or dependency-gated producers; do not replay deeper admission, caller/pool re-entry, cooperative runners, row credits, or ready-queue issue caps. |
| SCALE-011-K10.32 | Rejected | `/root` + one read-only audit agent | Find a cohesive reference-sample ownership change that removes wall-visible lock contention without delaying progressive reads. | SCALE-011-K10.31 rejection | All 75 semaphore waits use one progressive-workspace RwLock: 69 reader acquisitions and only 6 terminal writer acquisitions. Nonterminal publication cannot park, and the wait total includes concurrent readers behind one writer. Dav2d's direct final-picture rows require the segmented/disjoint ownership already rejected by K10.12. | No implementation. Keep terminal backlog-copy shortening and freeze-validation reduction in the small ledger; preserve progressive row conditions and contiguous hot-path references. |
| SCALE-011-K10.33 | Rejected | `/root` + one read-only audit agent | Classify every remaining exclusive top-of-stack entry of at least five samples and find any unclosed cohesive domain above the gate. | SCALE-011-K10.32 rejection | All 130 leaves partition exactly: 5,823 of 6,013 samples were already covered, and the remaining 190 split into general-intra kernels 51, sample validation 41, intra metadata 41, partition/frontier 30, runtime 22, and row-commit bookkeeping 5. | No implementation. No remaining cohesive domain reaches 137 worker samples; retain the exact residual partition as the small-win ledger. |
| SCALE-011-K10.34 | Rejected | `/root` + one read-only audit agent | Find a parallelizable wall-critical subset inside ordered commit rather than another self-time reduction. | SCALE-011-K10.33 rejection | The commit subtree is normative intra/deblock/inter work. Prediction-only extraction has a 24.94 worker-ms upper bound before eligibility or side-buffer cost; the current prepass already moves the safe leading run, and K10.9–K10.11 measured the remaining ownership shapes negative. | No implementation. Do not retry later-entry prediction sidecars or within-row leaf graphs without a new eligible 24 ms wall interval. |
| SCALE-011-K10.35 | Rejected | `/root` + one sequential implementation agent | Measure the exact performance ceiling from host-native compiler scheduling/code generation without source changes. | SCALE-011-K10.34 rejection | Raw output was exact. Native codegen regressed 8T by 1.071%, improved 10T by only 0.271%, and regressed 1T by 0.550%. | No implementation. Keep generic AArch64 code generation; no CI or simplification gate was warranted. |
| SCALE-011-K10.36 | Rejected | `/root` + one sequential implementation agent | Preserve installed-pool semantics while giving `N` requested compute workers a distinct caller-side driver lane. | SCALE-011-K10.35 rejection | Exact output and focused tests passed. The joined caller improved 8T only 0.369%, regressed 10T by 0.766%, and Rayon cannot durably unregister the caller for reusable contexts. | Source/tests fully reverted. Do not retry spare caller lanes or internal `N + 1` registries. |
| SCALE-011-K10.37 | Rejected | `/root` + one sequential implementation agent | Eliminate the second clamp/pack traversal after single-reference 8x8 block-warp output. | SCALE-011-K10.36 rejection; K10.23 ledger | Both implementations were exact. The dedicated kernel initially improved five-pair medians, but 11-pair 10T confirmation regressed 1.163% with 8 of 11 candidate runs slower. | Source/tests fully reverted. Do not retain a noisy second-traversal deletion or reintroduce a generic row-sink abstraction. |
| SCALE-011-K10.38 | Rejected | `/root` + one read-only audit agent | Replace deblock candidate-mask walks with compact prederived edge descriptors. | SCALE-011-K10.24 ledger | Every oriented edge is consumed once, so descriptor construction repeats rather than amortizes adjacency, transform, sub-PU, strength, and applicability work. The strict 40–90-worker-ms ceiling is only about 7–16 ms wall before construction, while a 1080p edge list adds roughly 3–6 MB per in-flight frame. | No implementation. Preserve compact candidate masks and retain only zero-storage exact-mask or tile-boundary-bit specializations as micro-ledger ideas. |
| SCALE-011-K10.39 | Rejected | `/root` + one read-only audit agent | Remove repeated active-bit-depth validation from decoder-owned sample paths. | SCALE-011-K10.33 ledger | Workspace output contributes 31 samples and Wiener-NS sources 10, but public range/fail-atomic validation cannot be removed. The whole family has only an optimistic 2.9% wall ceiling. | No implementation. Retain private clipped-MC direct writes or prevalidated LR windows only as later small-ledger options; do not weaken public boundaries or bundle the domains. |
| SCALE-011-K10.40 | Rejected | `/root` + one read-only audit agent | Centralize coefficient scan order as immutable static slices. | SCALE-011-K10.22 ledger | The complete scan-generation family is only 12 worker samples; a static design adds about 16 KiB and spans four or five production files. | No implementation. Revisit only in a later combined ledger if a fresh profile still shows the family. |
| SCALE-011-K10.41 | Rejected | `/root` + one sequential implementation agent | Skip redundant default-valued intra-context writes for ordinary non-intra leaves. | SCALE-011-K10.33 ledger | Focused tests and exact output passed, but a cleaner 11-pair 10T confirmation regressed in all pairs, with a 0.709% median loss. | Source/tests fully reverted. Do not retain the lazy-default deletion. |
| SCALE-011-K10.42 | Rejected | `/root` + one sequential implementation agent | Skip redundant range validation for proven-clipped scheduled MC output. | SCALE-011-K10.39 audit | Focused checks and exact output passed. Five-pair medians improved 1T by 1.601%, were noise at 8T, and regressed 10T by 0.243%. | Source/tests fully reverted. Do not bypass the range rescan without new multithread evidence. |
| SCALE-011-K10.43 | Rejected | `/root` + one sequential implementation agent | Replace the 29-entry inverse block-size search with exact O(1) geometry mapping. | SCALE-011-K10.33 ledger | Exhaustive mapping/partition tests and exact output passed, but 1T/8T regressed and the 10T gain was only 0.796% with scattered pairs. | Source/tests fully reverted. Do not retain the O(1) mapping. |
| SCALE-011-K10.44 | Rejected | `/root` + one read-only audit agent | Use deblock candidate masks as a zero-storage exact luma edge fast path. | SCALE-011-K10.38 audit | Masks retain superseded boundaries and do not encode default sub-PU edges; proving one safe transform-edge comparison needs a new construction invariant for an approximately one-sample ceiling. | No implementation. Preserve current mask/grid semantics. |
| SCALE-011-K10.45 | Rejected | `/root` + one read-only audit agent | Reuse odd-direction first-distance CDEF tap aliases. | SCALE-011-K10.24 ledger | The exact alias affects only both-strength odd-direction blocks and has a realistic ceiling below 18.75 worker samples. | No implementation. Retain only behind larger measured scalar candidates. |
| SCALE-011-K10.46 | Rejected | `/root` + one sequential implementation agent | Reuse duplicated neighbor loads/checks in two-row GDF kernels. | SCALE-011-K10.24 ledger | The full design was disproportionate; the compact fallback increased code size, stack frames, vector loads, and stores in all affected AArch64 kernels. | Reverted before exact-output/timing. Preserve the simple reload loops. |
| SCALE-011-K10.47 | Rejected | `/root` + one read-only audit agent | Remove redundant work from general-intra scalar kernels. | SCALE-011-K10.33 ledger | The only new deletion is CfL's constant-DC fill/read, bounded by an eight-sample leaf; other kernels are already hoisted or preserve validation. | No implementation. Retain direct CfL construction only for a future combined ledger. |
| SCALE-011-K10.48 | Rejected | `/root` + one read-only audit agent | Fuse Wiener-NS luma rows to reuse overlapping source vectors. | SCALE-011-K10.24 ledger | The current kernel is spill-free; multirow fusion's register demand would force narrower lanes or spills for a generous ceiling below 35 worker samples. | No implementation. Preserve the bulk full-grid kernel. |
| SCALE-011-K10.49 | Rejected | `/root` + one sequential implementation agent | Train and measure an exact workload-specific LLVM PGO build. | SCALE-011-K10.35 rejection; K10.48 close | Exact PGO improved 1T by 1.821% but regressed 8T by 29.794% and 10T by 33.802%, with every high-thread pair slower. | No source change. Do not use the workload profile for concurrent decode. |
| SCALE-011-K10.50 | Rejected | `/root` + one sequential implementation agent | Measure fat cross-crate LTO alone with default codegen units. | SCALE-011-K10.49 rejection | Exact fat LTO was smaller and improved 8T only 2.38%, while 10T regressed 0.19%. | **Superseded by SCALE-066**, which re-priced this row under the 2026-07-31 stacking policy and retained fat LTO with `codegen-units = 1`. The rejection was an artifact of the old 10% gate: +1.00% at 1T and +2.38% at 8T clear today's bar several times over and the -0.19% at 10T is inside the modern null band. |
| SCALE-011-K10.51 | Rejected | `/root` + one read-only audit agent | Remove disabled timing/diagnostic hook overhead. | SCALE-011-K10.33 ledger | Disabled hot hooks execute only branches; concrete unconditional leaks are a few thousand relaxed loads, ~31 formats, and one counter snapshot. | No implementation. Preserve runtime timing diagnostics. |
| SCALE-011-K10.52 | Rejected | `/root` + one read-only audit agent | Add local row-band parallelism inside CDEF stripe tasks. | SCALE-011-K10.24 ledger | Ideal eight-way fanout misses the 10% gate before thousands of tiny tasks/joins and replays earlier staged banding. | No implementation. Preserve serial CDEF inside globally parallel stripes. |
| SCALE-011-K10.53 | Rejected | `/root` + one sequential implementation agent | Split only hot GDF output rows into four coarse local children. | SCALE-011-K10.24 ledger; K10.52 close | Focused parity and exact output passed, but nested fanout regressed 8T by 0.992% and 10T by 1.377%. | Source/tests fully reverted. Preserve serial GDF inside globally parallel stripes. |
| SCALE-011-K10.54 | Rejected | `/root` + one read-only audit agent | Defer deblock-record grid construction out of ordered commit. | SCALE-011-K10.34 evidence | Grid construction already happens before scheduled reconstruction; the commit bucket is actual filtering/walking. | No implementation. Record deferral deletes zero critical-path work. |
| SCALE-011-K10.55 | Rejected | `/root` + one read-only audit agent | Remove inverse-transform/residual materialization. | SCALE-011-K10.22 ledger | The full transform-block subtree is only 79 worker samples and already skips zero work/reuses scratch; the largest remaining traversal is 18. | No implementation. Retain general-intra DC/fused residual only as micro-ledger ideas. |
| SCALE-011-K10.56 | Rejected | `/root` + one sequential implementation agent | Store internal trajectory MVs as exact i16 pairs. | SCALE-011-K10.27 ledger | Exact tests/output and codegen passed, but medians improved only 0.791%/0.925% at 8T/10T with mixed pairs and 1T was neutral/slightly slower by 0.025%. | Source/tests fully reverted. Preserve the existing i32 trajectory representation. |
| SCALE-011-K10.57 | Rejected | `/root` + one read-only audit agent | Compact or split retained projected temporal-motion cells. | SCALE-011-K10.27 ledger; K10.56 storage audit | A lossless 12-byte cell saves 25% storage, but the generous ceiling is only 1.55% at 1T and 3.3% at 4T; dynamic 12-byte indexing may regress AArch64. | No implementation. Preserve the current 16-byte cell. |
| SCALE-011-K10.58 | Completed | `/root` + read-only audit and diagnostics agents | Re-rank unclosed profile families after K10.12–K10.57. | K10.56/K10.57 rejection | Completed with a settle census (0 of 954 batches settle-collapsed; hypothesis closed) and a frame-lifecycle timeline that decomposed the 7.26 ms/frame steady pace into the lane recurrence L/3 = 7.11 ms and the publication recurrence W = 7.48 ms, with 54% pool utilization and negligible scheduler cost. Full evidence in the mission ledger. | Successor SCALE-013 selected from the timeline arithmetic. |
| SCALE-013 | Completed | `/root` + one sequential implementation agent | Shorten the first reference-row publication recurrence via the exact six-mode-info-row deblock frontier lead; lane-cap lift measured and dropped. | SCALE-011-K10.58 | Committed in `d1eb9ad0e`: 10T +5.64% and 8T +5.52% over eleven alternating pairs (11/11 positive at both), 4T +1.36%, 1T/2T neutral, byte-exact, corpus oracle and `cargo xtask ci` green. First publication latency 13.1 to 6.9 ms. Full evidence in the mission ledger. | Retained under the 2026-07-31 stacking policy. Preserve the six-row lead derivation and its tests. |
| SCALE-014 | Completed | `/root` + one sequential implementation agent | Narrow the subpel MC intermediate from i32 to i16 lanes where the normative value ranges allow it, preserving the active-tap-span pruning. | SCALE-013; 2026-07-31 dav2d review | Committed in `de4d4d6df`: consistently positive at all five thread levels (paired medians -0.55% to -1.13%), byte-exact, oracle and gates green. Actual magnitude ~1% — the 1T profile shows subpel MC is only ~13% of CPU on this stream. Full evidence in the mission ledger. | Retained. Re-rank the remaining scalar queue against the fresh 1T profile before each next pick. |
| SCALE-022 | Completed | `/root` + one sequential implementation agent | Enable the scheduled split parse/reconstruction path at two and three workers. | SCALE-013 | Committed in `851bf8816`: 2T +20.07% (11/11 pairs), 3T +39.39% (5/5), other levels noise, byte-exact at 1/2/3/10T, `cargo xtask ci` green. The storage objection from SCALE-002 dissolved: the split-path peak is serial peak plus resolved-frame-delay frames at every width, already paid by main at four or more workers; the retargeted `pipelined_hash_decode_bounds_reference_storage_by_the_resolved_frame_delay` asserts that invariant at 2/3/8 workers. Full evidence in the mission ledger. | Retained. Preserve the depth-proportional storage test. |
| SCALE-015 | Completed | `/root` + one sequential implementation agent | Vectorize directional intra prediction (including IDIF) over the contiguous padded edge buffer; zone-3 taken, zone-2 and bilinear measured and left scalar. | SCALE-013 | The 3–5% estimate was wrong by 4x. A shadow-pass ablation bounds the **whole** `intra_directional_angle.rs` family at **0.84% of the 1T wall**, split zone-3 one-sided IDIF 0.45%, zone-2 middle IDIF (+MRL) 0.26%, bilinear chroma 0.15%; zone-1 was already vectorized and is only ~0.1%. Committed in `4f7d51140`: § 7.13.2.8 step 3 fixes `idx` by the column, so down a column `shift` is loop-invariant and `base` advances by one — the same contiguous 4-tap shape step 1 already used along a row. The existing row writer is now generalized over the destination step, with `CONTIGUOUS` monomorphizing zone-1 back to its previous stores. Paired medians vs `8de097ece`: 1T **-0.314% (11/11 positive)** against a same-source-rebuild A/A null of +0.056%, 2T -0.302%, 4T -0.377%, 8T -1.528%, 10T -0.034% against a -0.029% A/A null (10T is pipeline-latency bound, as expected). Byte-exact 1T/10T (SHA-256 `48f0dc14…`), new per-sample equivalence test over 10 angles x 25 sizes x 3 MRL indices (mutation-checked), `check-duplication` at budget, bare `cargo xtask ci` exit 0. | Retained. Zone-2 middle IDIF is the only remaining vectorizable slice: its Above segment has the same invariance (`idx += 64` per column leaves `(idx >> 1) & 0x1F` fixed), but the row splits into a scalar Left prefix plus an Above suffix, so the realistic capture is ~0.1% of 1T — below the complexity bar unless it rides along with other work. |
| SCALE-016 | Completed | `/root` + one sequential implementation agent | Remove redundant LR staging traffic; rolling CDEF and post-LR bulk-copy elimination measured and closed. | SCALE-013 | Committed in `aaab561b3`: LR and PC-Wiener blocks now filter directly into the post-LR stripe rectangle, deleting the per-block staging fill/copy (~4.2 ms/1T run). Dataflow proof: post-LR is destination-only until GDF; GDF's dual pre-LR/post-LR sources (K10.12A) make the two stripe planes structurally required, and measured ceilings for the surviving bulk copies (CDEF copy 3.73 ms, post-LR copy 2.05 ms per 1008 ms run, before fallback machinery) close the in-place options as disproportionate. Paired medians: 1T +0.48% (8/11), 2T +0.82%, 4T +0.00%, 8T +0.27% (5/5), 10T +0.96% (7/11) — consistent positive, no regression. Byte-exact 1T/10T; full `cargo xtask ci` green. | Retained. Do not revisit rolling CDEF output or post-LR copy coverage without new evidence. |
| SCALE-017 | Rejected; fully reverted | `/root` + one sequential implementation agent | Carry parse-side nonzero-row knowledge into the inverse transform; DC-only fast path; recursive DCT. | SCALE-013 | A byte-exact per-row nonzero mask (eob alone is wrong: FSC walks the scan tail `bob..seg_eob`) removed the dequant zero-rescan for an isolated −0.19% at 1T but only −0.07% net after its parse-side bookkeeping, 7x under the bar for a 22-file diff. A transform-doubling ablation bounds the whole § 7.15.4.1 family at +0.67% of 1T, so the recursive-DCT idea is closed by the same measurement; DC-only DCT_DCT already ships on the inter path (`dct_dc_residual`), and the remaining intra slice is ≲0.1%. Methodology finding recorded for all future timing: A/A null controls reproduce ±0.3–1.0% paired medians at 10T (~0.5% second-slot bias), so sub-1% 10T results are not evidence on this host; 1T resolves to ±0.05%. | Closed. Do not retry residual-pipeline scalar work; require A/A controls for any sub-1% claim. |
| SCALE-018 | Rejected; fully reverted | `/root` + one sequential implementation agent | Vectorize MSAC symbol CDF scan and adaptation. | SCALE-013 | A shadow-pass ablation bounds the whole scan+`update_cdf` family at +0.30% of the 1T wall (mean arity 3.72, mean scan depth 1.73, mean update length 2.72), and both byte-exact SIMD variants regressed 1T by +0.43%/+0.50% (0/5 pairs) because widening packed variable-length CDF rows costs more than the scalar work. Closed unless the CDF bank layout changes, and SCALE-061 measured that layout at zero, so the caveat is discharged. Ancillary: `read_symbol_u16` is a non-inlined cross-crate call (~83 ns/symbol over 482,502 calls, no release LTO) — per-call overhead dominates the entropy spine; successor SCALE-026. | Do not retry symbol-primitive SIMD on the packed layout. |
| SCALE-026 | Rejected; fully reverted | `/root` + one sequential implementation agent | Inline the hot cross-crate symbol-decode entry points. | SCALE-018 finding | Byte-exact `#[inline]` variants (four entry points, then `read_symbol_u16` alone) regressed 1T by +0.11%/+0.27% paired over 11 pairs against an A/A null of +0.00% (±0.05% band), sign never flipping: instantiating the `read_symbol_from_cdf!` body at 12+ decode call sites costs more I-cache/regalloc pressure than the out-of-line call saves. The K10.50 fat-LTO 1T gain does not come from these entry points. | Closed. Do not re-add `#[inline]` here; any LTO-shaped win lives elsewhere in the cross-crate graph. |
| SCALE-019 | Completed; delivered by SCALE-039 | `/root` + one sequential implementation agent | Re-evaluate the frame-delay auto policy against a bounded frame-context count. | SCALE-013; SCALE-038 | dav2d runs `min(8, ceil(sqrt(threads)))` frame contexts (four at 10T); splot's auto frame delay equals the thread count. K10.16 measured about 2% for delay four/five under the old gate. SCALE-038 measured the other end: `FrameDelay::resolve` clamps any requested depth to the worker count, so no low-width sweep has ever changed anything, and with the clamp removed **two workers at depth 3 measure −3.546% (11/11) against a +0.065% A/A null**, byte-exact, with the depth-proportional storage gate 40/40 green; 3T depth 4 is −1.085% (8/11) and 4T is null. The blocker is not evidence but policy: `--frame-delay=N` is documented as clamped, and the benchmark contract pins `--frame-delay=T`, so both the resolved-depth rule and the contract's splot arm have to change together. | Closed. The maintainer took the policy decision and SCALE-039 shipped it: `Auto` resolves to `max(threads, 3)` above one worker, `Fixed` is honored as requested, and the contract's splot arm is `--frame-delay=auto`. |
| SCALE-020 | Rejected; no source change | `/root` + one sequential implementation agent | Micro batch: remove the per-block GDF `Vec` allocation and the per-call FSC scan-order allocation, and vectorize CCSO. | SCALE-013 | Ceiling ablation closed all three members before any timing, so no paired screen was run; every count below is one 30-frame 1T decode of the mission stream. (1) `copy_base_block` (`filters/gdf.rs`) runs **0 times**: the stream never disables a GDF block, so the allocation it serves is unreachable here. (2) The FSC scan-order allocation is reached only through `apply_staged_nonzero_coeff_fsc_branch_from_tx_size` — **34 calls, 0.027 ms total, 0.003% of the 1T wall**, two orders below the ±0.05% 1T resolution. (3) The premise that `filters/ccso.rs` is scalar is stale: `ccso_simd_row` already vectorizes at 16 `u16` lanes, and forcing it off costs **+41.6 ms** (`ccso_units` 9.79 to 51.37 ms). The whole CCSO family is 9.79 ms = **0.95% of the 1T wall**, only luma runs (chroma stripe count 0), and the measured geometry — `blk_w` 256 over a 1920-wide plane — leaves the trailing scalar loop with **zero** samples. Further CCSO vectorization therefore has a ~0% ceiling, under the 0.5% bar the task set. | Closed. All instrumentation reverted; no source change and no timing run. Reopen only for a stream that actually uses per-block GDF or FSC/IDTX coefficient blocks, and only after noting CCSO is already vectorized. |
| SCALE-021 | Rejected without implementation | `/root` | Never-block lane ownership: let a frame waiting on cross-frame rows occupy zero recon lanes. | SCALE-023 depth sweep | Lane yielding is arithmetically an effective depth increase, and the SCALE-023 sweep bounds it on the stacked tree: L inflates near-proportionally with depth because the added frame's head is dependency-gated, not pool-gated (L/D 6.75 at depth 3 versus 6.62 at depth 4), depth 5 is −1.395% head-to-head, and the residual gap between L/4 and the first-publication segment is 0.89 ms/frame. | Closed. Revisit only after the first-publication segment S falls materially. |
| SCALE-023 | Completed | `/root` + one sequential diagnostics/implementation agent | Admit a fourth reconstruction frame context. | SCALE-013/014/016/022 stack | Committed in `541eb1ffc`: 10T +1.506% (9/11), 8T +0.814% (9/11), byte-exact, storage and `cargo xtask ci` green; depth 5 dropped. Full evidence in the mission ledger. | Retained. Depth re-sweep only after S falls. |
| SCALE-024 | Rejected; no commit | `/root` + one sequential implementation agent | Re-key the admission scheduler so the pace-setting first-band publication chain is not the lowest-priority work in the pool. | SCALE-023 | Prerequisite held (the order key is a pure hint; every dependency is a condition), and three byte-exact re-keys were measured: a minimal head-only class −0.72%, a full publication class −0.04% at 10T over 11 pairs (9/22 positive pooled with 8T), and a blanket newest-first inversion probe +0.885% — bounding the entire order-key lever at about ±1%. Mechanism data: S fell only 0.3–0.5 ms because scheduler-attributable spine idle is just 1.6–1.7 ms/frame; the dominant 6.2 ms `spine_prewait` is the spine waiting on its own cross-frame row-gated precompute, which no priority can remove; and `admit_ready` drains the ready heap completely at every publication, so cross-frame reordering has no material. Fully reverted. Successor: SCALE-025. | Do not retry order-key variants. |
| SCALE-025 | Completed re-scoped | `/root` + one sequential investigation/implementation agent | De-quantise the frontier — rejected as stated; superseded by superblock-row-aligned precompute batches. | SCALE-024 mechanism data | The instrumented cycle map proved sub-SB-row frontier progress geometrically impossible (commit units are full-superblock-height column blocks; only left-prefix column progress exists, the rejected K10.12B family) and exposed the real defect: a 4-unit batch straddling two superblock rows made the row-closing batch gate on the predecessor's stripe `2r+4` instead of `2r+2`, a measured 1.1–1.7 ms hold per row on a 5.58 ms period. `superblock_row_batches` now splits batches at superblock-row boundaries (commit spine, unit order, batch size otherwise unchanged; owned-bands path preserved). Committed in `8de097ece`: 8T +2.45%/+2.14% and 10T +1.95%/+2.23% across two 11-pair matrices (10/11, 11/11, 9/11, 10/11 positive), 1T/2T/4T neutral-positive, byte-exact, bare `cargo xtask ci` exit 0. Remaining edge: the required one-SB-row predecessor lead plus ~0.76 ms filter latency; measured P (5.58 ms) is near the 41.8 worker-ms / 10 = 4.2 ms work floor, so further 10T pace gains need less per-frame work rather than shorter chains. | SCALE-024 mechanism data | `commit0 → filt_first` is 3.79 ms because `safe_deblock_mi_end` counts only complete superblock rows: on 128-row superblocks stripe 0 (rows 0..56) waits for the full first row (commits 0..3). Advancing the § 7.17 frontier at sub-superblock-row granularity shortens the publication chain execution itself — what the SCALE-024 measurement says is needed. Complexity warning: a partial-width row cannot be deblocked full-width; this needs a careful left-prefix or per-pass sub-row design honoring the D-series invariants and the six-row read lead. Host noise is ±3.9% per pair; claim direction only at 11+ pairs. | Byte-exact 1T/10T; consistent positive at 8T/10T; no regression elsewhere; D-series invariant tests; standard gates. | SCALE-023 | `order_base = frame_index * ORDER_KEY_FRAME_STRIDE` is strictly frame-major, so frame N's final filter stripes outrank frame N+1's resolve[0]/row-0 precompute/first commit links/stripe-0 chain — the chain that now sets the pace. Measured: S = pre0-to-first-publication is 5.73 ms mean with only ~2.0 ms of execution, i.e. ~3.7 ms/frame of pure queueing in a 42%-idle pool. Ceiling: capturing a third to a half of it makes R/D binding again and lands 10T near 0.152–0.160 s. Prerequisite check: the order key must be a pure priority hint with correctness carried entirely by conditions — verify before editing. Distinct from K10.25 (which preserved the frame-major key), K10.16/K10.19 (depth/credits, pre-SCALE-013). | Byte-exact 1T/10T; consistent positive pairs at 8T/10T with no 1T/2T/4T regression; storage test; standard gates. |
| SCALE-027 | Rejected; fully reverted | `/root` + one sequential implementation agent | Take the independent temporal-band projection off the ordered § 7.12 resolve baton. | SCALE-011-K10.18 | The discriminating step-1 trace — per resolve link, its start minus the moment its last source motion band settled, floored at the frame's own chain origin, over three 10T runs (279 links, 31 frames each) — put the serial chain's own wait at **6.17–7.24 ms/frame**, well past the 2 ms/frame implement threshold, and the split then delivered exactly the latency it promised: projections started **0.85–1.47 ms/frame** after their sources settled. It still did not convert. Every projection was submitted up front behind its own `resolve_conditions(index)`, the § 7.12 loop was gated on its own band plus the previous link, and the decoupling was gated at pool width two (1T never builds a `ScheduledFrame`, so it keeps the fused walk by construction). Output was byte-exact at 1/2/4/8/10T (186,624,000 B, SHA-256 `48f0dc14…`), but paired medians against frozen `0b5321706` were 10T **+1.083%** (4/11 faster) against a same-source A/A null of +0.119%, 8T **+0.731%** (4/11) against a null of −0.303%, 4T +0.282% (1/5) against +0.135%, 2T +0.737% (1/5) against +0.818%, and 1T +0.274% (0/3) against +0.056% — never positive at any width, and about +1% adrift of the null exactly where the change was aimed. The residual ordered-link wait *rose* to 7.66–9.14 ms/frame, so the baton lost more to the extra completion hop and to consuming each band on a worker that did not produce it than it saved from the ~0.31 ms/link of projection it shed. All runtime edits reverted. | Closed. K10.18's conclusion holds with a properly powered measurement behind it: the independent-projection worker time is real, but releasing it costs more than it returns unless the § 7.12 consumer and its band producer stay on one worker. Do not resubmit projection decoupling without that. |
| SCALE-028 | Committed; retention call open | `/root` + one sequential implementation agent | Take the § 7.17 frontier and its stripe-window extraction off the ordered commit baton onto a per-frame ordered frontier chain. | SCALE-025; SCALE-013 six-row lead | Committed in `20f25c390` + `5a99a068d` + `006c2603b`, byte-exact at 1/2/4/8/10T, bare `cargo xtask ci` exit 0. 10T −3.385% (10/11, null −0.572%) and 2T −2.987% (5/5) clear the 2%-beyond-null bar; 8T −1.160% (10/11, null −0.894%) does not; 4T +0.497% and 1T unchanged code. Full evidence in the mission ledger, including the 2.8x segmented-deblock rejection of the `OwnedFrameBands` shape and the nested-Rayon deadlock that forces sequential plane passes on the chain. The third commit builds the filter copy over recycled plane storage instead of allocating and zero-filling it, and cumulative against `0200bf08f` that reaches 10T −2.934% (10/11), 8T −2.113% (8/11), 2T −2.101% (5/5), 1T −0.659% (3/3), 4T +0.971% (1/5) — 8T now clears the 2% bar the chain alone missed. The frontier relaxation this row's follow-up proposed was implemented, measured null and reverted. The SCALE-023 depth re-sweep that the shortened commit chain was predicted to re-open was also run and rejected: `ReconAdmissionLane::new(ring.capacity().min(4))` to `min(5)` is byte-exact and 9/9 on `frame_pipelining` with the storage gate untouched, but 10T measures −0.298% (6/11) and +0.514% (4/11) against a −0.944% A/A null and 8T −0.306% (8/11) against a −0.457% null, so depth 5 is null-to-slower at both levels; depth 6 was not built, and 1T/2T/4T are provably identical code because the ring clamps the depth at frame delay 4. The `OwnedFrameBands`-style `release_through` of consumed sealed rows was skipped on inspection rather than built (see the mission ledger). | Maintainer decision under the 2026-07-31 stacking policy: keep the three commits, or `git reset --hard 0200bf08f`. The remaining sealed-copy cost is the 4.5–5.3 ms row copy itself; the six-row read lead and the lane depth are no longer candidate levers (see the mission ledger). |
| SCALE-029 | Rejected; no source change | `/root` + one sequential implementation agent | Publish reference-row watermarks as § 7.21 finalizes rows inside a filter stripe rather than only at stripe end. | SCALE-028 ranked fallback | Step-1 diagnostics over three 10T runs put the recoverable wait at 0.55–0.60 ms/frame summed over blocked consumers and 0.29–0.30 ms/frame as an interval union, under the 1 ms/frame kill line and a fifth of the 1.5 ms implement line. § 7.21 runs on only 7 of 28 steady frames, and where it runs the recoverable window is capped by one stripe's guided-detail pass (0.85–0.98 ms) because CDEF, CCSO and loop restoration are whole-stripe passes. Full evidence in the mission ledger. | Closed. Do not retry sub-stripe publication keyed on the guided detail filter. The adjacent term that does dominate is SCALE-030. |
| SCALE-030 | Committed; retention call open | `/root` + one sequential implementation agent | Close the gap between a filter stripe finishing its chain and its rows becoming consumer-visible. | SCALE-029 step-1 measurement | Committed in `c46a24085`, byte-exact at 1/2/4/8/10T, bare `cargo xtask ci` exit 0. The stripe backlog moves from `OwnedFilterSetup` into `FrameProgress` as queued copies, and `PublishedFrame`'s drop drains it — the reader that made `try_write` fail publishes the stripe it blocked, at the instant it releases the prefix. Every attempt is still `try_write`, so the documented non-blocking rationale is preserved intact and the cross-frame reader deadlock the borrow rules rule out stays impossible. Mechanism over three 10T runs, before to after: deferred stripes 27.8–29.4% to 15.6–20.0%, p90 1.05–1.24 ms to 211–305 us, p99 3.02–4.07 ms to 1.33–1.67 ms, summed publication lag 169.6–194.4 ms to 51.8–55.0 ms, stripes landing only at the terminal drain 41–55 to 2–16; 23.2–26.3% of all stripes now land on a reader that just released the prefix. Paired medians against frozen `f1a0ac1ae`: 10T **-3.309%** (9/11, null -0.090%), 8T **-2.567%** (11/11, null -0.158%), 4T -0.716% (5/5), 2T -0.160% (4/5), 1T -0.239% (3/3) — the first candidate in this series to clear the 2%-beyond-null bar at both 8T and 10T. Cumulative against `0200bf08f`: 10T -6.456% (10/11), 8T -4.044% (11/11), 2T -2.938% (5/5), 1T -0.841% (3/3), 4T -0.362% (4/5) — SCALE-028's lone 4T regression is now gone. Full evidence in the mission ledger. | Maintainer decision under the 2026-07-31 stacking policy. The publication term is not exhausted: 15.6–20.0% of stripes still wait, because a reader release only helps when it is the last reader. The next lever is the read hold itself — one shared borrow per predicted block over the whole of its motion compensation — not another drain trigger. SCALE-031 measured that lever and closed it. |
| SCALE-031 | Rejected; no source change | `/root` + one sequential implementation agent | Shorten the per-block reference-sample hold, or defer arriving readers, so a queued filter stripe does not wait to be the last reader out. | SCALE-030 | Step-1 diagnostics on exact `ddb14def1` reproduce the premise (14.2–18.4% of stripes deferred past 100 us at 10T, p90 161–289 us, p99 1.36–1.90 ms) and then bound the whole remaining term: against the perfect-publication counterfactual, the recoverable consumer wait is 0.64–0.86 ms/frame as an interval union at 10T and 0.27–0.34 ms/frame at 8T, both under SCALE-029's 1 ms/frame kill line. Calibrated by SCALE-030's own union-to-wall realization, that is at most −0.9 to −1.2% at 10T and −0.4% at 8T with a cost-free mechanism, so 8T misses the retention bar by about five times before any cost is paid. § 7.13.5 TIP owns 67% of hold time, only 1.3–1.8% of reader arrivals during a queued stripe find the lock free, and 1T/2T build no progressive workspace at all. Full evidence in the mission ledger. | Closed. Do not retry hold narrowing, hold batching, or reader deferral on the progressive workspace. Reopen only for a design that stops a reconstructing frame reading a still-filtering reference at all, and only if a fresh profile puts the recoverable union back above 1 ms/frame. |
| SCALE-032 | Investigated; no source change | `/root` + one read-only investigation agent | Explain the steady-state CPU divergence: splot matches dav2d's CPU at `--limit=30` but exceeds it by 20% at `--limit=120`. | SCALE-031; 2026-07-31 re-profile | Frame counts verified equal (both decoders emit exactly 30/60/120 output frames). Nothing accumulates: the marginal CPU of one steady frame is flat at 1.304x / 1.318x / 1.266x over frames 30–60 / 60–120 / 120–240, and the aggregate walk from 1.045x to 1.219x is a pure mix effect with asymptote 1.313x, predicted to within 0.003 by the two-segment model. The 30-frame benchmark is weighted by an intra-heavy prologue and understates the steady-state CPU gap about sevenfold. The gap is created by threading, not content or state: at 1T splot's steady frames cost 1.054x dav2d's CPU and run 5% *fewer* instructions, while at 10T instructions are at parity (+1.9%) and cycles are +31.8%. Not footprint (frame-delay 2 to 10 moves RSS 374 to 710 MB with cycles/frame flat at ~114.6) and not intra-decode contention (8 busy cores regrouped as 1x8, 2x4 or 4x2 threads vary by at most ~3%). The step is the split parse/reconstruct path: at identical 8-core pressure, 8x1-thread fused-walk processes cost 92.2 M cycles/frame against 4x2-thread split-path processes at 104.2, +13.0%, while dav2d's own 1T-to-8T threading costs only +3.2%. Phase counters at 2 workers versus 1 show the streaming filter and MC kernels indifferent (+1 to +2%) while entropy and intra inflate on identical item counts (`block` +37%, `intra_leaf` +44%, `coeff` +47%). Full evidence and ceiling arithmetic are in the mission ledger. The raw/Y4M 35-frame ceiling found during this work was a separate output-retention bug, not a performance finding. | Successor candidate: reduce the split path's per-frame cost, ranked by the 12 M cycles/frame it adds (10.5% of splot's 10T per-frame cycles; 5.22 to 4.67 ms/frame wall at unchanged occupancy). Smallest discriminating next measurement: instrument the uninstrumented prepass and publication work that replaced the fused `commit_inter` (7.62 to 0.81 ms/frame, 288.8 to 83.7 items) at `--threads=2` against `--threads=1`, which accounts for ~2.8 of the +4.1 ms/frame that two workers add and is the only unattributed part of the step. Note for all future work: the coordination bound caps every remaining scheduler, publication and lock candidate at about 3% of per-frame CPU, though it does not bound occupancy-side wins. |
| SCALE-033 | Rejected; no source change | `/root` + one sequential investigation/implementation agent | Attribute the split parse/reconstruct path's +12 M cycles/frame and implement the removable part. | SCALE-032 | Hardware counters bound the extra-work term before any phase timer: marginal steady-frame instructions are 412.4 / 436.3 / 435.3 / 440.4 / 444.9 M at 1/2/4/8/10 threads against cycles 79.3 / 90.4 / 93.4 / 103.0 / 110.9 M, so the second worker adds 5.8% instructions and 2 to 10 workers adds only 2.0% more while cycles add 22.7%; priced at the 1T IPC of 5.20, all of the 1T-to-10T instruction growth buys at most 6.25 of the 31.6 M cycles/frame, leaving **at least 80% of the 10-thread penalty as stall on constant work**. Phase attribution against the CPU each width adds over 1T splits it as (a) work the fused walk never does 41% / 29% / 28%, (b) identical work running slower 13% / 50% / 54%, (c) duplicated publication bookkeeping 7% / 4% / 3% at 2/8/10 threads, with a 40% / 17% / 15% uninstrumented residual. SCALE-032's unattributed prepass is attributed and is not extra work: `commit_inter` 310.4 ms/run at 1T becomes `prepass_row` 200.8 + `prepass_motion_pass` 83.0 + `commit_inter` 22.8 = 306.6 at 2T on identical 7,676 motion and 7,676 prediction items. The named top mechanism is that the split path materialises each frame's pixels three times — per-superblock `OwnedFrameRect`, frame workspace, sealed filter copy — worth 3.6 M cycles/frame at 2T, with `_platform_memmove` at 6.82% of busy CPU at 2T against 3.23% at 1T. Its removal needs a restructuring of scheduled-reconstruction surface ownership, and every smaller candidate measured at 0.6 to 0.8 M cycles/frame; the banded temporal projection and the plane-buffer pool size were both measured and disproven. Full evidence in the mission ledger. | Closed as a CPU-side candidate family. Do not reopen the split path's extra work without a design that removes the shadow-surface copy itself; the flag-log replay is worth 0.09 ms/frame and is not a lever. The live term at the retention widths is (b): identical items inflating x1.3 to x1.7 while instruction counts stay flat. Next discriminating measurement is a width sweep with the pool restricted to performance cores, which separates core heterogeneity (10 threads on 8P+4E) from per-worker working-set pressure; the entropy parse and `commit_intra` inflate first and most and are the layout targets after that. |
| SCALE-034 | Investigated; closed | `/root` + one read-only investigation agent | Separate host core heterogeneity from per-worker working-set pressure in the 10-thread CPU penalty, and decide whether a layout candidate remains. | SCALE-033 | Width sweep (marginal steady frame, `--limit=120` minus `--limit=60` over 60 frames, `/usr/bin/time -l`, median of 3): splot 79.6 / 90.4 / 93.7 / 98.1 / 102.1 / 110.5 M cycles/frame at 1/2/4/6/8/10 threads against dav2d 73.8 / 75.0 / 74.9 / 75.2 / 77.0 / 82.2, confirming dav2d's near-free threading on this host (1T to 8T +4.3%). Cycle inflation is present and monotone well inside the eight performance cores (+13.6 / +17.7 / +23.3 / +28.2% at 2/4/6/8T), at a flat slope near +2 M per added worker after the split-path switch, against +4.23 M per worker for the 8-to-10 step. `taskpolicy -b` prices an efficiency core at 2.08x the cycles for identical instructions, and saturating the efficiency cluster with background-priority spinners moves splot only −0.4% at 6T and −1.1% at 8T, so at eight workers splot is already effectively performance-core-only. The decisive control is N independent single-thread decodes against one N-thread decode: splot 79.6 to 88.6 (+11.3% hardware) to 103.1 (+16.5% software) at eight cores, and 79.6 to 95.1 (+19.5%) to 111.0 (+16.7%) at ten cores where both shapes carry the identical core mix; dav2d is 73.8 to 78.2 to 76.9. The software term is flat at 14.5 and 15.9 M cycles/frame, so the whole 8-to-10 step is host topology. It splits into 7.6 M cycles of extra work and **8.4 M cycles of stall on constant work**. Per-phase, the hardware column is uniform across every phase while the software column is zero on every streaming filter and motion kernel and carries entirely on `block`, `coeff`, `intra_leaf` and `commit_intra`. Full evidence, structure map and instrumentation control in the mission ledger. | Closed. The addressable term is 8.4 M cycles/frame, 7.6% of ten-thread per-frame CPU, below the 10 M line this row was set and flat from eight to ten workers; 49% of the penalty is host topology and 24% is SCALE-033's already-closed split-path work. Do not reopen without a design that pins the ordered commit and its scratch to the worker that produced the row, and only if a fresh sweep puts the software stall term back above 10 M cycles/frame. |
| SCALE-035 | Rejected; no source change | `/root` + one sequential design/implementation agent | Let the parallel prepass reconstruct blocks in place in the frame workspace instead of into per-superblock owned rectangles. | SCALE-033; SCALE-034 | The whole triple materialisation is 0.84 ms/frame of rect publication plus about 0.42 ms/frame of sealed filter copy against a 35.83 ms/frame marginal steady frame at 10 threads, **3.4% of steady-frame CPU before any mechanism is paid for**. In-place is not expressible: precompute batches are column ranges inside one superblock row, not disjoint row ranges, so the claim must be the 2-D `rect_surfaces` partition, which borrows the whole workspace while the ordered commit concurrently needs it for cross-superblock general-intra and IntraBC reads. The byte-exact POC that moved 63–85% of the publication off the ordered spine measured null-to-negative (pooled 10T +0.11% over 22 pairs, 8T −0.46%, 4T −0.32%, nothing consistently positive) because the spine already waits on its own precompute. The existing zero-copy `owned_bands` path prices the trade directly: forcing its 21-of-121 frames onto the copying path adds 23.6 ms of copies and removes 24.0 ms of segmented deblock, a net zero. Full evidence in the mission ledger. | Closed. Do not retry prepass surface ownership, opportunistic or early publication, or rect coalescing: the copies are 3.4% of per-frame CPU, the only in-place shape that exists returns none of it, and the ordered spine's execution time is not what binds. Reopen only for a design that also makes the § 7.17 frontier read contiguous storage it does not own a copy of. |
| SCALE-036 | Rejected; fully reverted | `/root` + one sequential implementation agent | Replace process-global pooling in the entropy and intra paths with ownership that fits the measured buffer lifetimes. | SCALE-034 | Per-pool measurement selected the one pool that pays and refuted the proposed shape. The coefficient-quant pool costs 4.34 worker-ms at 1T against 33.29 at 8T and 27.64 at 10T on identical operation counts, because its best-fit scan runs over a pool that saturates at 2,560 buffers under the split path (537-entry mean take scan at 8T); the parsed-residual-plane pool costs 3.5 worker-ms per run, the inter-scratch pool 0.4, and the ordered commit already keeps its scratch on one worker across 79% of 4,226 links. Worker-local free lists are the wrong shape: takes and recycles are structurally on different workers (6,594 takes / 126 recycles on one, 0 / 1,769 on another at 10T). Bucketing the shared pool by capacity made both ends O(1), was byte-exact at 1/2/4/8/10T and measured 10T -3.413% (11/11, null -0.491%), 8T -2.657% (10/11, null -0.448%), 2T -2.021%, 4T -0.992%, 1T -0.166%, with marginal steady-frame CPU 36.25 to 35.50 ms/frame at 10T. It was rejected because `pipelined_hash_decode_bounds_reference_storage_by_the_resolved_frame_delay` fails 8 times in 80 runs at two workers (serial peak plus three decoded frames against a bound of two) where exact base is 60 of 60 clean. A control that keeps the identical structure and restores only the take-side scan cost passes 40 of 40, so the cause is the parse lead, not any lifetime or retention change. Full evidence in the mission ledger. | Closed as built. Two live findings for successors. First, the SCALE-022 depth-proportional reference-storage bound is empirical and has zero headroom at (2 workers, delay 2), so it will reject **any** entropy-parse acceleration at that width until the bound is re-derived — a maintainer call, not an implementation one. Second, the commit-side half of this win is real and parse-neutral: the eviction-only control measures 10T -1.351% (11/11), 8T -2.230% (10/11), 2T -3.408% (5/5) and passes the storage gate, so a shippable O(1) eviction over the existing flat pool that leaves the best-fit take scan alone is the named successor. **Both findings are discharged by SCALE-037**, which located the unbounded term outside the pipeline, re-derived the bound, and re-landed the full pool. |
| SCALE-037 | Completed | `/root` + one sequential correctness/perf agent | Derive the true structural bound on live decoded frames under the split path, fix or retarget the reference-storage gate accordingly, and re-land SCALE-036's bucketed coefficient-quant pool. | SCALE-036 | The extra frame was a real, unbounded retention outside the pipeline: `decode_hash_frames_pipelined` shared every emitted frame's storage into a pool task with no cap, and the driver retires a frame only when its storage has a single handle, so `max_reference_store_bytes` held or failed by how far the pool trailed the driver. A handoff-depth sweep isolates it — max live frames at (2,2) are 8 synchronous, 8 at one outstanding, 9 at two, 11 at four and unbounded — while the pipeline's own term is the serial live set plus at most the resolved frame delay at every width, from the in-flight ring alone, with the entropy-context limit and the reconstruction lane depth adding nothing because both are at most that delay and every frame they hold is a ring frame. Bounding the handoff at four outstanding frames is free at every width (4T +0.23% against an A/A null of −0.18%) where synchronous hashing costs +3.65% and one outstanding +9.3% to +12.7%, so the gate is retargeted to the derived `serial peak + (delay + 4) decoded frames` with the derivation on the test. Stress: 80/80 release and 40/40 debug clean at the SCALE-036 stress shape, 40/40 release and 40/40 debug plain, against a same-host falsification control (uncapped handoff, old bound, bucketed pool) that fails 4/80 with the SCALE-036 signature verbatim. The bucketed pool then re-landed unchanged: byte-exact at 1/2/4/8/10 workers, 10T −3.279% (11/11), 8T −3.062% (11/11), 4T −1.645%, 2T −1.488%, 1T −0.311%, marginal steady-frame CPU 34.00 to 32.83 ms/frame at 10T. Committed in `7591ce56d` and `48a41e7b7`; full evidence in the mission ledger. | Closed. Both of SCALE-036's live findings are discharged: the SCALE-022 bound is re-derived and no longer has zero headroom at (2 workers, delay 2), and the full parse-side win shipped, so the eviction-only successor it named is unnecessary. A future change may still be rejected here honestly — the gate now bounds pipeline retention only, and its handoff term is a constant. |
| SCALE-038 | Investigated; no source change | `/root` + one sequential investigation agent | Profile the 2/3/4-thread widths specifically, where the gap is smallest and every earlier profile targeted 1 or 10 threads. | SCALE-037 | Four 11-pair matrices on frozen `223906d7a` put the gap at 1.038–1.073x / 1.058–1.074x / 1.094–1.097x at 2/3/4T, 0/11 every time; the 2T/3T spread is host warmth acting on splot's CPU alone, and is now recorded as a caveat on every cross-decoder ratio. Occupancy is the invariant term — splot 1.86 / 2.74 / 3.57 cores against dav2d 1.98 / 2.94 / 3.88 in every matrix — and accounts for the whole cold-host gap at 2T/3T and 91% at 4T. Idle splits by output-frame region: **57% of splot's 2T idle and 81% of its 4T idle is pipeline fill**, against a dav2d whose entire 30-frame idle is in the first five frames. Steady state is 94–95% occupied (sampling confirms 96% per worker, and the two-thread driver is a real worker — 2,836 of 2,868 samples inside `harvest_oldest`, only 107 of them parked). The largest single fill item is the key frame's fused serial walk, 16.2–16.4 ms with every other worker idle = 1.47/2.76/3.97% of the pool. The one lever with headroom is pipeline depth, which `FrameDelay::resolve` has always clamped to the worker count. Full evidence in the mission ledger. | Closed as an investigation. Successors are SCALE-019 (the depth policy, now measured), and — if the maintainer wants the fill attacked — overlapping the key frame's post-filters into its own walk (~1.6/2.2/2.6% at 2/3/4T) before any parse/reconstruct split of an intra frame (~1.5/2.8/4.0%, and it needs intra frames on the scheduled path). Do not retry `ASSIST_PARK` shortening; it measured inside the null band. |
| SCALE-039 | Completed | `/root` + one sequential implementation agent | Remove the resolved-depth clamp and give `FrameDelay::Auto` a multiworker floor of three frames. | SCALE-019; SCALE-038 | `Fixed(n)` is now honored as given and `Auto` resolves to 1 at one worker and `max(threads, 3)` above it; the benchmark contract's splot arm is `--frame-delay=auto`. Floor 3 was taken over floor 4 on a head-to-head tiebreak: floor 4 is -0.553%/-0.893% (9/11, 10/11) at 3T but +0.507%/+0.698% (1/11, 2/11) at 2T, so it buys three workers about 0.7% by costing two workers about 0.6%. Byte-exact at (1,2,3,4,8,10, auto); **2T -3.121% (11/11)** against a +0.343% A/A null, every other width inside its own null on a provably identical resolved depth; two-thread ratio to dav2d 1.0707x to 1.0397x at matched delay on one host state. Storage gate 40/40 release, 20/20 debug, its ceiling still derived from the resolved delay. Full evidence in the mission ledger. | Retained under the 2026-07-31 stacking policy. Preserve the resolved-delay derivation on the storage gate; any future depth sweep at 2/3/4 threads is now a real sweep. |
| SCALE-040 | Completed | `/root` + one sequential implementation agent | Attack the pipeline-fill idle SCALE-038 attributed to the key frame's fused serial walk. | SCALE-038; SCALE-039 | Committed in `6203e92af`. A fill timeline on exact `fad149dca` proved the pool runs **0.00 job-core-ms** before the key frame's walk ends at 2/3/4/8/10 workers, decomposed that walk into ~19% prologue, ~38% entropy parse and ~43% intra reconstruction, and showed the fused walk is the key frame alone (every other frame is `splittable_inter_frame`). Candidate (a) closed on the terminal-CDF chain; candidate (b) deferred as SCALE-040-B with its dataflow blocker named; candidate (c) taken by splitting `parse_ahead` (`PARSE_AHEAD_POOL_WIDTH = 2`) from the shadow-surface `parallel_prepass` in `decode_tiles`. Key walk 17.1/17.0/17.2/17.2/17.4 ms to 10.7/11.3/11.5/11.8/11.8 at 2/3/4/8/10 workers. Byte-exact at 1/2/3/4/10 workers (186,624,000 B, SHA-256 `48f0dc14…`). Paired medians against frozen `fad149dca`, two matrices per level: 2T -0.814%/-0.925%/-0.758% (29/33 positive) against a +0.039% null, 3T -1.549%/-1.008% (21/22) against -0.084%, 4T -2.190%/-1.519% (22/22) against +0.068%, 8T -3.487%/-2.049% (10/10), 10T -1.776%/-2.427%/-2.659% (21/21), 1T neutral on a provably identical code path. Bare `cargo xtask ci` exit 0. Full evidence in the mission ledger. | Retained under the 2026-07-31 stacking policy. Keep `parse_ahead` and `parallel_prepass` separate: the precompute's four-worker and no-global-intra-block-copy conditions are its own, and folding them back re-serializes every fused walk. |
| SCALE-040-B | Rejected | Unassigned | Overlap the key frame's post-filters into its own fused walk. | SCALE-040 | Killed on its own measure-first gate; no runtime source changed. A tagged pool timeline on `f4e1b96c3` shows the tail already overlapped — occupancy inside `walk_end..finish_end` is 94% at 2T, 90% at 3T, 84% at 4T, 50% at 10T — while SCALE-040's `parse_ahead` left only 2.5-4.9 core-ms of two-worker walk slack against ~17 core-ms of filter work. Deleting the key frame's whole filter phase, a strict upper bound, moves the wall only -0.91%/-1.12% at 2T, -0.70% at 3T, and to within A/A null at 4T and 10T, so the predicted overlap gain at 2T is 0.15-0.3% against a 0.8% kill line. Full evidence, per-stage decomposition, and the deeper dataflow blocker are in the mission ledger. | Do not retry. Re-open only if the key frame's walk stops saturating the two-worker pool or the 4T/10T critical path moves so the tail stops being hidden. |
| SCALE-041 | Rejected | Unassigned | Admit the key frame's reconstruction to more than the two workers `parse_ahead` gives it, by scheduled-path admission for intra frames, coarse fan-out inside the fused walk, or an exact IntraBC source-liveness derivation. | SCALE-038; SCALE-040; SCALE-011-B4 | Killed on its own step-1 gate; no runtime source changed. An env-gated per-superblock-unit timeline on exact `41afff63c` shows the walk is a two-stage pipeline both of whose stages are normatively sequential and which are already balanced within 0.5 ms: parse 7.85-8.29 ms busy at 98% saturation, ordered reconstruction 8.40-8.66 ms at 95%, walk 10.41/10.42/10.46 ms and width-invariant at 3/4/10 workers. The walk cannot end before its parse chain does, so the whole fan-out family is capped at the measured `walk_end - parse_end` = **0.80-1.14 ms (median 1.02)**, which at SCALE-040's 1.0-1.55x walk-to-wall conversion is 0.26-0.40% at 3T and 0.54-0.83% at 10T *if the entire post-parse tail vanished*; a real offload captures only its neighbour-independent slice, 0.1-0.3%, against A/A nulls of +0.216% and +0.285%. Options (i) and (iii) are separately dead: the stream carries exactly one key frame in 6,481 coded frames, `splittable_inter_frame` excludes it by `ObuType` *and* by `global_intrabc_enabled` (measured true), and IntraBC costs the frame nothing because `select_prepass_entry` admits only `ReferenceOnly` entries, of which an all-intra frame has none. Full evidence in the mission ledger. | Do not retry reconstruction fan-out on an intra frame. Re-open only if the walk's two chains stop being balanced — that is, if the parse chain becomes materially shorter than the reconstruction chain — or for a stream with an intra frame that is not the whole pipeline's head. |
| SCALE-042 | Rejected; fully reverted | Unassigned | Keep the ordered commit spine and its scratch on the worker that produced the row: continue into an already-runnable successor inline, bounded, instead of handing it back to the admission scheduler. | SCALE-034; SCALE-041 | Two of the row's premises were already shipped and the third was built and measured null. Inline continuation already exists on the fused-walk path (`take_next_ordered_done`, `tile/ready_rows.rs:110`, merged in `#1126` before SCALE-034 measured 79%), and the ordered scratch is already frame-keyed (`TileDecodeScratch.ordered` moves into `ScheduledCommit.ordered`; the pool at `tile.rs:874` serves precompute, not commit). The unbuilt half — the scheduled split path — was implemented as a per-link claim plus bounded same-worker continuation (`COMMIT_INLINE_LIMIT = 4`), was byte-exact at 1/2/4/8/10 workers, and did what it promised: at 10T same-worker links 63.9-67.3% to 75.7-77.9%, ready handoffs that change worker 20.5-20.9% to 4.9-5.0%, runnable-successor handoff latency 1.91-2.13 to 0.59-0.81 ms/frame. Wall time did not move: 10T +0.126%/-0.006% against A/A nulls of -0.810%/+0.252%, 8T -0.183% against -0.031%, 4T -0.511% against -0.257%, 2T -0.524% against -0.049%, 1T identical code. The chain has slack — 13.15 ms median span inside a ~25 ms frame reconstruction lifetime — and only 2.1 ms of its 9.0 ms idle is reachable by any handoff change. Full evidence in the mission ledger. | Closed. Do not retry ordered-commit handoff placement, pinning, or scratch keying. Reopen only if the commit chain's span approaches the frame reconstruction lifetime, or for a design that shortens the spine's wait on its own precompute. |
| SCALE-043 | Investigated; no source change | `/root` + one read-only investigation agent | Build both decoders' ten-thread steady-state task timelines, align them per frame, and attribute the 1.2-core occupancy difference. | SCALE-041; SCALE-042 | Task timelines on both decoders (splot via an env-gated `TaskScope::spawn` trace with admission order keys, submit and ready instants; dav2d via an instrumented scratch **copy** of its tree, output-identical at `--muxer md5` `e88dd812b981f96fb66b0fe4b30f0e58` and wall-neutral), ten threads, `--limit=90`, coded frames 30-60, three runs each. Frames in flight are equal (3.62 against 3.68) and dav2d runs ten frame contexts at the contract's `--framedelay 10`, not four, and gains nothing from them. The difference is chain-overlap depth: with `C` the strictly ordered per-frame chain work and `k = C / P`, splot is `C = 5.85` core-ms at `k = 1.13` and dav2d `C = 7.13` at `k = 2.11`, and `0.820 x 1.867 = 1.531` reproduces the measured period ratio exactly. Splot's own head-of-frame publication cycle is 1.88 ms against dav2d's 3.24, so splot runs 2.8x above its own floor while dav2d runs at 96% of its period. The depth family is closed with a cause: 86% of frames are admitted by the depth-4 lane gate, but their first real work waits another 3.9-4.6 ms on their own § 7.12 sources, and 31-36% of the 20.9 ms lane residency is dead time. Full evidence, the idle-by-edge attribution and the ceiling arithmetic are in the mission ledger. | Closed as an investigation. **No scheduling candidate reaches the remaining ten-thread gap**; `k` rises only by moving per-frame CPU off the ordered commit spine, which is SCALE-011-B4, K10.9-K10.11, K10.34 and SCALE-035, all closed. If the maintainer wants the superblock-row rebuild scoped, the smallest discriminating pre-measurement is a schedule replay from logged per-link `need`/watermark pairs: if the replayed `k` does not reach 1.58 the rebuild cannot pay and the ten-thread target is unreachable on this stream. |
| SCALE-044 | Investigated; no source change | `/root` + one sequential diagnostics agent | Run the SCALE-043 go/no-go pre-measurement for the superblock-row rebuild: per commit link, the batch's exact per-list reference `need` against the producing frame's watermark at that instant, then an offline replay of the schedule with each superblock row an independent task. | SCALE-043 | **No-go.** Replayed `k` is **0.62-0.65** at ten workers and 0.68-0.71 at eight, against today's 1.13 and the 1.58 go line; projected ten-thread wall 0.31 s against 0.1895 s. The harness reproduces the shipped structure's measured steady period to +2.4% at ten workers. The needs are already exactly row-granular (`128 (r + 1) + 26` luma rows at the median), so there is no slack to recover; the rebuild loses because it moves the 11.56 core-ms of already-fanned-out precompute onto the ordered chain (5.08 to 17.23 core-ms, 3.4x dav2d's 7.13) while the measured needs cap overlap near 2. Sensitivity is flat across +-20% task overhead, filter granularity and lane depth. Full evidence in the mission ledger. | Closed. **By SCALE-043's stated rule the ten-thread target is unreachable on this stream.** Do not scope the superblock-row rebuild. The one term the replay does size is SCALE-011-C's whole-frame fallback: 29% of steady frames have no incremental § 7.17 frontier, and giving every frame one is worth -18.4% on the shipped structure (0.1895 s to 0.1547 s at ten threads, -13.9% at eight). That needs the exact IntraBC raw-source liveness derivation SCALE-011-C deferred, and is still short of 0.136 s on its own. **SCALE-045 took it**, at -6.49% on the 30-frame benchmark, whose prologue holds only 2 of the 9 steady-window fallbacks. |
| SCALE-045 | Completed | `/root` + one sequential implementation agent | Give the § 7.17 frontier to the frames SCALE-011-C left on the whole-frame fallback by deriving the deferred IntraBC raw-source liveness. | SCALE-044; SCALE-028; SCALE-013 | Committed in `b66f80358`. The derivation is vacuous: the frontier filters a sealed copy, so IntraBC keeps reading raw samples and no source ceiling is needed; the `contains_intrabc` term was stale since SCALE-028. Census on exact `1225a5b5c`: 14 of 92 coded frames fall back on IntraBC and 2 on a disabled deblock plan, and **all 9 fallbacks in SCALE-044's steady window are IntraBC**, confirming the replay's 29%. No global IntraBC on this stream; maximum upward source reach 80 luma rows, every source inside its own superblock row. Byte-exact at 1/2/4/8/10T, bare `cargo xtask ci` exit 0, `check-duplication` at budget. Eleven alternating pairs against frozen `1225a5b5c`: 10T **-6.49%** (11/11, null -0.48%), 8T **-5.57%** (11/11, null +0.24%), 4T -2.64%, 3T -1.18%, 2T -0.67%, 1T neutral on unchanged code. Ratio to dav2d 1.243x to **1.159x** at ten threads and 1.238x to **1.171x** at eight. Full evidence in the mission ledger. | Retained under the 2026-07-31 stacking policy. Re-measure with the other survivors after the next rebase. |
| SCALE-046 | Rejected; no source change | `/root` + one measurement agent | Release filter stripes incrementally on frames with no active deblock plan. | SCALE-045 | The last whole-frame fallback: 2 of 92 coded frames on this stream, both banded and both in the prologue, so none is in the steady window. `ScheduledTileRecon::frontier` releases stripes only inside `if let Some(deblock)`, and `extract_ready_window`/`extract_ready_band_window` ask the `FrameDeblock` plan which rows are final, so a frame with no plan needs a different final-row source — a mechanism, not a policy flip. | **Rejected on its own sizing gate by SCALE-047.** Census on exact `648d5581f`: the 2 frames are order hints 0 and 1, coded sequence 0 and 5, **both in the 30-frame prologue, neither in the steady window**. Frame 0 is the key frame with no filter chain to overlap and SCALE-041's closed serial pair; frame 5's whole interval already runs at 9.89-9.96 of ten busy cores with 0.23-0.68 core-ms of idle. Ceiling under 0.1 ms on the benchmark, exactly zero in steady state. Do not build it. |
| SCALE-047 | Investigated; no source change | `/root` + one measurement agent | Re-measure the ground-truth matrix on exact `648d5581f` and re-decompose the ten-thread wall after the SCALE-045 fallback removal. | SCALE-045 | Three sweeps of 11 alternating pairs: 8T **1.1714 / 1.1781x**, 10T **1.1703x** twice, 1T **0.9305 / 0.9400 / 0.9394x** at 11/11. Marginal steady frame at ten threads **-12.2%** (4.166 to 3.658 ms) at unchanged CPU, occupancy **7.900 to 9.007 against dav2d's 8.979**. Re-traced steady window: `k` **1.13 to 1.61-1.68**, idle **2.51-2.64 to 0.58-0.79 cores**. At `--limit=30` the prologue carries 75-80% of in-pool idle. Full evidence in the mission ledger. | Closed. **The next candidate is steady-state work, not scheduling**: at parity occupancy each core-ms per frame converts at 1/9.0 ms of wall, and splot's reconstruction is 15.99 against dav2d's 6.29 core-ms per frame. Re-opens SCALE-033/034/035 on that changed condition; **measure the coded frames 1-5 fill before touching the prologue**, which no steady-window trace has ever covered. |
| SCALE-048 | Completed | `/root` + one correctness agent | Explain and fix the one-worker versus multi-worker output divergence from coded frame 45. | None | Discovered by SCALE-047: `decode --output-format hash` gave different frame hashes at `--threads=1` than at 2, 4, 8 and 10 from output index 45 on, deterministically, on `648d5581f` and on pre-SCALE-045 `1225a5b5c` alike. **The serial arm was the wrong one.** First diverging coded frame is 45 (order hint 46, output index 46); `avmdec --i420 --rawvideo` matches the multi-worker result exactly and rejects the one-worker result. The frame is the stream's only `tmvp_sample_step_minus_1 = 1` frame with TIP as-ref and hole fill in the prologue, so its § 7.9 projection step is 2 and `average_tip_motion` writes only one cell in four. That function resized its destination scratch without clearing it, and the destination is a per-frame-context scratch that the preceding step-one frames had filled completely, so 24,240 of 32,400 projected cells arrived carrying the previous frame's TIP motion; `fill_temporal_sampling_gaps` replaces those only where the sampled anchor is valid, so 75 stale cells survived into the field, changed the § 7.12 MV stack of 24 blocks from superblock row 7 on, and moved 24,739 luma samples of the emitted frame. The split walk never saw it because `TemporalBandPlan::project` builds each band's TIP field in fresh storage. Fixed by resetting the destination in `average_tip_motion` (`crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs`), which is what `prepare_tip_field` already does for the projection scratch. Byte-exact 1T = 2T = 10T over all 545 output frames the decoder supports on this stream (frame 546 fails closed on `inter.partition_walk`, unrelated); 30-frame raw output unchanged at `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945` at 1/2/10T; AVM agreement over output frames 0-119 at every width, against 0-44 before. Bare `cargo xtask ci` exit 0. Alternating pairs against frozen `648d5581f` show no wall cost: eleven at ten threads give median 0.1785 s versus 0.1790 s (-0.29%, paired median +0.11%, candidate faster 5/11) and five at one thread 0.9740 s versus 0.9744 s (-0.04%). | Preserve `tip_averaging_never_inherits_the_previous_frame_between_sampled_cells`, which fails on the pre-fix function. Any further scratch reused across frames by a stride-sampled writer needs the same treatment. **Separately open:** from output frame 120 all widths agree with each other and disagree with AVM identically, on this fix and on frozen `648d5581f`; that is a plain conformance gap past the mission's benchmark window, not a scaling or width bug. |
| SCALE-049 | Investigated; no source change | `/root` + one measurement agent | Build the definitive normalized cross-decoder steady-state CPU ledger at ten threads with the same stage boundaries on both sides, attribute every stage where splot exceeds dav2d by 0.5 core-ms per frame, and re-price the closed CPU families under the new one-for-one conversion. | SCALE-047 | The ledger is in the mission's normalized-ledger section; method, cross-checks and re-pricing are in its ledger row. Headline: the +7.33 core-ms per frame steady gap is **prediction and MC +6.14, publication and copies +3.58, MV bookkeeping +2.19**, against a **-5.9 filter credit** (LR 4.205 against 8.758, GDF 0.848 against 2.203). **82% of the gap is parallel inflation** — at one worker splot is only +1.32 core-ms — and all of splot's excess inflation lands on those same three stages. The § 7.9 temporal projection is at parity (3.27 against 3.15). Sampler and instrumented dav2d copy agree to 1.6% on the total; the copy is output-identical and wall-neutral to +0.05%. | Closed as an investigation. **The 10 Mcyc line SCALE-034 set is retired**: at parity occupancy the 1%-of-wall stacking bar is 0.33 core-ms per frame, and SCALE-034's own addressable term is 7.6% of wall. Successors are SCALE-050, SCALE-051 and SCALE-052, in that order. |
| SCALE-050 | Completed; retention call open | `/root` + one sequential investigation/implementation agent | Close the prediction and motion-compensation kernel gap. | SCALE-049 | **The pre-measurement answered the row's own question: the touch counts are equal and the structure is identical, so the gap is lane width and code generation, not an extra pass.** Block-shape histogram of the marginal steady frame (`--limit=120` minus `--limit=60`, one worker): **9,851 warp 8x8 sections** and 3,535 two-pass sub-pel blocks per frame, the warp horizontal 87% uniform-column (`alpha == 0`) and 8% sheared, the warp vertical 88.5% uniform-column; sub-pel splits 564 copy, 483 horizontal-only, 1,537 vertical-only and ~951 two-axis, with 14,412 two-axis horizontal rows of which 32% take the clamped-edge path; compound splits 2,130 fused-fast, 1,691 clamped two-axis, 766 full-pel, 441 horizontal, 438 fused 2-D and 1,287 mixed-phase rejects. **Samples touched per output sample are equal on both sides**: warp is 15x8 horizontal taps plus 8x8 vertical taps per 8x8 section in both decoders (`build_intermediate`/`build_output` against `warp_filter_horz_neon` plus its vertical pass), and sub-pel is one `h + 7`-row separable intermediate in both (`subpel_predict_block_internal_into_validated` against `prep_8tap`/`put_8tap`); splot does not run a separable path where dav2d runs a fused one. **The code generation differs and one kernel was fixable in safe Rust.** SCALE-014's `i16`-lane narrowing never reached warp: `warp_predict_block_prepared_into::<u16>` disassembled to **0 `smlal`, 32 `mul`/`mla` and 48 widening `ushll`/`sshll`**, because `Simd::<u16, 8>::from_slice(..).cast::<i32>()` times an `i32` splat is a mixed zext/sext multiply the target cannot fold, while `build_output` — whose operands were already 16-bit — was already **32 `smlal`, 0 widening**. Committed in `c5abafe24`: `warp_source_lanes` reads the eight samples as `i16` (§ 6 Table 6.3 caps `BitDepth` at 10, so the reinterpretation preserves the value, the same argument the § 7.13.3.18 taps already ship) and `warp_tap_mac` sign-extends both operands, which folds the widening into the multiply-accumulate: the kernel is now **14 `smlal`, 16 `mul`/`mla`, 32 widening**, all of the remainder in the sheared-column fallback. **Measurements.** Byte-exact at 1/2/4/8/10 workers (186,624,000 B, SHA-256 `48f0dc14...`) and 1T = 10T over all 545 output frames. Marginal steady-frame CPU, the undiluted instrument: **1T 22.734 to 22.580 ms (-0.675%)** over five alternating reps and **10T 32.866 to 32.708 ms (-0.480%)** over seven, the same **-0.155 core-ms per frame** at both widths as a pure scalar reduction must be, which at parity occupancy is 0.0175 ms = **0.47% of the 3.733 ms steady wall**. A direct per-call phase timer over identical call counts put the warp kernel at 2.061 to 2.005 core-ms per frame at one worker, -2.75%. On the 30-frame benchmark the change is **null at every width and regresses nowhere**: 10T -0.043% (6/11) against a +0.109% A/A null, 8T +0.051% (5/11) against +0.167%, 1T -0.034% (6/11) against -0.005%, 2T +0.009% against +0.049%, 4T -1.417% on five noisy pairs against -0.134% — as a 0.47%-of-steady-wall change must be on a benchmark whose prologue is 30% of the wall and carries almost no warp. Bare `cargo xtask ci` exit 0. | Maintainer decision under the 2026-07-31 stacking policy: keep the commit for its clear marginal-CPU reduction and byte-exact strictly-smaller kernel, or drop it because no benchmark width is consistently positive. **Two rejected sub-candidates, do not retry.** (1) The pre-approved thread-local-scratch deletion is **refuted by direct measurement**: `McScratch::with_installed`'s whole swap pair is 0.011 core-ms per frame at one worker and **0.034 at ten**, thirty times under SCALE-049's 0.333 sampler attribution, which was the *other* thread-local takes inlined into its frame. (2) Vectorising the sheared-column (`alpha != 0`) warp horizontal with `build_output`'s transposed-tap shape **regressed** the warp kernel +1.04% against the same base that the uniform-column narrowing improved -2.75%: 15 rows of 8x8 tap transposition costs more than the per-output `addv` reduction LLVM already emits. **What remains is NEON-class hand-scheduled code generation, outside safe-Rust scalar reach**: dav2d computes eight warp outputs with eight *different* filter rows in 16 `smull` plus a 14-instruction `addp` tree over two `ld1` loads and seven `ext` windows, where splot's now-folded uniform-column case still needs eight overlapping unaligned loads for the same 16 `smlal`. **Re-opened and delivered by SCALE-058**: the closure assumed safe code cannot express `ext`, but `simd_swizzle!` over two vectors lowers to exactly `ext.16b`, and the uniform-column horizontal pass now emits one `ldp` plus seven `ext` for the same 14 `smlal`. The 1T-to-10T inflation of the MC stage is separately attributed and is **not** a kernel-arithmetic term: over identical per-frame call counts the pure kernel costs 2.00 core-ms at one worker, 2.84 as ten independent one-worker processes and 4.69 as one ten-worker decode, so **31% of the inflation is host and memory-system pressure and 69% is splot's own threading**, which is SCALE-034's software-stall term and not addressable here. |
| SCALE-051 | Committed; retention call open | `/root` + one sequential investigation/implementation agent | Delete the publication, filter-source-window and validation copies that dav2d has no analogue for. | SCALE-049; SCALE-035 | **Step 1 priced all three slices before any code, and only the validation one crosses the bar.** Env-gated census of the marginal steady frame (`--limit=120` minus `--limit=60`, ten threads; the frame is 3,110,400 samples), samples per frame: `validate_write_source` **2,180,139 over 10,549 calls** (207 samples a call), `validate_prediction_range` 236,171, chroma Wiener NS sources 265,937, `write_u16_rect` 512 and both PC-Wiener sites 0 — so the validation family is `validate_write_source` and nothing else on this stream; `CurrentFramePlane::new` zero fill **6,721,920 = 2.16x the whole frame** over 8.2 calls; `OwnedFrameRect` fill 3,058,560 and `publish_into` 2,436,480; and the `filters::source` family **14.35 M = 4.61x the frame** — stripe windows 4,991,872, CDEF seed 3,058,560, post-LR seed 1,779,840, LR padded windows 2,079,462, sealed copy 2,436,480. **Over-copy ratio, the row's own question: the stripe windows carry 1.60x the deblocked frame their filters consume**, and the whole 0.60x excess is `STRIPE_WINDOW_MARGIN = 16` plane rows duplicated between adjacent stripes (96 luma rows copied per 64-row stripe, 64 chroma per 32). Ablations, marginal steady CPU in core-ms per frame (10T median of 7, 1T of 5): validation off **-0.339 / -0.135**, every workspace fill off **-0.245 / -0.109**, margin 16 to 8 **-0.080 / +0.020**, margin 16 to 4 -0.186, all three together **-0.358 / -0.273**. **The joint ceiling is therefore 0.36 core-ms = 1.1% of steady wall, not the 2.05 this row projected**: the 1.10 stripe-window term is a borrow the architecture cannot give (K10.12/K10.32 closed segmented reference storage), so only its margin is reachable. **Shipped in `5d2d16b48`**, and it captures the whole validation family plus the freeze scan the ablation never covered: `u16_samples_exceed` accumulates a lane-wise peak and compares once instead of comparing and cross-lane-reducing every eight samples; `validate_write_source` takes the whole run in one pass when the source is tightly packed, which every `write_rect_block` caller is, so a 16x16 block runs one 256-sample scan instead of sixteen 16-sample scans; and `DecodedFrame::try_new`'s per-plane scalar `fold` over 3.11 M samples per frame shares the same scan, which is what took the two helpers out of `workspace.rs` into `sample_range.rs` — six modules already used them and the file was one edit from its 2500-line hard cap. Every public entry point still validates every sample before the first target write and every error is byte-identical, so **no fail-atomic boundary moves and K10.39's constraint is met without a witness type**. Marginal steady CPU **10T 32.667 to 32.332 (-0.335 core-ms, -1.026%) over seven pairs, 1T 22.286 to 22.121 (-0.165, -0.739%) over five**. Byte-exact at 1/2/4/8/10 workers (186,624,000 B, SHA-256 `48f0dc14...`) and 1T = 10T over all 545 output frames. Benchmark, eleven alternating pairs at 8T/10T and five/three below: 10T -0.054% (6/11) against a +0.137% A/A null, 8T **-0.635% (8/11)** against -0.114%, 4T -0.815% (5/5) against -0.575%, 2T -0.123% against +0.310%, **1T -0.375% (3/3) against a +0.008% null** — the expected size for a 1.0%-of-steady-wall change on a benchmark whose prologue is 30% of the wall, and no width regresses. Bare `cargo xtask ci` exit 0. | Maintainer decision under the 2026-07-31 stacking policy. **Two slices measured and rejected, do not rebuild them.** (1) **Zero fill**: the two full-frame fills are `FrameProgress::new` and the prepass shadow workspace, and splitting them at one worker prices them at **-0.028 and -0.056 core-ms per frame** (both together -0.088) — 0.08% and 0.17% of steady wall, at the resolution floor of the metric, and the shadow half is the nondeterministic-unwritten-sample shape SCALE-035 already flagged. `new_recycled` on the filtered workspace also needs a stripe-coverage guard the partition does not carry today. (2) **Window right-sizing**: margin 16 to 8 is **null at one worker (+0.020)** and only -0.080 at ten, so what little it moves is deblock-frontier lead and not copy volume; margins 12/10/8/6/4 are all byte-exact on this stream and margin 1 fails closed loudly, but the constant's own doc comment derives 8 from CDEF's two rows plus loop restoration's one plus eight for a tile end that is not stripe aligned, which does not obviously cover 10, and a stream this one does not contain would fail closed rather than corrupt. Reopen only with the exact per-filter row reach derived from the spec. **What remains of the 3.642 publication stage is 1.5 core-ms of rect and sealed-copy materialisation that SCALE-035 closed on its segmented-deblock control, 1.10 of stripe source windows that need a borrow the workspace cannot lend, and 0.5 of fills worth 0.09.** |
| SCALE-052 | Committed; retention call open | `/root` + one sequential investigation/implementation agent | Collapse the duplicated neighbour MV grid maintenance. | SCALE-049; SCALE-033 | Step 1's census answered the row's own question both ways: motion is recorded into one grid, flags into two, and the larger term is neither — it is the 72-byte per-cell record. Committed in the mission ledger's SCALE-052 row: lazy motion-plane sizing plus a 20-byte leaf-indexed motion cell, **marginal steady CPU 32.597 to 31.286 core-ms per frame at ten threads (-4.02%) and 22.255 to 21.784 at one (-2.12%)**, byte-exact at 1/2/4/8/10 workers and 1T = 10T over 545 frames, `cargo xtask ci` exit 0. | Re-measure after the next rebase and keep or drop on neutral confirmation. |
| SCALE-053 | Investigated; no source change | `/root` + one measurement agent | Re-measure the ground-truth matrix on exact `2f6a5d1fc` and rank the endgame for whatever widths remain lost. | SCALE-050; SCALE-051; SCALE-052 | Two 11-pair sweeps per width plus a 2/8/10T tiebreak: **splot wins at 1T (0.9173 / 0.9206x, 11/11) and 2T (0.9872 / 0.9916 / 0.9928x)** and loses at 3T 1.012-1.016x, 4T 1.016-1.019x, 8T 1.122-1.125x and 10T 1.119-1.134x. Marginal steady frame 31.216 against 25.749 core-ms at 8.900 against 8.935 cores; the one-worker steady gap is down to 2.0%, so 92% of the steady gap is parallel inflation. A `--limit` region split shows **every remaining benchmark loss is occupancy** and names coded frames 1-5 as the largest term at every lost width. Full evidence in the mission ledger. | Closed. **The next candidate is the first-pyramid fill**: raising its occupancy to dav2d's is worth -8.72 ms at 10T (to 1.055x), -6.83 at 8T (to 1.085x), -4.61 at 3T (parity) and -1.60 at 4T (1.005x), and it is not closed by SCALE-040/040-B/041, which bound the key frame only, nor by the depth family, which was measured on whole-benchmark medians the region is under 16% of. At 8T and 10T it must then stack with the steady CPU residual. |
| SCALE-054 | Rejected; no source change | `/root` + one sequential investigation agent | Raise occupancy across the first pyramid, coded frames 1-5, which SCALE-053 named the largest remaining term at every lost width. | SCALE-053 | An env-gated fill timeline at 3/8/10 workers attributes the region to the § 8.2 CDF chain: frames 3-7 each start their parse within 0.01-0.05 ms of their predecessor's parse end, on exact admission conditions, and the stream's single tile with `context_update_tile_id = 0` makes the end of the frame's only tile the earliest legal publication point. Forcing the fused walk over the first N frames, which delays CDF publication past reconstruction, costs +3.34 / +4.65 / +7.61% at N = 1 / 2 / 5. Entropy-context depth and recon-lane depth are both null-to-negative at eight workers, and the trough runs 1.0-4.3 of ten busy cores, so there is no queued work for priority to reorder. Full evidence in the mission ledger. | Closed. The successor with headroom is a within-frame row-granular parse-to-reconstruct handoff (ceiling -8.72 ms at 10T, -6.83 at 8T); it is a mission-scale rebuild and needs a maintainer decision before scoping. Do not re-measure depth, priority or fused-walk prefixes in this region. |
| SCALE-055 | Rejected; no source change | `/root` + one measurement agent | Price the within-frame row-granular parse-to-reconstruct handoff before scoping the rebuild SCALE-054 named. | SCALE-054 | **NO-GO against a go line of 4% of the benchmark wall at eight and ten workers, stated before the replay ran.** A wall-neutral env-gated trace (per-superblock-row parse crossings, per-batch reference needs named by producing coded frame, exact resolve motion-band sources, exact CDF producers, task intervals; six traces per width) drives a replay that reproduces the shipped structure's pool window, fill occupancy and steady period to -1.0/+2.6%, -1.4/+0.5% and -2.8/+3.4% at ten workers. The handoff prices **-2.04% at ten workers and -2.97% at eight** (-2.71% and -4.39% with a parse re-key bundled in), fill-only **-1.47% and -1.06%**, and an unreachable bound with no parse gate at all reaches only -3.28% at ten. It compresses the first pyramid from 6.42 to 7.12 of ten busy cores, 23% of the gap to dav2d's 9.46, because the pyramid becomes a diagonal wavefront: row `r` of frame `N` still needs `128 (r + 1) + 8` filtered luma rows and motion band `r` of frame `N-1`. Steady `k` does not rise at ten workers. Full evidence in the mission ledger. | Closed. **Do not scope the row-granular parse-to-reconstruct rebuild.** With SCALE-054 and SCALE-053 this closes the occupancy family at eight and ten workers; the ranked list begins at the steady CPU residual. |
| SCALE-056 | Rejected; no source change | `/root` + one sequential investigation agent | Decompose the fixed `--limit=1` startup cost per width on both decoders, name the width-dependent component, and remove it. | SCALE-053 | **The width dependence is dav2d's frame-context count, not splot's startup.** At `--framedelay 1` dav2d's `--limit=1` wall is flat at 17.0-19.8 ms from three workers up; the contract's `--framedelay max(T, 3)` adds +0.71 / +1.61 / +6.94 / +10.93 ms at 3/4/8/10T against splot's own +0.74 / +1.00 / +1.59 / +1.46, which is the entire 10T credit. Splot's pool build is 0.06-0.19 ms and is not a term; its real process cost is the whole-file read (1.13-1.34 ms), the whole-file container plan (0.61-0.97) and teardown at about 11 us per resident MB (1.83-6.26 ms at `--limit=30`). **Rejected on the 1.5 ms kill line**: the unreachable bound for read plus plan is -0.59 to -1.57 ms at 3T against A/A nulls up to -2.03, and the dependency-free subset predicts 0.9-1.4. Full evidence in the mission ledger. | Closed. Do not re-open the pool-spawn or per-worker-init reading. Two shapes need a maintainer decision before anyone builds them: a memory-mapped input (third-party dependency) and a limit-bounded incremental read (stops validating the stream past the limit). The sized successor is the footprint round trip, which is SCALE-011-G. **Discharged by SCALE-057**, which censused it and rejected it at its own bound. |
| SCALE-057 | Rejected; no source change | `/root` + one sequential investigation agent | Census the pooled scratch SCALE-056 sized, cut the hoarding half, and price the teardown and parallel-inflation channels it was suspected to drive. | SCALE-056; SCALE-011-G | **Killed at its own stated bound.** The eight process-global pools hold 23.74 / 62.07 / 68.81 / 82.66 / 86.04 MB at 1/2/4/8/10 workers, 16-27% of peak resident set; the rest is 34.7 MB per in-flight frame and 9.2 MB per worker. Dropping every pool returns 38.5 MB of peak RSS (0.42 ms of teardown, 0.24% of the ten-worker wall) and costs 0.728% of steady CPU, and a code-free depth sweep prices footprint at 0.4% of steady CPU per 36% of resident set. Full evidence in the mission ledger. | Closed with SCALE-011-G. The remaining ranked candidate is the steady CPU residual, not memory. |
| SCALE-058 | Committed; retention call open | `/root` + one sequential investigation/implementation agent | Reopen SCALE-050's `ext`-window closure and express dav2d's warp horizontal load shape in safe Rust. | SCALE-050 | **The closure was right about `core::arch` and wrong about `std::simd`.** Every intrinsic the `ext` design needs — `vextq_s16`, `vmull_s16`, `vmlal_s16`, `vmlal_high_s16`, `vget_low_s16`, `vaddvq_s32` — is a *safe* `fn` on nightly-2026-07-22, so the re-marking the delta predicted did happen, but each carries `#[target_feature(enable = "neon")]` and rustc refuses to honour the baseline: "the neon target feature being enabled in the build configuration does not remove the requirement to list it in `#[target_feature]`", even though `neon` is in `--print cfg` for this target. Annotating the helpers compiles their bodies under `forbid(unsafe_code)`, but every plain-safe caller then needs `unsafe`, so the whole `core::arch` route is closed. **The portable route is open and was not considered**: `simd_swizzle!(lo, hi, [t..t+8])` is safe, needs no attribute, and lowers to `ext.16b`. `warp_windows` builds the eight tap windows from two loads, and LLVM folds those into one `ldp q0, q1`, so the § 7.13.3.19 uniform-column horizontal pass goes from **eight overlapping unaligned loads (`ldur` at byte offsets 0,2,4,6,8,10,12,14) to `1 ldp + 7 ext`** for the same 14 `smlal` — dav2d's shape, one instruction better on the load. The § 7.13.3.19 arithmetic is untouched; only the load shape moves, so windows are value-identical. Admission reserves one extra column because the window reads one sample past the taps' own reach; sections at the final column fall to the clamped `build_intermediate`, which yields the same samples. Isolated kernel micro-bench on the SCALE-050 histogram (9,851 sections x 15 rows) **-8.55%**, with both shapes asserted bit-identical over 512 column positions. Byte-exact at 1/2/3/4/8/10 workers (186,624,000 B, SHA-256 `48f0dc14...`) and 1T = 10T over all 545 output frames. Marginal steady CPU, two-order averages that cancel slot bias by construction: **1T -0.060 and 10T -0.181 core-ms per frame**, both orders negative at both widths. The 30-frame benchmark is inside its own A/A null at every width and regresses nowhere: 10T +0.486% against a +0.398% null, 8T +0.236% against -0.401%, 1T +0.044%, 2T -0.313%, 3T +0.173%, 4T +0.070%. Bare `cargo xtask ci` exit 0. | Maintainer decision under the 2026-07-31 stacking policy: the marginal-CPU reduction is consistent in direction at both widths and nothing regresses, but it is a fifth of the 0.31 core-ms stacking bar and no benchmark width is consistently positive. **The shape does not transfer to sub-pel, do not retry it there**: SCALE-014's active-tap-span pruning makes the window base `tap_start` a runtime value, and `simd_swizzle!` needs constant indices, so selecting among constant windows by a runtime index spills the array — the same scalarisation that made a masked 16-lane load 35 `mov.h` in the step-0 probe. **SCALE-059 corrects the scope of this closure**: the full-span (`K = 8`) phase needs no select, because its `tap_start` is zero and every index is a literal, and that case is 74.6% of the stream's horizontal-filtered samples. |
| SCALE-059 | Committed; retention call open | `/root` + one sequential investigation/implementation agent | Sweep every remaining hot kernel for the constant-offset sliding-window loads SCALE-058's `simd_swizzle!` shape fits, and ship the ones that measure. | SCALE-058 | **The census produced a pricing rule that closes most of the family, and one site that the rule admits and that measured.** Rule: `K` overlapping windows over one contiguous span cost `2 loads + (K - 1) ext` against `K` loads, so the shape never lowers instruction count — it trades `K - 2` load-port operations for `K - 1` vector-ALU operations, and pays only when `K` is large and the kernel is load-port rather than ALU bound. **Census** (`objdump` overlapping-load ladders over the whole release binary, ranked by a 1T exclusive-leaf profile normalised to the 21.37 core-ms marginal steady frame): `pc_wiener::classify_grid_from_cache` 1.200 core-ms/frame — scalar feature-grid summation, 135 `ldr x` and 87 `str x` against 13 `ldr q`, not a window kernel; `wienerns_filter::filter_luma_segment_u16` 1.169 — runtime `(plus, minus)` pair offsets and runtime pair count, which is why K10.48 found it spill-free; `cdef_filter_block_padded_to_valid_stride` 1.095 plus `cdef_filter_block_chroma_pair` 0.594 — 13 windows per output row but at most 5 from any one source row (computed per direction: 13 loads become 8–10 loads plus 6–8 `ext`) against about 78 vector ALU operations per row, and the direction index is runtime so it would need an eight-way specialisation, which is K10.46's rejected shape; `warp_predict_block_prepared_into` 0.977 — shipped by SCALE-058; **sub-pel horizontal 0.711 at its largest single leaf — taken**; `deblock::filter_contiguous_edge` 0.526 — 81 `ldrh` plus 24 `ld1.h` lane inserts, a gather/transpose across a runtime `lane_stride`, not a contiguous window; `cdef_direction_padded` 0.396 — already 48 `ext.16b` and 36 `tbl.16b`; `ccso_stripe` 0.383 — runtime `±(dy, dx)` offsets, `K = 2` per span; GDF 0.550 across `compute_enabled_segment` and `gdf_uniform_width_rows` — its `dy = 0` row does carry 12 windows, which is the delta against K10.46 (that row closed *carried vector state across two rows*, not horizontal window reuse), but `tap_offsets` is a runtime `[usize; 18]` of `dy * stride + dx`, so it needs a per-`dy` restructure of the tap loop for a stage that is 0.848 core-ms total at ten workers and already 2.6x faster than dav2d's; `optflow::gradients` 0.247 — `K = 4` at constant `±1/±2`, ceiling about 0.02; `pc_wiener::filter_pc_wiener_padded_u16` 0.198 — runtime `pos_offsets`/`neg_offsets`; `intra_directional_angle` one-sided IDIF 0.074 — `K = 4` constant, under the bar and already closed by SCALE-015. **Blocker taxonomy: runtime tap offsets (Wiener-NS, PC-Wiener, GDF, CCSO), runtime direction (CDEF), low window multiplicity (CDEF, optflow, IDIF), and not-a-window shapes (deblock transpose, classify, publication copies).** **The site that survives is the § 7.13.3.18 horizontal pass, and SCALE-058's own closure of it was wrong in one specific way**: it is right that a runtime *select among* constant windows spills, but the full-span case needs no select. An env-gated probe over `--limit=120`, weighted by horizontal-filtered output samples (94.17 M samples, 784,750 per frame), puts the active tap span at `K = 8` for **74.6%** of them (K=6 13.6%, K=4 6.5%, K=5 3.1%, K=7 2.3%), and at `K = 8` the span is `[0, 8)` so `tap_start` is zero and every window index is a literal. Sites by share: `subpel_predict_block_internal_into_validated` 48%, `subpel_horizontal_only_into` 27%, `fused_compound_average_2d` 16%, `subpel_predict_block_compound_average_horizontal_validated` 9% — all four take the new path. **Isolated micro-bench** of the 8-lane row kernel against a **const-unrolled control that separates the swizzle from the unrolling**: K=8 **-30.98%** (and -38.26% against the shipped runtime-length loop), K=7 -29.08%, K=6 -26.08%, K=4 -18.06%. **Shipped in `dafb25174`.** A new `subpel_mc::slide` module implements one `SlideLanes` trait for `Simd<i32, {4,8,16}>`: two loads at the window origin and origin + LANES, seven `simd_swizzle!` slides, and a constant-index accumulate, plus an `admits` bound that is the whole safety argument — the second load reaches `SPAN` samples past the origin while the windows only use `NUM_TAPS - 1 + LANES`, so the extra lanes are loaded and discarded, and `admits` refuses the shape unless they are readable. The two window-slicing sites ask for `SLIDE_RESERVE = 9` extra samples and fall back to the taps' own reach when the plane cannot spare them; the two open-ended-slice sites check the remaining row directly. Every non-full-span phase, every 8-bit source and every row that fails `admits` keeps the existing pruned loop unchanged. A mutation-checked unit test asserts all three lane widths bit-identical to the overlapping-load sum at 24 origins x 8 taps x 16 lanes. Codegen: the u16 instantiations gain 13–27 `ext.16b` and their `smlal` counts rise 17 to 66, 6 to 27 and 43 to 85 as both paths are emitted. **Measurements** against frozen base `a6c9ecdb9` (binary `dc6c82f0...`), candidate `7aa1462f...` rebuilt after `cargo fmt` and hash-verified as the measured artifact. Byte-exact at 1/2/3/4/8/10 workers (186,624,000 B, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and 1T = 10T over all 545 output frames. Marginal steady CPU, two-order averages: **1T order A -0.075 and order B -0.094, mean -0.085 core-ms per frame (-0.386%)** against an A/A null of +0.039, reproduced at -0.084 in an independent session and -0.106 in a slot-matched four-way; **10T order A -0.033 and order B -0.091, mean -0.062**, against a 10T A/A null of -0.195 that bounds what that width can resolve. 30-frame benchmark paired medians: **1T -0.617% (11/11)** against a -0.025% null and -0.705% (5/5) in a second sweep, **2T -1.126% (11/11)** against -0.029%, 3T -0.420% (5/5), 4T -1.106% (5/5), **8T -1.332% (9/11)** against +0.277%, **10T -1.242% (8/11)** against -0.604%. Every width is negative and 1T/2T — the two widths splot already wins — are 11/11. Bare `cargo xtask ci` exit 0. | Retained under the 2026-07-31 stacking policy: consistently negative at all six widths, 11/11 at 1T and 2T, byte-exact, and the diff is one 190-line module plus four call sites. **The swizzle technique is now exhausted**: every other site in the census is closed on runtime offsets, runtime direction, or window multiplicity below the pricing rule's break-even, and re-opening any of them needs a restructure that separates constant column displacement from a runtime row stride — priced above at 0.02–0.09 core-ms per site. |
| SCALE-060 | Investigated; no source change | `/root` + one measurement agent | Re-measure the ground-truth matrix on exact `a17b3d7ed` after the swizzle family and declare a WIN/LOSS verdict per width. | SCALE-058; SCALE-059; SCALE-053 | Two 11-pair sweeps per width plus a third deciding sweep at three and four workers, on the shipped `7aa1462f...` release binary. **1T WIN 0.9135x (22/22), 2T WIN 0.9810x (22/22), 3T LOSS 1.0076x (1/33), 4T LOSS 1.0065x (0/33), 8T LOSS 1.1179x (0/22), 10T LOSS 1.1131x (0/22)**; every width moved 0.54-1.37 points toward parity, matching SCALE-059's paired medians. Sweeps never disagreed by more than 0.45 points, so the third sweep was run under the +-0.5% rule at 3T/4T rather than the disagreement rule. Residual gaps are +2.83 / +1.88 / +18.77 / +17.47 ms and are **entirely occupancy**: splot's total CPU is below dav2d's at 3T/4T/10T and only 9.6 core-ms above at 8T, while excess idle is 588 / 572 / 94 / 111% of the gaps. Full evidence and the closure trace for each cause in the mission ledger. | Closed as a measurement. Four widths remain lost with no open candidate: the occupancy family is closed by SCALE-053/054/055/056, the steady-CPU family by SCALE-049 through SCALE-052 and SCALE-057, and the kernel space by SCALE-059. Re-measure this matrix before any future retention call; never quote a single sweep at 3T or 4T as a verdict. |
| SCALE-061 | Rejected; no source change | `/root` + one sequential investigation agent | Re-lay out the per-tile CDF bank for access locality: census the access pattern, discriminate cross-worker cache sharing, and price a compaction. | SCALE-018; SCALE-034; SCALE-049 | **The step-1 kill line fired and two byte-exact layout controls then priced the whole family at zero.** Census (482,502 row accesses over `--limit=30`, 16,083 per frame): 95% of accesses land on 153 of the bank's 753 cache lines, 9.6 KiB, but spread over the whole 46.9 KiB at 20.4% density in 62 runs, and a 64-symbol window touches 36.6 rows totalling 332 B on 28.14 lines — 18.4% packing efficiency. **The discriminator refutes the mechanism**: 2x5T is neutral-to-*worse* than 1x10T on every stage (`block` 1.988 against 1.921 core-ms per frame) and 10x1T is *cheaper* (1.620), so the inflation scales with workers per decode process, not with CDF-bank sharing; the host column is uniform across `mv_stack` (+38.7%) and `mode_ctx` (+27.8%), which decode no CDF rows. **Controls**: a 2.4x spread (48,162 to 113,730 B) reads 10T -0.126% (6/11) and 1T -0.035%, and a wholly different `#[repr(C)]` field order reads 10T +0.368% (6/11) and 1T -0.049%, against a -0.240% (6/11) A/A null — one band, coin-flip counts, sign flipping between sweeps. Full census, clone accounting and ceiling in the mission ledger. | Closed. Do not retry CDF bank field ordering, cache-line alignment, hot/cold bank splitting, or clone narrowing. |
| SCALE-062 | Committed; retention call open | `/root` + one sequential implementation agent | Re-price the K10-era small ledger under 1:1 pricing, implement the survivors as one batch, and record each idea's fate. | SCALE-060; SCALE-061 | Committed in `c5abafe24`. A fresh 1T `/usr/bin/sample` profile calibrates 1 sample = 0.00712 core-ms per frame (cross-checked against SCALE-049's stage table on deblock, CDEF+CCSO and LR), making the 0.1 core-ms triage bar 14 samples. Of 24 parked ideas: 8 already discharged by SCALE-016/017/018/026/030/051 or by pool code that already ships, 2 built and rejected as K10.37/K10.42, 11 under bar, 1 (wider fused two-axis compound) out of scope on inspection, 1 (the LR coalescing sort key) built byte-exact and measured null with a sign flip, and 1 shipped — K10.40's static scan slices, which replace a per-transform-block `[u16; 1024]` build-and-move with a borrowed `&'static [u16]` from 16.1 KiB of `const fn` tables. Byte-exact at 1/2/3/4/8/10 workers and 1T = 10T over 545 frames; marginal steady CPU 1T -0.179 core-ms per frame in both orders and 10T -0.103; paired medians negative at all six widths and beyond the A/A null at each (1T -0.473% 5/5, 3T -0.965% 5/5, 8T -0.839% 10/11). Full evidence in the mission ledger. | Retention call under the 2026-07-31 stacking policy. The ledger is settled: do not re-price these rows without a fresh profile putting one above 14 samples. |
| SCALE-063 | Investigated; no source change | `/root` + one measurement agent | Re-measure the ground-truth matrix on exact `fefa2b9a4` after the K10 ledger settlement and declare a WIN/LOSS verdict per width. | SCALE-062; SCALE-060 | Two 11-pair sweeps per width at 1/2/8/10 workers and five at 3 and 4, on the shipped `aa60757131...` release binary behind a clean `cargo fmt --all -- --check`. **1T WIN 0.9113x (22/22), 2T WIN 0.9785x (22/22), 3T LOSS 1.0015x (17/55), 4T LOSS 1.0046x (7/55), 8T LOSS 1.1118x (0/22), 10T LOSS 1.1130x (0/22)**; every width moved 0.01-0.61 points toward parity, matching the direction of SCALE-062's paired medians at all six. 3T pooled inside +-0.3% after three sweeps, so it and 4T took a fourth and fifth to reach 55 pairs. Residual gaps are +0.57 / +1.31 / +17.80 / +17.41 ms and remain **entirely occupancy**: splot's CPU is below dav2d's at 3T/4T/10T and only 8.1 core-ms above at 8T, while excess idle is 47.1 / 44.4 / 134.3 / 193.3 core-ms. Full evidence in the mission ledger. | Closed as a measurement. Four widths remain lost with no open candidate; the occupancy family is closed by SCALE-053 through SCALE-056, the steady-CPU family by SCALE-049 through SCALE-052 plus SCALE-057, and the kernel space by SCALE-059 and SCALE-062. Re-measure before any future retention call; never quote a single sweep at 3T or 4T. |
| SCALE-064 | Committed; retention call open | `/root` + one sequential implementation agent | Implement SCALE-062's six under-bar K10 members as one bundle priced as a single candidate. | SCALE-062; SCALE-063 | Three of six built as separate byte-exact commits: intra-metadata default republication deleted (K10.41's shape, 24 of 27 bytes per MI cell), the IBP angular blend row-sliced with its zone-1 weight row hoisted plus a lazy IBP DC edge error, and CfL built directly instead of `resize`-then-re-read. Three skipped with witnesses: prevalidated LR windows are **obsolete**, since SCALE-051's single-pass `clean_rows` scan already is that shape and its consumer does not re-scan; coefficient-container movement is a 128-byte `NonZeroCoeffBlockStart` threaded by value through five nested `coeff_loop` geometry structs, needing a borrow refactor of the hottest parse path; the CDEF odd-direction alias is re-confirmed at dirs 1/3/5/7 but folding it multiplies the instantiations of an `inline(always)` kernel, which is K10.46's measured failure mode. Byte-exact at 1/2/3/4/8/10 workers and 1T equals 10T over 545 frames and equals the base. **1T marginal steady CPU -0.135 core-ms per frame (-0.626%), 9 of 9 reps, against a -0.014 null**; 30-frame paired medians -0.478 / -0.285 / -0.137 / -0.342 / -0.003 / -0.160% at 1/2/3/4/8/10T, each beyond or inside its own A/A null and none regressing. Bare `cargo xtask ci` exit 0. Full evidence in the mission ledger. | Maintainer retention call. **The marginal-CPU instrument is corrected here**: arms must interleave inside each rep, because SCALE-062's per-order sweeps gave this candidate the wrong sign at 1T against a null larger than the effect. The 3T flip is not demonstrated and the dav2d preview was not run. Do not re-open the remaining K10 members without a profile that moves one above 14 samples. |
| SCALE-065 | Investigated; no source change | `/root` + one measurement agent | Re-measure the ground-truth matrix on exact `ea71c8b85` after the SCALE-064 bundle and answer whether splot beats dav2d at three workers. | SCALE-063; SCALE-064 | **Answer: no.** One 11-pair sweep per width at 1/2/8/10 workers, two at 4 and five at 3, on the shipped `be713c756d...` release binary behind a clean `cargo fmt --all -- --check`. **1T WIN 0.9056x (11/11), 2T WIN 0.9784x (11/11), 3T LOSS 1.0017x (21/55), 4T LOSS 1.0035x (4/22), 8T LOSS 1.1126x (0/11), 10T LOSS 1.0955x (0/11)**; no width moved outside host drift, so the bundle's 3T preview did not reproduce against dav2d. The mandated 55 pairs resolve 3T only to +-0.15%, the width of the tie band itself, so 220 more pairs ran: pooled over 275 pairs the median is **+0.126% with a 95% bootstrap interval of [+0.03%, +0.18%], 110 of 275 faster, sign-test `p` = 0.001** — a tie on effect size and a decided loss on sign. Residual gaps are +0.60 / +1.00 / +17.90 / +14.80 ms and remain **entirely occupancy**: splot's CPU is below dav2d's at 3T/4T/10T and only 6.0 core-ms above at 8T, while excess idle is 48.8 / 47.0 / 137.2 / 176.0 core-ms. Full evidence in the mission ledger. | Closed as a measurement. Four widths remain lost with no open candidate. **Do not accept or reject a 3T candidate on 11 or 55 pairs unless its paired median exceeds 0.15%**; below that, run the 220-pair confirmation or report it undecided. Re-measure before any future retention call. |
| SCALE-066 | Committed; confirmed by SCALE-067 | `/root` + one sequential implementation agent | Re-price LTO release profiles on current main under the modern stacking bar and retain the best that clears it. | SCALE-011-K10.50; SCALE-065 | **Retained `lto = "fat"` plus `codegen-units = 1`; a `[profile.release]` change and nothing else.** All three variants (fat, thin, fat+cgu1) are byte-exact at 1/2/3/4/8/10 workers (186,624,000 B, SHA-256 `48f0dc14...`) with 545-frame reports identical at 1T and 10T and to the frozen base, so none is dead. Builds are 60.83 / 28.41 / 78.26 s against the base's 25.96, and binaries 8,257,248 / 9,754,528 / 7,891,184 B against 9,838,960. Screening at 1T/8T and a 7-pair head-to-head picked fat+cgu1. **Interleaved paired medians against a same-source A/A null: 1T -1.209% (11/11) vs +0.182%, 2T -1.317% (9/11) vs +0.378%, 3T -1.813% (32/33) vs -0.052%, 4T -1.319% (33/33) vs +0.042%, 8T -1.889% (31/33) vs -0.110%, 10T -1.803% (30/33) vs -0.158%**; the classic two-arm sweeps agree at every width. 3T needed no 220-pair confirmation, being an order of magnitude outside the +-0.15% band. Marginal steady CPU, interleaved, 9 reps: **1T -0.350 core-ms per frame (-1.633%) 9/9** against a +0.045 null and **10T -0.666 (-2.173%) 9/9** against +0.190. **A dav2d preview flips two widths: 3T 0.9862x (21/22, `p` = 0.00001) and 4T 0.9942x (19/22, `p` = 0.00086), so four of six widths are won**; 8T/10T move 1.50/0.50 points toward parity and stay lost. Local release build 26 s -> 78 s; `cargo xtask ci` builds no release profile and was unaffected, exit 0 in 68.5 s. Full evidence in the mission ledger. | Maintainer retention call under the 2026-07-31 stacking policy. **Re-measure the ground-truth matrix on the merged profile before the next retention call**: the 3T/4T flips are a preview at 22 pairs, not the mandated sweep. The transferable lesson is that a pre-2026-07-31 rejection decided on an under-10% margin is not evidence under the current bar and can be re-opened on its own numbers. |
| SCALE-067 | Investigated; no source change | `/root` + one measurement agent | Re-measure the official ground-truth matrix on exact `22e21b007` and confirm or drop SCALE-066's 22-pair 3T/4T preview. | SCALE-066; SCALE-065 | **Answer: confirmed.** Two 11-pair sweeps at 1/2/8/10 workers and five at 3 and 4, on the shipped `73d436e05b17...` fat-LTO binary behind a clean `cargo fmt --all -- --check`, after a throwaway warm sweep. **1T WIN 0.8953x (22/22), 2T WIN 0.9665x (22/22), 3T WIN 0.9871x (52/55), 4T WIN 0.9927x (45/55), 8T LOSS 1.0953x (0/22), 10T LOSS 1.0939x (0/22)**, with paired medians and 95% bootstrap intervals of -10.49% [-10.66, -10.27], -3.38% [-3.52, -3.00], -1.32% [-1.41, -1.10], -0.74% [-0.89, -0.57], +9.54% [+8.98, +9.83] and +9.02% [+8.69, +10.43]. SCALE-065's 220-pair rule was checked and not triggered: 3T and 4T land 8.8x and 4.9x outside the +-0.15% band and all ten sweeps at those widths read below parity. Splot's total CPU is now below dav2d's at **all six** widths and the 8T/10T excess idle, 15.8 and 19.0 core-ms per worker, is 103% and 130% of the whole wall gap. Full evidence in the mission ledger. | Closed as a measurement; it also discharges SCALE-066's re-measure action. **Four of six widths are won with no open candidate at the other two**; the remaining distance to the goal is the 8T/10T occupancy gap at about 1.09-1.10x, every named cause of which carries a closure row. Re-measure before any future retention call. |
| SCALE-068 | Investigated; no source change | `/root` + one measurement agent | Re-measure the 8T/10T loss decomposition on the merged fat-LTO profile, since every occupancy closure was priced pre-LTO, and either re-open exactly one family with a stated magnitude delta or reconfirm all of them. | SCALE-067; SCALE-047; SCALE-049; SCALE-053; SCALE-054; SCALE-055 | **Answer: reconfirmation, one magnitude delta.** On exact `96e1dbe50`, rebuilt to the shipped `73d436e05b17...` artifact. The `--limit` region split shows **LTO compressed every region except the fill**, whose share of the gap rose 58% to **79%** at ten workers and 42% to **62%** at eight (10T gap +17.17 to +13.69 ms, steady +7.21 to +7.00, fixed -3.98 to -6.60; 8T gap +20.20 to +15.20, steady +6.57 to +3.81). A task timeline on a byte-exact, wall-neutral binary explains it: the six parses of coded frames 2-7 are **100.0% back-to-back with zero slack**, and against a same-source plain-release build the chain moves only -4.3% where the benchmark moves -1.8% — the fill is latency-bound on the AV2 § 8.2 chain, so code generation cannot reach it. The fill's ceiling grew to 5.41% / 4.56% of the benchmark, re-pricing SCALE-055's replay to -2.19% / -3.54% and its best bundle to -2.91% / -5.23% against a 4%-at-both-widths line: **eight now crosses, ten never does**, and at the bundled price both widths still lose. Steady `C` fell ~10% while `P` held, so `k` fell to 1.37-1.53, back below SCALE-043's 1.58 line, and steady now runs 5.6-7.8% above its own work floor with the whole gap parallel inflation. Full evidence in the mission ledger. | Closed as a measurement. **Re-run the decomposition, not just the matrix, after any profile or toolchain change**: the closures held but the ranking's weights moved 20 points. No family re-opens; the two lost widths need the fill and the steady residual together and neither has a scoped successor. |
| SCALE-069 | Rejected; no source change | `/root` + one sequential implementation agent | Re-open the entropy family for parse **wall speed** on the fill chain — the closures priced symbol primitives under steady-state pricing, which does not cover a latency-bound chain where every parse millisecond is a wall millisecond — decompose a fill frame's parse against dav2d's, and take >=1 ms off each link. | SCALE-068; SCALE-054; SCALE-055; SCALE-049; SCALE-018; SCALE-026; SCALE-061 | **NO-GO on the stated line.** An independent light parse-span instrument reproduces SCALE-068 (chain 16.59-16.99 ms, frame 2 6.50-6.93 ms, successors starting 0.02 ms after their predecessor). Per-frame phase deltas with five temporary sub-phases decompose frame 2's parse: `intra_leaf` **50%**, its residual parse 39%, and the coefficient symbol walk only **9%** — **91% of a fill-frame parse is the code between symbol reads**, which is why SCALE-018/026/061 all read null. The 2.2x against dav2d is per-block object shape: dav2d bump-allocates coefficients from a per-tile arena and returns `int eob`, splot returns an 88-byte `LumaCoeffBlock` (pooled, whole-area-zeroed `Vec<i32>`, `Drop` through a global mutex) with a 96-byte error, wrapped in a 984-byte `ParsedResidualPlane`. One candidate was implemented, byte-exact and measured **null** (thread-local residual-plane recycler, -85/+20 lines, 0.037 ms ceiling); six more price at 0.02-0.06 ms each and **sum to about 0.45 ms against a 1 ms line**. Full decomposition, dav2d citations and per-candidate pricing in the mission ledger. | Closed. Do not re-open the entropy family for symbol primitives, CDF selection, coefficient-loop bounds checks, recycler locks or buffer zeroing under any pricing: each is measured at or below 0.1 ms of a fill link. Re-open only as a **per-block object-model rebuild** of the general-intra residual path — arena coefficients, out-pointer eob, no per-block owned residual value — scoped and priced like SCALE-055, and only with a stated reason it beats that row's strictly larger prize. |
| SCALE-070 | Rejected; no source change | `/root` + one investigation and pricing agent | Price the per-block object-model rebuild of the general-intra residual parse path — arena coefficients, out-pointer eob, no per-block owned residual value — the one successor SCALE-069 left open. | SCALE-069; SCALE-068; SCALE-055; SCALE-036 | **NO-GO on both halves of the stated line.** Go line, stated before measuring: >=1.0 ms off frame 2's parse link reproducing across traces **and** >=1.5% at ten workers on the screen. **Marginal-cost pricing** — each named object cost run eight extra times at its own site behind an env switch, `black_box`-fenced so the repeat cannot fold away, byte-exact at 1T/10T, delta divided by eight — puts the **probed family at 0.171 ms of a 6.78 ms link, 2.5% of it and 5.8x under the line**: zero-fill 0.059, the process-global quant-pool round trip 0.023, the two 984-byte `ParsedResidualPlane` moves a parsed plane makes 0.084 (0.042 each), and the 88-byte `LumaCoeffBlock` `Result` move 0.005 — raw, before the sweep's +0.050 A/A null, which would take the total to 0.147. The one removal expressible without threading an arena handle through the whole reconstruction path was **built byte-exact and regresses**: thread-local quant pool and plane-list recycler measure frame 2 +0.042 / +0.035 / +0.066 ms against a -0.006 A/A null, and 10T screens +0.467% (1/5) and +0.206% (1/5) against a +0.317% null — a negative result about worker-local recycling, not about a cross-worker arena handle, so the arena's verdict rests on the marginal-cost ceiling alone. **Two limits are recorded rather than papered over**: the probes bound move bandwidth and not the redesign's return ABI, construction or destruction, and the eight-repeat zero-fill is a warm cost that under-states the cold first fill. SCALE-069's decomposition contains that residue at 1.72 ms, and it would have to be worth 0.83 ms — 4.9x the probed family — to change the verdict. Full evidence in the mission ledger. | Closed. **With SCALE-053, SCALE-054, SCALE-055, SCALE-068 and SCALE-069 the first-pyramid fill family closes permanently**; it held the ledger's last named successor, so the two lost widths now have no candidate at any level. The re-open condition this row set — an ABI-faithful out-pointer probe plus a cold-buffer zero-fill control putting the unpriced slice above 0.83 ms — **was built and measured by SCALE-071 at 0.059 ms and is discharged**. |
| SCALE-071 | Rejected; no source change | `/root` + one measurement agent | Build the ABI-faithful probe SCALE-070's re-open condition names — an out-pointer `decode_general_intra_plane_coeffs` with no owned return value, plus a cold first zero-fill control — and price the slice that row could not. | SCALE-070; SCALE-069; SCALE-068 | **NO-GO; the gate does not fire, at 0.059 ms against its 0.83 ms line.** Go line stated before measuring. **(a) 0.000 ms.** A real out-pointer path (`decode_general_intra_plane_coeffs_into` plus twins of the staged and partition decoders) writes every field into a caller slot already in its final home and returns one register instead of a 104-byte `Result`; 23,087 of 28,767 decodes route through it, byte-exact, and frame 2 reads **+0.031 ms** over 40 interleaved reps against a -0.009 A/A null, with three traces at 6.503 versus 6.596. AAPCS64 already returns the `Result` indirectly through `x8`, so the shipped callee is *already* an out-pointer and only the caller's copy out of the slot was ever removable — the component SCALE-070 priced at 0.005 ms. **(b) 0.059 ms.** Filling eight *distinct* pool buffers at the pool's real temperature, with the extra fills counted exactly (66,488 against 27,620 real, 2.407x), puts the whole in-situ fill of the `level`, `quant_sign` and `quant` grids at 0.045 ms raw and **0.059 null-corrected** — SCALE-070's warm figure, reproduced by an instrument built to disagree with it. A DRAM-cold ring (21 MB circulating) costs +0.714/+0.775 ms, but the same ring at resident depth reads +0.021, equal to the shipped pool, so the shipped fill is 0.71 ms cheaper than cold and the cold arm prices a machine this decoder does not run. Even charging it whole, (a)+(b) = 0.806 ms stays under the line. All arms byte-exact at 1T/10T (186,624,000 B, SHA-256 `48f0dc14...`). Full evidence in the mission ledger. | Closed. **The first-pyramid fill family is now closed permanently at every level**: this row discharges SCALE-070's re-open condition on its own terms and it was the ledger's last one. Do not re-open the per-block object model, the coefficient arena, the out-pointer return, or the zero-fill under any pricing. |
| SCALE-073 | Rejected; fully reverted | `/root` + one sequential investigation/implementation agent | Price the fill link's CDF-independent prologue, never measured on a fill link, and hoist it off the § 8.2 chain into the trough's idle cores. | SCALE-068; SCALE-069; SCALE-041 | **Step 1 passed at 2.2x and step 2 was then falsified by its own wall measurement.** Per link the § 8.2 CDF work is 0.027 ms (1.0% of the chain) and the header-sized construction is 0.876 ms (33%), split workspace 62% / `TileParser::new` 31% / filter and motion state 8%. A conditionless prep job byte-exactly removed the workspace term from the chain and cut the chain span 16.100 to 12.743 ms (-20.9%) with pass `row` unchanged, but 11 pairs at each lost width read inside their A/A nulls (10T -0.085% vs -0.137%, 8T +0.254% vs -0.235%), the `--limit` sweep is flat in every region, and the parse lead decays to zero within three frames because `InflightRing::reserve` paces admission by frame completion. Full evidence in the mission ledger. | Closed, and it closes the chain-side family with it: the fill chain's *length* converts to wall at a rate this measured at zero, so do not re-open the parse chain for speed — SCALE-069, SCALE-070 and SCALE-071 all rested on the same premise. Re-open only behind evidence that frame completion, not the ring, paces admission. |
| SCALE-074 | Investigated; no source change | `/root` + one measurement agent | Answer the maintainer's question after `#1183` was closed and safe-portable-only became policy: on the hottest portable kernel, would `wide` or `pulp` change anything against nightly `std::simd`? | SCALE-050; SCALE-058; SCALE-059; `#1183` | **No on aarch64.** Six reimplementations of the § 7.13.3.18 full-span horizontal row in an out-of-tree scratch crate on the shipped release profile, all bit-exact against a scalar reference, 21 rotated alternating reps and two sweeps. Ratios to the shipped `simd_swizzle!` slide at w=8/16/32/64: `wide` **1.30-1.58x, 0 of 21 pairs faster anywhere**; `pulp` portable **2.47-6.13x, 0 of 21**; `pulp`'s aarch64 arch API 0.92-0.99x, 15-21 of 21. Codegen: the shipped slide is `1 ldp + 7 ext.16b + 16 smlal + 2 rshrn` per eight outputs; `wide` has **no cross-vector shuffle at all** so the slide is inexpressible, and `pulp`'s portable trait emits **zero `smlal`** because it has no widening load, no 32-bit-keeping i16 multiply, no arithmetic i32 shift and no narrowing store. The pruned runtime-`tap_start` span reproduces `#1183`'s -32.88% independently at 0.71-0.98x, and `#1183` already measured that kernel integrated at null. Posture: `wide` 2 crates / 36.4k lines, `pulp` 8 crates / 73.7k lines / ~2,092 `unsafe` mentions, the intrinsic wrappers being `core_arch`'s `delegate!` macro. x86: splot already ships `-C target-cpu=x86-64-v3`; `wide` has no runtime dispatch at all, and `pulp`'s x86 `Arch` falls to **`Scalar`** below AVX2, not SSE2. Full evidence in the mission ledger. | Closed as a measurement. **Do not re-open `wide` or `pulp` for the decode kernels.** Re-opening needs a maintainer decision that a dependency-hosted `unsafe` is acceptable where an in-tree one is not, or an x86 host on which the single-binary pre-AVX2-plus-AVX-512 scenario is actually splot's. |
| SCALE-075 | Completed | `/root` + one sequential implementation agent | Hoist the sub-pel vertical-pass clamp off the tap loop via an interior fast path. | SCALE-059; 2026-08-02 codegen audit | An interior fast path takes over when the whole `h + 7` tap-row window is provably inside the clip bounds and the plane, where the row clamp is the identity and tap rows sit exactly one stride apart: eight row slices bound once per chunk, a const 8-tap loop, taps as lane-indexed operands. Codegen 21 to 4 instructions per tap with the 16 widening MACs per 8 outputs unchanged and zero per-tap bounds or clamp work; the clipped/edge walk is untouched and a 1386-case equivalence test covers all clamp regimes at 8- and 10-bit. Byte-exact 30-frame raw at 1/2/4/8/10 workers (SHA-256 `48f0dc14...`) and `--limit=285` hash equality at 1T/10T. 285-frame paired CPU/frame -0.27 to -0.43 core-ms across 1/2/4/8/10/12 workers, sign-consistent (13/13 at 8T and 12T); the 30-frame wall is inside noise, as a ~1.2% CPU change must be. Bare `cargo xtask ci` exit 0. | Retained under the 2026-07-31 stacking policy. The edge/clipped fallback keeps the original shape; a 16-lane arm is a separate candidate. |
| SCALE-076 | Completed | `/root` + one sequential implementation agent | Pair both multiply operands as i16 across the loop-restoration filter family so LLVM folds widening into `smlal`. | SCALE-075; SCALE-014/050 pattern | The Wiener-NS luma and chroma filters and the PC-Wiener filter emitted widen-then-`mla` with zero `smlal`. Pairing lands `add.8h`/`sub.8h` plus `smlal`/`smlal2` (ranges: `plus+minus <= 2046`, `plus+minus-2*center` and `tap-center` inside i16 at BitDepth <= 10): `filter_pc_wiener_padded_u16` 3216 to 2213 instructions with rolled lane groups and zero per-tap bounds checks, `filter_chroma_padded_u16` 1536 to 1233 with zero `mla`/widening ops, `filter_luma_segment_u16` tap body 39 to 35 spill-free (K10.48 held). Byte-exact 30-frame raw at 1/2/4/8/10 workers (SHA-256 `48f0dc14...`) and full-stream hash equality at 1T/12T; three mutation-checked equivalence tests. Paired `--limit=1000` CPU/frame: 1T -0.121 core-ms median (9/9), 8T -0.123 (5/5), 12T inside noise; 30-frame wall negative at all six widths. Bare `cargo xtask ci` exit 0. | Retained under the 2026-07-31 stacking policy. Named successor: hoist the luma kernel's per-tap slice bounds checks via row-level tap slices (~0.2 core-ms ceiling), a separate candidate. |
| SCALE-077 | Completed | `/root` + one sequential implementation agent | Hoist the PC-Wiener classify grid's bounds checks and checked geometry to row level. | SCALE-076; 2026-08-02 codegen audit | The classify symbol spent ~40% of its feature-grid loop on per-chunk bounds branches (26 of 111 instructions) and a 47-instruction checked-geometry prologue per cell. Row-level pre-sliced windows and one per-row span validation cut the 16-column loop 111 to 80 instructions (4 bounds ops) and the cell loop 118 to 69 (one GVN-merged check); `finish`, `get_qval*`, the tx-skip run fill and the 12-sample u16 partials are untouched. Span validation now fires once per feature row with the same fail-closed `BufferLengthMismatch` (the retired per-cell arms are unreachable: `build_feature_grid` sizes the grid to exactly `geo.feature_count`, verified algebraically). Byte-exact 30-frame raw at 1/2/4/8/10 workers and full-stream frame-hash equality at 1T/12T; 200-shape mutation-checked oracle test. Paired `--limit=1000` CPU/frame: 1T -0.181 core-ms (9/9), 8T -0.167 (5/5), 12T -0.202 (7/7), 2T/4T/10T 5/5 each. Bare `cargo xtask ci` exit 0. | Retained under the 2026-07-31 stacking policy. `Vec::push` (6 insns/cell) deliberately kept — safe Rust has no capacity-eliding append and overwrite semantics would change the generic contract. |
| SCALE-079 | Rejected; fully reverted | `/root` + one sequential implementation agent | Test SCALE-075's named 16-lane vertical-interior sub-pel successor. | SCALE-075; 2026-08-03 safe-codegen audit | The minimal +13/-1-line candidate ran 16-wide chunks before the existing 8/4/scalar tails and passed the 1,386-shape vertical oracle plus compound tests at 8/10 bit. The clipped-u16 monomorph reached the intended shape: one back-edge per 16 outputs, paired loads/stores, unchanged widening arithmetic and no spills. The compound-u16 monomorph failed the mandatory assembly gate: five new vector stack reloads landed inside every 16-output iteration, the frame grew 576 to 768 bytes, and the function grew 5,932 to 6,748 bytes with extra callee-saved vector state. No benchmark was run; source and release artifact were restored exactly to base SHA-256 `dc4257bd...`. | Closed. A 16-lane portable vector is software unrolling on NEON, not wider hardware; do not retry this whole-output arm without a changed register-pressure premise. |
| SCALE-080 | Completed | `/root` + one sequential implementation agent | Pre-slice optical-flow preparation and gradients, and reduce predictor range validation once per prepared block. | SCALE-059; 2026-08-03 safe-codegen audit | One-module safe-Rust change, production +208/-129 and tests +58, on exact base `07f1719ac` (`dc4257bd...` binary; candidate `48444a5f...`). General derivation falls 477 to 344 instructions; gradients 631 to 554 with its frame 288 to 96 bytes, bounds calls 28 to 13 and no vector stack traffic; the TIP caller falls 702 to 543. A deliberately non-inlined strided-row helper avoids the spills of the rejected inline shape. Fifteen rotated direct pairs: strided 8x8 209.577 to 161.637 ns (-22.89%, every pair), general 16x16/unit-8 626.449 to 380.349 ns (-39.28%), general 32x16/unit-4 2,044.098 to 1,560.079 ns (-23.70%), against a +0.038% A/A null. Error priority and arithmetic match exact base across 3,250 public-API cases. Raw 30-frame output is 186,624,000 bytes and SHA-256 `48f0dc14...` at 1/2/3/4/8/10 workers. Interleaved 285-frame CPU deltas are 1T -0.1053 core-ms/frame (8/9, null 0.0000), 4T -0.1053 (9/9, null +0.0351), 10T -0.0702 (6/9, null 0.0000). Eleven-pair 30-frame wall versus exact base/null: 1T -0.393%/+0.051%, 2T -0.371%/+0.475%, 3T -0.087%/+0.047%, 4T -0.178%/-0.241%, 8T -0.508%/-0.245%, 10T -0.784%/+0.402%; no width regresses beyond its null. Retained splot/dav2d ratios are 0.8751/0.9540/0.9738/0.9785/1.0853/1.0721 at 1/2/3/4/8/10 workers. `rust-simplify`, 246-vector conformance, 240-fixture AVM-hash oracle, duplication 2,587/2,619, and bare `cargo xtask ci` pass. | Retained under the 2026-07-31 stacking policy as a direct and marginal-CPU small win. Preserve the cold scalar error rescan, logical sample/predictor priority, fixed 16-column gradient boundaries, and the measured non-inline boundary; solve-unit codegen was unchanged and is not part of this row. |
| SCALE-083 | Completed | `/root` | Restore independent precompute fanout for zero-copy reconstruction bands and order progressive-reference lock acquisition. | SCALE-011-B3; SCALE-011-D2/E; SCALE-082 | Profiling the exact 6,481-frame command showed the scaling miss is 68% idle/critical-path loss and 32% parallel CPU inflation, not ordinary mutex occupancy. The owned-band scheduler unnecessarily fused each independent precompute with its ordered commit; splitting those existing nodes improves every five-pair 10T `--limit=1000` run from 4.13-4.16 s to 3.93-3.96 s while 5T moves only 5.75-5.76 s to 5.67-5.68 s, so the gain is scaling-specific. Stress also exposed a frozen-base deadlock: eight workers held or waited for progressive-reference read locks while two terminal filter publishers waited for write locks. Compound and TIP pairs now acquire distinct live references by one progress-lock address order and share one borrow for aliased progress. Twenty candidate stress runs complete with no alarm; the full command runs in 27.57-28.64 s, with the final adjacent pair 29.55 s base versus 28.26 s candidate. The all-6,481-frame report is identical (SHA-256 `45ae6faf...`), normalized 1T/10T 1,000-frame reports are identical (`8b7d912d...`), and full `cargo xtask ci` passes. | Retained. Keep independent precompute plus ordered commit for both owned and legacy surfaces. No pool task may acquire a second progressive reference out of the canonical order. The 20-second target remains open; the refreshed profile ranks dependency-gated idle time first, then a fully attributed copy bucket whose largest family is still below the required remaining gain. |
| SCALE-084 | Completed | `/root` | Profile the exact 5T-to-10T scaling miss after SCALE-083 and remove its largest newly exposed serial work. | SCALE-083; `DECODE-INTER-TIP-REFERENCE` | Full-command/task/sample profiles refute a dominant-lock hypothesis: late steady-state runs about 7.65 top-level tasks, while sampled worker sleep is much larger than any project lock family. Stage CPU inflation from 5T to 10T is led by CDEF +18.1%, LR +12.5%, deblock +20.4%, TIP prediction +16.3% and TIP motion +19.2%; the host is an M2 Max with 8 performance plus 4 efficiency cores, so ten workers cannot have ten homogeneous cores. TIP census found 14 full-frame 32,400-cell grids among the first 1,000 frames. Parallel complete-row motion refinement preserves horizontal reuse without copying the unit list, and parallel complete prediction rows preserve the fused kernels; isolated full timing saves 128 ms in TIP motion and 244 ms end to end. Exact raw `/dev/null` output now resolves displayed frames and film-grain effects but skips sample-byte materialization, worth about 0.09 s per 1,000 frames. Combined repeated 1,000-frame pairs improve about 5%; full adjacent SCALE-083/candidate pairs improve 27.60 to 26.65 s, and the final adjacent 5T/10T candidate pair is 39.48/26.62 s (1.48x versus the original 1.39x), with 10T user CPU still 13.9% higher. The all-6,481-frame report is identical (SHA-256 `45ae6faf...`), normalized 1T/10T 1,000-frame reports are identical (`cfea584d...`), ten stress runs complete, and full `cargo xtask ci` passes. Two measured no-gos were fully reverted: shortening `FrameProgress` write-lock occupancy was neutral, while parallelizing the common 256-cell TIP grids made a warm 1,000-frame run exceed 60 s. | Retain only full-frame TIP row parallelism and exact `/dev/null` raw discard. Do not lower the TIP threshold or revisit stripe-lock shortening without a new profile. The 20-second target remains open: current CPU work alone has a 21.14 s homogeneous-ten-core floor before M2 efficiency-core and dependency-idle costs. |
| SCALE-085 | Completed | `/root` | Stop output-only TIP frames from synchronously blocking frame admission on their pixel-reference barrier. | SCALE-084; `DECODE-INTER-TIP-REFERENCE` | TIP output reconstruction is now an ordinary dependency-gated admission job with the existing bounded reconstruction scratch lane; its pending frame slot and CDF/CCSO/motion products settle from that job, so the driver can keep admitting independent entropy and reconstruction work. Every sample, entropy, and motion product the job reads is an admission condition, and the rare fused inter path assists the pool until selected motion fields are terminal; the oracle caught and the final design fixes the incomplete first version. Six of six warm 1,000-frame pairs improve 3.77 to 3.69 s median (~2.1%), while full 10T pairs save about 0.25 s. Final exact-command timing is 39.34 s at 5T and 26.31 s at 10T (1.50x); instructions are nearly flat at 3.163T/3.186T while cycles rise 614.2B/677.9B and involuntary context switches 77,514/472,080, confirming broad heterogeneous-core/coordination inflation rather than a dominant lock or copy. Mutex waits remain under 1% and sampled `memmove` under 3%. The all-6,481-frame report is byte-identical to SCALE-084 (SHA-256 `45ae6faf...`), normalized 1T/10T 1,000-frame reports match (`8b7d912d...`), the decoder oracle and ten stress runs pass. Filter concurrency caps, finer reconstruction batches, frame-delay changes, native CPU codegen, Wiener slicing, TIP motion coalescing, and serial TIP deblocking were measured and rejected or had zero stream coverage. | Retain the dependency-gated TIP output job. The 20-second target remains open; reaching it now needs a large scalar-work reduction or a different heterogeneous-core execution design, not mutex or memcpy cleanup. |
| SCALE-089 | Completed | `/root` | Remove the largest newly profiled serialized compound-blend work after SCALE-088. | SCALE-088; `DECODE-INTER-COMPOUND-AVERAGE`; `DECODE-INTER-TIP-REFERENCE` | A 20-second 1 ms Time Profiler trace contains 166,197 running-thread samples, only 8.15 active cores on average, and 45 low-occupancy 10 ms bins dominated by `predict_compound_from_grid`; the hot non-uniform implicit-mask loop repeatedly ran 9-11 ms while whole-process occupancy was about one core. Traversing by motion-grid cell now resolves one motion cell per tile instead of per pixel and removes per-sample row division/modulus. Its leaf falls from 1.71% to 0.93% in the refreshed trace and its low-occupancy dominance falls from 45 bins to 10. Seven of seven 1,000-frame pairs improve 3.64 to 3.60 s median; three alternating all-frame pairs improve 26.02 to 25.52 s median (-1.92%) and user CPU 208.13 to 206.49 s. The all-6,481-frame report remains byte-identical (SHA-256 `45ae6faf...`), focused inter tests pass, and `rust-simplify` found no removable layer. Nested Rayon at 128/256-square thresholds was neutral or catastrophically slower, an unscaled coordinate hoist was neutral-to-worse, and overlapping Y/U/V parallel phases were neutral with slightly higher CPU; all were fully reverted. | Retain only the motion-cell traversal. The refreshed profile ranks fused compound prediction, CDEF/GDF, restoration, warp and temporal projection as the next CPU families; aggregate `memmove` is 6.79%, but its largest individual caller remains about 1.1%, so copy cleanup is not a target-scale first move. |
| SCALE-090 | Completed | `/root` | Remove the artificial frame-concurrency and buffer-retention caps so both scale with the resolved thread/frame-delay width. | SCALE-011-A1; SCALE-011-A2; SCALE-083 | **Maintainer decision on 2026-08-14: accept a small regression to remove the artificial caps.** Four constants did not scale with the pool: `ReconAdmissionLane::new(ring.capacity().min(4))`, two `prepare_entropy_submission(.., ring.capacity().min(3), ..)` sites, `MAX_RETAINED_DEBLOCK_GRIDS = 4` and `MAX_RETAINED_NEIGHBOUR_MV_GRIDS = 8`. The lane and entropy-context bounds now follow `ring.capacity()` (the resolved frame delay, `auto` = threads) and both retention pools follow `current_pool_width()` floored at their old values, so no width below the floor changes. Byte-exact against `aedeb69a9` at 1/2/4/8/10 workers on `test.ivf --limit=400` (frame hashes identical across every width once the `selected_thread_policy` metadata line is excluded). **Priced before the decision and recorded rather than papered over**: on `--limit=1000`, 7 rotated pairs per width, the full change is **8T +0.71% [+0.25, +1.04] (1/7 faster), 4T +0.68%, 10T +0.42%, 2T +0.02%** — the 8T interval excludes zero, so the regression is real. Isolation A/B (9 pairs) shows the two retention pools alone are free or better (**8T -0.21% (5/9), 10T -0.64% (7/9)**), so the whole cost sits in the `mod.rs` concurrency bounds. Standalone sweeps agree and show no width is better than the old values: lane cap 4/6/8/10/12 = 3.9432/3.9472/3.9684/3.9524/3.9525 s and entropy contexts 3/4/6/8/12 = 3.9619/3.9570/3.9432/3.9632/3.9760 s at 8T, all inside a +-0.75% A/A floor. **Neither cap was ever the 8T bottleneck**: an admission trace shows the entropy-parse span is unchanged at 1.854/1.855/1.856 s for frame-delay 8/16/24 and 1.849 s with the lane cap at 12, with blocked-parse peak pinned at 3 (the `min(3)`) until the cap is lifted, then 7 with no wall gain. | Closed as an accepted-cost cleanup. **Do not re-tune these four constants for speed** — the sweeps cover 4-12 and none beats the floor. The 8T occupancy work continues at SCALE-091. |
| SCALE-012 | Open | Unassigned | Make deferred finish reporting happen-before in-flight harvest. | Keep separate from SCALE-011-D | `FrameSlotWriter` may settle before `FinishOutcome` publishes records or the filter error, allowing harvest to observe an empty outcome. | Cover success, failure, and exactly-once settlement; pass focused in-flight tests and CI. |
| SCALE-010 | Active; SCALE-081/082/083/084/085/087/088/089 retained | `/root` | Reduce scalar CPU work after the scaling barriers move. | SCALE-011-I architecture measurement; SCALE-080; `DECODE-INTER-COMPOUND-AVERAGE`; `DECODE-INTER-TIP-REFERENCE` | The 10-thread target still requires a large scalar reduction. Child SCALE-081 pre-slices the optical-flow least-squares unit into row chunks: `solve_unit` shrinks 4,180 to 2,844 AArch64 bytes with three outer 8x8 range checks, a 48-byte stack, and no spills. A bit-identical 5-million-call public derivation benchmark improves 132.667 to 132.027 ns/call (-0.48%, 19/21 rotated pairs); 5T is neutral-to-lower, 1T has no repeatable regression, and the 10T integrated screen is inside wall noise. Candidate/base reports match all 6,481 output frames at 10T and normalized 1T/10T reports match 1,000 frames. Focused tests/clippy and the zero-growth full `cargo xtask ci` pass. Child SCALE-082 walks only recorded trajectory phases and inlines the measured position write: `check_intersection` shrinks 349 to 332 AArch64 instructions, removes its only call, and cuts the stack frame from 272 to 128 bytes. On the exact final artifact, `--limit=1000` paired user CPU improves by 0.18 s at 10T (8/9) and 0.14 s at 5T (6/7), with paired wall medians -0.02 s and -0.03 s; five 1T `--limit=285` pairs improve by 0.02 s median. Its report matches SCALE-081 across all 6,481 outputs at 10T and normalized 1T/10T reports match 1,000 outputs. Child SCALE-083 removes an owned-band scheduling serialization and the progressive-reference lock cycle it exposed, with repeated 10T pairs about 5% faster and the full command observed as low as 27.57 s. Child SCALE-084 parallelizes only the giant full-frame TIP motion/prediction grids and removes raw byte materialization for exact `/dev/null` output, improving the immediate full 10T base by about 0.95 s and measured 5T-to-10T scaling to 1.48x. Child SCALE-085 admits output TIP frames behind reference conditions instead of blocking the driver, saving another repeatable ~0.25 s on the full 10T command. A fresh 8T/10T counter matrix shows the two E-core workers buy only 3.9% wall while cycles rise 8.0% and involuntary switches 2.2x; a 16-to-12-byte projected temporal-cell packing then measured +0.043% over nine high-resolution pairs and was fully reverted, closing padding/copy cleanup as a target-scale route. Child SCALE-087 profiles the fused compound-MC body to its clipped two-axis branch, hoists invariant horizontal edge geometry out of every working row, and replaces per-sample clamps with one contiguous span plus edge fills: instructions fall 0.66%, 1T/5T/10T paired medians improve 0.47%/0.54%/0.36%, all three full 10T pairs improve with a 26.334-to-26.199-second median, and all 6,481 reports remain byte-identical. Child SCALE-088 hoists the clipped warp kernel's 15-sample source window and replaces repeated per-tap clamping and addressing with one contiguous span plus edge fills per row: instructions/cycles fall 1.21%/1.03%, 1T/5T/10T 1,000-frame medians improve 1.31%/0.78%/0.40%, all three full 10T pairs improve with a 26.196-to-26.033-second median, and all 6,481 reports remain byte-identical. Child SCALE-089 removes per-pixel motion-grid lookup and row division from the newly isolated non-uniform implicit blend; its 45 dominant low-occupancy bins fall to 10, and all-frame 10T median improves 26.02 to 25.52 s with unchanged output. Frame delays 14 through 30 were neutral or worse and were rejected. | Retained measured wins; continue re-ranking hotspots toward the unchanged 20-second goal. |

## Attachment reconciliation

SCALE-003 and SCALE-004 rejected only standalone POCs — a whole-frame
synchronous entropy admission and a bounded schedule around shadow surfaces —
not the architectural tasks. Both were delivered as `SCALE-011-A` and
`SCALE-011-B` through the cohesive SCALE-011-H row graph in `#1140`; their
standalone variants remain evidence against queue-only or context-depth tuning.
