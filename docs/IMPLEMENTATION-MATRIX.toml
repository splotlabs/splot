# Canonical AV2 implementation matrix for `splot`.
#
# This file is the SINGLE SOURCE OF TRUTH for what is implemented and how far.
# GitHub Issues/Projects and README checklists are NOT canonical.
#
# Schema, status model, and proof rules are enforced by:
#                                       cargo xtask check-feature-status
# Render it:                              cargo xtask feature-status
# Check it for drift:                     cargo xtask check-feature-status
# Summarize coverage:                     cargo xtask spec-coverage
#
# Status values: todo | partial | done | blocked | not-applicable | experimental | pending
# A code stage may be "done" only when [feature.proof] records proof (see schema).
# Do NOT use percentages.

matrix_version = 1
last_reviewed = "2026-06-15"

# ---------------------------------------------------------------------------
# Normative AV2 bitstream syntax and semantics
# ---------------------------------------------------------------------------

[[feature]]
id = "AV2-4.11.6-LEB128"
name = "LEB128 descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/leb128.rs"
change_id = "parse-annexb-and-obu-headers"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "AV2 LEB128 is byte-aligned, up to 8 bytes, bounded to u32, and permits non-minimal encodings. §4.11.6 states exactly two requirements of bitstream conformance (docs/spec/av2/1.0.0/04-conventions.md#s-4-11-6, lines 657-664): the returned value is <= (1 << 32) - 1, and the MSB of leb128_byte is 0 when i == 7. read_leb128 (crates/splot-core/src/leb128.rs) enforces both as Error::InvalidLeb128, which the validator converts into bitstream/parse-error (crates/splot-validate/src/validator/diagnostics.rs::parse_error_diagnostic). No LEB128-specific rule_id remains because the spec carries no further locally-decidable constraint."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/leb128.rs::tests", "crates/splot-validate/src/validator/tests/public_api.rs::parse_error_becomes_a_single_error_diagnostic", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core leb128", "cargo test -p splot-core write"]
fixtures = []
diagnostics = ["bitstream/parse-error"]

[[feature]]
id = "AV2-4.11.3-UVLC"
name = "Unsigned variable-length code descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Implemented as BitReader::read_uvlc() from AV2 §4.11.3. AV2 treats leadingZeros >= 32 as a conformance violation rather than returning the AV1 sentinel; the parser rejects EOF and the AV2 bound without panics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "DECODE-LOSSLESS-LUMA-PREDICTION"
name = "Lossless luma prediction without mode allowlist"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.5", "7.13.2.1", "7.13.2.2", "7.13.2.7", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-lossless-luma-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Removes the non-normative lossless luma mode, bit-depth, and block-geometry allowlist. Lossless luma now uses the same square and rectangular prediction planners as other intra blocks, including their normative no-neighbour synthesis, while independent chroma, MRL, palette, residual, and filter capability checks remain at their own boundaries. FSC prediction is tracked separately by DECODE-LOSSLESS-FSC-PREDICTION. A deterministic AVMenc 10-bit 4:2:0 lossless PAETH_PRED fixture failed at the removed general_intra_lossless_other_nondc_luma_unverified marker before this change; fixed splot, pinned AVM, and current AVM emit byte-identical 12,288-byte raw output SHA-256 c48f76e4dee5eee8980bd7da14bebe2eb1e24d566fc972d1fb10aaf87963940b. A second deterministic 69-byte AVMenc 10-bit V_PRED fixture pins the newly admitted top-left no-neighbour cardinal synthesis path to three-way byte-identical raw SHA-256 840b2119b3f2459bab99bc2e5814c5e9a35a4969301030d2808ebd8a08da90c6."
replaces = []

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::lossless_luma_uses_generic_prediction_planner",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_luma_10bit_frames_decode_to_oracle",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode lossless_luma --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-paeth-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-lossless-cardinal-y-v-intra-64x64-10bit.ivf",
    "tests/conformance/decoder-oracle.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-LOSSLESS-CHROMA-PREDICTION"
name = "Lossless chroma prediction without mode allowlists"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["5.20.5.6", "7.13.2.1", "7.13.2.7", "7.13.2.8"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-lossless-chroma-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Removes the non-normative lossless normal-block and SDP/chroma-part chroma mode, bit-depth, geometry, and neighbour allowlists. Resolved chroma modes now use the generic capability boundary at every bit depth and sampling format. Directional plans retain the resolved mode and raw AngleDeltaUV until reconstruction, where wide-angle mapping uses the actual transform-unit geometry; this prevents parent-block angles from being copied into lossless TX_4X4 units. The deterministic 102-byte AVMenc 10-bit 4:2:0 lossless V_PRED fixtures cover both normal-block and SDP/chroma-part syntax: each failed at its obsolete guard before the corresponding change, and fixed splot, pinned AVM, and current AVM emit byte-identical 12,288-byte raw output SHA-256 7ffd0c993409687c97ff25a4f75d407ccda6b73cfd9fdb8d60790b87cf4ffe6f."
replaces = []

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/residual/pipeline/tests.rs::chroma_directional_plan_uses_transform_unit_wide_angle_mapping",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nondc_chroma_v_and_lossless_sdp_nondc_chroma_v_frames_decode_to_oracle",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode chroma_directional_plan_uses_transform_unit_wide_angle_mapping --locked",
    "cargo test -p splot-decode lossless_nondc_chroma_v_and_lossless_sdp_nondc_chroma_v_frames_decode_to_oracle --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-v-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-v-intra-64x64-10bit.ivf",
    "tests/conformance/decoder-oracle.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-LOSSLESS-FSC-PREDICTION"
name = "Lossless FSC prediction without admission allowlist"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["5.20.5.5", "5.20.7.27", "5.20.7.29", "7.13.2.7", "7.15.2.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-2-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-lossless-fsc-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Removes the non-normative lossless FSC prediction-mode, bit-depth, geometry, and neighbour allowlist. Active lossless FSC now proceeds through the existing generic intra prediction and FSC/IDTX coefficient-reconstruction pipeline; unsupported prediction planners and independently unsupported syntax still retain their typed capability boundaries. A deterministic 203-byte AVMenc 10-bit 4:2:0 lossless 32x32 D135_PRED active-FSC, non-DPCM fixture failed at byte 65 with general_intra_lossless_fsc_unverified before this change. Fixed splot, pinned AVM, and current AVM emit byte-identical complete 3,072-byte raw output SHA-256 7c5ddec5653a8baddfbfa039dd8048666a3526e1a7a62a4f77d0a4e58bc82ac8."
replaces = []

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_fsc_d135_10bit_frame_decodes_to_oracle",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode lossless_fsc_d135_10bit_frame_decodes_to_oracle --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-lossless-fsc-d135-intra-32x32-10bit.ivf",
    "tests/conformance/decoder-oracle.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTRABC-REF-MV-BANK-SB-LIFETIME"
name = "Ordered IntrABC reference-MV bank lifetime"
category = "conformance"
kind = "bitstream-syntax"
spec_sections = ["5.18.2", "5.20.2.2", "5.20.4.1", "5.20.7.13", "6.19.2.2", "7.12.2.5", "7.12.2.6", "7.12.2.21"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-21",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block/resolve.rs"
change_id = "decode-intrabc-ref-mv-bank-sb-lifetime"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Reads IntrABC DRL, precision, MVD, morph and residual syntax in the entropy pass, but resolves the BVP in the existing ordered §7.12 motion pass. IntrABC and ordinary inter leaves therefore share the same tagged nine-list RefMvBank, global hit budget, size-four list-8 ring, duplicate refresh and superblock-row lifetime. Parse time captures only the ordered IntrABC spatial probe coordinates and availability; resolve time reads their values after earlier leaves publish motion and before later leaves do. The ordered IntrABC leaf resets or seeds the bank, selects the spatial/list-8/default BVP, constructs checked prediction geometry, publishes its BV, then updates tagged list 8. Intra-only frames clear per superblock row without seeding. For an intra-only frame whose sequence superblock is 256 samples, `effective_intrabc_sb_h4` caps the shared bank, spatial scan, default-BVP validation, and reset boundary to 32 four-pixel units (128 samples); inter frames retain the sequence superblock geometry. The entropy-pass `TileIntrabcPreludeState` uses the same frame-aware geometry, so a sequence-256 row at 128 samples is a superblock boundary only in an intra frame; the paired regressions pin both NPos context filtering and the distinct spatial SMVP probe. The focused shared-bank regression proves the 128-sample default BVP and bank clear at the effective row boundary. The external mission stream's output frame 749 is byte-identical to AVM in both one-thread and ten-thread prefix checks; AVMenc cannot practically force its carried list-8 eviction topology, so the external stream remains the differential oracle. The 10-bit IntraBC fixture additionally pins §7.12.2.6 scan weights after superblock-border alignment and freezes the decoded-location availability test at parse time. Its subsequently reachable INTERINTRA block requires the coupled §5.20.7.13 MVD-sign scope guards, preventing the geometry fix from turning the former fail-closed path into a later entropy desynchronization."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/find_mv_stack/bank_tests.rs::shared_ref_mv_bank_ordinary_entries_evict_intrabc_candidate",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/bank_tests.rs::shared_ref_mv_bank_intrabc_candidates_are_newest_first",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/bank_tests.rs::shared_ref_mv_bank_seeds_intrabc_from_previous_superblock_row",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/bank_tests.rs::shared_ref_mv_bank_late_leaf_uses_only_existing_unit_budget",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::intrabc_spatial_probe_waits_for_ordered_motion_publication",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::intrabc_spatial_probe_excludes_future_block_geometry",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::mvd_sign_derivation_requires_the_full_block_scope",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::intra_frame_intrabc_uses_128_sample_shared_bank_geometry",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_ref_mv_stack_tests.rs::spatial_scan_uses_aligned_sb_border_offset_for_weight",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records_tests.rs::sequence_256_intrabc_context_uses_the_frame_superblock_size",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records_tests.rs::sequence_256_intrabc_spatial_probe_uses_the_frame_superblock_size",
]
commands = [
    "cargo test -p splot-decode --lib shared_ref_mv_bank_ordinary_entries_evict_intrabc_candidate --locked",
    "cargo test -p splot-decode --lib intrabc_spatial_probe_waits_for_ordered_motion_publication --locked",
    "cargo test -p splot-decode --lib intrabc_spatial_probe_excludes_future_block_geometry --locked",
    "cargo test -p splot-decode --lib mvd_sign_derivation_requires_the_full_block_scope --locked",
    "cargo test -p splot-decode --lib spatial_scan_uses_aligned_sb_border_offset_for_weight --locked",
    "cargo test -p splot-decode --lib intra_frame_intrabc_uses_128_sample_shared_bank_geometry --locked",
    "cargo test -p splot-decode --lib sequence_256_intrabc_ --locked",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-3frame-intrabc-bv-10bit-256x128-q120.ivf",
    "tests/conformance/decoder-oracle.toml",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-TRANSFORM-UNIT-PREDICTION"
name = "Per-transform-unit intra prediction"
category = "conformance"
kind = "bitstream-semantics"
spec_sections = ["5.4.8", "5.20.6.1", "5.20.6.3", "5.20.7.24", "5.20.7.27", "6.4.8", "6.19.2.1", "6.19.6.3", "7.13.2.1", "7.13.2.7", "7.13.2.8"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-8",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-24",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-8",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-6-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/residual/pipeline/transform_units.rs"
change_id = "decode-general-intra-transform-unit-prediction"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Completes transform-partition syntax and prediction without policy gaps. The §5.20.6.1 context retains validated BlockSize and derives Max_Tx_Size_Rect internally; §5.20.6.3 records have only one, two, four, or five units and preserve raster order, middle flags, sparse clipping, and the pre-clipping record count. SequenceHeader.reduced_tx_part_set independently selects the reduced TxPartitionType CDF and suppresses tx_2or3 on one-axis geometry; FrameHeader.reduced_tx_set remains a transform-type restriction. Writer tests prove both CDF banks, flag independence, one-axis suppression, and §6.19.6.3 malformed narrow four/five-way geometry. The §5.20.7.24 walk retains unit geometry even when a four-unit edge partition clips to one visible unit, through prediction, reconstruction, deblock, and publication. The pinned 66-byte reduced-set D135 witness fails exact base at terminal entropy validation; fixed splot at one and eight threads matches pinned/current AVM raw SHA-256 608619fe3b10c3464841b2269baa417c29d48d7e4793ca38913a68c107caa0a9. A byte-distinct full-set control decodes successfully on exact base with identical raw output. A 96-stream official-AVMenc geometry/QP sweep did not isolate the one-axis branch, so that branch is writer-proven without hand-edited syntax."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::every_block_size_context_derives_its_table_max_tx_size",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::every_transform_partition_has_spec_geometry_order_and_middle_flags",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::writer_uses_sequence_selected_transform_partition_cdf",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::sequence_partition_reduction_suppresses_one_axis_four_way_symbol",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::sequence_partition_and_frame_transform_type_reductions_are_independent",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::writer_produced_narrow_four_and_five_way_partitions_are_nonconforming",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::sparse_partition_filter_keeps_record_order_and_original_count",
    "crates/splot-decode/src/pipeline/general_intra_unit_tests.rs::invalid_transform_partition_dimensions_are_a_typed_conformance_error",
    "crates/splot-decode/src/residual/pipeline/tests.rs::cfl_chroma_keeps_read_order_and_defers_reconstruction",
    "crates/splot-decode/src/residual/pipeline/tests.rs::palette_map_is_sliced_for_each_partitioned_transform_size",
    "crates/splot-decode/src/residual/pipeline/tests.rs::smooth_first_partition_handoff_replans_the_origin_transform_unit",
    "crates/splot-decode/src/residual/pipeline/tests.rs::active_mrl_directional_plan_uses_transform_unit_wide_angle_mapping",
    "crates/splot-decode/src/residual/pipeline/tests.rs::mrl_chunk_rows_use_transform_local_superblock_boundary",
    "crates/splot-decode/src/residual/pipeline/tests.rs::four_way_partition_clipped_to_one_unit_keeps_unit_reconstruction_and_deblock_geometry",
    "crates/splot-decode/src/output/hash_tests.rs::reduced_tx_partition_fixtures_match_reference_output_hashes_at_one_and_eight_threads",
    "crates/splot-decode/src/output/hash_tests.rs::reduced_tx_partition_fixtures_eof_fail_closed",
    "crates/splot-decode/src/output/hash_tests.rs::directional_tx_partition_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::directional_tx_partition_fixture_eof_fails_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --lib transform_partition --locked",
    "cargo test -p splot-decode partition_handoff --locked",
    "cargo test -p splot-decode directional_tx_partition_fixture --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-txpart-d135-intra-64x64-q100.ivf",
    "tests/conformance/vectors/valid/syn-reduced-txpart-d135-intra-64x64-q160.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-INTRA-CARDINAL-MRL-BOTTOM-EDGE"
name = "Cardinal MRL luma prediction at cropped frame edges"
category = "conformance"
kind = "bitstream-semantics"
spec_sections = ["5.20.7.27", "7.13.2.1", "7.13.2.8"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct/one_sided.rs"
change_id = "decode-intra-cardinal-mrl-bottom-edge"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Cardinal MRL prediction now applies the AV2 section 7.13.2.1 cropped-frame leftLimit and aboveLimit bounds before reading horizontal left-reference rows and vertical above-reference columns. A bottom-edge 32x32 transform at y=1056 in the mission stream previously attempted to read CurrFrame[Y][1080][380] for prediction row 24; rows 24 through 31 now repeat the last stored row at maxY=1079 as the specification requires. Symmetric TX32 and MrlIndex=3 focused tests prove that bottom-edge rows repeat maxY and right-edge columns repeat maxX. The external mission stream advances past byte 2569232. No AVMenc fixture is added because the encoder does not expose controls that force this exact combination of a frame-edge 32x32 transform, cardinal mode, and active MRL index 3; oracle coverage remains partial pending full-stream identity."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::horizontal_cardinal_mrl_tx32_clamps_bottom_edge_rows",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::vertical_cardinal_mrl_tx32_clamps_right_edge_columns",
]
commands = [
    "cargo test -p splot-decode cardinal_mrl_tx32_clamps --locked",
    "cargo xtask check-feature-status",
]
fixtures = []
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-REPEATED-KEY-SEQUENCE-ACTIVATION"
name = "Repeated CLK sequence activation and runtime state reset"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.2", "7.3.6", "7.4.3", "7.21.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-4-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-repeated-key-sequence-activation"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "At every repeated OBU_CLOSED_LOOP_KEY frame unit, the runtime implements the AV2 §7.3.6/§7.4.3 start of a new coded video sequence: validate the referenced sequence, flush pending implicit output, recreate scheduler slots for the new NumRefFrames, invalidate and recreate reference state, reset the reference frame counter, and make the new sequence active for the CLK and following inter frames. Output history remains in display order. Raw output supports per-frame visible-size, chroma, and bit-depth transitions; Y4M accepts only transitions compatible with its single stream header and rejects incompatible dimensions, chroma, or bit depth transactionally. Two committed AVMenc-derived Annex B fixtures prove compatible and crop-changing sequence activation; pinned/current AVM and splot are byte-identical. Additional self-contained composition tests prove an 8-to-10-bit raw transition and a following inter frame decoded with the new sequence/reference state. The obsolete repeated-sequence compatibility gate is deleted while malformed, EOF, output-limit, retained-storage-limit, and output-format failures remain typed."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/tests/sequence_transition.rs",
    "crates/splot-decode/src/pipeline/output_schedule.rs::tests::new_sequence_flushes_pending_output_and_recreates_slots",
    "crates/splot-cli/tests/decode_sequence_transition_cli.rs",
]
commands = [
    "cargo test -p splot-decode --test sequence_transition --locked",
    "cargo test -p splot-cli --test decode_sequence_transition_cli --locked",
    "cargo clippy -p splot-decode -p splot-cli --all-targets --all-features --locked -- -D warnings",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/output_schedule.rs",
    "crates/splot-decode/tests/sequence_transition.rs",
    "crates/splot-cli/tests/decode_sequence_transition_cli.rs",
    "tests/conformance/vectors/valid/syn-2seq-compatible-intra-64x64.obu",
    "tests/conformance/vectors/valid/syn-2seq-crop-intra-64x64.obu",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature", "decode/malformed-source", "decode/resource-limit", "decode/output-error"]

[[feature]]
id = "DECODE-GENERAL-INTRA-RECT-CHROMA"
name = "General intra generic rectangular-plan chroma prediction"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["5.20.5.3", "7.13.2.1", "7.13.2.2", "7.13.2.7", "7.13.2.8", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-rect-chroma"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Completes the generic rectangular-plan chroma admission domain by deleting the redundant `ensure_supported_rect_chroma_capability` policy gate. Every valid §5.20.5.3 non-CfL UV mode already resolves to `SupportedChromaMode`; `rect_chroma_plan_for_mode` retains directional mode plus raw AngleDeltaUV, and `unit_directional_replan` maps them against the actual transform-unit geometry before cardinal, one-sided, or middle reconstruction; non-directional modes keep their direct plan. Those reconstruction paths already implement §7.13.2.1 real and synthesized edge construction for all neighbour combinations. The deleted gate duplicated this enum classification and rejected directional or Paeth modes solely when no real edge existed. An exhaustive test covers all 21 resolved chroma modes at frame and nonzero tile origins without real edges. The committed q7 AVMenc fixture reaches the former reason with D113_PRED luma and D113-follow chroma at the top-left block; Splot, pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774, and current AVM revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce byte-identical 6,144-byte raw I420 output SHA-256 f831430d302267653add61fbc5054c1f3ab193a10ab7529fbcda74be6cdff70e. The obsolete `general_intra_rect_non_dc_chroma` diagnostic is removed."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/output/hash_tests.rs::rect_noedge_d113follow_chroma_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::partition_symbol_mutation_fails_closed_at_exit_symbol",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode pipeline::general_intra::tests --locked",
    "cargo test -p splot-decode rect_noedge_d113follow_chroma --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-rect-noedge-d113follow-chroma-64x64-q7.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = []

[[feature]]
id = "DECODE-GENERAL-INTRA-OVERSIZED-CHROMA-CHUNKS"
name = "Oversized rectangular chroma residual chunking"
category = "conformance"
kind = "bitstream-semantics"
spec_sections = ["5.20.7.23", "5.20.7.25", "5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-23",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/residual/pipeline/plan.rs"
change_id = "decode-general-intra-oversized-chroma-chunks"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Implements the complete AV2 section 5.20.7.23 chunk geometry and read order for oversized rectangular chroma residuals. Each coding block is walked in two-by-two groups of 64x64 luma chunks; 4:4:4 parses U and V after every luma chunk, while 4:2:2 and 4:2:0 pair the required horizontal and/or vertical chunks through doubleChromaW and doubleChromaH. Explicit ChromaMiSize geometry is chunked when it names the same oversized coding block, preserving its origin, tile bounds, reconstruction availability, and U-before-V entropy order. The plan retains the original chroma residual width and height separately from the chunk-local transform geometry because section 8.3.2 derives the V all_zero size contribution from get_plane_residual_size(ChromaMiSize), not miSizeChunk. An exhaustive table test covers all 29 AV2 block shapes across 4:4:4, 4:2:2, and 4:2:0 in lossless and non-lossless modes. The committed 77-byte BLOCK_128X64 4:4:4 fixture previously rejected at byte 65 at the rectangular chroma transform-size guard; fixed Splot, pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774, and current AVM revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce identical 24,576-byte native planar output at SHA-256 7cbec0da6486a3ef302af168bcd58cdf5658acef05ffbfb64dbb14b068e6e3a6."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/residual/pipeline/tests.rs::oversized_explicit_chroma_ref_is_chunked_in_spec_read_order",
    "crates/splot-decode/src/residual/pipeline/tests.rs::every_av2_block_shape_maps_to_valid_luma_and_chroma_chunk_tx_sizes",
    "crates/splot-decode/src/output/hash_tests.rs::rect_chroma_chunks_444_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::rect_chroma_chunks_444_fixture_eof_fails_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode residual::pipeline --locked",
    "cargo test -p splot-decode rect_chroma_chunks_444 --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-rect-chroma-chunks-444-intra-128x64-q80.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-INTER-NON420-CHROMA"
name = "Non-4:2:0 inter reconstruction"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["6.4.1", "7.13.3.18", "7.13.3.19", "7.13.3.20", "7.13.3.25", "7.13.3.30"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-19",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-20",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-25",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-30",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/mc.rs"
change_id = "decode-inter-non420-chroma"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Removes the obsolete inter_non_420_chroma format guard and derives U/V plane geometry from the decoded PixelFormat throughout the inter reconstruction graph. Single-reference, compound, warp, BAWP, inter-intra, and deferred reconstruction windows now use the sequence's independent horizontal and vertical chroma subsampling. The 110-byte current-AVMenc 4:4:4 fixture is a validator-clean two-frame stream whose second frame reached the exact pre-fix gap at byte 102; fixed splot and current avmdec produce the same complete 24,576-byte native planar SHA-256 92cb67a5828266a983aca929f2c97b1e86ea5047ea9bca6ded0e467c3cb781e2. A separate validator-clean 4:2:2 local encode produces byte-identical current-avmdec output SHA-256 1c5ef1aff429214b04b817441492b0c90b449ac1f373256787deb3e29094c83a. This feature is complete for non-4:2:0 geometry across the currently implemented inter tools; independently unsupported inter syntax and tools retain their own fail-closed gates."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::motion_compensation_planes_follow_output_chroma_format",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::dispatcher_copies_non420_reference_planes",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::compound_and_warp_copy_non420_chroma_at_native_resolution",
    "crates/splot-decode/src/prediction/inter/bawp_tests.rs::inter_bawp_uses_full_resolution_444_chroma_geometry",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::interintra_chroma_planes_follow_non420_subsampling",
    "crates/splot-decode/src/output/hash_tests.rs::inter_444_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::inter_444_fixture_eof_fails_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode non420 --locked",
    "cargo test -p splot-decode inter_444_fixture --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-inter-444-64x64-q128.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = []

[[feature]]
id = "AV2-8.2-SYMBOL-DECODER"
name = "AV2 symbol decoder foundation"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["3", "8.2.2", "8.2.3", "8.2.4", "8.2.5", "8.2.6", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-core"
module = "crates/splot-core/src/symbol.rs"
change_id = "symbol-decoder-foundation"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "The generic AV2 §8.2 symbol-decoder primitive over a bounded tile payload byte slice is complete and spec-exact: init_symbol(sz) arithmetic setup, read_bool(), read_literal(n), caller-supplied-CDF read_symbol(cdf) renormalization and CDF adaptation using generated §9.2 Prob_Inc / Para_Adjustment_List tables, the exit_symbol() §8.2.4 trailing-one-bit and zero-padding conformance check, typed CDF/state errors instead of panics, signed SymbolMaxBits, and a lossless arithmetic-state checkpoint for caller-owned syntax frontiers. Completeness is proven by spec-exact unit vectors for every arity N=2..8, exact CDF-update results at the minimum and maximum §8.2.6 adaptation rates, a deep-negative-SymbolMaxBits implicit-zero-padding run, a random-valid-CDF property test, the symbol_decoder_bytes fuzz target, and end-to-end use by the supported decode-minimal-tier-runtime-success runtime. Out of scope here and tracked in their own rows: §8.2.4 CDF copy/averaging and §8.2.2 named Tile/Saved CDF banks (tile-cdf-save-lifecycle-boundary), §8.3 syntax-element CDF selection (tile-cdf-selection-boundary), default §9.3 CDF banks, decode_tile(), tile syntax traversal, reconstruction, hashes, runtime Y4M output, AVM/dav2d invocation, and decoder orchestration. perf remains future work."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "todo"

[feature.proof]
tests = [
    "crates/splot-core/src/symbol.rs::tests",
    "crates/splot-core/src/symbol.rs::proptests",
    "fuzz/fuzz_targets/symbol_decoder_bytes.rs",
    "crates/splot-core/tests/tables_spot.rs::conversion_para_adjustment_list_matches_mirror",
    "crates/splot-core/tests/tables_spot.rs::conversion_prob_inc_matches_mirror",
]
commands = [
    "cargo test -p splot-core symbol --locked",
    "cargo test -p splot-core --test tables_spot --locked",
    "cargo clippy -p splot-core --all-targets --all-features --locked -- -D warnings",
    "cargo +nightly fuzz run symbol_decoder_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-SYMBOL-DECODER-FUZZ"
name = "Symbol decoder no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["4.11.11", "4.11.12", "8.2.2", "8.2.3", "8.2.4", "8.2.5", "8.2.6", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-11-11",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-11-12",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/symbol_decoder_bytes.rs"
change_id = "symbol-decoder-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for the existing public `splot_core::symbol::SymbolDecoder` API: it feeds bounded arbitrary in-memory tile-payload bytes, drives bounded operation streams over `read_bool`, `read_literal`, `read_symbol`, `checkpoint`, and `exit_symbol` or `finish`, generates bounded valid and intentionally malformed caller-supplied CDF rows, asserts only public invariants after successful calls, and accepts typed `splot_core::Error` returns for malformed CDF rows, invalid literal widths, EOF, or invalid symbol decoder state. This row does not implement or claim §8.3 syntax-element CDF selection, Tile or Saved CDF banks, default CDF initialization beyond synthetic caller rows, tile payload framing, `decode_tile()`, runtime `splot decode`, reconstruction, hash output, Y4M output, AVM agreement, dav2d agreement, ffmpeg agreement, filesystem I/O, network I/O, subprocesses, new dependencies, scheduler changes, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/symbol_decoder_bytes.rs",
    "crates/splot-core/src/symbol.rs::tests",
    "crates/splot-core/src/symbol.rs::proptests",
    "crates/splot-core/tests/tables_spot.rs::conversion_para_adjustment_list_matches_mirror",
    "crates/splot-core/tests/tables_spot.rs::conversion_prob_inc_matches_mirror",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-core symbol --locked",
    "cargo test -p splot-core --test tables_spot --locked",
    "cargo +nightly fuzz run symbol_decoder_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = ["fuzz/fuzz_targets/symbol_decoder_bytes.rs"]
diagnostics = []

[[feature]]
id = "CONF-TILE-PAYLOAD-DECODE-FUZZ"
name = "Tile payload decode no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["5.19", "5.20.1", "5.20.2.1", "5.20.3.1", "5.20.3.2", "6.19.1", "6.19.2.1", "6.19.3", "8.2.2", "8.2.4", "8.2.6", "8.3.1", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/tile_payload_decode_bytes.rs"
change_id = "tile-payload-decode-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for the current minimal `splot-decode` tile-payload runtime byte frontier: it calls a `fuzzing`-feature harness over the existing crate-private tile-payload boundary and minimal block-symbol frontier, feeds bounded arbitrary in-memory tile-payload bytes plus bounded mutations of the known-good two-byte minimal frontier payload, enforces finite `DecodeLimits`, asserts only stable boundary/frontier success invariants, and accepts typed decoder errors for malformed or unsupported mutations. The row provides robustness evidence only; it does not implement or claim full AV2 §5.20 `decode_tile()`, recursive partition/block syntax, broad §8.3 CDF selection, all Tile/Saved CDF banks, reconstruction expansion, hash/raw/Y4M output behavior changes, reference refresh, public production APIs, AVM/dav2d/ffmpeg agreement, filesystem I/O, network I/O, subprocesses, new dependencies, scheduler changes, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/tile_payload_decode_bytes.rs",
    "fuzz/decode_tile_payload_harness.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs::tests",
    "crates/splot-decode/src/output/hash.rs::tests::tile_payload_byte_mutations_return_typed_results",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo test -p splot-decode output::hash --locked",
    "cargo +nightly fuzz run tile_payload_decode_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/tile_payload_decode_bytes.rs",
    "fuzz/decode_tile_payload_harness.rs",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "AV2-4.11.4-SVLC"
name = "Signed variable-length code descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "qm-filmgrain-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Implemented as BitReader::read_svlc() from AV2 §4.11.4: value = uvlc(); half = (value + 1) >> 1; svlc = (value & 1) ? half : -half. EOF and the uvlc() leadingZeros >= 32 conformance bound propagate from read_uvlc() without panics. Needed by the user_defined_qm() helper (AV2-5.4.11-USER-QM)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-4.11.5-LE"
name = "Little-endian fixed-width descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Implemented as BitReader::read_le(n) from AV2 §4.11.5, returning arbitrary-width little-endian bytes with a read_le_u64(n) convenience helper for n <= 8."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-4.11.8-NS"
name = "Non-symmetric integer descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Implemented as BitReader::read_ns(n) from AV2 §4.11.8. Covers n=1, power-of-two and non-power-of-two ranges, EOF, and invalid n=0 without panics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-4.11.7-SU"
name = "Signed integer descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "segmentation-tile-params-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Implemented as BitReader::read_su(n) from AV2 §4.11.7 (sign-extension via signMask). Reused by seg_info() (AV2-5.4.9-SEGMENT-INFO). Covers su(1), positive/negative multi-bit values, EOF, and invalid widths without panics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-4.11.10-RG"
name = "Rice-Golomb descriptor"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["4.11.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "bit-writer-primitives"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Implemented as BitReader::read_rg(n) from AV2 §4.11.10 (mirror docs/spec/av2/1.0.0/04-conventions.md#s-4-11-10): a unary quotient prefix, a terminating zero bit, then an n-bit remainder, with the prefix-must-terminate-within-32-bits conformance bound; used by ci_color_description_idc (§6.14). The inverse BitWriter::write_rg(value, n) (crates/splot-core/src/write/bit_writer.rs) landed with bit-writer-primitives, proven by the read(write(x)) == x property test. The parse stage predates this row; the row was added when the writer landed so the rg descriptor is tracked independently like its §4.11 siblings (the foundational f(n) read/write stays under the umbrella ENC-BITSTREAM-WRITER row)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-core write"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.2.2-OBU-HEADER"
name = "OBU header syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.2.2", "6.2.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/obu.rs"
change_id = "parse-annexb-and-obu-headers"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Critical AV2/AV1 difference: no AV1 obu_forbidden_bit, obu_has_size_field, or AV1 extension header. validate covers the header-only constraints; sequence/frame-level conformance is tracked elsewhere."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/obu.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-core/src/write/obu.rs::tests", "crates/splot-core/src/write/obu.rs::proptests"]
commands = ["cargo test -p splot-core obu", "cargo test -p splot-validate", "cargo test -p splot-core write::obu"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/bad-global-xlayer.av2"]
diagnostics = [
    "obu-header/global-xlayer-required",
    "obu-header/global-xlayer-requires-base-layers",
    "obu-header/global-xlayer-allowed-types",
    "obu-header/base-layer-only-types",
    "obu-header/temporal-layer-zero-only-types",
    "obu-header/reserved-obu-type",
]

[[feature]]
id = "AV2-5.2.1-OBU-TYPE"
name = "OBU type and OBU-class predicates"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.2.1", "6.2.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/types.rs"
change_id = "parse-annexb-and-obu-headers"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "ObuType enum (AV2 Table 6.1, raw round-trip for 0..=31, reserved preserved) plus AV2 § 5.2.1 OBU-class predicates (is_tile_group, is_sef, requires_global_xlayer, ...) — citations match crates/splot-core/src/types.rs. No AV1 OBU type table. Every §6.2.2 layer-id-vs-obu_type requirement that is decidable from the OBU header and these predicates alone has landed (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2, lines 183-217): GLOBAL_XLAYER_ID base-layer rule (obu-header/global-xlayer-requires-base-layers), MSDO/TD require-global (obu-header/global-xlayer-required), GLOBAL_XLAYER_ID allowed-types (obu-header/global-xlayer-allowed-types), base-layer-only types (obu-header/base-layer-only-types), and tlayer-zero-only types (obu-header/temporal-layer-zero-only-types). The only §6.2.2 residual — obu_tlayer_id/obu_mlayer_id <= max_tlayer_id/max_mlayer_id from an activated sequence header (lines 190-194) — is owned by AV2-6.2.2-OBU-HEADER-ACTIVATED-SEQUENCE-LIMITS, not by this predicate row."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/types.rs::tests", "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::global_xlayer_requires_base_layers_is_flagged", "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::global_xlayer_on_disallowed_type_is_flagged", "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::base_layer_only_type_with_nonzero_layer_is_flagged", "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::temporal_layer_zero_only_type_is_flagged", "crates/splot-validate/src/validator/tests/public_api.rs::temporal_delimiter_without_global_xlayer_is_flagged"]
commands = ["cargo test -p splot-core types", "cargo test -p splot-validate xlayer"]
fixtures = []
diagnostics = ["obu-header/global-xlayer-requires-base-layers", "obu-header/global-xlayer-required", "obu-header/global-xlayer-allowed-types", "obu-header/base-layer-only-types", "obu-header/temporal-layer-zero-only-types"]

[[feature]]
id = "AV2-5.2.1-OBU-DISPATCH"
name = "open_bitstream_unit payload dispatch"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.2.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/obu.rs"
change_id = "obu-dispatch-frame-payloads"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Stateless dispatch after parsing obu_header(): temporal delimiter payloads are parsed through trailing_bits(), reserved payload bytes stay opaque per AV2 5.3, and every fully-decidable OBU payload is parsed into PayloadStatus::Parsed. Under obu-dispatch-frame-payloads the 11 frame-carrying types (the tile-group family CLK/OLK/leading-/regular-tile-group/switch/RAS and the SEF/TIP/bridge family) no longer return a blanket Unimplemented: dispatch parses their state-free §5.18.2 / §5.19 activation prefix (parse_tile_group_prefix / parse_frame_header_prefix, passing FirstPictureInTU=None since the stateless front door holds no per-extended-layer decoder state — a statelessly-dispatched CLK exposes starts_cvs=None rather than a fabricated value; non-CLK types derive Some(false) from the type alone) and returns the honest PayloadStatus::PrefixParsed { prefix: FramePayloadPrefix::{TileGroup|FrameHeader}, blocked_on = 'active sequence header state', feature = AV2-5.19-TILE-GROUP / AV2-5.18-FRAME-HEADER }; an EOF/invalid-uvlc inside the prefix is the existing structured error path. The deeper §5.18 / §5.19 syntax beyond the prefix stays owned by AV2-5.18-FRAME-HEADER / AV2-5.19-TILE-GROUP (the inspector's stateful frame-header views and the validator's direct-call path are the richer, state-aware surface). The dispatch row's own 11-type Unimplemented residual is now closed — no OBU type returns a blanket Unimplemented; the catch-all fallback is dead. inspect --json reports the prefix_parsed_awaiting_state status (syntax + feature + blocked_on). WRITER (partial): the NON-frame-carrying payload dispatch is inverted by write_complete_obu / write_obu_payload (crates/splot-core/src/write/dispatch.rs), routing every ParsedObu variant (all 14 non-frame OBU types) to its body writer + the §5.2.1/§6.2.1 tail; round-trip proof on ENC-BITSTREAM-WRITER. The FRAME-CARRYING OBU types the parser returns as PayloadStatus::PrefixParsed (the tile-group family + SEF/TIP/bridge) are NOT routed through the unified write_complete_obu — they have their own composers (write_tile_group_obu / write_tile_group_continuation_obu; SEF/TIP/bridge remain), so a tool cannot re-emit a parsed frame-carrying OBU through the complete-OBU writer yet. write stays partial until those PrefixParsed arms are writable through the dispatch."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "partial"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/dispatch_tests.rs::tests", "crates/splot-core/src/obu.rs::tests::dispatch_parses_tile_group_prefix_and_blocks_on_state", "crates/splot-core/src/obu.rs::tests::dispatch_parses_bridge_frame_prefix_and_blocks_on_state", "crates/splot-core/src/obu.rs::tests::dispatch_parses_sef_prefix_and_blocks_on_state", "crates/splot-core/src/obu.rs::tests::dispatch_tile_group_eof_in_prefix_is_structured_error", "crates/splot-core/src/obu.rs::tests::dispatch_frame_header_eof_in_prefix_is_structured_error", "crates/splot-core/src/obu.rs::tests::dispatch_covers_every_frame_carrying_type_without_unimplemented", "crates/splot-core/src/annexb.rs::tests::envelope_payload_status_is_opt_in_and_preserves_raw_payload", "crates/splot-cli/tests/cli.rs::inspect_json_exposes_frame_header_prefix", "crates/splot-cli/tests/cli.rs::inspect_json_includes_payload_status_without_dropping_header_fields"]
commands = ["cargo test -p splot-core dispatch", "cargo test -p splot-core envelope_payload_status", "cargo test -p splot-cli inspect_json_exposes_frame_header_prefix"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/frame-header-prefix.av2"]
diagnostics = []

[[feature]]
id = "AV2-B-ANNEXB-OBU-ENVELOPE"
name = "Annex B length-delimited OBU envelope"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["Annex B"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/annexb.rs"
change_id = "parse-annexb-and-obu-headers"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Annex B packs each OBU as leb128 length then open_bitstream_unit(num_bytes_in_obu). Header parsing is bounded to the declared OBU size; partial parse retains the prefix before a structural error."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/annexb.rs::tests", "crates/splot-core/src/annexb.rs::proptests", "crates/splot-core/src/write/obu.rs::tests", "crates/splot-core/src/write/obu.rs::proptests"]
commands = ["cargo test -p splot-core annexb", "cargo test -p splot-core write::obu"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/truncated.av2", "tests/fixtures/prefix-then-truncated.av2"]
diagnostics = ["bitstream/parse-error"]

[[feature]]
id = "AV2-IVF-CONTAINER"
name = "IVF container support"
category = "conformance"
kind = "bitstream-syntax"
spec_sections = []
sources = ["https://wiki.multimedia.cx/index.php/Duck_IVF", "https://crates.io/crates/ivf"]
crate = "splot-core"
module = "crates/splot-core/src/ivf.rs"
change_id = "add-ivf-container-support"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "IVF is a non-normative container used by common AV bitstream tooling. splot parses and writes the DKIF header/frame envelope locally, then validates each frame payload as AV2 Annex B while preserving original byte offsets. Decode treats IVF width, height, and declared frame count as advisory transport metadata, traverses actual complete records until EOF, and ignores complete zero-size records after charging them to the IVF-record limit. The AV02 FourCC remains a typed input-domain boundary (`decode/unsupported-ivf-codec` nested under malformed-source); truncated frame headers and payloads remain typed malformed sources. Annex B.1 explicitly permits derived container formats, while Annex B.2/B.3 own only the enclosed OBU stream and system-dependent end detection. Pinned AVMdec, current AVMdec, and splot produce byte-identical raw output for the committed advisory-metadata/zero-record derivative. The crates.io ivf crate is BSD-2-Clause and legally usable, but not adopted because the needed surface is small, its writer helpers panic on I/O errors, and it defaults to AV1 fourcc behavior."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/ivf.rs::tests", "crates/splot-core/src/stream.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-cli/tests/cli.rs", "crates/splot-decode/src/output/raw_tests.rs::advisory_ivf_metadata_and_empty_record_preserve_raw_output", "crates/splot-decode/src/output/raw_tests.rs::empty_ivf_record_is_charged_to_the_record_limit", "crates/splot-decode/src/output/raw_tests.rs::non_av2_ivf_codec_is_a_typed_source_rejection", "crates/splot-decode/src/output/raw_tests.rs::trailing_partial_ivf_header_preserves_complete_frame_output", "crates/splot-decode/src/output/raw_tests.rs::truncated_ivf_payload_fails_before_raw_output"]
commands = ["cargo test -p splot-core ivf --locked", "cargo test -p splot-core stream --locked", "cargo test -p splot-validate ivf --locked", "cargo test -p splot-cli ivf --locked", "cargo test -p splot-decode advisory_ivf_metadata_and_empty_record_preserve_raw_output --locked", "cargo xtask decoder-fixtures verify"]
fixtures = ["tests/conformance/vectors/valid/syn-ivf-advisory-empty-record-64x64.ivf", "docs/LOCAL-REFERENCE-EVIDENCE.toml::lref-avm-syn-ivf-advisory-empty-record-64x64"]
diagnostics = [
    "ivf/invalid-header-length",
    "ivf/invalid-signature",
    "ivf/trailing-partial-frame-header",
    "ivf/truncated-frame-header",
    "ivf/truncated-frame-payload",
    "ivf/truncated-header",
]

[[feature]]
id = "AV2-5.2.3-TRAILING-BITS"
name = "Trailing bits syntax and semantics"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.2.3", "6.2.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/obu.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Implemented parse_trailing_bits(nbBits), enforcing nbBits > 0, trailing_one_bit == 1, and zero padding (§6.2.3, docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-3). The trailing-bits/* diagnostics are emitted via syntax_error_diagnostic() for every OBU type whose check calls finish_obu_payload() (sequence header, temporal delimiter, MFH, LCR, atlas, OPS, BRT, QM, film-grain, content-interpretation, padding, metadata). Residual: the trailing_bits() call sites inside the still-unparsed coded-frame syntax — the §5.19 tile_group_obu() bru_inactive early-return tail (line 8459 of docs/spec/av2/1.0.0/05-syntax-structures.md, owned by AV2-5.19-TILE-GROUP) and the §5.20 tile_group_payload() tail (owned by AV2-5.20-TILE-GROUP-PAYLOAD) — are unreachable until those parsers land; blocked on AV2-5.19-TILE-GROUP / AV2-5.20-TILE-GROUP-PAYLOAD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/obu.rs::tests", "crates/splot-core/src/obu.rs::proptests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/checks/syntax_error.rs::tests::syntax_error_diagnostic_maps_trailing_bits_errors", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core trailing", "cargo test -p splot-validate trailing", "cargo test -p splot-core write::bit_writer"]
fixtures = []
diagnostics = ["trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.2.4-BYTE-ALIGNMENT"
name = "Byte alignment syntax and semantics"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.2.4", "6.2.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/bitio.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Implemented BitReader::byte_align_zero() for byte_alignment() zero-bit validation (§6.2.4, docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-4), surfaced as byte-alignment/zero-bit-not-zero through syntax_error_diagnostic(). Residual: the §5.19 tile_group_obu() calls byte_alignment() before the tile_group_payload() handoff (line 8519 of docs/spec/av2/1.0.0/05-syntax-structures.md), and the §5.20 tile-payload boundary uses it likewise; the validator does not yet reach either because the tile-group parser is prefix-only. Blocked on AV2-5.19-TILE-GROUP / AV2-5.20-TILE-GROUP-PAYLOAD (the only OBU sites that call byte_alignment(); all currently-dispatched OBU tails use trailing_bits(), not byte_alignment())."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/bitio.rs::tests", "crates/splot-core/src/bitio.rs::proptests", "crates/splot-validate/src/checks/syntax_error.rs::tests::syntax_error_diagnostic_maps_byte_alignment_errors", "crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests"]
commands = ["cargo test -p splot-core bitio", "cargo test -p splot-validate syntax_error_diagnostic", "cargo test -p splot-core write"]
fixtures = []
diagnostics = ["byte-alignment/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.3-RESERVED-OBU"
name = "Reserved OBU handling"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["5.3", "6.2.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/byte_stream.rs"
change_id = "decode-reserved-obu-ignore"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements the complete reserved-OBU behavior defined by AV2 §5.3 and §6.2.2. The parser retains reserved types 0 and 26-31 with opaque payload bytes, and validation reports an informational reserved-type diagnostic while rejecting an all-zero non-empty payload because §5.3 requires at least one non-zero payload byte. The bounded stream planner counts and retains every reserved OBU as global, including extension headers with non-base layer ids, without treating it as a frame candidate or applying layer selection. The one-pass byte-stream preparation path plans from a single `FlatParsedBitstream`, then `FlatParsedBitstream::discard_runtime_noops` removes reserved OBUs before syntax scheduling, so all placement positions are ignored without disturbing ordinary OBU order, resource accounting, frame scheduling, reconstruction, or output. The structured AVM-derived two-frame fixture inserts seven conformant reserved OBUs around ordinary OBUs in both IVF records; splot, pinned avmdec, and current avmdec produce the same 12,288-byte raw I420 output with SHA-256 d98e1697e2f29c229620758433b43ed11ad65f888a6a82d952801573b87d2602."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::reserved_obu_with_all_zero_payload_is_an_error",
    "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::reserved_obu_with_nonzero_payload_is_conformant",
    "crates/splot-validate/src/validator/tests/obu_header_and_syntax.rs::reserved_obu_type_emits_info_and_stays_conformant",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::reserved_obus_are_retained_without_layer_selection",
    "crates/splot-decode/src/pipeline/mod_tests.rs::prepared_byte_stream_discards_reserved_obus_from_annex_b_and_ivf",
]
commands = [
    "cargo test -p splot-validate reserved_obu --locked",
    "cargo test -p splot-decode reserved_obu --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-reserved-obu-64x64.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["obu-reserved/all-zero-payload", "obu-header/reserved-obu-type"]

[[feature]]
id = "AV2-5.4-SEQUENCE-HEADER"
name = "Sequence header OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4", "6.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Umbrella for sequence_header_obu(). All §5.4.1-§5.4.13 child configs parse (see the child rows); the umbrella stays partial because its own types/parse/tests stages are partial (deeper §5.4 cluster) per the do-not-close-before-children rule (docs/IMPLEMENTATION-MATRIX.toml § 7, not an AV2 section). The §6.4 cross-element semantic-validation residuals (repeated-header identity, §6.4.1 distinct-mlayer count, §6.4.12 cross-embedded-layer timing consistency) live on AV2-6.4-SEQUENCE-HEADER-SEMANTICS; the per-config tool-flag-vs-usage gating residuals are blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing (see the child rows). WRITER (seq-header-writer-general / seq-header-writer-configs / seq-header-writer-tile): the full sequence_header_obu() body is now inverted. The §5.4.1 general fields (write_sequence_header_general + dependency maps + cropping) and §5.4.13 decoder-model info are in crates/splot-core/src/write/seq_header.rs; the §5.4.3-5.4.8 config cascade + §5.4.9 seg_info in crates/splot-core/src/write/seq_config.rs; the §5.4.10 filter config and §5.4.2 tile config (incl. §5.18.7.3 tile_params) plus the composing pub fn write_sequence_header in crates/splot-core/src/write/seq_tile.rs. write_sequence_header recomputes the parser's derivations (monochrome / single_picture / seq_sb_size / TileParamsInput), validates the whole header up front (reject-before-write, including every gated-off non-default field), and emits general -> partition -> segment -> intra -> inter -> scc -> tq -> filter -> tile -> film_grain_params_present_flag in §5.4.1 order with no OBU header / trailing bits. All §5.4.1-§5.4.13 child write rows are write=done and round-trip + byte-exact tested, so this umbrella write is done; its types/parse/tests stages stay partial on the deeper §5.4 cluster (do-not-close-before-children rule)."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_tile_tests.rs::tests", "crates/splot-core/src/write/seq_tile_proptests.rs::proptests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-cli/tests/cli.rs"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_tile", "cargo test -p splot-cli inspect"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/seq-header-tile-params.av2"]
diagnostics = ["hls/repeated-sequence-header-not-identical"]

[[feature]]
id = "AV2-5.4.1-SEQUENCE-HEADER-GENERAL"
name = "General sequence header syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.1", "6.4.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "qm-reset-mlayer-presence-arm"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Parses the §5.4.1 general fields through dependency maps; the §5.4.2-§5.4.13 child configs are parsed by their own rows' modules. The §5.4.1 MLayerDependencyMap/TLayerDependencyMap are now derived and stored on SequenceHeaderGeneral (unconditional default fill plus the signaled descending-refLayer overrides, including the multi_tlayer row-0 replication branch) instead of having their bits discarded; depends_on() queries are panic-free. Under qm-reset-mlayer-presence-arm the §5.4.1 MLayerPresenceMap is now DERIVED and exposed (MLayerDependencyMap::presence_map() -> MLayerPresenceMap, the reflexive-transitive closure of MLayerDependencyMap per the §5.4.1 double loop, mirror docs/spec/av2/1.0.0/05-syntax-structures.md :583-601, with a panic-free is_present() accessor): consumed by the §5.18.2 reset_qm() SWITCH/RAS arm (AV2-5.18.6-QUANTIZATION). presence_map_closes_transitive_dependency verifies the closure adds the transitive edge a direct dependency lacks."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/headers/sequence.rs::tests::dependency_maps_default_fill_when_flags_absent", "crates/splot-core/src/headers/sequence.rs::tests::mlayer_dependency_override_bit_order", "crates/splot-core/src/headers/sequence.rs::tests::mlayer_presence_map_closes_transitive_dependency", "crates/splot-core/src/headers/sequence.rs::tests::tlayer_dependency_row0_replication", "crates/splot-core/src/headers/sequence.rs::tests::tlayer_dependency_multi_rows_signaled", "crates/splot-core/src/headers/sequence.rs::tests::single_picture_header_collapses_maps", "crates/splot-core/src/headers/sequence.rs::tests::dependency_map_truncation_reports_eof", "crates/splot-validate/src/checks/syntax_error.rs::tests::syntax_error_diagnostic_maps_sequence_header_errors", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-core/src/write/seq_header.rs::tests", "crates/splot-core/src/write/seq_header.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core dependency", "cargo test -p splot-validate sequence_header", "cargo test -p splot-core write::seq_header"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = ["sequence-header/seq-header-id-out-of-range", "sequence-header/chroma-format-out-of-range", "sequence-header/bit-depth-out-of-range", "sequence-header/seq-max-mlayer-count-out-of-range", "sequence-header/crop-left-out-of-range", "sequence-header/timing-num-units-zero"]

[[feature]]
id = "AV2-5.4.2-SEQUENCE-TILE-CONFIG"
name = "Sequence tile configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.2", "6.4.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "seq-tile-nonuniform-reuse-starts"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. tile_params() is now parsed via the shared AV2-5.18.7.3-TILE-PARAMS helper, so a valid sequence tile config parses fully (no bounded status); only a reserved seq_level_idx (non-conformant) leaves a documented bounded residual. Under seq-tile-nonuniform-reuse-starts the §5.4.2 parse also persists the derived SeqSbColStarts / SeqSbRowStarts arrays (mirror docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-2 :654-656) on SequenceTileConfig, which the frame §5.18.7.4 non-uniform reuse_tile_params() branch consumes. Local §6.17.7 tile constraints are checked (tile-params/* diagnostics). Do not mark the umbrella done until this child is proven. WRITER (seq-header-writer-tile): write_sequence_tile_config + write_tile_params in crates/splot-core/src/write/seq_tile.rs invert the §5.4.2 / §5.18.7.3 parser exactly — the seq_tile_info_present_flag gate, the uniform increment-run encoding (with the no-stop-bit-at-max edge), and the non-uniform width/height run loops re-derived from the persisted SeqSb*Starts grid against the level/tier Tile_Width_Scaling_Factor / Tile_Area_Scaling_Factor tables. A reserved seq_level_idx has no defined tile layout, so a tile-present header at a reserved level is rejected before any bit with WriteError::UnwritableSequenceHeader (the §5.4.2 bounded residual, unwritable by construction); write=done and round-trip + byte-exact-tested across all seven tile modes. The §A Tile_Width/Area_Scaling_Factor tables are duplicated in write/seq_tile.rs (parser copies are private; the writer mission keeps the parser read-only) and drift-protected by the deterministic scaling_tables_drive_layout_across_all_levels test (a uniform round-trip at a 32768x32768/64x64-SB frame where both tables drive the layout, swept over every tier/level; mutation-verified)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/headers/sequence.rs::tests::sequence_tile_config_records_non_uniform_start_arrays", "crates/splot-core/src/headers/sequence.rs::tests::sequence_tile_config_reserved_level_records_empty_start_arrays", "crates/splot-core/src/tile.rs::tests", "crates/splot-core/src/write/seq_tile_tests.rs::tests", "crates/splot-core/src/write/seq_tile_proptests.rs::proptests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-cli/tests/cli.rs"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core tile", "cargo test -p splot-core write::seq_tile", "cargo test -p splot-validate sequence_header", "cargo test -p splot-cli inspect"]
fixtures = ["tests/fixtures/seq-header-tile-params.av2"]
diagnostics = ["tile-params/tile-cols-out-of-range", "tile-params/tile-rows-out-of-range", "tile-params/nonuniform-cols-do-not-cover-frame", "tile-params/nonuniform-rows-do-not-cover-frame"]

[[feature]]
id = "AV2-5.4.3-SEQUENCE-PARTITION-CONFIG"
name = "Sequence partition configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.3", "6.4.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.3 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-3, lines 664-728) states no requirement of bitstream conformance: every element is a descriptive enable/superblock-size flag. The validate residual is tool-flag-vs-usage gating (e.g. enable_ext_partitions / enable_uneven_4way_partitions consistency with their per-frame use), which is decidable only once frame-header inter-path parsing exists; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_config"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.4.4-SEQUENCE-SEGMENT-CONFIG"
name = "Sequence segment configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.4", "6.4.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "segmentation-tile-params-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. seg_info(MaxSegments) is now parsed via the shared AV2-5.4.9-SEGMENT-INFO helper, so the config parses fully (no bounded status). §6.4.4 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-4, lines 730-754) states no requirement of bitstream conformance: enable_ext_seg, seq_seg_info_present_flag, and seq_allow_seg_info_change are descriptive flags. The validate residual is the seq_allow_seg_info_change-vs-frame-level-override gating, decidable only with frame-header parsing; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/segment.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core segment", "cargo test -p splot-core write::seq_config"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.4.5-SEQUENCE-INTRA-CONFIG"
name = "Sequence intra configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.5", "6.4.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.5 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-5, lines 756-806) states no requirement of bitstream conformance: enable_dip, enable_intra_edge_filter, enable_mrls, enable_cfl_intra, cfl_ds_filter_index, enable_mhccp, enable_ibp are descriptive enable flags. The validate residual is tool-flag-vs-per-block-use gating (use_dip / cflAllowed / is_mhccp_allowed), decidable only with frame/tile parsing; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_config"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.4.6-SEQUENCE-INTER-CONFIG"
name = "Sequence inter configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.6", "6.4.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.6 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-6, lines 809-996) states exactly one requirement of bitstream conformance (lines 872-873): if long_term_frame_id_bits == 0, no OBU_RAS_FRAME shall be present in the coded video sequence. It is decidable from obu_type plus the active sequence header and has landed as frame-header/ras-requires-long-term-frame-id-bits (crates/splot-validate/src/context/mod.rs:6285). All other §6.4.6 elements are descriptive inter-tool enable flags whose per-frame use is gated under AV2-5.18.2-FRAME-HEADER-INFO. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_ras_requires_long_term_frame_id_bits", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-validate ras_requires", "cargo test -p splot-core write::seq_config"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = ["frame-header/ras-requires-long-term-frame-id-bits"]

[[feature]]
id = "AV2-5.4.7-SEQUENCE-SCC-CONFIG"
name = "Sequence screen-content-coding configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.7", "6.4.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.7 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-7, lines 999-1021) states no requirement of bitstream conformance: seq_choose_screen_content_tools / seq_force_screen_content_tools / seq_choose_integer_mv / seq_force_integer_mv only govern whether allow_screen_content_tools and force_integer_mv appear in the frame_header_info. The validate residual is that frame-level vs sequence-forced consistency, decidable only with frame-header parsing; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_config"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.4.8-SEQUENCE-TQ-ENTROPY-CONFIG"
name = "Sequence transform, quantization, and entropy configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.8", "6.4.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.8 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-8, lines 1024-1109) states no requirement of bitstream conformance: it is a block of transform/quant/entropy enable flags and delta-q offsets with inference rules (enable_fsc -> enable_idtx_intra, enable_tcq/choose_tcq_per_frame -> enable_parity_hiding) but no locally-decidable constraint at the sequence level. The validate residual is the tool-flag-vs-frame/coeff-use gating (allow_tcq, allow_parity_hiding, the *_delta_q_enabled offsets), decidable only with frame/coefficient parsing; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::tests", "crates/splot-core/src/write/seq_config_tests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_config"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.4.9-SEGMENT-INFO"
name = "Segment info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.9", "6.4.9"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/segment.rs"
change_id = "segmentation-tile-params-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Reusable seg_info(numSegments) parser shared by AV2-5.4.4-SEQUENCE-SEGMENT-CONFIG and AV2-5.7-MULTI-FRAME-HEADER. Uses the exact §5.4.9 Segmentation_Feature_Bits/_Signed/_Max tables and the AV2-4.11.7-SU descriptor; clips per spec (clipping is not a conformance error). Frame segmentation_params() reuse is future work."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/segment.rs::tests", "crates/splot-core/src/segment.rs::proptests", "crates/splot-core/src/write/segment.rs::tests", "crates/splot-core/src/write/segment.rs::proptests"]
commands = ["cargo test -p splot-core segment", "cargo test -p splot-core write::segment"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.4.10-SEQUENCE-FILTER-CONFIG"
name = "Sequence filter configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.10", "6.4.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.10 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-10, lines 1126-1198) states no requirement of bitstream conformance: it is loop-filter / CDEF / GDF / loop-restoration / CCSO enable and unit-size flags plus lr_tools_disable and df_par_bits_minus_2, all descriptive (the NOTEs make explicit that enabling a filter without using it is allowed). The validate residual is the filter-flag-vs-per-frame/per-block-use gating (df_delta_q bit width, CodedLossless auto-disable), decidable only with frame/tile parsing; blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing. Do not mark the umbrella done until this child is proven. WRITER (seq-header-writer-tile): write_sequence_filter_config in crates/splot-core/src/write/seq_tile.rs inverts the §5.4.10 parser exactly (the seq_sb_size-gated gdf/ccso unit flags, the lr mirrored-UV subfield chain, the single-picture cdef_on_skip_txfm adaptive gate, and df_par_bits_minus_2 f(2)); every field is pre-validated and every gated-off non-default field is rejected before any bit (reject-before-write), so write=done and round-trip-tested."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-core/src/write/seq_tile_tests.rs::tests", "crates/splot-core/src/write/seq_tile_proptests.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core write::seq_tile"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.4.11-USER-QM"
name = "Sequence user quantization matrix syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.11", "6.4.11"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/quantizer_matrix.rs"
change_id = "qm-filmgrain-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Implemented as the shared user_defined_qm(level, t, plane) helper (AV2 §5.4.11) used by quantizer_matrix_obu() (AV2-5.13-QUANTIZATION-MATRIX). Per the current parser it is NOT called directly from sequence_header_obu(); future frame-level quantization can reuse the helper. Covers the three Fundamental_Tx_Size shapes (TX_8X8/TX_8X4/TX_4X8), plane-copy, 8x8 symmetry, 4x8 transpose-from-8x4, the AV2 2D diagonal scan (get_scan, TX_CLASS_2D), svlc() quant deltas, and quant2 == 0 coefficient repeat; scan/transform derivation is from the AV2 spec and the AVM oracle obu_qm.c (no AV1 tables). It enforces the §6.4.11 quant_delta -128..=127 conformance bound (Error::InvalidQuantizerMatrix, surfaced as qm/quant-delta-out-of-range). Depends on AV2-4.11.4-SVLC. WRITER: user_defined_qm is inverted by write_quantizer_matrix (§5.13 quantizer_matrix_obu, crates/splot-core/src/write/quantizer_matrix.rs) to the canonical long form; round-trip proof on AV2-5.13-QUANTIZATION-MATRIX."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/quantizer_matrix_tests.rs::tests", "crates/splot-core/src/headers/quantizer_matrix.rs::tests", "crates/splot-core/src/headers/quantizer_matrix.rs::proptests"]
commands = ["cargo test -p splot-core quantizer"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.4.12-TIMING-INFO"
name = "Sequence timing info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.12", "6.4.12"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. timing_info() is now reached from the content-interpretation OBU (AV2-5.15-CONTENT-INTERPRETATION) via ci_timing_info_present_flag; cross-embedded-layer consistency (§6.4.12) is checked under AV2-6.4-SEQUENCE-HEADER-SEMANTICS. WRITER: the timing_info() structure is inverted by write_timing_info (crates/splot-core/src/write/content_interpretation.rs), used by the §5.15 content_interpretation_obu writer; round-trip proof on AV2-5.15-CONTENT-INTERPRETATION."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/content_interpretation_tests.rs::tests", "crates/splot-core/src/headers/sequence.rs::tests::sequence_timing_rejects_zero_display_tick", "crates/splot-core/src/headers/content_interpretation.rs::tests::parses_with_timing_info_present", "crates/splot-validate/src/checks/mod.rs::tests::syntax_error_diagnostic_maps_timing_errors"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-core content_interpretation", "cargo test -p splot-validate sequence_header"]
fixtures = []
diagnostics = ["sequence-header/timing-display-tick-zero", "sequence-header/timing-time-scale-zero", "sequence-header/timing-num-ticks-per-picture-out-of-range"]

[[feature]]
id = "AV2-5.4.13-SEQUENCE-DECODER-MODEL-INFO"
name = "Sequence decoder model info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.4.13", "6.4.13"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/sequence.rs"
change_id = "sequence-hls-validator-coverage"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.4-SEQUENCE-HEADER. §6.4.13 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-13, lines 1290-1310) states one locally-citable requirement of bitstream conformance — for a video sequence with random access points the sum decoder_buffer_delay + encoder_buffer_delay shall be kept constant (lines 1301-1302). That seq-header tier landed under buffer-delay-sum-constancy as the advisory decoder-model/buffer-delay-sum-changed-across-cvs and is tracked on AV2-6.4-SEQUENCE-HEADER-SEMANTICS (its proof.diagnostics carry the rule id), not on this syntax row. Residual: the remaining §6.4.13 elements (decoder_buffer_delay / encoder_buffer_delay smoothing-buffer arrival/removal timing and low_delay_mode_flag, lines 1293-1306) are Annex E decoder-model schedule semantics with no locally-decidable bitstream constraint of their own; decoder-model-blocked, owned by AV2-E-DECODER-MODEL. Do not mark the umbrella done until this child is proven."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests::sequence_decoder_model_info_parses_delays", "crates/splot-validate/src/validator/tests/sequence_state.rs::sequence_header_with_decoder_model_info_tail_can_activate", "crates/splot-core/src/write/seq_header.rs::tests", "crates/splot-core/src/write/seq_header.rs::proptests"]
commands = ["cargo test -p splot-core sequence", "cargo test -p splot-validate sequence_header", "cargo test -p splot-core write::seq_header"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-6.4-SEQUENCE-HEADER-SEMANTICS"
name = "Sequence header OBU semantics"
category = "normative"
kind = "validator-check"
spec_sections = ["6.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/checks/mod.rs"
change_id = "celu-orderhint-constraints"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Maps locally decidable §6.4.1 / §6.4.12 sequence-header and timing violations, plus cross-embedded-layer timing consistency (§6.4.12) compared across content-interpretation OBUs within a modeled CVS scope. CLK frame-header activation and exact §7.3.6 CVS scoping have landed (AV2-5.18-FRAME-HEADER / AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT), unblocking the distinct-mlayer-count, SWITCH/RAS dependency-self-containment, and monotonic-output-order checks (added under sequence-multistream-semantics). The §6.4.13 buffer-delay sum-constancy maintainer question (held in sequence-multistream-semantics) is resolved under buffer-delay-sum-constancy: AVM parses both decoder-model sites but never enforces or consumes the values (its only consumer hardcodes 70000/20000), so no differential oracle exists; per the 2026-06-10 two-tier maintainer decision the seq-header tier ships as the advisory decoder-model/buffer-delay-sum-changed-across-cvs (warning) because the spec's 'video sequence' scope is unspecified and the intra-CVS reading is already subsumed by §7.3.6 bit-identity (hls/repeated-sequence-header-not-identical). Still partial — documented-blocked split of the §6.4.1 residuals (celu-orderhint-constraints): (a) the OrderHint-agreement parts of §6.4.1's cross-operating-point and DOH consistency landed elsewhere as §7.3.6/§7.3.7 CELU checks (celu/output-order-hint-mismatch, celu/doh-order-hint-mismatch, celu/doh-order-hint-bits-mismatch, tracked by AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT / AV2-7.3.7-TEMPORAL-UNIT-ORDER; §6.4.1 line 331 defers the monotonic-output-order DOH constraints to §7.3.7, the home of those checks); (b) the §6.4.1 same-output-time constraint (mirror docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1 lines 327-329: all extended layers within a temporal unit share the same output time) is OUTPUT-TIMING-BLOCKED — it needs the decoder-model output-time schedule (no in-band per-frame output time is decidable; the §7.3.6/§7.3.7 OrderHint-agreement checks are the locally-decidable proxy that landed, but the output-time equality itself is owned by AV2-E-DECODER-MODEL); (c) the §6.4.1 get_disp_order_hint and explicit_ref_frame_map operating-point-consistency residuals (lines 583-601: the display-order-hint computation and implicit/explicit reference mapping must agree across all operating points associated with a frame) are REFERENCE-FRAME/OUTPUT-ORDER-BLOCKED — they need the reference-frame buffer state and get_ref_frames / get_disp_order_hint processes this phase does not model (blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing); and (d) the §7.3.6 monotonic_output_order_flag==0 OrderHint regression rule's full form (lines 579-584) stays deferred with its blocker named on AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT (output-order / MSB-extended OrderHint state, AV2-5.18.2-FRAME-HEADER-INFO). No bare-partial residual remains: each §6.4.1 residual now names its blocker."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "todo"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/sequence.rs::tests", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_repeated_non_identical_sequence_header_is_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_different_time_scale_across_embedded_layers_is_flagged", "crates/splot-validate/src/checks/syntax_error.rs::tests::syntax_error_diagnostic_maps_sequence_header_errors", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::switch_frame_depending_on_another_embedded_layer_is_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::ras_frame_depending_on_another_embedded_layer_is_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::self_contained_ras_frame_is_not_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::switch_frame_at_base_embedded_layer_is_not_flagged", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::distinct_mlayer_count_exceeds_seqmax_is_flagged", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::distinct_mlayer_count_within_seqmax_is_conforming", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::distinct_mlayer_count_resets_at_cvs_boundary", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::distinct_mlayer_count_before_first_clk_uses_active_header", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::distinct_mlayer_count_emits_once_per_cvs", "crates/splot-validate/src/validator/tests/monotonic_output_order.rs::monotonic_output_order_disagreement_inside_cmvs_is_flagged", "crates/splot-validate/src/validator/tests/monotonic_output_order.rs::monotonic_output_order_agreement_inside_cmvs_is_conforming", "crates/splot-validate/src/validator/tests/monotonic_output_order.rs::monotonic_output_order_disagreement_outside_cmvs_is_not_flagged", "crates/splot-validate/src/validator/tests/monotonic_output_order.rs::monotonic_output_order_disagreement_in_unknown_cmvs_is_not_flagged", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_seq_header_sum_change_across_cvs_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_seq_header_same_id_reconfiguration_across_cvs_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_seq_header_without_info_never_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_seq_header_fallback_guess_activation_never_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_external_hls_suppresses_both_ids", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_external_hls_without_seq_headers_still_suppresses_seq_advisory"]
commands = ["cargo test -p splot-validate sequence_header", "cargo test -p splot-validate hls", "cargo test -p splot-validate ci_", "cargo test -p splot-validate distinct_mlayer", "cargo test -p splot-validate monotonic_output_order", "cargo test -p splot-validate ras_frame", "cargo test -p splot-validate decoder_model"]
fixtures = []
diagnostics = ["sequence-header/timing-display-tick-zero", "sequence-header/timing-display-tick-mismatch", "sequence-header/timing-time-scale-mismatch", "sequence-header/timing-equal-picture-interval-mismatch", "sequence-header/timing-num-ticks-mismatch", "hls/repeated-sequence-header-not-identical", "frame-header/switch-or-ras-mlayer-dependency-not-self-contained", "sequence-state/distinct-mlayer-count-exceeds-seq-max", "sequence-state/monotonic-output-order-mismatch", "decoder-model/buffer-delay-sum-changed-across-cvs"]

[[feature]]
id = "AV2-6.2.2-OBU-HEADER-ACTIVATED-SEQUENCE-LIMITS"
name = "OBU header layer ids constrained by activated sequence header"
category = "normative"
kind = "validator-check"
spec_sections = ["6.2.2", "6.4", "7.3.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "obu-header-rap-activation-window"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Tracks parseable sequence headers by seq_header_id and xlayer context, then enforces the §6.2.2 obu_tlayer_id <= max_tlayer_id and obu_mlayer_id <= max_mlayer_id requirements (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2, lines 190-194) for non-global OBUs with an active sequence. The §7.3.8 availability dependency the residual was blocked on landed earlier: the §5.18.2 frame-confirmed activation path (sequence-multistream-semantics) plus the §7.3.8.1 random-access-point availability replay (rap-availability-replay, hls/unavailable-at-random-access-point) together model sequence-header activation and availability incl. resend-at-RAP and the BRT->OPS reference path. The §6.2.2 NOTE residual is now CLOSED (obu-header-rap-activation-window): the §6.2.2 NOTE (lines 197-198) scopes the limits to the *activated* header's window, so the limit check evaluates obu_tlayer_id/obu_mlayer_id against the limits snapshotted at the latest §5.18.2 frame-confirmed activation (frame_confirmed_activated_limits) rather than re-reading the active id's payload from the live store. A §7.3.6 same-seq_header_id redefinition (legal only at a coded-video-sequence boundary) is stored the moment it is sent but does not re-activate until its confirming frame, so an OBU between the redefinition and that frame — between a random access point and its resend-confirmed activation — stays bounded by the prior activation's limits (no false positive when a redefinition tightens; no under-report when it loosens). This mirrors the §6.4.1 distinct-mlayer 'compare against the CLK-activated header' precedent. A fallback-only layer (no frame confirmation) still reads the live store, preserving the eager pre-frame behavior. Reading confirmed against the AVM reference (decoder split: stored seq_list[xlayer][id] vs active_seq[xlayer]; the §6.2.2 max_tlayer/mlayer check runs only at frame activation against the active — not the just-stored redefined — header) and is internally consistent with §6.4.1 (per-OBU activation event vs per-CVS membership are different referents). A fully-conformant single-extended-layer regression vector pins the no-false-positive case: an OBU_PADDING at obu_tlayer_id 2 in the pre-CLK redefinition window is bounded by the prior activated max 3 (redefinition_window_padding_carrier_full_stream_is_conformant)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "todo"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/sequence_state.rs::active_sequence_header_bounds_temporal_layer_id", "crates/splot-validate/src/validator/tests/sequence_state.rs::active_sequence_header_bounds_embedded_layer_id", "crates/splot-validate/src/validator/tests/sequence_state.rs::redefinition_tightening_before_reconfirming_frame_is_not_flagged", "crates/splot-validate/src/validator/tests/sequence_state.rs::redefinition_before_reconfirming_frame_still_flags_old_limit_violation", "crates/splot-validate/src/validator/tests/sequence_state.rs::tightened_limit_applies_after_reconfirming_frame", "crates/splot-validate/src/validator/tests/sequence_state.rs::redefinition_window_padding_carrier_full_stream_is_conformant"]
commands = ["cargo test -p splot-validate sequence_header", "cargo test -p splot-validate active_sequence", "cargo test -p splot-validate redefinition"]
fixtures = []
diagnostics = ["sequence-state/tlayer-exceeds-max", "sequence-state/mlayer-exceeds-max", "sequence-state/no-active-sequence-header"]

[[feature]]
id = "AV2-5.5-TEMPORAL-DELIMITER"
name = "Temporal delimiter OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.5", "6.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/obu.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Payload syntax and validator state effect for temporal-unit boundaries. The locally-decidable temporal-delimiter rules have landed: §6.2.2 obu_xlayer_id == GLOBAL_XLAYER_ID and base-layer obu_tlayer_id/obu_mlayer_id == 0 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2, lines 201-214; obu-header/global-xlayer-required and obu-header/base-layer-only-types) and the §6.2.3 empty payload trailing_bits() tail. §6.5 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-5, lines 1318-1320) defines only the SeenFrameHeader frame-unit state variable. Residual: the temporal-delimiter's SeenFrameHeader reset and temporal-unit boundary effect (global TD, prefix-OBU ordering, ascending-xlayer coded-extended-layer-unit ordering) are modeled by the temporal-unit state machine and owned by AV2-7.3.7-TEMPORAL-UNIT-ORDER. Groundwork note: the §6.5 SeenFrameHeader frame-unit state has a structural analogue now that frame-unit-segmentation segments coded frame units per layer (crates/splot-validate/src/frame_unit.rs FrameUnitSegmenter) — the segmenter knows when a coded frame's first OBU begins a frame unit (the SeenFrameHeader set point), which the SeenFrameHeader-reset half of this row needs; the precise §6.5 reset semantics stay blocked on the §5.19 tile_group_obu() parser (AV2-5.19-TILE-GROUP), which sets/clears SeenFrameHeader at the frame-header / frame-header-copy level. WRITER landed (obu-writer-dispatch): the complete-OBU dispatch's ParsedObu::TemporalDelimiter arm (crates/splot-core/src/write/dispatch.rs) emits the §5.5 empty payload — no body and no tail (finish_obu_payload returns early for an empty payload) — so an OBU_TEMPORAL_DELIMITER round-trips through write_complete_obu; there is nothing else to serialize, so the writer is complete."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/obu.rs::tests", "crates/splot-core/src/write/dispatch_roundtrip_tests.rs::tests::temporal_delimiter_payload_round_trips", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_duplicate_temporal_delimiter_is_flagged"]
commands = ["cargo test -p splot-core obu", "cargo test -p splot-core write::dispatch", "cargo test -p splot-validate hls"]
fixtures = ["tests/fixtures/conformant.av2"]
diagnostics = ["obu-order/duplicate-temporal-delimiter", "obu-order/temporal-unit-missing-delimiter"]

[[feature]]
id = "AV2-5.6-MSDO"
name = "Multistream decoder operation OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.6", "6.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/hls.rs"
change_id = "msdo-substream-constraint-checks"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Full syntax parsed; the §6.6 layer-id (obu_tlayer_id/obu_mlayer_id == 0, obu_xlayer_id == GLOBAL_XLAYER_ID) and num_streams_minus_2 <= 2 checks are enforced (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-6, lines 1330-1337; msdo/non-global-layer-id, msdo/too-many-streams). The msdo-substream-constraint-checks change landed the remaining §6.6 conformance sentences and the §7.3.8.2 non-RAP identity rule, all from already-parsed state. (a) The §6.6 sub-stream PTL floors/ceilings: locally-decidable multistream_profile_idc >= sub_stream_max_profile[i] for all i (line 1347; msdo/profile-below-substream-max) and a reserved multistream_profile_idc (lines 1339-1341 bind its value space to seq_profile_idc / Annex A.2 Table A.1 — the spec's 'Table A.4' is an erratum; annex-a/profile-reserved); plus the cross-OBU seq_profile_idc <= sub_stream_max_profile[i] / seq_level_idx <= sub_stream_max_level[i] / seq_tier <= sub_stream_max_tier[i] for each sequence header activated by sub-stream i (lines 1362-1378; msdo/substream-profile-exceeds-max / -level- / -tier-), evaluated on §5.18.2 frame-confirmed activation against the recorded sub_xlayer_id[i]->ceiling map in both arrival orders (MSDO-then-activation and activation-then-MSDO), suppressed under ExternalHlsMode::Provided(declares a sequence header), deduped by (xlayer, seq_header_id, cvs_epoch, ceiling, value-space fingerprint). (b) The monotonic_output_order_flag==0 => multistream_doh_constraint_flag==1 rule (lines 1391-1393; msdo/doh-constraint-required), gated on CmvsState::Inside exactly like the §6.4.1 monotonic agreement check, both arrival orders. (c) §7.3.8.2 non-RAP identity (line 716; msdo/non-rap-not-identical): the §7.4.1 random-access-point definition (a TU containing CLK/OLK/RAS) is OBU-type-detectable from raw headers — NOT frame-header-activation-blocked, correcting the earlier note — so each TU's MSDO payload fingerprint is buffered and, at TU end (including the end-of-stream flush), a non-RAP TU whose MSDO differs from the previous OBU_MSDO is flagged (pairwise-previous for multiple MSDOs in one TU; RAP TUs update the reference without comparison). Residuals keeping validate=partial: the §7.3.8.2 first-sentence availability-at-RAP requirement ('shall be available ... by inclusion in the bitstream or by provision through external means') was reviewed under rap-availability-replay and intentionally NOT landed as a generic error — §7.3.8.2's own second sentence ('The requirements on the presence of MSDO OBUs depend on the interoperability point, as specified in Annex A.2 Profiles') makes a missing MSDO at a RAP interop-point-dependent, not unconditionally a violation, and §7.4.1 step 2 lets a RAP legitimately drop MSDO to set MultiStreamDecoderMode==0 (a §7.3.2 end-condition-2 CMVS end); firing without the Annex A.2 MSDO-presence model would false-positive on a conformant multistream→single-stream RAP transition, so it stays a named residual blocked on that model (the Table A.4 IOP MSDO-presence machinery under msdo-global-lcr-agreement is the home for the interop-gated presence requirement); the §6.6 NOTE's MultiStreamDecoderMode==1 substream level scaling is deferred on AV2-A-LEVELS-TIERS; and the Table A.4 IOP MSDO-presence machinery re-lands with msdo-global-lcr-agreement (it reuses the sub_xlayer_id[i]->ceiling state recorded here). WRITER landed (msdo-writer; write_msdo in crates/splot-core/src/write/msdo.rs inverts §5.6 for both allocation forms; the complete-OBU dispatch routes ParsedObu::Msdo to it + the non-extensible trailing-bits tail instead of Unimplemented; reject-before-write on sub_stream_count/unused-slot/gated-large_picture_idc/field-width; NonCanonicalMsdo variant added; the second of the 9 unwritten OBU-type body writers)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/hls.rs::tests", "crates/splot-core/src/write/msdo_tests.rs::tests", "crates/splot-core/src/write/dispatch_tests.rs::tests", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_msdo_too_many_streams_is_flagged", "crates/splot-validate/src/validator/tests/msdo_substream.rs::msdo_profile_below_substream_max_is_flagged", "crates/splot-validate/src/validator/tests/msdo_substream.rs::msdo_reserved_multistream_profile_is_flagged", "crates/splot-validate/src/validator/tests/msdo_substream.rs::substream_level_exceeds_max_is_flagged_msdo_first", "crates/splot-validate/src/validator/tests/msdo_substream.rs::substream_level_exceeds_max_is_flagged_activation_first", "crates/splot-validate/src/validator/tests/msdo_substream.rs::substream_level_equal_to_max_is_conforming", "crates/splot-validate/src/validator/tests/msdo_substream.rs::doh_constraint_required_is_flagged_msdo_first", "crates/splot-validate/src/validator/tests/msdo_substream.rs::doh_constraint_required_is_flagged_activation_first", "crates/splot-validate/src/validator/tests/msdo_substream.rs::non_rap_changed_msdo_is_flagged", "crates/splot-validate/src/validator/tests/msdo_substream.rs::rap_changed_msdo_is_conforming", "crates/splot-validate/src/validator/tests/msdo_substream.rs::non_rap_changed_msdo_at_end_of_stream_is_flagged"]
commands = ["cargo test -p splot-core hls", "cargo test -p splot-validate hls", "cargo test -p splot-validate substream", "cargo test -p splot-validate -- doh_constraint non_rap rap_changed msdo_profile msdo_reserved"]
fixtures = []
diagnostics = ["msdo/non-global-layer-id", "msdo/too-many-streams", "msdo/profile-below-substream-max", "annex-a/profile-reserved", "msdo/substream-profile-exceeds-max", "msdo/substream-level-exceeds-max", "msdo/substream-tier-exceeds-max", "msdo/doh-constraint-required", "msdo/non-rap-not-identical"]

[[feature]]
id = "AV2-5.7-MULTI-FRAME-HEADER"
name = "Multi-frame header OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.7", "6.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/hls.rs"
change_id = "mfh-frame-header-state"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "multi_frame_header_obu() syntax is now parsed in full, including seg_info() via the shared AV2-5.4.9-SEGMENT-INFO helper (no bounded status). Local id ranges are checked, and the mfh_seq_header_id -> sequence-header reference availability (§7.3.8.6/§7.3.8.7) is validated against the in-band store plus optional external HLS (see AV2-7.3.8-HLS-AVAILABILITY). Fully parsed MFHs run the §5.2.1 payload-tail check and are recorded as MultiFrameHeaderRecord availability, consumed by the frame-header cur_mfh_id reference check (hls/unavailable-multi-frame-header). Under mfh-frame-header-state the MultiFrameHeaderRecord now also carries the parsed §5.7 state a cur_mfh_id > 0 frame header consumes at §5.18.2 (mfh_frame_size, mfh_seg_info_present_flag/mfh_ext_seg_flag/mfh_allow_seg_info_change + parsed MfhFeatureEnabled/Data, and mfh_deblocking_filter_update groundwork); the validator and inspect resolve cur_mfh_id > 0 frames through this record into the core parse (AV2-5.18.4-FRAME-SIZE / AV2-5.18.7-SEGMENTATION-TILING). A cur_mfh_id whose in-band MFH is unresolvable keeps the existing Unknown routing. The §7.3.8.7 layer-dependency constraints are enforced at that reference with the §6.17.2 predicate against the loaded in-band sequence header's maps (frame-header/mfh-mlayer-dependency-missing, frame-header/mfh-tlayer-dependency-missing). The §7.3.8.1 availability half is complete under rap-availability-replay: an MFH (re)send is recorded in the RapReplayTracker, the frame-header cur_mfh_id reference is replayed (a MFH referenced at/after a random access point but not resent in/after it fires hls/unavailable-at-random-access-point), and the MFH's own mfh_seq_header_id sequence-header reference is replayed the same way. The §6.17.2 MFH frame-size bound (after load_sequence_header(), mfh_frame_width/height_minus_1[ cur_mfh_id ] <= max_frame_width/height_minus_1; mirror docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2 :4348-4349) now fires at the frame on the referenced MFH's STORED dims via the dedicated frame-header/mfh-frame-size-exceeds-sequence-max: frame_header_core_checks resolves the cur_mfh_id > 0 record (shared resolve_frame_mfh_record §7.3.8.7 discipline) and compares the record's stored mfh_frame_size dims against the active maxima REGARDLESS of frame_size_override_flag AND of the frame-header core parse outcome — so a frame overriding to in-range dims no longer hides an out-of-range MFH, and a truncated/malformed frame-header remainder no longer silences the check (the predicate needs only the resolved record + the sequence maxima, so it runs at the load_sequence_header point before the core parse). An omitted mfh_frame_size infers the maxima (§5.18.2, mirror :4101) and stays silent; an unresolvable MFH stays silent. No double-fire on the override==0 default path: the §6.17.2 stored-MFH rule is the single home and the §6.17.4.1 derived frame-header/frame-size-exceeds-sequence-max defers by parsed PATH (frame_size_override_flag == 0 on the resolved cur_mfh_id > 0 frame), NOT by dimension equality — so an override==1 frame coding the same out-of-range dims fires both rules (tracked on AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS / AV2-5.18.4-FRAME-SIZE). Remaining residual (keeps validate=partial): mfh_deblocking_filter_update is recorded but the deblocking parse  it gates lands with frame-filtering-deblocking-gdf-cdef (AV2-5.18.5-FILTERING). WRITER landed (multi-frame-header-writer; write_multi_frame_header in crates/splot-core/src/write/multi_frame_header.rs inverts §5.7 — the uvlc ids, the gated mfh_frame_size, the deblocking-update flags, and seg_info(mfh_ext_seg_flag ? 16 : 8) via the shared write_seg_info; the complete-OBU dispatch routes ParsedObu::MultiFrameHeader to it + the extensible tail instead of Unimplemented. Reject-before-write on the forced-false mfh_apply_deblocking_filter array, the seg-info Options vs the stored present-flag, and the frame-size bit-width range; tolerated out-of-range ids reproduced verbatim; NonCanonicalMultiFrameHeader variant added; the seventh of the 9 unwritten OBU-type body writers, validated by an adversarial review + the roundtrip_obu_bytes fuzz target)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/hls.rs::tests", "crates/splot-core/src/write/multi_frame_header_tests.rs::tests", "crates/splot-core/src/write/dispatch_tests.rs::tests", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_mfh_out_of_range_ids_are_flagged", "crates/splot-validate/src/validator/tests/hls_frame_header.rs::hls_frame_header_mfh_available_is_accepted", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::frame_mfh_mlayer_dependency_missing_is_flagged", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::frame_mfh_tlayer_dependency_missing_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_multi_frame_header_reference_before_rap_is_flagged", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_backed_frame_is_decided_not_unknown", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_referencing_different_seq_than_active_is_unknown", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_default_dims_parse_through_tile_info", "crates/splot-cli/tests/cli.rs::inspect_json_exposes_mfh_backed_frame_header_core"]
commands = ["cargo test -p splot-core hls", "cargo test -p splot-core write::multi_frame_header", "cargo +nightly fuzz run roundtrip_obu_bytes", "cargo test -p splot-validate hls", "cargo test -p splot-validate mfh_", "cargo test -p splot-validate frame_mfh", "cargo test -p splot-validate rap_replay", "cargo test -p splot-validate celu_mfh", "cargo test -p splot-cli mfh"]
fixtures = ["tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = ["mfh/seq-header-id-out-of-range", "mfh/id-out-of-range", "mfh/sequence-header-unavailable", "hls/unavailable-multi-frame-header", "hls/unavailable-at-random-access-point", "frame-header/mfh-mlayer-dependency-missing", "frame-header/mfh-tlayer-dependency-missing"]

[[feature]]
id = "AV2-5.8-LAYER-CONFIG-RECORD"
name = "Layer configuration record OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8", "6.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Full §5.8 layer_config_record_obu() parser (global/local; lcr_global_info, lcr_local_info, lcr_aggregate_info, lcr_seq_profile_tier_level_info, the length-bounded lcr_global_payload, lcr_xlayer_info, lcr_rep_info, lcr_embedded_layer_info, lcr_xlayer_color_info), dispatched by open_bitstream_unit and surfaced in inspect --json. splot-validate warns on non-zero reserved-zero fields (§6.8), records in-band LCR availability, checks local->global LCR (§7.3.8.3), local->local-atlas (§7.3.8.4), sequence-header seq_lcr_id resolution including the global lcr_xlayer_map membership rule (§6.4.1), and the §6.8.9 dependency-map agreement of the activated LCR's embedded-layer maps against the activated sequence header (lcr/mlayer-dependency-missing, lcr/tlayer-dependency-missing). §6.8 defines no identical-repeated-LCR requirement, so a duplicate-not-identical check is intentionally not emitted. WRITER landed (layer-config-record-writer): write_layer_config_record (crates/splot-core/src/write/layer_config_record.rs) inverts parse_layer_config_record and the §5.8.1-5.8.9 nest (global/local branch, the length-bounded lcr_global_payload filler, the lcr_xlayer_info byte_alignment sites, the embedded-vs-atlas else-branch), routed by the complete-OBU dispatch + the generic extensible tail; reject-before-write on the decidable inconsistencies (gated Options vs flags, the lcr_xlayer_map / lcr_mlayer_map set-bit-derived list lengths/ids, the embedded-vs-atlas exclusivity, the payload filler invariant), reproducing the §6.8 reserved-zero and descriptive values verbatim. Header-derived parse-context ids (LcrLocalInfo.xlayer_id, the PTL xlayer_id) are not bit fields and are ignored on write. The complete-OBU dispatch now writes 13 of 14 OBU types (only QuantizationMatrix remains). Encoder remains future work."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::hls_seq_lcr_missing_record_is_flagged", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_local_missing_global_is_flagged"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-core write::layer_config_record", "cargo test -p splot-core write::dispatch", "cargo +nightly fuzz run roundtrip_obu_bytes", "cargo test -p splot-validate lcr", "cargo test -p splot-validate hls"]
fixtures = []
diagnostics = ["lcr/reserved-bits-nonzero", "lcr/payload-size-overflow", "lcr/global-id-out-of-range", "lcr/xlayer-map-empty", "lcr/local-id-zero", "lcr/dependent-xlayers-flag-nonzero", "lcr/global-lcr-unavailable", "lcr/global-xlayer-map-missing-xlayer", "hls/unavailable-layer-configuration-record", "lcr/mlayer-dependency-missing", "lcr/tlayer-dependency-missing"]

[[feature]]
id = "AV2-5.8.1-LCR-GLOBAL-INFO"
name = "LCR global info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.1", "6.8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "msdo-global-lcr-agreement"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. The locally-decidable §6.8.2 requirements landed earlier (recorded on the umbrella AV2-5.8-LAYER-CONFIG-RECORD): lcr_global_config_record_id in 1..7 (lcr/global-id-out-of-range), lcr_xlayer_map in 1..(1<<31)-1 (lcr/xlayer-map-empty), and lcr_dependent_xlayers_flag==0 decoder-ignore (lcr/dependent-xlayers-flag-nonzero) — docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-2, lines 1543-1579. The cross-OBU MSDO<->activated-global-LCR agreement (mirror lines 1646-1678) and the §6.8.2 LCR DOH-constraint requirement (lines 1619-1621) now land under msdo-global-lcr-agreement, deferred to CMVS-membership resolution (the same mechanism as msdo/doh-constraint-required, gated on the §7.3.2 CmvsTracker resolving Inside and the association chain resolving an *activated* global LCR — an observed-but-unactivated global LCR fires nothing): lcr/msdo-stream-count-mismatch (constraint 1), lcr/msdo-sub-xlayer-not-in-lcr (constraint 2), lcr/msdo-aggregate-mismatch (constraint 3: Table A.6 config consistency, Table A.1 interop equality, level, tier), lcr/msdo-substream-ptl-mismatch (constraint 4, exact equality), lcr/msdo-doh-flag-mismatch (constraint 5), and lcr/doh-constraint-required (the §6.8.2 DOH requirement). Residual: the §6.8.2 \"all extended layers present in the multistream shall reference the same activated global LCR\" rule (lines 1550-1551) and the lcr_enforce_tile_alignment_flag cross-layer tile-structure check (lines 1627-1632) stay future (the same-global-LCR reconciliation needs multi-activation arbitration; the tile-alignment check is frame/tile-blocked). AV2-7.3.8-HLS-AVAILABILITY owns the availability model. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_stream_count_mismatch_is_flagged_both_orders", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_sub_xlayer_not_in_lcr_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_aggregate_level_and_tier_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_aggregate_interop_and_config_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_substream_ptl_mismatch_is_flagged_both_orders", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_doh_flag_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_doh_constraint_required_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::lcr_msdo_agreement_inert_for_unactivated_global_lcr"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_msdo", "cargo test -p splot-validate lcr_doh"]
fixtures = []
diagnostics = ["lcr/msdo-stream-count-mismatch", "lcr/msdo-sub-xlayer-not-in-lcr", "lcr/msdo-aggregate-mismatch", "lcr/msdo-substream-ptl-mismatch", "lcr/msdo-doh-flag-mismatch", "lcr/doh-constraint-required"]

[[feature]]
id = "AV2-5.8.2-LCR-LOCAL-INFO"
name = "LCR local info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.2", "6.8.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.3 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-3, lines 1690-1734) has one requirement of bitstream conformance — lcr_local_id != 0 (line 1705) — landed as lcr/local-id-zero, plus the lcr_local_reserved_zero_3bits/5bits decoder-ignore fields landed as lcr/reserved-bits-nonzero (via has_nonzero_reserved_bits). Nothing further is locally decidable for §6.8.3. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_local_id_zero_is_flagged", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_reserved_bits_nonzero_is_warned"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_local_id"]
fixtures = []
diagnostics = ["lcr/local-id-zero", "lcr/reserved-bits-nonzero"]

[[feature]]
id = "AV2-5.8.3-LCR-AGGREGATE-INFO"
name = "LCR aggregate info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.3", "6.8.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "lcr-aggregate-info-annex-a-range"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.4 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-4, lines 1737-1760) states no requirement of bitstream conformance other than three Annex-A value-space constraints: lcr_config_idc, lcr_aggregate_level_idx, and lcr_max_interop shall not take values outside those specified in Annex A (lcr_max_tier_flag is a 1-bit field with no such clause). All three now land in splot-validate (checks/mod.rs LayerConfigRecordSyntax, Global arm), decidable from the parsed global LCR's lcr_aggregate_info() alone (the requirement is on the bitstream *containing* the value — no activation): lcr/config-idc-reserved (Annex A.3 Table A.5 defines configs 0..=2; 3..=63 reserved, mirror lines 1744-1747), lcr/aggregate-level-idx-reserved (Annex A.4 Table A.7 reserves level indices 22..=30, mirror lines 1749-1752), and lcr/max-interop-reserved (Annex A.3 Table A.3 defines interoperability points 0, 1, 2, 15; 3..=14 reserved, mirror lines 1757-1759). Backed by annex_a.rs is_defined_config_idc / is_reserved_level / is_defined_max_interop helpers, each table-verified by a unit test. No §6.8.4 value-space residual remains. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/write/layer_config_record_tests.rs::tests",
    "crates/splot-core/src/headers/layer_config_record.rs::tests",
    "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_config_idc_reserved_value_is_flagged",
    "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_aggregate_level_idx_reserved_value_is_flagged",
    "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_max_interop_reserved_value_is_flagged",
    "crates/splot-validate/src/validator/tests/lcr_atlas.rs::lcr_aggregate_info_defined_values_are_accepted",
    "crates/splot-validate/src/annex_a.rs::tests::defined_max_interop_values_match_table_a3",
]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.8.4-LCR-SEQ-PTL-INFO"
name = "LCR sequence profile tier level information syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.4", "6.8.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "lcr-ptl-activated-sequence-agreement"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.5 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-5, lines 1768-1814) PTL-vs-activated-sequence-header ceilings now LAND under lcr-ptl-activated-sequence-agreement in splot-validate (crates/splot-validate/src/context/mod.rs, check_lcr_ptl_ceilings): when lcr_seq_profile_tier_level_info(i) is present in the LCR activated by extended layer i's frame-confirmed sequence header, seq_profile_idc <= lcr_seq_profile_idc[i] (lcr/ptl-profile-exceeds-max, lines 1774-1776), seq_level_idx <= lcr_max_level_idx[i] (lcr/ptl-level-exceeds-max, 1782-1784), seq_tier <= lcr_tier_flag[i] (lcr/ptl-tier-exceeds-max, 1793-1795), and seq_max_mlayer_cnt_minus_1+1 <= lcr_max_mlayer_count[i] (lcr/ptl-mlayer-count-exceeds-max, 1808-1810), for each sequence header activated by the xlayer-i sub-bitstream (<= semantics, equality passes; absent PTL info compares nothing). The §6.8.5 sentence keys the PTL on the *local* LCR (\"associated with the local LCR ... indicated in an extended layer with obu_xlayer_id equal to i\"), so the local record's lcr_seq_profile_tier_level_info(xlayerId) is snapshotted into the §6.4.1 association at association time (LcrAssociation.local_ptl); a global record carrying lcr_seq_profile_tier_level_info(i) for that xlayer is used as the fallback when the association resolved a global record. Comparisons run from on_sequence_activation against the frame-confirmed activated header; a non-identical LCR redefinition re-checks at the next activation via the content-fingerprinted dedup key (LcrPtlFindingKey); suppressed under ExternalHlsMode::Provided that declares a sequence header. Residual (partial): (2) Annex-A range enforcement on the LCR-declared maxima lcr_seq_profile_idc[i]/lcr_max_level_idx[i]/lcr_tier_flag[i]/lcr_max_mlayer_count[i] (each 'shall not contain values outside those specified in Annex A') is NOT in this change — only lcr_max_level_idx[i] maps onto Table A.7's value space (annex-a/level-reserved territory), and the profile/tier/mlayer-count value spaces and the lcr_max_level_idx wiring stay on the Annex A profile/level/tier table backlog; (3) lsptli_reserved_2bits==0 (lines 1812-1814) is decoder-ignored, so it is at most a producer-anomaly warning, not yet emitted; and no AVM differential oracle has been run (avm_diff pending). WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_level_exceeds_max_is_flagged", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_profile_exceeds_max_is_flagged", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_tier_exceeds_max_is_flagged", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_mlayer_count_exceeds_max_is_flagged", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_equality_passes", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_absent_info_compares_nothing", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_unconfirmed_activation_is_silent_then_fires_on_frame", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_redefinition_rechecks_affected_layer", "crates/splot-validate/src/validator/tests/lcr_ptl.rs::lcr_ptl_global_record_ceiling_is_checked"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_ptl"]
fixtures = []
diagnostics = ["lcr/ptl-profile-exceeds-max", "lcr/ptl-level-exceeds-max", "lcr/ptl-tier-exceeds-max", "lcr/ptl-mlayer-count-exceeds-max"]

[[feature]]
id = "AV2-5.8.5-LCR-GLOBAL-PAYLOAD"
name = "LCR global payload syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.5", "6.8.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.6 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-6, lines 1818-1834) states one requirement of bitstream conformance — RemainingLcrPayloadBits shall not be less than 0 (lines 1833-1834) — parse-enforced in parse_lcr_global_payload (crates/splot-core/src/headers/layer_config_record.rs:563) and surfaced as lcr/payload-size-overflow. lcr_remaining_payload_bit is decoder-ignored, so nothing further is locally decidable for §6.8.6. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests::global_payload_too_small_size_is_overflow"]
commands = ["cargo test -p splot-core lcr"]
fixtures = []
diagnostics = ["lcr/payload-size-overflow"]

[[feature]]
id = "AV2-5.8.6-LCR-XLAYER-INFO"
name = "LCR xlayer info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.6", "6.8.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.7 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-7, lines 1838-1915) states no requirement of bitstream conformance: it is a presence-flag / purpose-id / priority / rendering-method container whose only constrained value (lcr_xlayer_rendering_method 5..255) is a reserved interpretation that decoders shall ignore. The structure is validated by the §5.8 syntax parse: the LayerConfigRecordSyntax check (registry id lcr/syntax in crates/splot-validate/src/checks/mod.rs:601) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error, the trailing-bits/* ids, and obu-header/extension-flag-not-zero for the extensible payload tail); nothing further is locally decidable for §6.8.7. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests"]
commands = ["cargo test -p splot-core lcr"]
fixtures = []
diagnostics = ["bitstream/parse-error", "obu-header/extension-flag-not-zero", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.8.7-LCR-REP-INFO"
name = "LCR representation info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.7", "6.8.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "lcr-ptl-activated-sequence-agreement"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.8 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-8, lines 1918-1969) rep-info equality now LANDS under lcr-ptl-activated-sequence-agreement in splot-validate (crates/splot-validate/src/context/mod.rs, check_lcr_rep_info_agreement), resolving the honesty-sweep (PR #45) residual note: for the LCR activated by extended layer j's frame-confirmed sequence header, lcr_max_pic_width[i][j] == max_frame_width_minus_1 + 1, lcr_max_pic_height[i][j] == max_frame_height_minus_1 + 1 (lines 1925-1933, always present), lcr_bit_depth_idc == bit_depth_idc and lcr_chroma_format_idc == chroma_format_idc (lines 1950-1958, gated on lcr_format_info_present_flag), lcr_cropping_window_present_flag == seq_cropping_window_present_flag and (when the LCR cropping window is present) lcr_cropping_win_*_offset == seq_cropping_win_*_offset (lines 1943-1968). Single rule lcr/rep-info-mismatch (error, exact equality), the disagreeing field named in the message; absent rep-info / absent format-info compares nothing for those fields. seq_cropping_window_present_flag is now retained on SequenceHeaderGeneral (it was previously collapsed to a default cropping window) so the flag-equality sentence is exact. The rep-info lives on the *local* record at LcrLocalInfo.xlayer_info.rep_info (isGlobal=0) and on the *global* payload at LcrGlobalPayload.xlayer_info.rep_info (isGlobal=1); the association-time snapshot (LcrAssociation.local_rep_info / global_rep_info_by_xlayer) keeps the comparison pinned to the revision the header associated to. Comparisons run from on_sequence_activation against the frame-confirmed activated header; a non-identical LCR redefinition re-checks at the next activation via the content-fingerprinted dedup key (LcrRepInfoFindingKey); suppressed under ExternalHlsMode::Provided that declares a sequence header. Residual (partial): no AVM differential oracle has been run (avm_diff pending). WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_width_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_height_bit_depth_chroma_mismatches_are_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_cropping_present_flag_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_cropping_offset_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_full_agreement_passes", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_absent_format_info_compares_nothing", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_absent_rep_info_compares_nothing", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_global_record_is_checked", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_unconfirmed_activation_is_silent_then_fires_on_frame", "crates/splot-validate/src/validator/tests/lcr_rep_info.rs::lcr_rep_info_redefinition_rechecks_affected_layer"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_rep_info"]
fixtures = []
diagnostics = ["lcr/rep-info-mismatch"]

[[feature]]
id = "AV2-5.8.8-LCR-EMBEDDED-LAYER-INFO"
name = "LCR embedded layer info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.8", "6.8.9"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. The §6.8.9 dependency-map agreement of the activated LCR's lcr_mlayer_map/lcr_tlayer_map against the activated sequence header is checked in splot-validate (diagnostics recorded on the umbrella row). reference-state-and-random-access adds the §6.8.9 pure-arithmetic expected-dimension bound: lcr/max-expected-dims-exceed-sequence-max fires when an activated LCR's lcr_max_expected_width[..][j] / lcr_max_expected_height[..][j] (per embedded layer, present when lcr_same_sh_max_resolution_flag == 0) exceeds the activated sequence header's max_frame_width/height_minus_1 + 1 (mirror docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-9 :2135-2148). The LcrEmbeddedMaps association snapshot now retains the per-layer expected dims, and check_lcr_expected_dims_bounds (context.rs) joins them against the frame-confirmed activated sequence header (same Disabled-only / strict-activation gating as the §6.8.8 rep-info check, anchored at the LCR OBU). RESIDUAL: the companion per-frame FrameWidth/FrameHeight <= lcr_max_expected_* clause (mirror :2137-2139 / :2144-2146) needs each frame's (obu_xlayer_id, obu_mlayer_id) -> (xId, j) mapping joined against the activated LCR and stays future; the §6.8.9 reserved-value decoder-ignore rules also stay future, so validate is partial. BLOCKED: the §6.8.9 per-frame FrameWidth/FrameHeight <= lcr_max_expected_* clause needs a per-frame (obu_xlayer_id, obu_mlayer_id) -> (xId, j) frame-to-LCR-record mapping the validator does not model yet. (The activated-LCR association itself IS modeled — snapshot_lcr_association / activated_global_lcr_in_window — so only the per-frame embedded-layer join is missing.) WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_mlayer_dependency_missing_is_flagged", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_tlayer_dependency_missing_is_flagged", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_max_expected_dims_exceed_sequence_max_is_flagged", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_max_expected_dims_at_sequence_max_boundary_is_silent", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_max_expected_dims_without_frame_confirmation_is_silent", "crates/splot-validate/src/validator/tests/layer_dependency_core.rs::lcr_max_expected_dims_suppressed_under_external_provided"]
commands = ["cargo test -p splot-core lcr", "cargo test -p splot-validate lcr_"]
fixtures = []
diagnostics = ["lcr/max-expected-dims-exceed-sequence-max"]

[[feature]]
id = "AV2-5.8.9-LCR-XLAYER-COLOR-INFO"
name = "LCR xlayer color info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.8.9", "6.8.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/layer_config_record.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.8-LAYER-CONFIG-RECORD; parsed by the §5.8 parser in layer_config_record.rs and tested there. §6.8.10 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-8-10, lines 2151-2167) states no requirement of bitstream conformance: layer_color_* are defined to share the interpretation of the ops_color_* elements with no extra §6.8.10 constraint. The structure is validated by the §5.8 syntax parse: the LayerConfigRecordSyntax check (registry id lcr/syntax in crates/splot-validate/src/checks/mod.rs:601) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error, the trailing-bits/* ids, and obu-header/extension-flag-not-zero for the extensible payload tail); nothing further is locally decidable for §6.8.10. WRITER: serialized by write_layer_config_record (the §5.8 umbrella writer, crates/splot-core/src/write/layer_config_record.rs), which inverts the whole §5.8.1-5.8.9 nest; round-trip proof on AV2-5.8-LAYER-CONFIG-RECORD."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/layer_config_record_tests.rs::tests", "crates/splot-core/src/headers/layer_config_record.rs::tests"]
commands = ["cargo test -p splot-core lcr"]
fixtures = []
diagnostics = ["bitstream/parse-error", "obu-header/extension-flag-not-zero", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.9-ATLAS-SEGMENT"
name = "Atlas segment info OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9", "6.9"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Full §5.9 atlas_segment_info_obu() parser (all five modes: enhanced/basic/single/multistream/multistream-with-alpha, plus ats_region_info, ats_region_to_segment_mapping, ats_label_segment_info), dispatched by open_bitstream_unit and surfaced in inspect --json. The parser range-checks ats_atlas_segment_mode_idc (<=4) and the segment/region counts before looping (typed errors -> atlas/* diagnostics). splot-validate records local atlas availability and checks local LCR -> local atlas references (§7.3.8.4); the global atlas reference is can be available (§7.3.8.4) so a missing global atlas is intentionally not flagged. See the child rows for §5.9.1 through §5.9.5. WRITER landed (atlas-segment-writer; write_atlas_segment in crates/splot-core/src/write/atlas_segment.rs inverts §5.9 + the §5.9.1–5.9.5 sub-structs for all five modes (single/enhanced/basic/multistream/multistream-with-alpha); the complete-OBU dispatch routes ParsedObu::AtlasSegment to it + the extensible tail instead of Unimplemented. num_segments is a DERIVED parse-context value the writer re-derives per mode (not written) and rejects a stored mismatch; reject-before-write on mode-vs-mode_info / gated-Options / count-vs-len / region dims, the §6.9.2 descriptive segment-id values reproduced verbatim; NonCanonicalAtlasSegment variant added; the sixth of the 9 unwritten OBU-type body writers, validated by an adversarial review + the roundtrip_obu_bytes fuzz target). The encoder remains future work."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/atlas_segment.rs::tests", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::atlas_local_atlas_unavailable_is_flagged"]
commands = ["cargo test -p splot-core atlas", "cargo test -p splot-validate atlas"]
fixtures = []
diagnostics = ["atlas/segment-mode-out-of-range", "atlas/region-dimension-out-of-range", "atlas/segment-count-out-of-range", "atlas/multistream-requires-global-xlayer", "atlas/duplicate-input-stream-id", "atlas/local-atlas-unavailable"]

[[feature]]
id = "AV2-5.9.1-ATLAS-LABEL-SEGMENT-INFO"
name = "Atlas label segment info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9.1", "6.9.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.9-ATLAS-SEGMENT; parsed by the §5.9 parser in atlas_segment.rs and tested there. §6.9.2 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-9-2, lines 2293-2307) states no requirement of bitstream conformance: ats_signaled_atlas_segment_ids_flag and ats_atlas_segment_id are descriptive id-assignment elements. The structure is validated by the §5.9 syntax parse: the AtlasSegmentSyntax check (registry id atlas/syntax in crates/splot-validate/src/checks/mod.rs:715) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error, the trailing-bits/* ids, and obu-header/extension-flag-not-zero for the extensible payload tail); nothing further is locally decidable for §6.9.2."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/atlas_segment.rs::tests"]
commands = ["cargo test -p splot-core atlas"]
fixtures = []
diagnostics = ["bitstream/parse-error", "obu-header/extension-flag-not-zero", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.9.2-ATLAS-ENHANCED-INFO"
name = "Atlas enhanced atlas info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9.2", "6.9.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.9-ATLAS-SEGMENT; parsed by the §5.9 parser in atlas_segment.rs and tested there. The §6.9.3 cluster's requirements of bitstream conformance (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-9-3-1, lines 2358-2368 and #s-6-9-3-2, lines 2403-2404) are all locally decidable and landed: ats_num_region_columns_minus_1 < MAX_ATLAS_COLS and ats_num_region_rows_minus_1 < MAX_ATLAS_ROWS as atlas/region-dimension-out-of-range, and NumRegionsInAtlas <= MAX_NUM_ATLAS_SEGMENTS (single-region mapping) as atlas/segment-count-out-of-range. The remaining region geometry (column widths / row heights) carries no stated §6.9.3 conformance requirement, so nothing further is locally decidable for this child. WRITER: serialized by write_atlas_segment (the §5.9 umbrella writer, crates/splot-core/src/write/atlas_segment.rs), which inverts all five atlas-mode bodies; round-trip proof on AV2-5.9-ATLAS-SEGMENT."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/atlas_segment_tests.rs::tests", "crates/splot-core/src/headers/atlas_segment.rs::tests::rejects_region_dimension_out_of_range", "crates/splot-core/src/headers/atlas_segment.rs::tests::rejects_segment_count_out_of_range"]
commands = ["cargo test -p splot-core atlas"]
fixtures = []
diagnostics = ["atlas/region-dimension-out-of-range", "atlas/segment-count-out-of-range"]

[[feature]]
id = "AV2-5.9.3-ATLAS-MULTISTREAM-INFO"
name = "Atlas multistream info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9.3", "6.9.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.9-ATLAS-SEGMENT; parsed by the §5.9 parser in atlas_segment.rs and tested there. §6.9.4 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-9-4, lines 2430-2465) states no requirement of bitstream conformance of its own: ats_msi_* share the semantics of the §6.9.6 ats_* elements and the background color values are inference-only. The locally decidable segment-count bound is parse-enforced in parse_ats_multistream_info (crates/splot-core/src/headers/atlas_segment.rs:504) as atlas/segment-count-out-of-range; cross-OBU multistream-needs-GLOBAL_XLAYER_ID and duplicate-input-stream-id are validated under AV2-5.9-ATLAS-SEGMENT. Nothing further is locally decidable for §6.9.4. The atlas_multistream_duplicate_input_stream_id_is_flagged proof test exercises the parent-level atlas/duplicate-input-stream-id check (owned by AV2-5.9-ATLAS-SEGMENT) in the multistream context as a regression guard; this row's own diagnostic is atlas/segment-count-out-of-range. WRITER: serialized by write_atlas_segment (the §5.9 umbrella writer, crates/splot-core/src/write/atlas_segment.rs), which inverts all five atlas-mode bodies; round-trip proof on AV2-5.9-ATLAS-SEGMENT."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/atlas_segment_tests.rs::tests", "crates/splot-core/src/headers/atlas_segment.rs::tests::rejects_segment_count_out_of_range", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::atlas_multistream_duplicate_input_stream_id_is_flagged"]
commands = ["cargo test -p splot-core atlas", "cargo test -p splot-validate atlas_multistream"]
fixtures = []
diagnostics = ["atlas/segment-count-out-of-range"]

[[feature]]
id = "AV2-5.9.4-ATLAS-MULTISTREAM-ALPHA-INFO"
name = "Atlas multistream with alpha info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9.4", "6.9.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.9-ATLAS-SEGMENT; parsed by the §5.9 parser in atlas_segment.rs and tested there. §6.9.5 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-9-5, lines 2468-2490) states no requirement of bitstream conformance: ats_msi_alpha_segments_present_flag and ats_msi_alpha_segment_flag are presence/inference-only flags layered on the §6.9.4 multistream info. The locally decidable segment-count bound shared with §6.9.4 is parse-enforced as atlas/segment-count-out-of-range; nothing further is locally decidable for §6.9.5. WRITER: serialized by write_atlas_segment (the §5.9 umbrella writer, crates/splot-core/src/write/atlas_segment.rs), which inverts all five atlas-mode bodies; round-trip proof on AV2-5.9-ATLAS-SEGMENT."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/atlas_segment_tests.rs::tests", "crates/splot-core/src/headers/atlas_segment.rs::tests::rejects_segment_count_out_of_range"]
commands = ["cargo test -p splot-core atlas"]
fixtures = []
diagnostics = ["atlas/segment-count-out-of-range"]

[[feature]]
id = "AV2-5.9.5-ATLAS-BASIC-INFO"
name = "Atlas basic info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.9.5", "6.9.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/atlas_segment.rs"
change_id = "hls-lcr-atlas-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child of AV2-5.9-ATLAS-SEGMENT; parsed by the §5.9 parser in atlas_segment.rs and tested there. §6.9.6 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-9-6, lines 2493-2528) states two locally decidable constraints, both landed: ats_num_atlas_segments_minus_1 < MAX_NUM_ATLAS_SEGMENTS (lines 2512-2513) as atlas/segment-count-out-of-range, and 'all values in ats_input_stream_id[] shall be unique' (line 2518) as atlas/duplicate-input-stream-id. The segment top-left/width/height geometry carries no stated §6.9.6 conformance requirement, so nothing further is locally decidable for §6.9.6. WRITER: serialized by write_atlas_segment (the §5.9 umbrella writer, crates/splot-core/src/write/atlas_segment.rs), which inverts all five atlas-mode bodies; round-trip proof on AV2-5.9-ATLAS-SEGMENT."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/atlas_segment_tests.rs::tests", "crates/splot-core/src/headers/atlas_segment.rs::tests::rejects_segment_count_out_of_range", "crates/splot-validate/src/validator/tests/lcr_atlas.rs::atlas_duplicate_input_stream_id_is_flagged"]
commands = ["cargo test -p splot-core atlas", "cargo test -p splot-validate atlas_duplicate"]
fixtures = []
diagnostics = ["atlas/segment-count-out-of-range", "atlas/duplicate-input-stream-id"]

[[feature]]
id = "AV2-5.10-OPERATING-POINT-SET"
name = "Operating point set OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.10", "5.11", "6.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Full §5.10 operating_point_set_obu() parser including reset/update fields and operating_point_payload() dispatch, surfaced in inspect --json. Validator records non-monotonic active OPS state (§6.10.1), enforces the locally-decidable §6.10 conformance rules, and checks the §6.10.7 dependency-map agreement of explicitly signalled maps against the activated in-band sequence header (ops/mlayer-dependency-missing, ops/tlayer-dependency-missing; never fabricated when no header activates). Annex A/E level/schedule validation remains future, so validate stays partial. WRITER landed (operating-point-set-writer; write_operating_point_set in crates/splot-core/src/write/operating_point_set.rs inverts §5.10/§5.11 + the §5.11.1–5.11.5 sub-structs for the reset/global/local forms, threading obu_xlayer_id; the complete-OBU dispatch routes ParsedObu::OperatingPointSet to it + the extensible tail instead of Unimplemented; reject-before-write across the gated Options/counts/indices, with the §6.10.2 declared-vs-computed ops_data_size mismatch reproduced VERBATIM not rejected — only computed_size_bytes vs the re-derived opsBytes is locally-decidable; NonCanonicalOperatingPointSet variant added; the third of the 9 unwritten OBU-type body writers, validated by the roundtrip_obu_bytes fuzz target)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate ops"]
fixtures = ["tests/fixtures/operating-point-set.av2"]
diagnostics = ["ops/local-reserved-bits-nonzero", "ops/mlayer-info-idc-reserved", "ops/payload-size-mismatch", "ops/inherited-op-index-out-of-range", "ops/mlayer-dependency-missing", "ops/tlayer-dependency-missing"]

[[feature]]
id = "AV2-5.10-OPS-SYNTAX-ELEMENTS"
name = "Operating point set syntax elements"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.10", "6.10.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.10-OPERATING-POINT-SET. Top-level operating_point_set_obu() syntax elements (reset flag, ops_id, ops_cnt, present flags, global ops_mlayer_info_idc / local ops_reserved_2bits). Validator implements reset/update semantics and reserved-bit checks; the §6.10.7 dependency-map agreement checks are landed on the umbrella row, and full operating-point semantics depend on Annex A/E, so validate stays partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate ops"]
fixtures = ["tests/fixtures/operating-point-set.av2"]
diagnostics = ["ops/local-reserved-bits-nonzero", "ops/mlayer-info-idc-reserved"]

[[feature]]
id = "AV2-5.11-OPERATING-POINT-PAYLOAD"
name = "Operating point payload syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11", "6.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Parses operating_point_payload() including ops_data_size, optional intent/PTL/color/decoder-model/initial-delay fields, global ops_xlayer_map with per-layer PTL and explicit/inherited mlayer info, local mlayer info, byte alignment, and exact opsBytes accounting. Validator checks payload-size equality and inheritance bounds; Annex A/E stays future, so validate is partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate ops"]
fixtures = ["tests/fixtures/operating-point-set.av2"]
diagnostics = ["ops/payload-size-mismatch", "ops/inherited-op-index-out-of-range"]

[[feature]]
id = "AV2-5.11.1-OPS-AGGREGATE-INFO"
name = "Operating point aggregate info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11.1", "6.10.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.11-OPERATING-POINT-PAYLOAD. Parses ops_aggregate_info() (§5.11.1) for global OPS payloads. §6.10.3 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-10-3, lines 2760-2775) states no requirement of bitstream conformance: ops_config_idc, ops_aggregate_level_idx, ops_max_tier_flag, and ops_max_interop are descriptive aggregate profile/level/tier/interoperability indicators for the combined multistream operating point. Residual: enforcing those aggregate constraints (and any consistency against the per-extended-layer §6.10.4 PTL) is Annex-A semantics; blocked on the Annex A profile/level/tier table backlog (AV2-E-DECODER-MODEL / Annex A modeling), so validate stays partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests"]
commands = ["cargo test -p splot-core ops"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.11.2-OPS-SEQ-PTL-INFO"
name = "Operating point sequence profile tier level information syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11.2", "6.10.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.11-OPERATING-POINT-PAYLOAD. Parses ops_seq_profile_tier_level_info() (§5.11.2). Validator enforces the §6.10.4 ops_ptl_reserved_2bits==0 rule; Annex A level interpretation stays future, so validate is partial. BLOCKED: operating-point-to-frame mapping — §6.10.4 per-frame conformance against an OPS-advertised ops_level_idx (annex-a/frame-exceeds-ops-level) needs a model of which frames belong to which operating point, unmodeled. (The OPS PTL value space — ops_seq_profile_idc / ops_level_idx / ops_tier_flag — already ships via check_ops_level_tier_value_space: annex-a/profile-reserved / level-reserved / high-tier-below-4-0.)"

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate ops"]
fixtures = []
diagnostics = ["ops/ptl-reserved-bits-nonzero"]

[[feature]]
id = "AV2-5.11.3-OPS-DECODER-MODEL-INFO"
name = "Operating point decoder model info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11.3", "6.10.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "buffer-delay-sum-constancy"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.11-OPERATING-POINT-PAYLOAD. Parses ops_decoder_model_info() (§5.11.3). splot-validate enforces the §6.10.5 intra-CVS per-(obu_xlayer_id, ops_id, op) buffer-delay sum-constancy under buffer-delay-sum-constancy (decoder-model/buffer-delay-sum-changed, plus the advisory decoder-model/buffer-delay-sum-changed-across-cvs). The error tier is gated on a started coded video sequence (CvsTracker::cvs_started) so a pre-first-CLK pair — which is in no CVS (§7.3.6) — never fires. A §6.10.1 case-3 targeted reset (ops_reset_flag=0, ops_cnt=0: 'Only OPS x is reset') re-baselines exactly that (obu_xlayer_id, opsID) via a per-OPS targeted-reset generation, so a redefinition spanning it drops to the advisory tier like a full reset, without disturbing other OPS. The error is routed through CvsTracker::defer_or_emit_with_replacement so a CLK later in the same temporal unit that splits the comparison across a CVS boundary drops the deferred error and emits the cross-CVS advisory in its place instead of losing the finding silently; both keep the error tier provably silent on streams conforming under the per-CVS reading. Annex E decoder-schedule simulation stays future, so validate is partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_intra_cvs_ops_sum_change_is_error", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_intra_cvs_ops_same_sum_is_not_flagged", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_sum_change_before_first_clk_is_not_error", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_sum_change_with_late_clk_in_same_tu_is_not_error", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_sum_change_across_cvs_is_not_error_but_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_sum_change_across_reset_is_not_error_but_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_sum_change_across_targeted_reset_is_not_error_but_warns", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_targeted_reset_of_other_ops_still_errors", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_ops_redefinition_without_explicit_info_clears_baseline", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_annex_e_defaults_are_never_compared", "crates/splot-validate/src/validator/tests/decoder_model.rs::decoder_model_external_hls_suppresses_both_ids"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate decoder_model"]
fixtures = []
diagnostics = ["decoder-model/buffer-delay-sum-changed", "decoder-model/buffer-delay-sum-changed-across-cvs"]

[[feature]]
id = "AV2-5.11.4-OPS-COLOR-INFO"
name = "Operating point color info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11.4", "6.10.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.11-OPERATING-POINT-PAYLOAD. Parses ops_color_info() (§5.11.4) including the rg(2) color description idc and the explicit color triple. §6.10.6 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-10-6, lines 2842-3005) states one numeric bound — ops_color_description_idc shall be in 0..127 (lines 2867-2869) — which the rg(2) encoding already caps at 127 (BitReader::read_rg, crate test read_rg_two_caps_at_127), so a successful parse satisfies it with no separate diagnostic. The ops_color_primaries / ops_transfer_characteristics / ops_matrix_coefficients / ops_full_range_flag values are defined entirely by ISO/IEC 23091-4 / ITU-T H.273 (Tables 6.14-6.15), an externally specified registry whose unlisted values are 'reserved ... and shall be ignored by decoders'; §6.10.6 states no cross-OBU color-info agreement rule. Residual: there is no further locally-decidable §6.10.6 conformance rule; any color-vs-Annex-A operating-point consistency is Annex-A semantics, blocked on the Annex A backlog, so validate stays partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests"]
commands = ["cargo test -p splot-core ops"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.11.5-OPS-MLAYER-INFO"
name = "Operating point mlayer info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.11.5", "6.10.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/operating_point_set.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.11-OPERATING-POINT-PAYLOAD. Parses ops_mlayer_info() (§5.11.5) mlayer/tlayer maps and retains inherited references. Validator enforces inherited operating-point-index bounds (§6.10.2) and the §6.10.7 dependency-map agreement of explicitly signalled maps against the activated in-band sequence header (diagnostics recorded on AV2-5.10-OPERATING-POINT-SET); entries whose extended layer never activates an in-band header are not checked, and Annex A/E semantics stay future, so validate is partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/operating_point_set.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core ops", "cargo test -p splot-validate ops"]
fixtures = []
diagnostics = ["ops/inherited-op-index-out-of-range"]

[[feature]]
id = "AV2-5.12-BUFFER-REMOVAL-TIMING"
name = "Buffer removal timing OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.12", "6.11"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/buffer_removal_timing.rs"
change_id = "ops-brt-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Parses §5.12 buffer_removal_timing_obu() in both extended-layer (br_time) and OPS-dependent forms, surfaced in inspect --json. Validator checks OPS availability and br_ops_cnt equality against active in-band OPS (§6.11 / §7.3.8.5) under external-HLS-disabled mode, and classifies BRT ordering per §7.3.3/§7.3.4/§7.3.7. Annex E decoder schedule/resource conformance remains future, so validate is partial. WRITER landed (buffer-removal-timing-writer; write_buffer_removal_timing in crates/splot-core/src/write/buffer_removal_timing.rs inverts §5.12 for both forms; the complete-OBU dispatch routes ParsedObu::BufferRemovalTiming to it + the non-extensible trailing-bits tail instead of Unimplemented; reject-before-write on op_count/op_index/gated-br_time_op/rg-range; the first of the 9 unwritten OBU-type body writers — NonCanonicalBufferRemovalTiming variant added)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/buffer_removal_timing.rs::tests", "crates/splot-core/src/write/buffer_removal_timing_tests.rs::tests", "crates/splot-core/src/write/dispatch_tests.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core brt", "cargo test -p splot-core write::buffer_removal_timing", "cargo test -p splot-validate brt"]
fixtures = ["tests/fixtures/buffer-removal-timing.av2"]
diagnostics = ["brt/unavailable-operating-point-set", "brt/ops-count-mismatch"]

[[feature]]
id = "AV2-5.13-QUANTIZATION-MATRIX"
name = "Quantization matrix OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.13", "6.12"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/quantizer_matrix.rs"
change_id = "qm-filmgrain-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Full §5.13 quantizer_matrix_obu() parser (qm_bit_map f(15), qm_chroma_info_present_flag, reset/default path, per-level qm_is_default_flag, user-defined matrices via the AV2-5.4.11-USER-QM helper), dispatched from open_bitstream_unit() with a QuantizationMatrix ParsedObu variant and inspect JSON summary (level/plane/default status; matrices summarized by shape, not dumped). The parser enforces the §6.4.11 quant_delta -128..=127 bound (qm/quant-delta-out-of-range via the QuantizerMatrixSyntax check), and the validator implements the local §6.12 duplicate-reset-between-frames and duplicate-level-between-frames checks and records per-level HLS availability (QmMLayerId/QmTLayerId/QmDataPresent/QmNumPlanes, reset to defaults on a qm_bit_map==0 reset) as foundation. The between-coded-frames window (QmSeen, '...seen since the last frame', §6.12) resets at each coded frame, INCLUDING a SEF: §7.3.3 lists OBU_LEADING_SEF / OBU_REGULAR_SEF as one of the two alternatives for 'the coded frame' of a coded frame unit and calls it a frame ('Such a frame is associated with ... OrderHint'), so a single SEF is its own coded frame unit and is a coded-frame boundary for QmSeen. The reset therefore fires for every frame-bearing OBU (tile groups, TIP, bridge, and SEF — is_frame_bearing) and NOT at a bare temporal-unit boundary (no intervening coded frame). Consequently a level reused across a SEF lies in two different coded frame units and is CONFORMANT (regression test qm_duplicate_level_across_sef_is_not_flagged), while a level reused across a bare temporal delimiter with no intervening coded frame is still a duplicate (qm_duplicate_level_across_temporal_delimiter_is_flagged); a decoded coded frame between two equal levels likewise clears the window (qm_duplicate_level_across_decoded_frame_is_not_flagged). reference-state-and-random-access lands the §7.3.8.9 frame quantization-reference availability check and the QmProtected reset_qm() discipline that gates it: the QuantizerMatrixState now tracks a qm_protected bitmap, cleared at a temporal delimiter (§5.5 mirror :1626-1630), set per sent level by a QM OBU (§5.13 mirror :3010/:3033), and consumed by reset_qm_availability_for_key (CLK/OLK, unconditional needsReset arm) / reset_qm_availability_for_switch_or_ras (RAS / restricted SWITCH) at the frame header. Under qm-reset-mlayer-presence-arm that SWITCH/RAS arm now models BOTH needsReset clauses (mirror :5350-5352): the QmMLayerId==-1 arm AND the MLayerPresenceMap[QmMLayerId[level]][obu_mlayer_id] arm (the §5.4.1 reflexive-transitive presence map, AV2-5.4.1-SEQUENCE-HEADER-GENERAL), so a same-layer RAS/SWITCH now resets the QmMLayerId==obu_mlayer_id level it previously left available. The availability presence check (frame-header/qm-level-unavailable, §7.3.8.9) is recorded on AV2-5.18.6-QUANTIZATION. RESIDUAL: the using_qmatrix/qm_y/qm_u/qm_v §6.17.6.2 layer-dependency constraints stay deferred. WRITER landed (quantization-matrix-writer): write_quantizer_matrix (crates/splot-core/src/write/quantizer_matrix.rs) inverts parse_quantizer_matrix + user_defined_qm, CANONICALIZING like the §5.14 film-grain writer — the model stores only the DECODED coefficients (1..=255), not the wire deltas or the symmetric/transpose/copy/coefficient-repeat compressions, so the writer emits the LONG FORM (every skip flag 0, one svlc() quant_delta per cell in 2D diagonal scan order, recomputing the minimal in-range delta that drives the running quant to the target); the re-emission decodes to the same coefficients (semantic round-trip; byte-exact only when the input was already long form). Routed by the complete-OBU dispatch + the generic non-extensible tail; reject-before-write on the decidable inconsistencies (num_planes vs chroma flag, set-bit-derived levels, is_default vs matrices, transform count/order, plane count/dims/value-count, a 0 coefficient — unrepresentable since quant2==0 is the repeat sentinel). This was the LAST unwritten OBU-type body writer: the complete-OBU dispatch now writes all 14 OBU payload types. Encoder remains future work."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/quantizer_matrix.rs::tests", "crates/splot-core/src/write/quantizer_matrix_tests.rs::tests", "crates/splot-core/src/write/dispatch_roundtrip_tests.rs::tests::quantization_matrix_payload_round_trips", "crates/splot-core/src/write/roundtrip_tests.rs::tests::quantization_matrix_round_trips", "crates/splot-core/src/obu.rs::tests::dispatch_parses_quantizer_matrix_payload", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::qm_duplicate_reset_between_frames_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::qm_duplicate_level_between_frames_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::qm_malformed_payload_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::qm_quant_delta_out_of_range_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::qm_duplicate_level_across_temporal_delimiter_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::qm_duplicate_level_across_sef_is_not_flagged", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::qm_duplicate_level_across_decoded_frame_is_not_flagged", "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_clears_unprotected_level_across_temporal_units", "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_preserves_resent_level_in_current_temporal_unit"]
commands = ["cargo test -p splot-core quantizer", "cargo test -p splot-core write::quantizer_matrix", "cargo +nightly fuzz run roundtrip_obu_bytes", "cargo test -p splot-validate qm"]
fixtures = ["tests/fixtures/quantizer-matrix.av2"]
diagnostics = ["qm/duplicate-reset-between-frames", "qm/duplicate-level-between-frames", "qm/quant-delta-out-of-range"]

[[feature]]
id = "AV2-5.14-FILM-GRAIN"
name = "Film grain OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.14", "5.18.10.2", "6.13", "6.17.10.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/film_grain.rs"
change_id = "qm-filmgrain-hls-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Full §5.14 film_grain_obu() parser (fgm_update_flags f(8), fgm_chroma_idc uvlc() with subX/subY/monochrome derivation, per-slot film_grain_model()) and the complete §5.18.10.2 film_grain_model() parser (scaling points, grain scaling, AR coefficient arrays, chroma multipliers/offsets, overlap/restricted-range/identity/block-size), dispatched from open_bitstream_unit() with a FilmGrain ParsedObu variant and inspect JSON summary (arrays summarized by count, not dumped). Field order verified against the AVM oracle obu_fgm.c (no AV1 code). Validator implements the local §6.13 update-flags-zero, chroma-idc-out-of-range, and duplicate-slot-in-coded-frame-unit checks, the local §6.17.10.2 film-grain-model checks (scaling-points-out-of-range for num_*_points>14, scaling-point-not-increasing for non-monotonic/>=256 point values, and chroma-points-not-paired for the 4:2:0 cb/cr pairing rule), and records per-slot HLS availability (FgmChromaIdc/FgmMLayerId/FgmTLayerId) as foundation. The coded-frame-unit window closes at each coded frame, INCLUDING a SEF: §6.13 scopes the duplicate-slot rule to the 'same coded frame unit' and its NOTE permits reuse 'in a subsequent coded frame unit', and §7.3.3 makes a single OBU_LEADING_SEF / OBU_REGULAR_SEF its own coded frame unit, so a SEF ends the current film-grain coded-frame-unit window (the next film-grain OBU belongs to a subsequent unit). The reset therefore fires for every frame-bearing OBU (tile groups, TIP, bridge, and SEF — is_frame_bearing) and NOT at a bare temporal-unit boundary (no intervening coded frame). Consequently a slot reused across a SEF lies in two different coded frame units and is CONFORMANT (regression test film_grain_duplicate_slot_across_sef_is_not_flagged), while a slot reused across a bare temporal delimiter with no intervening coded frame is still a duplicate (film_grain_duplicate_slot_across_temporal_delimiter_is_flagged). Frame film-grain reference checks (apply_grain/fgm_id, §5.18.10.1 / §6.17.10.1) — the FilmGrainPresent availability requirement and the FgmMLayerId/FgmTLayerId/FgmChromaIdc layer-dependency / chroma constraints — are implemented on AV2-5.18.10-FILM-GRAIN-STRUCTURES (frame_film_grain_reference_checks), reading this row's per-slot FgmSlotRecord identity recorded by observe_film_grain. WRITER landed (film-grain-writer; write_film_grain in crates/splot-core/src/write/film_grain.rs inverts §5.14 / §5.18.10.2; the complete-OBU dispatch routes ParsedObu::FilmGrain to it + the non-extensible trailing-bits tail instead of Unimplemented. The model is LOSSY versus the wire (no per-array bit widths / increments stored), so the writer CANONICALIZES — re-derives the minimal in-range point-increment/scaling/ar-coeff bit width, recomputes increments from cumulative values, re-biases AR coeffs; semantic round-trip holds, byte-exactness does not. Reject-before-write on non-monotonic points / count-vs-len / gated-Option / forced-false flags / derived sub_x-sub_y-monochrome / models-vs-update_flags / no-fitting-width; NonCanonicalFilmGrain variant added; the fifth of the 9 unwritten OBU-type body writers, validated by an exhaustive width-boundary review + the roundtrip_obu_bytes fuzz target)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/film_grain.rs::tests", "crates/splot-core/src/obu.rs::tests::dispatch_parses_film_grain_payload", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_zero_update_flags_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_chroma_idc_out_of_range_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_duplicate_slot_in_coded_frame_unit_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_malformed_payload_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_too_many_scaling_points_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_420_unpaired_chroma_points_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_non_increasing_scaling_point_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::film_grain_duplicate_slot_across_temporal_delimiter_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::film_grain_duplicate_slot_across_sef_is_not_flagged"]
commands = ["cargo test -p splot-core film", "cargo test -p splot-validate film"]
fixtures = ["tests/fixtures/film-grain.av2"]
diagnostics = ["film-grain/update-flags-zero", "film-grain/chroma-idc-out-of-range", "film-grain/duplicate-slot-in-coded-frame-unit", "film-grain/scaling-points-out-of-range", "film-grain/scaling-point-not-increasing", "film-grain/chroma-points-not-paired"]

[[feature]]
id = "AV2-5.15-CONTENT-INTERPRETATION"
name = "Content interpretation OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.15", "6.14"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/content_interpretation.rs"
change_id = "sequence-timing-hls-availability"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Full §5.15 parser (scan type, rg(2) color description, chroma sample position, aspect ratio incl. extended SAR) reaching timing_info() via ci_timing_info_present_flag. Dispatched by open_bitstream_unit and surfaced in inspect --json. splot-core derives §6.14 Table 6.13 color presets and the §5.15 aspect tables (ColorDescription::derived / AspectRatioInfo::derived_sar). splot-validate enforces the §6.14 locally-decidable ranges (chroma sample position <= 5, aspect_ratio_idc <= 16), the reserved-bits anomaly, and the repeated-CI same-information check (compared on derived color/aspect so alias-equivalent re-encodings are not flagged while genuinely different info is); cross-embedded-layer timing consistency is tracked under AV2-6.4-SEQUENCE-HEADER-SEMANTICS. WRITER landed (content-interpretation-writer; write_content_interpretation in crates/splot-core/src/write/content_interpretation.rs inverts §5.15 — scan-type, the 4 gated color/chroma/aspect/timing sub-structs (+ a private write_timing_info inverting §5.4.12 parse_timing_info), and reserved_2bit; the complete-OBU dispatch routes ParsedObu::ContentInterpretation to it + the extensible tail instead of Unimplemented; tolerated/reserved values reproduced VERBATIM, rejects only the decidable gates and the §6.4.12 zero-tick/zero-time_scale parser-unproducible values; NonCanonicalContentInterpretation variant added; the fourth of the 9 unwritten OBU-type body writers, validated by the roundtrip_obu_bytes fuzz target)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/content_interpretation.rs::tests", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_reserved_bits_nonzero_is_warned", "crates/splot-validate/src/validator/tests/mfh_external_hls.rs::ci_repeat_differing_in_color_preset_is_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_repeat_differing_only_in_color_encoding_is_not_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_chroma_sample_position_out_of_range_is_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_aspect_ratio_idc_out_of_range_is_flagged"]
commands = ["cargo test -p splot-core content_interpretation", "cargo test -p splot-validate ci_"]
fixtures = []
diagnostics = ["content-interpretation/reserved-bits-nonzero", "content-interpretation/repeated-ci-not-identical", "content-interpretation/chroma-sample-position-out-of-range", "content-interpretation/aspect-ratio-idc-out-of-range"]

[[feature]]
id = "AV2-5.16-PADDING"
name = "Padding OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.16", "6.15"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/padding.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Needed for padding payload validation and ordering exceptions. WRITER landed (obu-writer-dispatch): write_padding_payload (crates/splot-core/src/write/dispatch.rs) emits the §5.16 obu_padding_byte run (the opaque passthrough) then the trailing_bits() tail beginning at the last non-zero byte; the complete-OBU dispatch routes ParsedObu::Padding to it (padding owns its own tail), so an OBU_PADDING round-trips through write_complete_obu byte-exactly. Reject-before-write rejects a parser-unreachable trailing_len==0 with padding_len>0 (the §5.16 parser splits at the last non-zero byte)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/padding.rs::tests", "crates/splot-core/src/write/dispatch_roundtrip_tests.rs::tests::padding_payload_round_trips", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::padding_all_zero_payload_is_flagged", "crates/splot-validate/src/validator/tests/qm_film_grain.rs::padding_invalid_trailing_bits_is_flagged"]
commands = ["cargo test -p splot-core padding", "cargo test -p splot-core write::dispatch", "cargo test -p splot-validate padding"]
fixtures = ["tests/fixtures/padding.av2"]
diagnostics = ["padding/all-zero-payload", "padding/invalid-trailing-bits"]

[[feature]]
id = "AV2-5.17-METADATA"
name = "Metadata OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17", "6.16"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-semantic-validation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Umbrella for metadata OBUs; child rows carry the per-type status. The validator enforces locally decidable syntax, reserved-value, HDR repeat/first-picture, scan-type, and timecode constraints over their specified scopes. §6.16.3 persistence, cancellation, and layer applicability constrain decoder state rather than bitstream validity; the selected-layer decoder implements them in crates/splot-decode/src/pipeline/output_effects.rs for its base embedded-layer output: GLOBAL replacement and cancel immunity, same-scope BASIC/ENHANCED replacement and cancellation, NO expiry after one frame snapshot, LAYER_GLOBAL/LAYER_CURRENT/LAYER_VALUES applicability to embedded layer 0, and pruning of metadata from earlier temporal units at a coded-video-sequence start. Unit tests exercise each state transition directly and the AVM-derived output-effect fixtures protect the integrated path; the validator does not duplicate this lifetime state. The current one-output-layer runtime does not expose or claim a generalized cross-layer/dependency-map metadata query. The in-frame-unit metadata suffix/prefix placement within a coded frame unit (§7.3.3/§7.3.4) is segmented by AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT/AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT (frame_unit.rs). validate stays partial according to the child-row residuals, including decoded-frame-hash verification against §7.21 output. WRITER (metadata-obu-writer): the full §5.17 metadata-OBU syntax is inverted in crates/splot-core/src/write/metadata.rs — write_metadata_short_obu (§5.17.2), write_metadata_group_obu (§5.17.3), write_metadata_unit (§5.17.1 + the §6.16.1 unit padding), and write_metadata_payload over all 11 typed §5.17.4-§5.17.13 payloads. Each composing writer drafts into a scratch BitWriter and appends on full success (reject-before-write: every reject leaves bit_len()==0), validating every f(n) domain, Option/gate presence, the metadata_type<->payload-variant agreement, and the muh_* header accounting up front. The short-OBU metadata_type leb128 (from metadata_type_leb128_bytes) and the group-unit muh_payload_size leb128 (length derived from muh_header_size) are reproduced byte-exact; the group metadata_type/metadata_unit_cnt leb128, the unit padding, and the discarded header-extension bytes are canonical-subset (semantic always, byte-exact on the minimal/zero-padded subset). The four length-summarized payloads (ITU-T T.35, ICC, user-data, reserved/unknown raw) take their opaque bytes via a passthrough input (no model change). A non-canonical MetadataType::Reserved value that re-maps to a named type, and an over-large declared payload_size, are rejected before any bit. All §5.17.1-§5.17.13 child write rows are write=done and round-trip + byte-exact + never-panic tested, so this umbrella write is done; its validate/avm_diff stages stay partial per the existing residuals."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_truncated_observer_is_silent", "crates/splot-core/src/write/metadata_tests.rs::tests", "crates/splot-decode/src/pipeline/output_effects.rs::tests::global_metadata_overwrites_global_and_survives_cancel", "crates/splot-decode/src/pipeline/output_effects.rs::tests::basic_and_enhanced_metadata_replace_same_scope_and_cancel", "crates/splot-decode/src/pipeline/output_effects.rs::tests::metadata_snapshot_applies_only_to_selected_base_layer", "crates/splot-decode/src/pipeline/output_effects.rs::tests::no_persistence_metadata_expires_after_frame_snapshot", "crates/splot-decode/src/pipeline/output_effects.rs::tests::coded_video_sequence_start_prunes_metadata_from_earlier_temporal_units", "crates/splot-decode/src/output/hash_tests.rs::output_effect_single_frame_fixtures_match_reference_hashes"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata", "cargo test -p splot-decode output_effect --locked"]
fixtures = ["tests/fixtures/metadata-short.av2", "tests/fixtures/metadata-group.av2"]
diagnostics = ["metadata/unit-payload-underflow", "metadata/short-layer-idc-out-of-range", "metadata/group-unit-count-too-large", "metadata/group-header-underflow", "metadata/group-reserved-bits-nonzero", "metadata/group-xlayer-map-global-bit-set", "metadata/group-mlayer-map-below-obu-mlayer", "metadata/temporal-point-info-not-short", "metadata/timecode-seconds-out-of-range", "metadata/timecode-minutes-out-of-range", "metadata/timecode-hours-out-of-range", "metadata/scan-type-pic-struct-reserved", "metadata/persistence-idc-reserved", "metadata/group-layer-idc-reserved", "metadata/hdr-cll-repeat-content-differs", "metadata/hdr-mdcv-repeat-content-differs", "metadata/scan-type-ci-scan-type-mismatch", "metadata/scan-type-ci-scan-type-unestablished", "metadata/scan-type-equal-picture-interval-required", "metadata/scan-type-pic-struct-group-inconsistent"]

[[feature]]
id = "AV2-5.17.1-METADATA-UNIT"
name = "Metadata unit syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.1", "6.16.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.1 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-1, lines 3337-3352) states one requirement of bitstream conformance — remainingMuPayloadBits shall not be less than 0 (lines 3351-3352) — which is parse-enforced: parse_metadata_unit() runs the child payload against a reader bounded to metadataPayloadSize bytes and maps an overrun to MetadataErrorKind::UnitPayloadUnderflow (crates/splot-core/src/headers/metadata.rs:646), surfaced as a metadata bitstream/parse-error. metadata_unit_remaining_bit is decoder-ignored (lines 3341-3344). Residual: the placement of metadata units within a coded frame unit (in-frame-unit suffix vs prefix ordering, §7.3.3/§7.3.4) is frame-parsing-blocked, owned by AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT / AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT. Do not mark the umbrella done until this child is proven. WRITER (metadata-obu-writer): write_metadata_unit in crates/splot-core/src/write/metadata.rs inverts metadata_unit() incl. the §6.16.1 zero padding to metadataPayloadSize; reject-before-write, round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata"]
fixtures = []
diagnostics = ["metadata/unit-payload-underflow"]

[[feature]]
id = "AV2-5.17.2-METADATA-SHORT"
name = "Short metadata OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.2", "6.16.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.2 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-2, lines 3361-3398) states one requirement of bitstream conformance — muh_layer_idc < 3 (line 3375) — landed as metadata/short-layer-idc-out-of-range (crates/splot-validate/src/checks/mod.rs:1128). Its other fields (metadata_is_suffix, metadata_necessity_idc, metadata_application_id, muh_cancel_flag, muh_persistence_idc, metadata_type) share the §6.16.3 semantics, validated by the metadata syntax parse, and the §6.16.3 muh_persistence_idc reserved range is surfaced as the metadata/persistence-idc-reserved warning. Residual: the in-frame-unit suffix/prefix placement of the short metadata OBU (§7.3.3/§7.3.4, decided by metadata_is_suffix relative to the surrounding frame unit) is frame-parsing-blocked, owned by AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT / AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT. Do not mark the umbrella done until this child is proven. WRITER (metadata-obu-writer): write_metadata_short_obu inverts metadata_short_obu() (the 1-byte muh_* header, the byte-exact metadata_type leb128, and the bounded unit/cancel arm); reject-before-write, round-trip + byte-exact tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata"]
fixtures = ["tests/fixtures/metadata-short.av2"]
diagnostics = ["metadata/short-layer-idc-out-of-range", "metadata/unit-payload-underflow"]

[[feature]]
id = "AV2-5.17.3-METADATA-GROUP"
name = "Metadata group OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.3", "6.16.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-semantic-validation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. The §6.16.3 group-header conformance requirements (unit count, reserved bits, xlayer/mlayer-map rules) are checked; reserved muh_persistence_idc and group-form muh_layer_idc values (4-7) warn as 'reserved for AOMedia use'. These locally decidable validator checks are complete, so validate remains done. §6.16.3 persistence, cancellation, and layer applicability describe decoder behavior, not additional bitstream-conformance findings; crates/splot-decode/src/pipeline/output_effects.rs owns the active state used by the current selected base embedded-layer output. Its direct state tests prove GLOBAL overwrite/cancel immunity, same-scope BASIC/ENHANCED replacement and cancellation, NO frame expiry, LAYER_GLOBAL/LAYER_CURRENT/LAYER_VALUES targeting of embedded layer 0, and coded-video-sequence pruning by temporal-unit epoch. The one-output-layer runtime does not expose a generalized cross-layer/dependency-map metadata query. The validator therefore parses canceled units and reports their local syntax facts but does not duplicate decoder lifetime state; cancelling a never-signaled type remains non-diagnostic, and malformed/truncated metadata proves the observer adds no semantic false positive while the validator preserves the syntax errors. WRITER (metadata-obu-writer): write_metadata_group_obu + the per-unit writer invert metadata_group_obu() incl. the muh_header_size accounting, the cancel arm, the §6.16.3 layer maps (local/global keyed off obu_xlayer_id), and the byte-exact muh_payload_size leb; reject-before-write, round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_persistence_reserved_idc_warns", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_persistence_defined_idc_is_accepted", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_group_layer_idc_reserved_warns", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_group_layer_idc_defined_values_accepted", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::cancel_unknown_type_emits_nothing", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_truncated_observer_is_silent", "crates/splot-decode/src/pipeline/output_effects.rs::tests::global_metadata_overwrites_global_and_survives_cancel", "crates/splot-decode/src/pipeline/output_effects.rs::tests::basic_and_enhanced_metadata_replace_same_scope_and_cancel", "crates/splot-decode/src/pipeline/output_effects.rs::tests::metadata_snapshot_applies_only_to_selected_base_layer", "crates/splot-decode/src/pipeline/output_effects.rs::tests::no_persistence_metadata_expires_after_frame_snapshot", "crates/splot-decode/src/pipeline/output_effects.rs::tests::coded_video_sequence_start_prunes_metadata_from_earlier_temporal_units", "crates/splot-decode/src/output/hash_tests.rs::output_effect_single_frame_fixtures_match_reference_hashes"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata", "cargo test -p splot-decode output_effect --locked"]
fixtures = ["tests/fixtures/metadata-group.av2"]
diagnostics = ["metadata/group-unit-count-too-large", "metadata/group-header-underflow", "metadata/group-reserved-bits-nonzero", "metadata/group-xlayer-map-global-bit-set", "metadata/group-mlayer-map-below-obu-mlayer", "metadata/persistence-idc-reserved", "metadata/group-layer-idc-reserved"]

[[feature]]
id = "AV2-5.17.4-METADATA-ITUT-T35"
name = "ITU-T T.35 metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.4", "6.16.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.4 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-4, lines 3627-3648) states no requirement of bitstream conformance that AV2 can check: itu_t_t35_country_code, its extension byte, and itu_t_t35_payload_bytes are all defined entirely by Recommendation ITU-T T.35 (an externally specified registry), so there is no locally decidable AV2 constraint. The structure is validated by the §5.17 metadata syntax parse: the MetadataSyntax check (registry id metadata/syntax in crates/splot-validate/src/checks/mod.rs:1100) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error and the trailing-bits/* ids; the metadata OBU is non-extensible, so no obu-header/extension-flag check applies). WRITER (metadata-obu-writer): write_metadata_payload's ItutT35 arm writes the country code, optional 0xFF extension byte, and the passthrough payload bytes; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests"]
commands = ["cargo test -p splot-core metadata"]
fixtures = []
diagnostics = ["bitstream/parse-error", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.17.5-METADATA-HDR-CLL"
name = "HDR CLL metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.5", "6.16.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-semantic-validation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. The §6.16.5 'shall have the same content' rule for additional HDR CLL units in a CVS is checked per intersecting embedded-layer association over the exact §7.3.6 CVS boundary (metadata/hdr-cll-repeat-content-differs): each unit's association set is derived from its §6.16.3 muh_layer_idc targeting (global LAYER_GLOBAL = all layers, local LAYER_GLOBAL = every embedded layer of the carrying xlayer, LAYER_CURRENT = the carrying (xlayer, mlayer), LAYER_VALUES = the full muh_xlayer_map/muh_mlayer_map pair set incl. multi-map global units), so cross-mode pairs that share an embedded layer are compared while disjoint targeting never is. Units with no bitstream-derivable association (LAYER_UNSPECIFIED, LAYER_CURRENT on a global OBU, reserved muh_layer_idc) enter no comparison and no baseline — a documented conservative false negative. The §6.16.5 'shall be indicated at the first coded picture of that embedded layer in the coded video sequence' half landed under frame-unit-segmentation for the sound subset (metadata/hdr-cll-first-coded-picture): an explicit-pair-targeted unit (LAYER_CURRENT / LAYER_VALUES) that FIRST establishes content (no prior intersecting baseline of the same type in the CVS) when every named embedded layer's first coded picture of the CVS has already passed (embedded_layer_first_picture_seen, set at the layer's first frame-bearing OBU and reset per CVS) was not indicated at the first coded picture and is flagged; a unit in the layer's first coded frame unit's pre-frame region is observed before that picture and never fires, and an allowed later repeat (matching an earlier-established baseline) is not flagged. XLayerWide (local LAYER_GLOBAL) / Universal (global LAYER_GLOBAL) targeting names no single concrete first coded picture and stays deferred (zero false positives). validate stays partial on the deferred XLayerWide/Universal first-coded-picture half and the color-inheritance refinement. WRITER (metadata-obu-writer): the HdrCll arm writes max_cll/max_fall f(16); byte-exact round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_repeat_same_content_accepted", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_repeat_content_differs_flagged", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_after_cvs_restart_new_content_accepted", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_cross_tu_repeat_differs_flagged_at_flush", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_global_then_current_layer_differing_content_flagged", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_global_then_current_layer_same_content_accepted", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_unspecified_layer_targeting_not_compared", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_global_group_disjoint_layer_targeting_not_flagged", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_global_group_overlapping_layer_targeting_flagged", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_cll_cross_mode_deferral_dropped_at_clk", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::metadata_hdr_cll_first_coded_picture_late_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::metadata_hdr_cll_at_first_coded_picture_is_conformant"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata", "cargo test -p splot-validate hdr_cll"]
fixtures = []
diagnostics = ["metadata/hdr-cll-repeat-content-differs", "metadata/hdr-cll-first-coded-picture"]

[[feature]]
id = "AV2-5.17.6-METADATA-HDR-MDCV"
name = "HDR MDCV metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.6", "6.16.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-semantic-validation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. The §6.16.6 'shall have the same content' rule for additional HDR MDCV units in a CVS is checked per intersecting embedded-layer association over the exact §7.3.6 CVS boundary (metadata/hdr-mdcv-repeat-content-differs), with the association set derived from the §6.16.3 muh_layer_idc targeting exactly as for AV2-5.17.5-METADATA-HDR-CLL (incl. the LAYER_UNSPECIFIED / global-LAYER_CURRENT / reserved-idc conservative skips). The §6.16.6 'shall be indicated at the first coded picture of that embedded layer' half landed under frame-unit-segmentation for the explicit-pair sound subset (metadata/hdr-mdcv-first-coded-picture), sharing the check_hdr_first_coded_picture path with AV2-5.17.5-METADATA-HDR-CLL (an explicit-pair unit first establishing content after the named layers' first coded picture of the CVS has passed; XLayerWide/Universal targeting and the color-inheritance refinement stay deferred). validate stays partial on those deferred halves. WRITER (metadata-obu-writer): write_hdr_mdcv writes the interleaved primary x/y, white point, and luminance fields; byte-exact round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::hdr_mdcv_repeat_content_differs_flagged", "crates/splot-validate/src/validator/tests/frame_unit_metadata.rs::metadata_hdr_mdcv_first_coded_picture_late_is_flagged"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata", "cargo test -p splot-validate hdr_mdcv"]
fixtures = []
diagnostics = ["metadata/hdr-mdcv-repeat-content-differs", "metadata/hdr-mdcv-first-coded-picture"]

[[feature]]
id = "AV2-5.17.7-METADATA-TIMECODE"
name = "Timecode metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.7", "6.16.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-residual-local-semantics"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. The locally-decidable §6.16.7 range bounds landed earlier (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-7): seconds_value 0..59 (lines 3873-3876), minutes_value 0..59 (3882-3885), hours_value 0..23 (3890-3893) as metadata/timecode-seconds-out-of-range / -minutes-out-of-range / -hours-out-of-range (crates/splot-validate/src/checks/mod.rs check_metadata_unit_payload). The metadata-residual-local-semantics change added two more locally-decidable §6.16.7 facts (crates/splot-validate/src/context/mod.rs check_timecode_consistency / recheck_timecode_n_frames_after_ci): (a) the inference-presence rules — when seconds_value/minutes_value/hours_value is not present it is inferred from 'the previous set of clock timestamp syntax elements in decoding order, and it is required that such a previous value shall have been present' (lines 3873-3893) — as metadata/timecode-inferred-without-previous (error), tracked with per-CVS-scope decoding-order field-presence state keyed by the carrying OBU's concrete (obu_xlayer_id, obu_mlayer_id): METADATA_TYPE_TIMECODE is layer-specific (§6.16.3 Table 6.17, 'Y'), so a timecode on embedded layer (x, m0) is NOT the 'previous set in decoding order' of one on (x, m1) and must not seed its inference (metadata_timecode_inference_keyed_per_targeted_embedded_layer); a LAYER_UNSPECIFIED timecode (§6.16.3: 'does not specify to what layers the metadata applies to') still chains per its carrying OBU's own (obu_xlayer_id, obu_mlayer_id) scope — the only concrete scope the bitstream pins down, a documented sound choice that never compares across unrelated targets. A global timecode's chain (and its global-bucket n_frames observations) is detached by ANY CLK, since a global timecode spans every layer; a concrete-layer timecode's chain only by a CLK for that extended layer (§7.3.6 CVS boundaries are per extended layer). The reading is LITERAL-presence, matching the check_timecode_consistency doc comment: 'present' means the element was *coded* in the immediate predecessor set in decoding order (its own §5.17.7 presence flags), so an INFERRED value in the predecessor does NOT make the element present for the next set — a set that omits a field, followed by another that also omits it, fires on the second omission too (metadata_timecode_omitted_after_omitted_is_flagged); the chain never seeds itself from an inference and resets at each §7.3.6 CLK boundary. The rejected lenient carry-forward reading (where a first omitted-but-inferred value would count as 'present' and satisfy the next omitting set) lacked textual support: the sentence speaks of the element having 'been present', not of a value having been *established*; AVM differential testing may revisit it if the reference decoder treats a propagated inferred value as satisfying the requirement; (b) the n_frames bound — 'When ci_timing_info_present_flag is equal to 1, n_frames shall be less than maxPicPerSecond = ceil(time_scale / TicksPerPicture)' (lines 3865-3867) — as metadata/timecode-n-frames-exceeds-rate (error), decidable because ci_timing_info_present_flag is the content-interpretation OBU's flag (annex-e-decoder-model.md line 293: 'ci_timing_info_present_flag equal to 1 in the content interpretation OBU associated with this extended layer') and time_scale/num_units_in_display_tick/num_ticks_per_picture_minus_1 are parsed into TimingInfo and tracked in the content_interpretations store, so the bound is paired against the in-scope CI timing at/after the layer's §7.3.8.11 epoch in both arrival orders (timecode-before-CI re-checks via recheck_timecode_n_frames_after_ci), with strict 'less than' boundary tests (n_frames == maxPicPerSecond fails, == maxPicPerSecond-1 passes). 'In scope' is the §6.16.3 targeting (derive_hdr_association): a derivable targeting (LAYER_GLOBAL -> Universal pairs every CI; LAYER_CURRENT / LAYER_VALUES pair only the named (xlayer, mlayer) pairs) decides the pairing exactly, so a global LAYER_VALUES timecode naming only some layers does not pair with an untargeted layer's CI (metadata_timecode_n_frames_targeting_excludes_untargeted_layer_ci); an UNDERIVABLE targeting (LAYER_UNSPECIFIED and the other derive_hdr_association gaps) compares NOTHING — the spec leaves the layer association unspecified, so no CI's rate binds the timecode and the prior coarse 'pair with every CI in the obu_xlayer_id scope' fallback (which could fire a hard error against an unrelated layer) is dropped (metadata_timecode_n_frames_unspecified_targeting_compares_nothing). The n_frames observations are pruned per the §7.3.6 PER-EXTENDED-LAYER CVS boundary: an observation belongs to the coded video sequences of the extended layers it targets, so a CLK for one extended layer leaves a global-bucket observation aimed at another extended layer untouched (metadata_timecode_n_frames_global_layer_values_survives_other_layer_clk). The CI-re-send dedup that suppresses re-reporting an unchanged repeat (§6.14) is EPOCH-aware (the unified model shared with the §6.16.10 scan-type guard): the recheck is skipped only when the re-sent CI is content-identical AND the existing record is still the post-epoch authority for it (existing.tu_index >= ci_rap_epoch), so an ordinary identical repeat in a LATER temporal unit with no random access point in between never re-reports the already-paired observation (metadata_timecode_n_frames_identical_ci_repeat_no_rap_reports_once / scan_type_identical_ci_repeat_no_rap_reports_once — the over-correction the earlier temporal-unit-identity predicate caused). When a CI is re-sent BEFORE its CLK in a §7.3.8.11 random access point the lagging epoch cannot yet tell it apart from an ordinary repeat at CI-time, so the dedup also skips there; the new coded video sequence's observations are then re-paired against the re-sent CI at the CLK (repair_post_rap_ci_pairings, run after observe_ci_rap advances the epoch and drops the stale pre-RAP deferred pairings) exactly once — the bound is never lost across the RAP (metadata_timecode_n_frames_rap_resent_identical_ci_repairs / scan_type_rap_resent_identical_ci_repairs). (c) counting_type reserved values 7..31: the §6.16.7 counting_type table (line 3823) marks 7..31 'reserved' with NO conformance sentence forbidding them — §6.16.7 only says counting_type 'should be the same for all pictures' (line 3804, a recommendation, not a 'shall'). So a reserved counting_type is a decoder-ignored producer anomaly as metadata/timecode-counting-type-reserved (WARNING, never a conformance error), matching the established reserved-value pattern for table-'reserved'-without-'shall' fields (metadata/persistence-idc-reserved, metadata/group-layer-idc-reserved) — NOT an error like mps_pic_struct_type > 12, which §6.16.10 Table 6.18 backs with a 'shall not be present' sentence (crates/splot-validate/src/checks/mod.rs check_metadata_unit_payload; metadata_timecode_counting_type_reserved_is_warned / _defined_is_silent). validate STAYS partial for TWO named residuals. (1) The §6.16.7 clockTimestamp output-order monotonicity sentence — 'When ci_timing_info_present_flag is equal to 1 and discontinuity_flag is equal to 0, the value of clockTimestamp shall be greater than or equal to the value of clockTimestamp for the previous set of clock timestamp syntax elements in OUTPUT order' (lines 3858-3860) — is blocked on frame OUTPUT-order state (no decoder / output process; the previous set in output order, not decoding order, is unavailable). (2) §7.3.8.11 step-3 content-interpretation INHERITANCE (docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-11, lines 925-929): when no CI OBU is present for embedded layer m, m's parameters are inherited from the highest lower layer k with MLayerPresenceMap[m][k] == 1. The n_frames bound for a LAYER_VALUES timecode targeting m should then pair with the inherited layer-k CI, but MLayerPresenceMap — the §5.4.1 transitive closure of the modeled MLayerDependencyMap (docs/spec/av2/1.0.0/05-syntax-structures.md lines 583-607) — is NOT modeled (splot-core derives MLayerDependencyMap but no presence closure; no mlayer_presence/MLayerPresenceMap state exists in the validator). Rather than guess the closure, timecode_ci_in_scope stays exact: an exact-pair miss with possible inheritance (timecode targets m, only CI is at k != m) compares NOTHING — an honest false-negative, not a silent in-scope-clean pass — and is treated as not-pairable; see the TODO(spec: AV2-5.17.7-METADATA-TIMECODE) at that gate. Do not mark the umbrella done until this child's output-order and inheritance residuals are resolved. Known false-negative: group-form LAYER_VALUES inference chains share the global carrying-pair key across disjoint target sets (TODO in context.rs; never a false positive). WRITER (metadata-obu-writer): write_timecode inverts the full/partial timestamp presence chains and time_offset; field domains + presence validated up front; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_inferred_seconds_without_previous_is_flagged", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_inference_after_present_value_passes", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_inference_names_each_absent_field", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_inference_chain_resets_at_clk", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_n_frames_exceeds_rate_is_flagged", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_n_frames_boundary_passes", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_n_frames_ci_arrives_after_metadata_flagged", "crates/splot-validate/src/validator/tests/timecode_basic.rs::metadata_timecode_n_frames_unequal_interval_bound", "crates/splot-validate/src/validator/tests/timecode_targeting.rs::metadata_timecode_n_frames_targeting_excludes_untargeted_layer_ci", "crates/splot-validate/src/validator/tests/timecode_targeting.rs::metadata_timecode_n_frames_olk_reinit_drops_deferred_pairing", "crates/splot-validate/src/validator/tests/timecode_targeting.rs::metadata_timecode_n_frames_rap_resent_identical_ci_repairs"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate timecode"]
fixtures = []
diagnostics = ["metadata/timecode-seconds-out-of-range", "metadata/timecode-minutes-out-of-range", "metadata/timecode-hours-out-of-range", "metadata/timecode-inferred-without-previous", "metadata/timecode-n-frames-exceeds-rate"]

[[feature]]
id = "AV2-5.17.8-METADATA-BANDING-HINTS"
name = "Banding hints metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.8", "6.16.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.8 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-8, lines 3905-3987) states no requirement of bitstream conformance: it opens by declaring that no normative debanding processing is required for decoder conformance, and every element is a descriptive hint or *_minus_N width/count with no locally decidable AV2 constraint. The structure is validated by the §5.17 metadata syntax parse: the MetadataSyntax check (registry id metadata/syntax in crates/splot-validate/src/checks/mod.rs:1100) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error and the trailing-bits/* ids; the metadata OBU is non-extensible, so no obu-header/extension-flag check applies). WRITER (metadata-obu-writer): write_banding_hints inverts the coding/source flags, the banding_hints_flag gate, the per-component fields, and the (varying-size) band-units recursion; all vec lengths + f(n) domains validated; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests"]
commands = ["cargo test -p splot-core metadata"]
fixtures = []
diagnostics = ["bitstream/parse-error", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.17.9-METADATA-ICC-PROFILE"
name = "ICC profile metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.9", "6.16.9"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.9 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-9, lines 3991-4019) states no requirement of bitstream conformance that AV2 can check: icc_profile_data_payload_bytes is defined to be an ICC profile per ISO 15076-1 / ISO 20677, with ICCmajorVer/ICCminorVer interpretations whose unlisted values are explicitly 'unspecified or specified by other means'. There is no locally decidable AV2 constraint; the structure is validated by the §5.17 metadata syntax parse: the MetadataSyntax check (registry id metadata/syntax in crates/splot-validate/src/checks/mod.rs:1100) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error and the trailing-bits/* ids; the metadata OBU is non-extensible, so no obu-header/extension-flag check applies). WRITER (metadata-obu-writer): the IccProfile arm emits the profile bytes via the passthrough input (the parser reads none); round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests"]
commands = ["cargo test -p splot-core metadata"]
fixtures = []
diagnostics = ["bitstream/parse-error", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.17.10-METADATA-SCAN-TYPE"
name = "Scan type metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.10", "6.16.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-semantic-validation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. The Table 6.18 ci_scan_type_idc cross-checks, the equal_picture_interval rule for mps_pic_struct_type 7/8, and the CVS-wide single-pic-struct-group conformance requirement are enforced over the exact §7.3.6 CVS boundary (per-xlayer scopes plus a global bucket; global metadata is compared against each concrete xlayer scope and vice versa). The Table 6.18 CI pairings are additionally scoped to the §7.3.8.11 CI-parameter epoch: a CLK or OLK temporal unit re-initializes the extended layer's content interpretation parameters, so pre-epoch CI records and pre-epoch observations are never paired across the random access point (a CI re-sent in the RAP temporal unit re-enters pairing per §7.3.8.11 step 2); each (observation, CI-content) pair is evaluated exactly once — a repeated CI with identical Table 6.18-decisive content skips re-evaluation, while a CI for a new embedded layer or with changed content is paired with every stored observation. Reserved mps_pic_struct_type values (>12) are excluded from the group/CI state per 'Decoders shall ignore reserved values'. Negative finding (spec honesty): the mirror defines mps_source_scan_type_idc only as having 'the same semantics as for ci_scan_type_idc' (§6.16.10) — no mps_source_scan_type_idc/ci_scan_type_idc consistency rule exists, so none is checked; mps_duplicate_flag is indicative only. metadata/scan-type-ci-scan-type-unestablished is a derived literal-reading warning (the §7.3.8.11 default ci_scan_type_idc 0 satisfies no Table 6.18 row) and deliberately ignores the RAP epoch (lenient documented approximation for a warning-severity diagnostic); absent timing_info is silent for the 7/8 equal-picture-interval rule (the mirror gives no absent-case rule). WRITER (metadata-obu-writer): write_scan_type writes mps_pic_struct_type/idc/duplicate with domain checks; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_group_mixing_in_cvs_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_group_mixing_across_tu_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_group_change_after_clk_accepted", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_group_mixing_between_global_and_xlayer_scopes_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_reserved_value_excluded_from_group_state", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_ci_mismatch_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_ci_arrives_after_metadata_mismatch_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_frame_doubling_requires_equal_picture_interval", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_without_ci_warns_unestablished_at_eos", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_unestablished_warned_at_cvs_restart", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_ci_for_second_embedded_layer_rechecked", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_ci_mismatch_on_second_xlayer_rechecked", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_equal_picture_interval_rechecked_for_second_layer", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_contradicting_ci_repeat_rechecked_and_co_reported", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_pre_olk_ci_not_paired_with_post_olk_metadata", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_ci_resent_at_olk_pairs_with_post_olk_metadata", "crates/splot-validate/src/validator/tests/scan_type.rs::scan_type_pre_olk_metadata_not_paired_with_olk_tu_ci", "crates/splot-validate/src/validator/tests/scan_type.rs::repeated_ci_differs_across_olk_still_flagged", "crates/splot-validate/src/validator/tests/scan_type.rs::ci_timing_mismatch_across_olk_still_flagged"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata", "cargo test -p splot-validate scan_type"]
fixtures = []
diagnostics = ["metadata/scan-type-pic-struct-reserved", "metadata/scan-type-ci-scan-type-mismatch", "metadata/scan-type-ci-scan-type-unestablished", "metadata/scan-type-equal-picture-interval-required", "metadata/scan-type-pic-struct-group-inconsistent"]

[[feature]]
id = "AV2-5.17.11-METADATA-TEMPORAL-POINT-INFO"
name = "Temporal point info metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.11", "6.16.11"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.11 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-11, lines 4103-4129) states one requirement of bitstream conformance — METADATA_TYPE_TEMPORAL_POINT_INFO shall only appear in OBU_METADATA_SHORT (lines 4110-4112) — landed as metadata/temporal-point-info-not-short (crates/splot-validate/src/checks/mod.rs:1306). The only other element, frame_presentation_time, is a DispCT-counted presentation time relative to random access points: it is a decoder-model quantity (Annex E frame timing) with no locally decidable bitstream constraint, so its semantics are decoder-model-blocked (Annex E, AV2-E-DECODER-MODEL territory) and nothing further is locally decidable for §6.16.11. WRITER (metadata-obu-writer): the TemporalPointInfo arm writes frame_presentation_time leb128 (minimal); round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/metadata_syntax.rs::metadata_temporal_point_info_in_group_is_flagged"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate metadata_temporal"]
fixtures = []
diagnostics = ["metadata/temporal-point-info-not-short"]

[[feature]]
id = "AV2-5.17.12-METADATA-DECODED-FRAME-HASH"
name = "Decoded frame hash metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.12", "6.16.13"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-residual-local-semantics"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.13 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13, lines 4149-4263) defines the decoded-frame-hash content over OutY/OutU/OutV produced by the §7.21.1 output process (plus §7.21.2 intermediate output preparation and §7.21.7 film-grain synthesis), with the MD5 algorithm of [RFC1321]. The metadata-residual-local-semantics change landed the one locally-decidable reserved-field fact: 'reserved shall be set to 0 and ignored by decoders' (line 4248) as metadata/decoded-frame-hash-reserved-nonzero (warning, crates/splot-validate/src/checks/mod.rs check_metadata_unit_payload), following the established decoder-ignored reserved-field pattern (content-interpretation/reserved-bits-nonzero, metadata/group-reserved-bits-nonzero). validate STAYS partial for ONE named residual: verifying plane_hash/frame_hash against the decoded output is decoder-blocked — it needs the §7.21 output process (and §7.21.7 film-grain synthesis when has_grain == 1), which is not implemented (no decoder). (hash_type's 'all other values reserved for future use' is a value-space note with no 'shall', so no diagnostic is added for it.) Tracked behind the decoder/output-process backlog. WRITER (metadata-obu-writer): write_decoded_frame_hash inverts the flags + the per-plane/single 16-byte hash selection; presence + domains validated; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_decoded_frame_hash_reserved_nonzero_is_warned", "crates/splot-validate/src/validator/tests/metadata_semantics.rs::metadata_decoded_frame_hash_reserved_zero_is_silent"]
commands = ["cargo test -p splot-core metadata", "cargo test -p splot-validate decoded_frame_hash"]
fixtures = []
diagnostics = ["metadata/decoded-frame-hash-reserved-nonzero"]

[[feature]]
id = "AV2-5.17.13-METADATA-USER-DATA-UNREGISTERED"
name = "User data unregistered metadata syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.17.13", "6.16.12"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/metadata.rs"
change_id = "metadata-padding-foundation"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Child row of AV2-5.17-METADATA. §6.16.12 (docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-12, lines 4133-4145) states no requirement of bitstream conformance that AV2 can check: uuid_iso_iec_11578 conforms to ISO/IEC 11578:1996 and the spec explicitly 'does not specify or restrict the format or interpretation of the user_data_payload_byte payload bytes'. There is no locally decidable AV2 constraint; the structure is validated by the §5.17 metadata syntax parse: the MetadataSyntax check (registry id metadata/syntax in crates/splot-validate/src/checks/mod.rs:1100) emits no per-field diagnostic for this child, so a malformed payload surfaces the actually-emitted parse-failure rule ids via syntax_error_diagnostic()/payload_parse_error_diagnostic() (bitstream/parse-error and the trailing-bits/* ids; the metadata OBU is non-extensible, so no obu-header/extension-flag check applies). WRITER (metadata-obu-writer): the UserDataUnregistered arm writes the 16-byte UUID then the user-data passthrough bytes; round-trip tested. write=done."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/metadata.rs::tests"]
commands = ["cargo test -p splot-core metadata"]
fixtures = []
diagnostics = ["bitstream/parse-error", "trailing-bits/empty", "trailing-bits/missing-one-bit", "trailing-bits/zero-bit-not-zero"]

[[feature]]
id = "AV2-5.18-FRAME-HEADER"
name = "Frame header syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18", "6.17"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/mod.rs"
change_id = "frame-header-core-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Activation-prefix parser plus a state-aware frame-header core parser that covers selected §5.18.2 / §5.18.3 / §5.18.4 paths (frame kind, output control, order hint, refresh flags, direct/default frame_size(), screen_content_params(), intrabc_params()) and stops with an explicit status before deeper §5.18 syntax. Filtering, quantization, segmentation/tiling, transform/coding modes, global motion, frame film-grain structures, the inter reference map, frame_header_copy(), and tile payload remain future. Under obu-dispatch-frame-payloads the SEF/TIP/bridge family is reached by the stateless dispatcher's PayloadStatus::PrefixParsed arm (crates/splot-core/src/obu.rs): dispatch parses the §5.18.2 activation prefix via parse_frame_header_prefix and reports blocked_on='active sequence header state' for the state-dependent remainder owned here, instead of a blanket Unimplemented. WRITER: the frame-header writer is landing as a layered slice backlog (#4a-#4i, intra path only). #4a (frame-header-writer-prefix) inverts the §5.18.2 activation prefix (write_frame_header_prefix, AV2-5.18.1-FRAME-HEADER-GENERAL write=partial); the umbrella write stays todo until the composing write_frame_header lands (#4i). WRITER: the INTRA frame_header() path is inverted by write_frame_header_core (crates/splot-core/src/write/frame_header_core.rs, IntraHeaderComplete); the inter / show-existing paths remain (blocked on the inter frame-header parser), so write stays partial."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/write/frame_header_core_tests.rs::tests", "crates/splot-core/src/headers/frame/mod.rs::tests", "crates/splot-core/src/headers/frame/info.rs::tests", "crates/splot-core/src/headers/tile_group.rs::tests", "crates/splot-validate/src/validator/tests/hls_frame_header.rs::hls_frame_header_missing_sequence_header_is_flagged"]
commands = ["cargo test -p splot-core frame_header", "cargo test -p splot-core tile_group", "cargo test -p splot-validate frame_header"]
fixtures = ["tests/fixtures/frame-header-core.av2"]
diagnostics = ["hls/unavailable-sequence-header", "hls/unavailable-multi-frame-header", "frame-header/seq-header-id-out-of-range", "frame-header/cur-mfh-id-out-of-range"]

[[feature]]
id = "AV2-5.18.1-FRAME-HEADER-GENERAL"
name = "Frame header general syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.1", "6.17"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/mod.rs"
change_id = "frame-header-copy-bit-accounting"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. frame_header(isFirst) is parsed beyond the activation fields by the core parser (AV2-5.18.2-FRAME-HEADER-INFO). Under frame-header-copy-bit-accounting the §5.18.1 NumFrameHeaderBits accounting and the isFirst==0 frame_header_copy() path are now MODELED for completed-intra first headers: when a first tile group's frame_header_info() parses to completion (FrameHeaderParseStatus::IntraHeaderComplete) the core's consumed_bits IS NumFrameHeaderBits (mirror :3924), and splot-core records the exact first-header bits (RecordedFrameHeaderBits, starting AFTER the tile_group_obu() is_first_tile_group flag per §6.17.1 mirror :4303-4305) and parses a non-first tile group's frame_header_copy() as exactly that many header_bit f(1) reads, comparing bit-for-bit (parse_frame_header_copy → FrameHeaderCopyOutcome, crates/splot-core/src/headers/tile_group.rs). The validator (crates/splot-validate/src/context/mod.rs observe_frame_header_copy / check_frame_header_copy) pairs a non-first tile group with ITS coded frame's first tile group using the FrameUnitSegmenter boundary authority (OpensNewUnit records, ContinuesUnit compares, Ambiguous drops the pairing AND poisons/removes the triple's record — an unreadable is_first_tile_group may have opened a new coded frame, so the recorded header can no longer pair until the next OpensNewUnit re-records), keyed per (xlayer, mlayer, tlayer) and cleared at each temporal delimiter. §6.17.1 states it is a requirement of bitstream conformance that header_bit[i] equals the bit at offset i of the first frame header (mirror :4299-4300), surfaced as frame-header/copy-bits-mismatch; a copy region shorter than NumFrameHeaderBits is a §5.18.1/§6.2.1 truncation, frame-header/copy-bits-truncated. An incomplete/coverage-stopped/unresolvable first header records nothing (copy stays unparsed, Unknown routing). inspect surfaces the copy region's presence on non-first tile groups (frame_header_copy view). Residual: frame_header_copy() for an INTER first header (the gate extends when the inter path completes, AV2-5.18.2-FRAME-HEADER-INFO), and a show-existing-frame CLK never pairs (decode_frame_wrapup() runs with no following tile group). WRITER (frame-header-writer-prefix): the §5.18.2 activation prefix is inverted by write_frame_header_prefix (crates/splot-core/src/write/frame_header.rs) — it writes cur_mfh_id uvlc (omitted/inferred 0 for a bridge frame) and, when cur_mfh_id==0, seq_header_id_in_frame_header uvlc; the derived is_*/startCVS fields carry no bits and are validated against the obu_type derivation up front (reject-before-write via NonCanonicalFrameHeader). write stays partial — the composing write_frame_header and the §5.18.1 NumFrameHeaderBits / frame_header_copy accounting land with the later frame-header slices (#4b-#4i)."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/mod.rs::tests::frame_header_prefix_reads_cur_mfh_id_zero_and_seq_header_id", "crates/splot-core/src/headers/frame/mod.rs::tests::frame_header_prefix_eof_is_structured_error", "crates/splot-core/src/write/frame_header.rs::tests", "crates/splot-core/src/write/frame_header.rs::proptests", "crates/splot-core/src/headers/tile_group.rs::tests::recorded_frame_header_bits_round_trips_through_copy", "crates/splot-core/src/headers/tile_group.rs::tests::frame_header_copy_reports_first_mismatch_bit", "crates/splot-core/src/headers/tile_group.rs::tests::frame_header_copy_reports_truncation_when_payload_short", "crates/splot-core/src/headers/tile_group.rs::proptests::frame_header_copy_never_panics", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_silent_on_matching_frame_header_copy", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_frame_header_copy_mismatch", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_frame_header_copy_truncated", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_frame_header_copy_silent_when_first_header_incomplete", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_frame_header_copy_dropped_on_ambiguous_boundary", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_frame_header_copy_record_resets_across_temporal_units", "crates/splot-cli/tests/cli.rs::inspect_json_surfaces_frame_header_copy_on_non_first_tile_group"]
commands = ["cargo test -p splot-core frame_header", "cargo test -p splot-core write::frame_header", "cargo test -p splot-core tile_group", "cargo test -p splot-validate frame_header_copy", "cargo test -p splot-cli frame_header_copy"]
fixtures = []
diagnostics = ["frame-header/copy-bits-mismatch", "frame-header/copy-bits-truncated"]

[[feature]]
id = "AV2-5.18.2-FRAME-HEADER-INFO"
name = "Frame header info syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.2", "6.17.2"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/info.rs"
change_id = "decode-inter-header-shared-tail"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. State-aware core parser (FrameHeaderParseMode::Core) over the control region: derived frame-kind flags, bridge_frame_ref_idx, the single-picture / show-existing-frame branch, frame-type determination, long-term id, output-control flags, order_hint, and refresh_frame_flags. Under mfh-frame-header-state the two cur_mfh_id > 0 stops that previously fired purely for missing MFH state are removed: with the resolved MultiFrameHeaderRecord threaded in, the intra path parses through frame_size() (AV2-5.18.4-FRAME-SIZE) and segmentation_params() (AV2-5.18.7-SEGMENTATION-TILING) on the cur_mfh_id > 0 path the same as cur_mfh_id == 0. Under frame-filtering-deblocking-gdf-cdef the intra-path stop advanced past the §5.18.2 tail loop-filter cluster — deblocking_filter_params() (AV2-5.18.5-FILTERING), gdf_params(), and cdef_params() (both AV2-5.18.7-SEGMENTATION-TILING). Under frame-loop-restoration-ccso-params the stop advanced past lr_params() (§5.18.7.11) and ccso_params() (§5.18.7.12, both AV2-5.18.7-SEGMENTATION-TILING). Under frame-header-intra-tail-completion the intra path is now PARSED TO COMPLETION: after ccso_params() the §5.18.2 tail parses read_tx_mode() (AV2-5.18.8-TRANSFORM-CODING-MODES), the no-bit frame_reference_mode()/skip_mode_params() intra inferences (same row), the no-bit allow_bawp/allow_warpmv_mode inferences, reduced_tx_set f(2), the no-bit global_motion_params() intra arm (AV2-5.18.9-GLOBAL-MOTION), and film_grain_config() (AV2-5.18.10-FILM-GRAIN-STRUCTURES), reaching the terminal IntraHeaderComplete (parse_intra_tail_structures, crates/splot-core/src/headers/frame/tail.rs). The show-existing-frame path likewise completes (ShowExistingFrameComplete) after its film_grain_config(). On the intra path NumTotalRefs == 0 (mirror :4573), so lr_params()'s temporal-prediction arm and ccso_params()'s reuse arm are dead. When an lr plane signals frame_filters_on, lr_params() now consumes the fixed-coded read_wienerns_filter() frame-level Wiener bank and preserves it on LrPlaneParams::frame_filter_bank. The parser now has one complete result path: every modeled Wiener branch completes or returns a typed parse error. A payload that ends INSIDE the loop-filter cluster (deblocking through ccso) reports StoppedInsideFilterParams; a payload that ends INSIDE the §5.18.2 tail reports StoppedInsideIntraTail — both preserve the already-parsed facts and leave the unreached fields None, so the validator/inspect .ok() of the core still sees those facts. A cur_mfh_id > 0 frame whose in-band MFH is unresolvable still stops with UnsupportedUntilFeature (no guessing). Now that the intra tail, the loop-filter cluster, and the SEF film_grain_config() tail are FULLY modeled, an EOF inside them is a decidable bitstream defect: the SEF film-grain EOF reports the dedicated StoppedInsideShowExistingFrame, and FrameHeaderParseStatus::is_truncated_in_modeled_region() partitions the three EOF-in-a-fully-modeled-region statuses (StoppedInsideFilterParams / StoppedInsideIntraTail / StoppedInsideShowExistingFrame) from every coverage / complete status. frame_header_core_checks surfaces that partition as frame-header/truncated-frame-header (error, §6.2.1: frame_header( ) reads the §5.18.2 mandatory syntax from the §6.2.1 OBU payload, which must contain it before the trailing bits) while preserving the facts path unchanged (the StoppedInside* core fields still feed every other diagnostic; celu / frame_unit judgments unchanged). Coverage stops (`UnsupportedUntilFeature`, including the MFH-unresolvable stops) stay silent. The SHOW-EXISTING-FRAME OBU payload boundary IS now fully decidable (codex round): a SEF OBU is not an is_tile_group() type, so usedArith == 0 and §5.2.1 (:124-152) makes the rest of the payload exactly trailing_bits( remainingPayloadBits ) — the SEF arm of §5.18.2 (:4145) return()s right after film_grain_config() (:4186) and there is NO tile data. The core parser classifies that boundary (SefTrailingBits, recorded on core.sef_trailing_bits on the ShowExistingFrameComplete path) and the validator surfaces a non-conformant tail as frame-header/sef-trailing-bits-invalid (§6.2.3 / §6.2.1); this also catches the grain_seed-eats-the-trailing_one_bit case (a grain_seed short by its final bit consumes the marker, so no trailing_one_bit remains). film_grain_params_present (§5.4.1, read LAST in the sequence header after every child config) no longer gates the WHOLE CoreSeqView::from_sequence (codex round): it is carried as Option<bool> and the requirement is deferred to film_grain_config() consumption, so a frame against a sequence header recorded from a bounded sequence_tile_config() stop now parses its control region (frame size / output / order-hint / tile / quant / segmentation diagnostics fire) and stops honestly (UnsupportedUntilFeature, facts preserved, no guessed apply_grain) only at the grain-gated tail. Under frame-reference-state-model the FrameReferenceStateView is no longer an always-unknown placeholder: the validator now models the §7.23 per-extended-layer reference-frame buffer (AV2-7.23-REFERENCE-FRAME-UPDATE) and threads the modeled RefValid/RefOrderHint/dims into the core parse via the new FrameReferenceStateView::from_slots constructor. No §5.18 INTRA parse branch consumes reference_state today (the intra paths derive their state without it), so this is forward plumbing for the §5.18 INTER reference paths; the validator already consumes the modeled buffer directly for the §6.17.2 show-existing-frame slot check (frame-header/show-existing-frame-invalid-slot). Residuals: (1) the FIRST-tile-group header/tile-data boundary is now EXACT on the INTRA-COMPLETE path (tile-group-structure-completion): once frame_header_info() reaches IntraHeaderComplete on the intra path, use_bru/bru_inactive are the §5.18.2 derived constant 0 (mirror :4127-4129 / :4653), so AV2-5.19-TILE-GROUP's parse_tile_group_structure consumes the §5.19 tile_group_obu() remainder (tile_start_and_end_present_flag, tg_start/tg_end, byte_alignment(), headerBytes) and records the §5.20 tile_group_payload() boundary — the §5.18.2 intra tail is no longer confusable with tile data or padding for an intra-complete first tile group, and a non-zero byte_alignment() pad is flagged (tile-group/byte-alignment-zero-bit §6.2.4). The remaining first-tile-group ambiguity is narrowed to the NON-intra-complete first tile group (an inter/TIP/bridge first header, or a coverage/truncation stop), where use_bru/bru_inactive cannot be derived (BruUndecidable::NotIntraComplete) so the §5.19 structure stays unjudged; the §5.20 tile_group_payload() body itself stays unparsed (AV2-5.20-TILE-GROUP-PAYLOAD). The NON-FIRST tile-group case is PARTIALLY RESOLVED (frame-header-copy-bit-accounting): when the first tile group's frame_header_info() completed (IntraHeaderComplete), NumFrameHeaderBits is known, so a non-first tile group's frame_header(isFirst==0)==frame_header_copy() region has an EXACT header/tile-data boundary (parse_frame_header_copy consumes exactly NumFrameHeaderBits, §5.18.1) and is bit-checked against the first header (frame-header/copy-bits-mismatch §6.17.1, frame-header/copy-bits-truncated §6.2.1) before the §5.19 tile-data tail; the SEF case is the other decidable half (no tile data, sef-trailing-bits-invalid). inspect surfaces the parsed FrameHeaderParseStatus (status field), the SEF trailing-bits classification (sef_trailing_bits field), the non-first frame_header_copy() region presence (frame_header_copy view), and the §5.19 tile_group_structure view (tg range, headerBytes/payload_size boundary) for the intra-complete first tile group. (2) Under frame-header-inter-reference-paths the NON-INTRA §5.18.2 control region is now PARSED (crates/splot-core/src/headers/frame/inter.rs, InterControl): after the shared output-control flags, the inter/switch/TIP path reads frame_size_override_flag / order_hint, then the reference-control region — the primary-reference signaling (signal_primary_ref_frame, disable_cross_frame_cdf_init, primary_ref_frame f(3) / PRIMARY_REF_CHOOSE / the SWITCH/bridge PRIMARY_REF_NONE inference), bridge_frame_overwrite_flag, the inter/switch/short refresh_frame_flags arms, the explicit reference map (frame_explicit_ref_frame_map f(1), num_total_refs f(3), ref_frame_idx[i] f(CeilLog2(NumRefFrames))), the reference-grounded frame size (frame_size_with_refs() §5.18.4.3 found_ref + RefFrameWidth/Height, frame_size_with_bridge() §5.18.4.2 Min with the bridge ref dims, or the non-override default — all AV2-5.18.4-FRAME-SIZE), the BRU triple (use_bru / bru_ref f(CeilLog2(NumTotalRefs)) / bru_inactive), use_ref_frame_mvs / tmvp_sample_step_minus_1, frame_opfl_refine_type() (§5.18.3.2, AV2-5.18.3-FRAME-CONFIGURATION), screen_content_params() / intrabc_params() (§5.18.3.3/.4), the change_drl / max_drl_bits_minus_1 ns(MAX_REF_MV_STACK_SIZE-2) override, the MV-precision block (use_qtr_precision_mv / allow_high_precision_mv -> FrameMvPrecision), read_interpolation_filter() (§5.18.5.1, AV2-5.18.5-FILTERING), and frame_enabled_motion_modes[mode], converging into the shared tail (InterStop::ReachedSharedTail, mirror :5183). The parse is HONEST about reference-state dependence: a branch needing a derivation this phase does not model (get_ref_frames() for the implicit reference map, get_past_future_cur_ref_lists() for the TIP block's usesEqualWeight past/future ref counts) stops with InterStop::UnmodeledDerivation; a branch needing a slot the modeled §7.23 buffer has not proven valid (frame_size_with_refs/with_bridge dims) stops with InterStop::PoisonedReferenceState — both are COVERAGE stops (the core status is the unsupported-coverage class, NEVER a truncation), with the facts parsed before the stop preserved on core.inter. The bru_inactive / IsBridge early-return arm (mirror :4971/:5045) and the TIP_FRAME_AS_OUTPUT return arm (mirror :4945/:5177) read film_grain_config() / tile_info() against reference-frame dims this phase does not thread on those arms, so they stop at the start of that arm (InterStop::BruInactiveOrBridgeReturn / TipAsOutputReturn). The parsed ref_frame_idx feeds the §6.17.2 inter ref-idx slot-validity check (frame-header/ref-frame-idx-invalid-slot, AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS): the validator's reference_state_checks fires ONLY where the §7.23 buffer PROVES RefValid[ref_frame_idx[i]] == 0 (a CLK-invalidated, never-refreshed slot); an Unknown slot or the implicit reference map stays silent (no guessing). inspect surfaces the whole region (the inter view). Under decode-inter-header-shared-tail (DECODE-INTER-HEADER-SHARED-TAIL) the inter path now CONTINUES PAST InterStop::ReachedSharedTail through the §5.18.2 shared tail to a new terminal FrameHeaderParseStatus::InterHeaderComplete for the verified minimal-tool single-reference inter subset (crates/splot-core/src/headers/frame/inter_shared_tail.rs::parse_inter_shared_tail): it REUSES the intra shared-tail sub-parsers with the inter inputs — tile_info() with frame_is_intra==false, quantization_params(), setup_qm_params(), delta_q_params(), parse_lossless_info(), deblocking_filter_params() with the inter allow_df_sub_pu arm (§5.18.5.2 mirror :5935, new read_allow_df_sub_pu param + enable_df_sub_pu on CoreSeqFilterView), gdf_params(), cdef_params(), lr_params(), ccso_params(), read_tx_mode(), the §5.18.9.1 global_motion_params() inter arm (parse_global_motion_params), and film_grain_config() — plus the inter-specific frame_reference_mode() reference_select f(1), skip_mode_params() skip_mode_present f(1), and the gated allow_bawp / allow_warpmv_mode reads (enable_bawp / frame_enabled_motion_modes[DELTAWARP]). The parsed inter-tail arms live on FrameHeaderCore::inter_tail (the new InterTail struct). The parse is HONEST about the inter-specific arms the shared sub-parsers do NOT model: it stops with UnsupportedUntilFeature (a coverage stop, never a truncation) BEFORE reading any unmodeled-arm bit — when segmentation_enabled==1 (the §5.18.7.1 DerivedPrimaryRefFrame arm; the shared tail reads only segmentation_enabled f(1) then stops), at the TOP-OF-TAIL admission gate when enable_restoration && NumTotalRefs>0 (the §5.18.7.11 temporal-prediction arm) or enable_ccso (the §5.18.7.12 reuse arm) — stopping before ANY shared-tail bit so no possibly-mis-positioned setup_qm/using_qmatrix is exposed — or when global_motion_params() reaches its cross-frame GlobalMotionStop (use_global_motion==1 warp models). Verified bit-exact on the syn-2frame-inter-64x64.ivf fixture (reaches InterHeaderComplete, base_q_idx 119, single tile, all arms read at the right bit; the 56-bit inter payload = 27-bit control region + 21-bit shared tail + 8-bit §5.19 tile-group tail); the richer syn-key-inter-64x64.ivf inter frames (enable_ccso on) stop at the admission gate, preserving that fixture's clean verdict (no regression). NO decode-output change: the runtime still rejects the inter frame at §5.20 mode_info (the next brick). Still TODO past the verified subset: the enabled-segmentation inter arm (DerivedPrimaryRefFrame from choose_primary_secondary_ref_frame()'s RefBaseQIdx ranking), the lr temporal-prediction / ccso reuse inter arms, the §5.18.9 use_global_motion warp decode, quantization_params()' TIP arm, and the §5.19 BRU tile-group arms (bru_inactive trailing_bits early-return, the use_bru bru_tile_active loop, AV2-5.19-TILE-GROUP) which need the inter frame to COMPLETE (NumFrameHeaderBits known) — now unblocked for the minimal subset by InterHeaderComplete; the entropy-coded LR unit Wiener syntax, loop-restoration reconstruction, and the bridge-frame remainder past frame_size_with_bridge() likewise remain TODO. WRITER (frame-header-writer-compose, slice #4i): the composing intra frame-header writer pub fn write_frame_header_core (crates/splot-core/src/write/frame_header_core.rs) is now the exact inverse of parse_frame_header_core on the IntraHeaderComplete path. It emits the whole intra frame_header_info() in §5.18.2 read order — delegating the §5.18.2 activation prefix to write_frame_header_prefix (#4a), writing the frame-type-dependent control-region glue directly (the bridge_frame_ref_idx; the frame-type arm frame_is_inter==0 / the CLK/OLK no-bit Key inference; the KEY long_term_id_plus_1 and OLK num_key_ref_frames + ref_long_term_id[i] reads; the immediate/implicit output-control flags with their OLK / immediate||monotonic inferences; order_hint; the refresh_frame_flags arm — the CLK closed-loop allFrames no-bit inference, the KEY/INTRA_ONLY short-refresh frame_to_refresh arms, and the direct f(NumRefFrames) arm — inverting read_refresh_frame_flags exactly; and disable_cdf_update), and delegating every sub-structure to its #4b-#4h sub-writer (frame_size/screen_content/intrabc #4c, tile_info #4f, the quant cluster #4b, segmentation #4g, the loop-filter cluster #4d, lr/ccso #4e/#4h, and the §5.18.2 intra tail). CoreSeqView / MfhFrameView (info.rs) are exposed pub (crate-private fields) with their from_sequence/from_record constructors so the writer takes them as inputs. A full pre-write check (check_frame_header_core_encodable) rejects — before any bit — a status != IntraHeaderComplete, a show-existing / inter / non-intra model, any required Option that is None on the intra path, a control-region inferred value that disagrees with its derivation, a refresh_frame_flags the selected arm cannot represent, or an out-of-domain coded field (NonCanonicalFrameHeader{what}); the whole header is drafted into a scratch BitWriter (the new BitWriter::append commits it on full success) so a sub-writer reject mid-compose leaves the caller's writer untouched (reject-before-write for the entire composition, bit_len()==0). Proven by parse->write->parse byte-exact round-trips (CLK non-lossless, OLK, INTRA_ONLY, single-picture key, lossless, grain present, multi-tile, cur_mfh_id>0) plus reject tests and a canonicalizing-inverse proptest. write flips todo->partial — the inter frame-header paths and the §5.18.7.11 frame-level Wiener bank remain unwritten. Under frame-header-single-picture-bridge-fix the single-picture IsBridge corner is corrected: a single_picture_header_flag==1 OBU_BRIDGE_FRAME is forced to KEY_FRAME / FrameIsIntra=1 BEFORE the IsBridge INTER_FRAME assignment (mirror :4131-4205), so it travels the FrameIsIntra reads but ends on the shared IsBridge early-return arm (:4971/:5045), NOT the full intra structure cluster. parse_single_picture_bridge_tail (crates/splot-core/src/headers/frame/info.rs) reads bridge_frame_overwrite_flag f(1) (:4423), the OVERWRITE-GATED refresh_frame_flags (per §6.17.2 semantics + AVM, NOT the §5.18.2 KEY-arm literal — see the divergence note: overwrite==0 -> inferred 1<<bridge_frame_ref_idx with no bits; overwrite==1 -> read it, the AVM bridge short/long arm), the AVM/§5.18.4.2 bridge maximum width and height fields clamped to the selected reference, screen_content_params() (:4569) and intrabc_params() (:4571), and — per codex PR review — the decidable §5.18.10.1 film_grain_config() tail of the IsBridge arm (:5011): apply_grain is inferred from single_picture_header_flag + immediate_output_frame (:8169-8171) and reads fgm_id f(3) + grain_seed f(16) when film_grain_params_present, the LAST modeled frame-header bits (a non-single bridge has immediate_output==0 -> apply_grain==0 -> no grain bits, which is why only the single-picture bridge reads them); the parsed grain config is retained. With proven selected-reference dimensions and quantizers, the parser also derives the zero-bit bridge tile layout and reference quantization and completes as InterHeaderComplete; missing reference state remains an honest UnsupportedUntilFeature coverage stop, and EOF in any modeled read remains StoppedInsideInterControl. The pre-fix parser wrongly routed it through parse_intra_tail (reading order_hint + disable_cdf_update + the full quant/segmentation/delta_q/loop-filter/lr/ccso cluster and reaching a bogus IntraHeaderComplete), violating parse_intra_tail's own TipFrameMode==TIP_FRAME_DISABLED && !IsBridge invariant. SPEC vs references (documented in change_id:frame-header-single-picture-bridge-fix): the single-picture bridge is a degenerate corner where the normative spec is INTERNALLY inconsistent. (1) refresh_frame_flags: §5.18.2 syntax would read it unconditionally on the KEY arm (:4429-4445), but §6.17.2 semantics (06-syntax-structures-semantics.md :4522-4524) says it is inferred 1<<bridge_frame_ref_idx when bridge_frame_overwrite_flag==0; AVM (av2/decoder/decodeframe.c:8394-8422) implements that overwrite-gated reading. Per the maintainer decision (codex PR #180 review) splot follows §6.17.2 + AVM (overwrite-gated) so the validator matches the reference decoder and does not misparse a real overwrite==0 stream. (2) AVM setup_frame_size and §5.18.4.2 read bridge maximum width and height fields even though the literal §5.18.2 FrameIsIntra control flow points at frame_size(); splot follows the bridge-specific syntax and AVM. Unmodified AVM revision 3935f4bf crashes before reconstruction because KEY classification clears the selected reference; the isolated decoder-only repair recorded by lref-avm-single-picture-bridge preserves that buffer without changing codec math, and its 7,680-byte raw output is byte-identical to Splot. dav2d does not model this corner."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/info.rs::tests", "crates/splot-core/src/headers/frame/inter.rs::tests", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_single_picture_bridge_reads_prefix_then_bridge_return", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_single_picture_bridge_reads_scc_and_intrabc_conditionals", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_single_picture_bridge_eof_in_prefix_is_truncation", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_single_picture_bridge_reads_film_grain_tail", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_default_dims_parse_through_tile_info", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_unresolvable_mfh_default_size_stays_unsupported", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_full_structure_cluster", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_lr_ccso_and_tail_to_completion", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_intra_tail_preserves_cluster_facts", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_with_grain_present_reads_id_and_seed", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_with_grain_reads_apply_grain_then_completes", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_eof_inside_film_grain_preserves_facts", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_deblocking_filter_params_preserves_facts", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_ccso_params_preserves_facts", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_implicit_map_stops_unmodeled", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_explicit_map_reaches_shared_tail", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_shared_tail_reads_inter_arms_with_asymmetric_values", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_shared_tail_segmentation_on_stops_unmodeled", "crates/splot-core/src/headers/frame/inter.rs::tests::frame_header_core_inter_fixture_reaches_inter_header_complete", "crates/splot-core/src/headers/frame/filtering.rs::tests::deblocking_inter_reads_allow_df_sub_pu_before_apply", "crates/splot-core/src/headers/frame/filtering.rs::tests::deblocking_intra_skips_allow_df_sub_pu", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_ras_with_unknown_reference_state_stops_after_long_term_ids", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_explicit_map_parses_through_to_shared_tail", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_stops_unmodeled", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_ref_idx_out_of_buffer_range_is_flagged", "crates/splot-core/src/headers/frame/inter.rs::tests::switch_frame_infers_primary_ref_none_and_reads_explicit_map", "crates/splot-core/src/headers/frame/inter.rs::tests::frame_size_with_refs_copies_valid_ref_dims", "crates/splot-core/src/headers/frame/inter.rs::tests::bru_triple_reads_and_inactive_returns", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_eof_before_primary_ref_is_error", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_frame_size_exceeds_max_when_truncated_inside_deblocking", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_truncated_frame_header_inside_intra_tail", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_truncated_frame_header_inside_sef_film_grain", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_truncated_frame_header_silent_on_complete_and_coverage_stops", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_bridge_ref_index_out_of_range", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_ras_requires_long_term_frame_id_bits", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_proven_invalid_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_valid_slot_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_poisoned_slot_drops_to_silence", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_backed_frame_is_decided_not_unknown", "crates/splot-cli/tests/cli.rs::inspect_json_exposes_mfh_backed_frame_header_core", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_nonzero_bits_after_fields_flag_trailing_bits", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_grain_seed_short_one_bit_eats_trailing_marker", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_unknown_grain_flag_parses_control_region_then_stops", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_unknown_grain_flag_preserves_facts_then_stops", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_sef_nonzero_bits_after_fields_as_trailing_bits_defect", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_sef_grain_seed_short_one_bit_as_trailing_bits_defect", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_sef_trailing_bits_silent_on_conformant_sef", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_frame_size_check_fires_against_bounded_tile_config_sequence", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::clk_non_lossless_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::olk_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::intra_only_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::single_picture_key_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::lossless_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::grain_present_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::multi_tile_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::mfh_nonzero_round_trips", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::reject_non_intra_status", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::reject_missing_required_option", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::reject_show_existing_model", "crates/splot-core/src/write/frame_header_core_tests.rs::tests::reject_refresh_flags_arm_cannot_represent", "crates/splot-core/src/write/frame_header_core_proptests.rs::proptests::intra_header_round_trips"]
commands = ["cargo test -p splot-core frame_header", "cargo test -p splot-core headers::frame::inter", "cargo test -p splot-validate frame_header", "cargo test -p splot-validate ref_state_inter", "cargo test -p splot-validate celu_mfh", "cargo test -p splot-cli mfh"]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = ["hls/unavailable-sequence-header", "hls/unavailable-multi-frame-header", "frame-header/seq-header-id-out-of-range", "frame-header/cur-mfh-id-out-of-range", "frame-header/bridge-ref-index-out-of-range", "frame-header/ras-requires-long-term-frame-id-bits", "frame-header/truncated-frame-header", "frame-header/sef-trailing-bits-invalid", "frame-header/ref-frame-idx-invalid-slot"]

[[feature]]
id = "AV2-5.18.3-FRAME-CONFIGURATION"
name = "Frame configuration syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.3"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-3"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/config.rs"
change_id = "frame-header-core-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. screen_content_params() (§5.18.3.3) and intrabc_params() (§5.18.3.4) are parsed on the reached intra path. Under frame-header-inter-reference-paths they also parse on the INTER path (parse_screen_content_params_full now returns force_integer_mv for the inter MV-precision gate, config.rs), and frame_opfl_refine_type() (§5.18.3.2) is parsed on the inter non-TIP-output path (read_frame_opfl_refine_type, inter.rs): with TipFrameMode != TIP_FRAME_AS_OUTPUT it reads opfl_refine_type f(1) (+ conditional opfl_refine_all f(1)) only when enable_opfl_refine == REFINE_AUTO, else no bits. get_relative_dist() (§5.18.3.1) is a reference-state derivation (RefOrderHint / RESTRICTED_OH) that feeds ScoresDistance / FrameDistance / the primary-ref choice — all no-bit derivations the inter path does not need to compute for bit positions, so it stays a TODO derivation (its outputs are consumed only past the convergence point). MODEL EXTENSION (frame-header-writer-size-config, maintainer-approved exception to the writer mission's additive/read-only-parser rule for full byte-exact write): intrabc_params() now surfaces its read-and-discarded allow_global_intrabc / allow_local_intrabc / change_bvp_drl / max_bvp_drl_bits_minus_1 via IntrabcParams + parse_intrabc_params_full (config.rs), stored on FrameHeaderCore.intrabc; screen_content_params() surfaces force_integer_mv on the intra path (FrameHeaderCore.force_integer_mv). WRITER: write_screen_content_params (§5.18.3.3) and write_intrabc_params (§5.18.3.4) in crates/splot-core/src/write/frame_config.rs invert those parsers byte-exactly (every gated/inferred field validated up front, reject-before-write); write=partial (the §5.18.3.2 frame_opfl_refine_type / §5.18.3.1 get_relative_dist inter paths are unwritten)."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/config.rs::tests", "crates/splot-core/src/write/frame_config.rs::tests", "crates/splot-core/src/write/frame_config.rs::proptests", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_explicit_map_parses_through_to_shared_tail"]
commands = ["cargo test -p splot-core frame::config", "cargo test -p splot-core write::frame_config", "cargo test -p splot-core headers::frame::inter"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-5.18.4-FRAME-SIZE"
name = "Frame size syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.4", "6.17.4.1"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-4", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/size.rs"
change_id = "mfh-frame-header-state"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. frame_size() (§5.18.4.1) is modeled for the explicit-override, the direct/default (cur_mfh_id == 0) path, and — under mfh-frame-header-state — the cur_mfh_id > 0 default path: FrameWidth/Height come from mfh_frame_width/height_minus_1[ cur_mfh_id ] (mirror :5767), with the §5.18.2 omitted-size inference to the sequence maxima (mirror :4101) applied when the resolved MFH carried no frame-size payload. A cur_mfh_id > 0 frame whose in-band MFH is unresolvable keeps the size unknown (no guessing). Under frame-header-inter-reference-paths frame_size_with_refs() (§5.18.4.3) and frame_size_with_bridge() (§5.18.4.2) are parsed on the inter path (crates/splot-core/src/headers/frame/inter.rs): frame_size_with_refs() reads found_ref f(1) per ref until a hit and copies RefFrameWidth/Height[ ref_frame_idx[i] ] from the modeled §7.23 buffer (falling through to the override frame_size() explicit f(n) dims on an all-miss); frame_size_with_bridge() reads bridge_frame_width/height_minus_1 f(n) then Min()s with RefFrameWidth/Height[ bridge_frame_ref_idx ]. Where a found_ref hit (or the bridge Min) lands on a slot the model has NOT proven valid, the dims are undecidable and the parser stops honestly (InterStop::PoisonedReferenceState, the found_ref / explicit-dim bits already consumed so the bit position stays correct, size left None) — a poisoned-state COVERAGE stop, never a truncation. The frame-size-vs-sequence-maximum diagnostics are split by predicate: the explicit-override §6.17.4.1 bound on the derived FrameWidth/Height emits frame-header/frame-size-exceeds-sequence-max, and the §6.17.2 bound on the referenced MFH's STORED dims (mfh_frame_width/height_minus_1[ cur_mfh_id ] <= max_frame_width/height_minus_1, mirror :4348-4349) emits the dedicated frame-header/mfh-frame-size-exceeds-sequence-max — evaluated against the resolved record's stored dims REGARDLESS of frame_size_override_flag AND of the frame-header core parse outcome (a truncated/malformed remainder still fires it, since the predicate needs only the resolved record and the sequence maxima), so an in-range override no longer hides an out-of-range MFH and a truncated frame no longer silences a decidable diagnostic. The §6.17.4.1 derived rule's no-double-fire deferral keys on the parsed PATH (frame_size_override_flag == 0 on a resolved cur_mfh_id > 0 frame, where FrameWidth/Height ARE the MFH default dims), NOT on dimension value equality: an override==1 frame that explicitly codes the same out-of-range dims the MFH stores is a separate §6.17.4.1 violation of its own frame_width/height_minus_1 fields, so both rules fire. Omitted-size MFHs (inferred maxima) and unresolvable MFHs stay silent. WRITER (frame-header-writer-size-config): write_frame_size (crates/splot-core/src/write/frame_config.rs) inverts the §5.18.4.1 frame_size() modeled paths — the override f(frame_width_bits)/f(frame_height_bits) and the non-override default (no bits, validated equal to default_dims); reject-before-write rejects a dimension that overflows its f(n) field or a non-override size != default_dims. write=partial — frame_size_with_refs() (§5.18.4.3) and frame_size_with_bridge() (§5.18.4.2) are inter paths, unwritten."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/size.rs::tests", "crates/splot-core/src/write/frame_config.rs::tests", "crates/splot-core/src/write/frame_config.rs::proptests", "crates/splot-core/src/headers/frame/inter.rs::tests::frame_size_with_refs_copies_valid_ref_dims", "crates/splot-core/src/headers/frame/inter.rs::tests::switch_frame_infers_primary_ref_none_and_reads_explicit_map", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_default_dims_parse_through_tile_info", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_omitted_size_infers_sequence_maxima", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_unresolvable_mfh_default_size_stays_unsupported", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_frame_size_exceeds_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_default_frame_size_exceeds_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_stored_frame_size_exceeds_sequence_max_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_stored_frame_size_within_sequence_max_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_omitted_frame_size_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_double_fire_mfh_frame_size_on_default_path", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_stored_frame_size_when_mfh_unresolvable", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_default_frame_size_within_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_default_frame_size_when_mfh_unresolvable", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_stored_frame_size_exceeds_sequence_max_on_truncated_frame", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_within_sequence_max_on_truncated_frame", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_both_frame_size_rules_when_override_repeats_mfh_out_of_range_dims"]
commands = ["cargo test -p splot-core frame::size", "cargo test -p splot-core write::frame_config", "cargo test -p splot-core frame_header_core_mfh", "cargo test -p splot-validate frame_size", "cargo test -p splot-validate mfh_"]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = ["frame-header/frame-size-exceeds-sequence-max", "frame-header/mfh-frame-size-exceeds-sequence-max"]

[[feature]]
id = "AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS"
name = "Frame header info semantics"
category = "normative"
kind = "validator-check"
spec_sections = ["6.17.2", "6.17.4.1", "6.4.6"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "inter-bru-ref-frame-size"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Locally decidable, state-supported frame-header-info checks driven by the core parser: bridge_frame_ref_idx range, frame_to_refresh range, reserved ref_long_term_id, refresh_frame_flags-when-output-deferred, still-picture KEY/immediate constraints, and the INTRA_ONLY all-slot-refresh rule (§6.17.2); frame size vs sequence maximum split by predicate: the §6.17.4.1 explicit-override bound on the derived FrameWidth/Height (frame-header/frame-size-exceeds-sequence-max) and the §6.17.2 bound on the referenced MFH's STORED dims mfh_frame_width/height_minus_1[ cur_mfh_id ] <= max_frame_width/height_minus_1, mirror :4348-4349 (frame-header/mfh-frame-size-exceeds-sequence-max — frame_header_core_checks resolves the cur_mfh_id > 0 record and compares its stored dims against the sequence maxima REGARDLESS of frame_size_override_flag AND of the core parse outcome, so an in-range override does not hide an out-of-range MFH and a truncated/malformed frame-header remainder still fires it; the §6.17.4.1 derived rule's no-double-fire deferral keys on the parsed PATH (frame_size_override_flag == 0 on a resolved cur_mfh_id > 0 frame) NOT on dimension equality, so an override==1 frame coding the same out-of-range dims fires both rules; omitted-size and unresolvable MFHs stay silent); and the RAS-frame long_term_frame_id_bits requirement (§6.4.6). Under frame-header-inter-reference-paths the §6.17.2 INTER explicit-reference-map ref_frame_idx validity check lands (frame-header/ref-frame-idx-invalid-slot): reference_state_checks parses the inter frame's core against its referenced sequence header (reference-state-grounded), and for each parsed core.inter.ref_frame_idx[i] fires ONLY where the §7.23 buffer PROVES RefValid[idx] == 0 (a CLK-invalidated, never-refreshed slot) — exactly the proven-invalid discipline of the show-existing-frame slot check; an Unknown slot (mid-stream join, poisoned buffer) or the implicit reference map (get_ref_frames(), unmodeled) stays silent (no guessing). The parser ALSO flags an out-of-NUM_REF_FRAMES ref_frame_idx (a non-conformant CeilLog2(NumRefFrames) with NumRefFrames > 16, decidable from the active sequence alone, frame_header_core_checks). reference-state-and-random-access adds the §6.17.2 RAS long_term_id_in_use(RefLongTermId[ ref_frame_idx[i] ]) == 1 check (frame-header/ras-ref-long-term-id-not-in-use, mirror :4615-4616): a per-slot RefLongTermId is now tracked in the §7.23 reference-state model (reference_state.rs SlotFacts.long_term_id) from a KEY frame's long_term_id_plus_1 - 1 (§5.18.2 mirror :4231-4239; every other refresh sets -1), and reference_state_checks fires when a RAS frame's proven-valid selected slot's RefLongTermId is not in the frame's own ref_long_term_id list — a proven-valid -1 (non-long-term) slot is never in-use and fires; an Unknown/ProvenInvalid slot drops to silence. For max_mlayer_id == 0, the parser reaches ref_frame_idx when modeled §7.23 state grounds the inferred refresh_frame_flags loop (§5.18.2 mirror :4493-4503); without that state it stops before the reference map and stays honestly silent. A max_mlayer_id != 0 RAS reads refresh_frame_flags explicitly and does not need that derivation. Remaining reference-frame-buffer / long-term-reference availability checks (primary_ref_frame range against NumTotalRefs, NumTotalRefs bounds beyond the f(3) width, the implicit-map slot validity) need the unmodeled get_ref_frames() derivation and stay partial, never guessed. Under inter-ref-frame-scale-ratio the §6.17.2 reference-scaling-ratio constraint lands (frame-header/ref-frame-scale-ratio, mirror :4638-4644): once the inter frame's FrameWidth/FrameHeight is resolved (core.frame_size) and a ref_frame_idx[i] slot is PROVEN valid in the §7.23 buffer (SlotState::Valid, so RefFrameWidth/Height are known), reference_state_checks verifies 2*FrameWidth >= RefFrameWidth, 2*FrameHeight >= RefFrameHeight, FrameWidth <= 16*RefFrameWidth, FrameHeight <= 16*RefFrameHeight (a reference may be upscaled by at most 2x and downscaled by at most 16x per axis), with saturating products so overflow never invents a violation; an Unknown/ProvenInvalid slot has no proven dims and drops to silence (ProvenInvalid is the frame-header/ref-frame-idx-invalid-slot domain), and the implicit reference map records no ref_frame_idx and stays silent. §6.17.4.3 (mirror :5251-5258) restates the same four inequalities over 0..REFS_PER_FRAME-1; the slots beyond NumTotalRefs need the unmodeled implicit map and stay a named residual. Under inter-bru-ref-frame-size the §6.17.2 BRU reference-dimension equality lands (frame-header/bru-ref-frame-size-mismatch, mirror :4594-4595): when use_bru == 1, RefFrameWidth/RefFrameHeight[ref_frame_idx[bru_ref]] must equal FrameWidth/FrameHeight (a BRU frame matches the reference it updates). Decidable from the same modeled state (core.frame_size + the proven-valid bru_ref SlotState::Valid dims); bru_ref is bounds-checked against the recorded ref_frame_idx; Unknown/ProvenInvalid slots drop to silence; distinct from the ≤2x/≤16x ref-frame-scale-ratio bound (a BRU frame must match exactly). Under bru-ref-refresh-flag the §6.17.2 use_bru refresh-mask-bit clause lands (frame-header/bru-ref-refresh-flag-unset, mirror :4596): when use_bru == 1, refresh_frame_flags & (1 << ref_frame_idx[bru_ref]) must be non-zero — a BRU frame refreshes the slot it updates. Decidable from parsed header state alone (core.inter.refresh_frame_flags + bru_ref + ref_frame_idx[bru_ref], no reference-state lookup); bounds-checked bru_ref + shift-guarded slot index. validate stays partial — the remaining §6.17.2 / §6.17.4.3 inter-reference clauses are BLOCKED on unmodeled derivations: the implicit-map (explicit_ref_frame_map==0) slot validity / scaling and NumTotalRefs derivation need §7.7 get_ref_frames(); the use_bru OrderHint>=RefOrderHint[i] / RefOrderHint-RESTRICTED_OH items need OrderHint = get_disp_order_hint() (output-order-dependent, decode track); the BRU RefCounter-uniqueness (:4625-4627) needs a RefCounter[] array; and the ref-slot MLayer/TLayerDependencyMap checks need RefMLayerId/RefTLayerId per slot. BLOCKED: §7.7 get_ref_frames() implicit reference-map derivation + output-order-dependent get_disp_order_hint() (decode track) — these gate the residual inter-reference conformance clauses."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_bridge_ref_index_out_of_range", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_frame_size_exceeds_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_default_frame_size_exceeds_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_stored_frame_size_exceeds_sequence_max_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_stored_frame_size_within_sequence_max_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_omitted_frame_size_on_override", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_double_fire_mfh_frame_size_on_default_path", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_stored_frame_size_when_mfh_unresolvable", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_default_frame_size_within_sequence_max", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_does_not_flag_mfh_default_frame_size_when_mfh_unresolvable", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_mfh_stored_frame_size_exceeds_sequence_max_on_truncated_frame", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_accepts_mfh_within_sequence_max_on_truncated_frame", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_both_frame_size_rules_when_override_repeats_mfh_out_of_range_dims", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_ras_requires_long_term_frame_id_bits", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_frame_to_refresh_out_of_range", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_reserved_ref_long_term_id", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_still_picture_non_key_frame", "crates/splot-validate/src/validator/tests/frame_size_semantics.rs::validator_flags_intra_only_refresh_all_slots", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_proven_invalid_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_valid_slot_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_ref_frame_idx_poisoned_slot_drops_to_silence", "crates/splot-validate/src/validator/tests/reference_state.rs::validator_ras_ref_long_term_fires_when_max_mlayer_zero_derivation_is_grounded", "crates/splot-validate/src/validator/tests/reference_state.rs::validator_silent_on_ras_ref_long_term_when_slot_unknown", "crates/splot-validate/src/reference_state.rs::tests::slot_long_term_id_reads_only_valid_slots", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_unit_ratio_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_upscale_boundary_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_unknown_slot_drops_to_silence", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_width_too_small_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_height_too_small_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_width_too_large_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_height_too_large_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_scale_ratio_skips_proven_invalid_slot", "crates/splot-validate/src/validator/tests/reference_state.rs::bru_ref_frame_size_match_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::bru_ref_frame_size_mismatch_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::bru_ref_frame_size_unknown_slot_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::bru_ref_frame_size_not_checked_without_use_bru", "crates/splot-validate/src/validator/tests/reference_state.rs::frame_header_bru_ref_refresh_flag_unset_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::frame_header_bru_ref_refresh_flag_set_is_silent"]
commands = ["cargo test -p splot-validate frame", "cargo test -p splot-validate validator_flags", "cargo test -p splot-validate mfh_", "cargo test -p splot-validate ref_state_inter", "cargo test -p splot-validate ras_ref_long_term", "cargo test -p splot-validate ref_scale_ratio", "cargo test -p splot-validate bru_ref_frame_size"]
fixtures = []
diagnostics = ["frame-header/bridge-ref-index-out-of-range", "frame-header/frame-size-exceeds-sequence-max", "frame-header/mfh-frame-size-exceeds-sequence-max", "frame-header/ras-requires-long-term-frame-id-bits", "frame-header/frame-to-refresh-out-of-range", "frame-header/ref-long-term-id-reserved", "frame-header/refresh-frame-flags-zero-on-deferred-output", "frame-header/still-picture-requires-key-frame", "frame-header/intra-only-refresh-all-slots", "frame-header/ref-frame-idx-invalid-slot", "frame-header/num-total-refs-out-of-range", "frame-header/primary-ref-frame-out-of-range", "frame-header/bru-ref-out-of-range", "frame-header/bru-without-immediate-output", "frame-header/ras-ref-long-term-id-not-in-use", "frame-header/ref-frame-scale-ratio", "frame-header/bru-ref-frame-size-mismatch", "frame-header/bru-ref-refresh-flag-unset"]

[[feature]]
id = "AV2-5.18.5-FILTERING"
name = "Frame filtering syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.5", "5.18.5.2", "6.17.5.2"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-5-2", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-5-2"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/filtering.rs"
change_id = "frame-filtering-deblocking-gdf-cdef"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. deblocking_filter_params() (§5.18.5.2) is typed and parsed on the intra frame-header path under frame-filtering-deblocking-gdf-cdef: apply_deblocking_filter[0..4] (NumPlanes-gated chroma pair), df_delta_q_present[i], and the derived DfDeltaQ[i] (df_delta_q[i] - (1 << (dfParBits-1)) with the (i==1)?DfDeltaQ[0]:0 / outer-else-0 inferences), where dfParBits = df_par_bits_minus_2 + 2 (§5.4.10). The in-tree sequence parser reads df_par_bits_minus_2 as f(2) (0..=3), but a direct/fuzz caller is not bound by that read; since the DfDeltaQ derivation shifts 1 << (dfParBits-1) and df_delta_q[i] is f(dfParBits), the parser rejects an out-of-range width with a structured Error::BitWidthTooLarge before the shift (dfParBits > 32) rather than panicking in debug (workspace no-reachable-panic rule). The deblocking never-panics proptest is widened to the full u8 df_par_bits_minus_2 range. The cur_mfh_id > 0 arm consults the resolved MultiFrameHeaderRecord's mfh_deblocking_filter_update / mfh_apply_deblocking_filter (§5.7, mirror :5949): when the update bit is set apply is copied from the MFH with no apply bits read; otherwise apply[0]/[1] (and the chroma pair) are read from the frame. CodedLossless short-circuits to all-0 apply (no bits). §6.17.5.2 states no requirement of bitstream conformance on these fields (all bounded by fixed widths), so no diagnostic is added. Under frame-header-inter-reference-paths §5.18.5.1 read_interpolation_filter() now LANDS (read_interpolation_filter, InterpolationFilter, filtering.rs): is_filter_switchable f(1) selects SWITCHABLE, else interpolation_filter f(2) names the fixed filter (EIGHTTAP / EIGHTTAP_SMOOTH / EIGHTTAP_SHARP / BILINEAR); it is parsed on the inter MV-precision block (mirror :4919) and surfaced on core.inter.interpolation_filter. Residual: the inter allow_df_sub_pu / apply_deblocking_filter_tip reads (mirror :4953-4967) live on the TIP_FRAME_AS_OUTPUT arm, which the inter path stops before (InterStop::TipAsOutputReturn) because that arm reads quantization_params() / tile_info() against reference-frame dims this phase does not thread; they land when the TIP-output arm completes. WRITER (frame-header-writer-loop-filters): deblocking_filter_params() (§5.18.5.2) is inverted by write_deblocking_filter_params (crates/splot-core/src/write/frame_filters.rs) — the CodedLossless all-default early return, the MFH-update arm (apply copied with no bits, validated against the derived array) vs the direct arm (apply[0]/[1] + the NumPlanes-gated chroma pair), and the per-index df_delta_q cascade (df_delta_q_present f(1); the f(dfParBits) raw recovered as DfDeltaQ[i] + (1 << (dfParBits-1)); the (i==1)?DfDeltaQ[0]:0 absent inference). ADDITIVE (no model change): the read-but-not-stored df_delta_q raw is a reversible derivation, validated into the f(dfParBits) domain. Reject-before-write throughout; an over-wide dfParBits (>32, a non-conformant §5.4.10 field) is rejected with WriteError::BitWidthTooLarge on the non-lossless path only (the CodedLossless path never codes the width, matching the parser). write stays partial — §5.18.5.1 read_interpolation_filter() is an inter-path element outside the intra writer scope."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/filtering.rs::tests",
    "crates/splot-core/src/headers/frame/filtering.rs::tests::deblocking_oversized_df_par_bits_is_structured_error_not_panic",
    "crates/splot-core/src/headers/frame/filtering.rs::tests::interpolation_filter_switchable_reads_one_bit",
    "crates/splot-core/src/headers/frame/filtering.rs::tests::interpolation_filter_fixed_reads_two_bit_code",
    "crates/splot-core/src/headers/frame/filtering.rs::tests::interpolation_filter_eof_is_structured_error",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_full_structure_cluster",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_deblocking_update_copies_apply_no_apply_bits",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_deblocking_filter_params_preserves_facts",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_gdf_params_preserves_facts",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_cdef_params_preserves_facts",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_explicit_map_reaches_shared_tail",
    "crates/splot-core/src/write/frame_filters.rs::tests::deblocking_coded_lossless_ignores_oversized_df_par_bits",
    "crates/splot-core/src/write/frame_filters.rs::proptests::deblocking_round_trips",
    "crates/splot-core/src/write/frame_filters.rs::proptests::deblocking_writer_never_panics_on_constructed_models",
]
commands = [
    "cargo test -p splot-core headers::frame::filtering",
    "cargo test -p splot-core deblocking",
    "cargo test -p splot-core interpolation",
    "cargo test -p splot-core write::frame_filters",
]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.18.6-QUANTIZATION"
name = "Frame quantization syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.6", "5.18.7.8", "6.17.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/quant.rs"
change_id = "frame-tiling-quant-segmentation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. quantization_params() (§5.18.6.1), setup_qm_params() (§5.18.6.2), read_delta_q() (§5.18.6.3), delta_q_params() (§5.18.7.8), and the §5.18.2 lossless/allow_tcq/allow_parity_hiding tail are typed and parsed, but only on the intra frame-header path (cur_mfh_id == 0; TipFrameMode gating is a caller input, always false on that path). Inter and MFH-gated paths are unparsed, so parse/tests stay partial. The §6.17.6.2 custom-QM plane-count check is live. reference-state-and-random-access adds the §7.3.8.9 quantizer-matrix availability check: frame-header/qm-level-unavailable fires when using_qmatrix == 1 references a custom level with no available record (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-9 :847-858), honoring the QmProtected reset_qm() discipline — a temporal delimiter clears QmProtected (§5.5), a QM OBU sets it for sent levels (§5.13), and reset_qm() clears the availability of unprotected levels at a CLK/OLK (keyFrame && FirstPictureInTU) or a RAS/restricted-prediction SWITCH (§5.18.2 :4106-4108 / :4278-4286; the SWITCH arm consumes the now-recorded restricted_prediction_switch). The RAS/SWITCH reset_qm() call sits at a §5.18.2 parse point (mirror :4283) after restricted_prediction_switch, so it is applied only when the frame core CONFIRMS reaching it (a resolvable RAS core, or a SWITCH core with restricted_prediction_switch == 1); an UNCONFIRMED reset (truncated header, unresolvable core, or a SWITCH whose gate bit was never read) POISONS the unprotected levels' availability (a per-level availability_poisoned bitmap) instead of clearing them — the §7.3.8.9 check DROPS its judgment for a poisoned level (no clear-to-fire false positive, no stale-available false negative; symmetric across the RAS and SWITCH arms), the QM-availability analogue of the §7.23 unconfirmed-effect staging gate. The poison is lifted by a QM OBU re-sending the level or by a later confirmed reset, and persists across temporal delimiters. Fires only under ExternalHlsMode::Disabled (ExternalHlsSet cannot express QM OBUs). Under qm-reset-mlayer-presence-arm the SWITCH/RAS reset_qm() MLayerPresenceMap arm of needsReset is now MODELED (mirror docs/spec/av2/1.0.0/05-syntax-structures.md :5352): reset_qm_availability_for_switch_or_ras takes the frame's obu_mlayer_id and the activated sequence header's §5.4.1 MLayerPresenceMap (AV2-5.4.1-SEQUENCE-HEADER-GENERAL) and clears a level with recorded QmMLayerId == m when MLayerPresenceMap[m][obu_mlayer_id] == 1 (the reflexive m == obu_mlayer_id case resets every same-layer level — the dominant single-layer behavior, previously under-reported), alongside the existing QmMLayerId == -1 arm. An unavailable presence map (unresolved activation) leaves the level available (the zero-FP direction for an availability reset). The §7.3.8.1 random-access-point visibility direction is already covered by the QM RAP replay (AV2-7.3.8-HLS-AVAILABILITY). Under qm-layer-dependency the §6.17.6.2 QM layer-dependency constraints now LAND (mirror :5413-5419): for each referenced custom level with a recorded QmMLayerId >= 0, frame_qm_reference_checks fires frame-header/qm-mlayer-dependency-missing unless MLayerDependencyMap[obu_mlayer_id][QmMLayerId[level]] == 1, and frame-header/qm-tlayer-dependency-missing unless TLayerDependencyMap[obu_mlayer_id][obu_tlayer_id][QmTLayerId[level]] == 1 — joining the recorded QmLevelRecord layer identity against the §5.4.1 maps + the frame's obu_mlayer_id/obu_tlayer_id, the proven frame-header/film-grain-{mlayer,tlayer}-dependency-missing pattern. A level reset to defaults (QmMLayerId == -1) has no defining layer and is exempt. Suppressed under any Provided external-HLS mode (QM OBUs / the activated header's §5.4.1 maps may be external), matching the film-grain dependency check (Codex P2). No §6.17.6.2 QM residual remains. WRITER (frame-header-writer-quantization): all five quant parsers are inverted in crates/splot-core/src/write/frame_quant.rs (write_read_delta_q, write_quantization_params, write_setup_qm_params, write_delta_q_params, write_lossless_info), so write=done for the modeled intra surface. ADDITIVE — no model/parser-error change (only get_qindex_ignore_delta_q + ceil_log2 exposed pub(crate)). Four redundant-encoding CANONICAL FORMS (NOT model extensions — the value is fully preserved, like the §5.4 leb128-minimal / num_ref_frames=8 cases): read_delta_q delta_q==0 → delta_coded=0; setup_qm qm_uv_same_as_y when chroma==qm_y; equal_ac_dc_q discards the coded chroma-DC (re-emits the retained AC value); lossless qm_index recovered by reverse-lookup over the full f(CeilLog2(qmNum)) coded domain. Semantic round-trip universal; byte-exact on the canonical subset. Reject-before-write throughout (incl. the write_lossless_info pic_qm_num_minus_1 f(2) bound). write_lossless_info re-derives LosslessArray/CodedLossless via get_qindex_ignore_delta_q to reject a stored model the parser could not have produced."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "partial"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/write/frame_quant_tests.rs::tests",
    "crates/splot-core/src/write/frame_quant_proptests.rs::proptests",
    "crates/splot-core/src/headers/frame/quant.rs::tests",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_full_structure_cluster",
    "crates/splot-validate/src/validator/tests/mod.rs",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_flags_qm_level_unavailable_when_no_qm_obu",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_silent_on_qm_level_available_via_qm_obu",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_level_unavailable_boundary_default_level_silent",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_level_unavailable_suppressed_under_external_provided",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_clears_unprotected_level_across_temporal_units",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_preserves_resent_level_in_current_temporal_unit",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_confirmed_ras_reset_clears_same_layer_level_via_presence_map",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_confirmed_ras_reset_preserves_cross_layer_level_via_presence_map",
    "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_qm_level_only_before_rap_is_flagged",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_flags_qm_mlayer_dependency_missing",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_flags_qm_tlayer_dependency_missing",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_layer_dependency_satisfied_is_silent",
    "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_layer_dependency_suppressed_under_external_provided",
]
commands = [
    "cargo test -p splot-core headers::frame",
    "cargo test -p splot-core write::frame_quant",
    "cargo test -p splot-validate",
]
fixtures = ["tests/fixtures/frame-header-core.av2"]
diagnostics = ["frame-header/qm-plane-count-mismatch", "frame-header/qm-level-unavailable", "frame-header/qm-mlayer-dependency-missing", "frame-header/qm-tlayer-dependency-missing"]

[[feature]]
id = "AV2-5.18.7-SEGMENTATION-TILING"
name = "Frame segmentation and tiling syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.7", "6.17.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/tiling.rs"
change_id = "frame-loop-restoration-ccso-params"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. tile_info() (§5.18.7.2, MiCols/MiRows per §5.18.4.4) and segmentation_params() (§5.18.7.1, reusing seg_info()) are typed and parsed on the intra frame-header path for both reference forms: cur_mfh_id == 0 (sequence/zero arm) and — under mfh-frame-header-state — cur_mfh_id > 0, where the MFH-gated arm derives haveSegParams = (mfh_ext_seg_flag == enable_ext_seg), allowChange = haveSegParams && mfh_allow_seg_info_change, mfhId = cur_mfh_id, and a reuse_seg_info copy draws from MfhFeatureEnabled/MfhFeatureData (mirror :6266-6298). The MFH branch takes priority over the sequence branch per the spec order; a cur_mfh_id > 0 frame whose in-band MFH is unresolvable still stops (no guessing). Under seq-tile-nonuniform-reuse-starts the §5.18.7.4 non-uniform sequence-reuse branch is fully wired: the persisted SeqSbColStarts / SeqSbRowStarts (AV2-5.4.2-SEQUENCE-TILE-CONFIG) flow into reuse_tile_params() (mirror :6449/:6469-6477), so a frame reusing a non-uniform sequence tile layout parses through tile_info() instead of stopping unimplemented. §6.17.7.2 TileCols/TileRows/context_update_tile_id checks are live. Under frame-filtering-deblocking-gdf-cdef the §5.18.7 filter-param children gdf_params() (§5.18.7.9) and cdef_params() (§5.18.7.10) are parsed on the intra path in crates/splot-core/src/headers/frame/filtering.rs (§6.17.7.5 / §6.17.7.6 state no requirement of bitstream conformance — all fixed-width — so no diagnostic). Under frame-loop-restoration-ccso-params the remaining §5.18.7 filter-param children lr_params() (§5.18.7.11) and ccso_params() (§5.18.7.12) are parsed on the intra path in crates/splot-core/src/headers/frame/restoration.rs. On the intra path NumTotalRefs == 0 (mirror :4573): lr_params()'s temporal-prediction arm (temporal_pred_flag/rst_ref_pic_idx) and ccso_params()'s reuse arm (reuse_ccso/sb_reuse_ccso/ccso_ref_idx/load_ccso_params) are dead, so both parse on plain f/ns/tu reads. lr_params() reads the per-plane tool_index ns(n) -> FrameRestorationType, frame_filters_on f(1) (WIENER_NONSEP/SWITCHABLE arm), num_filter_classes_idx f(3) -> NumFilterClasses (plane 0), and the luma/chroma LoopRestorationSize signaling (lr_*_use_half/max/quarter_size with the BLOCK_256X256/BLOCK_128X128 inferences). When a plane signals frame_filters_on the structure consumes the fixed-coded read_wienerns_filter() frame-level Wiener bank (search_frame_filters/predict_group/decode_signed_subexp_with_ref over the Wiener_Ns_Taps_* tables) and preserves it on LrPlaneParams::frame_filter_bank; all modeled Wiener branches complete or return a typed parse error. ccso_params() reads ccso_frame_flag (single_picture infers 1), then per plane ccso_planes, ccso_bo_only, ccso_scale_idx f(2), the !ccso_bo_only ccso_quant_idx f(2)/ccso_ext_filter f(3)/ccso_edge_clf f(1) (the last gated on CCSO_Quant_Sz[scale][quant] != 0), ccso_max_band_log2 f(2 + ccso_bo_only), and the d0×d1×band offset loop of ccso_offset_idx tu(7). Under ccso-offset-index-model the ccso_offset_idx values (previously read-and-discarded) are now SURFACED in CcsoPlaneParams.ccso_offset_idx — a flat (d0, d1, band)-ordered Vec<u8> of the tu(7) values, length maxEdgeInterval^2 * maxBand, empty when ccso_planes[plane] == 0 — and in the inspect JSON; CcsoPlaneParams drops its Copy derive. This is forward-plumbing for the §5.18.7.12 writer (a maintainer-approved model extension for full byte-exact, like #4b/#4c). §6.17.7.8 yields two locally decidable diagnostics: frame-header/ccso-ext-filter-reserved (ccso_ext_filter != 7, mirror :5819) and frame-header/ccso-max-band-out-of-range (1 << ccso_max_band_log2 <= CCSO_BAND_NUM, mirror :5824). The §6.17.7.7 lr requirements (usesChromaLr 64>>SubsamplingY <= LoopRestorationSize[1]; check_ru_size() tile-boundary divisibility) remain pending. The §6.17.7.8 ccso_ref_idx < NumTotalRefs requirement is emitted by ccso_reference_slot as a typed frame-header conformance error before reference-state lookup; ccso_reference_slot_checks_all_reuse_modes_and_num_total_refs covers the last valid index plus representable invalid indices, ccso_inter_eof_mid_reference_index_is_structured_error covers syntax truncation, and syn-2frame-ccso-reuse-inter-64x64-10bit remains the 1/8-thread byte-exact positive oracle. SavedCcsoPlanes[idx][plane] == 1 is already enforced before reuse by resolve_ccso_reference_reuse and covered by missing_reference_ccso_plane_is_a_malformed_source_diagnostic. The remaining §6.17.7.8 checks for restricted RefOrderHint, RefMiRows/RefMiCols, SavedCcsoLumaSizeLog2, and the current CcsoLumaSizeLog2 constraint remain pending; the lr size/RU bounds are deferred (residual). Remaining residual (keeps parse/tests partial): entropy-coded LR unit Wiener syntax, loop-restoration reconstruction, and the inter frame-header paths. WRITER (frame-header-writer-tiling): tile_info() (§5.18.7.2) is inverted by write_tile_info (crates/splot-core/src/write/frame_tiling.rs) — the reuse_tile_info inference, the reuse branch (no bits, validated against reuse_tile_params() §5.18.7.4), the explicit branch (reusing the shared write_tile_params §5.18.7.3 with a TileParams surfaced on TileInfo via a maintainer-approved model extension, like #4b), and the gated context_update_tile_id / tile_size_bytes tail; byte-exact + reject-before-write. WRITER (frame-header-writer-segmentation): segmentation_params() (§5.18.7.1) is inverted by write_segmentation_params (crates/splot-core/src/write/frame_segmentation.rs) on the intra path — segmentation_enabled f(1), the allowChange-gated reuse_seg_info f(1), the reuse branch (no bits, validated against the §5.18.7.1 reuse source: MfhFeatureData on the MFH arm, SeqFeatureData on the sequence arm, all-disabled when absent), and the fresh seg_info(MaxSegments) body (reusing the shared §5.4.9 write_seg_info, AV2-5.4.9-SEGMENT-INFO); the intra-inferred segmentation_update_map = 1 / segmentation_temporal_update = 0 and the derived SegIdPreSkip / LastActiveSegId are re-derived and rejected on mismatch, never coded; additive (no model change), byte-exact + reject-before-write. WRITER (frame-header-writer-loop-filters): gdf_params() (§5.18.7.9) and cdef_params() (§5.18.7.10) are inverted by write_gdf_params / write_cdef_params (crates/splot-core/src/write/frame_filters.rs) — the CodedLossless / enable-flag disabled returns, the single-picture inferences, the gdf_per_block coded/inferred gate (re-derived via the shared pub(crate) gdf_per_block_is_coded the parser now also calls, so the gdfBlkSize derivation never drifts), the gdf f(2) indices, and the cdef damping/strengths/skip-txfm cascade + per-set Y/UV strengths. ADDITIVE (no model change): the cdef_*_pri_zero zero-flag and the cdef_*_sec_strength 3<->4 remap are canonical forms / reversible derivations (like the §5.18.6 quant canonical forms). Reject-before-write throughout. WRITER (frame-header-writer-restoration-ccso): lr_params() (§5.18.7.11) and ccso_params() (§5.18.7.12) are inverted by write_lr_params / write_ccso_params (crates/splot-core/src/write/frame_restoration.rs) on the intra path. LR is additive with a HARD RESIDUAL: the parser can now carry a fixed-coded frame-level Wiener bank on LrPlaneParams::frame_filter_bank, but the writer does not emit that syntax yet; it rejects models with a bank (what=lr_frame_filter_bank) or frame_filters_on==true (what=lr_frame_filters_on) and ships the frame_filters_on==false surface — the tool_index ns(n) reverse-lookup over the shared pub(crate) indexToTool table (lr_plane_tool_table, the parser now also calls it) validated against tool_index < n, and the LoopRestorationSize size-shift reversal (validated reachable for the frame SbSize). CCSO is byte-exact, reproducing the per-plane ccso_offset_idx tu(7) loop from the values surfaced by ccso-offset-index-model, via the new BitWriter::write_tu primitive. Both reject-before-write (bit_len()==0); every constructed-model shift/subtraction/index guarded (incl. an out-of-range LrGeometry subsampling). write stays partial — the inter frame-header paths and the §5.18.7.11 frame-level Wiener bank remain."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/tiling.rs::tests",
    "crates/splot-core/src/headers/frame/tiling.rs::tests::non_uniform_sequence_reuse_passes_recorded_starts_through",
    "crates/splot-core/src/headers/frame/tiling.rs::tests::non_uniform_sequence_reuse_gate_mismatch_parses_fresh",
    "crates/splot-core/src/headers/frame/tiling.rs::tests::non_uniform_sequence_reuse_eof_in_context_fields",
    "crates/splot-core/src/headers/frame/segmentation.rs::tests",
    "crates/splot-core/src/headers/frame/segmentation.rs::tests::mfh_arm_reuse_inferred_copies_mfh_feature_data_when_change_not_allowed",
    "crates/splot-core/src/headers/frame/segmentation.rs::tests::mfh_arm_ext_seg_mismatch_yields_no_seg_params_and_parses_fresh",
    "crates/splot-core/src/headers/frame/segmentation.rs::tests::mfh_arm_takes_priority_over_sequence_branch",
    "crates/splot-core/src/headers/frame/filtering.rs::tests",
    "crates/splot-core/src/headers/frame/restoration.rs::tests",
    "crates/splot-core/src/headers/frame/restoration.rs::tests::ccso_offset_idx_values_surface_in_iteration_order",
    "crates/splot-core/src/headers/frame/restoration.rs::proptests",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_full_structure_cluster",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_eof_inside_ccso_params_preserves_facts",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_segmentation_arm_reuses_mfh_feature_data",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_unresolvable_mfh_with_explicit_size_stops_before_segmentation",
    "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_ccso_ext_filter_reserved",
    "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_accepts_ccso_ext_filter_within_range",
    "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_ccso_max_band_out_of_range",
    "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_accepts_ccso_max_band_within_range",
    "crates/splot-validate/src/validator/tests/mod.rs",
    "crates/splot-core/src/write/frame_tiling_tests.rs::tests",
    "crates/splot-core/src/write/frame_tiling_proptests.rs::proptests",
    "crates/splot-core/src/write/frame_segmentation_tests.rs::tests",
    "crates/splot-core/src/write/frame_segmentation_proptests.rs::proptests",
    "crates/splot-core/src/write/frame_filters.rs::proptests::gdf_round_trips",
    "crates/splot-core/src/write/frame_filters.rs::proptests::cdef_round_trips",
    "crates/splot-core/src/write/frame_restoration_proptests.rs::proptests",
    "crates/splot-core/src/write/frame_restoration.rs::tests::lr_switchable_when_not_allowed_is_rejected",
    "crates/splot-core/src/write/frame_restoration.rs::tests::lr_out_of_range_subsampling_geometry_is_rejected",
]
commands = [
    "cargo test -p splot-core headers::frame",
    "cargo test -p splot-core write::frame_tiling",
    "cargo test -p splot-core write::frame_segmentation",
    "cargo test -p splot-core write::frame_filters",
    "cargo test -p splot-core write::frame_restoration",
    "cargo test -p splot-validate",
]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = [
    "frame-header/tile-cols-out-of-range",
    "frame-header/tile-rows-out-of-range",
    "frame-header/context-update-tile-id-out-of-range",
    "frame-header/ccso-ext-filter-reserved",
    "frame-header/ccso-max-band-out-of-range",
]

[[feature]]
id = "AV2-5.18.7.3-TILE-PARAMS"
name = "Tile params helper and tile-partitioning tables"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.7.3", "5.18.7.5", "5.18.7.7", "6.17.7"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/tile.rs"
change_id = "segmentation-tile-params-foundation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18.7-SEGMENTATION-TILING. Reusable tile_params() helper (tile_log2, uniform_spacing, the §9.3 block-size conversion tables, and the level/tier Tile_Width_Scaling_Factor / Tile_Area_Scaling_Factor tables). Wired into the sequence tile config (AV2-5.4.2-SEQUENCE-TILE-CONFIG) and, under frame-header-writer-tiling, the frame tile_info() path. WRITER: the shared §5.18.7.3 inverse write_tile_params (crates/splot-core/src/write/seq_tile.rs, now pub(crate)) is reused by BOTH the sequence tile-config writer and the frame tile_info() writer (write_tile_info, crates/splot-core/src/write/frame_tiling.rs) — its explicit branch re-runs the §5.18.7.3 derivation forward (uniform increment runs / non-uniform ns() loops) and the reuse branch re-derives via reuse_tile_params() (§5.18.7.4) with no bits; both byte-exact + reject-before-write, so write=done. Reserved seq_level_idx values have no defined layout and are rejected by the writer (the parser's bounded residual)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/tile.rs::tests", "crates/splot-core/src/tile.rs::proptests", "crates/splot-core/src/write/seq_tile_tests.rs::tests", "crates/splot-core/src/write/frame_tiling_tests.rs::tests", "crates/splot-core/src/write/frame_tiling_proptests.rs::proptests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-core tile", "cargo test -p splot-core write::seq_tile", "cargo test -p splot-core write::frame_tiling", "cargo test -p splot-validate sequence_header"]
fixtures = ["tests/fixtures/seq-header-tile-params.av2"]
diagnostics = ["tile-params/tile-cols-out-of-range", "tile-params/tile-rows-out-of-range", "tile-params/nonuniform-cols-do-not-cover-frame", "tile-params/nonuniform-rows-do-not-cover-frame"]

[[feature]]
id = "AV2-5.18.8-TRANSFORM-CODING-MODES"
name = "Frame transform and coding mode syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.8", "6.17"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-8"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/tail.rs"
change_id = "frame-header-intra-tail-completion"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. The §5.18.2 intra tail parses the §5.18.8 coding-mode structures via crates/splot-core/src/headers/frame/tail.rs (parse_intra_tail): read_tx_mode() (§5.18.8.1, mirror :7634) reads tx_mode_select f(1) gated on the already-derived CodedLossless (TxMode = ONLY_4X4 when CodedLossless == 1, no bit); frame_reference_mode() (§5.18.8.3, mirror :7739) infers reference_select = 0 on the intra path (no bit); skip_mode_params() (§5.18.8.2, mirror :7671) infers skipModeAllowed = 0 / skip_mode_present = 0 on the intra path (FrameIsIntra, no bit). The inter-path arms (tx_mode_select always read, reference_select f(1), skip_mode_present gating with the get_relative_dist/OrderHints derivation) need reference-frame state and remain TODO (blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path parsing). WRITER (frame-header-writer-intra-tail): read_tx_mode() (§5.18.8.1) is inverted by write_tx_mode (crates/splot-core/src/write/frame_tail.rs) — ONLY_4X4 inferred (no bit) on the lossless path, else tx_mode_select f(1); the no-bit reference_select / skip_mode_present intra inferences are re-derived and rejected on mismatch in write_intra_tail. Additive, reject-before-write. write STAYS partial in lockstep with parse: the §5.18.8.1 intra surface (tx_mode + the no-bit inferences) is fully inverted, but the inter-path coded arms (the always-read tx_mode_select, reference_select f(1), and the skip_mode_present gating) are neither parsed nor written yet, so the whole §5.18.8 writer is not complete."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/tail.rs::tests", "crates/splot-core/src/headers/frame/tail.rs::tests::read_tx_mode_lossless_reads_no_bits", "crates/splot-core/src/headers/frame/tail.rs::tests::read_tx_mode_select_and_largest", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_parses_lr_ccso_and_tail_to_completion", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_mfh_omitted_size_infers_sequence_maxima", "crates/splot-core/src/write/frame_tail.rs::tests::tx_mode_largest_and_select_round_trip", "crates/splot-core/src/write/frame_tail.rs::proptests::tx_mode_round_trips"]
commands = ["cargo test -p splot-core frame::tail", "cargo test -p splot-core frame_header", "cargo test -p splot-core write::frame_tail"]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2"]
diagnostics = []

[[feature]]
id = "AV2-5.18.9-GLOBAL-MOTION"
name = "Frame global motion syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.9", "6.17"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-9"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/global_motion.rs"
change_id = "frame-global-motion-params"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. The intra/disabled no-bit return and the complete inter §5.18.9.1-.6 syntax are production-wired. GlobalMotionReferenceState supplies current/ref order hints, RefNumTotalRefs, SavedOrderHints, and SavedGmParams retained by the §7.23 reference buffer; parse_global_motion_params implements our_ref/their_ref base-model selection, distance scaling, IDENTITY/ROTZOOM/AFFINE signalling, and all signed subexp parameters. The §6.17.9.1 restricted-order-hint requirements are typed parse errors, malformed counts/slots fail without panics, and state-free inspector callers retain an explicit GlobalMotionStop instead of guessing. Three minimal AVMenc fixtures select GLOBALMV with translation-only ROTZOOM, genuine ROTZOOM, and AFFINE models; focused tests assert the exact parsed parameters and Splot matches isolated/current avmdec byte-for-byte. The decoder runtime is tracked by DECODE-INTER-GLOBAL-MOTION. The writer remains partial because only the intra no-bit arm is inverted; inter global-motion encoding is out of scope."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "partial"
encode = "todo"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/tail.rs::tests::parse_intra_tail_full_lossy_with_grain", "crates/splot-core/src/headers/frame/global_motion.rs::tests", "crates/splot-decode/src/reference/buffer_tests.rs::reference_refresh_preserves_global_motion_predictor_state", "crates/splot-decode/src/prediction/inter/tests.rs::global_motion_fixtures_match_reference_hashes", "crates/splot-core/src/write/frame_tail.rs::tests::intra_tail_inference_true_is_rejected"]
commands = ["cargo test -p splot-core global_motion --locked", "cargo test -p splot-decode global_motion --locked", "cargo xtask conformance", "cargo xtask check-reference-evidence"]
fixtures = ["tests/fixtures/frame-header-core.av2", "tests/conformance/vectors/valid/syn-2frame-global-translation-64x64-q80.ivf", "tests/conformance/vectors/valid/syn-2frame-global-rotzoom-64x64-q80.ivf", "tests/conformance/vectors/valid/syn-2frame-global-affine-64x64-q80.ivf"]
diagnostics = []

[[feature]]
id = "AV2-5.18.10-FILM-GRAIN-STRUCTURES"
name = "Frame film grain structures syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.18.10", "6.17.10.1"]
sources = ["docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-10", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-10-1"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/tail.rs"
change_id = "film-grain-layer-dependency-constraints"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Child row of AV2-5.18-FRAME-HEADER. film_grain_config() (§5.18.10.1, mirror :8163) is parsed in crates/splot-core/src/headers/frame/tail.rs (parse_film_grain_config) on both the intra tail and the show-existing-frame path: the apply_grain derivation (§5.18.10.1 :8165 — inferred 0 when !film_grain_params_present or neither output flag set; inferred 1 for a single-picture header; else f(1)), then fgm_id f(3) and grain_seed f(16) when apply_grain. The load_grain_model( fgm_id ) call (mirror :8183) reads NO bits — per §6.17.10.1 it is a memory-load reference to a film-grain model slot previously decoded by a film_grain_obu() (§5.14), so no in-band film_grain_model() (§5.18.10.2) syntax is present here and the §5.14 FilmGrainModel parser (AV2-5.14-FILM-GRAIN, crates/splot-core/src/headers/film_grain.rs) is intentionally not invoked from the frame-header path. The §5.18.10.2 in-band film_grain_model() is parsed only in its §5.14 OBU context (that row). The §6.17.10.1 FilmGrainPresent[ fgm_id ] == 1 availability requirement (and §7.3.8.8) is now checked (codex round): when apply_grain == 1, frame_film_grain_reference_checks (crates/splot-validate/src/context/film_grain.rs) fires frame-header/film-grain-model-unavailable if no film grain OBU has set FilmGrainPresent[ fgm_id ] for the referenced slot, reading the existing monotonic per-slot FilmGrainState.available[] recorded by observe_film_grain. The §6.17.10.1 layer-dependency / chroma constraints (residual (b) below) are now ALSO checked when an in-band model is recorded (FgmSlotRecord, which stores the defining film_grain_obu()'s FgmMLayerId/FgmTLayerId as EmbeddedLayerId/TemporalLayerId and FgmChromaIdc): MLayerDependencyMap[obu_mlayer_id][FgmMLayerId[fgm_id]] == 1 (frame-header/film-grain-mlayer-dependency-missing), TLayerDependencyMap[obu_mlayer_id][obu_tlayer_id][FgmTLayerId[fgm_id]] == 1 (frame-header/film-grain-tlayer-dependency-missing), and FgmChromaIdc[fgm_id] == chroma_format_idc (frame-header/film-grain-chroma-idc-mismatch) — evaluated against the active sequence header's §5.4.1 maps and chroma_format_idc (§6.8 makes lcr_chroma_format_idc equal to that single sequence value, so the comparison is layer-independent), mirroring the §7.3.8.7 frame-header/mfh-{m,t}layer-dependency-missing check. SCOPE (zero-false-positive discipline): all of these fire ONLY under ExternalHlsMode::Disabled — ExternalHlsSet cannot express film grain OBUs, so under any Provided mode the model (and its stored layer identity / chroma idc) MAY be supplied externally (the inexpressible-kind blanket-suppression policy); the layer-dependency constraints are checked only when an in-band model is recorded (an absent model is the availability case). RESIDUALS: (a) CLOSED by rap-replay-film-grain-references: the §7.3.8.1 random-access-point-unavailability direction (a model available only before a RAP, not resent, shows present in the monotonic available[] but is unavailable from that decode start) is now covered by the RapReplayTracker film-grain key (RapHlsKey::FilmGrain { slot }, recorded by record_film_grain; the linear frame-header/film-grain-model-unavailable still owns the never-sent case, disjoint from the replay). Tracked on AV2-7.3.8-HLS-AVAILABILITY. (b) CLOSED by film-grain-layer-dependency-constraints: the §6.17.10.1 layer-dependency / chroma constraints (TLayerDependencyMap[..][FgmTLayerId]/MLayerDependencyMap[..][FgmMLayerId]/FgmChromaIdc) are implemented per the above. Proof: cargo test -p splot-validate frame_film_grain (8 tests in layer_dependency_core.rs, covering both the SEF and CLK/intra-tail film_grain_config paths). The chroma-mismatch check is gated on a conformant stored fgm_chroma_idc (<= 3) so an out-of-range value already flagged by the §6.13 film-grain/chroma-idc-out-of-range check is not double-reported. WRITER (frame-header-writer-intra-tail): film_grain_config() (§5.18.10.1) is inverted by write_film_grain_config (crates/splot-core/src/write/frame_tail.rs) on both the intra tail and the SEF path — the three-way apply_grain gate (forced 0 when !film_grain_params_present or not output; inferred 1 for a single-picture header; else coded f(1)), then fgm_id f(3) and grain_seed f(16) when set (load_grain_model reads no bits). Additive (no model change), reject-before-write; write=done for the modeled surface."

[feature.status]
mapped = "done"
types = "partial"
parse = "done"
validate = "partial"
write = "done"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/tail.rs::tests", "crates/splot-core/src/headers/frame/tail.rs::tests::film_grain_config_single_picture_infers_apply_grain_one_then_reads_id_and_seed", "crates/splot-core/src/headers/frame/tail.rs::tests::film_grain_config_reads_apply_grain_bit_then_fields", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_intra_tail_with_grain_present_reads_id_and_seed", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_with_grain_reads_apply_grain_then_completes", "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_sef_eof_inside_film_grain_preserves_facts", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_flags_film_grain_model_unavailable", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_film_grain_model_available_in_band_is_silent", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_film_grain_model_unavailable_fires_when_fgm_obu_follows_frame", "crates/splot-validate/src/validator/tests/frame_header_core.rs::validator_film_grain_model_unavailable_suppressed_under_external_hls", "crates/splot-core/src/write/frame_tail.rs::proptests::film_grain_round_trips", "crates/splot-core/src/write/frame_tail.rs::tests::film_grain_single_picture_infers_apply_round_trips"]
commands = ["cargo test -p splot-core frame::tail", "cargo test -p splot-core frame_header", "cargo test -p splot-validate film_grain_model", "cargo test -p splot-core write::frame_tail"]
fixtures = ["tests/fixtures/frame-header-core.av2"]
diagnostics = ["frame-header/film-grain-model-unavailable"]

[[feature]]
id = "AV2-5.19-TILE-GROUP"
name = "Tile group OBU syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.19", "5.20", "6.18", "6.19"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/tile_group.rs"
change_id = "tile-group-structure-completion"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Prefix-only §5.19 parser for is_first_tile_group, frame_header_present_flag, and the optional frame-header prefix (parsed only for the first tile group). Under frame-header-copy-bit-accounting the NON-FIRST tile group's frame_header(isFirst==0) == frame_header_copy() region (§5.18.1, called at line 8449 when is_first_tile_group==0 and frame_header_present_flag==1) is PARSED where decidable: parse_frame_header_copy (crates/splot-core/src/headers/tile_group.rs) consumes exactly NumFrameHeaderBits header_bit f(1) reads and compares them bit-for-bit against the recorded first header (RecordedFrameHeaderBits), so the record-but-skip is replaced by the real parse when the first header completed (validate side: crates/splot-validate/src/context/mod.rs check_frame_header_copy → frame-header/copy-bits-mismatch §6.17.1, frame-header/copy-bits-truncated §6.2.1). Under tile-group-structure-completion the §5.19 STRUCTURE after frame_header() is now PARSED on the intra-complete first-tile-group path: parse_tile_group_structure (crates/splot-core/src/headers/tile_group.rs) reads tile_start_and_end_present_flag (NumTiles>1 gate, mirror :8467-8473), the tg_start/tg_end f(tileBits) reads or their 0 .. NumTiles-1 inference (mirror :8475-8493; tileBits = TileColsLog2+TileRowsLog2, capped at 32), the closing byte_alignment() (mirror :8519, §6.2.4 zero_bit validated via BitReader::byte_align_zero), and the headerBytes/payload boundary (mirror :8521-8527; payload bytes stay unparsed — §5.20). TileGroupLayout carries NumTiles/TileColsLog2/TileRowsLog2 from the FIRST tile group's parsed tile_info() (the validator/inspector hold the paired first header's core; non-first groups reuse that layout via the §5.18.1 frame_header_copy() identity). The BRU arms are DECIDABLE on intra: §5.18.2 sets use_bru=0/bru_inactive=0 as defaults (mirror :4127-4129) and only reassigns them inside the FrameType==INTER_FRAME gate (mirror :4653), which an intra frame never enters — so the bru_inactive early-return trailing_bits() arm (mirror :8453-8463) and the use_bru BruTileActives loop (mirror :8495-8517) are DEAD on intra (the TileGroupStructure parser asserts the precondition; a caller that cannot establish it records BruUndecidable::NotIntraComplete). Validate side (crates/splot-validate/src/context/mod.rs, shared helper tile_group_structure_checks): the locally-decidable §6.18 tg-range diagnostics run on EVERY tile group of an intra-complete coded frame — the FIRST (via tile_group_range_checks, layout from its parsed tile_info()) AND each CONTINUATION (via check_frame_header_copy, layout from the recorded first header's FrameHeaderCopyRecord; the structure starts after the optional frame_header_copy() region, both the frame_header_present_flag == 1 and == 0 arms handled, at the exact bit 2 + NumFrameHeaderBits, so it runs even after a copy mismatch). The diagnostics: tile-group/first-tg-start-not-zero (§6.18 :6215-6216, TileNum==0 — FIRST-group-only, since a continuation's tg_start is previous tg_end + 1), tile-group/tg-end-before-tg-start (§6.18 :6220), tile-group/tg-end-out-of-range (§6.18 :6218-6223), tile-group/truncated-structure (§6.2.1), tile-group/byte-alignment-zero-bit (§6.2.4). inspect surfaces the tile_group_structure view (num_tiles, tg_start/tg_end, status, header_bytes, payload_size). Residuals: (1) the §5.20 tile_group_payload() body: under tile-payload-boundary-validation the §5.20.1 per-tile FRAMING SLICE now CONSUMES this handoff for both first and continuation tile groups — the structure's headerBytes/payload_size boundary plus tg_start/tg_end and the recorded TileSizeBytes feed AV2-5.20-TILE-GROUP-PAYLOAD's parse_tile_group_framing, which frames each tile (tile_size_minus_1 le(TileSizeBytes), lastTile/IsBridge arms) and flags the provable framing defects (tile-payload/size-field-truncated, tile-payload/tile-size-overflows-payload); the deeper decode_tile() / §5.20.2-.10 block syntax stays owned by AV2-5.20-TILE-GROUP-PAYLOAD's child rows; (2) the INTER-path BRU arms (bru_inactive trailing_bits() early-return :8453-8463 and the use_bru BruTileActives f(1) loop :8495-8517) are reachable only once frame-header inter-reference-path parsing derives use_bru/bru_inactive (AV2-5.18.2-FRAME-HEADER-INFO inter path); (3) the cross-tile-group continuity (tg_start == previous tg_end + 1) and last-group tg_end == NumTiles-1 §6.18 clauses need prior-tile-group state threaded through the segmenter (only the FIRST tile group's TileNum==0 instance is decided now). Under obu-dispatch-frame-payloads the tile-group family is reached by the stateless dispatcher's PayloadStatus::PrefixParsed arm (crates/splot-core/src/obu.rs): dispatch parses the §5.19 prefix via parse_tile_group_prefix and reports blocked_on='active sequence header state' for the state-dependent remainder owned here, instead of a blanket Unimplemented. This row stays partial until the §5.20 payload body and the inter BRU arms land. WRITER (tile-group-structure-writer): write_tile_group_structure in crates/splot-core/src/write/tile_group.rs inverts the §5.19 tile_group_obu() STRUCTURE on the intra path — tile_start_and_end_present_flag f(1) (NumTiles>1 gate), tg_start/tg_end f(tileBits) (NumTiles>1 && flag; tileBits = TileColsLog2+TileRowsLog2 capped at 32), and the closing byte_alignment() zero pad; the outcome/header_bytes/payload_size parse-context artifacts are NOT emitted (owned by the composer/OBU writer, a following slice). Reject-before-write via check_tile_group_structure_encodable (incomplete_structure / degenerate_layout / flag_without_multi_tile / inferred_range_mismatch / tg_range [inverted or f(tileBits) overflow] / tg_out_of_range [tg_end >= NumTiles, a §6.18 conformance refusal]; additive WriteError::NonCanonicalTileGroup, panic-free on any constructed model). Round-trip (incl. a deterministic tileBits==32 cap-boundary test) + reject + never-panic tested. write flips todo->partial. COMPOSER (tile-group-obu-composer): write_tile_group_obu (same module) sequences the FIRST-tile-group tile_group_obu() payload — is_first_tile_group f(1)=1, the embedded frame_header() via write_frame_header_core (intra path), then write_tile_group_structure and write_tile_group_payload — into a scratch BitWriter committed only on full success (reject-before-write for the whole OBU payload: a sub-writer reject leaves the caller's writer at bit_len()==0, proven by frame-header/structure/payload reject-propagation tests). A non-first frame_header_copy() continuation is rejected (continuation_unsupported). Round-trip is a stage-by-stage reparse (prefix -> frame-header core -> structure -> framing) of a real CLK-fixture core. CONTINUATION COMPOSER (tile-group-continuation-writer): write_tile_group_continuation_obu (same module) sequences a NON-FIRST tile_group_obu() payload — is_first_tile_group f(1)=0, frame_header_present_flag f(1), then (when set) the recorded first header's NumFrameHeaderBits frame_header_copy() bits VERBATIM (from RecordedFrameHeaderBits, exposed via a new pub bit() accessor — additive, no parse change; the continuation is a §5.18.1 bit-copy, NOT a re-serialized core), then write_tile_group_structure (no tg_start==0 restriction — a continuation's tg_start is the running tile offset) and write_tile_group_payload. Scratch writer, reject-before-write (frame_header_copy_gate for a flag-vs-recorded mismatch; framing_range_mismatch for framing.tiles.len() != tg_end-tg_start+1; plus the byte-align guard and the delegated sub-writer rejects). Round-trip reparses prefix -> recorded copy bits -> structure -> framing for a tg_start>0 continuation (the case the first-group composer forbids) with frame_header_present_flag true and false. Remaining: the §5.20 payload framing decode_tile() body, the INTER first-group composer (blocked on the inter frame-header writer), and the inter/BRU paths."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/tile_group.rs::tests::tile_group_prefix_reads_first_tile_group_and_frame_header", "crates/splot-core/src/headers/tile_group.rs::tests::recorded_frame_header_bits_round_trips_through_copy", "crates/splot-core/src/headers/tile_group.rs::tests::frame_header_copy_reports_first_mismatch_bit", "crates/splot-core/src/headers/tile_group.rs::tests::frame_header_copy_reports_truncation_when_payload_short", "crates/splot-core/src/headers/tile_group.rs::tests::structure_single_tile_infers_range_and_payload_boundary", "crates/splot-core/src/headers/tile_group.rs::tests::structure_multi_tile_reads_flag_and_explicit_range", "crates/splot-core/src/headers/tile_group.rs::tests::structure_multi_tile_flag_zero_infers_full_range", "crates/splot-core/src/headers/tile_group.rs::tests::structure_eof_inside_range_is_truncation_preserving_facts", "crates/splot-core/src/headers/tile_group.rs::tests::structure_eof_before_flag_is_truncation", "crates/splot-core/src/headers/tile_group.rs::tests::structure_nonzero_byte_alignment_pad_is_invalid", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_tile_group_range_silent_on_conforming_multitile", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_first_tile_group_tg_start_not_zero", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tg_end_before_tg_start", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tg_end_out_of_range", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tile_group_structure_truncation", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tile_group_byte_alignment_nonzero_pad", "crates/splot-core/src/write/tile_group.rs::tests::round_trips_multi_tile_flag_set_explicit_range", "crates/splot-core/src/write/tile_group.rs::obu_tests::whole_obu_round_trips_stage_by_stage", "crates/splot-core/src/write/tile_group.rs::obu_tests::reject_framing_range_mismatch", "crates/splot-core/src/write/tile_group.rs::obu_tests::frame_header_sub_writer_reject_propagates", "crates/splot-core/src/write/tile_group.rs::continuation_tests::non_first_tile_group_round_trips", "crates/splot-core/src/write/tile_group.rs::continuation_tests::rejects_framing_range_mismatch", "crates/splot-cli/tests/cli.rs::inspect_json_exposes_frame_header_core"]
commands = ["cargo test -p splot-core tile_group", "cargo test -p splot-validate tile_group", "cargo test -p splot-cli inspect_json_exposes_frame_header_core"]
fixtures = ["tests/fixtures/frame-header-core.av2"]
diagnostics = ["frame-header/copy-bits-mismatch", "frame-header/copy-bits-truncated", "tile-group/first-tg-start-not-zero", "tile-group/tg-end-before-tg-start", "tile-group/tg-end-out-of-range", "tile-group/truncated-structure", "tile-group/byte-alignment-zero-bit"]

[[feature]]
id = "AV2-5.20-TILE-GROUP-PAYLOAD"
name = "Tile group payload syntax"
category = "normative"
kind = "bitstream-syntax"
spec_sections = ["5.20", "6.19"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/tile_group.rs"
change_id = "tile-payload-boundary-validation"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "Deep payload syntax and arithmetic boundary validation. Under tile-payload-boundary-validation the §5.20.1 per-tile FRAMING SLICE has landed: parse_tile_group_framing (crates/splot-core/src/headers/tile_group.rs) walks the tile_group_payload() loop verbatim (mirror :8553-8640) over the §5.19 structure's COMPLETE payload_size region — each non-last, non-bridge tile reads tile_size_minus_1 le(TileSizeBytes) (§4.11.5: exactly TileSizeBytes bytes, little-endian), sets tileSize = +1 (mirror :8569), and bookkeeps sz -= tileSize + TileSizeBytes (mirror :8571); the last tile takes the remaining sz (mirror :8557); a bridge frame's tiles read no size field (the else-if(!IsBridge) arm is skipped, mirror :8559). It records per-tile framing (size-field offset, tile-data offset, tileSize) and stops at the first provable defect. TileSizeBytes is the parsed tile_info()'s tile_size_bytes_minus_1 + 1 (§6.17.7.3, 1..=4). Validate side (crates/splot-validate/src/context/mod.rs tile_group_framing_checks via the shared tile_group_structure_checks, gated on a COMPLETE §5.19 structure with a self-consistent tg-range): three provable framing defects — tile-payload/size-field-truncated (§5.20.1 / §4.11.5 / §6.2.1: the le(TileSizeBytes) field runs past the region), tile-payload/tile-size-overflows-payload (§5.20.1 mirror :8571: tileSize + TileSizeBytes exceeds the remaining sz), and tile-payload/zero-size-tile (§8.2.2/§8.2.4: a zero-size non-bridge tile cannot satisfy SymbolMaxBits >= -14 at exit_symbol()), each anchored at the offending tile's size-field byte offset within the bitstream. The framing now runs on EVERY tile-group OBU of a paired intra-complete coded frame, NOT only the first: the FIRST tile group (layout from its parsed tile_info(), via tile_group_range_checks) AND each CONTINUATION (is_first_tile_group == 0, via check_frame_header_copy) — the §5.18.1 layout (NumTiles / tileBits / TileSizeBytes) for a continuation comes from the recorded first tile group's header (FrameHeaderCopyRecord), since a continuation reads frame_header_copy() not a fresh tile_info(). A continuation's structure starts after the optional frame_header_copy() region (both the frame_header_present_flag == 1 copy arm and the frame_header_present_flag == 0 no-copy arm are handled); the structure-start bit is exact (2 prefix bits + NumFrameHeaderBits) whether or not the copy matched, so the framing still runs after a frame-header/copy-bits-mismatch (a copy TRUNCATION leaves no structure bits, so it does not). A continuation with no recorded first-header layout (Unknown routing) leaves the structure unparsed. inspect surfaces the per-tile framing (tile_framing array: tile_num / size_field_offset / tile_data_offset / tile_size, plus tile_framing_defect) on the completed FIRST tile group only — a continuation's view needs the validator's pairing state (the layer-triple key + the segmenter's FrameBoundary decisions incl. the Ambiguous poison), so the per-OBU inspector omits it rather than mis-pair interleaved layers (named residual: a stateful inspect surface); a continuation's framing IS validated via the recorded first-header layout. §8.2 checkability decision (grounded): a zero-size non-bridge tile IS framing-provable — init_symbol(0) (§8.2.2, mirror 08:87) starts SymbolMaxBits at 8*0-15 = -15, the counter only decreases (08:327), and §8.2.4 requires >= -14 at exit_symbol() (08:342), unsatisfiable regardless of tile content — emitted as tile-payload/zero-size-tile (bridge tiles exempt: they read no size field and take the region differently). The exit_symbol() conformance for NONZERO tiles (the exact exit value, the trailing one-bit at trailingBitPosition, §8.2.4) depends on the symbol decoder's consumption during decode_tile() and stays a named residual. CHILD TERRITORY (named residuals, split further): decode_tile() and the §5.20.2-.10 block syntax / §8 entropy decode / §9 table consumers / pixel reconstruction; the bridge and use_bru/BruTileActives size-skipping arms (mirror :8559 / :8585) are dead on the intra-complete tile-group path (IsBridge == 0, use_bru == 0) and inter-gated (owned by frame-header-inter-reference-paths). The AV2-5.19-TILE-GROUP handoff (headerBytes + payload_size + tg_start/tg_end + the recorded boundary) feeds this framing slice. WRITER (tile-group-payload-writer): write_tile_group_payload in crates/splot-core/src/write/tile_group.rs inverts the §5.20.1 tile_group_payload() per-tile framing on the intra/non-bridge path — each non-last tile writes tile_size_minus_1 le(TileSizeBytes) then its coded-tile bytes; the last tile writes its bytes only. The coded-tile bytes are an opaque per-tile PASSTHROUGH (tile_data: &[&[u8]]; the parser models only offsets/sizes — tile_num/size_field_offset/tile_data_offset are parse-context the writer ignores and a reparse recomputes, so the round-trip is semantic on tile_size + byte-exact on the passthrough region). Reject-before-write (reusing WriteError::NonCanonicalTileGroup / WriterNotByteAligned): framing_defect / bridge_unframeable / empty_framing / tile_data_count / tile_size_bytes_domain [1..=4] / zero_size_tile / tile_data_len / tile_size_field_overflow, plus a non-byte-aligned writer; panic-free on any constructed model. Round-trip + reject + parse-context + never-panic tested (parser-driven). write flips todo->partial — decode_tile() / §5.20.2-.10 block syntax and the bridge/BRU arms remain unwritten."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "partial"
write = "partial"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/tile_group.rs::tests::framing_single_tile_takes_whole_region_no_size_field", "crates/splot-core/src/headers/tile_group.rs::tests::framing_multi_tile_records_sizes_and_offsets", "crates/splot-core/src/headers/tile_group.rs::tests::framing_bridge_tiles_read_no_size_field", "crates/splot-core/src/headers/tile_group.rs::tests::framing_flags_size_field_truncated", "crates/splot-core/src/headers/tile_group.rs::tests::framing_flags_tile_size_overflows_payload", "crates/splot-core/src/headers/tile_group.rs::tests::framing_exact_fit_two_tiles_is_conformant", "crates/splot-core/src/headers/tile_group.rs::tests::framing_zero_size_last_tile_is_a_defect", "crates/splot-core/src/headers/tile_group.rs::tests::framing_zero_size_last_bridge_tile_is_exempt", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_zero_size_single_tile", "crates/splot-core/src/headers/tile_group.rs::proptests::parse_tile_group_framing_never_panics", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_silent_on_conformant_tile_payload_framing", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tile_payload_size_field_truncated", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_tile_payload_tile_size_overflows", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_tile_payload_anchors_at_offending_tile_offset", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_silent_on_single_tile_no_size_field", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_continuation_tile_group_size_field_truncated", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_continuation_tile_group_tile_size_overflows", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_silent_on_conformant_continuation_tile_group_framing", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_flags_continuation_tile_group_framing_without_header_copy", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_continuation_tile_group_framing_runs_after_copy_mismatch", "crates/splot-validate/src/validator/tests/tile_group.rs::validator_continuation_tile_group_framing_silent_without_record", "crates/splot-cli/tests/cli.rs::inspect_json_exposes_frame_header_core"]
commands = ["cargo test -p splot-core framing", "cargo test -p splot-validate tile_payload", "cargo test -p splot-cli inspect_json_exposes_frame_header_core"]
fixtures = ["tests/fixtures/frame-header-core.av2"]
diagnostics = ["tile-payload/size-field-truncated", "tile-payload/tile-size-overflows-payload", "tile-payload/zero-size-tile"]

[[feature]]
id = "AV2-7.3-OBU-ORDERING"
name = "Ordering of OBUs"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Umbrella row. Initial temporal-unit header ordering is implemented in ValidatorContext; frame-unit, metadata suffix, random-access, and long-term-reference ordering remain future work."

[feature.status]
mapped = "done"
types = "partial"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-validate"]
fixtures = []
diagnostics = ["obu-order/temporal-unit-missing-delimiter", "obu-order/global-hls-after-coded-layer", "obu-order/xlayer-order-not-ascending", "obu-order/padding-non-global-outside-coded-layer"]

[[feature]]
id = "AV2-7.3.2-CMVS-BOUNDARIES"
name = "CMVS boundary ordering semantics"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "msdo-global-lcr-agreement"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. A §7.3.2 coded-multistream-video-sequence (CMVS) begin/end tracker landed under sequence-multistream-semantics: a three-state Outside/Inside/Unknown machine (CmvsState) fed by a stateful §5.6 MSDO observer (MsdoObserver, the six §7.3.2 condition-2 key fields), CLK observations, and global-LCR presence, with the verbatim §7.3.2 begin/end sentences quoted inline at each transition. It scopes the sequence-state/monotonic-output-order-mismatch check (only fires on CmvsState::Inside) as a sound under-approximation; begin condition 3 and the global-LCR end clause route to Unknown because exact §7.3.8 global-LCR activation is not modeled. The §7.3.2 boundary-set identity requirement (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-2, line 351) now lands under msdo-global-lcr-agreement as cmvs/boundary-set-mismatch, the first cmvs/* diagnostic: decidable-disagreement-only and resolved at temporal-unit completion (after cmvs.complete_temporal_unit), it fires when a CLK-bearing temporal unit with no OBU_MSDO but an *activated* global LCR (resolved via the association chain) diverges the MSDO-alone boundary set from the MSDO+global-LCR set; the tracker's Unknown states never fire. Residual for validate=done: the remaining cmvs/* boundary-ORDERING diagnostics and the §7.3.3-§7.3.5 frame-unit segmentation they depend on (AV2-7.3.5-CODED-FRAME-UNIT) stay blocked on frame-unit segmentation."

[feature.status]
mapped = "done"
types = "partial"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/context/tests/mod.rs::tests::msdo_observer_reports_first_then_unchanged", "crates/splot-validate/src/context/tests/mod.rs::tests::msdo_observer_detects_each_condition_two_key_field_change", "crates/splot-validate/src/context/tests/mod.rs::tests::msdo_observer_ignores_non_key_field_changes", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_starts_outside", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_begin_condition_1_clk_plus_msdo_enters_inside", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_begin_condition_2_changed_msdo_keeps_inside", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_begin_condition_3_global_lcr_only_is_unknown", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_end_condition_2_clk_without_msdo_exits_inside", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_end_condition_2_with_global_lcr_is_unknown", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_inside_continues_across_non_boundary_tu", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_no_clk_temporal_unit_does_not_begin", "crates/splot-validate/src/context/tests/mod.rs::tests::cmvs_unknown_is_conservative_and_persists", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::cmvs_boundary_set_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::cmvs_boundary_set_no_mismatch_when_clk_carries_msdo", "crates/splot-validate/src/validator/tests/lcr_msdo_cmvs.rs::cmvs_boundary_set_silent_for_unactivated_global_lcr"]
commands = ["cargo test -p splot-validate cmvs", "cargo test -p splot-validate msdo_observer"]
fixtures = []
diagnostics = ["cmvs/boundary-set-mismatch"]

[[feature]]
id = "AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT"
name = "Coded output frame unit ordering semantics"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.3", "7.3.8.10"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/frame_unit.rs"
change_id = "frame-unit-segmentation"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. The coded-output-frame-unit segmentation and the §7.3.3 presence-order checks landed under frame-unit-segmentation (crates/splot-validate/src/frame_unit.rs, FrameUnitSegmenter): each (obu_xlayer_id, obu_mlayer_id, obu_tlayer_id) triple's consecutive OBUs partition into coded frame units with the region order CI (zero or one) -> MFHs -> pre-frame region (BRT/QM/FGM/prefix-metadata, any order) -> the coded frame (same-obu_type tile OBUs with the is_first_tile_group 1-then-0 rule, or exactly one SEF) -> the suffix-metadata tail, OBU_PADDING position-free. Output classification (which selects the §7.3.3 grammar) comes from the parsed output flags: a SEF is always output (frame_unit.rs SefFrame), the tile/TIP frames carry immediate_output_frame/implicit_output_frame from the core parser (context.rs frame_output_class). Eager structural diagnostics (decidable from OBU types and the is_first_tile_group / metadata_is_suffix bits, independent of output class): frame-unit/region-order, frame-unit/duplicate-content-interpretation, frame-unit/first-tile-group-flag, frame-unit/mixed-coded-frame-types, frame-unit/sef-single-obu, frame-unit/suffix-metadata-before-coded-frame. Residual (Unknown path): when a tile/TIP frame's output classification is undecidable (an unsupported FrameHeaderParseStatus stop, blocked on AV2-5.18-FRAME-HEADER), the output-class-derived judgment (the grammar branch and the §7.3.4 BRT bound) is silently dropped for that unit (zero false positives); the OBU-type-decidable structural checks still fire. The prefix-region-after-coded-frame placement is NOT a diagnostic: the grammar permits back-to-back units, so a pre-frame OBU after a coded frame soundly starts a new unit (documented residual, no false positive). §7.3.8.10 ('CI OBUs shall only appear in the first coded frame unit of each embedded layer within a temporal unit', mirror line 880) is enforced from the same per-(xlayer, mlayer) segmentation as frame-unit/ci-not-in-first-frame-unit (independent of output class). decode_check stays partial on the same Unknown-path residual."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_first_tile_group_flag_zero_on_first_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_adjacent_units_split_on_first_tile_group_flag_is_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_first_tile_group_flag_conformant_is_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_undecidable_first_tile_group_stays_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_duplicate_content_interpretation_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_ci_after_mfh_is_region_order_error", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_sef_single_obu_violation_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_mixed_coded_frame_types_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_suffix_metadata_before_coded_frame_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_padding_is_position_free", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_conformant_output_unit_is_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_ci_in_second_frame_unit_is_flagged"]
commands = ["cargo test -p splot-validate frame_unit"]
fixtures = []
diagnostics = ["frame-unit/region-order", "frame-unit/duplicate-content-interpretation", "frame-unit/first-tile-group-flag", "frame-unit/mixed-coded-frame-types", "frame-unit/sef-single-obu", "frame-unit/suffix-metadata-before-coded-frame", "frame-unit/ci-not-in-first-frame-unit"]

[[feature]]
id = "AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT"
name = "Coded nonoutput frame unit ordering semantics"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.4"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "frame-unit-segmentation"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. The coded-non-output-frame-unit segmentation and the 7.3.4 presence-order checks landed under frame-unit-segmentation (crates/splot-validate/src/frame_unit.rs): shares the FrameUnitSegmenter per-(obu_xlayer_id, obu_mlayer_id, obu_tlayer_id) state machine with AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT, enforcing the 7.3.4 non-output region order. The asymmetry the spec encodes is enforced: a coded NON-output frame unit allows zero or one OBU_BUFFER_REMOVAL_TIMING (mirror line 452), whereas an output frame unit allows zero or more (mirror line 384) - frame-unit/buffer-removal-timing-multiplicity fires only for a non-output unit with two-or-more BRT, resolved at the unit boundary once the output class is known. OBU_BRIDGE_FRAME is the type-decidable non-output coded frame (mirror line 470). The is_first_tile_group / mixed-type / SEF structural checks are shared with AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT (same diagnostics). Residual (Unknown path): a tile/TIP frame whose output classification is undecidable (an unsupported FrameHeaderParseStatus stop, blocked on AV2-5.18-FRAME-HEADER) drops the non-output BRT bound for that unit (the output class is not NonOutput, so the bound cannot be asserted) - zero false positives; decode_check stays partial on this residual."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_brt_multiplicity_in_non_output_unit_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_brt_multiplicity_in_output_unit_is_conformant", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_undecidable_first_tile_group_stays_silent"]
commands = ["cargo test -p splot-validate frame_unit"]
fixtures = []
diagnostics = ["frame-unit/buffer-removal-timing-multiplicity"]

[[feature]]
id = "AV2-7.3.5-CODED-FRAME-UNIT"
name = "Coded frame unit ordering semantics"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "frame-unit-segmentation"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. AV2-7.3.5-CODED-FRAME-UNIT is the union 'a coded frame unit is either a coded output frame unit or a coded non-output frame unit' (mirror line 508), realized by the single FrameUnitSegmenter (crates/splot-validate/src/frame_unit.rs) that classifies each unit as output / non-output / Unknown from the parsed output flags and applies the AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT / AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT grammars accordingly. The segmentation is the named blocker this change unblocks for the AV2-7.3.2-CMVS-BOUNDARIES / AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT coded-frame-unit residuals. Residual (Unknown path): an undecidable output classification routes a unit output-class-derived judgment to silence (zero false positives), narrowing as frame-header parsing lands (AV2-5.18-FRAME-HEADER); the OBU-type-decidable structural presence-order checks fire regardless. decode_check stays partial on this residual."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_conformant_output_unit_is_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_undecidable_first_tile_group_stays_silent", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::frame_unit_ci_in_second_frame_unit_is_flagged"]
commands = ["cargo test -p splot-validate frame_unit"]
fixtures = []
diagnostics = []

[[feature]]
id = "AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT"
name = "Coded extended layer unit ordering semantics"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "celu-orderhint-constraints"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. The validator now models the exact §7.3.6 sequential-decoding CVS boundary: a new CVS for an extended layer starts at each temporal unit containing a CLK in that layer's coded extended layer unit (detected from raw OBU headers, per-xlayer per-temporal-unit). Same-temporal-unit records join the new CVS (the CVS starts at the temporal unit, so a sequence header preceding the activating CLK belongs to the new CVS); cross-temporal-unit CVS-scoped comparison diagnostics are deferred to the temporal-unit boundary and dropped when that xlayer's CVS started in the just-completed temporal unit. The former temporal-unit-reset approximation's documented cross-TU false negatives are now true positives. The §6.4.1 distinct-obu_mlayer_id count (DistinctMlayerTracker, tracked by AV2-6.4-SEQUENCE-HEADER-SEMANTICS) likewise re-attributes the boundary temporal unit's pre-CLK ids (e.g. the §7.3.8.1 resent-at-RAP sequence header at obu_mlayer_id 0) to the new CVS at reset_cvs rather than dropping them, so a new CVS that truly exceeds SeqMaxMlayerCnt is flagged (exact re-attribution replacing the former documented sound under-count); ids from earlier temporal units never enter the re-seeded set. The re-seeded set's §6.4.1 exceedance comparison runs after the CLK's frame header activates the header the CLK references (retroactive_distinct_mlayer_check on the §5.18.2 activation path), so it is checked against the header associated with the new CVS rather than the outgoing header still active when the boundary event fires (PR #41 false-positive fix); if the CLK's frame header is unparsable or its referenced header is not in-band, the comparison is skipped (sound false negative). §7.4.4 OLK decode-initiation behavior is out of validator scope (OLK/RAS are not CVS boundary events in sequential decoding); CMVS boundaries are tracked by AV2-7.3.2-CMVS-BOUNDARIES. Documented approximation: global-bucket (GLOBAL_XLAYER_ID) CVS-scoped records are pruned at every CVS boundary event. The §7.3.6 single-active-sequence-header rule (\"Within each extended layer, only one sequence header shall remain active for the duration of a coded video sequence\") is the citation home for hls/multiple-active-sequence-headers, landed under sequence-multistream-semantics on the §5.18.2 frame-confirmed activation path (gated on same-CVS-epoch, differing seq_header_id, and suppression only when caller-provided external HLS declares a sequence header via declares_any_sequence_header()); its matrix tracking row is AV2-7.3.8-HLS-AVAILABILITY. The §7.3.6 coded-extended-layer-unit constraint family landed under celu-orderhint-constraints (crates/splot-validate/src/celu.rs, the CodedExtendedLayerTracker keyed per obu_xlayer_id across a temporal unit, above the FrameUnitSegmenter). Coded-frame-unit boundaries are NOT re-derived in the CELU tracker: the FrameUnitSegmenter is the single source of truth and returns a per-frame-bearing-OBU boundary signal (FrameBoundary: OpensNewUnit / ContinuesUnit / Ambiguous) that the tracker consumes, so the two layers agree by construction (e.g. on TIP, BRT, TIP the segmenter splits at the BRT new-unit head into two units, where the former last-obu_type comparison merged the TIPs; the segmenter keys per (xlayer, mlayer, tlayer) triple and the tracker aggregates per (xlayer, mlayer), so an OpensNewUnit in any triple of an (xlayer, mlayer) opens an embedded-layer frame unit). An Ambiguous boundary (same-type no-delimiter TIP, or unreadable tile-group delimiter) poisons that embedded layer's unit-count-dependent judgments (the output-slot grammar and the CLK/OLK-first-unit rule) — dropped, never guessed (Unknown invariant), replacing the former same-type-run-is-one-unit behavior. Ambiguity-poison precision (codex round-3 F1): an ambiguous OBU also poisons the output-presence judgments (celu/missing-output-frame-unit at CELU scope, celu/non-output-without-output at layer scope) ONLY when it could itself be an output unit, i.e. its output class is not type-decided non-output; an ambiguous OBU_BRIDGE_FRAME is type-decided non-output whichever way the boundary resolves and so does NOT poison those rules (over-poisoning would hide a genuine missing-output). The type-decided CLK/OLK identity and leading-ness are boundary-independent and are recorded for every frame-bearing OBU regardless of its coded-frame-unit boundary: on an Ambiguous boundary (F5) an OLK plus an ambiguous CLK still fires celu/clk-olk-mixed and a LEADING frame plus an ambiguous Regular-typed OBU still fires celu/leading-frame-mix; and on a ContinuesUnit boundary (codex round-5 F2 — the segmenter reports ContinuesUnit for a differently-typed non-first tile group of an already-open coded frame, also flagging frame-unit/mixed-coded-frame-types) record_clk_olk_identity and record_leadingness now run before the ContinuesUnit early return, where the former code only recorded them on the OpensNewUnit / Ambiguous arms — so a CLK opener plus an OLK-typed flag-0 continuation fires celu/clk-olk-mixed, and a LEADING opener plus a REGULAR-typed flag-0 continuation fires celu/leading-frame-mix (the unit-count-dependent accounting stays skipped on a continuation). The family: the in-unit OBU ordering between the four HLS-header phases, the ascending-obu_mlayer_id frame-unit ordering (which counts every frame-unit constituent at its own obu_mlayer_id — the CI head and the BRT/QM/FGM/prefix-metadata/MFH interior OBUs as well as the coded frame, codex round-3 F2: a head heading a higher embedded layer's unit makes a later lower-mlayer coded frame out of order; a suffix metadata shares its frame's mlayer and so never lowers the high-water mark; codex round-4 F3: the ordering is boundary-independent, so a ContinuesUnit frame OBU — a non-first tile group, which belongs to its opener's already-begun unit — and an Ambiguous frame OBU — which belongs to some layer-m unit either way — both run note_embedded_layer_ordering before their observe_frame early returns, where the former code only counted OpensNewUnit frames; e.g. mlayer0 first tile, mlayer1 frame, mlayer0 non-first tile now fires celu/in-unit-order, as does an ambiguous OBU at a lower mlayer after a higher one began, while a normal split frame at one embedded layer stays silent), AND the within-layer output-slot presence grammar (zero or more coded non-output frame units then zero or one coded output frame unit, the coded output frame unit last per embedded layer — mirror lines 528-529: a later decided unit after the layer's decided-output unit fires, regardless of its own output class; an Unknown-class earlier unit does not consume the slot) (all three under celu/in-unit-order, disjoint from obu-order/non-global-hls-before-coded-layer which owns the HLS-header-after-frame-region case), at-least-one-output-frame-unit (celu/missing-output-frame-unit, mirror line 536: applies to EVERY CELU — fires for a header-only CELU with >=1 non-padding constituent OBU at a non-global obu_xlayer_id and zero frame-bearing OBUs, anchored at the CELU's first constituent OBU, as well as a frame-bearing CELU with a decided unit and no output unit; dropped when a frame-bearing unit's output class is Unknown or when an ambiguous OBU could itself be an output unit (F1); a padding-only / reserved-type-only obu_xlayer_id group never constitutes a CELU), non-output-implies-output per embedded layer (celu/non-output-without-output), with output type-decided by obu_type for a SEF (output, §7.3.3 \"Or\" branch) and an OBU_BRIDGE_FRAME (non-output, §7.3.4 list only) BEFORE any flag parse (codex round-3 F3: the shared frame_unit::type_decided_output helper is the single source of truth used by both the FrameUnitSegmenter and the CELU wiring, so a bridge-only CELU fires celu/missing-output-frame-unit and a bridge layer alongside an output layer fires celu/non-output-without-output, where the former parser-stops-early path routed both to Unknown and dropped them), same-OrderHint across the CELU's output units (celu/output-order-hint-mismatch, order_hint read from the §5.18.2 core parse on the intra / SEF-with-derive_sef_order_hint==0 paths), CLK/OLK only-first-frame-unit and lowest-embedded-layer-first rules (celu/key-not-in-first-unit, celu/lowest-layer-not-key), no CLK+OLK mix (celu/clk-olk-mixed), all-leading-or-none (celu/leading-frame-mix; leading-ness is the type-decided tri-state Leadingness mirroring AVM is_leading_picture at av2/decoder/obu.c:2544-2549 — the av2_is_leading_vcl_obu set LEADING_TILE_GROUP/LEADING_SEF/LEADING_TIP is Leading, the §5.18.2 IsRegular==1 / av2_is_regular_vcl_obu set OLK/REGULAR_*/SWITCH/RAS/BRIDGE is Regular, and a CLK lands in neither so it is Indeterminate and is excluded from the all-leading-or-none judgment entirely; the rule fires only when a Leading and a Regular unit coexist in one CELU; the §6.4.1 gloss at 06-syntax-structures-semantics.md:4546 reads IsRegular==0 as exactly leading and would class a CLK as leading, but the AVM oracle tri-states it indeterminate, so the validator under-reports the CLK-plus-regular structure the §7.3.6 CLK rule explicitly contemplates — mirror lines 541-549, a higher embedded layer's first frame unit may be a non-CLK regular frame — as a documented sound under-approximation; type-decided, so it never routes to Unknown), and the CELU-scoped content-interpretation-first-frame-unit rule (celu/content-interpretation-not-in-first-unit, distinct from the §7.3.8.10 temporal-unit form frame-unit/ci-not-in-first-frame-unit). Output-class- and OrderHint-derived judgments drop to silence when the §5.18.2 core parse stops before the field or the active sequence header is unavailable (the Unknown invariant). Residuals (validate stays partial): the §7.3.6 bit-identity cross-layer fingerprint baseline (the activated-sequence-header repeat-content check, tracked here as hls/repeated-sequence-header-not-identical, retargeted to this row by rap-availability-replay/PR #51) and the §7.3.6 monotonic_output_order_flag==0 OrderHint regression rule's full form (mirror lines 579-584: within a CVS, for a given (obu_xlayer_id, obu_mlayer_id), a later-in-output-order coded output frame unit must not have an OrderHint <= an earlier one's unless a restricted_prediction_switch==1 SWITCH intervenes) stay named residuals — the monotonic regression needs output-order and MSB-extended OrderHint state this phase does not model (blocked on AV2-5.18.2-FRAME-HEADER-INFO inter-path / output-order state). Named residual (OrderHint LSB proxy, celu-orderhint-constraints follow-up): celu/output-order-hint-mismatch compares the raw order_hint LSB syntax as a proxy for the §7.3.6 DECODED OrderHint (get_disp_order_hint's MSB extension for non-CLK frames); within one CELU all output units share one active sequence header (one OrderHintBits), so the LSB comparison is a sound under-approximation (differing LSBs imply differing OrderHints; same LSBs with diverging MSBs under-report). The decoded-OrderHint comparison stays blocked on reference-state modelling / get_disp_order_hint (AV2-5.18.2-FRAME-HEADER-INFO). Stale-activation safety: the §5.18.2 core parse for the output class / order_hint is only trusted when the frame's referenced sequence header equals the in-band active header it was parsed against; a frame referencing an absent/out-of-range/different header (while a stale earlier activation persists) routes to Unknown rather than misparsing field widths off the wrong header (the frame-unit segmenter's output-class derivation applies the same guard). MFH-backed frame resolution (codex round-4 F2): the referenced header resolves through EITHER (a) cur_mfh_id==0 with seq_header_id_in_frame_header in range (the §5.18.2 prefix's referenced_sequence_header_id) OR (b) cur_mfh_id>0 in range whose IN-BAND multi-frame header (§7.3.8.7, HlsAvailabilityStore::multi_frame_header) has mfh_seq_header_id equal to the parsed-against active id. The §5.18.2 control region through the output flags (immediate_output_frame / implicit_output_frame) is read BEFORE the cur_mfh_id>0-gated structures and is determined by the active (==resolved) sequence header alone, so an MFH-backed frame's output class is decidable; parse_frame_core is now invoked WITH the resolving MFH record (forward plumbing — the record carries no field the currently-reachable paths consult, so it changes no current decision but is threaded for the §5.18 MFH coverage to come). The former guard required referenced_sequence_header_id==Some(seq_id), which is None for every cur_mfh_id>0 frame, so decidable MFH-backed frames wrongly routed to Unknown and silently dropped celu/missing-output-frame-unit, celu/non-output-without-output, the non-output BRT bound, etc. External-HLS caveat (PR #49 partial-declaration policy): an MFH only externally declared (ExternalHlsMode::Provided, not in-band) is not a verifiable association — multi_frame_header returns None for it — so the frame stays Unknown; an out-of-range cur_mfh_id, an absent record, or an MFH whose mfh_seq_header_id resolves to a header other than the active one (e.g. its target sequence header was never sent in-band) all keep Unknown. First-CELU-of-the-sequence CI presence (codex round-6 F3): the §7.3.6 sentence \"If an OBU_CONTENT_INTERPRETATION is present in any coded extended layer unit, this OBU shall also be present in the first coded extended layer unit of the sequence ... for a given embedded layer\" (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-6 lines 560-562) has two halves: the CONTENTS-IDENTITY half (\"the same contents in all its repetitions\") is owned by content-interpretation/repeated-ci-not-identical (§6.14, AV2-5.15-CONTENT-INTERPRETATION), and the PRESENCE half is now landed as celu/content-interpretation-not-in-first-celu. The presence rule is coded-video-sequence-scoped, so it lives in ValidatorContext (crates/splot-validate/src/context/mod.rs::resolve_ci_first_celu_for_tu), NOT the per-temporal-unit CELU tracker: per extended layer it records the embedded layers whose CI appeared in the CVS's first coded extended layer unit (its CELU in the CVS's first temporal unit — a CVS starts at the CLK temporal unit, §7.3.6 — re-seeded per CVS in start_cvs_for_xlayer), and a later CELU adding a CI for an embedded layer the first CELU lacked fires once per (xlayer, mlayer, CVS epoch), anchored at the offending CI. CIs are buffered per temporal unit (ci_observed_in_tu) and resolved at the temporal-unit boundary (and end of bitstream) because the whole temporal unit containing a CLK belongs to the new CVS, so a CI's CELU membership is final only once the unit completes. DROPS when the first CELU of the CVS was not observed: a mid-CVS join (no CLK seen for the layer, the implicit CVS began before the first observed OBU, first_celu_tu == None) or any external-HLS Provided mode (an external CI in the first CELU cannot be enumerated by ExternalHlsSet, which expresses only sequence headers and operating point sets — under-report per the partial-declaration suppression policy). Distinct from the CELU-scoped first-FRAME-UNIT form celu/content-interpretation-not-in-first-unit (lines 557-559) and the §7.3.8.10 temporal-unit form frame-unit/ci-not-in-first-frame-unit. Codex round-7 precision (celu-orderhint-constraints follow-up): (F3/F4/F5) the three §7.3.6/§7.3.7 OrderHint/OrderHintBits mismatch checks (celu/output-order-hint-mismatch, celu/doh-order-hint-bits-mismatch, celu/doh-order-hint-mismatch) now emit a mismatch PROVEN between two known samples regardless of any undecidable participant: an undecidable frame / output unit / CELU can only prevent proving AGREEMENT (never reported) and cannot make a pair already proven to differ conforming, so the former per-temporal-unit undecidable gates no longer suppress proven mismatches (the per-pair known-and-equal-bits gate for the cross-CELU OrderHint LSB proxy is kept — an unknown-or-unequal-bits pair still records no mismatch, and a CELU with an undecidable output OrderHint is simply not contributed to the cross-CELU comparison so a recorded mismatch is always between two fully-decidable CELUs). (F2) a same-type no-delimiter OBU_BRIDGE_FRAME adjacency now reports FrameBoundary::Ambiguous in the FrameUnitSegmenter (crates/splot-validate/src/frame_unit.rs): the UNIT COUNT (one bridge coded frame or two back-to-back same-type bridge units) is genuinely ambiguous with no in-band delimiter, and that boundary ambiguity is INDEPENDENT of output-class decidability, so the CELU layer poisons its unit-count-dependent judgments for that embedded layer rather than silently under-counting the run as one unit; the bridge's type-decided non-output class is preserved (NOT routed to Unknown), so the §7.3.4 BRT multiplicity bound stays evaluable across the run and an ambiguous bridge still does not poison output-presence (non-output either way — round-3 precision). Codex round-8 precision (celu-orderhint-constraints follow-up — the POISON-SCOPE RULE): an ambiguity changes the embedded layer's unit COUNT/INDEX, not the relative order of two DECIDED units (an OpensNewUnit is the segmenter's decided split that no resolution can merge), so the per-layer ambiguous poison is scoped to exactly what it can change — INDEX-QUANTIFIED judgments drop (the per-unit accounting / OrderHint accumulators and the output-presence judgments) while DECIDED-PAIR-ORDER judgments survive. (F1) the within-layer output-slot grammar (celu/in-unit-order) no longer consults the poison: once a DECIDED output unit consumes the layer's slot, any LATER DECIDED OpensNewUnit unit fires regardless of an earlier/intervening ambiguous OBU (e.g. decided output, ambiguous OBU, decided unit -> fires, pre-fix silent), while an ambiguous OBU with no later decided unit stays silent. (F2) celu/key-not-in-first-unit is gated on \"a DECIDED earlier unit already exists in the layer\" (units_opened >= 1, which counts only decided OpensNewUnit units) rather than the blanket poison: a decided non-key unit, an ambiguous same-type OBU, then a decided CLK/OLK fires (the key is provably not the layer's first coded frame unit whatever the intervening ambiguity was, pre-fix silent); the dropped ASYMMETRIC case is the key ITSELF being the first DECIDED unit while an ambiguous OBU preceded it (units_opened == 0 -> already silent by the same guard). celu/lowest-layer-not-key keeps that asymmetry by construction (its predicate is the identity of the lowest layer's FIRST unit, which an ambiguous OBU before the candidate leaves unknown — no decided-pair-order reformulation exists, so it stays dropped). The now-redundant per-layer ambiguous_poisoned flag (consulted by no judgment after F1/F2) was removed; the output-presence poison (output_presence_poisoned / missing_output_poisoned, set only when the ambiguous OBU could itself be an output unit) is the remaining ambiguity-poison state. NAMED RESIDUAL (per-operating-point CLK/OLK-first rule, mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-6 lines 592-598 / page boundary 595-603): for each CELU containing a CLK or OLK, the OBUs within the CELU FOR EACH OPERATING POINT shall (1) contain one or more coded frame units and (2) have the first coded frame unit be the key type. The validator implements only the CELU-WIDE forms — the per-layer celu/key-not-in-first-unit and the lowest-embedded-layer-first celu/lowest-layer-not-key (mirror lines 541-553) — but NOT the per-operating-point projection. It is OPERATING-POINT-FRAME-MAPPING-BLOCKED: projecting a CELU's decided frame units onto an operating point's embedded-layer (mlayer) set needs (a) a resolvable per-CELU ACTIVE operating point set — not modeled; OPS records (OperatingPointSetRecord, AV2-5.10-OPERATING-POINT-SET) are looked up only by an explicit referencing ops_id (e.g. a BRT's br_ops_id), with no in-band selection of which OPS/OP applies to a temporal unit's CELU — and (b) per-OP per-xlayer mlayer membership retained for projection — only Explicit ops_mlayer_info() entries are kept (for the §6.10.7 dependency-map checks), while Inherited/Absent sources are dropped, so a CELU's full per-OP membership is not reconstructable. The validator explicitly does not model the operating-point-to-frame mapping (crates/splot-validate/src/context/mod.rs check_ops_level_tier_value_space TODO(spec: AV2-A-LEVELS-TIERS): \"an operating-point-to-frame mapping (which frames belong to which operating point) the validator does not model yet\"), the same blocker behind the backlogged annex-a/frame-exceeds-ops-level diagnostic. Implementing the per-OP rule without this mapping would require guessing OP membership and risk false positives, so it stays a named residual rather than a guessed check; unblocked by the operating-point frame mapping (AV2-5.10-OPERATING-POINT-SET / the VALIDATOR-ROADMAP operating-point frame-mapping item)."

[feature.status]
mapped = "done"
types = "todo"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/sequence_activation.rs::sequence_activation_uses_clk_referenced_sequence_header", "crates/splot-validate/src/validator/tests/sequence_activation.rs::sequence_fingerprint_preserved_for_in_cvs_repeat", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_repeated_sequence_header_across_temporal_units_without_clk_is_flagged", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_cross_temporal_unit_repeat_is_flushed_at_next_temporal_delimiter", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_clk_for_other_xlayer_does_not_end_coded_video_sequence", "crates/splot-validate/src/validator/tests/sequence_activation.rs::sequence_reconfiguration_in_clk_temporal_unit_is_not_flagged", "crates/splot-validate/src/validator/tests/sequence_activation.rs::first_picture_in_tu_is_tracked_per_extended_layer", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_repeated_non_identical_across_temporal_units_without_clk_is_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_repeated_non_identical_in_clk_temporal_unit_is_not_flagged", "crates/splot-validate/src/celu.rs::tests::hls_headers_out_of_order_is_flagged", "crates/splot-validate/src/celu.rs::tests::hls_headers_in_order_is_silent", "crates/splot-validate/src/celu.rs::tests::hls_header_after_frame_region_is_not_celu_in_unit_order", "crates/splot-validate/src/celu.rs::tests::descending_mlayer_frame_units_is_flagged", "crates/splot-validate/src/celu.rs::tests::padding_is_transparent_to_in_unit_order", "crates/splot-validate/src/celu.rs::tests::celu_with_no_output_unit_is_flagged", "crates/splot-validate/src/celu.rs::tests::missing_output_unit_drops_when_a_unit_output_class_is_unknown", "crates/splot-validate/src/celu.rs::tests::non_output_without_output_in_layer_is_flagged", "crates/splot-validate/src/celu.rs::tests::non_output_without_output_drops_when_layer_output_unknown", "crates/splot-validate/src/celu.rs::tests::output_units_with_different_order_hint_is_flagged", "crates/splot-validate/src/celu.rs::tests::output_order_hint_mismatch_fires_despite_a_third_unit_with_unknown_hint", "crates/splot-validate/src/celu.rs::tests::output_order_hint_no_proven_mismatch_with_unknown_hint_is_silent", "crates/splot-validate/src/celu.rs::tests::clk_and_olk_mixed_is_flagged", "crates/splot-validate/src/celu.rs::tests::clk_not_in_first_unit_of_layer_is_flagged", "crates/splot-validate/src/celu.rs::tests::clk_lowest_layer_not_key_is_flagged", "crates/splot-validate/src/celu.rs::tests::mixed_leading_and_non_leading_is_flagged", "crates/splot-validate/src/celu.rs::tests::ci_outside_first_frame_unit_is_flagged", "crates/splot-validate/src/celu.rs::tests::tip_brt_tip_yields_two_units", "crates/splot-validate/src/celu.rs::tests::decided_unit_then_ambiguous_then_decided_clk_fires_key_not_in_first_unit", "crates/splot-validate/src/celu.rs::tests::ambiguous_then_decided_clk_no_decided_earlier_unit_is_silent_on_key_rule", "crates/splot-validate/src/celu.rs::tests::ambiguous_run_with_no_output_fires_missing_output_on_decided_unit", "crates/splot-validate/src/celu.rs::tests::second_output_unit_in_layer_fires_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::decided_unit_after_output_with_intervening_ambiguous_fires_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::output_then_ambiguous_no_later_decided_unit_is_silent_on_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::non_output_after_output_in_layer_fires_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::unknown_class_after_output_in_layer_fires_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::unknown_class_earlier_unit_does_not_consume_output_slot", "crates/splot-validate/src/celu.rs::tests::non_outputs_then_output_in_layer_is_silent_on_output_slot_grammar", "crates/splot-validate/src/celu.rs::tests::header_only_celu_fires_missing_output", "crates/splot-validate/src/celu.rs::tests::padding_only_xlayer_group_is_silent", "crates/splot-validate/src/celu.rs::tests::frame_bearing_with_unknown_classes_is_silent_on_missing_output", "crates/splot-validate/src/celu.rs::tests::fully_unknown_celu_is_silent", "crates/splot-validate/src/celu.rs::tests::ambiguous_undecided_class_obu_poisons_missing_output", "crates/splot-validate/src/celu.rs::tests::ambiguous_undecided_class_obu_poisons_non_output_without_output", "crates/splot-validate/src/celu.rs::tests::ambiguous_bridge_does_not_poison_missing_output", "crates/splot-validate/src/celu.rs::tests::ambiguous_bridge_does_not_poison_non_output_without_output", "crates/splot-validate/src/celu.rs::tests::ci_at_higher_mlayer_then_frame_at_lower_fires_in_unit_order", "crates/splot-validate/src/celu.rs::tests::frame_interior_at_higher_mlayer_then_frame_at_lower_fires_in_unit_order", "crates/splot-validate/src/celu.rs::tests::conformant_ascending_with_ci_heads_is_silent", "crates/splot-validate/src/celu.rs::tests::suffix_interior_same_mlayer_does_not_break_monotonicity", "crates/splot-validate/src/celu.rs::tests::olk_then_ambiguous_clk_fires_clk_olk_mixed", "crates/splot-validate/src/celu.rs::tests::leading_then_ambiguous_regular_fires_leading_frame_mix", "crates/splot-validate/src/celu.rs::tests::ambiguous_clk_indeterminate_leadingness_stays_excluded", "crates/splot-validate/src/celu.rs::tests::clk_opener_then_olk_continuation_fires_clk_olk_mixed", "crates/splot-validate/src/celu.rs::tests::leading_opener_then_regular_continuation_fires_leading_frame_mix", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_fires_despite_a_third_undecidable_celu", "crates/splot-validate/src/celu.rs::tests::doh_order_hint_bits_mismatch_fires_despite_an_undecidable_frame", "crates/splot-validate/src/celu.rs::tests::doh_order_hint_bits_mismatch_fires_with_undecidable_between_known", "crates/splot-validate/src/frame_unit.rs::tests::same_type_adjacent_bridge_reports_ambiguous_boundary", "crates/splot-validate/src/frame_unit.rs::tests::same_type_adjacent_bridge_keeps_type_decided_non_output_class", "crates/splot-validate/src/validator/tests/celu.rs::celu_same_celu_output_order_hint_mismatch_is_flagged", "crates/splot-validate/src/validator/tests/celu.rs::celu_same_celu_output_order_hint_agreement_is_silent", "crates/splot-validate/src/validator/tests/celu.rs::celu_frame_referencing_absent_seq_header_is_unknown_not_misparsed", "crates/splot-validate/src/validator/tests/celu.rs::celu_frame_referencing_inband_seq_header_is_decided", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::celu_bridge_only_celu_fires_missing_output", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::celu_bridge_layer_with_output_layer_fires_non_output_without_output", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_backed_frame_is_decided_not_unknown", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_referencing_different_seq_than_active_is_unknown", "crates/splot-validate/src/validator/tests/celu.rs::celu_mfh_out_of_range_or_absent_is_unknown", "crates/splot-validate/src/validator/tests/celu.rs::celu_in_unit_order_continuation_at_lower_mlayer_after_higher_fires", "crates/splot-validate/src/validator/tests/celu.rs::celu_in_unit_order_normal_split_frame_stays_silent", "crates/splot-validate/src/validator/tests/celu.rs::celu_in_unit_order_ambiguous_obu_at_lower_mlayer_after_higher_fires", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_in_later_celu_absent_from_first_celu_of_sequence_is_flagged", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_in_first_celu_of_sequence_then_repeated_later_is_silent", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_in_later_celu_with_mid_cvs_join_is_silent", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_first_celu_presence_judgment_resets_at_new_cvs", "crates/splot-validate/src/validator/tests/content_interpretation_timing.rs::ci_in_later_celu_under_external_hls_is_silent"]
commands = ["cargo test -p splot-validate sequence_activation", "cargo test -p splot-validate sequence_fingerprint", "cargo test -p splot-validate temporal_unit", "cargo test -p splot-validate celu"]
fixtures = []
diagnostics = ["hls/repeated-sequence-header-not-identical", "content-interpretation/repeated-ci-not-identical", "sequence-state/tlayer-exceeds-max", "celu/in-unit-order", "celu/missing-output-frame-unit", "celu/non-output-without-output", "celu/output-order-hint-mismatch", "celu/clk-olk-mixed", "celu/key-not-in-first-unit", "celu/lowest-layer-not-key", "celu/leading-frame-mix", "celu/content-interpretation-not-in-first-unit", "celu/content-interpretation-not-in-first-celu"]

[[feature]]
id = "AV2-7.3.7-TEMPORAL-UNIT-ORDER"
name = "Temporal unit OBU order"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.7", "7.4.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "celu-orderhint-constraints"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Temporal unit state machine: global temporal delimiter, HLS prefix OBUs, coded extended layer units in ascending xlayer order, and padding exceptions. The two formerly-backlogged §7.3.7/§7.3.6 ordering rows landed under frame-unit-segmentation, now decidable from the segmentation: obu-order/global-hls-after-metadata-suffix (a global HLS prefix OBU after a global suffix metadata, classified now that metadata_is_suffix is parsed in TemporalUnitState::observe_metadata) and obu-order/non-global-hls-before-coded-layer (a non-global HLS header OBU — LCR/OPS/atlas/sequence header — after the coded frame region of its §7.3.6 coded extended layer unit has begun). The §7.3.7 / §7.4.6 display-order-hint (DOH) constraints (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-7 lines 650-657, #s-7-4-6 lines 1316-1320) landed under celu-orderhint-constraints in the CodedExtendedLayerTracker (crates/splot-validate/src/celu.rs): when the temporal unit's recorded DOH constraint flag is active (multistream_doh_constraint_flag of the preceding MSDO, or lcr_doh_constraint_flag of the activated global LCR, == 1 — resolved by ValidatorContext::doh_constraint_flag_active at temporal-unit-boundary completion), all frame units in the temporal unit must share one OrderHintBits (celu/doh-order-hint-bits-mismatch, the bits read from the active sequence header's order_hint_bits per frame) and coded output frame units present in multiple CELUs of the temporal unit must share one OrderHint (celu/doh-order-hint-mismatch). Both stay silent with the flag off and drop their judgment when the bits / OrderHint is undecidable (the Unknown invariant). OrderHintBits stale-guard (codex round-3 F4): a frame contributes its OrderHintBits only when its referenced sequence header resolved to the active header it parsed against (the same frame_core_against_referenced_header resolution that gates the output class — facts AND bits now come from ONE parse + resolution in frame_celu_facts, removing the former double parse); a frame referencing an absent / stale header contributes None (which sets bits_undecidable and so also gates the cross-CELU OrderHint comparison) rather than the stale active header's order_hint_bits, which could otherwise false-positive celu/doh-order-hint-bits-mismatch. OrderHintBits is fed per frame UNIT, not per OBU (codex round-4 F1): the ValidatorContext::observe_obu CELU wiring notes the accumulator on the segmenter's FrameBoundary — OpensNewUnit notes the unit's resolved bits (Some, or None when its opener does not resolve, since an unresolved opener still soundly poisons the unit's unknowable bits), Ambiguous notes None (it might open a unit with unknowable bits), and ContinuesUnit (a non-first tile group of an already-counted coded frame, which parses no core and contributes None) is SKIPPED. The former per-OBU feeding let a continuation's None set bits_undecidable and poison the whole temporal unit's same-OrderHintBits judgment, over-suppressing a real bits mismatch on a frame split across first + non-first tile groups; the per-unit feeding makes only the unit's opener contribute, so a genuine cross-unit OrderHintBits disagreement now fires. Global frame-bearing OBUs are excluded before the accumulator (codex round-5 F3): the observe_obu wiring now applies the same non-global guard (obu_xlayer_id != GLOBAL_XLAYER_ID) that the CodedExtendedLayerTracker::observe filter uses, BEFORE feeding note_order_hint_bits — where the former wiring fed it unconditionally. A frame-bearing OBU at GLOBAL_XLAYER_ID is itself invalid (diagnosed by obu-header/global-xlayer-allowed-types) and is not part of any CELU (§7.3.6), so no sequence header is active for xlayer 31 and it resolved no bits; the former path fed a spurious None that set bits_undecidable and suppressed a real cross-CELU bits mismatch between the valid CELUs in the temporal unit. The global OBU keeps its own obu-header diagnostic. DOH flag governing-window sampling (codex round-5 F1): ValidatorContext::doh_constraint_flag_active_for_completed_tu resolves the lcr_doh_constraint_flag side against the CMVS window that CONTAINS the just-completed temporal unit, captured BEFORE CmvsTracker::complete_temporal_unit applies the unit's §7.3.2 begin/end conditions and mutates the live window. Per §7.3.7 the DOH constraints apply 'for each temporal unit in the coded multistream video sequence'; a CLK boundary temporal unit with no MSDO and no activated global LCR ENDS the CMVS (§7.3.2 end condition 2) and is the LAST temporal unit of the ENDING CMVS, so its governing window is the pre-completion start — the former code sampled the flag against the live window the tracker had just Closed to None, reading the LCR flag false and skipping the §7.3.7 checks for that unit (a false negative). The governing window is the post-completion window when Some (the unit BEGAN a new CMVS and is the first unit of it), else the pre-completion window (Close); the MSDO side is window-independent (live last-wins msdo_substream_max, which complete_temporal_unit does not clear, so it already reflects the preceding MSDO that governed the completed unit). Named residual (OrderHint LSB proxy, celu-orderhint-constraints follow-up): celu/doh-order-hint-mismatch compares the raw order_hint LSB syntax as a proxy for the §7.3.7 DECODED OrderHint (get_disp_order_hint's MSB extension for non-CLK frames); ACROSS CELUs the proxy is UNSOUND when the participating layers' OrderHintBits differ (equal decoded OrderHints can carry different-width LSB encodings -> a false positive), so the cross-CELU comparison is gated on the temporal unit's OrderHintBits being KNOWN and EQUAL — when they differ celu/doh-order-hint-bits-mismatch fires and the OrderHint comparison drops. The decoded-OrderHint comparison itself stays blocked on reference-state modelling / get_disp_order_hint (AV2-5.18.2-FRAME-HEADER-INFO). Two-constraint per-pair bits gating (codex round-6 F2): §7.3.7 imposes TWO independent constraints — (1) ALL frame units in the temporal unit share one OrderHintBits (celu/doh-order-hint-bits-mismatch, over every frame unit) and (2) coded OUTPUT frame units in multiple CELUs share one OrderHint (celu/doh-order-hint-mismatch). The cross-CELU OrderHint comparison (2) is now gated PER COMPARED PAIR on the two output CELUs' OWN OrderHintBits being known and equal — the per-output-unit bits are carried alongside each output-unit OrderHint sample (FrameFacts::order_hint_bits, populated from the same frame_celu_facts resolution), NOT taken from constraint (1)'s temporal-unit-wide bits_undecidable / bits_mismatch judgment. The former code gated (2) on the TU-wide same-bits judgment, so an unknown-bits NON-OUTPUT (or otherwise unrelated) frame unit elsewhere in the temporal unit set bits_undecidable and SILENTLY suppressed a decidable (2) mismatch between two output CELUs whose own bits were known and equal (a false negative). Now (1) still drops on any unknown-bits frame unit, while (2) fires from the compared output units' own bits; when a compared pair's bits are known but UNEQUAL, (1) fires the bits-mismatch and (2) drops for that pair (unsound cross-width proxy). Per-bits grouping of the cross-CELU comparison (codex round-8 F3): to realise the per-pair equal-bits gate across ALL output CELUs (not only against the very first sample), output-CELU OrderHint samples are now GROUPED BY their known OrderHintBits value (DohTuAccumulator::output_order_hint_by_bits) and each is compared to its OWN group's representative (the group's first decidable sample) rather than to a single first_output_order_hint. The former single-representative scheme missed a proven WITHIN-GROUP mismatch when the first sample had different bits: samples (OrderHintBits, OrderHint) = (4, 0), (5, 1), (5, 2) left both (5, *) samples failing the equal-bits gate against the (4, 0) representative, so the (5, 1)/(5, 2) mismatch was silently dropped; grouping records it (celu/doh-order-hint-mismatch fires while the 4 != 5 width difference also fires constraint (1) celu/doh-order-hint-bits-mismatch). An unknown-bits output sample stays out of all groups; a known-but-unequal-bits pair lands in different groups and is never compared by (2) (covered by (1) instead); one diagnostic is emitted per temporal unit (the first within-group disagreement found), anchored at the offending later sample. End-of-bitstream governing-window symmetry (codex round-6 F1): the round-5 internal-boundary DOH-flag governing-window capture is now mirrored at the END OF THE BITSTREAM. ValidatorContext::finish captures the CMVS window before the end-of-stream CmvsTracker::complete_temporal_unit applies the final unit's §7.3.2 begin/end conditions and clears the live window, and samples the flag via doh_constraint_flag_active_for_completed_tu (the same governing-window helper the temporal-delimiter path uses) instead of the live-window doh_constraint_flag_active. The final temporal unit has no trailing temporal delimiter, so it is completed by finish; a CLK final unit with no MSDO and no activated global LCR ENDS the CMVS at EOF (§7.3.2 end conditions 2 and 3) and is the LAST temporal unit of the ending CMVS, governed by that CMVS's activated global LCR — the former finish path read the flag against the live window finish had just Closed to None (a false negative on the last CELU/doh check). The now-unused doh_constraint_flag_active helper was removed (both the internal-boundary and EOF paths use the governing-window form). Residual: the in-unit metadata-suffix placement WITHIN a coded frame unit and the frame-unit segmentation details proper are owned by AV2-7.3.3-CODED-OUTPUT-FRAME-UNIT / AV2-7.3.4-CODED-NONOUTPUT-FRAME-UNIT / AV2-7.3.5-CODED-FRAME-UNIT (frame_unit.rs)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "todo"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_duplicate_temporal_delimiter_is_flagged", "crates/splot-validate/src/validator/tests/sequence_state.rs::temporal_unit_accepts_ascending_coded_xlayers", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::obu_order_global_hls_after_metadata_suffix_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::obu_order_non_global_hls_before_coded_layer_is_flagged", "crates/splot-validate/src/validator/tests/frame_unit_order.rs::obu_order_non_global_hls_header_before_frame_is_conformant", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_cross_celu_order_hint_mismatch_under_msdo_flag_is_flagged", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_cross_celu_order_hint_mismatch_without_flag_is_silent", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_under_flag_is_flagged", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_without_flag_is_silent", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_drops_when_a_celu_hint_is_unknown", "crates/splot-validate/src/celu.rs::tests::doh_order_hint_bits_mismatch_under_flag_is_flagged", "crates/splot-validate/src/celu.rs::tests::doh_order_hint_bits_mismatch_without_flag_is_silent", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_drops_when_bits_differ", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_drops_when_bits_unknown", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_cross_celu_order_hint_mismatch_with_different_bits_is_silent", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_cross_celu_order_hint_mismatch_with_equal_bits_is_flagged", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_order_hint_bits_absent_ref_frame_contributes_no_bits", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_order_hint_bits_resolved_frames_with_differing_bits_still_fires", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_order_hint_bits_continuation_obu_does_not_poison_unit", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_order_hint_bits_continuation_only_does_not_poison", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_global_frame_obu_does_not_poison_bits_accumulator", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_flag_via_lcr_survives_cmvs_close_at_boundary_tu", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_flag_via_lcr_off_at_boundary_tu_stays_silent", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_flag_via_lcr_survives_eof_close_at_boundary_tu", "crates/splot-validate/src/validator/tests/celu.rs::celu_doh_flag_via_lcr_off_at_eof_boundary_tu_stays_silent", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_fires_despite_unknown_bits_nonoutput_unit", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_drops_for_unequal_bits_pair_despite_third_match", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_mismatch_fires_within_a_later_bits_group", "crates/splot-validate/src/celu.rs::tests::doh_cross_celu_order_hint_groups_agree_within_each_bits_group_is_silent"]
commands = ["cargo test -p splot-validate hls", "cargo test -p splot-validate temporal_unit", "cargo test -p splot-validate obu_order", "cargo test -p splot-validate celu"]
fixtures = []
diagnostics = ["obu-order/duplicate-temporal-delimiter", "obu-order/temporal-unit-missing-delimiter", "obu-order/xlayer-order-not-ascending", "obu-order/global-hls-after-metadata-suffix", "obu-order/non-global-hls-before-coded-layer", "celu/doh-order-hint-mismatch", "celu/doh-order-hint-bits-mismatch"]

[[feature]]
id = "AV2-7.3.8-HLS-AVAILABILITY"
name = "High-level syntax availability"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "rap-replay-qm-references"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Active sequence lookup by xlayer, the repeated-identical (bit-fingerprint) check (normative home: the §7.3.6 bit-identity rule, tracked by AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT; §7.3.8.6 only cross-references it), an in-band sequence-header + multi-frame-header availability store, the multi-frame-header -> sequence-header reference check (mfh_seq_header_id), and the generic frame-header reference checks (seq_header_id_in_frame_header and cur_mfh_id) are implemented, with caller-provided external HLS via ValidationOptions (default disabled; external MFH/LCR/atlas are future). Layer-configuration-record availability (§7.3.8.3: local->global LCR and sequence-header seq_lcr_id resolution incl. lcr_xlayer_map membership) and local atlas-segment availability (§7.3.8.4) are now modeled; the global atlas (§7.3.8.4 'can be available') is not flagged. The §7.3.8.1 random-access-point availability replay landed under rap-availability-replay (hls/unavailable-at-random-access-point): a RapReplayTracker records each HLS object's in-band (re)send EVENTS (temporal unit, sending extended layer, and whether that unit later proved leading) and buffers each linearly-resolved reference (sequence header via frame seq_header_id_in_frame_header and MFH mfh_seq_header_id; multi-frame header via frame cur_mfh_id; operating point set via BRT br_ops_id; global/local layer configuration record via local-LCR lcr_global_id and sequence-header seq_lcr_id; local atlas segment via LCR lcr_local_atlas_id), then at temporal-unit completion — when the unit's §7.4.1 random-access-point-ness (CLK/OLK/RAS) and leading-frame-ness (OBU_LEADING_* type-detectable) are known — fires when a reference governed by its anchor R (the reference's governing layer's most recent random access point, or the any-layer anchor for a global reference) has no (re)send that is VISIBLE-under-start-at-R (mirror lines 685-693). The qualification is ANCHOR-RELATIVE, not a single global last-good state: a (re)send S is visible iff (a) S's temporal unit == R, or (b) S's temporal unit > R AND that unit carries no leading frame in any layer (§7.3.8.1 drops whole leading temporal units) AND (c) §7.4.6 sender-decodability — S's sending layer is GLOBAL, or had a random access point in the closed interval [R, S's temporal unit] (so its coded extended layer units are decoded by then). A reference whose own temporal unit drops under start-at-R (a strictly-post-R unit carrying any leading frame, for a global referencing OBU as for a frame-bearing one) is moot (no diagnostic). (Replaces the cycle-1/2 single last_good_resend_tu scalar, which mis-promoted a (re)send in a mixed leading/random-access temporal unit for all later anchors and mis-suppressed global references in dropped leading temporal units — Codex cycle-3 findings 1-4.) The replay predicate is disjoint from the linear hls/unavailable-* checks (only linearly-available references are buffered) and from the §7.3.6 bit-identity check; a strictly-post-anchor (re)send in a leading temporal unit is not visible (the whole unit drops), an undecidable (non-LEADING_*) leading frame leaves the unit visible (sound under-approximation), a reference whose own (strictly-post-anchor) temporal unit carries any leading frame is moot (the whole unit drops — global referencing OBUs included), and dedup is per (object, random access point). External-HLS suppression is PER REFERENCED KEY (not a blanket whole-replay suppression): for an externally-declarable kind — sequence headers (ExternalHlsSet::with_sequence_header_id) and operating point sets (with_operating_point_set) — a replay finding is suppressed only when the EXACT referenced key is declared external (a Provided set that omits the key asserts it is not external, so an in-band-only object dangling at a random access point still fires); for a kind ExternalHlsSet cannot express — multi-frame headers, LCRs, atlas segments — any Provided mode keeps the blanket suppression, since such an OBU MAY exist externally unenumerated (the external-means escape: externally-provided HLS remains available until superseded). The default Disabled mode lets every replay fire. LCR (§7.3.8.3: local->global lcr_global_id and sequence-header seq_lcr_id) and local-atlas (§7.3.8.4: lcr_local_atlas_id) references ARE now replayed anchor-relative (global LCR sends are GLOBAL_XLAYER_ID-keyed, local LCR/atlas sends carry their own extended layer); the global atlas stays excluded (§7.3.8.4 'can be available' is permissive). Residuals: the §7.3.8.1 random-access-point REPLAY of film-grain (apply_grain/fgm_id) references IS now wired (rap-replay-film-grain-references): RapHlsKey::FilmGrain { slot } is recorded as a (re)send by record_film_grain (film-grain availability is monotonic, so a model sent only before a random access point and not resent is unavailable from that start — the case the linear monotonic frame-header/film-grain-model-unavailable under-reports), and frame_film_grain_reference_checks returns the linearly-available slot (Option<u8>) so observe_frame_bearing_obu buffers the reference disjoint from the linear check; the film-grain family is inexpressible by ExternalHlsSet, so any Provided mode blanket-suppresses. Proof: cargo test -p splot-validate rap_replay_film_grain (4 tests in rap_replay_foundation.rs). The quantization-matrix (using_qmatrix/qm_*) REPLAY is ALSO now wired (rap-replay-qm-references): RapHlsKey::QmLevel { level } is recorded as a (re)send by check_quantizer_matrix for each level a QM OBU makes available — every qm.levels entry AND all levels on a qm_bit_map==0 reset-to-defaults — and frame_qm_reference_checks returns the linearly-available (non-poisoned) referenced levels so observe_frame_bearing_obu buffers them. The replay records the OBU SEND and is disjoint from the linear availability/reset/poison state (no level is available from a decode start without a QM OBU send at/after it; §6.12 makes even a default matrix an in-OBU field), so reset_qm() (which governs only the linear check) does not affect replay soundness. The firing case is reachable via a level that survives a RAS reset (QmMLayerId != -1, the deferred MLayerPresenceMap arm) referenced by an INTRA_ONLY frame (which reaches setup_qm without resetting QM). Proof: cargo test -p splot-validate rap_replay_qm (5 tests in rap_replay_foundation.rs). The LINEAR §6.17.10.1/§7.3.8.8 film-grain availability check DID land (codex round, frame-header/film-grain-model-unavailable on AV2-5.18.10-FILM-GRAIN-STRUCTURES): when apply_grain == 1 and the referenced fgm_id slot has no in-band received model, it fires under ExternalHlsMode::Disabled only (ExternalHlsSet cannot express film grain, so any Provided mode suppresses). That linear check under-reports the RAP-unavailability direction (monotonic available[] never resets at a RAP); the film-grain RAP replay (above) now covers that direction, and the §6.17.10.1 fgm layer-dependency constraints landed (AV2-5.18.10-FILM-GRAIN-STRUCTURES). The QM REPLAY side now also covers that direction (above). §7.4.4's leading-temporal-unit content-identity divergence (a post-leading regular frame referencing an HLS object redefined in a leading temporal unit is non-conformant even though the object is AVAILABLE at the random access point — the anchor-relative availability check correctly stays silent because a valid random-access-point version is visible) is a documented residual, not a diagnostic: detecting it needs per-resend content identity plus post-leading reference tracking and would otherwise risk a false positive on an identical leading re-send (Codex cycle-3 finding 4 spec decision);the §7.3.8.2 first-sentence MSDO-availability-at-RAP is interop-point-gated (see AV2-5.6-MSDO) and not landed as an error; the ExternalHlsSet MFH/LCR/atlas declaration keys stay a named non-goal (partial-declaration policy already covers the escape conservatively). The MFH layer-dependency constraints (§7.3.8.7, concrete predicate §6.17.2) are enforced at the frame-header cur_mfh_id reference against the loaded in-band sequence header's §5.4.1 maps (frame-header/mfh-mlayer-dependency-missing, frame-header/mfh-tlayer-dependency-missing; diagnostics recorded on AV2-5.7-MULTI-FRAME-HEADER). The validator also records per-level quantizer-matrix availability (AV2-5.13-QUANTIZATION-MATRIX) and per-slot film-grain availability (AV2-5.14-FILM-GRAIN); the frame film-grain reference check (apply_grain/fgm_id FilmGrainPresent[ fgm_id ] == 1) now consumes the per-slot state (frame-header/film-grain-model-unavailable; see above), and the frame quantization reference check (using_qmatrix/qm_* availability) consumes the per-level state too — both the linear frame-header/qm-level-unavailable (§7.3.8.9) and the §7.3.8.1 RAP replay (above). The remaining QM residual is the §6.17.6.2 QM layer-dependency constraints (the linear frame_qm_reference_checks TODO). The §7.3.6 single-active-sequence-header rule landed under sequence-multistream-semantics as hls/multiple-active-sequence-headers: it fires when a §5.18.2 frame-confirmed activation of a different seq_header_id follows an earlier frame-confirmed activation within the same coded video sequence (no intervening CLK), suppressed only when caller-provided external HLS declares at least one sequence header (declares_any_sequence_header(); an empty or sequence-free external set cannot supply an out-of-band active header and does not suppress, matching the sibling gates) (the normative sentence lives in §7.3.6, cited on AV2-7.3.6-CODED-EXTENDED-LAYER-UNIT)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "partial"
write = "todo"
encode = "todo"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/hls_frame_header.rs::hls_frame_header_missing_sequence_header_is_flagged", "crates/splot-validate/src/validator/tests/hls_frame_header.rs::hls_frame_header_missing_mfh_is_flagged", "crates/splot-validate/src/validator/tests/hls_frame_header.rs::hls_frame_header_sequence_header_available_external_is_accepted", "crates/splot-validate/src/validator/tests/hls_syntax.rs::hls_repeated_non_identical_sequence_header_is_flagged", "crates/splot-validate/src/options.rs::tests", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::second_activation_without_clk_is_flagged", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::reactivation_across_clk_is_conforming", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::fallback_guess_then_frame_reference_is_not_flagged", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::unreferenced_extra_sequence_header_is_conforming", "crates/splot-validate/src/validator/tests/distinct_mlayer.rs::second_activation_under_external_hls_is_not_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_sequence_header_only_before_rap_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_resend_in_rap_temporal_unit_passes", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_leading_temporal_unit_resend_does_not_qualify", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_non_leading_resend_after_rap_qualifies", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_resend_after_reference_in_same_temporal_unit_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_deduplicates_per_object_per_random_access_point", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_distinct_random_access_points_each_report", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_suppressed_under_external_hls_provided", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_olk_random_access_point_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_ras_random_access_point_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_foundation.rs::rap_replay_multi_frame_header_reference_before_rap_is_flagged", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_global_reference_in_post_rap_leading_tu_is_moot", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_global_resend_in_mixed_leading_tu_not_visible_to_earlier_anchor", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_global_resend_in_mixed_leading_tu_visible_to_anchor_at_that_tu", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_resend_by_undecodable_other_layer_does_not_satisfy_reference", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_resend_by_decodable_other_layer_satisfies_reference", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_leading_tu_redefinition_does_not_invalidate_available_rap_version", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_leading_tu_redefinition_with_no_rap_version_still_fires", "crates/splot-validate/src/validator/tests/rap_replay_layers.rs::rap_replay_global_reference_in_non_leading_tu_still_fires"]
commands = ["cargo test -p splot-validate hls", "cargo test -p splot-validate frame_header", "cargo test -p splot-validate mfh_", "cargo test -p splot-validate activation", "cargo test -p splot-validate rap_replay"]
fixtures = []
diagnostics = ["hls/repeated-sequence-header-not-identical", "hls/unavailable-sequence-header", "hls/unavailable-multi-frame-header", "hls/unavailable-layer-configuration-record", "hls/unavailable-at-random-access-point", "lcr/global-lcr-unavailable", "lcr/global-xlayer-map-missing-xlayer", "atlas/local-atlas-unavailable", "frame-header/seq-header-id-out-of-range", "frame-header/cur-mfh-id-out-of-range", "sequence-state/no-active-sequence-header", "mfh/sequence-header-unavailable", "hls/external-hls-disabled", "hls/multiple-active-sequence-headers"]

[[feature]]
id = "AV2-7.3.9-LONG-TERM-REFERENCE-AVAILABILITY"
name = "Long-term reference availability"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.3.9", "6.17.2", "7.4.5"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html", "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-9", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "reference-state-and-random-access"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Child row of AV2-7.3-OBU-ORDERING. reference-state-and-random-access lands the header-observable long-term-reference part: a per-slot RefLongTermId is now MODELED in the §7.23 reference-state tracker (reference_state.rs SlotFacts.long_term_id + slot_long_term_id), grounded from a KEY frame's long_term_id_plus_1 - 1 (§5.18.2 mirror docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2 :4231-4239; -1 sentinel for every other frame type), poisoned like the rest of the buffer. The §6.17.2 RAS long_term_id_in_use(RefLongTermId[ ref_frame_idx[i] ]) == 1 requirement (mirror docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2 :4615-4616; the §7.4.5 'a RAS may reference only the long-term frames it lists' constraint) is checked via frame-header/ras-ref-long-term-id-not-in-use (recorded on AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS). RESIDUALS: the §7.3.9.1 general long-term availability (each long-term reference frame must be available, held in the same slot, prior to reference) and the RAP-CELU CLK-then-OLK first-frame-units rule with immediate/implicit_output == 0 (mirror :953-968) need the random-access-replay long-term key (not yet modeled); the §7.4.2 long-term preconditions and the §7.4.4 OLK ref_long_term_id iff-conditions (held-in-buffer-at-OLK / at-first-Regular-frame, same/dependent embedded layer) need sequential-decode buffer-membership state; the §7.4.4/§7.4.5 OrderHint < (1 << OrderHintBits) bound is NOT header-decidable (OrderHint = get_disp_order_hint() is the unwrapped value derived from reference-state DOH accumulation, also output-order-dependent — a non-goal); for max_mlayer_id == 0, reference-map reachability is complete when the modeled §7.23 state grounds the inferred refresh mask and otherwise stops honestly before ref_frame_idx. validate/decode_check stay partial."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/reference_state.rs::tests::slot_long_term_id_reads_only_valid_slots", "crates/splot-validate/src/reference_state.rs::tests::slot_facts_requires_order_hint_and_dims_but_not_long_term_id", "crates/splot-validate/src/validator/tests/reference_state.rs::validator_ras_ref_long_term_fires_when_max_mlayer_zero_derivation_is_grounded", "crates/splot-validate/src/validator/tests/reference_state.rs::validator_silent_on_ras_ref_long_term_when_slot_unknown"]
commands = ["cargo test -p splot-validate slot_long_term", "cargo test -p splot-validate ras_ref_long_term"]
fixtures = []
diagnostics = ["frame-header/ras-ref-long-term-id-not-in-use"]

[[feature]]
id = "AV2-7.4-RANDOM-ACCESS"
name = "Random access decoding"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.4.2", "7.4.4", "7.4.5", "7.3.9"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html", "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-4"]
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "reference-state-and-random-access"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Umbrella row for the §7.4 random-access decoding conformance constraints in their header-observable forms. reference-state-and-random-access lands the §7.4.5 RAS reference restriction via the per-slot RefLongTermId model and the §6.17.2 long_term_id_in_use check (frame-header/ras-ref-long-term-id-not-in-use, recorded on AV2-7.3.9-LONG-TERM-REFERENCE-AVAILABILITY / AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS), plus the §7.3.8.9 reset_qm() QmProtected discipline that the §7.4.3/§7.4.4/§7.4.5 random-access processes invoke (recorded on AV2-5.18.6-QUANTIZATION / AV2-5.13-QUANTIZATION-MATRIX). RESIDUALS (validate stays partial): the §7.4.2 long-term preconditions and the §7.4.4 OLK ref_long_term_id iff-conditions (held-in-buffer-at-OLK, at-first-Regular-frame, same/dependent embedded layer) need sequential-decode buffer-membership state not yet modeled; the §7.3.9.1 RAP-CELU CLK-then-OLK first-frame-units ordering with immediate/implicit_output == 0 (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-9 :953-968) needs CELU first-frame-unit ordering + a long-term RAP-replay key; the §7.4.4/§7.4.5 OrderHint < (1 << OrderHintBits) bound is NOT header-decidable (OrderHint = get_disp_order_hint() is the unwrapped reference-state-DOH-derived value, also output-order-dependent — a non-goal of this tranche); the no-back-reference §7.4.4 rules (regular frames after an OLK shall not reference pre-OLK info) need cross-frame reference-graph state. The §6.4.6 RAS-requires-long-term-frame-id-bits and §6.4.1 SWITCH/RAS self-contained-mlayer checks are on AV2-6.17.2-FRAME-HEADER-INFO-SEMANTICS."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/reference_state.rs::validator_ras_ref_long_term_fires_when_max_mlayer_zero_derivation_is_grounded", "crates/splot-validate/src/validator/tests/reference_state.rs::validator_silent_on_ras_ref_long_term_when_slot_unknown", "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_clears_unprotected_level_across_temporal_units", "crates/splot-validate/src/validator/tests/frame_tile_qm.rs::validator_qm_protected_reset_preserves_resent_level_in_current_temporal_unit"]
commands = ["cargo test -p splot-validate ras_ref_long_term", "cargo test -p splot-validate qm_protected"]
fixtures = []
diagnostics = ["frame-header/ras-ref-long-term-id-not-in-use", "frame-header/qm-level-unavailable"]

[[feature]]
id = "AV2-7.23-REFERENCE-FRAME-UPDATE"
name = "Reference frame update process"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.23", "6.17.2"]
sources = ["docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23", "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2"]
crate = "splot-validate"
module = "crates/splot-validate/src/reference_state.rs"
change_id = "frame-reference-state-model"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "The §7.23 reference-frame buffer state is now MODELED for intra streams (frame-reference-state-model), replacing the always-unknown FrameReferenceStateView placeholder. A per-extended-layer ReferenceStateTracker (crates/splot-validate/src/reference_state.rs) holds NUM_REF_FRAMES (16, §3 mirror docs/spec/av2/1.0.0/03-symbols.md :607) slots, each Unknown / ProvenInvalid / Valid{RefOrderHint, RefFrameWidth, RefFrameHeight}. The buffer is scoped per extended layer (obu_xlayer_id) to match the §7.3.6 CVS / CELU keying — the granularity at which a CLK starts a new coded video sequence and resets the reference buffers. UPDATE SEMANTICS (mirror docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23 :14095-14124): for each set bit i of refresh_frame_flags, slot i stores the frame's facts and RefValid[i] = (FrameType == KEY_FRAME || FrameType == SWITCH_FRAME) ? first : 1 where first is 1 only for the lowest refreshed slot (so a key/switch frame leaves only its lowest refreshed slot valid). UPDATE POINT: §7.23 runs at decode_frame_wrapup AFTER the frame is decoded, so each frame's update is DEFERRED in ValidatorContext::pending_ref_update and committed at the NEXT frame's coded-frame boundary (the segmenter-authoritative FrameBoundary::OpensNewUnit, with the no-trailing-delimiter end-of-bitstream flush in finish()) — a later frame's reference checks see the earlier frame's updates, matching the spec ordering. A FrameBoundary::ContinuesUnit (non-first tile group of the same coded frame) does not re-update. RESETS: a CLK at FirstPictureInTU (OBU_CLOSED_LOOP_KEY && startCVS) clears RefValid[i] = 0 over 0..NumRefFrames (§5.18.2 mirror docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2 :4449-4455) then applies its allFrames refresh (max_mlayer_id == 0, mirror :4431-4433) per §7.23 — so after a CLK only slot 0 is Valid, the rest ProvenInvalid. A mid-stream join (validator starts at a random access point) begins all-Unknown until the first grounded reset (sound under-approximation: an unestablished slot is never PROVEN invalid). HONEST POISONING: a frame whose refresh mask is not parsed (inter / TIP / bridge / truncated / Unknown-classed frame, or an ambiguous coded-frame boundary) poisons ALL slots to Unknown — the unknown mask could refresh any slot; per-slot Unknown is the resting state, never a guess. SEF: refresh_frame_flags = 0 (§5.18.2 :4180) so a show-existing-frame updates NO slot, but it DISPLAYS frame_to_show_map_idx, making RefValid[ frame_to_show_map_idx ] checkable. DIAGNOSTIC: frame-header/show-existing-frame-invalid-slot (§6.17.2) fires when the modeled buffer PROVES RefValid[ frame_to_show_map_idx ] == 0; a poisoned (Unknown) slot drops to silence (the Unknown invariant). The check runs only when the frame's core resolved against its active sequence header. No external-HLS suppression: reference-frame buffers are written only in-band by the §7.23 process, so the modeled state is always validator-derived. THREADING: the modeled FrameReferenceStateView (via the new FrameReferenceStateView::from_slots constructor) is threaded into the validator's core parse (frame_core_against_referenced_header) — this is FORWARD PLUMBING for the §5.18 INTER reference paths (explicit reference map, frame_size_with_refs, primary-ref), which are not yet parsed: no §5.18 INTRA branch consumes reference_state today, so the threading has no behavioral effect on the intra path (verified: only FrameReferenceStateView's fields/input are read in splot-core, no parse branch). The inspector keeps unknown() by design (no cross-OBU state). RESIDUALS (keeps validate/decode_check partial): the §5.18 inter reference-path CONSUMERS of the modeled state (explicit_ref_frame_map / ref_frame_idx RefValid checks §6.17.2 :4605-4607, frame_size_with_refs, primary_ref_frame range, use_bru OrderHint/dim constraints §6.17.2 :4587-4596) await inter-path parsing (AV2-5.18.2-FRAME-HEADER-INFO inter path); the §7.3.9 long-term reference availability is its own row (AV2-7.3.9-LONG-TERM-REFERENCE-AVAILABILITY) and now consumes the per-slot RefLongTermId this model tracks: reference-state-and-random-access extends SlotFacts with long_term_id (RefLongTermId[i] = LongTermId, §7.23 mirror :14113), grounded from a KEY frame's long_term_id_plus_1 - 1 (-1 sentinel otherwise) and queried via slot_long_term_id (Some only for a PROVEN-valid slot); the §7.23 motion-vector / CDF / grain / CCSO / segment-id stores remain out of scope for the validator's reference-state checks; the §6.17.2 derive_sef_order_hint already-shown / RefImplicitOutputFrame / RefImmediateOutputFrame SEF constraints (mirror docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2 :4375-4380) need output-frame-buffer / shown state this phase does not model."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/reference_state.rs::tests", "crates/splot-validate/src/reference_state.rs::tests::refresh_stores_facts_into_set_slots_only", "crates/splot-validate/src/reference_state.rs::tests::key_switch_first_rule_validates_only_lowest_refreshed_slot", "crates/splot-validate/src/reference_state.rs::tests::clk_reset_invalidates_then_refreshes", "crates/splot-validate/src/reference_state.rs::tests::clk_reset_with_all_frames_mask_validates_only_lowest", "crates/splot-validate/src/reference_state.rs::tests::sef_does_not_update", "crates/splot-validate/src/reference_state.rs::tests::poison_all_makes_every_slot_unknown", "crates/splot-validate/src/reference_state.rs::tests::view_into_reflects_slot_validity", "crates/splot-validate/src/reference_state.rs::tests::apply_never_panics", "crates/splot-validate/src/reference_state.rs::tests::refresh_validates_exactly_set_bits", "crates/splot-validate/src/reference_state.rs::tests::poison_all_dominates", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_sef_proven_invalid_slot_fires", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_sef_valid_slot_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_sef_poisoned_slot_drops_to_silence", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_inter_frame_poisons_then_sef_is_silent", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_sef_invalid_then_inter_then_sef_silent_proves_poison_lifetime", "crates/splot-validate/src/validator/tests/reference_state.rs::ref_state_clk_reset_across_cvs_revalidates_only_slot_zero", "crates/splot-validate/src/reference_state.rs::tests::slot_long_term_id_reads_only_valid_slots", "crates/splot-validate/src/reference_state.rs::tests::slot_facts_requires_order_hint_and_dims_but_not_long_term_id"]
commands = ["cargo test -p splot-validate reference_state", "cargo test -p splot-validate ref_state"]
fixtures = []
diagnostics = ["frame-header/show-existing-frame-invalid-slot"]

[[feature]]
id = "AV2-7.7-GET-REF-FRAMES"
name = "Get ref frames process"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["7.7", "5.18.2", "5.18.3.1", "6.17.2"]
sources = ["docs/spec/av2/1.0.0/07-decoding-process.md#s-7-7", "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2", "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-3-1", "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/get_ref_frames.rs"
change_id = "decode-inter-ref-frame-map"
tracking_issue = ""
owner = "core"
risk = "high"
notes = "The §7.7 get_ref_frames() implicit reference-map ranking is MODELED as a typed, total, panic-free derivation (crates/splot-core/src/headers/frame/get_ref_frames.rs, get_ref_frames). It implements the full §7.7 algorithm on EXPLICIT per-slot inputs (GetRefFramesInput / RefSlot): the distinct-reference detection first_slot_with_ref (RefValid + RefCounter dedup), the resolution gate valid_ref_frame_size (the §7.7 2*FrameWidth >= RefFrameWidth .. FrameWidth <= 16*RefFrameWidth window), the per-reference scoring (the §3 Dist_Score_Lookup[7] = {0,64,96,112,120,124,126} table, the maxDisp > OrderHint vs decay arms with DIST_WEIGHT_BITS == 6 and DECAY_DIST_CAP == 6, the refRatio = FloorLog2(RefFrameWidth*RefFrameHeight) penalty, tDist = Abs(dispDiff) + obu_mlayer_id - RefMLayerId[i]), get_relative_dist (§5.18.3.1, the RESTRICTED_OH sentinel arms + Clip3(-127,127,a-b)), the duplicate-score rejection new_score_or_dist, the over-selection drop get_unmapped_ref (NRanked > REFS_PER_FRAME == 7), the exact spec bubble_sort_ref_scores (ScoresScore[j] > ScoresScore[j+1]), the NumTotalRefs = Min(NRanked, ActiveNumRefFrames) cut (ActiveNumRefFrames = Min(REFS_PER_FRAME, NumRefFrames), §5.4.6 mirror :963), and the trailing restricted-frame append (the checkRes && !IsBridge loop). Derived from the §7.7 spec text, never from AVM source (AVM av2/common/pred_common.c was read only to CONFIRM the constants match; no code/table/comment copied). WIRING (crates/splot-core/src/headers/frame/inter.rs, derive_implicit_ref_map): the §5.18.2 inter control parser's get_ref_frames(0) (mirror :4607) and get_ref_frames(1) (mirror :4647) calls now consult §7.7 instead of stopping with InterStop::UnmodeledDerivation, GATED to the AT-MOST-ONE-VALID-REFERENCE case the modeled FrameReferenceStateView (RefValid / RefOrderHint / dims) can resolve EXACTLY — with <= 1 proven-valid slot the score/sort/drop/restricted machinery is irrelevant, so the deterministic single-spatial-layer defaults (each valid slot a distinct RefCounter, AllowedFrames == -1, layer-dependency maps == 1) yield the EXACT §7.7 answer (not a hardcoded [0]); a richer reference state (>= 2 valid slots, or no modeled view) stays an honest UnmodeledDerivation stop. The two §7.7 calls run with checkRes == 0 (before frame_size) then checkRes == 1 (after, applying the resolution gate + restricted append), faithfully reproducing the spec's two-call sequence. This is PARSE-LEVEL ONLY: no decode output changes (the inter frame still won't reconstruct — mode_info / MV / motion-compensation are later bricks). The minimal syn-key-inter-64x64 inter frame (CLK key refresh_frame_flags == 255 leaves only slot 0 RefValid per the §7.23 first rule; inter frame OrderHint == 1, explicitRefFrameMap == 0) now advances PAST UnmodeledDerivation to InterStop::ReachedSharedTail with NumTotalRefs == 1, ref_frame_idx == [0] (proven on the real fixture bytes). DEFERRED (keeps types/parse/decode_check partial): the >= 2-valid-reference ranking needs the unmodeled RefBaseQIdx / RefCounter / RefMLayerId / RefTLayerId / per-frame AllowedFrames / TLayerDependencyMap / MLayerDependencyMap state (FrameReferenceStateView carries only RefValid / RefOrderHint / dims today); the inter SHARED tail past ReachedSharedTail (tile_info() / quant / segmentation / inter coding modes / global motion) and the §5.20 inter tile-group body remain future work; the inspector's unknown() reference view means standalone `splot inspect` still shows the UnmodeledDerivation stop (no cross-OBU state) — the modeled advance is realized in the validator path and the unit tests."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/get_ref_frames.rs::tests", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::minimal_single_valid_slot_after_key_yields_one_ref", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::no_valid_slot_yields_zero_refs", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::equal_ref_counter_collapses_to_first_slot", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::two_distinct_refs_rank_by_score", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::active_num_ref_frames_caps_the_result", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::check_res_drops_resolution_incompatible_reference", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::restricted_frame_appended_only_with_check_res", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::allowed_frames_mask_excludes_slot", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::get_relative_dist_clamps_and_handles_sentinels", "crates/splot-core/src/headers/frame/get_ref_frames.rs::tests::floor_log2_u64_matches_spec", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_one_valid_slot_reaches_shared_tail", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_no_valid_slot_reaches_shared_tail_with_zero_refs", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_two_valid_slots_stops_unmodeled", "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_stops_unmodeled", "crates/splot-core/src/headers/frame/inter.rs::tests::frame_header_core_inter_implicit_map_reaches_shared_tail_on_fixture", "crates/splot-core/src/headers/frame/inter.rs::tests::frame_header_core_inter_implicit_map_two_valid_slots_stops_unmodeled"]
commands = ["cargo test -p splot-core get_ref_frames", "cargo test -p splot-core headers::frame::inter", "cargo test -p splot-core frame_header_core_inter_implicit_map"]
fixtures = ["tests/conformance/vectors/valid/syn-key-inter-64x64.ivf"]
diagnostics = []

[[feature]]
id = "AV2-9-ADDITIONAL-TABLES"
name = "Additional spec tables (codegen)"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["9"]
sources = ["https://av2.aomedia.org/v1.0.0/attachments/all_tables.h"]
crate = "splot-core"
module = "crates/splot-core/src/tables/"
change_id = "gen-tables-additional-tables"
tracking_issue = ""
owner = "core"
risk = "medium"
notes = "Code-generated by `cargo xtask gen-tables` from the committed, sha256-pinned § 9 attachment docs/spec/av2/1.0.0/attachments/all_tables.h. 240 tables are emitted as typed i32 arrays across 7 § 9.2-9.8 modules (conversion, cdf, quantizer, warp_filter, transform_1d, secondary_transform, loop_restoration): 234 fully numeric tables, the two §9.2 `BLOCK_*` symbolic partition-size tables (`H_Partition_Midsize`, `Partition_Subsize`) resolved by the narrow block-size symbol map, the three §9.2 `TX_*` symbolic transform-size tables (`Adjusted_Tx_Size`, `Tx_Size_Sqr`, `Tx_Size_Sqr_Up`) resolved by the AV2 §6.19.6.1 TxSize ordinal map, and the §9.2 `Mode_To_Txfm` TxType-valued table resolved by the AV2 §3 TxType ordinal map. The §9.4 `quantizer`, §9.6 `transform_1d`, §9.7 `secondary_transform`, and §9.8 `loop_restoration` modules are emitted into the dependency-free `splot-tables` crate (so `splot-recon` can consume them without depending on `splot-core`, INFRA-SHARED-SPEC-TABLES); `loop_restoration` is also emitted into `splot-core::tables` to preserve existing core consumers, while conversion/cdf/warp-filter remain core-only. The drift check `gen-tables --check` runs in `cargo xtask ci`. RESIDUAL: 5 tables with unresolved symbolic element values are explicitly allowlisted and skipped (need broader AV2 enum/reserved-value maps, out of scope): Max_Tx_Size_Rect (TxSize enum-valued but not yet needed by decode), Size_To_Tx_Type_Group_Vert_And_Horz, Size_To_Tx_Type_Group_Vert_Or_Horz, Tile_Area_Scaling_Factor, and Tile_Width_Scaling_Factor (`reserved` placeholders). Never hand-transcribe table contents. The §8.2 symbol decoder foundation consumes generated §9.2 `Prob_Inc` and `Para_Adjustment_List`; the decoder partition-size table boundary consumes generated §9.2 `Partition_Subsize` and `H_Partition_Midsize`; `DECODE-TX-SIZE-SYMBOLIC-TABLES` makes the generated TxSize tables available for future coefficient wrappers; `DECODE-MODE-TO-TXFM-SYMBOLIC-TABLE` makes generated `Mode_To_Txfm` available for future `compute_tx_type`; `RECON-PC-WIENER-CLASSIFICATION` consumes generated §9.8 `Pc_Wiener_Lut_To_Class` through `splot-tables`; broader §9 table consumers remain future work."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "partial"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/tests/tables_spot.rs",
    "crates/splot-tables/tests/tables_spot.rs",
    "xtask/src/gen_tables.rs::tests",
]
commands = [
    "cargo xtask gen-tables",
    "cargo xtask gen-tables --check",
    "cargo test -p splot-core --test tables_spot",
    "cargo test -p splot-tables --test tables_spot",
]
fixtures = ["docs/spec/av2/1.0.0/attachments/all_tables.h"]
diagnostics = []

[[feature]]
id = "AV2-A-PROFILES"
name = "Annex A profile constraints"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["Annex A"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/annex_a.rs"
change_id = "annex-a-profile-newtype"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Landed (static subset): the Annex A.2 Table A.1 profile value-space checks on the activated sequence header — annex-a/profile-reserved (seq_profile_idc 5-30, also applied to OPS-signaled ops_seq_profile_idc per the §6.10.4 OPS-derived mapping), annex-a/profile-chroma-format-mismatch (chroma_format_idc outside the profile's set; profile 31 permits all four formats), annex-a/profile-bit-depth-mismatch (defensive; the parsed bit_depth_idc only models 0/1, so unreachable today). The table data is transcribed verbatim from the mirror in crates/splot-validate/src/annex_a.rs (Tables A.1, A.6, and the IOP helper, each cell-verified with spot-value tests). The Table A.4 interoperability-point OBU-presence checks (annex-a/msdo-required-for-iop, annex-a/msdo-prohibited-for-iop the documented defensive arm, annex-a/lcr-required-for-iop) — descoped from PR #46's skeleton because correct semantics need MSDO aggregate-profile state, LCR activation state, and §7.3.6-correct per-TU attribution — now LAND under msdo-global-lcr-agreement with all those requirements: a whole-coded-(multistream-)video-sequence IOP window (flushed at the next CVS start / EOS) whose interoperability point comes from the MSDO's multistream_profile_idc (Table A.1) when an MSDO is present, else from frame-confirmed activated headers; only *activated* global LCRs (resolved via the association chain) satisfy the global-LCR arms; §7.3.6 per-TU attribution commits each temporal unit's pending facts at temporal-unit completion so a pre-CLK MSDO/LCR in a CLK-bearing temporal unit seeds the NEW CVS; same-id CLK reactivations re-seed the window from the active confirmed header; Table A.3 definition-order extended-layer counting (MSDO num_streams, then activated-global-LCR LcrMaxNumXLayerCount, then observed distinct xlayers); and suppression under any Provided external HLS. The Table A.3 interoperability-point layer-budget bound now LANDS under annex-a-iop-layer-budget (annex-a/layer-budget-exceeds-iop): evaluate_annex_a_iop_window (context/annex_a_iop.rs) checks, per the window's table-determined IOP, the Number of Extended Layers (<= 4), the Number of Embedded Layers (IOP0: 1, IOP1: 2, IOP2: 3), and the Extended-and-Embedded combination (E && M forbidden for IOP0/IOP1, permitted for IOP2) — mirror lines 125-170. The extended/embedded counts are conservative LOWER bounds (under-counted when activations are missing, never over-counted), so a count exceeding its limit is a proven violation; the IOP is table-determined (reserved/Configurable/disagreeing profiles already return). Suppressed under any Provided external HLS with the rest of the IOP window. Proof: cargo test -p splot-validate annex_a (5 budget tests in annex_a_iop.rs). The profile newtype now LANDS under annex-a-profile-newtype (types=done): ProfileIdc (crates/splot-core/src/headers/sequence.rs) is an enum over the Annex A.2 Table A.1 value space (Main420Ip0..Main444Ip1 / Reserved(u8) for 5-30 / Configurable for 31), with from_bits/get round-tripping every 5-bit value, is_reserved/is_configurable helpers, and variants declared in idc order so the derived Ord matches the raw value order. The MSDO multistream_profile_idc, OPS ops_seq_profile_idc, and LCR lcr_seq_profile_idc fields are now ProfileIdc too (consumers extract .get() at the boundary, the ChromaFormatIdc pattern). The decode runtime now applies Table A.1 admission to the full profile/chroma value space: Configurable profile 31 reaches decode for every legal chroma format, reserved and illegal fixed-profile/chroma pairs are typed malformed-source errors, and the pinned 16x16 monochrome profile-31 AVM fixture matches byte-for-byte at one and eight threads. Remains (partial): validator-side Table A.5 implicit multi-sequence-configuration derivation and the Table A.3 'Number of Layers' (sum across singlestreams) bound (a named residual — the per-singlestream sum is not tracked); the one profile-31 oracle does not claim exhaustive Annex A differential coverage."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/annex_a.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop0_two_xlayers_without_msdo_is_flagged", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop2_requires_global_lcr_unactivated_does_not_satisfy", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop2_requires_global_lcr_activated_satisfies", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop_window_seeds_pre_clk_msdo_to_new_cvs", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop_same_id_reactivation_seeds_new_window", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop_window_late_tu_second_xlayer_counts", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop_declared_count_precedence_over_observed", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop_window_suppressed_under_external_hls", "crates/splot-validate/src/annex_a.rs::tests::config_idc_profile_sets_match_table_a6", "crates/splot-validate/src/annex_a.rs::tests::interoperability_points_match_table_a1", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop0_more_than_one_embedded_layer_exceeds_budget", "crates/splot-validate/src/validator/tests/annex_a_iop.rs::annex_a_iop1_extended_and_embedded_combination_exceeds_budget", "crates/splot-core/src/headers/sequence.rs::tests::profile_idc_round_trips_every_5_bit_value", "crates/splot-core/src/headers/sequence.rs::tests::profile_idc_classifies_table_a1_values", "crates/splot-core/src/headers/sequence.rs::tests::profile_idc_ord_matches_raw_value_order", "crates/splot-core/src/headers/sequence.rs::tests::profile_idc_identity_is_canonical_under_misconstruction", "crates/splot-decode/src/pipeline/frame_lifecycle.rs::sequence_profile_tests", "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest"]
commands = ["cargo test -p splot-validate annex_a", "cargo test -p splot-core profile_idc", "cargo test -p splot-decode sequence_profile_tests --locked", "cargo xtask decoder-fixtures verify", "cargo test -p splot-cli --test decoder_oracle --locked"]
fixtures = ["tests/conformance/vectors/valid/syn-profile31-mono-intra-16x16.ivf"]
diagnostics = ["annex-a/profile-reserved", "annex-a/profile-chroma-format-mismatch", "annex-a/profile-bit-depth-mismatch", "annex-a/msdo-required-for-iop", "annex-a/msdo-prohibited-for-iop", "annex-a/lcr-required-for-iop", "annex-a/layer-budget-exceeds-iop", "decode/malformed-source"]

[[feature]]
id = "AV2-A-LEVELS-TIERS"
name = "Annex A level and tier constraints"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["Annex A"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/annex_a.rs"
change_id = "annex-a-profile-level-skeleton"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Landed (static subset): the Annex A.4 level/tier value-space checks (annex-a/level-reserved on the activated seq_level_idx and on observed ops_level_idx for reserved 22-30; annex-a/high-tier-below-4-0 warning, sourced from the Table A.9 NOTE and unreachable in conformant syntax since seq_tier is only signaled for level_idx > 3) plus the Annex A.4 static level-limit checks on the parsed intra frame path (annex-a/frame-size-exceeds-level for MaxPicSize/MaxHSize/MaxVSize, annex-a/frame-size-below-minimum for the >= 16 rule, annex-a/tile-count-exceeds-level for MaxTiles/MaxTileCols), all skipped at seq_level_idx 31 and for reserved levels. The Table A.7/A.8/A.9 columns (LevelIdx 0-21 -> MaxPicSize, MaxHSize/MaxVSize, MaxTiles, MaxTileCols) are transcribed verbatim from the mirror in crates/splot-validate/src/annex_a.rs with per-row citations; rate columns are NOT transcribed. Remains (partial): the rate-based level constraints (MaxDisplayRate/MaxDecodeRate/MaxHeaderRate, CompressedSize, FrameSymbolCount, MaxLevelRefFrames, buffer model — owned by the Annex E change), the per-tile TileWidth/TileHeight scaling-factor constraints (TODO(spec: AV2-A-LEVELS-TIERS) in check_active_sequence_limits), the MultiStreamDecoderMode == 1 substream level scaling and the Table A.4 layer-budget bound (the IOP 0/1 combination flag must be 0; a TODO(spec: AV2-A-LEVELS-TIERS) in evaluate_annex_a_iop_window — the Table A.4 IOP-presence checks themselves landed on AV2-A-PROFILES under msdo-global-lcr-agreement), the § 6.10.4 per-frame conformance against each OPS-advertised ops_level_idx (the static level limits run only against the activated seq_level_idx; running them against operating-point levels needs operating-point-to-frame mapping the validator does not model yet, so the planned annex-a/frame-exceeds-ops-level diagnostic is backlogged — see the Planned diagnostics backlog in docs/CONFORMANCE.md and the TODO(spec: AV2-A-LEVELS-TIERS) in check_ops_level_tier_value_space), still-picture-gated constraints, and no AVM differential oracle has been run (avm_diff pending)."

[feature.status]
mapped = "done"
types = "todo"
parse = "not-applicable"
validate = "partial"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/annex_a.rs::tests", "crates/splot-validate/src/validator/tests/mod.rs"]
commands = ["cargo test -p splot-validate annex_a"]
fixtures = []
diagnostics = ["annex-a/level-reserved", "annex-a/high-tier-below-4-0", "annex-a/frame-size-exceeds-level", "annex-a/frame-size-below-minimum", "annex-a/tile-count-exceeds-level"]

[[feature]]
id = "AV2-E-DECODER-MODEL"
name = "Annex E decoder model constraints"
category = "normative"
kind = "bitstream-semantics"
spec_sections = ["Annex E"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-validate"
module = "crates/splot-validate/src/checks/mod.rs"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "validator"
risk = "high"
notes = "Decoder-model checks depend on timing, buffer-removal, and stream-level state."

[feature.status]
mapped = "done"
types = "todo"
parse = "not-applicable"
validate = "todo"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "todo"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

# ---------------------------------------------------------------------------
# Encoder roadmap (strategy is not always a direct spec section)
# ---------------------------------------------------------------------------

[[feature]]
id = "ENC-BITSTREAM-WRITER"
name = "Bitstream writer foundation"
category = "encoder"
kind = "writer"
spec_sections = ["8.2.2", "8.2.3", "8.2.4", "8.2.5", "8.2.6", "9.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/write/mod.rs"
change_id = "bit-writer-primitives"
tracking_issue = ""
owner = "encoder"
risk = "high"
notes = "BitWriter (crates/splot-core/src/write/) inverts every BitReader primitive (f(n), su, uvlc, svlc, le, leb128, ns, rg, and zero-pad byte alignment), proven by read(write(x)) == x property tests; landed by the bit-writer-primitives change. The maintainer lifted the VALIDATOR-ROADMAP writer fence (2026-06-14). The OBU-header, §5.2.3 trailing-bits, and Annex B OBU framing writers landed (obu-header-and-size-writer; crates/splot-core/src/write/obu.rs + BitWriter::write_trailing_bits). The complete §5.4 sequence_header_obu() body is now inverted (seq-header-writer-general / seq-header-writer-configs / seq-header-writer-tile; the composing pub fn write_sequence_header in crates/splot-core/src/write/seq_tile.rs — see AV2-5.4-SEQUENCE-HEADER write=done). The §5.18 frame-header, §5.19/§5.20.1 tile-group (structure + payload + first-group composer), and §5.17 metadata OBU writers all landed (see their AV2-* rows). The UNIFIED COMPLETE-OBU WRITER DISPATCH landed (obu-writer-dispatch; write_complete_obu / write_obu_payload in crates/splot-core/src/write/dispatch.rs — the inverse of dispatch_obu_payload + finish_obu_payload: the typed body plus the obu_extension_flag/trailing_bits tail, dispatching over ParsedObu; the 5 written types (temporal-delimiter, sequence-header, padding, metadata short/group) delegate to their writers, the other 9 return WriteError::Unimplemented{feature}, and a header/payload obu_type mismatch is rejected with ObuTypePayloadMismatch). AV2 v1.0.0 Annex B is flat (write_annexb_obu + a loop, no temporal/frame-unit hierarchy) and the IVF write helpers exist (AV2-IVF-CONTAINER write=done), so the container muxers are effectively done. The ROUNDTRIP-AND-FUZZ HARNESS landed (writer-roundtrip-harness; write::roundtrip_obu + recover_roundtrip_passthrough in crates/splot-core/src/write/roundtrip.rs parse->write->reparse-check the dispatch, recovering the opaque passthrough — the real obu_padding_byte run for padding, a modeled-length zero-fill for the metadata blobs since their values are not modeled — and reframing via write_complete_obu + the Annex B size prefix so the global-xlayer metadata-group branch round-trips; driven over arbitrary bytes by the roundtrip_obu_bytes fuzz target). The CROSS-TOOL writer->splot-validate agreement test landed (writer-validate-crosstool; crates/splot-validate/tests/writer_roundtrip_conformance.rs — re-emits the three conformant writable-type fixtures (padding/metadata-short/metadata-group) through write_complete_obu and asserts the re-emission is byte-exact AND validates with zero error diagnostics; lives in splot-validate, which already depends on splot-core, so the writer never depends on the validator). ALL 9 of the previously-unwritten OBU-type body writers landed: §5.12 buffer_removal_timing_obu() (buffer-removal-timing-writer; AV2-5.12-BUFFER-REMOVAL-TIMING write=done), §5.6 multistream_decoder_operation_obu() (msdo-writer; AV2-5.6-MSDO write=done), §5.10 operating_point_set_obu() (operating-point-set-writer; AV2-5.10-OPERATING-POINT-SET write=done), §5.15 content_interpretation_obu() (content-interpretation-writer; AV2-5.15-CONTENT-INTERPRETATION write=done), §5.14 film_grain_obu() (film-grain-writer; AV2-5.14-FILM-GRAIN write=done, the writer canonicalizes the lossy bit-width syntax), §5.9 atlas_segment_info_obu() (atlas-segment-writer; AV2-5.9-ATLAS-SEGMENT write=done), and §5.7 multi_frame_header_obu() (multi-frame-header-writer; write_multi_frame_header — see AV2-5.7-MULTI-FRAME-HEADER write=done — the dispatch routes ParsedObu::MultiFrameHeader to it instead of Unimplemented; reuses write_seg_info), and §5.8 layer_config_record_obu() (layer-config-record-writer; write_layer_config_record — see AV2-5.8-LAYER-CONFIG-RECORD write=done — the dispatch routes ParsedObu::LayerConfigurationRecord to it + the generic extensible tail; branches on the Global/Local variant, inverts the §5.8.1-5.8.9 nest with its byte_alignment sites and the length-bounded lcr_global_payload filler), and §5.13 quantizer_matrix_obu() (quantization-matrix-writer; write_quantizer_matrix — see AV2-5.13-QUANTIZATION-MATRIX write=done — the LAST OBU-type body writer; the dispatch routes ParsedObu::QuantizationMatrix to it + the generic non-extensible tail; canonicalizes the lossy model to the long form, every skip flag 0 + one svlc delta per cell in 2D diagonal scan order). **The complete-OBU dispatch now writes ALL 14 OBU payload types — every ParsedObu variant has a body writer and the dispatch no longer returns WriteError::Unimplemented.** The non-first frame_header_copy() tile-group continuation composer also landed (tile-group-continuation-writer; write_tile_group_continuation_obu — see AV2-5.19-TILE-GROUP — emits is_first_tile_group=0 + frame_header_present_flag + the verbatim frame_header_copy() bits + structure + payload, with tg_start not restricted to 0). The generated writer-coverage matrix landed (writer-coverage-doc; docs/spec-coverage-writer.md via `cargo xtask writer-coverage`, drift-guarded by check-feature-status — see XTASK-FEATURE-STATUS): one row per writable feature with its write maturity + module. The generic AV2 §8.2 SymbolEncoder primitive now exists in crates/splot-core/src/symbol_encoder.rs: write_bool, write_literal, write_symbol with shared CDF validation/update, deterministic output, explicit byte and primitive-operation limits (including high-skew zero-bit symbol streams), exit_symbol-valid finalization, and encode->SymbolDecoder round-trip/property/fuzz evidence via symbol_encoder tests and symbol_encoder_bytes. Finalization is correctness-first and currently uses a bounded O(range * operations) final-value search; TODO(spec: ENC-BITSTREAM-WRITER) tracks replacing it with direct carry-propagating arithmetic finalization before large coded tile payload use. Remaining writer surface: the INTER first-group tile-group composer (blocked on the inter frame-header writer), §8.3 syntax-element CDF selection for encoder-owned token streams, and the §5.20 decode_tile() coded tile body. The CLI does not expose an encode command."

[feature.status]
mapped = "partial"
types = "partial"
parse = "not-applicable"
validate = "not-applicable"
write = "partial"
encode = "todo"
decode_check = "todo"
tests = "partial"
avm_diff = "pending"
perf = "todo"

[feature.proof]
tests = ["crates/splot-core/src/write/bit_writer.rs::tests", "crates/splot-core/src/write/bit_writer.rs::proptests", "crates/splot-core/src/write/obu.rs::tests", "crates/splot-core/src/write/dispatch_tests.rs::tests", "crates/splot-core/src/write/roundtrip_tests.rs::tests", "crates/splot-core/src/symbol_encoder.rs::tests", "crates/splot-core/src/symbol_encoder.rs::proptests", "crates/splot-validate/tests/writer_roundtrip_conformance.rs", "fuzz/fuzz_targets/symbol_encoder_bytes.rs"]
commands = ["cargo test -p splot-core write", "cargo test -p splot-core symbol_encoder --locked", "cargo test -p splot-core symbol --locked", "cargo test -p splot-validate --test writer_roundtrip_conformance", "cargo check --manifest-path fuzz/Cargo.toml --bins --locked", "cargo +nightly fuzz run roundtrip_obu_bytes", "cargo +nightly fuzz run symbol_encoder_bytes"]
fixtures = ["tests/fixtures/padding.av2", "tests/fixtures/metadata-short.av2", "tests/fixtures/metadata-group.av2", "fuzz/fuzz_targets/symbol_encoder_bytes.rs"]
diagnostics = []

[[feature]]
id = "ENC-Y4M-INPUT"
name = "Y4M input reader integration"
category = "encoder"
kind = "encoder-api"
spec_sections = []
sources = []
crate = "splot-encode"
module = "crates/splot-encode/src/frame.rs"
change_id = "encoder-frame-input-views"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "Input format plumbing; not normative AV2 syntax. The encoder has validated borrowed 8-bit YUV420 frame input views over splot-recon PlaneRef, typed frame id/timestamp metadata, explicit RetainedFrame sharing over SharedFrame, and Context::send_frame copies the visible pixels and metadata into its bounded queue. Context::receive_packet returns a coded Packet for the supported 64x64 all-128 8-bit 4:2:0 input; unit/property, fuzz, and decoder-backed CLI coverage exercise the input boundary and successful packet path. Residual: no Y4M reader, no 10-bit input view, and no lookahead or motion-analysis pipeline."

[feature.status]
mapped = "partial"
types = "partial"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "todo"
decode_check = "not-applicable"
tests = "partial"
avm_diff = "not-applicable"
perf = "todo"

[feature.proof]
tests = ["crates/splot-encode/src/frame.rs::tests", "fuzz/fuzz_targets/encoder_frame_input_views_bytes.rs"]
commands = ["cargo test -p splot-encode --locked", "cargo check --manifest-path fuzz/Cargo.toml --bins", "cargo +nightly fuzz run encoder_frame_input_views_bytes"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-CONTEXT-STATE-MACHINE"
name = "Encoder context state machine"
category = "encoder"
kind = "encoder-api"
spec_sections = []
sources = []
crate = "splot-encode"
module = "crates/splot-encode/src/context.rs"
change_id = "encoder-context-state-machine"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "Non-normative encoder API lifecycle plumbing. Context exposes accepting/draining/finished/failed state, operation-specific send/receive/flush statuses, typed invalid-state errors, retryable queue-full backpressure without consuming the caller's borrowed frame, validation before queueing, and fixed bounded accounting over owned visible-pixel copies plus metadata. receive_packet returns a real AV2 Annex B coded Packet for the supported 64x64 all-128 8-bit 4:2:0 input; unsupported frames are retired without a packet, and broader encoding remains out of scope."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/context.rs::tests", "fuzz/fuzz_targets/encoder_context_state_machine_bytes.rs", "crates/splot-cli/tests/encode_decode_roundtrip.rs::context_encodes_all_128_frame_to_a_packet_that_decodes_to_all_128"]
commands = ["cargo test -p splot-encode --locked", "cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo check --manifest-path fuzz/Cargo.toml --bins --locked", "cargo +nightly fuzz run encoder_context_state_machine_bytes -- -runs=128", "cargo xtask check-fuzz-targets"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-INTRA-MODE-SYMBOL-EMISSION"
name = "Encoder intra-mode symbol emission"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.5.5", "8.3.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/intra_mode_emission.rs"
change_id = "encoder-intra-mode-symbol-emission"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "The production DC general-intra path emits y_mode_set=0 and y_mode_index=0 at the tile-origin context through direct IntraModeToken values. The mode tokens are composed into every supported packet trace and are protected by token-construction tests plus decoder-backed packet oracles; the obsolete standalone self-decoder and syntax metadata were removed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/intra_mode_emission.rs::tests::emits_dc_pred_luma_tokens", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode intra_mode_emission --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-UV-MODE-SYMBOL-EMISSION"
name = "Encoder chroma uv_mode symbol emission"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.5.6", "8.3.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/intra_mode_emission.rs"
change_id = "encoder-uv-mode-emission"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "The production DC general-intra path emits uv_mode=0 at the non-directional context through a direct IntraModeToken. It is composed before residual tokens in every supported packet and protected by the constructor test plus decoder-backed packet oracles; obsolete standalone roundtrip machinery was removed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/intra_mode_emission.rs::tests::emits_dc_pred_chroma_token", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode intra_mode_emission --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-INTRA-BLOCK-MODE-TRACE"
name = "Encoder intra-block mode trace composition"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.5.3", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/block_symbol_trace"
change_id = "encoder-intra-block-mode-trace"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "compose_minimal_intra_dc_block_mode_trace directly combines the live luma and chroma DC mode tokens in y_mode_set, y_mode_index, uv_mode order. Every supported general-intra trace consumes this prefix; composition tests and decoder-backed packet oracles protect its order."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/skip.rs::tests::composes_general_skip_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode general_intra_trace::skip::tests::composes_general_skip_block_trace_in_order --locked", "cargo test -p splot-cli --test encode_decode_roundtrip encoder_skip_ivf_decodes_to_a_flat_128_frame --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-INTRA-BLOCK-TRACE-BYPASS-LITERAL"
name = "Encoder block-symbol bypass-literal token"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["8.2", "5.20.7.27", "5.20.7.28"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/block_symbol_trace"
change_id = "encoder-block-trace-bypass-literal"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "BlockSymbolToken::Bypass remains production syntax for chroma and non-DC coefficient sign bits. encode_block_symbol_trace writes these values with SymbolEncoder::write_literal; asymmetric decoder-backed U, V, two-nonzero, and 2-D packet oracles protect value and ordering. The obsolete self-roundtrip decoder and unused Golomb compatibility path were removed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_coded_chroma_ivf_decodes_to_a_flat_127_u_frame", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_2d_ivf_decodes_to_a_diagonal_gradient_luma"]
commands = ["cargo test -p splot-encode general_intra_trace::chroma::tests::composes_general_coded_chroma_block_trace_in_order --locked", "cargo test -p splot-cli --test encode_decode_roundtrip encoder_coded_chroma_ivf_decodes_to_a_flat_127_u_frame --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-BLOCK-SYMBOL-ENCODE"
name = "Encoder block-symbol entropy-coding entry point"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["8.2", "5.20.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/block_symbol_trace"
change_id = "encoder-block-symbol-encode"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "encode_block_symbol_trace is the production entropy-coding entry point for the supported general-intra tile body. It routes partition, mode, coefficient, and bypass tokens to the live default CDF rows or literal writer, finalizes the SymbolEncoder, and feeds every emitted IVF and Context packet. Decoder-backed CLI oracles replace the deleted internal self-decoder proof."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/skip.rs::tests::skip_tile_data_is_deterministic", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode general_intra_trace::skip::tests::skip_tile_data_is_deterministic --locked", "cargo test -p splot-cli --test encode_decode_roundtrip encoder_skip_ivf_decodes_to_a_flat_128_frame --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-WRITER-INPUT-FRAMING"
name = "Encoder writer-input single-tile framing constructor"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20", "6.19"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/tile_group.rs"
change_id = "encoder-writer-input-framing"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the constructor (write-surface) exists and round-trips, but no
# splot-encode code consumes it yet — encoder consumption lands in a later bridge brick.
notes = "First brick of the maintainer-approved splot-core WRITER BRIDGE (the encoder→packet critical path). splot-core's tile-group/frame writer-input models (TileGroupFraming, TileGroupStructure, FrameHeaderCore, CoreSeqView) are #[non_exhaustive] with NO public constructor — produced only by parsing — so splot-encode cannot hand-build them to drive write_tile_group_obu/payload. This adds the smallest one: `TileGroupFraming::single_tile(tile_size) -> TileGroupFraming` in crates/splot-core/src/headers/tile_group.rs — the defect-free §5.20.1 framing for a first single-tile tile group (TileNum 0, size_field_offset None [the only tile is the last tile, no tile_size_minus_1 field, mirror :8557], tile_data_offset 0, tile_size, defect None). It is the EXACT encoder-side INVERSE of parse_tile_group_framing(payload, 0, 0, _, false) for ALL tile_size: a nonzero size yields defect None; a degenerate tile_size==0 yields the SAME §8.2.2 ZeroSizeTile-defective framing the parser would (not a falsely-conformant defect:None) — so the parser-inverse contract holds even for the degenerate input. #[non_exhaustive] is preserved (the internal TileFraming + the ZeroSizeTile defect are within-crate literals). Proven: value-equal to the parser's framing for both a nonzero size and the zero-size defect case; a write_tile_group_payload of a nonzero framing produces the byte-exact coded bytes and reparses value-equal (the round-trip is the correctness oracle). Encoder-mission-DRIVEN (owner=encoder) though the code lands in splot-core (crate=splot-core), per the maintainer's 'build the splot-core writer bridge' decision. encode status stays `todo`: no splot-encode code consumes the constructor yet (only the splot-core round-trip tests reference it). This row does not claim a multi-tile framing constructor, a TileGroupStructure/FrameHeaderCore/CoreSeqView constructor, a tile-group OBU, a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT bridge bricks: TileGroupStructure ctor (§5.19) → CoreSeqView + FrameHeaderCore ctors → drive write_tile_group_obu → seq-OBU + frame → Packet → receive_packet → CLI encode→decode-hash."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/tile_group.rs::tests::single_tile_constructor_matches_parser", "crates/splot-core/src/headers/tile_group.rs::tests::single_tile_constructor_matches_parser_for_zero_size", "crates/splot-core/src/headers/tile_group.rs::tests::single_tile_framing_write_then_reparse_round_trips"]
commands = ["cargo test -p splot-core single_tile"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-WRITER-INPUT-STRUCTURE"
name = "Encoder writer-input single-tile structure constructor"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.19"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/tile_group.rs"
change_id = "encoder-writer-input-structure"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the constructor (write-surface) exists and round-trips, but no
# splot-encode code consumes it yet — encoder consumption lands in a later bridge brick.
notes = "Writer-bridge BRICK 2 (after ENC-WRITER-INPUT-FRAMING). write_tile_group_obu needs a §5.19 TileGroupStructure alongside the §5.20.1 framing; like the framing it is #[non_exhaustive] / parse-only. Adds `TileGroupStructure::single_tile_first_group() -> TileGroupStructure` in crates/splot-core/src/headers/tile_group.rs — the structure for a single-tile FIRST tile group (NumTiles==1): tile_start_and_end_present_flag inferred 0 (parser never reads it for a single tile, mirror :8467-8473), tg_start 0, tg_end 0 (= NumTiles-1), outcome Complete. UNLIKE the framing ctor (an exact parser-inverse), the structure's header_bytes/payload_size are parse-context BYTE-ACCOUNTING the §5.19 writers IGNORE (write_tile_group_structure only validates outcome==Complete + the flag + the tg-range; write_tile_group_obu recomputes the header length + takes the payload from the tile data), so the ctor leaves them None and the oracle is a SEMANTIC round-trip (write→reparse → flag/tg_start/tg_end match + Complete) — the same contract the existing write_tile_group_structure round-trip tests use. #[non_exhaustive] preserved (within-crate literal). Proven: canonical fields + semantic round-trip via write_tile_group_structure for a TileGroupLayout::new(1,1,0,0). Encoder-mission-DRIVEN (owner=encoder), code in splot-core (crate=splot-core), per the maintainer's writer-bridge decision. encode=todo (no splot-encode code consumes it yet). This row does not claim a multi-tile/continuation structure, a CoreSeqView/FrameHeaderCore constructor, a tile-group OBU, a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT bridge bricks: CoreSeqView + FrameHeaderCore ctors → drive write_tile_group_obu → seq-OBU + frame → Packet → receive_packet → CLI encode→decode-hash."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/tile_group.rs::tests::single_tile_first_group_structure_has_canonical_fields", "crates/splot-core/src/headers/tile_group.rs::tests::single_tile_first_group_structure_round_trips"]
commands = ["cargo test -p splot-core single_tile_first_group"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-WRITER-INPUT-INTER-VIEW"
name = "Encoder writer-input minimal-intra inter view constructor"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.4.6"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-coreseq-inter-ctor"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the constructor (write-surface) exists and the round-trips
# regress it, but no splot-encode code consumes it yet — the CoreSeqView/FrameHeaderCore
# constructors and the encoder call-site are later bridge bricks.
notes = "Header-bridge BRICK 1 (from the vetted design workflow). To build a CoreSeqView for write_tile_group_obu, the encoder needs its nested CoreSeqInterView — the ONLY nested CoreSeq*View that is #[non_exhaustive] with crate-private fields and lacked a public ctor (the other six — Quant/Seg/Tile/Filter/Restoration/Ccso — are already all-pub non-#[non_exhaustive] → externally literal-constructible). Adds `CoreSeqInterView::new_minimal_intra() -> CoreSeqInterView` in crates/splot-core/src/headers/frame/encoder_input.rs (moved there from info.rs) — the all-disabled §5.4.6 inter view (every inter tool off, seq_enabled_motion_modes [false;5], enable_opfl_refine 0), the value the base_inter() test helpers built by hand. An intra frame never reads the inter config (the §5.18.2 control region skips the inter tail), so this is the inert state the writer needs to invert parse_frame_header_core. #[non_exhaustive] preserved (within-crate literal). CoreSeqInterView has NO PartialEq → ORACLE = the three base_inter() parser/writer test helpers (info.rs, frame_header_core_tests.rs, tile_group_obu_tests.rs) were PROMOTED to call the ctor, so the entire existing frame-header round-trip suite (83 frame_header_core tests) regresses it at zero new-test cost; plus a direct all-disabled field test. Encoder-mission-DRIVEN (owner=encoder), code in splot-core, per the maintainer's writer-bridge decision. encode=todo. This row does not claim a CoreSeqView/FrameHeaderCore constructor, a tile-group OBU, a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: CoreSeqView::new_minimal_intra (brick 2, uses this + the 6 already-public views) → FrameHeaderCore parse-backed assembler (brick 3) → re-export TileGroupStructure/TileGroupFraming (brick 4) → encoder write_tile_group_obu call-site (brick 5)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::core_seq_inter_view_minimal_intra_is_all_disabled"]
commands = ["cargo test -p splot-core core_seq_inter_view_minimal_intra", "cargo test -p splot-core frame_header_core"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-WRITER-INPUT-SEQ-VIEW"
name = "Encoder writer-input minimal-intra CoreSeqView constructor"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.4.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-coreseqview-ctor"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the constructor (write-surface) exists and the round-trip suite
# regresses it, but no splot-encode code consumes it yet — the FrameHeaderCore assembler
# and the encoder call-site are later bridge bricks.
notes = "Header-bridge BRICK 2 (after ENC-WRITER-INPUT-INTER-VIEW). write_tile_group_obu serializes the whole frame header from a &CoreSeqView + &FrameHeaderCore; the encoder needs a CoreSeqView, otherwise #[non_exhaustive]/crate-private-field and built only from a parsed SequenceHeader (from_sequence). Adds `CoreSeqView::new_minimal_intra(max_frame_width, max_frame_height) -> Option<CoreSeqView>` in crates/splot-core/src/headers/frame/encoder_input.rs — the §5.4.1 view a minimal intra frame needs: every unused sequence tool disabled (inter via CoreSeqInterView::new_minimal_intra(); no segmentation/tiles/loop-filters/restoration/CCSO; no film grain), 8-bit YUV420, 3 planes. Builds the NON-single-picture view (single_picture_header_flag=false), parameterized by the §5.4.1 frame-size maxima; Returns Option (None for maxima outside 1..=2^16, since §5.4.1 frame_*_bits_minus_1 is f(4) → frame_*_bits 1..=16, so no valid sequence header describes a wider/zero maximum — a Codex P2 fix); frame_width_bits/frame_height_bits are DERIVED from the maxima via ceil_log2(max).max(1) (clamped to the 1-bit minimum — another Codex P2; ceil_log2(4096)==12 keeps the base_seq shape, ceil_log2(64)==6). The SINGLE-PICTURE variant infers a different §5.4.1 shape across four config parsers (§5.4.6 OrderHintBits=0/NumRefFrames=2, §5.4.1 SCC SELECT=2 force fields, §5.4.10 (enable_avg_cdf,avg_cdf_type)=(true,1)) and is a later, separately-round-trip-verified constructor (the 4 Codex single-picture-inference findings on this PR were resolved by scoping to non-single-picture). #[non_exhaustive] preserved (within-crate literal). CoreSeqView has NO PartialEq → ORACLE = the base_seq() test helper was PROMOTED to delegate to the ctor (base_seq() = new_minimal_intra(4096, 2304).unwrap()), so the entire frame-header round-trip suite (83 frame_header_core tests) regresses it; the now-dead nested-view helpers were removed so the ctor replaces them; plus a direct parameterization/validation test. The two WRITER round-trip base_seq helpers (frame_header_core_tests.rs, tile_group_obu_tests.rs) ALSO delegate to the ctor (writer-base-seq-delegate), so the writer round-trips — the strongest oracle — regress it too. Also replaced the remaining hand-rolled all-disabled CoreSeqInterView literal in frame_header_core_proptests.rs with new_minimal_intra() (the brick-1 follow-up de-dup). Encoder-mission-DRIVEN (owner=encoder), code in splot-core, per the maintainer's writer-bridge decision. encode=todo. This row does not claim a FrameHeaderCore constructor, a tile-group OBU, a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: FrameHeaderCore minimal-intra parse-backed assembler (brick 3) → re-export TileGroupStructure/TileGroupFraming (brick 4) → encoder write_tile_group_obu call-site (brick 5)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::core_seq_view_minimal_intra_derives_dim_bits_and_is_non_single_picture"]
commands = ["cargo test -p splot-core core_seq_view_minimal_intra", "cargo test -p splot-core frame_header_core"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-FRAME-HEADER-CORE-ASSEMBLER"
name = "Encoder writer-input minimal-intra FrameHeaderCore parse-backed assembler"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.18.2", "5.4.1", "5.4.6", "5.4.7", "5.4.8"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-frame-header-core-assembler"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the assembler (write-surface) produces a conformant FrameHeaderCore
# and the round-trip proves it, but no splot-encode code consumes it yet — the tile-group
# OBU call-site and the packet path are later bridge bricks.
notes = "Header-bridge BRICK 3 (the keystone, after ENC-WRITER-INPUT-SEQ-VIEW). write_frame_header_core / write_tile_group_obu serialize a §5.18.2 FrameHeaderCore; the encoder needs one, otherwise #[non_exhaustive]/crate-private-field and produced only by the parser. Rather than a fragile field-by-field literal, adds a PARSE-BACKED assembler: `build_minimal_intra_clk_core() -> Result<(FrameHeaderCore, CoreSeqView), MinimalIntraCoreError>` is SELF-CONTAINED (builds the matched 64x64 single-picture view INTERNALLY and returns the (core, seq) pair, so a caller cannot mis-pair body and view — this resolved 3 Codex P2s: a non-64x64 / non-single-picture / nonzero-seq-id view passed to an arbitrary-&CoreSeqView signature would mis-parse the fixed bits into a different-but-complete core). It serializes the canonical 64x64/base_q_idx=255 single-picture OBU_CLOSED_LOOP_KEY intra §5.18.2 body (BitWriter, one write per element — order_hint OMITTED because OrderHintBits=0, an explicit allow_screen_content_tools bit because SCC=SELECT, uniform_tile_spacing_flag with NO increment bits because 64x64 is one superblock, base_q_idx=255) and PARSES it to an IntraHeaderComplete core, so the result is conformant by construction (inverts the same parser the decoder runs). Also adds the matched single-picture seq view `CoreSeqView::new_minimal_intra_single_picture(max_w, max_h) -> Option<Self>` = new_minimal_intra + exactly eight §5.4.x single-picture inferences a non-single header signals differently: single_picture_header_flag (top-level + filter + CCSO), §5.4.6 OrderHintBits=0 & NumRefFrames=2, §5.4.7 seq_force_screen_content_tools/seq_force_integer_mv=SELECT(=2), §5.4.8 (enable_avg_cdf,avg_cdf_type)=(true,1), §5.4.1 monotonic_output_order_flag=true (the rest stays the disabled-tool value, a legal single-picture choice). Plus the MinimalIntraCoreError typed error (Body/Parse arms, both unreachable for the canonical input, present for no-panic) and un-gating the existing pub(crate) init_core_from_prefix/parse_core_body re-export (was #[cfg(test)]) for the non-test assembler. The §5.4.x facts + the exact body bit-sequence were adversarially verified by a Workflow (it caught order_hint_bits=0 → order_hint omitted, the allow_screen_content_tools bit, and monotonic_output_order_flag=true). ORACLE = a round-trip test: parse the canonical body against the single-picture view → assert IntraHeaderComplete/Key/64x64/order_hint_lsb=0/refresh_frame_flags=3/immediate-output, then write_frame_header_core → reparse → equal core (the existing single_picture_clk_bits fixture is NOT a spec-real oracle: it is self-consistent with a non-degenerate order_hint_bits=4/SCC=0 test seq); plus a field-delta test for the eight inferences. Public API: one new CoreSeqView ctor + build_minimal_intra_clk_core + MinimalIntraCoreError, all splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder), per the maintainer's writer-bridge decision. encode=todo. This row does not claim a tile-group OBU, a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: encoder write_tile_group_obu call-site (brick 4) → frame/Packet/receive_packet (brick 5) → CLI encode→decode-hash (brick 6)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::build_minimal_intra_clk_core_round_trips", "crates/splot-core/src/headers/frame/encoder_input.rs::tests::new_minimal_intra_single_picture_applies_eight_spec_inferences"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-MINIMAL-INTRA-TILE-GROUP-OBU"
name = "Encoder writer-input minimal-intra tile-group OBU payload assembler"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.19", "5.20.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-tile-group-obu-callsite"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the payload assembler (write-surface) emits a round-trippable
# tile_group_obu() payload, but no splot-encode code consumes it yet and the tile_data
# content is a caller input — a complete coded tile + the OBU wrapper are later bricks.
notes = "Header-bridge BRICK 4 (after ENC-FRAME-HEADER-CORE-ASSEMBLER). Connects the keystone to the tile-group writer: `encode_minimal_intra_clk_tile_group_obu(tile_data: &[u8]) -> Result<Vec<u8>, MinimalIntraTileGroupError>` builds the matched frozen-tier (FrameHeaderCore, CoreSeqView) via build_minimal_intra_clk_core(), frames tile_data as the single (last) tile of the first tile group (TileGroupStructure::single_tile_first_group() [writer-bridge brick 2] + TileGroupFraming::single_tile(tile_data.len()) [brick 1]), and drives write_tile_group_obu (§5.19/§5.20.1) → returns the tile_group_obu() PAYLOAD (embedded frame header + tile framing + tile data). The FIRST time the bridge pieces compose into framed OBU output. tile_data is a genuine param (different frames carry different §8.2 coded bytes; the §5.20.1 framing is derived from its length); the fixed frozen-tier context is internal. MinimalIntraTileGroupError typed enum (Core(#[from] MinimalIntraCoreError) / Write(#[from] WriteError)); empty tile_data → §8.2.2 ZeroSizeTile defect → typed Write error (no panic). ORACLE = a round-trip test: encode coded tile bytes → reparse the payload tile-group prefix (is_first_tile_group, frame_header_present_flag, embedded frame header) + assert tile_data is the byte-aligned trailing region (the lone last tile reads no size field); + an empty-tile_data reject test. The writer's own whole_obu_round_trips_stage_by_stage already proves write_tile_group_obu round-trips a test core; this proves the KEYSTONE feeds it. Public API: one new fn + one new error, all splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder), per the maintainer's writer-bridge decision. encode=todo. This row does NOT claim the §5.2.2 OBU header/size wrapper, a COMPLETE spec-conformant coded tile (tile_data content is the caller's, a separate axis = the block-symbol trace), a frame, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: the §5.2.2 OBU header/size wrapper + a complete coded 64x64 tile (wire the block-symbol trace) → a temporal unit / frame / packet → CLI encode→decode-hash."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_tile_group_obu_round_trips", "crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_tile_group_obu_rejects_empty_tile_data"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-MINIMAL-INTRA-CLK-ANNEXB-OBU"
name = "Encoder writer-input minimal-intra Annex B OBU wrapper"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.2.2", "B.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-annexb-obu-wrapper"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the wrapper emits a self-delimiting CLK OBU, but it is a single frame
# OBU (no temporal delimiter / sequence header / IVF), the tile_data content is a caller
# input, and no splot-encode code consumes it yet.
notes = 'Header-bridge BRICK 5 (after ENC-MINIMAL-INTRA-TILE-GROUP-OBU). Brick 4 emits the bare §5.19 tile_group_obu() PAYLOAD (no length framing → cannot stand alone); this wraps it in AV2 Annex B framing (§B.2): `encode_minimal_intra_clk_annexb_obu(tile_data: &[u8]) -> Result<Vec<u8>, MinimalIntraTileGroupError>` assembles the brick-4 payload, builds the no-extension OBU_CLOSED_LOOP_KEY §5.2.2 header (inferred layer ids 0 — CLK is not OBU_MSDO/OBU_TEMPORAL_DELIMITER, the only types that infer the GLOBAL xlayer), and drives write_annexb_obu (leb128 total-size prefix + obu_header + payload). The FIRST complete, self-delimiting OBU the encoder emits. The §5.2.2 CLK header is a fixed literal (the no-ext layer-id inference is a documented constant, not a param); the round-trip oracle verifies it (a wrong field would reparse differently). ORACLE = parse_annex_b_obus_partial(result) is exactly ONE OBU, no error, header type ClosedLoopKey, no extension, payload == the brick-4 tile_group_obu() payload (ending in the coded tile bytes); + an empty-tile_data reject test (propagates the §8.2.2 ZeroSizeTile Write error). Reuses MinimalIntraTileGroupError (write_annexb_obu WriteError → ::Write #[from]; no new error type). Public API: one new fn, splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder). encode=todo. This row is a SINGLE FRAME OBU, NOT a decodable temporal unit: it does NOT claim a temporal delimiter, a sequence-header OBU, an IVF stream, a COMPLETE spec-conformant coded tile (tile_data content = the block-symbol trace axis), a frame/packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: a temporal-delimiter OBU + a sequence-header OBU → assemble the temporal unit (TD + seq + CLK) → wrap in IVF → a complete coded 64x64 tile (wire the block-symbol trace) → CLI encode→decode-hash.'

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_annexb_obu_round_trips", "crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_annexb_obu_rejects_empty_tile_data"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-TEMPORAL-DELIMITER-OBU"
name = "Encoder temporal-delimiter OBU primitive"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.5", "5.2.2", "B.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-temporal-delimiter-obu"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: a single OBU primitive (one building block of a temporal unit); no
# sequence header / IVF / packet, and no splot-encode consumer yet.
notes = 'Toward a decodable temporal unit. The decoder minimal-tier IVF frame requires its OBUs in order OBU_TEMPORAL_DELIMITER, OBU_SEQUENCE_HEADER, then the frame OBU (pipeline/mod.rs:94-104); the frame OBU is brick 5. This adds the first of the two missing OBUs — the temporal delimiter (the simplest, empty payload). `encode_temporal_delimiter_obu() -> Result<Vec<u8>, WriteError>` builds the no-extension OBU_TEMPORAL_DELIMITER §5.2.2 header (inferred obu_mlayer_id=0, GLOBAL obu_xlayer_id=31 — the temporal delimiter and OBU_MSDO are the only two types that infer the global xlayer, §5.2.2; DIFFERENT from CLK which infers 0) with an empty payload, in Annex B framing (§B.2) via write_annexb_obu → the canonical two bytes [0x01, 0x08]. ORACLE = a round-trip test: result == [0x01, 0x08] + parse_annex_b_obus_partial yields exactly one OBU, no error, type TemporalDelimiter, no extension, empty payload. Returns WriteError directly (no payload assembly step). Public API: one new fn, splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder). encode=todo. This row is a SINGLE OBU PRIMITIVE, NOT a temporal unit or a decodable stream: it does NOT claim a sequence header, an IVF stream, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: a sequence-header OBU (write_sequence_header + a minimal parse-backed SequenceHeader — the gating piece) → assemble the temporal unit (TD+seq+CLK) → wrap in IVF (write_ivf_header/write_ivf_frame exist) → a complete coded 64x64 tile → CLI encode→decode-hash.'

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_temporal_delimiter_obu_round_trips"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-SEQUENCE-HEADER-OBU"
name = "Encoder minimal-intra sequence-header OBU"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.4", "5.2.2", "5.2.3", "B.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-sequence-header-obu"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the sequence-header OBU is byte-exact to the conformance vector, but
# it is a single OBU — the temporal unit, the consistent frame OBU, and the IVF wrap are
# later bricks, and no splot-encode code consumes it yet.
notes = "The GATING piece for a decodable temporal unit. The decoder minimal-tier IVF frame requires its OBUs in order OBU_TEMPORAL_DELIMITER (brick 6), OBU_SEQUENCE_HEADER, then the frame OBU (brick 5). This adds the sequence header — the activated tier the frame header parses against. `build_minimal_intra_sequence_header() -> Result<SequenceHeader, MinimalIntraSequenceHeaderError>` PARSE-BACKS the committed syn-cos-intra-64x64-q180 conformance vector's 11-byte sequence_header() body [82 0a 55 ff f0 c0 04 d1 16 e0 22] into a SequenceHeader (conformant by construction — the exact seq header the decoder accepts). `encode_minimal_intra_sequence_header_obu()` emits the body-PLUS-§5.2.3-TAIL payload via write_obu_payload (NOT the body-only write_sequence_header — a write_sequence_header-only attempt produced ...e0 20 [zero padding] vs the vector's ...e0 22 [the trailing_bits 1-marker], caught by the byte-exact oracle: the seq header is an EXTENSIBLE OBU so its tail = obu_extension_flag(0) + trailing_bits) under the no-extension OBU_SEQUENCE_HEADER §5.2.2 header (inferred layer ids 0), in Annex B framing → byte-exact to the vector's seq header OBU [0c 04 82 0a 55 ff f0 c0 04 d1 16 e0 22]. MinimalIntraSequenceHeaderError (Parse #[from] error::Error / Write #[from] WriteError). ORACLE = byte-exact to the decoder-accepted conformance vector (the strongest splot-core oracle — caught the body-vs-tail bug) + the payload round-trips byte-exact + reparses as 1 OBU_SEQUENCE_HEADER. Public API: 2 new fns + 1 new error, splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder). encode=todo. This row is a SINGLE OBU, NOT a temporal unit or decodable stream: it does NOT claim the frame OBU made CONSISTENT with this seq header (the assembler brick must reconcile them — the frame header parses AGAINST this seq header), an IVF stream, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT: the temporal-unit assembler (TD + this seq header + a CONSISTENT frame OBU) → IVF wrap (write_ivf_header/write_ivf_frame) → CLI encode→decode-hash."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_sequence_header_obu_matches_conformance_vector"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = ["tests/conformance/vectors/valid/syn-cos-intra-64x64-q180.ivf"]
diagnostics = []

[[feature]]
id = "ENC-MINIMAL-INTRA-IVF"
name = "Encoder minimal-intra IVF temporal-unit assembler"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.2.1", "B.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-ivf-temporal-unit"
tracking_issue = ""
owner = "encoder"
risk = "low"
# encode stays `todo`: the IVF is structurally valid with consistent headers, but the coded
# tile content is a caller input, and no splot-encode code consumes it yet.
notes = "The integration finale first half. The encoder now emits all three OBUs the decoder minimal-tier IVF frame requires (TD brick 6, sequence header byte-exact, frame OBU brick 5); this assembles them into one IVF temporal unit (the first complete container the encoder produces). encode_minimal_intra_clk_ivf(tile_data) -> Result<Vec<u8>, MinimalIntraIvfError> concatenates the TD + sequence-header + frame Annex B OBUs into one AV2 temporal unit (decoder-required order) and wraps it in one AV02 64x64 IVF frame (write_ivf_header(IvfHeader::new(AV02,64,64,30,1,1)) + write_ivf_frame(_,0,tu); timebase 30/1 + frame_count 1 match the committed conformance vector). CONSISTENCY VERIFIED: the sequence header + the frame header describe the SAME tier - CoreSeqView::from_sequence(build_minimal_intra_sequence_header()) matches the view build_minimal_intra_clk_core built the frame header against on EVERY frame-header-parse-driving field (single_picture=true, OrderHintBits=0, NumRefFrames=2, SCC/imv SELECT=2, 64x64 maxima, (enable_avg_cdf,avg_cdf_type)=(true,1), monotonic_output_order_flag=true, seq_sb_size=Block64x64). The Block64x64 override added for the keystone #364 [Important] finding is EXACTLY what makes seq+frame consistent (the new_minimal_intra default Block128x128 would NOT match the vector Block64x64). MinimalIntraIvfError (SequenceHeader/Frame/Write/Ivf #[from], incl. std::io::Error). ORACLE = parse_ivf_partial -> valid AV02 64x64 IVF, 1 frame; frame payload parse_annex_b -> [TemporalDelimiter, SequenceHeader, ClosedLoopKey] in order; from_sequence(seq) is the frozen Block64x64 64x64 single-picture tier; + empty-tile_data reject. Public API: 1 new fn + 1 new error, splot-core, no dependency-graph change. Encoder-mission-DRIVEN (owner=encoder). encode=todo. STRUCTURALLY VALID IVF with CONSISTENT HEADERS, NOT yet a decode-hash match: tile_data is a caller input -> a COMPLETE spec-conformant coded tile (and base_q_idx) is a SEPARATE axis = the block-symbol trace wiring. Does NOT claim a decodable image, a packet, receive_packet output, CLI success, or Baseline Encoder Profile v1. NEXT (integration finale 2nd half): wire the block-symbol trace -> a complete coded 64x64 tile -> feed encode_minimal_intra_clk_ivf -> CLI encode + splot-decode decode-hash (and AVM/dav2d agreement)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "todo"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_ivf_assembles_consistent_temporal_unit", "crates/splot-core/src/headers/frame/encoder_input.rs::tests::encode_minimal_intra_clk_ivf_rejects_empty_tile_data"]
commands = ["cargo test -p splot-core encoder_input"]
fixtures = ["tests/conformance/vectors/valid/syn-cos-intra-64x64-q180.ivf"]
diagnostics = []

[[feature]]
id = "ENC-DO-SPLIT-PARTITION-SYMBOL"
name = "Encoder root do_split partition symbol"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.3.2", "8.3.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/partition_emission.rs"
change_id = "encoder-do-split-partition-symbol"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_root_do_split_none produces the live root 64x64 do_split=0 token at PlaneStart 0, context 12. It is the first token in every supported general-intra trace and is protected by production composition assertions plus decoder-backed packet oracles. Unsupported square-split scaffolding and self-roundtrip tests were removed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/skip.rs::tests::composes_general_skip_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode general_intra_trace::skip::tests::composes_general_skip_block_trace_in_order --locked", "cargo test -p splot-cli --test encode_decode_roundtrip encoder_skip_ivf_decodes_to_a_flat_128_frame --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-SKIP-BLOCK-TRACE"
name = "Encoder general-intra DC skip-block trace"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.3.2", "5.20.5.3", "5.20.7.27", "8.3.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/skip.rs"
change_id = "encoder-general-intra-skip-block-trace"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "compose_general_intra_dc_skip_block_trace builds the live seven-token root-partition, DC-mode, and Y/U/V txb_skip trace at TX_64X64 and TX_32X32 contexts. Its order is asserted locally and its actual encoded bytes are verified by the decoder-backed flat-128 CLI oracle."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/skip.rs::tests::composes_general_skip_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode general_intra_trace::skip::tests::composes_general_skip_block_trace_in_order --locked", "cargo test -p splot-cli --test encode_decode_roundtrip encoder_skip_ivf_decodes_to_a_flat_128_frame --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-SKIP-TILE-DATA"
name = "Encoder general-intra DC skip tile_data bytes"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20", "8.2.4", "8.2.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/skip.rs"
change_id = "encoder-general-intra-skip-tile-data"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "encode_general_intra_dc_skip_tile_data feeds the live skip trace to encode_block_symbol_trace and returns the finalized single-tile payload. Determinism is checked locally and decodability is protected by the flat-128 CLI oracle; the former internal self-decoder equality test was removed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/skip.rs::tests::skip_tile_data_is_deterministic", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame"]
commands = ["cargo test -p splot-encode general_intra_trace --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-MINIMAL-CLK-BASE-Q-IDX"
name = "base_q_idx-parameterized minimal CLK container"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.18.2", "5.19", "5.2.2", "Annex B"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/encoder_input.rs"
change_id = "encoder-minimal-clk-base-q-idx"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "Decodable-tile arc BRICK 4: parameterizes the minimal OBU_CLOSED_LOOP_KEY container by base_q_idx (splot-core writer bridge). Bricks 2-3 produced a general-intra DC skip tile_data coded at coeff CDF q-context 0, which the decoder derives only from base_q_idx<=90; the existing assembler hardcoded base_q_idx=255 (q-context 3). Threads base_q_idx through minimal_intra_clk_body_bytes + build_minimal_intra_clk_core + the tile-group/AnnexB/IVF impls; the frozen no-arg public fns delegate at FROZEN_TIER_BASE_Q_IDX=255 (signatures + behavior unchanged). Adds ONE public fn encode_minimal_intra_clk_ivf_with_base_q_idx(base_q_idx, tile_data) + rejects base_q_idx==0 (CodedLossless would change the \u00a75.18.2 body layout) with a typed MinimalIntraCoreError::LosslessBaseQIdx. HEADLINE ORACLE: a measurement showed the existing 255-assembler differs from the AVM/dav2d-validated syn-flat-intra-64x64-q80.ivf in EXACTLY 2 bytes (both in the CLK frame header's base_q_idx field); so encode_minimal_intra_clk_ivf_with_base_q_idx(80, q80_tile_data) reproduces the fixture BYTE-FOR-BYTE (test ivf_with_base_q_idx_80_reproduces_the_avm_validated_q80_fixture). This proves the emitted container (TD + sequence header + CLK frame header + IVF framing) matches the AVM reference structure, with base_q_idx as the only parameterized field - so a later brick muxing the SKIP tile_data into this container varies only the tile_data. base_q_idx<=90 + the canonical disable_cdf_update==0 satisfy the decodability contract a prior multi-agent verification established. Encoder-mission-DRIVEN (owner=encoder). This row is a base_q_idx-PARAMETERIZED CONTAINER: it does NOT claim a decode, a coded skip frame, CLI success, or Baseline Encoder Profile v1; the cross-crate decode oracle is a later splot-cli brick."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/headers/frame/encoder_input.rs::tests::ivf_with_base_q_idx_80_reproduces_the_avm_validated_q80_fixture", "crates/splot-core/src/headers/frame/encoder_input.rs::tests::ivf_with_base_q_idx_zero_is_rejected_as_lossless"]
commands = ["cargo test -p splot-core encoder_input --locked"]
fixtures = ["tests/conformance/vectors/valid/syn-flat-intra-64x64-q80.ivf"]
diagnostics = []

[[feature]]
id = "ENC-MINIMAL-INTRA-SKIP-IVF"
name = "First decodable minimal intra skip IVF"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.18.2", "5.20", "7.13.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/skip.rs"
change_id = "encoder-minimal-intra-skip-ivf"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "Decodable-tile arc BRICK 5 - THE MILESTONE: splot-encode's FIRST end-to-end decodable output, cross-crate decode-proven. emit_minimal_intra_skip_ivf() (splot-encode public) composes the general-intra DC skip tile_data (bricks 2-3) and muxes it through encode_minimal_intra_clk_ivf_with_base_q_idx(80, ..) (brick 4 splot-core), returning a complete AV2 IVF for one 64x64 all-intra OBU_CLOSED_LOOP_KEY DC skip frame. CROSS-CRATE ORACLE (crates/splot-cli/tests/encode_decode_roundtrip.rs - splot-cli is the only crate depending on both splot-encode + splot-decode): emit the IVF -> `splot decode --output-format raw` -> assert 6144 bytes (8-bit 420 64x64: Y 64*64 + U 32*32 + V 32*32) ALL == 128. The single block is do_split=0 + DC_PRED luma/chroma + all-zero residual, so the no-neighbour \u00a77.13.2 DC predictor 1<<(8-1)=128 reconstructs a flat frame. This proves bricks 1-4 (do_split + modes + per-plane all_zero txb_skip at general contexts + \u00a78.2.4 finalize + base_q_idx-80 container) DECODE through splot's own decoder. Added Error::MinimalIntraSkipIvf for the container-assembly failure path. Also a splot-encode test that the emitted bytes parse as a 1-frame AV02 64x64 IVF + are deterministic. decode_check=done (splot-decode, itself AVM/dav2d-validated on the coded q80/q180 fixtures); avm_diff NOT claimed (this exact stream not decoded with AVM - the container is byte-identical to the AVM-validated q80 fixture apart from tile_data). Encoder-mission-DRIVEN (owner=encoder). HONEST SCOPE: the FIRST decodable minimal-intra skip stream - NOT a general encoder, NOT a Context::receive_packet packet, NOT Baseline Encoder Profile v1."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_skip_ivf_decodes_to_a_flat_128_frame", "crates/splot-encode/src/general_intra_trace/skip.rs::tests::emit_minimal_intra_skip_ivf_is_a_parseable_one_frame_av02_ivf"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode general_intra_trace --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-CODED-LUMA-DC"
name = "First decodable coded-DC intra frame"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "5.20.7.28", "7.13.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/coded_dc.rs"
change_id = "encoder-general-intra-coded-luma-dc"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_coded_dc_ivf emits the supported TX_64X64 luma DC residual with eob_pt_1024, low-frequency base and range symbols, and dc_sign while chroma is skipped. The production composition test protects token order and the decoder-backed CLI oracle proves flat luma 127 with flat chroma 128."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/coded_dc.rs::tests::composes_general_coded_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_coded_dc_ivf_decodes_to_a_flat_127_luma_frame"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode coded --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-CODED-CHROMA-DC"
name = "First decodable coded-chroma intra frame"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.2.5", "7.13.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/chroma.rs"
change_id = "encoder-general-intra-coded-chroma-dc"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_coded_chroma_ivf emits the supported TX_32X32 U-plane DC residual, including its bypass sign, while luma and V are skipped. Production composition assertions protect token order and the decoder-backed CLI oracle proves flat U 127 with the other planes 128."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/chroma.rs::tests::composes_general_coded_chroma_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_coded_chroma_ivf_decodes_to_a_flat_127_u_frame"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode chroma --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-CODED-CHROMA-V-DC"
name = "Decodable coded-V intra frame"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.2.5", "7.13.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/chroma.rs"
change_id = "encoder-general-intra-coded-chroma-v-dc"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_coded_chroma_v_ivf emits the supported TX_32X32 V-plane DC residual through the dedicated V txb_skip row and a bypass sign while luma and U are skipped. Composition assertions and the decoder-backed CLI oracle protect the live behavior."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/chroma.rs::tests::composes_general_coded_chroma_v_and_all_planes_traces_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_coded_chroma_v_ivf_decodes_to_a_flat_127_v_frame"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode chroma_v --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-ALL-PLANES-CODED"
name = "All-planes-coded intra frame"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.2.5", "8.3.2", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/chroma.rs"
change_id = "encoder-general-intra-all-planes-coded"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_all_planes_coded_ivf composes the live coded luma, U, and V DC paths in one block, including both chroma bypass signs and the EobU-dependent V context. Composition assertions and the decoder-backed all-planes 127 CLI oracle protect the interaction."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/chroma.rs::tests::composes_general_coded_chroma_v_and_all_planes_traces_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_all_planes_coded_ivf_decodes_to_a_flat_127_frame"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode all_planes --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-TWO-COEFF"
name = "First multi-coefficient intra frame"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.3.2", "8.2.5", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/multi_coeff.rs"
change_id = "encoder-general-intra-two-coeff"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_two_coeff_ivf emits the live TX_64X64 eob=2 luma trace with one AC and a zero DC. Production composition assertions protect the base-pass and sign order; the decoder-backed CLI oracle proves the emitted stream decodes successfully."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/multi_coeff.rs::tests::composes_general_two_coeff_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_two_coeff_ivf_decodes_successfully"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode two_coeff --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-VISIBLE-AC"
name = "First visibly non-flat intra reconstruction"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.3.2", "8.2.5", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/multi_coeff.rs"
change_id = "encoder-general-intra-visible-ac"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_visible_ac_ivf emits the live eob=2 level-4 AC trace using the level-derived DC context 2. Composition assertions and the decoder-backed vertical-cosine CLI oracle protect the production token selection and output."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/multi_coeff.rs::tests::composes_general_visible_ac_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_visible_ac_ivf_decodes_to_a_vertical_cosine_luma"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode visible_ac --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-TWO-NONZERO"
name = "First two-nonzero-coefficient intra block"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.3.2", "8.2.5", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/multi_coeff.rs"
change_id = "encoder-general-intra-two-nonzero"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_two_nonzero_ivf emits the live eob=2 base pass for a level-4 AC and negative level-1 DC, followed by signs in reverse-scan order. Composition assertions and the asymmetric decoder-backed CLI oracle protect that order."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/multi_coeff.rs::tests::composes_general_two_nonzero_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_two_nonzero_ivf_decodes_to_a_cosine_plus_dc_offset"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode two_nonzero --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-EOB3"
name = "First eob>2 intra frame (eob_extra)"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.3.2", "8.2.5", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/multi_coeff.rs"
change_id = "encoder-general-intra-eob3"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_eob3_ivf emits the live eob_pt_1024 and eob_extra sequence for a level-4 AC at scan index 2, with the supported low-frequency base contexts. Composition assertions and the decoder-backed horizontal-cosine CLI oracle protect the production path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/multi_coeff.rs::tests::composes_general_eob3_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_eob3_ivf_decodes_to_a_horizontal_cosine_luma"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode eob3 --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-GENERAL-INTRA-2D"
name = "First 2-D intra reconstruction"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.20.7.27", "8.3.2", "8.2.5", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/general_intra_trace/multi_coeff.rs"
change_id = "encoder-general-intra-2d"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "emit_minimal_intra_2d_ivf emits the live eob=3 base pass with two nonzero AC coefficients and asymmetric bypass signs. Composition assertions and the decoder-backed diagonal-gradient CLI oracle protect CDF selection and reverse-scan sign order."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/general_intra_trace/multi_coeff.rs::tests::composes_general_2d_block_trace_in_order", "crates/splot-cli/tests/encode_decode_roundtrip.rs::encoder_2d_ivf_decodes_to_a_diagonal_gradient_luma"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode _2d_ --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-CONTEXT-RECEIVE-PACKET"
name = "Public Context emits a real packet (Milestone A keystone)"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["7.13.2", "5.20", "8.2"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/context.rs"
change_id = "encoder-context-receive-packet"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "MILESTONE A KEYSTONE: the FIRST real packet through the PUBLIC encoder API. Context::receive_packet (splot-encode public push/pull lifecycle) now returns a real decodable AV2 Packet from a send_frame input \u2014 for the subset the minimal encoder encodes LOSSLESSLY: a 64x64 8-bit YUV420 frame whose every visible Y/U/V sample == the 128 \u00a77.13.2 no-neighbour DC predictor. Such a frame has ZERO residual, so the (decode-proven) skip frame reconstructs flat-128 == the input. Packet.data carries ONE CODED ACCESS UNIT (the Annex B temporal unit via `emit_minimal_intra_skip_temporal_unit_with_base_q_idx`, with the public-Context proof using `DEFAULT_QP` and the canonical skip proof using `SKIP_FRAME_BASE_Q_IDX`, delegating to splot-core `encode_minimal_intra_clk_temporal_unit_with_base_q_idx`), NOT a full IVF file (Codex #409 P2: a packet must be an access unit so consumers can mux many into one stream; emitting an IVF file per packet would double the DKIF header). HONESTY INVARIANT: decode(encode(input)) == input (proven by the cross-crate e2e oracle driving the public Context: send_frame -> flush -> receive_packet -> Packet[access unit] -> mux into IVF -> splot decode -> all-128). Wiring: input_queue changed from VecDeque<FrameInfo> to a private QueuedFrame owning the visible Y/U/V samples (the borrowed Frame doesn't outlive send_frame, so receive_packet needs an owned copy to inspect). receive_packet: 64x64 all-128 -> skip Packet; any other frame retired WITHOUT a packet (NO canned/wrong output); emitter failure -> Failed (terminal). Context unit tests cover supported and unsupported lifecycle behavior alongside the e2e oracle. Stale 'no packet production' lib/context module docs updated. USER (maintainer) EXPLICITLY AUTHORIZED Milestone A (\u00a710) 'go for milestone A'. \u00a710 FOLLOW-UPS (maintainer-gated, NOT in this deterministic brick): uniform-V forward quant (dequant-inversion vs real forward DCT), non-uniform DC approximation, mode decision, transform-type selection, RDO/rate-control, larger sizes, a cli `encode` command + typed error for unsupported input. decode_check=done; avm_diff=not-applicable. Encoder-mission-DRIVEN (owner=encoder). HONEST SCOPE: first public-API packet, lossless ONLY for all-128; NOT a general encoder, NOT Baseline Encoder Profile v1."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/encode_decode_roundtrip.rs::context_encodes_all_128_frame_to_a_packet_that_decodes_to_all_128", "crates/splot-encode/src/context.rs::tests::receive_packet_encodes_a_64x64_all_128_frame_to_the_skip_packet"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode context:: --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-CONFIG-QP-FIELD"
name = "Encoder config single fixed-quantizer (qp) source"
category = "encoder"
kind = "encoder-tool"
spec_sections = ["5.18.6.1"]
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/config.rs"
change_id = "encoder-config-qp-field"
tracking_issue = ""
owner = "encoder"
risk = "low"
notes = "EncoderConfig owns the single fixed `qp: u8` value (DEFAULT_QP=80, matching the AVM/dav2d-validated syn-flat-intra-64x64-q80 fixture). Context reads `config.qp` directly when constructing the supported packet and threads it into the frame-header `base_q_idx` (§5.18.6.1); no wrapper or rate-controller abstraction sits between the configuration and writer. The supported range is 1..=90, whose coefficient CDF q-context is 0; values outside that range retire the frame without a packet until the q-context mapping is widened. Decoder-backed CLI tests prove both default QP 80 and non-default QP 40 reconstruct the all-128 input exactly. No forward quantization, per-frame adaptation, per-block deltas, or rate control is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "done"
encode = "done"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/encode_decode_roundtrip.rs::context_encodes_all_128_frame_at_non_default_qp_that_decodes_to_all_128", "crates/splot-encode/src/context.rs::tests::receive_packet_encodes_a_64x64_all_128_frame_to_the_skip_packet"]
commands = ["cargo test -p splot-cli --test encode_decode_roundtrip --locked", "cargo test -p splot-encode context:: --locked"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-RECON-DEPENDENCY"
name = "Encoder reconstruction dependency boundary"
category = "encoder"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-encode"
module = "crates/splot-encode/src/frame.rs"
change_id = "encoder-recon-dependency"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "Non-normative encoder infrastructure. The encoder crate has the approved direct `splot-recon` dependency edge, used by validated frame-plane views and the owned input-copy path; it does not expose a public reconstruction API or implement a general closed-loop encoder. The dependency-direction gate also enforces that `splot-cli` may use `splot-encode` only as a dev dependency for decoder-backed oracles, never as a normal or build dependency. Public frame ingestion and the limited 64x64 all-128 Packet success path are tracked by ENC-Y4M-INPUT and ENC-CONTEXT-RECEIVE-PACKET; this row claims only the dependency boundary."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/frame.rs::tests::borrowed_yuv420_frame_accepts_odd_visible_size_without_copying", "xtask/src/main.rs::tests::dependency_direction_allows_encoder_core_parallel_and_recon_edges", "xtask/src/main.rs::tests::dependency_direction_keeps_cli_encoder_edge_dev_only"]
commands = ["cargo test -p splot-encode frame::tests::borrowed_yuv420_frame_accepts_odd_visible_size_without_copying --locked", "cargo test -p xtask dependency_direction --locked", "cargo xtask check-dependency-direction"]
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-INTRA-TOY-V0"
name = "Minimal toy intra encoder path"
category = "encoder"
kind = "encoder-tool"
spec_sections = []
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "splot-encode"
module = "crates/splot-encode/src/context.rs"
change_id = "toy-intra-encoder-v0"
tracking_issue = ""
owner = "encoder"
risk = "high"
notes = "Bootstrap-era toy intra change remains unchecked and parked. It is superseded as the implementation starting point by the Baseline Encoder Profile v1 contract (`DOC-ENCODER-PROGRAM-CONTRACT`); future all-intra work must be re-proposed with current writer, reconstruction, validation, and conformance gates."

[feature.status]
mapped = "partial"
types = "todo"
parse = "not-applicable"
validate = "not-applicable"
write = "todo"
encode = "todo"
decode_check = "todo"
tests = "todo"
avm_diff = "pending"
perf = "todo"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-RATE-CONTROL-V0"
name = "Initial rate control strategy"
category = "encoder"
kind = "encoder-tool"
spec_sections = []
sources = []
crate = "splot-encode"
module = "crates/splot-encode/src/context.rs"
change_id = "rate-control-v0"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "Encoder strategy, not direct spec implementation. Requires design before code."

[feature.status]
mapped = "partial"
types = "todo"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "todo"
decode_check = "not-applicable"
tests = "todo"
avm_diff = "not-applicable"
perf = "todo"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "ENC-SPEED-PRESETS"
name = "Encoder speed preset framework"
category = "encoder"
kind = "encoder-api"
spec_sections = []
sources = []
crate = "splot-encode"
module = "crates/splot-encode/src/runtime.rs"
change_id = "encoder-speed-presets"
tracking_issue = ""
owner = "encoder"
risk = "medium"
notes = "Non-normative encoder runtime-policy framework. `SpeedPreset` accepts the documented 0..=10 range, defaults to 6, is stored in `EncoderRuntimeConfig` beside thread policy, and is exposed through `Context`. It deliberately stays out of `EncoderConfig` and does not yet alter the fixed supported packet path or make a rate-control or mode-decision claim; it cannot by itself satisfy Baseline Encoder Profile v1."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-encode/src/runtime.rs::tests", "crates/splot-encode/src/context.rs::tests::context_exposes_config_and_threads"]
commands = ["cargo test -p splot-encode speed_preset --locked", "cargo test -p splot-encode context_exposes_config_and_threads --locked"]
fixtures = []
diagnostics = []

# ---------------------------------------------------------------------------
# Conformance and differential testing
# ---------------------------------------------------------------------------

[[feature]]
id = "CONF-AVM-DIFF-HARNESS"
name = "AVM differential testing harness"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec/avm"]
crate = "xtask"
module = "xtask/src/conformance.rs"
change_id = "avm-differential-harness"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Reframed (maintainer decision): AVM is a LOCAL oracle/generator only — it is NOT vendored and NOT a build or CI dependency, and no committed code path invokes AVM. AVM locally encodes small AV2 streams (avmenc --codec=av2 --ivf ...) from project-owned synthetic input; those bytes MAY be committed as plain project fixtures and are then validated by the self-contained committed runner (CONF-AVM-VALID-STREAMS, `cargo xtask conformance` / crates/splot-cli/tests/conformance.rs) with no network or AVM. That committed-corpus runner landed under the archived conformance-corpus-foundation change. This row tracks what REMAINS: a LIVE local differential harness against an AVM checkout (opt-in, never CI) — the still-active change_id:avm-differential-harness change — and the `splot-encode` output -> avmdec reverse direction (the encoder library remains fenced). Kept partial / avm_diff pending."

[feature.status]
mapped = "partial"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-AVM-DECODE-ORACLE"
name = "AVM decode-output oracle differential"
category = "conformance"
kind = "conformance"
spec_sections = ["6.18", "7.21"]
sources = ["https://github.com/AOMediaCodec/avm"]
crate = "xtask"
module = "xtask/src/decoder_fixtures.rs"
change_id = "decoder-avm-oracle-corpus"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Committed decode-output differential over the reused CONF-AVM-VALID-STREAMS corpus (tests/conformance/vectors/): tests/conformance/decoder-oracle.toml records, per stream, the AVM decoder raw-output oracle sha256 (avmdec --i420 --rawvideo, visible I420 samples, §6.18) and an expected splot outcome. The committed CI runner (crates/splot-cli/tests/decoder_oracle.rs) decodes each fixture in-process through splot_decode and asserts must_pass (splot decode --output-format raw sha256 equals the recorded oracle) or xfail_splot (fails closed with a recorded decode/unsupported-feature rule id + unsupported reason). AVM is the LOCAL oracle-hash generator only and is never invoked/vendored by the runner, build, or CI; the oracle hashes are committed offline. As of the initial corpus: 47 must_pass, 21 xfail_splot, 0 mismatches (splot matches AVM byte-for-byte or fails closed). tests/conformance/decoder-oracle-coverage.toml is the capability taxonomy; `cargo xtask decoder-fixtures coverage` renders the optional generated coverage report on demand, and `cargo xtask decoder-fixtures verify` plus the optional coverage drift check run in `cargo xtask ci`. xfail_splot fixtures never block CI; an unexpected pass (XPASS) is reported and fails only in strict local mode (SPLOT_DECODER_ORACLE_STRICT_XPASS). Local regeneration tooling lives under tools/decoder-fixtures/. The live avm encode -> splot validate harness stays separate (avm-differential-harness)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
    "xtask/src/decoder_fixtures.rs::tests",
]
commands = [
    "cargo test -p splot-cli --test decoder_oracle",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask decoder-fixtures coverage --check",
]
fixtures = [
    "tests/conformance/decoder-oracle.toml",
    "tests/conformance/decoder-oracle-coverage.toml",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "tests/conformance/vectors/valid/syn-key-intra-64x64.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "CONF-PUBLIC-VECTORS"
name = "Public AV2 vector corpus integration"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec"]
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "fetch-public-vectors"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Use only redistributable/public vectors. Do not vendor unclear-license samples. Only the `cargo xtask fetch-vectors` stub exists today."

[feature.status]
mapped = "partial"
types = "todo"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "todo"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-AVM-PARSER-TRACES"
name = "AVM parser trace comparison"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec/avm"]
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "avm-differential-harness"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Future trace-comparison mode for parser behavior. Depends on AVM harness and stable parser trace output."

[feature.status]
mapped = "todo"
types = "todo"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "todo"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-AVM-VALID-STREAMS"
name = "AVM valid stream conformance"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec/avm"]
crate = "xtask"
module = "xtask/src/conformance.rs"
change_id = "avm-positive-vector-generation"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Committed, self-contained conformance corpus under tests/conformance/: valid AV2 IVF vectors with per-vector provenance plus a manifest.toml of expected outcomes, validated by a committed runner with NO AVM dependency (the CI gate is crates/splot-cli/tests/conformance.rs; the ergonomic entry is `cargo xtask conformance`). AVM is the LOCAL generator/source seed described by each vector's provenance and is never invoked/vendored by the runner/build/CI, so the proof is the committed-vector runner, not a live avm_diff run (avm_diff stays pending). The positive set spans diverse codec features (avm-positive-vector-generation): 64x64 + 128x128 + non-square 96x64 resolutions, 8-bit and 10-bit, intra-only and key+inter, an OBU_OPERATING_POINT_SET stream, and the explicitly retimed minimal tier fixture — all validate clean. DIFFERENTIAL FINDING: AVM's --enable-lcr / --enable-qm produce streams that reference EXTERNAL-HLS-provided resources (a global LCR id with no in-band global LCR; using_qmatrix referencing a QM level with no QM OBU); validated standalone with external HLS disabled, the validator CORRECTLY emits lcr/global-lcr-unavailable (§7.3.8.3) / frame-header/qm-level-unavailable (§7.3.8.9) — confirming those availability checks fire on real reference-encoder output (these two live under tests/conformance/vectors/needs-external-hls/ with the diagnostic as the expected outcome). One bootstrap negative (a truncated copy) exercises the truncation arm. Mutated negatives are conformance-negative-mutator; the live AVM differential harness remains avm-differential-harness."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "xtask/src/conformance.rs::tests",
]
commands = [
    "cargo test -p splot-cli --test conformance",
    "cargo xtask conformance",
]
fixtures = [
    "tests/conformance/manifest.toml",
    "tests/conformance/vectors/valid/syn-key-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-key-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-intra-128x128.ivf",
    "tests/conformance/vectors/valid/syn-inter-96x64.ivf",
    "tests/conformance/vectors/valid/syn-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-ops-64x64.ivf",
    "tests/conformance/vectors/needs-external-hls/syn-lcr-64x64.ivf",
    "tests/conformance/vectors/needs-external-hls/syn-qm-64x64.ivf",
    "tests/conformance/vectors/invalid/syn-key-intra-64x64-truncated.ivf",
]
diagnostics = ["ivf/truncated-frame-payload", "lcr/global-lcr-unavailable", "frame-header/qm-level-unavailable"]

[[feature]]
id = "CONF-AVM-INVALID-STREAMS"
name = "AVM invalid stream conformance"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec/avm"]
crate = "splot-cli"
module = "crates/splot-cli/tests/negative_mutations.rs"
change_id = "conformance-negative-mutator"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Committed, deterministic NEGATIVE MUTATOR (crates/splot-cli/tests/negative_mutations.rs): a table of (committed valid seed, documented in-memory byte/field mutation, expected registered diagnostic) rows. Each clones a committed valid seed from tests/conformance/vectors/valid/, asserts the UNMUTATED seed validates clean (causation), applies a deterministic mutation in memory (the committed file is never written), runs splot_validate::Validator::validate_bytes, and asserts the expected error rule_id is PRESENT with no panic (validate_bytes returning is itself proof of no-panic). Targets stable, decidable diagnostics across the IVF-container / OBU-header / LEB128-framing layers (4 distinct ids across 3 layers): ivf/invalid-header-length, bitstream/parse-error, obu-header/temporal-layer-zero-only-types, obu-header/base-layer-only-types. Exercises only EXISTING registered diagnostics (no new ones). These are hand-crafted, targeted mutations with named expected diagnostics — NOT random fuzzing (the cargo-fuzz targets own no-panic over arbitrary bytes) and NOT a live avm_diff run (avm_diff stays pending; AVM is the LOCAL generator of the seeds only). Note: the container auto-detect (parse_bitstream_partial) routes a corrupt-DKIF input to raw Annex B, so ivf/invalid-signature is unreachable via validate_bytes; the IVF-layer row keeps the magic intact and corrupts header_len instead."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/negative_mutations.rs::negative_mutations_emit_expected_diagnostics",
]
commands = [
    "cargo test -p splot-cli --test negative_mutations",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-key-intra-64x64.ivf",
]
diagnostics = [
    "ivf/invalid-header-length",
    "bitstream/parse-error",
    "obu-header/temporal-layer-zero-only-types",
    "obu-header/base-layer-only-types",
]

[[feature]]
id = "CONF-PUBLIC-VECTOR-LICENSE-REVIEW"
name = "Public vector license review"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = ["https://github.com/AOMediaCodec"]
crate = "docs"
module = "docs/CONFORMANCE.md"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Review redistributability before vendoring or linking public AV2 vector corpora."

[feature.status]
mapped = "todo"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "todo"
tests = "todo"
avm_diff = "pending"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-INSPECT-SNAPSHOTS"
name = "Inspector snapshot tests"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/src/commands/inspect.rs"
change_id = "inspect-snapshot-tests"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "insta golden snapshot tests of `splot inspect --json` now freeze the inspector's per-OBU JSON for a diverse set of committed fixtures (crates/splot-cli/tests/inspect_snapshots.rs, snapshots in crates/splot-cli/tests/snapshots/). The inspect output is deterministic — per-OBU byte offsets, sizes, and parsed fields, with no paths, timestamps, or filenames — so any future change to the inspect output for a committed fixture surfaces as a snapshot diff for explicit review (cargo insta review). insta is a dev-only workspace dependency. Basic CLI integration tests also exist (see CLI-INSPECT)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/inspect_snapshots.rs"]
commands = ["cargo test -p splot-cli --test inspect_snapshots"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/operating-point-set.av2", "tests/fixtures/film-grain.av2", "tests/fixtures/buffer-removal-timing.av2", "tests/fixtures/metadata-group.av2", "tests/fixtures/metadata-short.av2", "tests/fixtures/frame-header-core.av2", "tests/fixtures/frame-header-core-mfh.av2", "tests/fixtures/frame-header-prefix.av2", "tests/fixtures/quantizer-matrix.av2", "tests/fixtures/seq-header-tile-params.av2", "tests/fixtures/padding.av2"]
diagnostics = []

[[feature]]
id = "CONF-CLI-SNAPSHOT-COVERAGE"
name = "CLI help + inspect text snapshots"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/tests/help_snapshots.rs"
change_id = "cli-snapshot-coverage"
tracking_issue = ""
owner = "conformance"
risk = "low"
notes = "Extends CONF-INSPECT-SNAPSHOTS beyond the --json surface. insta golden snapshots freeze (1) the `splot validate --help` and `splot inspect --help` text — the backward-compat tripwire for the validator productization flag work, so additive flags update the goldens intentionally and an accidental surface change is caught; the top-level `splot --help` is deliberately not snapshotted to stay decoupled from decode/encode wording — and (2) the `splot inspect` human dump (default + --headers) over representative committed fixtures. Goldens are deterministic (no paths/timestamps/version). Tests: crates/splot-cli/tests/help_snapshots.rs, crates/splot-cli/tests/inspect_text_snapshots.rs (snapshots in crates/splot-cli/tests/snapshots/)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/help_snapshots.rs",
    "crates/splot-cli/tests/inspect_text_snapshots.rs",
]
commands = [
    "cargo test -p splot-cli --test help_snapshots --test inspect_text_snapshots",
]
fixtures = [
    "tests/fixtures/conformant.av2",
    "tests/fixtures/operating-point-set.av2",
    "tests/fixtures/frame-header-core.av2",
    "tests/fixtures/metadata-group.av2",
]
diagnostics = []

[[feature]]
id = "CONF-FUZZ-NO-PANIC"
name = "Parser no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = []
sources = []
crate = "fuzz"
module = "fuzz/fuzz_targets/parse_obu.rs"
change_id = "fuzz-validator-targets"
tracking_issue = ""
owner = "conformance"
risk = "high"
notes = "Malformed parser/validator input must return errors/reports, never panic. Four parser and validator fuzz targets cover the original public byte-consuming validation surfaces: parse_obu (read_leb128 / read_obu_header / parse_annex_b_obus), parse_ivf (is_ivf / parse_ivf_header / parse_ivf_partial), parse_bitstream (parse_bitstream_partial container auto-detect + Annex-B/IVF envelope parsing; OBU payload parsers are reached via validate_bytes, not this target), and validate_bytes (Validator::validate_bytes_with_options, which transitively reaches every OBU payload parser, both container formats, and every validator check). Decode planner fuzzing is tracked by `DECODE-BYTE-STREAM-PLANNER`; minimal tier hash fuzzing is tracked by `CONF-DECODE-RUNTIME-HASH-FUZZ`. Stable-toolchain proptests cover the parser/validator invariant in CI (splot-core parser modules plus crates/splot-validate/tests/validator_never_panics.rs); CI also runs a blocking per-target fuzz smoke (~45s each) on every cargo-fuzz target in every PR (.github/workflows/ci.yml fuzz-smoke), and cargo-fuzz still needs nightly to run locally. The cargo-fuzz corpus (fuzz/corpus) is gitignored and not committed, so it is not listed as proof. parse and validate are done: every parser surface and the validator are now fuzz-reachable and proptest-covered on stable. decode_check stays partial: the no-panic work hardens robustness but does not expand what the inspector/validator can check for conformance, which is what this stage tracks."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-core/src/annexb.rs::proptests", "crates/splot-core/src/ivf.rs::proptests", "crates/splot-core/src/stream.rs::proptests", "crates/splot-validate/tests/validator_never_panics.rs"]
commands = ["cargo test -p splot-core", "cargo test -p splot-validate", "cargo +nightly fuzz run parse_obu", "cargo +nightly fuzz run parse_ivf", "cargo +nightly fuzz run parse_bitstream", "cargo +nightly fuzz run validate_bytes"]
fixtures = []
diagnostics = []

[[feature]]
id = "CONF-DECODE-RUNTIME-HASH-FUZZ"
name = "Decode runtime hash no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.19", "5.20.1", "7.1", "7.21"]
sources = [
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/decode_runtime_hash_bytes.rs"
change_id = "decode-runtime-hash-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for `DecodeContext::decode_hash_report_bytes`: it reuses a single-thread `DecodeContext`, applies finite `DecodeLimits`, feeds both arbitrary raw bytes and bounded mutations of the committed minimal IVF fixture, asserts only the stable minimal hash-report shape on `Ok`, and accepts typed `DecodeError` returns on unsupported or malformed inputs. This is conformance/robustness proof for the existing minimal tier hash byte API only. It does not claim broad runtime decode, full tile syntax, full reconstruction, Y4M/raw output fuzzing, metadata hash verification, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/decode_runtime_hash_bytes.rs",
    "crates/splot-decode/src/output/hash.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-decode output::hash --locked",
    "cargo +nightly fuzz run decode_runtime_hash_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "fuzz/fuzz_targets/decode_runtime_hash_bytes.rs",
]
diagnostics = ["decode/malformed-source", "decode/resource-limit", "decode/unsupported-feature"]

[[feature]]
id = "CONF-DECODE-RUNTIME-Y4M-FUZZ"
name = "Decode runtime Y4M no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.19", "5.20.1", "6.4.1", "6.16.13", "6.17.4.4", "7.1", "7.21.1", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/decode_runtime_y4m_bytes.rs"
change_id = "decode-runtime-y4m-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for `DecodeContext::decode_y4m_bytes`: it reuses a single-thread `DecodeContext`, applies finite `DecodeLimits`, feeds both arbitrary raw bytes and bounded mutations of the committed minimal IVF fixture, writes only to bounded in-memory writers, asserts only the stable minimal Y4M stream shape on `Ok`, and accepts typed `DecodeError` returns on unsupported, malformed, resource-limit, or output failures. This is conformance/robustness proof for the existing minimal tier Y4M byte API only. It does not claim broad runtime decode, new AV2 syntax or semantics, CLI filesystem publication, temp-file atomicity fuzzing, raw output, hash-report output, post-film-grain output, show-existing/flush scheduling, film-grain synthesis, reference refresh, motion-field storage, metadata MD5 verification, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, new dependencies, or Y4M support beyond the committed minimal IVF tier."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/decode_runtime_y4m_bytes.rs",
    "crates/splot-decode/src/output/y4m.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-decode output::y4m --locked",
    "cargo +nightly fuzz run decode_runtime_y4m_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "fuzz/fuzz_targets/decode_runtime_y4m_bytes.rs",
]
diagnostics = ["decode/malformed-source", "decode/resource-limit", "decode/unsupported-feature", "decode/output-error"]

[[feature]]
id = "CONF-DECODE-RUNTIME-RAW-FUZZ"
name = "Decode runtime raw no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.19", "5.20.1", "6.4.1", "6.16.13", "6.17.4.4", "7.1", "7.21.1", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/decode_runtime_raw_bytes.rs"
change_id = "decode-minimal-tier-raw-output"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for `DecodeContext::decode_raw_bytes`: it reuses a single-thread `DecodeContext`, applies finite `DecodeLimits`, feeds both arbitrary raw bytes and bounded mutations of the committed minimal IVF fixture, writes only to bounded in-memory writers, asserts only the stable minimal raw sample byte stream shape on `Ok`, and accepts typed `DecodeError` returns on unsupported, malformed, resource-limit, or output failures. This is conformance/robustness proof for the existing minimal tier raw byte API only. It does not claim broad runtime decode, new AV2 syntax or semantics, CLI filesystem publication, temp-file atomicity fuzzing, hash-report output, Y4M output, post-film-grain output, show-existing/flush scheduling, film-grain synthesis, reference refresh, motion-field storage, metadata MD5 verification, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, new dependencies, or raw support beyond the committed minimal tier frame-unit shape."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/decode_runtime_raw_bytes.rs",
    "crates/splot-decode/src/output/raw.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-decode output::raw --locked",
    "cargo +nightly fuzz run decode_runtime_raw_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "fuzz/fuzz_targets/decode_runtime_raw_bytes.rs",
]
diagnostics = ["decode/malformed-source", "decode/resource-limit", "decode/unsupported-feature", "decode/output-error"]

[[feature]]
id = "CONF-RECON-Y4M-OUTPUT-FUZZ"
name = "Y4M output serialization no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["6.4.1", "6.16.13", "6.17.4.4", "7.21.1", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/recon_y4m_output_bytes.rs"
change_id = "recon-y4m-output-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for `splot-recon` Y4M output serialization: it normalizes arbitrary bytes into small valid `DecodedFrame<u8>` or `DecodedFrame<u16>` values across supported bit depths and pixel formats, serializes stream headers, frame headers, and visible sample payloads through `Y4mWriter`, and exercises typed stream/frame mismatch and failing-writer paths. This is conformance/robustness proof for caller-supplied decoded-frame output serialization only. It does not claim AV2 bitstream decode, `DecodeContext::decode_y4m_bytes` fuzzing, CLI output publication, raw output, hash report output, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/recon_y4m_output_bytes.rs",
    "crates/splot-recon/src/y4m.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-recon y4m --locked",
    "cargo +nightly fuzz run recon_y4m_output_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/recon_y4m_output_bytes.rs",
]
diagnostics = []

[[feature]]
id = "CONF-RECON-FRAME-HASH-FUZZ"
name = "Decoded-frame hash serialization no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["6.4.1", "6.16.13", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/recon_frame_hash_bytes.rs"
change_id = "recon-frame-hash-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for `splot-recon` decoded-frame hash input serialization and digest computation: it normalizes arbitrary bytes into small valid `DecodedFrame<u8>` or `DecodedFrame<u16>` values across supported bit depths, sample storage choices, pixel formats, aligned crop origins, storage padding, and stride padding; exercises `DecodedFrameHashInput::byte_len`, `write_to`, and `compute_hash`; checks stable hash identifiers/hex/raw-byte helpers; and verifies non-visible padding and output index metadata do not affect emitted bytes or digests. This is conformance/robustness proof for caller-supplied decoded-frame hash input only. It does not claim AV2 bitstream decode, `DecodeContext` fuzzing, CLI output publication, raw/Y4M output, AV2 decoded-frame-hash metadata MD5 verification, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/recon_frame_hash_bytes.rs",
    "crates/splot-recon/src/hash_input.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-recon hash --locked",
    "cargo +nightly fuzz run recon_frame_hash_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/recon_frame_hash_bytes.rs",
]
diagnostics = []

[[feature]]
id = "CONF-RECON-FRAME-PLANE-TYPES-FUZZ"
name = "Decoded-frame and plane runtime type no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["6.4.1", "6.17.4.1", "6.17.4.4", "7.1", "7.21.1", "7.21.2", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/recon_frame_plane_types_bytes.rs"
change_id = "recon-frame-plane-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for the `splot-recon` decoded-frame and plane runtime type model: it normalizes arbitrary bytes into bounded valid and intentionally invalid `BitDepth`, `PixelFormat`, `PlaneSize`, `PlaneRect`, `Plane<T>`, `FramePlanes<T>`, `DecodedFrameInfo`, `DecodedFrame<T>`, borrowed view, and `SharedFrame` cases, then checks constructor/accessor invariants and typed `ReconError` returns with no panic. This is conformance/robustness proof for caller-supplied frame/plane model infrastructure only. It does not claim AV2 bitstream decode, `DecodeContext` fuzzing, reconstruction algorithms, frame hash/raw/Y4M serialization behavior, output scheduling, film grain, metadata MD5 verification, reference refresh, §7.23 frame-store update semantics, resource diagnostics, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics beyond the existing source-backed runtime type constructors."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/recon_frame_plane_types_bytes.rs",
    "crates/splot-recon/src/format.rs::tests",
    "crates/splot-recon/src/geometry.rs::tests",
    "crates/splot-recon/src/plane.rs::tests",
    "crates/splot-recon/src/frame.rs::tests",
    "crates/splot-recon/src/views.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-recon frame --locked",
    "cargo test -p splot-recon plane --locked",
    "cargo test -p splot-recon views --locked",
    "cargo +nightly fuzz run recon_frame_plane_types_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/recon_frame_plane_types_bytes.rs",
]
diagnostics = []

[[feature]]
id = "CONF-RECON-REFERENCE-FRAME-STORE-FUZZ"
name = "Reference-frame store no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["3"]
sources = ["docs/spec/av2/1.0.0/03-symbols.md#s-3"]
crate = "fuzz"
module = "fuzz/fuzz_targets/recon_reference_frame_store_bytes.rs"
change_id = "recon-reference-frame-store-refresh-flags"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for the `splot-recon` reference-frame store storage API: it normalizes arbitrary bytes into bounded public `ReferenceSlot`, `ReferenceRefreshMask`, and `ReferenceFrameStore<F>` operation sequences over a tiny non-Clone payload, then checks capacity validation, slot construction, refresh-mask construction, valid-but-out-of-capacity errors, mask containment and selected-slot iteration, `contains_slot`, `get`, `put`, replacement return values, `refresh_slots_with`, `take`, `clear`, occupancy, `is_empty`, and ascending `entries` iteration against a simple oracle model. Refresh fuzzing verifies zero-mask no-ops, selected-slot replacement returns, non-selected slot preservation, ascending producer calls, and no producer/store mutation for out-of-capacity masks. This is conformance/robustness proof for caller-supplied reference-frame storage only. It does not claim AV2 bitstream decode, `DecodeContext` fuzzing, AV2 `NumRefFrames` derivation, `ActiveNumRefFrames`, `RefValid`, `frame_to_refresh`, `refresh_frame_flags` parsing or inference, output scheduling, show-existing behavior, motion-field storage, full §7.23 frame-store update semantics, resource diagnostics, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/recon_reference_frame_store_bytes.rs",
    "crates/splot-recon/src/reference.rs::tests",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-recon reference --locked",
    "cargo +nightly fuzz run recon_reference_frame_store_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/recon_reference_frame_store_bytes.rs",
]
diagnostics = []

[[feature]]
id = "CONF-RECON-INTRA-PREDICTION-FUZZ"
name = "Reconstruction intra prediction no-panic fuzzing"
category = "conformance"
kind = "conformance"
spec_sections = ["3", "4.8", "6.4.1", "6.17.4.1", "6.17.4.4", "7.13.2.1", "7.13.2.2", "7.13.2.8", "7.13.2.10", "7.13.2.11", "7.13.2.12", "7.13.2.13", "7.13.3.22", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-22",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "fuzz"
module = "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs"
change_id = "recon-intra-prediction-fuzz"
tracking_issue = ""
owner = "conformance"
risk = "medium"
notes = "Self-contained cargo-fuzz target for existing `splot-recon` intra prediction and current-frame workspace primitives: it normalizes arbitrary bytes into bounded structured DC, subsampled DC, IBP DC, PAETH, smooth, H/V cardinal directional, one-sided directional-angle, middle directional-angle (`RECON-INTRA-MIDDLE-DIRECTIONAL-ANGLE-PREDICTION`), workspace directional-angle handoff (`RECON-WORKSPACE-DIRECTIONAL-ANGLE-PREDICTION`), and workspace prediction cases over public APIs, exercises caller-owned output buffers, in-storage edge extraction, successful interior chroma workspace writes, luma directional-angle rejection, and typed edge/output-shape/workspace-boundary errors. This is conformance/robustness proof for source-backed reconstruction primitives only. It does not claim AV2 bitstream decode, `DecodeContext` fuzzing, full §7.13 intra reconstruction, luma/MRL/IDIF/full-dispatch directional angles, data driven intra prediction, general directional-angle IBP, filter intra, CfL/CCTX, palette, residual, transform, quantization, loop filtering, runtime output scheduling, AVM/dav2d agreement, filesystem I/O, network I/O, subprocesses, or new AV2 semantics."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
    "crates/splot-recon/src/intra.rs::tests",
    "crates/splot-recon/src/intra_basic.rs::tests",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests",
    "crates/splot-recon/src/intra_directional.rs::tests",
    "crates/splot-recon/src/intra_directional_angle.rs::tests",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests",
    "crates/splot-recon/src/intra_smooth.rs::tests",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/workspace_intra_directional_angle_tests.rs",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-recon ibp --locked",
    "cargo test -p splot-recon intra_dc_subsampled --locked",
    "cargo test -p splot-recon intra_directional --locked",
    "cargo test -p splot-recon intra_directional_angle --locked",
    "cargo test -p splot-recon intra --locked",
    "cargo test -p splot-recon workspace_intra_directional_angle --locked",
    "cargo test -p splot-recon workspace --locked",
    "cargo +nightly fuzz run recon_intra_prediction_bytes (local/nightly; CI fuzz smoke runs every target)",
]
fixtures = [
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
    "crates/splot-recon/src/intra_directional_angle.rs",
    "crates/splot-recon/src/intra_directional_angle_tests.rs",
    "crates/splot-recon/src/intra_ibp_dc.rs",
    "crates/splot-recon/src/workspace_intra_dc.rs",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs",
    "crates/splot-recon/src/workspace_intra_directional_angle_tests.rs",
]
diagnostics = []

# ---------------------------------------------------------------------------
# CLI behaviors (thin UI over the libraries)
# ---------------------------------------------------------------------------

[[feature]]
id = "CLI-VALIDATE"
name = "splot validate command"
category = "cli"
kind = "cli"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/src/commands/validate.rs"
change_id = ""
tracking_issue = ""
owner = "cli"
risk = "low"
notes = "Thin CLI over splot-validate: human and --json reports, --strict mode, documented exit codes (0 conformant / 1 errors / 2 I/O)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/cli.rs"]
commands = ["cargo test -p splot-cli"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/bad-global-xlayer.av2", "tests/fixtures/truncated.av2"]
diagnostics = []

[[feature]]
id = "CLI-VALIDATE-OUTPUT-CONTROLS"
name = "splot validate output controls (--max-diagnostics / --summary-only)"
category = "cli"
kind = "cli"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/src/commands/validate.rs"
change_id = "validate-output-controls"
tracking_issue = ""
owner = "cli"
risk = "low"
notes = "Adds --max-diagnostics N and --summary-only to `splot validate`, backed by a library-first RenderOptions/RenderedReport in splot-validate (crates/splot-validate/src/render.rs): render_text() reproduces the Display output at default options (parity-tested) and rendered() produces the JSON view. Presentation-only: which diagnostics are computed, the summary counts, and the exit code (Validator::is_acceptable over the FULL report) are unchanged. The truncation notice is render metadata, never a Diagnostic/rule_id (so it does not touch the diagnostic registry). --summary-only takes precedence over --max-diagnostics; default options are byte-compatible with the historical {\"diagnostics\":[...]} JSON. The validate --help golden updates intentionally (CONF-CLI-SNAPSHOT-COVERAGE)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-validate/src/render.rs::tests",
    "crates/splot-cli/tests/validate_snapshots.rs",
    "crates/splot-cli/tests/cli.rs",
]
commands = [
    "cargo test -p splot-validate render",
    "cargo test -p splot-cli --test validate_snapshots",
    "cargo test -p splot-cli --test cli",
]
fixtures = [
    "tests/fixtures/bad-global-xlayer.av2",
    "tests/fixtures/operating-point-set.av2",
]
diagnostics = []

[[feature]]
id = "CLI-VALIDATE-EXPLAIN"
name = "splot explain command + diagnostic registry"
category = "cli"
kind = "cli"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/src/commands/explain.rs"
change_id = "validate-explain-rule"
tracking_issue = ""
owner = "cli"
risk = "low"
notes = "Adds `splot explain <rule-id>` (--json / --list) backed by a doc-generated diagnostic registry in splot-validate (crates/splot-validate/src/explain/, DiagnosticInfo + explain/all/did_you_mean). The registry (generated.rs) is generated from docs/DIAGNOSTICS.md by `cargo xtask gen-explain` (xtask/src/explain_registry.rs): it parses the 4-col emitted-diagnostics tables into id/severity/section/summary; `--check` (folded into cargo xtask ci) fails on drift, so the registry can never diverge from the CI-enforced doc or invent data. The generated table is rustfmt-stable (#[rustfmt::skip] + fully-qualified paths) and its id literals (all documented) keep check-diagnostic-registry green. The CLI is read-only: it changes no validator behavior and emits no diagnostics; an unknown id / missing arg is a clean exit-2 error with a same-namespace hint, never a panic. The 13 */syntax registry identifiers are excluded (not user-visible). The summary is the doc's one-line condition (no invented prose); richer per-id text would be added to the doc and flow through."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/explain_registry.rs::tests",
    "crates/splot-validate/src/explain/mod.rs::tests",
    "crates/splot-cli/tests/explain_cli.rs",
]
commands = [
    "cargo xtask gen-explain --check",
    "cargo test -p xtask explain_registry",
    "cargo test -p splot-validate explain",
    "cargo test -p splot-cli --test explain_cli",
]
fixtures = ["docs/DIAGNOSTICS.md"]
diagnostics = []

[[feature]]
id = "CLI-INSPECT"
name = "splot inspect command"
category = "cli"
kind = "cli"
spec_sections = []
sources = []
crate = "splot-cli"
module = "crates/splot-cli/src/commands/inspect.rs"
change_id = ""
tracking_issue = ""
owner = "cli"
risk = "low"
notes = "Thin CLI over splot-core: lists OBUs and headers, --headers and --json output, prints the parseable prefix before a tail error. Snapshot tests are tracked by CONF-INSPECT-SNAPSHOTS."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/cli.rs"]
commands = ["cargo test -p splot-cli"]
fixtures = ["tests/fixtures/conformant.av2", "tests/fixtures/prefix-then-truncated.av2"]
diagnostics = []

# ---------------------------------------------------------------------------
# Automation and documentation
# ---------------------------------------------------------------------------

[[feature]]
id = "XTASK-CONVENTIONAL-COMMITS"
name = "Conventional commit enforcement"
category = "automation"
kind = "automation"
spec_sections = []
sources = ["https://www.conventionalcommits.org/"]
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "enforce-conventional-commits"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds `cargo xtask check-conventional-title` and `cargo xtask check-conventional-commits`, documents the allowed subject/title format, and wires the checks into CI for PR titles plus PR and push commit ranges."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/main.rs::tests"]
commands = [
    "cargo xtask check-conventional-title \"ci: enforce conventional commits\"",
    "cargo xtask check-conventional-commits",
    "cargo test -p xtask conventional",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-FEATURE-STATUS"
name = "xtask feature status reporting and checks"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/feature_status.rs"
change_id = "spec-coverage-doc"
tracking_issue = ""
owner = "automation"
risk = "medium"
notes = "Implements feature-status (table/json/markdown), check-feature-status (drift guard for the matrix plus any committed generated renders), spec-coverage (text summary; markdown renders the per-spec-section coverage document with spec-mirror links on demand), and writer-coverage (text summary plus on-demand markdown render with one row per writable feature [every bitstream-syntax feature plus every feature with a landed writer], write maturity, module, and write-status counts). Wired into `cargo xtask ci`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/feature_status.rs::tests"]
commands = [
    "cargo xtask feature-status",
    "cargo xtask check-feature-status",
    "cargo xtask spec-coverage",
    "cargo xtask spec-coverage --format markdown --output docs/SPEC-COVERAGE.md",
    "cargo xtask writer-coverage --format markdown --output docs/spec-coverage-writer.md",
    "cargo test -p xtask",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-AUDIT-SCOPE"
name = "Changed-file AV2 audit scope"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/audit_scope.rs"
change_id = "add-splot-audit-protocols"
tracking_issue = ""
owner = "automation"
risk = "medium"
notes = "Computes deterministic changed-file scope for the heavy AV2 conformance audit. Supports diff mode, scheduled ledger mode, force-wide review triggers, dynamic workspace-member discovery, and deterministic ledger output."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/audit_scope.rs::tests"]
commands = [
    "cargo xtask audit-scope --format json",
    "cargo test -p xtask audit_scope",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-DIAGNOSTIC-REGISTRY"
name = "Validator diagnostic registry enforcement"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/diagnostic_registry.rs"
change_id = "diagnostic-registry-enforcement"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Enforces that docs/DIAGNOSTICS.md lists exactly the diagnostic rule ids present in crates/splot-validate/src (between the diagnostics-registry markers). Extracts rule-id string literals from non-test, non-comment validator source and diffs against the documented set, failing on drift in either direction. Strict full-id superset of the prefix-level scan in XTASK-FEATURE-STATUS."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/diagnostic_registry.rs::tests"]
commands = [
    "cargo xtask check-diagnostic-registry",
    "cargo test -p xtask diagnostic_registry",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-VALIDATOR-MODULE-SPLIT"
name = "Validator module split"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-validate"
module = "crates/splot-validate/src/validator/mod.rs"
change_id = "validator-source-line-budget"
tracking_issue = ""
owner = "validator"
risk = "low"
notes = "Replaces the former 22k-line crates/splot-validate/src/validator/mod.rs file with a responsibility-oriented validator/ module tree. The public splot_validate::Validator API and diagnostic behavior remain unchanged; tests stay under validator/tests/ as validator children so private helpers do not need to become public."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-validate/src/validator/tests/mod.rs"]
commands = [
    "cargo test -p splot-validate --all-targets --locked",
    "cargo xtask check-diagnostic-registry",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-SOURCE-LINES"
name = "Rust source-file line budget"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/source_lines.rs"
change_id = "validator-source-line-budget"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds cargo xtask check-source-lines: checked Rust source files warn above 1000 physical lines and fail above the 2500-line hard cap unless covered by a documented temporary allowance with a recorded cap and rationale. Wired into cargo xtask ci and GitHub Actions."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/source_lines.rs::tests"]
commands = [
    "cargo xtask check-source-lines",
    "cargo test -p xtask source_lines",
]
fixtures = []
diagnostics = []

[[feature]]
id = "XTASK-CHECK-FIXTURES"
name = "Test-fixture manifest gate"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/fixtures.rs"
change_id = "fixtures-manifest-and-check"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds tests/fixtures/MANIFEST.toml (name/path/sha256/description/category/expect, reusing the conformance expect grammar) and `cargo xtask check-fixtures`: hermetic (no validator, no decoder, no network) verification that every committed tests/fixtures/*.av2 is listed once, exists, hashes to its recorded sha256, has a unique name/path, and a category consistent with expect, with no orphan committed fixtures. Wired into cargo xtask ci and a GitHub Actions step. The expect outcomes are separately verified against the real validator in-process by crates/splot-cli/tests/fixture_manifest.rs. Documented in docs/TESTING.md. Complements CONF-INSPECT-SNAPSHOTS and the conformance corpus."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/fixtures.rs::tests",
    "crates/splot-cli/tests/fixture_manifest.rs",
]
commands = [
    "cargo xtask check-fixtures",
    "cargo test -p xtask fixtures",
    "cargo test -p splot-cli --test fixture_manifest",
]
fixtures = ["tests/fixtures/MANIFEST.toml"]
diagnostics = []

[[feature]]
id = "XTASK-GEN-TABLES"
name = "AV2 § 9 tables code generator"
category = "automation"
kind = "automation"
spec_sections = []
sources = ["https://av2.aomedia.org/v1.0.0/attachments/all_tables.h"]
crate = "xtask"
module = "xtask/src/gen_tables.rs"
change_id = "gen-tables-additional-tables"
tracking_issue = ""
owner = "automation"
risk = "medium"
notes = "`cargo xtask gen-tables [--check]`: parses the committed § 9 attachment all_tables.h (strip C comments; classify each `Name[dims] = { ... }` declaration as numeric or symbolic; brace-nesting infers the rectangular array shape) and emits typed i32 const arrays with SPDX + generated-from-provenance headers. Per-module output dirs (`output_dirs_for`): the §9.4 `quantizer`, §9.6 `transform_1d`, §9.7 `secondary_transform`, and §9.8 `loop_restoration` modules go to the dependency-free `crates/splot-tables/src/tables/` crate (INFRA-SHARED-SPEC-TABLES), while §9.8 also continues to be emitted into `crates/splot-core/src/tables/` for existing core consumers. Deterministic (stable ordering, no timestamps). Fails loudly on any unmodeled construct (ragged arrays, unbalanced braces, or a symbolic table not in the explicit SKIP_ALLOWLIST) — never silently truncates. `--check` diffs regenerated output against committed files across both output dirs and is wired into run_ci (always-run internal check, like check-feature-status/check-diagnostic-registry). Powers AV2-9-ADDITIONAL-TABLES."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "done"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/gen_tables.rs::tests"]
commands = [
    "cargo xtask gen-tables --check",
    "cargo test -p xtask gen_tables",
]
fixtures = ["docs/spec/av2/1.0.0/attachments/all_tables.h"]
diagnostics = []

[[feature]]
id = "INFRA-SHARED-SPEC-TABLES"
name = "Shared dependency-free spec-tables crate"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["9"]
sources = ["https://av2.aomedia.org/v1.0.0/attachments/all_tables.h"]
crate = "splot-tables"
module = "crates/splot-tables/src/lib.rs"
change_id = "shared-spec-tables-crate"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the dependency-free `splot-tables` crate as the single home for the AV2 §9 tables that `splot-recon` consumes without depending on `splot-core` (the one-way rule forbids `splot-recon -> splot-core`): the §9.6 `transform_1d` and §9.7 `secondary_transform` transform-kernel modules (for the §7.15 inverse transform), the §9.4 `quantizer` module (`QUANTIZER_MATRIX`, `QM_OFFSET`, relocated for the §7.14.4 quantization-matrix weighting, RECON-DEQUANT-QM-WEIGHT), and the §9.8 `loop_restoration` module needed by §7.20.4 pixel-classified Wiener classification (`RECON-PC-WIENER-CLASSIFICATION`). The shared modules are routed by `output_dirs_for` in `cargo xtask gen-tables` (AV2-9-ADDITIONAL-TABLES, XTASK-GEN-TABLES); §9.8 stays generated in `splot-core::tables` as well because core still has current consumers. The generator writes and drift-checks both output directories, and mirror cross-check spot tests cover the shared kernels and PC-Wiener tables. The crate depends on no other `splot-*` crate and no external crate (`check-dependency-direction` records it as a dependency-free leaf), so any crate may depend on it. It does not hand-transcribe or change table contents."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-tables/tests/tables_spot.rs",
    "xtask/src/gen_tables.rs::tests::regeneration_is_byte_identical_and_deterministic",
]
commands = [
    "cargo build --workspace --locked",
    "cargo test -p splot-tables --locked",
    "cargo xtask gen-tables --check",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-license-headers",
]
fixtures = [
    "crates/splot-tables/Cargo.toml",
    "crates/splot-tables/src/lib.rs",
    "xtask/src/gen_tables.rs",
]
diagnostics = []

[[feature]]
id = "XTASK-CI-QUALITY-GATES"
name = "CI quality gates (docs build + coverage threshold)"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "ci-quality-gates"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds the blocking strict-docs gate (RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps --locked) and the splot-validate line-coverage threshold (cargo llvm-cov report --fail-under-lines 90, scoped via --ignore-filename-regex) to both cargo xtask ci/coverage and .github/workflows/ci.yml, and fixes local/CI parity by adding --all-features to the offline cargo-deny invocation."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps --locked",
    "cargo xtask ci",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-CI-PIPELINE-SPEEDUPS"
name = "CI pipeline speedups"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "ci-pipeline-speedups"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Removes the redundant explicit `cargo build --workspace --all-targets --locked` pass from `cargo xtask ci` and `.github/workflows/ci.yml` because the preceding all-target clippy pass plus all-target tests already compile the workspace. GitHub CI also sets `CARGO_INCREMENTAL=0`, bumps the cargo cache namespace so future target caches are smaller, and caches the pinned `dupehound` binary so warm runs skip `cargo install`. No codec behavior, AV2 status, diagnostics, dependency graph, or test coverage changes."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "done"

[feature.proof]
tests = []
commands = [
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [".github/workflows/ci.yml", "README.md", "AGENTS.md"]
diagnostics = []

[[feature]]
id = "DECODE-GENERIC-RUNTIME-DESLOP"
name = "Decoder generic runtime deslop"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.3", "5.20.7.25", "5.20.7.27", "7.13.2", "7.13.3.16", "7.14.4", "7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-15",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decoder-runtime-deslop"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extracts shared block context, capability diagnostics, intra prediction, residual planning, and inter prediction dispatch from the minimal tier while preserving decoded output and existing unsupported-feature diagnostics. The follow-on cleanup collapses repeated CDF row lifecycle, chroma/recon write-back, inter residual, general-intra neighbour code, coefficient transform-size derivation, FSC pass plumbing, IntrABC probe scans and ranges, partition child construction, runtime diagnostics, MV-stack neighbour probes, intra joint neighbour sampling, decoder-facing recon directional-angle reference walks, Wiener NS LR transform-record/live-storage/recon helpers, inter decode/test helpers, root runtime/reconstruction helpers, CDEF/deblock scheduling helpers, tile-payload root/partition helpers, tile-payload CDF row traversal, selector-bound checks and tests, tile-payload test work-unit setup, and coeff-loop seeded context fixtures while deleting long runtime prose. The implementation-comment budget is ratcheted to 262 and the duplication budget is ratcheted to 6327; source-line hard allowances remain empty."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/tile/block_context.rs::tests",
    "crates/splot-decode/src/support/capability.rs::tests",
    "crates/splot-decode/src/pipeline/general_intra_unit_tests.rs::lossless_luma_uses_generic_prediction_planner",
    "crates/splot-decode/src/residual/pipeline.rs::tests",
    "crates/splot-decode/src/filters/cdef.rs::tests",
    "crates/splot-decode/src/filters/deblock.rs::tests",
    "crates/splot-decode/src/reference/buffer.rs::tests",
    "crates/splot-decode/src/pipeline/reconstruct.rs::tests",
    "crates/splot-decode/src/prediction/inter/mc.rs::tests",
    "crates/splot-decode/src/pipeline/general_intra_tests",
    "crates/splot-decode/src/pipeline/general_intra_tests/general_intra_cdef_tests.rs",
    "crates/splot-decode/src/prediction/inter/tests",
    "crates/splot-decode/src/bitstream/tile_payload",
    "crates/splot-decode/src/bitstream/tile_payload/cdf",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop",
]
commands = [
    "cargo clippy -p splot-decode -p splot-recon --all-targets --all-features --locked -- -D warnings",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-decode --lib tile_payload::cdf --locked",
    "cargo test -p splot-decode tile_payload --lib --locked",
    "cargo xtask check-comment-density",
    "cargo xtask check-duplication",
    "cargo xtask check-source-lines",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/tile/block_context.rs",
    "crates/splot-decode/src/support/capability.rs",
    "crates/splot-decode/src/filters/cdef.rs",
    "crates/splot-decode/src/filters/deblock.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs",
    "crates/splot-decode/src/pipeline/general_intra_tests/general_intra_cdef_tests.rs",
    "crates/splot-decode/src/prediction/intra.rs",
    "crates/splot-decode/src/support/pipeline_limits.rs",
    "crates/splot-decode/src/reference/buffer.rs",
    "crates/splot-decode/src/residual/pipeline.rs",
    "crates/splot-decode/src/prediction/inter.rs",
    "crates/splot-decode/src/prediction/inter/block.rs",
    "crates/splot-decode/src/prediction/inter/compound.rs",
    "crates/splot-decode/src/prediction/inter/cross_frame.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs",
    "crates/splot-decode/src/prediction/inter/mc.rs",
    "crates/splot-decode/src/prediction/inter/mv_scaling.rs",
    "crates/splot-decode/src/prediction/inter/read_mv.rs",
    "crates/splot-decode/src/prediction/inter/read_mv/tests.rs",
    "crates/splot-decode/src/prediction/inter/single_ref.rs",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs",
    "crates/splot-decode/src/prediction/inter/tests.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/diagnostics.rs",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_ref_mv_stack.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records/ccso.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/prediction/chroma/directional.rs",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs",
    "crates/splot-decode/src/filters/wienerns_lr/recon.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs",
    "crates/splot-decode/src/bitstream/tile_payload/mi_size_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed/spec_table_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal/partition_children.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows/mv.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/lifecycle.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/partition_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/util.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-recon/src/intra_directional_angle.rs",
    "tools/comments/budget.toml",
    "tools/dupehound/budget.toml",
    "change_id:decoder-runtime-deslop/proposal.md",
    "change_id:decoder-runtime-deslop/tasks.md",
    "change_id:decoder-runtime-deslop/specs/decoder-runtime-deslop/spec.md",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-RUNTIME-STRUCTURE"
name = "Decoder runtime domain structure"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decoder-runtime-structure"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Structural decoder cleanup that removes production decode organization under the retired old runtime module names and groups existing behavior-preserving code by decoder domain: pipeline orchestration, bitstream planning/tile payload boundaries, prediction, residual orchestration, reference state, filters/restoration, output serialization, support gates, and tile state. It does not add AV2 decode coverage, change public API, change dependency direction, or claim new AVM/dav2d agreement."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/mod.rs",
    "crates/splot-decode/src/output/mod.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/prediction/mod.rs",
    "crates/splot-decode/src/residual/mod.rs",
    "crates/splot-decode/src/reference/mod.rs",
    "crates/splot-decode/src/filters/mod.rs",
    "crates/splot-decode/src/support/mod.rs",
    "crates/splot-decode/src/tile/mod.rs",
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
]
commands = [
    "cargo fmt --all -- --check",
    "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test --workspace --all-features",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc --workspace --all-features --no-deps",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-source-lines",
    "cargo xtask check-comment-density",
    "cargo xtask check-duplication",
    "cargo xtask ci",
]
fixtures = [
    "crates/splot-decode/src/bitstream/mod.rs",
    "crates/splot-decode/src/output/mod.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/prediction/mod.rs",
    "crates/splot-decode/src/residual/mod.rs",
    "crates/splot-decode/src/reference/mod.rs",
    "crates/splot-decode/src/filters/mod.rs",
    "crates/splot-decode/src/support/mod.rs",
    "crates/splot-decode/src/tile/mod.rs",
    "docs/ARCHITECTURE.md",
    "docs/DECISIONS/decoder-runtime-structure.md",
    "change_id:decoder-runtime-structure/proposal.md",
    "change_id:decoder-runtime-structure/tasks.md",
    "change_id:decoder-runtime-structure/specs/decoder-runtime-structure/spec.md",
]
diagnostics = []

[[feature]]
id = "DOC-FEATURE-TRACKING"
name = "Feature tracking documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/IMPLEMENTATION-MATRIX.toml"
change_id = "feature-tracking-framework"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Documents the five-layer model, ID convention, status model, proof rules, diagnostic-ID convention, and agent workflow."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-VALIDATOR-ROADMAP"
name = "Validator coverage roadmap documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = ["https://av2.aomedia.org/v1.0.0/index.html"]
crate = "docs"
module = "docs/CONFORMANCE.md"
change_id = "validator-coverage-roadmap"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Documents validator gap analysis, roadmap phases, matrix expansion guidance, and diagnostic namespace planning."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-VALIDATOR-EXAMPLES"
name = "Validator CLI worked examples (README)"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "README.md"
change_id = "docs-validator-examples"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Documents the validator/inspector CLI surface in the README with worked, non-invented examples — `splot explain` (describe text, unknown-id did-you-mean hint, --json/--list) and the `splot validate` output controls (--max-diagnostics truncation notice, --summary-only), plus a Status-table row for the shipping `explain` command. Every shown output is captured verbatim from the built binary (the explain registry or the committed bad-global-xlayer.av2 fixture), so the README cannot drift into describing behavior that does not exist. Docs-only: no code, flags, or diagnostics change."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-ENCODER-REFERENCE-GATE"
name = "Encoder reference gate documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/references/THIRD-PARTY-NOTICES.md"
change_id = "encoder-reference-gate"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Documents the rav1e/SVT-AV1 inspiration-only policy, third-party notices, required encoder research gate, and reviewer/agent workflow wiring."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-ENCODER-PROGRAM-CONTRACT"
name = "Encoder program contract documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/README.md"
change_id = "encoder-program-contract"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Defines Baseline Encoder Profile v1, the encoder roadmap, gap audit, validator-roadmap carve-out, and parked toy-encoder supersession without changing Rust behavior or dependency direction."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-AUDIT-PROTOCOLS"
name = "Agent audit protocol"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "AGENTS.md; xtask/src/audit_scope.rs"
change_id = "add-splot-audit-protocols"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Defines repository audit guidance through AGENTS.md and deterministic audit-scope output. Generated assistant skill copies are not committed."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-AV2-SPEC-MIRROR"
name = "AV2 specification mirror"
category = "docs"
kind = "docs"
spec_sections = []
sources = ["https://av2.aomedia.org/v1.0.0/20260528_38f28e7_AV2_Spec_v1.0.0.pdf"]
crate = "xtask"
module = "xtask/src/main.rs"
change_id = "add-av2-spec-mirror"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Commits a byte-faithful, versioned AV2 v1.0.0 spec mirror under docs/spec/av2/1.0.0/ (pdftotext -layout, headings over verbatim fenced bodies) as the single source of truth. Adds scripts/spec/regenerate-av2-spec.sh and a deterministic `cargo xtask check-spec-mirror` integrity gate (CHECKSUMS + pinned PDF sha256) wired into CI. AOM-copyright material, quarantined per THIRD-PARTY-NOTICES.md section 11."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/main.rs::tests::spec_mirror_gate_detects_drift"]
commands = [
    "cargo xtask check-spec-mirror",
    "cargo test -p xtask spec_mirror",
]
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-DECODER-ROADMAP"
name = "Decoder roadmap documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "decoder-roadmap-matrix-boundary"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Documents the encoder-grade decoder/reconstruction scope, staged first tier, unsupported-feature contract, deterministic hash policy, local-only AVM/dav2d boundary, and the explicit dependency-graph approval needed before decoder crates are added."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DOC-DECODER-DIAGNOSTICS"
name = "Decoder diagnostics registry documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DIAGNOSTICS.md"
change_id = "decoder-diagnostic-registry"
tracking_issue = ""
owner = "docs"
risk = "low"
notes = "Adds docs/DIAGNOSTICS.md as the canonical registry for emitted `splot decode` diagnostic rule IDs. The registry documents stable field names (`rule_id`, `severity`, `spec_section`, `matrix_row`, `feature_id`, `message`, `remediation`), current decoder emission roots, and the `decode/*` namespace rule. It now registers emitted `decode/unsupported-feature`, `decode/malformed-source`, `decode/resource-limit`, and `decode/output-error` diagnostics from the CLI byte-planner/runtime output handoff."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/diagnostic_registry.rs::tests::decoder_registry_accepts_matching_source_and_doc"]
commands = ["cargo xtask check-diagnostic-registry", "cargo test -p splot-cli --test decode_cli --locked", "cargo test -p splot-decode --locked"]
fixtures = ["docs/DIAGNOSTICS.md"]
diagnostics = ["decode/unsupported-feature", "decode/malformed-source", "decode/resource-limit", "decode/output-error"]

[[feature]]
id = "DOC-DECODE-LIMITS-CONTRACT"
name = "Decode limits contract documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "decoder-limits-contract"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Documents the `DecodeOptions { limits: DecodeLimits }` contract for byte-consuming decoder planning before any bitstream-derived allocation. The contract cites AV2-derived dimension, tile, output, and reference-state surfaces in decoder support docs, but does not claim full runtime allocation coverage. `splot decode` now emits `decode/resource-limit` when CLI pre-read or byte-planner limits fail, including bounded `max_input_bytes` rejection before constructing a full input buffer. The finite default OBU and frame-count thresholds are sized for current local decoder mission traversal while remaining local resource policy; future decode-stage dimension, tile, output, and reference allocation limits still remain unimplemented."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/decode_cli.rs::decode_oversized_input_reports_resource_limit_without_touching_output"]
commands = ["cargo test -p splot-cli --test decode_cli --locked", "cargo xtask check-decoder-support"]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml", "docs/DIAGNOSTICS.md"]
diagnostics = []

[[feature]]
id = "DOC-DETERMINISTIC-FRAME-HASH-CONTRACT"
name = "Deterministic decoded-frame hash contract documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "deterministic-frame-hash-contract"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Documents the future repo-owned `splot.decoded_frame_hash` v1 contract before runtime hash computation exists. The contract uses SHA-256 `splot-dfh-sha256-v1` over AV2 §6.16.13 sample-byte serialization in §7.21 output order, excluding stride/padding and metadata, with raw pre-film-grain output as the default variant. AV2 metadata MD5 remains a separate future interop verification path; this docs-only row does not claim normative runtime implementation coverage."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = ["cargo xtask check-decoder-support"]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml"]
diagnostics = []

[[feature]]
id = "RECON-HASH-INPUT-SERIALIZATION"
name = "Decoded frame hash input serialization"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.16.13", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/hash_input.rs"
change_id = "recon-hash-input-serialization"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds dependency-free canonical decoded-frame hash input serialization in `splot-recon`: `DecodedFrameHashInput<'_, T>` exposes `BYTE_STREAM_ID == av2-output-samples-v1`, `VARIANT_ID == raw_intermediate_output`, checked `byte_len`, and `write_to` over caller-supplied `DecodedFrame<T>` visible rows. The byte stream follows AV2 §6.16.13 sample-byte conversion for modeled visible output rows: Y/U/V plane order, monochrome Y only, raster scan order, one byte for 8-bit output, and little-endian two-byte samples for >8-bit output. Digest computation is tracked separately by `RECON-FRAME-HASH-DIGEST`. `docs/LOCAL-REFERENCE-EVIDENCE.toml` records two checked AVM/dav2d raw MD5 agreement entries as local reference metadata for the future runtime decoder path, not as proof of `splot-dfh-sha256-v1`. This row does not implement AV2 metadata MD5 verification, output ordering, film-grain synthesis, byte-consuming decode, Y4M output, external decoder invocation, new dependency, or dependency graph change."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-recon/src/hash_input.rs::tests"]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/hash_input.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-FRAME-HASH-DIGEST"
name = "Decoded frame SHA-256 digest computation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.16.13", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/hash_input.rs"
change_id = "recon-frame-hash-digest"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds source-backed `splot-dfh-sha256-v1` SHA-256 digest computation in `splot-recon` over caller-supplied `DecodedFrame<T>` values by hashing the existing `DecodedFrameHashInput` `av2-output-samples-v1` / `raw_intermediate_output` byte stream. The digest excludes stride, padding, output index, dimensions, pixel format metadata, OBU/container bytes, and AV2 decoded-frame-hash metadata. This uses the existing workspace `sha2` dependency as a direct `splot-recon` dependency. It does not implement AV2 metadata MD5 verification, output ordering, film-grain synthesis, byte-consuming decode, Y4M output, external decoder invocation, or reference-tool evidence."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-recon/src/hash_input.rs::tests"]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/hash_input.rs",
    "crates/splot-recon/src/lib.rs",
    "crates/splot-recon/Cargo.toml",
]
diagnostics = []

[[feature]]
id = "RECON-Y4M-OUTPUT-WRITER"
name = "Y4M output writer"
category = "infrastructure"
kind = "writer"
spec_sections = ["6.4.1", "6.16.13", "6.17.4.4", "7.21.1", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/y4m.rs"
change_id = "recon-y4m-output-writer"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a dependency-free source-backed Y4M stream/frame writer in `splot-recon` for caller-supplied `DecodedFrame<T>` values. It writes visible output samples only, uses repository-owned Y4M chroma tags, validates frame rates and stream/frame mismatches before payload output, and does not implement byte-consuming decode, CLI output, reconstruction algorithms, output scheduling, film-grain synthesis, AVM/dav2d invocation, or new dependencies."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-recon/src/y4m.rs::tests"]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/y4m.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-DC-SQUARE-PREDICTION"
name = "Square DC intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.2.10", "7.13.3.22"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-22",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra.rs"
change_id = "recon-intra-dc-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a scheduler-free `splot-recon` square-block subset of AV2 §7.13.2.10 DC intra prediction: typed 4x4 through 64x64 square block sizes, caller-provided optional left/above edge samples, active bit-depth/sample-type validation, typed edge/sample/output/allocation errors, a no-allocation scalar value API, a caller-owned strided output API, and owned square row-major prediction block convenience output. The square public APIs are preserved as compatibility wrappers over the rectangular DC implementation tracked by RECON-INTRA-DC-RECTANGULAR-PREDICTION. This row does not implement non-DC intra prediction modes, `predict_intra()` dispatch, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use outside the future `DecodeContext` orchestration policy."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-recon/src/intra.rs::tests"]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/intra.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-DC-RECTANGULAR-PREDICTION"
name = "Rectangular DC intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.13.2.10", "7.13.3.22"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-22",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra.rs"
change_id = "recon-rectangular-dc-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` rectangular AV2 §7.13.2.10 DC intra prediction: typed 4x4 through 64x64 per-dimension `IntraRectBlockSize`, caller-provided optional left/above edge samples, active bit-depth/sample-type validation, typed edge/sample/output errors, a no-allocation scalar value API, and a caller-owned strided output API. The both-edge path uses the §7.13.3.22 `resolve_divisor` and `Div_Lut` approximate division path for non-power-of-two `w + h` denominators, with §4.8 `Round2` and §3 divisor constants. Square DC APIs remain compatible wrappers. This row does not implement a new owned rectangular block type, non-DC intra prediction modes, subsampled DC, IBP DC, `predict_intra()` dispatch, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra.rs::tests::rect_block_size_accepts_av2_transform_dimension_range",
    "crates/splot-recon/src/intra.rs::tests::rect_dc_prediction_with_both_edges_uses_av2_approximate_divide",
    "crates/splot-recon/src/intra.rs::tests::rect_dc_prediction_into_validates_output_stride_and_length",
    "crates/splot-recon/src/workspace_tests.rs::workspace_extracts_edges_and_predicts_rectangular_dc",
]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/intra.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-BASIC-PAETH-PREDICTION"
name = "Basic PAETH intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.2.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_basic.rs"
change_id = "recon-basic-paeth-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.2 basic/PAETH intra prediction over prepared `LeftCol`, `AboveRow`, and `AboveRow[-1]` samples: callers provide an active bit depth, typed rectangular block size, mandatory left and above edge slices, a top-left sample, and caller-owned strided output. The primitive validates sample type, edge lengths by height and width, bit-depth ranges, output stride and length, and uses signed intermediates for the PAETH base while returning one of the prepared edge samples without clipping. Workspace helpers can write in-storage PAETH prediction only when top-left, left, and above neighbors exist. This row does not implement §7.13.2.1 availability/fallback sample preparation, full `predict_intra()` dispatch, directional prediction, data-driven prediction, subsampled DC, IBP, CfL, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_selects_left_candidate",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_selects_above_candidate",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_selects_top_left_candidate",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_tie_order_is_left_then_above",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_validates_edge_lengths_by_dimension",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_validates_edge_samples_against_bit_depth",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_validates_sample_type_against_bit_depth",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_validates_output_stride_and_length",
    "crates/splot-recon/src/intra_basic.rs::tests::paeth_prediction_rejects_overflowing_output_shape",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_rectangular_paeth_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_paeth_rejects_missing_prepared_edges",
]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/intra_basic.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-DC-SUBSAMPLED-PREDICTION"
name = "Subsampled DC intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.13.2.1", "7.13.2.11", "7.13.3.22"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-22",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_dc_subsampled.rs"
change_id = "recon-intra-dc-subsampled-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.11 DC intra prediction subsampled process over prepared `LeftCol[0..h]` and `AboveRow[0..w]` samples. Callers provide an active bit depth, typed 4x4 through 64x64 per-dimension `IntraRectBlockSize`, optional prepared DC edges, and caller-owned strided output. The primitive validates sample type, full edge lengths and sample ranges even for skipped samples, output stride and length, then uses the §7.13.2.11 sampling rule: dimensions greater than 32 contribute every second sample, otherwise every sample. No-edge prediction emits `1 << (BitDepth - 1)`; nonzero sampled counts use §7.13.3.22 `resolve_divisor` / `approx_divide`, §4.8 `Round2`, and `Clip1`. Workspace helpers can write in-storage subsampled DC prediction using adjacent left/above neighbors, including the no-edge midpoint, without synthesizing partial missing-edge fallback samples. This row does not implement full §7.13.2.1 edge preparation, `largeChroma` / `UV_CFL_PRED` dispatch, CfL luma subsampling, IBP, full `predict_intra()` dispatch, data-driven prediction, general directional prediction, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_no_edges_use_midpoint",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_large_edges_use_every_second_sample",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_validates_skipped_large_edge_samples",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_uses_approximate_divide_for_nonzero_count",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_accepts_10_bit_edge_range",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_rejects_unsupported_sample_type",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_rejects_edge_length_mismatch",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_rejects_edge_samples_out_of_range",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_into_fills_rectangle_with_stride",
    "crates/splot-recon/src/intra_dc_subsampled.rs::tests::intra_dc_subsampled_into_validates_output_stride_and_length",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_subsampled_dc_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_subsampled_dc_top_left_uses_midpoint_without_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_subsampled_dc_uses_available_edge_without_synthesizing_missing_edge",
    "crates/splot-recon/src/workspace_tests.rs::workspace_subsampled_dc_rejects_missing_plane_and_out_of_bounds_target",
]
commands = [
    "cargo test -p splot-recon intra_dc_subsampled --locked",
    "cargo test -p splot-recon workspace --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo xtask check-source-lines",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/intra_dc_subsampled.rs",
    "crates/splot-recon/src/intra_dc_math.rs",
    "crates/splot-recon/src/workspace_intra_dc.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-SMOOTH-PREDICTION"
name = "Smooth intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_smooth.rs"
change_id = "recon-smooth-intra-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.13 smooth intra prediction over prepared `LeftCol[0..h]` and `AboveRow[0..w]` samples, including `LeftCol[h]` bottom-left and `AboveRow[w]` top-right sentinels. Callers provide an active bit depth, typed rectangular block size, explicit `SMOOTH_PRED`/`SMOOTH_V_PRED`/`SMOOTH_H_PRED` mode selection, prepared edge slices, and caller-owned strided output. The primitive validates sample type, edge lengths by height/width plus sentinel, bit-depth ranges, output stride and length, computed output range, and implements the spec formula with AV2 §3 `BLEND_WEIGHT_MAX = 32` and AV2 §4.8 plain `Round2` using signed intermediates. Workspace helpers can write in-storage smooth prediction only when left, above, bottom-left, and top-right prepared samples exist. This row does not implement §7.13.2.1 availability/fallback sample preparation, full `predict_intra()` dispatch, directional prediction, data-driven prediction, subsampled DC, IBP, CfL, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_horizontal_prediction_matches_spec_formula",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_vertical_prediction_matches_spec_formula",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_averages_horizontal_and_vertical_paths",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_fills_rectangle_with_stride",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_validates_edge_lengths",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_validates_edge_samples_against_bit_depth",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_validates_computed_sample_range",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_validates_sample_type_against_bit_depth",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_validates_output_stride_and_length",
    "crates/splot-recon/src/intra_smooth.rs::tests::smooth_prediction_rejects_overflowing_output_shape",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_rectangular_smooth_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_smooth_rejects_missing_prepared_edges",
]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/intra_smooth.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-CARDINAL-DIRECTIONAL-PREDICTION"
name = "Cardinal directional intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.2.1", "7.13.2.7", "7.13.2.8", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_directional.rs"
change_id = "recon-intra-cardinal-directional-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.8 cardinal directional intra prediction for the two non-interpolating `Mode_To_Angle` cases from §9.2: `V_PRED` (pAngle 90) copies prepared `AboveRow[0..w]` into every output row, and `H_PRED` (pAngle 180) copies prepared `LeftCol[0..h]` into every output column. Callers provide an active bit depth, typed 4x4 through 64x64 per-dimension `IntraRectBlockSize`, the required prepared edge slice, explicit `Vertical` or `Horizontal` mode selection, and caller-owned strided output. The primitive validates sample type, required edge presence, edge length by width or height, bit-depth range, output stride and length, and target geometry. Workspace helpers can write in-storage H/V prediction only when the required above or left neighbor edge exists. This row does not implement §7.13.2.1 fallback edge preparation, pAngle values other than 90 and 180, IDIF, MRL, angle deltas, wide-angle mapping, IBP, full `predict_intra()` dispatch, data-driven prediction, CfL/CCTX/MHCCP, palette, tile syntax traversal, dequantization, inverse transforms, residual addition, broad runtime decoded-frame hashes, broad runtime raw/Y4M output, reference refresh, AVM/dav2d invocation, repo dependency, wrapper, script, CI or runtime external-decoder path, or scheduler state in `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_directional.rs::tests::vertical_cardinal_prediction_copies_above_edge",
    "crates/splot-recon/src/intra_directional.rs::tests::horizontal_cardinal_prediction_copies_left_edge",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_fills_rectangle_with_stride",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_validates_required_edge_presence",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_validates_edge_lengths_by_direction",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_validates_edge_samples_against_bit_depth",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_validates_sample_type_against_bit_depth",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_validates_output_stride_and_length",
    "crates/splot-recon/src/intra_directional.rs::tests::cardinal_prediction_rejects_overflowing_output_shape",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_cardinal_directional_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_cardinal_directional_rejects_missing_prepared_edges",
]
commands = [
    "cargo test -p splot-recon intra_directional --locked",
    "cargo test -p splot-recon workspace --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/intra_directional.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-ONE-SIDED-DIRECTIONAL-ANGLE-PREDICTION"
name = "One-sided directional-angle intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.13.2.1", "7.13.2.7", "7.13.2.8", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_directional_angle.rs"
change_id = "recon-intra-directional-angle-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.8 one-sided directional-angle intra prediction for the non-IDIF chroma pAngles `45`, `67`, and `203` from §9.2, plus the §7.13.2.8 step-1 (`pAngle < 90`) ZONE-1 luma IDIF 4-tap for the ABOVE-reading angle (pAngle 45). Callers provide an active bit depth, typed 4x4 through 64x64 per-dimension `IntraRectBlockSize`, explicit supported pAngle, the required prepared edge, and caller-owned strided output. The BILINEAR path (`predict_intra_directional_angle_rect_into`) takes the `AboveRow[0..w+h)` / `LeftCol[0..w+h)` edge (`slice[0]` = logical 0, length `w + h`) and uses the §9.2 `Dr_Intra_Derivative[45] = 64` / `Dr_Intra_Derivative[67] = 24` entries with §4.8 `Round2` for non-IDIF bilinear interpolation, including the edge-end fallback when the computed base reaches `w + h - 1`. The ZONE-1 LUMA IDIF path (`predict_intra_directional_angle_rect_one_sided_idif_into`, `IntraDirectionalAngleIdifEdges::above`) takes the wider `AboveRow[-2 ..= w + h + 1]` slice (`slice[0]` = logical `-2`, length `w + h + 4`) because the §7.13.2.8 4-tap reads `AboveRow[base - 1 ..= base + 2]` with `base` projecting up-and-right into the above-right (`base = (idx >> 6) + j`, up to `maxBaseX = w + h - 1`); it applies the existing §7.13.2.8 / §9.2 `Dr_Interp_Filter[32][4]` table as `s = Σ(t=0..3) Dr_Interp_Filter[shift][t] * AboveRow[base + t - 1]; pred = Clip1(Round2(s, 7))` for `base <= maxBaseX`, else `pred = AboveRow[maxBaseX]`. At `shift == 0` the filter row is `{0, 128, 0, 0}`, so the IDIF reduces to `AboveRow[base]` — bit-identical to the bilinear branch (every pAngle 45 projection has `shift == 0`, so D45 luma stays byte-identical to the bilinear copy, but reads far into the above-right). Both paths validate sample type, pAngle support, required edge presence, exact edge length, edge sample range, output stride and length, and checked arithmetic before writing. This row does not implement §7.13.2.1 edge availability/fallback preparation, pAngles 90/180 (tracked by `RECON-INTRA-CARDINAL-DIRECTIONAL-PREDICTION`), middle angles 113/135/157 (`RECON-INTRA-MIDDLE-DIRECTIONAL-ANGLE-PREDICTION`), the zone-3 (pAngle 203, left + below-left) luma IDIF, MRL, angle deltas, wide-angle mapping, directional IBP, workspace edge synthesis, full `predict_intra()` dispatch, data-driven prediction, CfL/CCTX/MHCCP, palette, tile syntax traversal, dequantization, inverse transforms, residual addition, broad runtime decoded-frame hashes/Y4M, reference refresh, AVM/dav2d invocation, repo dependency, wrapper, script, CI or runtime external-decoder path, or scheduler state in `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_prediction_uses_above_edge_and_edge_end_fallback",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_one_sided_idif_matches_bilinear_copy_for_shift_zero",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_one_sided_idif_clamps_base_at_max_base_x",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_one_sided_idif_rejects_wrong_length_edge",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d67_prediction_matches_non_idif_bilinear_formula",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d203_prediction_matches_non_idif_bilinear_formula",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_accepts_10_bit_u16_samples",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_rejects_unsupported_pangles_without_mutation",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_rejects_all_excluded_pangles",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_validates_required_edge_presence",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_validates_edge_lengths",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_validates_edge_sample_ranges",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_validates_sample_type_against_bit_depth",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::directional_angle_prediction_validates_output_shape",
]
commands = [
    "cargo test -p splot-recon intra_directional_angle --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/intra_directional_angle.rs",
    "crates/splot-recon/src/intra_directional_angle_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-MIDDLE-DIRECTIONAL-ANGLE-PREDICTION"
name = "Middle directional-angle intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.13.2.1", "7.13.2.7", "7.13.2.8", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_directional_angle.rs"
change_id = "recon-intra-middle-directional-angle-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.8 middle directional-angle intra prediction for the pAngles `113`, `135`, and `157` from §9.2 over caller-prepared logical edge ranges, in BOTH the `enableIdif == 0` chroma bilinear branch AND the `enableIdif == 1` luma IDIF 4-tap branch. The bilinear path (`predict_intra_middle_directional_angle_rect_into`) takes `AboveRow[-1..w)` and `LeftCol[-1..h)` slices (`slice[0]` = logical `-1`, lengths `w + 1` / `h + 1`). The luma IDIF path (`predict_intra_middle_directional_angle_rect_idif_into`, `IntraMiddleDirectionalAngleIdifEdges`) takes the wider `AboveRow[-2..=w+1]` / `LeftCol[-2..=h+1]` slices (`slice[0]` = logical `-2`, lengths `w + 4` / `h + 4`) because the §7.13.2.8 4-tap reads `Edge[base - 1 ..= base + 2]`; it applies the §7.13.2.8 / §9.2 `Dr_Interp_Filter[32][4]` table (copied verbatim from `docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8`) as `s = Σ(t=0..3) Dr_Interp_Filter[shift][t] * Edge[base + t - 1]; pred = Clip1(Round2(s, 7))`, with the signed 4-tap sum floored by Round2 and Clip1 clamping a negative result to 0. At `shift == 0` the filter row is `{0, 128, 0, 0}`, so the IDIF reduces to `Edge[base]` — bit-identical to the bilinear branch (e.g. every pAngle 135 projection has `shift == 0`, so D135 luma stays byte-identical to the prior bilinear behaviour). Both paths validate sample type, pAngle support, edge presence, exact edge lengths, edge sample ranges, output stride/length, and checked signed arithmetic before writing. Derivatives: pAngle 113 `dx = Dr[67]`, `dy = Dr[23]`; pAngle 135 `dx = dy = Dr[45]`; pAngle 157 `dx = Dr[23]`, `dy = Dr[67]`. This row does not implement §7.13.2.1 edge availability/fallback preparation, pAngles 45/67/203 (tracked by `RECON-INTRA-ONE-SIDED-DIRECTIONAL-ANGLE-PREDICTION`), pAngles 90/180 (tracked by `RECON-INTRA-CARDINAL-DIRECTIONAL-PREDICTION`), one-sided / wide-angle IDIF, MRL, angle deltas, wide-angle mapping, directional IBP, full `predict_intra()` dispatch, data-driven prediction, CfL/CCTX/MHCCP, palette, tile syntax traversal, dequantization, inverse transforms, residual addition, broad runtime decoded-frame hashes/Y4M, reference refresh, AVM/dav2d invocation, repo dependency, wrapper, script, CI or runtime external-decoder path, or scheduler state in `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d135_middle_prediction_uses_above_and_left_negative_logical_edges",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d113_middle_prediction_matches_non_idif_bilinear_formula",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d157_middle_prediction_matches_non_idif_bilinear_formula",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_accepts_10_bit_u16_samples",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_accepts_asymmetric_block_bounds",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_rejects_unsupported_pangles_without_mutation",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_validates_required_edge_presence",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_validates_edge_lengths",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_validates_edge_sample_ranges",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_validates_sample_type_against_bit_depth",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::middle_directional_angle_prediction_validates_output_shape",
]
commands = [
    "cargo test -p splot-recon intra_directional_angle --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/intra_directional_angle.rs",
    "crates/splot-recon/src/intra_directional_angle_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-WORKSPACE-DIRECTIONAL-ANGLE-PREDICTION"
name = "Workspace directional-angle intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.13.2.1", "7.13.2.7", "7.13.2.8", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/workspace_intra_directional_angle.rs"
change_id = "recon-workspace-directional-angle-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` current-frame workspace handoff for the existing AV2 §7.13.2.8 directional-angle primitives. For one-sided pAngles `45`, `67`, and `203`, chroma gathers the fully available in-storage `AboveRow[0..w+h)` or `LeftCol[0..w+h)` edge and uses bilinear prediction. Luma gathers the same real edge plus the top-left corner, applies the §7.13.2.8 frame-edge corner fallback, extends the edge to the logical IDIF range `-2..=w+h+1`, and dispatches to the 4-tap one-sided IDIF primitive. Middle pAngles `113`, `135`, and `157` gather logical `AboveRow[-1..w)` and `LeftCol[-1..h)` edges and likewise select bilinear chroma or IDIF luma. The helpers validate target plane/rectangle and in-storage edge availability before output mutation; the underlying primitives validate sample type, edge length, sample range, output shape, and checked arithmetic. This row does not infer §5.20.2.3 decoded-neighbour availability, apply §7.13.2.18 edge filters, implement MRL or angle deltas, pAngles outside the typed subsets, wide-angle mapping, directional IBP, full `predict_intra()` dispatch, tile syntax traversal, residual addition, loop filtering, or scheduler state in `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_predicts_one_sided_directional_angle_from_in_storage_above_edge",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_predicts_one_sided_directional_angle_from_in_storage_left_edge",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_predicts_middle_directional_angle_from_in_storage_edges",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_directional_angle_accepts_10_bit_u16_samples",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_predicts_luma_one_sided_idif_from_in_storage_above_edge",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_predicts_luma_one_sided_idif_from_in_storage_left_edge",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_luma_one_sided_idif_uses_own_edge_sample_when_corner_is_out_of_frame",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_directional_angle_rejects_missing_prepared_edges",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_directional_angle_rejects_missing_plane_and_out_of_bounds_target",
    "crates/splot-recon/src/workspace_intra_directional_angle.rs::tests::workspace_directional_angle_invalid_edge_sample_does_not_mutate_target",
]
commands = [
    "cargo test -p splot-recon workspace_intra_directional_angle --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/workspace_intra_directional_angle.rs",
    "crates/splot-recon/src/workspace_intra_directional_angle_tests.rs",
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRA-IBP-DC-PREDICTION"
name = "IBP DC intra prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.13.2.1", "7.13.2.10", "7.13.2.12"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-12",
]
crate = "splot-recon"
module = "crates/splot-recon/src/intra_ibp_dc.rs"
change_id = "recon-intra-ibp-dc-prediction"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds scheduler-free `splot-recon` AV2 §7.13.2.12 IBP DC process support as an in-place modifier over an existing §7.13.2.10 DC prediction buffer. Callers provide an active bit depth, typed 4x4 through 64x64 per-dimension `IntraRectBlockSize`, optional prepared `LeftCol[0..h]` and `AboveRow[0..w]` DC edges, and caller-owned strided prediction storage. The primitive validates sample type, edge lengths and sample ranges, output stride and length, and all existing prediction samples that will be blended before mutation, then applies the §7.13.2.12 `Ibp_Weights` table with §3 `IBP_WEIGHT_MAX` / `IBP_WEIGHT_SHIFT` and §4.8 `Round2`. Both-edge overlap follows the spec wide/tall skip rules, and both edges absent is a validated no-op. Workspace helpers can write in-storage rectangular DC prediction and then apply IBP DC from adjacent left/above neighbors without synthesizing fallback samples. This row does not implement full §7.13.2.1 dispatch gates, full edge preparation, §7.13.2.9 directional IBP weights, general directional prediction, data-driven prediction, CfL/CCTX/MHCCP, palette, tile syntax traversal, dequantization, inverse transforms, residual addition, runtime decoded-frame hashes, runtime Y4M output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_above_only_modifies_top_rows",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_left_only_modifies_left_columns",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_both_edges_square_skips_left_top_overlap",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_both_edges_wide_uses_exact_weights_and_skip_boundary",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_both_edges_tall_uses_exact_weights_and_skip_boundary",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_max_size_10_bit_uses_last_weight_row",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_no_edges_is_validated_no_op",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_accepts_10_bit_samples",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_rejects_typed_invalid_inputs_without_mutation",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_rejects_invalid_prediction_output_without_mutation",
    "crates/splot-recon/src/intra_ibp_dc.rs::tests::ibp_dc_rejects_invalid_output_shape",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_ibp_dc_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_top_left_uses_dc_midpoint_without_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_rejects_missing_plane_and_out_of_bounds_target",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_invalid_edge_sample_does_not_mutate_target",
]
commands = [
    "cargo test -p splot-recon ibp --locked",
    "cargo test -p splot-recon workspace --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/intra_ibp_dc.rs",
    "crates/splot-recon/src/workspace_intra_dc.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "fuzz/fuzz_targets/recon_intra_prediction_bytes.rs",
]
diagnostics = []

[[feature]]
id = "RECON-CURRENT-FRAME-WORKSPACE"
name = "Current-frame reconstruction workspace"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "6.17.4.1", "6.17.4.4", "7.13.2.1", "7.13.2.2", "7.13.2.8", "7.13.2.10", "7.13.2.11", "7.13.2.12", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-recon"
module = "crates/splot-recon/src/workspace.rs"
change_id = "recon-current-frame-workspace"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a scheduler-free `splot-recon` mutable current-frame reconstruction workspace: `CurrentFrameWorkspace<T>` allocates Y/U/V plane buffers from `DecodedFrameInfo` using checked arithmetic and fallible allocation, validates sample type and active bit-depth range before writes, exposes bounded plane/rectangle reads, fills, row-strided writes, square/rectangular block writes, in-storage left/above edge extraction, and square/rectangular DC plus subsampled DC, IBP DC, rectangular PAETH, smooth, and cardinal H/V directional prediction write helpers that call the existing `RECON-INTRA-DC-SQUARE-PREDICTION`, `RECON-INTRA-DC-RECTANGULAR-PREDICTION`, `RECON-INTRA-DC-SUBSAMPLED-PREDICTION`, `RECON-INTRA-IBP-DC-PREDICTION`, `RECON-INTRA-BASIC-PAETH-PREDICTION`, `RECON-INTRA-SMOOTH-PREDICTION`, and `RECON-INTRA-CARDINAL-DIRECTIONAL-PREDICTION` primitives. `freeze()` reuses existing `Plane::from_vec`, `FramePlanes::new`, and `DecodedFrame::try_new`, and tests prove interop with decoded-frame hash input, Y4M writer, reference-frame store, no-edge midpoint behavior, IBP DC handoff, and missing-edge errors. This row itself did not add a `splot-decode -> splot-recon` dependency, `DecodeContext` API, runtime `splot decode` success path, tile syntax traversal, dequantization, inverse transforms, residual addition, loop filtering, output scheduling, runtime hashes, runtime Y4M, reference refresh, AVM/dav2d invocation, external tooling, full §7.13.2.1 fallback edge preparation, `enable_ibp`, `largeChroma`, `UV_CFL_PRED` dispatch, or scheduler use inside `splot-recon`; future parallel orchestration remains owned by `DecodeContext` and `splot_parallel::WorkerPool`. The later runtime dependency edge is tracked by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`, `DECODE-MINIMAL-INTRA-RECONSTRUCTION-FRONTIER`, and `DECODE-Y4M-RUNTIME-OUTPUT`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_subsampled_dc_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_predicts_ibp_dc_from_in_storage_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_top_left_uses_dc_midpoint_without_edges",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_rejects_missing_plane_and_out_of_bounds_target",
    "crates/splot-recon/src/workspace_tests.rs::workspace_ibp_dc_invalid_edge_sample_does_not_mutate_target",
]
commands = [
    "cargo test -p splot-recon workspace --locked",
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_intra_dc.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INTRABC-CURRENT-FRAME-COPY"
name = "IntrABC current-frame workspace copy"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.3.18"]
sources = ["docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18"]
crate = "splot-recon"
module = "crates/splot-recon/src/workspace.rs"
change_id = "decode-intrabc-prediction-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a checked same-plane `CurrentFrameWorkspace::copy_rect_within_plane` primitive for source-backed IntrABC current-frame copies. The helper validates source and target plane rectangles and equal shapes before mutation, snapshots the source through bounded scratch storage so overlapping rectangles read original source samples, writes the target through the existing checked workspace path, and returns typed `ReconError` failures for missing planes, out-of-bounds geometry, shape mismatch, arithmetic overflow, or scratch allocation failure. AV2 §7.13.3.18 defines `refIdx == -1` as the current-frame source for intra block copy; this row implements only the storage copy primitive over already reconstructed samples. It does not decide IntrABC availability, BlockDecoded state, block-vector derivation, chroma subsampling, residual addition, loop filtering, output/reference refresh, or successful local decoder mission decode."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/workspace_tests.rs::workspace_copy_rect_within_plane_copies_luma_samples",
    "crates/splot-recon/src/workspace_tests.rs::workspace_copy_rect_within_plane_snapshots_overlapping_source",
    "crates/splot-recon/src/workspace_tests.rs::workspace_copy_rect_within_plane_rejects_invalid_inputs_without_mutation",
    "crates/splot-recon/src/workspace_tests.rs::workspace_copy_rect_within_plane_rejects_missing_plane",
]
commands = [
    "cargo test -p splot-recon workspace_copy_rect_within_plane --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/workspace.rs",
    "crates/splot-recon/src/workspace_tests.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEQUANT-QUANTIZER-LOOKUP"
name = "Dequantization quantizer-value lookup"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "6.4.1", "7.14.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/dequant.rs"
change_id = "recon-dequant-quantizer-lookup"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a scheduler-free `splot-recon` AV2 §7.14.2 dequantization quantizer-value lookup core: the 25-entry `Ac_Qlookup` base table, the `qlookup` shift-extension function, `max_quantizer_index` deriving §6.4.1 Table 6.3 `MaxQ` (8-bit -> MAXQ_8_BITS 255, 10-bit -> MAXQ_10_BITS 303; `bit_depth_idc` greater than 1 is reserved, so AV2 v1.0.0 defines no other decoded bit depth and `MAXQ_BITS` is only the segmentation-feature ceiling, not this clamp), and `quantizer_value` implementing §7.14.2 `get_q( qindex, delta )` (the `Ac_Qlookup[0]` result when qindex is 0 and delta is non-positive, otherwise `qlookup` of `qindex + delta` clamped to `1..=MaxQ`). The functions take caller-resolved inputs, use `i64` clamp intermediates so every input is total and panic-free, and read no frame, segment, or tile state. Completeness is proven by spec-exact unit vectors for `Ac_Qlookup`, the shift extension at the 8-bit and 10-bit `MaxQ` extremes, the qindex-0 special case, delta addition, both clamp directions, and input-extreme panic-freedom. This row does not implement §7.14.2 `get_qindex` segment and `delta_q` index resolution, the per-plane `get_dc_quant` and `get_ac_quant` composition, the §7.14.4 dequantization process, quantizer-matrix weighting, the §7.14.3 reconstruct process, inverse transforms, residual addition, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/dequant.rs::tests::max_quantizer_index_matches_table_6_3",
    "crates/splot-recon/src/dequant.rs::tests::qlookup_returns_table_for_small_q",
    "crates/splot-recon/src/dequant.rs::tests::qlookup_applies_shift_extension_for_large_q",
    "crates/splot-recon/src/dequant.rs::tests::qlookup_is_total_beyond_contract",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_value_uses_zero_index_special_case",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_value_adds_delta_then_looks_up",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_value_clamps_into_one_through_max_q",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_value_is_panic_free_at_input_extremes",
]
commands = [
    "cargo test -p splot-recon dequant --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/dequant.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEQUANT-QUANTIZER-INDEX-RESOLUTION"
name = "Dequantization quantizer-index resolution and per-plane composition"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "6.4.1", "7.14.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/dequant.rs"
change_id = "recon-dequant-quantizer-index-resolution"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Extends the §7.14.2 lookup core (RECON-DEQUANT-QUANTIZER-LOOKUP) with quantizer-index resolution and per-plane quantizer composition. `quantizer_index` implements `get_qindex( ignoreDeltaQ, segmentId )`: when the alternative-quantizer segment feature is active it returns Clip3(0, MaxQ, base + segment_alt_q_data) where base is CurrentQIndex when delta_q applies (not ignore_delta_q and delta_q_present) and base_q_idx otherwise; when inactive it returns CurrentQIndex when delta_q applies and base_q_idx otherwise (unclamped, matching the spec, since the caller maintains those in range). The `QuantizerDeltas` carrier holds the caller-resolved per-plane DC and AC delta sums (y_dc, u_dc, v_dc, u_ac, v_ac; luma AC is 0 per spec), and `dc_quantizer` and `ac_quantizer` implement `get_dc_quant( plane )` and `get_ac_quant( plane )` by selecting the plane delta and applying the lookup `quantizer_value`. Segmentation and delta_q facts (seg_feature_active_idx, the FeatureData array, the CurrentQIndex update) are resolved by the caller; recon reads no frame, segment, or tile state. All functions are total and panic-free via i64 clamp intermediates. Runtime decode now threads nonzero effective quantizer deltas through the general-intra residual path: `effective_quantizer_deltas` sums the parsed frame `DeltaQ*` values with the §5.4.8 sequence `Base*DeltaQ` values, `decode_intra_frame` installs the resolved `QuantizerDeltas` for the frame walk, and the residual reconstruction path feeds them into §7.14.4 dequant instead of forcing zeros. The committed `syn-seqdeltaq-intra-128x64.ivf` fixture decodes byte-exact to AVM raw SHA-256 3febac78b0b0e4c27e1a080073d7f4ecf51b5a6d689f495169354834d4659c11, closing the former `general_intra_nonzero_quantizer_delta_unimplemented` gate for the general-intra subset. Inter residual paths and inter-frame intra leaves still reject nonzero effective quantizer deltas until separately oracle-proven. This row does not implement live AVM/dav2d invocation in CI, reference refresh, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/dequant.rs::tests::quantizer_index_baseline_returns_base_q_idx",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_index_delta_q_returns_current_q_index",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_index_segment_feature_uses_base_then_clips",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_index_segment_feature_clips_both_bounds",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_index_is_panic_free_at_input_extremes",
    "crates/splot-recon/src/dequant.rs::tests::dc_quantizer_selects_plane_delta",
    "crates/splot-recon/src/dequant.rs::tests::ac_quantizer_uses_zero_for_luma_and_delta_for_chroma",
    "crates/splot-recon/src/dequant.rs::tests::quantizer_composition_reaches_zero_index_special_case",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::seqdeltaq_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-recon dequant --locked",
    "cargo test -p splot-decode seqdeltaq_intra_frame_decodes_to_oracle --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-seqdeltaq-intra-128x64.ivf",
    "tests/conformance/decoder-oracle.toml",
    "crates/splot-decode/src/prediction/inter.rs",
    "crates/splot-decode/src/pipeline/frame_engine/intra.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-recon/src/dequant.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INVERSE-TRANSFORM-1D"
name = "Kernel-based 1D inverse transform"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.15.2.1", "7.15.4.1", "9.6"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4-1",
]
crate = "splot-recon"
module = "crates/splot-recon/src/inverse_transform.rs"
change_id = "recon-inverse-transform-1d"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.15.2.1 kernel-based 1D inverse transform `inverse_transform_1d`, the first residual-path transform brick and the first consumer of the shared `splot-tables` crate (INFRA-SHARED-SPEC-TABLES, so this row adds the `splot-recon -> splot-tables` dependency edge). It matrix-multiplies the input coefficients by the size-and-type §9.6 kernel (`splot_tables::tables::transform_1d`), then applies §4.8 `Round2` and the §7.15.2.1 `colTx`-dependent `Clip3(-(1<<(BitDepth+(colTx?0:7))), ...-1, .)`. The `InverseTransform1dType` enum models the §7.15.4.1 Table 7.1 kernel types Dct(0)/Adst(2)/Fdst(3)/Ddtx(4)/Fddt(5); IDT(1) is the separate §7.15.2.3 identity transform and is not represented. The spec dispatch is reproduced exactly: length 4 routes Fdst/Ddtx/Fddt to the FDST kernel (the spec `else`), length 32 uses the DCT kernel for every type (AV2 defines no other length-32 1D kernel), and Fddt indexes the DDTX kernel column in reverse. Accumulation uses `i64` intermediates (no overflow for in-range dequantized inputs) and the `Clip3` bound keeps outputs in `i32`. Typed `ReconError::{InvalidInverseTransformSize, InverseTransformLengthMismatch}` reject lengths other than 4/8/16/32 and output/source length mismatches. Completeness is proven by a DC-only flat-field vector, a single-coefficient kernel-row vector, the §4.8 arithmetic-downshift, both `colTx` clamp ranges, the Fddt-is-reversed-Ddtx property, the length-4 FDST fallback, the length-32 DCT-for-every-type property, and the two typed-error cases. This row does not implement the §7.15.2.2 Walsh-Hadamard transform, the §7.15.2.3 identity transform, the §7.15.3 secondary transform, the §7.15.4 2D inverse transform orchestration (`Transform_Shift`, `get_transform_1d_type`, row/column passes, DPCM, sample duplication), dequantization, residual addition, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/inverse_transform.rs::tests::dc_only_dct4_is_flat",
    "crates/splot-recon/src/inverse_transform.rs::tests::dct4_single_coefficient_matches_kernel_row",
    "crates/splot-recon/src/inverse_transform.rs::tests::round2_applies_arithmetic_downshift",
    "crates/splot-recon/src/inverse_transform.rs::tests::col_tx_flag_selects_clamp_range",
    "crates/splot-recon/src/inverse_transform.rs::tests::fddt_is_the_column_reverse_of_ddtx",
    "crates/splot-recon/src/inverse_transform.rs::tests::length_4_maps_fdst_ddtx_and_fddt_to_the_fdst_kernel",
    "crates/splot-recon/src/inverse_transform.rs::tests::length_32_uses_dct_kernel_for_every_type",
    "crates/splot-recon/src/inverse_transform.rs::tests::rejects_unsupported_size",
    "crates/splot-recon/src/inverse_transform.rs::tests::rejects_output_length_mismatch",
    "crates/splot-recon/src/inverse_transform.rs::tests::round2_matches_spec_and_is_total_for_large_shift",
]
commands = [
    "cargo test -p splot-recon inverse_transform --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/inverse_transform.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "crates/splot-recon/Cargo.toml",
]
diagnostics = []

[[feature]]
id = "RECON-INVERSE-TRANSFORM-MATRIX-FREE"
name = "Matrix-free 1D inverse transforms (Walsh-Hadamard, identity)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.15.2.2", "7.15.2.3"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-2-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/inverse_transform.rs"
change_id = "recon-transform-matrix-free"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Extends the §7.15.2 1D inverse transform module (RECON-INVERSE-TRANSFORM-1D) with the two matrix-free transforms. `inverse_walsh_hadamard` implements §7.15.2.2 exactly: the 4-element lossless butterfly over `src` with a pre-scaling `shift` (a=src0>>shift, c=src1>>shift, d=src2>>shift, b=src3>>shift; a+=c; d-=b; e=(a-d)>>1; b=e-b; c=e-c; a-=b; d+=c), no kernel and no Clip3, total via `i64` intermediates. `inverse_identity_transform` implements §7.15.2.3: per-sample `Clip3(-(1<<(BitDepth+(colTx?0:7))), ...-1, Round2(src[i]*scale, shift))`, sharing the `transform_clip_bounds` helper with the §7.15.2.1 kernel transform; `scale` is the caller-supplied §7.15.4.1 `get_identity_scale` value. Both read no frame/segment/tile state and are panic-free; the identity transform returns typed `ReconError::InverseTransformLengthMismatch` on a source/output length mismatch. Completeness is proven by spec-exact WHT butterfly vectors (DC-only and last-coefficient inputs), the WHT pre-shift, the identity scale/round/clamp vector, both `colTx` identity clamp ranges, and the identity length-mismatch error. This row does not implement the §7.15.4.1 `get_identity_scale` derivation, the §7.15.3 secondary transform, the §7.15.4 2D inverse transform orchestration (Transform_Shift, get_transform_1d_type, row/column passes, DPCM, sample duplication), dequantization, residual addition, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/inverse_transform.rs::tests::walsh_hadamard_matches_spec_butterfly",
    "crates/splot-recon/src/inverse_transform.rs::tests::walsh_hadamard_applies_pre_shift",
    "crates/splot-recon/src/inverse_transform.rs::tests::walsh_hadamard_is_total_for_large_shift",
    "crates/splot-recon/src/inverse_transform.rs::tests::identity_scales_rounds_and_clamps",
    "crates/splot-recon/src/inverse_transform.rs::tests::identity_clamp_uses_col_tx_range",
    "crates/splot-recon/src/inverse_transform.rs::tests::identity_rejects_output_length_mismatch",
    "crates/splot-recon/src/inverse_transform.rs::tests::identity_is_total_for_extreme_inputs",
]
commands = [
    "cargo test -p splot-recon inverse_transform --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/inverse_transform.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-RESIDUAL-ADDITION"
name = "Reconstruct residual-addition step"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.14.3"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/reconstruct.rs"
change_id = "recon-residual-addition"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.14.3 reconstruct residual-addition step `reconstruct_add_residual<T: ReconSample>(prediction, residual, bit_depth, out)`: each output is the §4.8 `Clip1(prediction[i] + residual[i])` = clamp to `0..=max_sample` for the active bit depth, matching `CurrFrame[plane][y+i][x+j] = Clip1(CurrFrame + Residual[i][j])`. It is independent of how the residual is produced (the §7.15.4 2D inverse transform that fills `Residual` is a separate row), validates the sample type against the bit depth and equal prediction/residual/output lengths (typed `ReconError`), and sums with `i64` intermediates so it is total and panic-free even for `i32` residual extremes; the `Clip1` bound keeps every written sample inside the validated storage type. Completeness is proven by a plain add vector, below-zero and above-max `Clip1` clamps, the 10-bit u16 path clamping to 1023, the `i32::MAX`/`i32::MIN` residual extremes, the narrow-sample-type rejection, and the length-mismatch rejection. This row does not implement the §7.15.4 2D inverse transform, the §7.14.4 dequantization process, the §7.15.3 secondary transform, the §7.14.3 DPCM adjustment, the lossless-conformance requirement, intra/inter prediction sample production, workspace integration, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/reconstruct.rs::tests::adds_residual_to_prediction",
    "crates/splot-recon/src/reconstruct.rs::tests::clip1_clamps_below_zero_and_above_max",
    "crates/splot-recon/src/reconstruct.rs::tests::ten_bit_uses_u16_and_clamps_to_1023",
    "crates/splot-recon/src/reconstruct.rs::tests::is_total_for_extreme_residual",
    "crates/splot-recon/src/reconstruct.rs::tests::rejects_sample_type_too_narrow_for_bit_depth",
    "crates/splot-recon/src/reconstruct.rs::tests::rejects_length_mismatch",
]
commands = [
    "cargo test -p splot-recon reconstruct --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/reconstruct.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-RECONSTRUCT-TRANSFORM-BLOCK"
name = "Transform-block reconstruction residual chain"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.14.3", "7.14.4", "7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/reconstruct_block.rs"
change_id = "recon-reconstruct-transform-block"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 transform-block reconstruction residual chain `reconstruct_transform_block_residual<T: ReconSample>(prediction, quant, dequant_params, transform, dequant_scratch, residual_scratch, out)`, composing the three existing residual primitives into the sequence a decoder runs to turn decoded quantized coefficients into reconstructed samples: §7.14.4 `dequantize_block` then §7.15.4 `inverse_transform_2d_outer` then §7.14.3 `reconstruct_add_residual`, i.e. `out = Clip1(prediction + inverse_transform(dequant(quant)))`. `quant` is the adjusted `tx_width by tx_height` decoded coefficients and `prediction` and `out` are the original transform-size samples; the two scratch buffers are caller-owned so the composition allocates nothing, and `transform` is resolved with `InverseTransform2dOuter::resolve` so its shifts, per-pass types, and dimensions stay consistent. Total and panic-free for consistent inputs (each step is total and rejects any buffer-length or geometry inconsistency with a typed `ReconError` before mutating `out`); reuses the underlying primitives' error variants with no new error. Strictly additive and provably no-output-change: a pure `pub` composition over already-tested primitives with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by an all-zero-preserves-prediction anchor and a uniform signed nonzero-DC residual at TX_4X4, the same two at TX_64X64 (exercising the adjusted 32x32 to original 64x64 sample-duplication geometry), and a fail-atomic inconsistent-buffer rejection. This is the residual sink for the coefficient pipeline; this row does not implement the coefficient entropy decode that produces `Quant`, the §7.15.3 secondary transform, the §7.14.4 useQm or shift derivation, the §7.15.4 DPCM-direction selection, intra/inter prediction sample production, runtime decode wiring, output, reference refresh, or AVM/dav2d invocation."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/reconstruct_block.rs::tests::all_zero_quant_preserves_prediction_4x4",
    "crates/splot-recon/src/reconstruct_block.rs::tests::nonzero_dc_produces_uniform_signed_residual_4x4",
    "crates/splot-recon/src/reconstruct_block.rs::tests::all_zero_quant_preserves_prediction_tx_64x64",
    "crates/splot-recon/src/reconstruct_block.rs::tests::nonzero_dc_is_uniform_after_expansion_tx_64x64",
    "crates/splot-recon/src/reconstruct_block.rs::tests::rejects_inconsistent_buffers_before_writing_output",
]
commands = [
    "cargo test -p splot-recon reconstruct_block --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/reconstruct_block.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-SECONDARY-INVERSE-TRANSFORM"
name = "Secondary inverse transform process"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.15.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/secondary_transform.rs"
change_id = "recon-secondary-inverse-transform"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.15.3 secondary inverse transform `secondary_inverse_transform(dequant, params)`, the matrix-based transform run over the dequantized coefficients before the §7.15.4 2D inverse transform when `sec_tx_type != 0`. It gathers the first `n` coefficients of the `w by h` `Dequant` block in §5.20.7.30 2D scan order via the existing `coefficient_scan_order` (zeroing those positions), multiplies the gathered vector by the §9.7 IST kernel from `splot-tables` (`IST_4X4_KERNEL` or `IST_8X8_KERNEL` selected by `large = w >= 8 && h >= 8`), applies §4.8 `Round2Signed(t, 7)` and the `Clip3` bound `1 shifted left by (BitDepth + 7)`, and scatters the results into the top-left scan sub-block via the hand-written spec-cited `Stx_Scan_Order_4x4` and `Stx_Scan_Order_8x8` process-body constants (and, for the large case, the generated `STX_SCAN_MAP`), honoring `transpose`. The §7.15.3 `kernel` / `transpose` / `n` derivation (the `YMode` and `is_directional_mode` and `pAngle` and `wide_angle_mapping` and `is_inter` and `PlaneTxType` and `most_probable_stx_set` and `Inv_Most_Probable_Stx_Mapping` block-state computation) stays caller-resolved, exactly as the other §7.15 transform primitives take caller-resolved `txSz`-derived values. Total and panic-free: the accumulation uses `i64` (at most 32 products of `Clip3`-bounded coefficients and small kernel weights), every table index is validated before use, the scan scratch is a fixed 32x32 stack buffer, and `w` / `h` / `dequant` length / `n` / `kernel` / `sec_tx_type` are validated before any coefficient is modified (typed `ReconError`, three new variants). Strictly additive and provably no-output-change: a `pub` primitive with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by a `Round2Signed` both-signs test, a hand-computed single-DC test against literal `IST_4X4_KERNEL` values (102, -45, -53), small-4x4 and large-8x8 matches against an independent in-test re-trace, transpose layout swap, the reduced 8x8 height case, fail-atomic shape/buffer/param rejection, and an `i32::MIN` / `i32::MAX` totality sweep. This completes the §7.15 residual-math transform stack. This row does not parse `sec_tx_type`, derive the kernel/transpose, read frame or block state, or wire into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/secondary_transform.rs::tests::round2_signed_matches_spec_for_both_signs",
    "crates/splot-recon/src/secondary_transform.rs::tests::small_4x4_dc_only_matches_hand_computed_kernel_values",
    "crates/splot-recon/src/secondary_transform.rs::tests::small_4x4_matches_independent_reference",
    "crates/splot-recon/src/secondary_transform.rs::tests::small_4x4_transpose_swaps_output_axes",
    "crates/splot-recon/src/secondary_transform.rs::tests::large_8x8_matches_independent_reference",
    "crates/splot-recon/src/secondary_transform.rs::tests::large_8x8_reduced_height_uses_only_n_inputs",
    "crates/splot-recon/src/secondary_transform.rs::tests::rejects_invalid_shape_buffer_and_params",
    "crates/splot-recon/src/secondary_transform.rs::tests::is_total_for_extreme_coefficients",
]
commands = [
    "cargo test -p splot-recon secondary_transform --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/secondary_transform.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEBLOCK-SAMPLE-FILTER"
name = "Deblocking sample filter process"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17.7.1"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-7-1",
]
crate = "splot-recon"
module = "crates/splot-recon/src/deblock_filter.rs"
change_id = "recon-deblock-sample-filter"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.17.7.1 deblocking sample-filter process `deblock_sample_filter(line, params)`, the first loop-filter primitive. Over a caller-supplied perpendicular sample line with the current-side base index `boundary` (q0 at boundary, p0 at boundary-1), it computes `deltaM2 = Clip3(-qThrClamp, qThrClamp, (p1 - q1 + 3*(q0 - p0)) * 4)` with `qThrClamp = q_thr * q_thresh_mult`, then for `i` from 0 to `Max(max_width_neg, max_width_pos) - 1` modifies the current-side sample at `boundary + i` to `Clip1(sample - Round2(deltaM2 * w_mult_pos * (max_width_pos - i), 3 + DF_SHIFT))` (unless curr_lossless) and, for `i < max_width_neg`, the previous-side sample at `boundary - 1 - i` to `Clip1(sample + Round2(deltaM2 * w_mult_neg * (max_width_neg - i), 3 + DF_SHIFT))` (unless prev_lossless), reading q0/q1/p0/p1 from the original line before any write. `DF_SHIFT = 8` (§3). The §7.17.1/§7.17.2 edge traversal, the §7.17.3 to §7.17.7.2 filter-size/strength/choice derivation (needing DeblockingTxSizes, filter levels, and block state), and the `Q_Thresh_Mults[width-1]` / `W_Mult[maxWidth-1]` §9.2 lookups stay caller-resolved: the caller passes `q_thr`, the per-side widths, and the three pre-indexed table weights as scalars (the §9.2 deblock tables live in splot-core's conversion module, which has core consumers and cannot be wholesale-moved to splot-tables, so the scalar handoff avoids a gen-tables split). Total and panic-free: the ramp uses i64 (each term far inside i64), `qThrClamp` is clamped non-negative so `Clip3` never inverts, and the line bounds and per-side widths (1..=8) are validated before any sample is read or written (typed `ReconError`, two new variants). Strictly additive and provably no-output-change: a `pub` primitive with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by a `Round2` toward-negative-infinity test, a hand-computed symmetric width-2 case (line `[10,20,60,50]` to `[18,36,44,42]`), a reference match across asymmetric widths and prev/curr lossless gating and a clamped deltaM2, a both-lossless no-op, a `Clip1` bit-depth clamp at 8 and 10 bits, fail-atomic width/line rejection, and an i32-extreme totality sweep. This row does not implement the §7.17 edge traversal or filter-strength/choice derivation, the other loop filters (CDEF, CCSO, loop restoration, GDF), or wiring into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/deblock_filter.rs::tests::round2_rounds_toward_negative_infinity",
    "crates/splot-recon/src/deblock_filter.rs::tests::matches_hand_computed_symmetric_width_2",
    "crates/splot-recon/src/deblock_filter.rs::tests::matches_reference_across_configs",
    "crates/splot-recon/src/deblock_filter.rs::tests::lossless_sides_are_untouched",
    "crates/splot-recon/src/deblock_filter.rs::tests::clip1_clamps_to_bit_depth",
    "crates/splot-recon/src/deblock_filter.rs::tests::rejects_invalid_width_and_short_line",
    "crates/splot-recon/src/deblock_filter.rs::tests::is_total_for_extreme_inputs",
]
commands = [
    "cargo test -p splot-recon deblock_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-zero-copy-policy",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/deblock_filter.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEBLOCK-FILTER-MAX-WIDTH"
name = "Deblocking filter maximum width derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/deblock_filter.rs"
change_id = "recon-deblock-filter-max-width"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `deblock_filter_max_width(filter_size, is_chroma, sb_edge) -> (max_width_neg, max_width_pos)`, the AV2 §7.17.3 filter-maximum-width derivation that produces the per-side widths the §7.17.7.1 sample filter (RECON-DEBLOCK-SAMPLE-FILTER) consumes. maxWidthPos is 1 for filter_size at most 4, 3 for 8, `is_chroma ? 4 : 6` for 16, and `is_chroma ? 4 : 8` otherwise; maxWidthNeg is `Min(maxWidthPos, is_chroma ? 2 : 6)` at a super-block edge (sb_edge) and maxWidthPos otherwise. `filter_size` is the §7.17.4 maximum filter size (a transform dimension, computed by the caller as `Min(Tx_Width/Height[prevTxSz], Tx_Width/Height[txSz])`) and `is_chroma` is the spec `plane != 0`, both caller-resolved scalars. Total with no error path (a `const fn` mapping every input to a defined pair); a module-level const evaluates the luma filter_size 32 contract at compile time. Strictly additive and provably no-output-change: a `const fn` with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by a branch-coverage test over every filter_size bucket, both planes, and the super-block-edge cap. This row does not implement the §7.17.4 filter size (a caller-side `Min`), the §7.17.5/§7.17.6 adaptive filter strength (which need segment and qindex state), the §7.17.1/§7.17.2 edge traversal, the other loop filters, or wiring into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/deblock_filter.rs::tests::max_width_covers_every_spec_branch",
]
commands = [
    "cargo test -p splot-recon deblock_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/deblock_filter.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEBLOCK-ADAPTIVE-STRENGTH"
name = "Deblocking adaptive filter strength derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17.5"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-5",
]
crate = "splot-recon"
module = "crates/splot-recon/src/deblock_filter.rs"
change_id = "recon-deblock-adaptive-strength"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the AV2 §7.17.5 adaptive filter strength derivation to the deblock_filter module: `deblock_side_threshold_index(lvl, bit_depth)` computes `qInd = Clip3(0, MAX_SIDE_TABLE - 1, lvl - 24 * (BitDepth - 8))` (MAX_SIDE_TABLE = 296) for the caller to index the §9.2 `Side_Thresholds` array, and `deblock_adaptive_filter_strength(lvl, side_threshold, bit_depth) -> (qThr, side)` computes `qThr = Round2(get_q(lvl, 0), QUANT_TABLE_BITS) >> 6` (via the existing §7.14.2 `quantizer_value`) and `side = Max(side_threshold + (1 << (12 - BitDepth)), 0) >> (13 - BitDepth)`. `qThr` is the threshold the §7.17.7.1 sample filter takes as q_thr; `side` is the side threshold the §7.17.7.2 filter-choice process uses. `lvl` is the §7.17.6 adaptive filter level and `side_threshold` is the caller-resolved `Side_Thresholds[qInd]` (that array lives in splot-core's conversion module, which has core consumers and cannot be wholesale-moved to splot-tables, so the scalar handoff avoids a gen-tables split); both are caller-resolved like the other §7.17 deblock derivations. The §7.17.6 filter-level selection (needing segment and qindex maps and block state) and the §7.17.1/§7.17.2 edge traversal stay with the caller. Total and panic-free: the quantizer lookup is total and the i64 arithmetic with the positive (12 - BitDepth) and (13 - BitDepth) shifts (BitDepth is 8 or 10) cannot overflow; `deblock_side_threshold_index` is a total `const fn`. Strictly additive and provably no-output-change: `pub` items with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by a qInd clip test over both bit depths and a strength test pinning the `side` arithmetic by hand and the `qThr` composition against the independently-tested `quantizer_value`. This row does not implement the §7.17.6 filter-level selection, the §7.17.7.2 filter choice, the §7.17.1/§7.17.2 edge traversal, the other loop filters, or wiring into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/deblock_filter.rs::tests::side_threshold_index_clips_to_table_range",
    "crates/splot-recon/src/deblock_filter.rs::tests::adaptive_filter_strength_matches_spec",
]
commands = [
    "cargo test -p splot-recon deblock_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/deblock_filter.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEBLOCK-FILTER-CHOICE"
name = "Deblocking filter-choice width derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17.7.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-7-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/deblock_filter.rs"
change_id = "recon-deblock-filter-choice"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the AV2 §7.17.7.2 filter-choice process to the deblock_filter module: `deblock_filter_choice(s, t, params) -> usize` chooses the deblocking filter width (0..=maxWidthPos) from the two perpendicular edge sample lines `s` (the first row/column) and `t` (the last, count-1 row/column). It returns 0 immediately when qThr or sideThr is 0, estimates the second derivative secondDeriv[-2..=1] of the samples at the edge from both lines, then walks the §7.17.7.2 threshold cascade (sideThr, sideThr>>2, sideThr>>3, (sideThr*3)>>4, and the per-distance (sideThr*dist)>>4 / qThr*Q_First[dist-4]) widening the chosen width while the local curvature stays under threshold and stopping at the first threshold it exceeds. The spec's directional-derivative gradient term is asymmetric (the negative side uses s[-1]-s[-2], the positive side uses s[0]-s[1]); both are transcribed exactly. `boundary` indexes the spec s[0]/t[0] in each line; the §7.17.3 widths maxWidthNeg/maxWidthPos and the §9.2 Q_First array (passed as a fixed-size [i32; DBL_REG_DECIS_LEN], DBL_REG_DECIS_LEN=9) are caller-resolved (Q_First lives in splot-core's conversion module, which splot-recon cannot reach), as are the per-edge sample gathering into s/t and the §7.17.5 qThr/sideThr thresholds. The §7.17.6 filter-level selection and the §7.17.1/§7.17.2 edge traversal stay with the caller. Total and panic-free: every sample access stays within the validated [boundary-maxSamplesNeg, boundary+maxSamplesPos-1] window (with the unconditional s[3] read covered for every maxWidthPos>1, and the deeper negative reads guarded by the matching maxWidthNeg conditions), line lengths are validated before any read (DeblockFilterLineTooShort), widths are validated 1..=8 (DeblockFilterInvalidWidth), the i64 arithmetic cannot overflow, and the fixed-size Q_First makes the Q_First[dist-4] lookup (dist-4<=4) in bounds. Strictly additive and provably no-output-change: `pub` items with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by hand-anchored deterministic tests (flat samples widen to the full width; a high-curvature spike returns 0; widths 1 and 3 return their immediate results; zero thresholds return 0; invalid width and short line error) plus a 4000-case deterministic-LCG property test comparing the function against an independent in-test re-trace of the spec pseudocode over varied samples, widths, and thresholds, and an adversarial spec re-trace of all seven cascade dimensions. This row does not implement the §7.17.6 filter-level selection, the §7.17.1/§7.17.2 edge traversal, the per-edge sample gathering, the other loop filters, or wiring into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/deblock_filter.rs::tests::filter_choice_flat_returns_full_width",
    "crates/splot-recon/src/deblock_filter.rs::tests::filter_choice_high_curvature_returns_zero",
    "crates/splot-recon/src/deblock_filter.rs::tests::filter_choice_matches_independent_reference",
]
commands = [
    "cargo test -p splot-recon deblock_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/deblock_filter.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-CDEF-FILTER"
name = "CDEF direction search and per-sample filter process"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.18.2", "7.18.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/cdef_filter.rs"
change_id = "decode-general-intra-cdef"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.18 CDEF sample math, the second loop-filter primitive set (after RECON-DEBLOCK-*). Three `pub` functions: (1) `cdef_direction(block) -> (yDir, var)`, the §7.18.2 direction search over an 8x8 luma block whose samples are already reduced to the spec `(CurrFrame >> (BitDepth-8)) - 128` form — it accumulates the eight `partial[dir][offset]` projections with the EXACT spec index mapping, computes each direction's `cost[dir]` from the squared partials times the inline `Div_Table[9] = {0,840,420,280,210,168,140,120,105}`, takes `yDir = argmax cost`, and returns `var = (bestCost - cost[(yDir+4)&7]) >> 10` (i64 accumulators: the squared 8x8 partials times Div_Table overflow i32); (2) `cdef_constrain(diff, threshold, damping) -> i32`, the §7.18.3 constrain clamp (`0` for a zero threshold, else `sign * Clip3(0, Abs(diff), threshold - (Abs(diff) >> Max(0, damping - FloorLog2(threshold))))`); (3) `cdef_filter_sample(taps, priStr, secStr, damping, coeffShift) -> i32`, the §7.18.3 per-sample primary/secondary tap accumulation that combines the center sample, the two primary directional taps, and the four secondary directional taps (each fetched and CdefAvailable-flagged by the caller into `CdefSampleTaps`/`CdefTap`) via the inline `Cdef_Pri_Taps[2][2]={{4,2},{3,3}}` / `Cdef_Sec_Taps[2][2]={{2,1},{2,1}}` selected by `(priStr>>coeffShift)&1`, the `(8 + sum - (sum<0)) >> 4` rounding, and the `Clip3(min, max, ...)` clamp. The inline §7.18.3 `Cdef_Directions[8][2][2]` neighbour-offset table and the §7.18.1 `Cdef_Uv_Dir[2][2][8]` chroma-direction remap are exposed as `pub const` for the caller's `cdef_get_at` addressing. The §7.18/§7.18.1 64x64-unit to 8x8-block traversal, the `cdef_idx` lookup, the §7.18.1 skip/strength derivation, the §5.20.9.3 `is_inside_filter_region` availability check, and the CurrFrame/CdefFrame I/O stay with the caller (the scheduler, DECODE-GENERAL-INTRA-CDEF). All three primitives are total and panic-free (i64 direction accumulators, no fallible inputs). Strictly additive and provably no-output-change: `pub` items with no rewiring, so existing fixtures stay byte-identical. Completeness is proven by deterministic tests: a flat block yields (yDir 0, var 0), a row-varying block selects direction 2 with positive var, the constrain branch table (zero-threshold, large-diff clamp, small-diff passthrough both signs, mid-diff rampdown), an all-unavailable-taps identity, a one-bright-primary-tap pull-toward-neighbour with a hand-computed +1 result, and the Cdef_Uv_Dir / Cdef_Directions table spot-checks. This row does not implement the §7.18 traversal/scheduling, the §7.18.1 strength derivation, the §5.20.10.1 read_cdef parse, the other loop filters, or wiring into the runtime decode path (those are DECODE-GENERAL-INTRA-CDEF)."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/cdef_filter.rs::tests::flat_block_has_zero_variance",
    "crates/splot-recon/src/cdef_filter.rs::tests::horizontal_gradient_picks_a_horizontal_direction",
    "crates/splot-recon/src/cdef_filter.rs::tests::constrain_matches_spec_branches",
    "crates/splot-recon/src/cdef_filter.rs::tests::filter_with_all_unavailable_taps_is_identity",
    "crates/splot-recon/src/cdef_filter.rs::tests::filter_pulls_center_toward_a_brighter_primary_neighbour",
    "crates/splot-recon/src/cdef_filter.rs::tests::uv_dir_table_is_the_spec_remap",
    "crates/splot-recon/src/cdef_filter.rs::tests::directions_table_matches_spec",
]
commands = [
    "cargo test -p splot-recon cdef_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/cdef_filter.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-LOOP-RESTORATION-SOURCE-SAMPLE"
name = "Loop restoration source-sample selector"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.20.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/loop_restoration.rs"
change_id = "recon-loop-restoration-source-sample"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a scheduler-free `splot-recon` AV2 §7.20.2 get source sample selector `loop_restoration_source_sample(plane, x, y, bounds)`. The helper takes caller-resolved luma `LumaStartX`, `LumaEndX`, `LumaStartY`, `LumaEndY`, `LumaStripeStartY`, `LumaStripeEndY`, plus sequence subsampling, derives plane subsampling as zero for luma and sequence subsampling for chroma, clips source coordinates to the luma-derived plane extents, selects `CdefFrame` for coordinates inside the current stripe, and selects `CurrFrame` above or below the stripe while clamping to at most two lines outside it. It returns only the clipped plane coordinates and selected source enum; callers still own frame storage reads, §7.20.1 loop-restoration traversal, restoration-unit selection, Wiener NS/PC-Wiener/GDF filtering, and runtime output. Bounds and subsampling are validated before resolution with typed `ReconError` failures, and the out-of-stripe two-line limits use saturating arithmetic to keep the helper total for extreme caller-supplied bounds. This feature's scope is the selector primitive; it does not by itself own traversal, filtering, or output. The current decoder's generic final-filter path uses the selector with retained active LR records and publishes successfully decoded typed `u8` or `u16` output for supported Wiener NS oracle fixtures. Proven by focused luma in-stripe CdefFrame selection, above/below stripe CurrFrame selection with two-line clamping, chroma subsampled extents, luma ignoring sequence subsampling, invalid subsampling, invalid luma ranges, and stripe-bound errors. The selector primitive itself does not perform frame reads, full loop restoration traversal, Wiener NS or chroma Wiener NS filtering, PC-Wiener classification, GDF, or BRU."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/loop_restoration.rs::tests::luma_inside_stripe_reads_cdef_frame",
    "crates/splot-recon/src/loop_restoration.rs::tests::luma_above_stripe_reads_curr_frame_clamped_to_two_lines",
    "crates/splot-recon/src/loop_restoration.rs::tests::luma_below_stripe_reads_curr_frame_clamped_to_two_lines",
    "crates/splot-recon/src/loop_restoration.rs::tests::chroma_uses_subsampled_bounds_and_stripe",
    "crates/splot-recon/src/loop_restoration.rs::tests::luma_ignores_sequence_subsampling",
    "crates/splot-recon/src/loop_restoration.rs::tests::rejects_invalid_subsampling",
    "crates/splot-recon/src/loop_restoration.rs::tests::rejects_invalid_luma_range",
    "crates/splot-recon/src/loop_restoration.rs::tests::rejects_stripe_outside_luma_range",
    "crates/splot-recon/src/loop_restoration.rs::tests::rejects_inverted_stripe_range",
    "crates/splot-recon/src/loop_restoration.rs::tests::rejects_unrepresentable_luma_bound",
]
commands = [
    "cargo test -p splot-recon loop_restoration --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/loop_restoration.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-LOOP-RESTORATION-SOURCE-READ"
name = "Loop restoration source-sample frame read"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.20.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/loop_restoration.rs"
change_id = "recon-loop-restoration-source-read"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds scheduler-free `splot-recon` AV2 §7.20.2 frame-view source reads with `loop_restoration_source_sample_value(plane, x, y, bounds, curr_frame, cdef_frame)`. The helper composes the already-proven `loop_restoration_source_sample` coordinate/source selector with immutable `FrameRef` reads, selecting `CurrFrame` for samples above or below the stripe and `CdefFrame` for in-stripe samples, then reading the selected current-plane coordinate as an absolute coded-storage coordinate through a direct `PlaneRef` stride offset. It validates that the two source frames describe the same frame metadata and selected-plane view geometry, rejects caller-resolved chroma subsampling that does not match the source frame pixel format, returns the existing missing-chroma error when the selected plane is absent, rejects caller-resolved bounds that address outside the selected coded plane, and rejects unsupported sample storage or source samples outside the active bit-depth range with typed `ReconError` failures. This feature's scope is the immutable frame-view read helper; it does not by itself own traversal, filtering, or output. The current decoder obtains bounded LR source windows through its generic final-filter path and publishes successfully decoded typed `u8` or `u16` output for supported Wiener NS oracle fixtures. Proven by focused tests for CdefFrame reads, CurrFrame reads after two-line clamp, chroma subsampled reads, coded-storage reads under a visible crop, frame-metadata mismatch, plane-view-geometry mismatch, chroma-subsampling mismatch, out-of-coded-plane bounds, unsupported sample storage, out-of-range source samples, and missing chroma. The frame-view helper itself does not perform §7.20 loop-restoration traversal, Wiener NS or chroma Wiener NS invocation, §7.20.4 PC-Wiener classification, GDF, or BRU."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_reads_cdef_frame_inside_stripe",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_reads_curr_frame_above_stripe_after_two_line_clamp",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_reads_chroma_cdef_frame_with_subsampled_bounds",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_reads_coded_storage_coordinates_despite_visible_rect_origin",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_mismatched_frame_info",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_mismatched_plane_view_geometry",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_chroma_subsampling_mismatch_for_yuv444",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_sample_outside_coded_plane",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_u8_storage_for_ten_bit_frame",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_source_sample_above_bit_depth",
    "crates/splot-recon/src/loop_restoration.rs::tests::sample_value_rejects_missing_chroma_plane",
]
commands = [
    "cargo test -p splot-recon loop_restoration --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/loop_restoration.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-WIENERNS-FILTER-PRIMITIVE"
name = "Wiener NS luma filter primitive"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.20.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/wienerns_filter.rs"
change_id = "recon-wienerns-filter-primitive"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a scheduler-free `splot-recon` AV2 §7.20.3 luma non-separable Wiener filter primitive `wiener_ns_filter_luma_block(output, params, source_sample)`. For each output sample, it reads the caller-resolved center sample, initializes `s = m << WIENER_NS_PREC_BITS` (`WIENER_NS_PREC_BITS = 7`), walks the verbatim `Wiener_Ns_Config_Y` tap table (`WIENER_NS_LUMA_TAPS = 32`, `WIENER_NS_LUMA_COEFFS = 16`), adds `(tap - m) * coeffs_by_class[subclass][idx]`, then writes `Clip1(Round2(s, 7))` into the caller's strided output. The caller resolves §7.20.2 source-sample clipping/stripe behavior, frame/restoration-unit traversal, frame-level-vs-unit coefficient selection, and any §7.20.4 pixel-classified Wiener subclass mapping. The primitive supports one class for the whole block or a row-major per-sample subclass map. It computes into a temporary buffer and validates dimensions, output stride/length, coefficient-class presence, subclass length/range, sample storage, and source sample range before mutating output, with typed `ReconError` failures. This feature's scope is the callback-based luma primitive; it does not by itself own runtime traversal or output. The current decoder's generic final-filter path uses the bit-identical padded luma variants and publishes successfully decoded typed `u8` or `u16` output for supported active-LR oracle fixtures. Proven by zero-coefficient source-copy, hand-computed tap accumulation, subclass selection, 8-bit and 10-bit Clip1 clamps, fail-atomic output shape/class/subclass/source-sample errors, and u8-vs-10-bit storage rejection. This luma primitive leaves the chroma branch to `RECON-WIENERNS-CHROMA-FILTER-PRIMITIVE`; it does not itself perform §7.20.2 source-sample clipping, §7.20 loop-restoration traversal, restoration-unit syntax, temporal/reference Wiener state, or PC-Wiener classification."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/wienerns_filter.rs::tests::zero_coefficients_copy_source_samples",
    "crates/splot-recon/src/wienerns_filter.rs::tests::hand_computed_luma_tap_accumulation",
    "crates/splot-recon/src/wienerns_filter.rs::tests::subclass_selects_coefficients_per_sample",
    "crates/splot-recon/src/wienerns_filter.rs::tests::clip1_clamps_eight_bit_output",
    "crates/splot-recon/src/wienerns_filter.rs::tests::ten_bit_u16_output_is_supported_and_clamped",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_invalid_output_stride_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_short_output_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_missing_coefficient_classes_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_short_subclass_map_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_out_of_range_subclass_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_source_sample_out_of_range_fail_atomically",
    "crates/splot-recon/src/wienerns_filter.rs::tests::rejects_u8_storage_for_ten_bit",
]
commands = [
    "cargo test -p splot-recon wienerns_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/wienerns_filter.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-WIENERNS-CHROMA-FILTER-PRIMITIVE"
name = "Wiener NS chroma filter primitive"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.20.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
]
crate = "splot-recon"
module = "crates/splot-recon/src/wienerns_chroma_filter.rs"
change_id = "recon-wienerns-chroma-filter-primitive"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a scheduler-free `splot-recon` AV2 §7.20.3 chroma non-separable Wiener filter primitive `wiener_ns_filter_chroma_block(output, params, chroma_source_sample, luma_source_sample)`. For each output sample, it reads the caller-resolved center chroma sample, initializes `s = m << WIENER_NS_PREC_BITS` (`WIENER_NS_PREC_BITS = 7`), walks the verbatim `Wiener_Ns_Config_Uv` table (`WIENER_NS_CHROMA_TAPS = 12`, `WIENER_NS_CHROMA_COEFFS = 18`), adds chroma tap differences using coefficient indexes 0 through 5, derives `mLuma` through the §7.20.3 `get_luma_sample` process, adds luma tap differences using coefficient indexes 6 through 17, then writes `Clip1(Round2(s, 7))` into the caller's strided output. The caller resolves §7.20.2 source-sample clipping/stripe/source-frame behavior, frame/restoration-unit traversal, frame-level-vs-unit coefficient selection, and current-plane coordinate offsets. The primitive validates dimensions, output stride/length, chroma subsampling, luma bounds, `cfl_ds_filter_index`, sample storage, and source sample range before mutating output, with typed `ReconError` failures and a temporary output buffer for fail-atomicity. This feature's scope is the callback-based chroma primitive; it does not by itself own runtime traversal or output. The current decoder's generic final-filter path uses the bit-identical padded 4:2:0 chroma variant and publishes successfully decoded typed `u8` or `u16` output for supported active-LR oracle fixtures. Proven by zero-coefficient chroma source-copy, hand-computed chroma tap accumulation, hand-computed luma tap contribution, 4:2:0 luma downsampling for `cfl_ds_filter_index` 0/1/3, 4:2:2 direct luma reads, 10-bit Clip1 clamp, fail-atomic output-shape/luma-bound/subsampling/filter-index/source-sample errors, and u8-vs-10-bit storage rejection through the shared sample-type guard. The chroma primitive itself does not perform §7.20 loop-restoration traversal, §7.20.2 frame reads, coefficient bank selection, temporal/reference Wiener state, §7.20.4 PC-Wiener classification, GDF, or BRU."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::zero_coefficients_copy_source_samples",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::hand_computed_chroma_tap_accumulation",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::hand_computed_luma_tap_contribution_without_subsampling",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::luma_420_filter_index_zero_averages_two_by_two",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::luma_420_filter_index_one_uses_vertical_left_column",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::luma_420_filter_index_three_maps_to_zero",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::luma_422_reads_scaled_direct_luma_sample_without_420_filter",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::clip1_clamps_ten_bit_output",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_invalid_output_stride_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_short_output_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_invalid_subsampling_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_inverted_luma_x_bounds_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_luma_last_x_underflow_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_luma_last_y_underflow_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_invalid_cfl_filter_index_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_source_sample_out_of_range_fail_atomically",
    "crates/splot-recon/src/wienerns_chroma_filter.rs::tests::rejects_luma_sample_out_of_range_fail_atomically",
]
commands = [
    "cargo test -p splot-recon wienerns_chroma_filter --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/wienerns_chroma_filter.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-PC-WIENER-CLASSIFICATION"
name = "PC-Wiener classification primitive"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.20.4", "9.8"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/09-additional-tables/09-08-loop-restoration-tables.md",
]
crate = "splot-recon"
module = "crates/splot-recon/src/pc_wiener.rs"
change_id = "recon-pc-wiener-classification"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Introduced the scheduler-free `splot-recon` AV2 §7.20.4 pixel-classified Wiener skip-filter classification primitive `pc_wiener_classify(params, source_sample, tx_skip)`. It evaluates the 6x6 `get_box_features` window, performs the seven second-derivative source reads per feature point, accumulates the three absolute derivative features plus `LrTxSkip`, applies the §7.20.4 normalizers and active-bit-depth scaling, computes `get_qval_given_tskip` from `base_q_idx`, constructs the four 3-bit `lutInput` fields with `Round2Signed` and `Clip3`, and returns the `Pc_Wiener_Lut_To_Class` lookup through the generated §9.8 table. The primitive validates sample storage and ranges, bounds, coordinate conversion, caller storage lookups, and boolean tx-skip values before returning typed `ReconError` failures. The later decoder storage and final-filter features now integrate this primitive with populated current/CDEF frame samples, live `LrTxSkip`, filter-class retention, subclass lookup, §7.20.3 filtering, and typed 8-bit or 10-bit output. Focused primitive tests retain its local arithmetic and error coverage, while current PC-Wiener oracle fixtures protect successful runtime filtering; the removed live-storage diagnostic is no longer reachable evidence."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/pc_wiener.rs::tests::flat_source_without_skips_classifies_to_lut_zero",
    "crates/splot-recon/src/pc_wiener.rs::tests::quadratic_ten_bit_source_accumulates_features_and_tx_skip",
    "crates/splot-recon/src/pc_wiener.rs::tests::grid_classification_matches_scalar_cells_and_reuses_features",
    "crates/splot-recon/src/pc_wiener.rs::tests::tx_skip_lookup_clips_to_block_stripe_and_tile_bounds",
    "crates/splot-recon/src/pc_wiener.rs::tests::rejects_source_samples_outside_bit_depth",
    "crates/splot-recon/src/pc_wiener.rs::tests::propagates_tx_skip_lookup_errors",
    "crates/splot-recon/src/pc_wiener.rs::tests::rejects_non_boolean_tx_skip_values",
    "crates/splot-recon/src/pc_wiener.rs::tests::rejects_u8_storage_for_ten_bit_classification",
    "crates/splot-recon/src/pc_wiener.rs::tests::rejects_inverted_bounds",
    "crates/splot-tables/tests/tables_spot.rs::loop_restoration_pc_wiener_tables_match_mirror",
]
commands = [
    "cargo test -p splot-recon pc_wiener --locked",
    "cargo test -p splot-tables loop_restoration_pc_wiener_tables_match_mirror --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask gen-tables --check",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/pc_wiener.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "crates/splot-tables/src/tables/loop_restoration.rs",
    "xtask/src/gen_tables.rs",
]
diagnostics = []

[[feature]]
id = "RECON-SUBPEL-MC"
name = "Sub-pel inter prediction convolution kernel"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.13.3.16", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-16",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-recon"
module = "crates/splot-recon/src/subpel_mc.rs"
change_id = "recon-subpel-mc"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the AV2 §7.13.3.18 block inter prediction (sub-pel motion compensation) kernel to splot-recon: `subpel_predict_block(reference, params) -> Result<Vec<u16>>`, the separable interpolation-filter convolution that produces the single-reference (non-compound) prediction block. The horizontal pass builds an intermediateHeight*w array `intermediate[r][c] = Round2(Σ Subpel_Filters[filter][(p>>6)&SUBPEL_MASK][t] * ref[Clip3(firstY,lastY,(y>>10)+r-3)][Clip3(firstX,lastX,(p>>10)+t-3)], InterRound0)` with `p = startX + stepX*c`; the vertical pass produces `Preds[r][c] = Round2(Σ Subpel_Filters[filter][(p>>6)&SUBPEL_MASK][t] * intermediate[(p>>10)+t][c], InterRound1)` with `p = (startY & 1023) + stepY*r`, and the kernel applies the final §7.13.3 single-reference write `Clip1(Preds[0][i][j])`. InterRound0 is 3 and InterRound1 is 11 (non-compound) per §7.13.3.16. The §7.13.3.18 small-block substitution (`dim <= 4` maps EIGHTTAP/EIGHTTAP_SHARP to the 4-tap index 4 and EIGHTTAP_SMOOTH to index 5) is applied per pass keyed on w (horizontal) and h (vertical). The §9 `Subpel_Filters[6][16][8]` table is transcribed verbatim from §7.13.3.18, diff-verified, with a test asserting every row sums to 128 (1<<FILTER_BITS) and all taps are even (the spec invariant) plus distinctive-row spot checks. The reference is a `ReferencePlaneView` over a row-major width*height u16 buffer; the §7.13.3.18 Clip3 to `[firstX,lastX]x[firstY,lastY]` plus the view's own dimension clamp implement the §7.11.3.x reference-border extension without copying a padded plane. The §7.13.3.17 motion-vector scaling (startX/startY/stepX/stepY), the §7.13.3.18 reference-clipping region (firstX/firstY/lastX/lastY), the §6 interp_filter, the block dimensions, and the reference samples are caller-resolved. Total and panic-free: the constructor validates the width*height buffer length and non-zero dimensions, the convolution rejects a zero/oversized (>128) block and a negative step, the intermediateHeight derivation guarantees the vertical-pass base+7 row is in range (with an explicit guard), and the i64 filter sums cannot overflow for in-range samples. Strictly additive and no-output-change (`pub` items with no rewiring), so the minimal flat-intra and zero-MV inter fixture snapshots stay byte-identical. Completeness is proven by hand-anchored worked examples (a full-pel position is a bit-exact reference copy; a flat reference reconstructs flat for any phase; a hand-computed EIGHTTAP_SHARP half-pel; the border-extension corner case; 10-bit Clip1; the small-block 4-tap substitution) plus a 2000-case deterministic-LCG property test comparing `subpel_predict_block` against an independent in-test re-trace of the §7.13.3.18 pseudocode over varied content, block sizes, filters, and sub-pel phases, and the error cases. This row does not implement the §7.13.3.17 motion-vector scaling, the §7.13.3 compound / mask-blend / distance-weighted prediction, the §7.13.3.19 block warp, the §7.13.3 reference-area clipping selection, intra block copy, the §5.20.7 read_mv / interp_filter symbol decode, the inter mode_info or partition syntax, or wiring into the runtime decode path."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/subpel_mc/tests.rs::tests::full_pel_zero_fraction_is_exact_copy",
    "crates/splot-recon/src/subpel_mc/tests.rs::tests::half_pel_horizontal_worked_example",
    "crates/splot-recon/src/subpel_mc/tests.rs::tests::matches_independent_reference_over_many_cases",
    "crates/splot-recon/src/subpel_mc/tests.rs::zero_phase_copy_matches_independent_reference",
    "crates/splot-recon/src/subpel_mc/tests.rs::half_pel_vertical_with_horizontal_zero_phase_matches_reference",
    "crates/splot-recon/src/subpel_mc/tests.rs::strided_view_matches_contiguous_view",
    "crates/splot-recon/src/subpel_mc/tests.rs::tests::subpel_filters_table_shape_and_sums",
]
commands = [
    "cargo test -p splot-recon subpel --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/subpel_mc.rs",
    "crates/splot-recon/src/subpel_mc/tests.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INVERSE-TRANSFORM-2D"
name = "2D matrix inverse transform core (row-then-column)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.15.4.1"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4-1",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-recon"
module = "crates/splot-recon/src/inverse_transform_2d.rs"
change_id = "recon-inverse-transform-2d"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Wires the §7.15.2 1D inverse transforms (§7.15.2.1 kernel, §7.15.2.2 Walsh-Hadamard, §7.15.2.3 identity) into the §7.15.4.1 row-then-column 2D matrix transform `inverse_transform_2d(params, dequant, residual)` over a caller-supplied dequantized block. `InverseTransform2d` carries the *original* (unadjusted) `txSz` log2 dimensions `log2_width`/`log2_height` (`log2W`/`log2H`, each 2..=6), the lossless flag, the per-dimension transform selection, and the row/column shifts. The operating (adjusted) size is derived internally as `1 << Min(log2, 5)` per the `Adjusted_Tx_Size` table (`adjLog2 = Min(log2, 5)`; see also §8 `bwl = Min(Tx_Width_Log2[txSz], 5)`), so the block transformed is at most 32x32. The §7.15.4.1 √2 rescale `Round2(Dequant*2896, 12)` fires when `Abs(log2W - log2H)` is odd computed from the ORIGINAL log2 dimensions (so a 64-sample side, whose adjusted parity differs, rescales correctly), and the identity scale is `get_identity_scale` of the ORIGINAL pass log2 dimension. The row pass applies the WHT (shift 3, lossless), identity (`get_identity_scale(log2W)`, rowShift, colTx=0), or kernel (rowType, rowShift, colTx=0) transform; the column pass applies WHT (shift 0), identity (`get_identity_scale(log2H)`, colShift, colTx=1), or kernel (colType, colShift, colTx=1). It reads no frame/segment/tile state, validates the log2 shape (each 2..=6; both 2 when lossless) and the `w*h` buffer lengths with typed `ReconError`, and is panic-free for valid shapes via fixed 32x32 stack buffers and the total 1D primitives. Completeness is proven by DC-only 4x4 and 8x8 DCT flat fields, the lossless 4x4 WHT vector, identity position preservation, the rectangular 4x8 rescale path, an original-vs-adjusted parity regression (TX_64X32 vs a pre-rescaled 32x32), a mixed row-DCT/column-identity energy-confinement case, the `round2_2896` √2 vector, and shape/lossless/buffer rejections. This row does not implement the §7.15.4 outer process (the `Adjusted_Tx_Size` lookup itself, the `Transform_Shift` and `get_transform_1d_type` derivations, the `Lossless && IDTX` bit-shift shortcut, the DPCM cumulative sum, the adjusted-size sample duplication), the §7.15.3 secondary transform, the §7.14.4 dequantization process, residual addition, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::dc_only_dct_4x4_is_flat",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::lossless_4x4_walsh_hadamard_dc",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::identity_2d_preserves_coefficient_position",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::round2_2896_applies_sqrt2_rescale",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::rectangular_4x8_applies_rescale_path",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::rescale_parity_uses_original_not_adjusted_dimensions",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::dc_only_dct_8x8_is_flat",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::mixed_row_dct_col_identity_confines_energy_to_row_zero",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::orchestration_matches_manual_row_then_column_for_non_square_kernels",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::rejects_unsupported_shape",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::rejects_non_4x4_lossless",
    "crates/splot-recon/src/inverse_transform_2d.rs::tests::rejects_buffer_length_mismatch",
]
commands = [
    "cargo test -p splot-recon inverse_transform_2d --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/inverse_transform_2d.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-INVERSE-TRANSFORM-2D-OUTER"
name = "2D inverse transform outer process (shortcut, DPCM, sample duplication)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/inverse_transform_2d_outer.rs"
change_id = "recon-inverse-transform-2d-outer"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Wraps the §7.15.4.1 2D matrix transform core (RECON-INVERSE-TRANSFORM-2D) with the surrounding §7.15.4 outer process `inverse_transform_2d_outer(params, dequant, residual)`. `InverseTransform2dOuter` carries the *original* (unadjusted) `txSz` log2 dims (`log2W`/`log2H`, each 2..=6), the lossless flag, a `plane_tx_type_is_idtx` flag, the caller-resolved §7.15.4.1 row/col type + shift selections, the bit depth, and an optional `DpcmDirection` (Vertical for V_PRED, else Horizontal). It derives the adjusted operating size `1 << Min(log2,5)` and the original size `1 << log2` from the log2 dims (so no `Adjusted_Tx_Size`/`Tx_Width`/`Tx_Height` conversion tables are needed and there is no splot-core dependency). The process: (1) if `lossless && plane_tx_type_is_idtx`, the §7.15.4 shortcut `Residual = Dequant >> (3 - shift)` with `shift = (pels>256)+(pels>1024)` (pels = adjusted w*h), else invoke §7.15.4.1 into an adjusted-size scratch; (2) DPCM cumulative sum over the adjusted block (Vertical sums down columns, Horizontal across rows) via `wrapping_add` so it is total; (3) sample duplication expanding the adjusted block into the original-size residual (nearest-neighbour 2x along any 64-sample dimension). `dequant` is the adjusted `adjW*adjH` block, `residual` the original `w*h`; both lengths and the log2 shape are validated with typed `ReconError` (new `InverseTransform2dOuterBufferMismatch`), and it is panic-free for valid shapes via the fixed 32x32 scratch and the total core. Completeness is proven by a no-adjustment-equals-core case, the lossless-IDTX shortcut vector, vertical and horizontal DPCM running-sum vectors, 64-wide and 64x64 sample-duplication cases (vs the adjusted core), a DPCM totality extreme, and the shape/buffer rejections. This row does not implement the §7.15.4 `Transform_Shift` or `get_transform_1d_type` derivations (rowType/colType/shifts stay caller-resolved), the §7.15.3 secondary transform, the §7.14.4 dequantization process, residual addition, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::non_adjusted_block_matches_core_transform",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::lossless_idtx_shortcut_is_dequant_shifted",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::vertical_dpcm_accumulates_down_columns",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::horizontal_dpcm_accumulates_across_rows",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::sample_duplication_expands_64_wide_block",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::sample_duplication_expands_64_tall_block",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::sample_duplication_expands_both_dimensions_for_64x64",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::dpcm_applies_to_adjusted_block_before_duplication",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::dpcm_applies_after_lossless_idtx_shortcut",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::dpcm_is_total_for_extreme_values",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::rejects_unsupported_shape",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::rejects_non_4x4_lossless",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::rejects_buffer_length_mismatch",
]
commands = [
    "cargo test -p splot-recon inverse_transform_2d_outer --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/inverse_transform_2d_outer.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEQUANT-PROCESS"
name = "Dequantization process (Quant to Dequant)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "4.8", "7.14.4"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/dequant_process.rs"
change_id = "recon-dequant-process"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the scheduler-free `splot-recon` AV2 §7.14.4 dequantization process that turns coded `Quant` coefficients into the `Dequant` array the §7.15.4 inverse transform consumes. `dequant_coefficient(quant_coeff, q2, dq_denom, bit_depth)` implements steps 3-8 exactly: `sign = (qc<0)?-1:1`; `dqHigh = Abs(qc)*q2`; `dq = Round2(dqHigh & 0xFFFFFF, QUANT_TABLE_BITS=3)`; `dq2 = sign*(dq/dq_denom)`; `Dequant = Clip3(-(1<<(7+BitDepth)), (1<<(7+BitDepth))-1, dq2)`. `dequantize_block(params, quant, out)` with `DequantBlockParams` applies it over a `tx_width*tx_height` (each 4/8/16/32) row-major block, selecting `dc_quant` for the (0,0) DC coefficient and `ac_quant` otherwise (the §7.14.2 get_dc_quant/get_ac_quant values). `q2` is the resolved per-coefficient quantizer (the §7.14.2 DC/AC quantizer, optionally QM-weighted `Round2(q*m,5)`) and `dq_denom = 1<<shift`, both caller-resolved. It is total and panic-free: the product/rounding use `i64`, `unsigned_abs` handles `i32::MIN`, a zero `dq_denom` is treated as 1, and the `Clip3` keeps every result in range; typed `ReconError` rejects non-4/8/16/32 shapes and `quant`/`out` length mismatches. Completeness is proven by the Round2-mask vector, the dq_denom divide, the 24-bit mask, both bit-depth clip bounds, the `i32::MIN`/max-quantizer totality extreme, the DC-vs-AC block selection, and the shape/length rejections. This row does not implement the §7.14.4 quantization-matrix weighting (the `Quantizer_Matrix`/`UserQm` lookups), the `shift`/`useFsc`/`allow_tcq` derivation, the coefficient entropy decode that produces `Quant`, the §7.15.4 inverse transform, tile syntax traversal, runtime decode output, reference refresh, AVM/dav2d invocation, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/dequant_process.rs::tests::dequant_coefficient_applies_round2_mask_and_denom",
    "crates/splot-recon/src/dequant_process.rs::tests::dequant_coefficient_divides_by_dq_denom",
    "crates/splot-recon/src/dequant_process.rs::tests::dequant_coefficient_masks_to_24_bits",
    "crates/splot-recon/src/dequant_process.rs::tests::dequant_coefficient_clips_to_bit_depth_bound",
    "crates/splot-recon/src/dequant_process.rs::tests::dequant_coefficient_is_total_for_extreme_inputs",
    "crates/splot-recon/src/dequant_process.rs::tests::dequantize_block_uses_dc_quant_for_origin_and_ac_elsewhere",
    "crates/splot-recon/src/dequant_process.rs::tests::dequantize_block_rejects_unsupported_shape",
    "crates/splot-recon/src/dequant_process.rs::tests::dequantize_block_rejects_length_mismatch",
]
commands = [
    "cargo test -p splot-recon dequant_process --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "RUSTDOCFLAGS=\"-D warnings\" cargo doc -p splot-recon --no-deps --locked",
    "cargo xtask check-source-lines",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/dequant_process.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DEQUANT-QM-WEIGHT"
name = "Dequantization quantization-matrix weighting"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.8", "7.14.4", "9.4"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/09-additional-tables/09-04-quantizer-matrix-tables.md",
]
crate = "splot-recon"
module = "crates/splot-recon/src/dequant_process.rs"
change_id = "recon-dequant-qm-weight"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the AV2 §7.14.4 step-2 quantization-matrix weighting to the dequant process, consuming the generated §9.4 `Quantizer_Matrix` / `Qm_Offset` tables. To make those tables reachable from `splot-recon` (which may not depend on `splot-core`), the §9.4 `quantizer` gen-tables module is relocated to the dependency-free `splot-tables` crate via the per-module `output_dir_for` routing (the same mechanism INFRA-SHARED-SPEC-TABLES used for the §9.6/§9.7 kernels; the module had no `splot-core` consumer, so the move is byte-identical and zero-churn — the 236-table determinism count is unchanged). `quantization_matrix_weight(QmWeightIndex)` returns the built-in weight `m = Quantizer_Matrix[segLvl][plane>0][Qm_Offset[txSz] + i*tw + j]` with bounds-checked indices (typed `ReconError::InvalidQuantizerMatrixIndex`), and `qm_weighted_quantizer(q, m)` computes the §7.14.4 step-2 weighted quantizer `q2 = Round2(q*m, 5)` in `i64` clamped into `u32` (total). The caller passes the resulting `q2` to `dequant_coefficient`. The runtime decode path now carries the current segment id through transform residual decode and stores all 16 `SegQMLevel` triples in `QmFrameLevels`; `syn-qmseg-intra-64x64.ivf` (AVM --enable-qm --aq-mode, last_active_seg_id 7, built-in QM level 7) decodes byte-exact to AVM raw SHA-256 e6d6dc0726e808ddbc5f4410a3d0d0cc351b17803edd76f012157d912e47396e, closing the combined QM+segmentation fail-closed runtime gap. This row does not implement user-defined `UserQm` matrices, live AVM/dav2d invocation in CI, reference refresh, or scheduler use inside `splot-recon`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/dequant_process.rs::tests::qm_weighted_quantizer_applies_round2_shift5",
    "crates/splot-recon/src/dequant_process.rs::tests::qm_weighted_quantizer_is_total_for_extreme_inputs",
    "crates/splot-recon/src/dequant_process.rs::tests::quantization_matrix_weight_matches_the_generated_table",
    "crates/splot-recon/src/dequant_process.rs::tests::quantization_matrix_weight_rejects_out_of_range_indices",
    "crates/splot-recon/src/dequant_process.rs::tests::quantization_matrix_weight_rejects_coords_outside_the_transform_sub_block",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::resolve_block_qm_selects_level_plane_and_offset",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::qmseg_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-recon dequant_process --locked",
    "cargo test -p splot-tables --locked",
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask gen-tables --check",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-qmseg-intra-64x64.ivf",
    "tests/conformance/decoder-oracle.toml",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/pipeline/frame_engine/intra.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/prediction/inter/block.rs",
    "crates/splot-recon/src/dequant_process.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
    "crates/splot-tables/src/tables/quantizer.rs",
    "xtask/src/gen_tables.rs",
]
diagnostics = []

[[feature]]
id = "RECON-TRANSFORM-SHIFT-LOOKUP"
name = "Inverse transform Transform_Shift lookup"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.15.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-recon"
module = "crates/splot-recon/src/transform_params.rs"
change_id = "recon-transform-shift-lookup"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `transform_shift(log2_width, log2_height) -> Result<(rowShift, colShift)>` to a new `splot-recon` `transform_params` module, the first of the §7.15.4 inverse-transform parameter derivations the RECON-INVERSE-TRANSFORM-2D-OUTER row left caller-resolved. It returns `(Transform_Shift[txSz][0], Transform_Shift[txSz][1])` for the `txSz` whose `(Tx_Width_Log2, Tx_Height_Log2)` equals the requested shape. The verbatim 25-row §7.15.4 `Transform_Shift[TX_SIZES_ALL][2]` constant is hand-written and spec-cited because it is a §7.15.4 process-body constant that is NOT present in the generated `all_tables.h` §9 attachment (so it is deliberately not a gen-tables output); the parallel `(log2W, log2H)` key table mirrors the §9.2 `Tx_Width_Log2`/`Tx_Height_Log2` values, which `splot-recon` cannot reach through `splot-core` under the one-way dependency rule — the same reason the §7.15 transforms take caller-resolved log2 dimensions. The result drops into the existing `InverseTransform2dOuter` `row_shift`/`col_shift` fields. Strictly additive and provably no-output-change: it adds a leaf function plus one typed error variant (`ReconError::InvalidTransformShiftShape`) and rewires no existing code path, so the minimal flat-intra fixture snapshots are byte-identical. This row does not implement the `get_transform_1d_type` row/column transform-type derivation, the DPCM-direction selection, any wiring of `transform_shift` into the runtime decode path, the §7.15.3 secondary transform, or the coefficient entropy decode that produces `Quant`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/transform_params.rs::tests::transform_shift_returns_the_parallel_table_entry_for_every_shape",
    "crates/splot-recon/src/transform_params.rs::tests::tx_size_log2_dims_keys_are_distinct",
    "crates/splot-recon/src/transform_params.rs::tests::transform_shift_matches_independently_transcribed_spec_values",
    "crates/splot-recon/src/transform_params.rs::tests::transform_shift_is_symmetric_under_transpose",
    "crates/splot-recon/src/transform_params.rs::tests::transform_shift_rejects_non_av2_shapes",
]
commands = [
    "cargo test -p splot-recon transform_params --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/transform_params.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-GET-TRANSFORM-1D-TYPE"
name = "Inverse transform get_transform_1d_type derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/transform_params.rs"
change_id = "recon-get-transform-1d-type"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `get_transform_1d_type(plane_tx_type, pass, size, use_ddt) -> Result<InverseTransform2dDim>` to the `splot-recon` `transform_params` module, the second of the §7.15.4 inverse-transform parameter derivations the RECON-INVERSE-TRANSFORM-2D-OUTER row left caller-resolved. It returns `Transform_1d_Type[PlaneTxType][dir]` with the spec `useDdt` substitution: when `use_ddt` (the caller-resolved `enable_inter_ddt && !use_intrabc && is_inter`) is set, the base type is `ADST` or `FDST`, and `size != 4`, the type becomes `DDTX` or `FDDT` respectively. The verbatim 16-row §7.15.4 `Transform_1d_Type[TX_TYPES][2]` constant is hand-written and spec-cited because, like `Transform_Shift`, it is a §7.15.4 process-body constant absent from the generated `all_tables.h` §9 attachment (deliberately not a gen-tables output); `IDT` maps to `InverseTransform2dDim::Identity` and the kernel types to `InverseTransform2dDim::Kernel`, the exact `row_type`/`col_type` field type of the existing `InverseTransform2dOuter`. The `TransformPass` enum selects the spec `dir` (Row=0, Col=1). Strictly additive and provably no-output-change: a `const fn` plus a `TransformPass` enum and one typed error variant (`ReconError::InvalidPlaneTxType`), with no rewiring of any decode path. The combined transform-parameter resolve helper that composes this row with RECON-TRANSFORM-SHIFT-LOOKUP is now RECON-RESOLVE-2D-TRANSFORM-PARAMS. This row does not implement the DPCM-direction selection, any wiring into the runtime decode path, the §7.15.3 secondary transform, or the coefficient entropy decode that produces `Quant`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/transform_params.rs::tests::get_transform_1d_type_matches_the_spec_table_without_ddt",
    "crates/splot-recon/src/transform_params.rs::tests::get_transform_1d_type_applies_ddt_substitution_only_when_eligible",
    "crates/splot-recon/src/transform_params.rs::tests::get_transform_1d_type_rejects_out_of_range_plane_tx_type",
]
commands = [
    "cargo test -p splot-recon transform_params --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/transform_params.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-RESOLVE-2D-TRANSFORM-PARAMS"
name = "Inverse transform 2D parameter resolve helper"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/inverse_transform_2d_outer.rs"
change_id = "recon-resolve-2d-transform-params"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `InverseTransform2dOuter::resolve(plane_tx_type, log2_width, log2_height, use_ddt, lossless, bit_depth, dpcm) -> Result<Self>` to the `splot-recon` `inverse_transform_2d_outer` module, the combined §7.15.4 transform-parameter resolve helper that the RECON-TRANSFORM-SHIFT-LOOKUP and RECON-GET-TRANSFORM-1D-TYPE rows left for a caller to compose. It derives `row_shift` and `col_shift` from `Transform_Shift[txSz]` via `transform_shift(log2_width, log2_height)`, derives `row_type` and `col_type` from `get_transform_1d_type` over the per-pass adjusted sample sizes `w = 1 << Min(log2_width, 5)` and `h = 1 << Min(log2_height, 5)` (exactly the §7.15.4.1 `rowType = get_transform_1d_type(0, w)` / `colType = get_transform_1d_type(1, h)`), and derives `plane_tx_type_is_idtx` from `PlaneTxType == IDTX (9)`. Resolving all of these from one `(plane_tx_type, log2_width, log2_height)` source the result also stores avoids the dual-source hazard of computing the shifts and types from one `txSz` and the dimensions from another. Total and panic-free: the shape is validated by `transform_shift` before any adjusted-size arithmetic and the type by `get_transform_1d_type`, so a rejected call resolves no partial parameters; reuses the existing `ReconError::InvalidTransformShiftShape` and `ReconError::InvalidPlaneTxType` variants with no new error. Strictly additive and provably no-output-change: a `const fn` constructor plus an IDTX constant and an adjusted-log2 cap helper, with no rewiring of any decode path, so the minimal flat-intra fixture snapshots stay byte-identical. This row does not implement the DPCM-direction selection from `YMode`/`UVMode`, any wiring into the runtime decode path, the §7.15.3 secondary transform, or the coefficient entropy decode that produces `Quant`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::resolve_wires_the_shift_and_type_helpers_with_the_right_arguments",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::resolve_applies_ddt_substitution_per_pass_on_the_adjusted_size",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::resolve_produces_params_that_drive_the_outer_transform",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::resolve_rejects_invalid_shape_and_plane_tx_type_without_partial_state",
    "crates/splot-recon/src/inverse_transform_2d_outer.rs::tests::resolve_is_total_for_every_valid_shape_and_tx_type",
]
commands = [
    "cargo test -p splot-recon inverse_transform_2d_outer --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/inverse_transform_2d_outer.rs",
    "crates/splot-recon/src/transform_params.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-DPCM-DIRECTION"
name = "Inverse transform DPCM direction selection"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-recon"
module = "crates/splot-recon/src/transform_params.rs"
change_id = "recon-dpcm-direction"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `dpcm_direction(use_dpcm, mode_is_v_pred) -> Option<DpcmDirection>` to the `splot-recon` `transform_params` module, the last of the §7.15.4 inverse-transform parameter derivations the RECON-INVERSE-TRANSFORM-2D-OUTER row left caller-resolved (after RECON-TRANSFORM-SHIFT-LOOKUP, RECON-GET-TRANSFORM-1D-TYPE, and RECON-RESOLVE-2D-TRANSFORM-PARAMS). §7.15.4 sets `useDpcm = (plane == 0 ? use_dpcm_y : use_dpcm_uv)` and `mode = (plane == 0 ? YMode : UVMode)`; when `useDpcm` is 1 the cumulative sum runs down each column for `V_PRED` (`DpcmDirection::Vertical`) and across each row otherwise (`DpcmDirection::Horizontal`), and is not applied otherwise (`None`). `use_dpcm` and `mode_is_v_pred` are caller-resolved scalars so `splot-recon` holds no frame state or prediction-mode enum, matching the crate-wide caller-resolves-spec-dimensions contract; the result drops into the `dpcm` field of `InverseTransform2dOuter`, which applies the cumulative sum after the inverse transform. Total with no error path (a `const fn` mapping every `(use_dpcm, mode_is_v_pred)` combination to a defined result). Strictly additive and provably no-output-change: a `const fn` with no rewiring, so the minimal flat-intra fixture snapshots stay byte-identical. Completeness is proven by the four-spec-case mapping (three pinned at compile time as `const` spec contracts) and an integration test driving the §7.15.4 outer transform through a lossless IDTX block so the selected `Vertical` direction produces the per-column `1, 2, 3, 4` cumulative sum while `None` leaves the residual flat. The §7.15.4 inverse-transform parameter-derivation surface is now complete. This row does not implement the runtime resolution of `use_dpcm_y` or `use_dpcm_uv` or the prediction mode (caller-resolved from frame and block syntax), any wiring into the runtime decode path, the §7.15.3 secondary transform, or the coefficient entropy decode that produces `Quant`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/transform_params.rs::tests::dpcm_direction_maps_the_four_spec_cases",
    "crates/splot-recon/src/transform_params.rs::tests::selected_direction_drives_the_outer_cumulative_sum",
]
commands = [
    "cargo test -p splot-recon transform_params --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/transform_params.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-COEFFICIENT-SCAN-ORDER"
name = "Coefficient scan order get_scan"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.30"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
]
crate = "splot-recon"
module = "crates/splot-recon/src/coefficient_scan.rs"
change_id = "recon-coefficient-scan-order"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `coefficient_scan_order(w, h, class, out)` to a new `splot-recon` `coefficient_scan` module, implementing the AV2 §5.20.7.30 `get_scan(txSz, txClass)` function. It writes the coefficient scan order (each `out[c]` is the flattened `y*w + x` position of the c-th scanned coefficient) for the three transform classes: `TX_CLASS_VERT` -> row-major raster (identity), `TX_CLASS_HORIZ` -> column-major (transpose), and `TX_CLASS_2D` -> the anti-diagonal scan (each anti-diagonal `x+y` traversed from high-y/low-x to low-y/high-x), translated verbatim from the spec algorithm (the 2D branch uses signed `i32` x/y to mirror the spec `y -= 1` / `y < 0` logic that underflows in `usize`). The block shape is caller-resolved (`w = Min(Tx_Width[txSz], 32)` / `h = Min(Tx_Height[txSz], 32)`, each 4/8/16/32), consistent with the §7.15 transforms, since `splot-recon` cannot reach `splot-core`'s §9.2 tables; positions never exceed `w*h-1 <= 1023` so `out` is `&mut [u16]`. A new `TransformClass` enum names the spec `txClass`; typed `ReconError::InvalidScanShape` / `ReconError::ScanLengthMismatch` reject bad shapes/lengths (total, no panic). This is a self-contained pure prerequisite for the §5.20.7 coefficient decode loop and the §7.14.4 coefficient placement; it is not yet wired into a decode path (no caller exists), so it is no-output-change. It does not implement `get_tx_class` (PlaneTxType -> txClass), the coefficient decode loop, the §7.15.3 secondary-transform `Stx_Scan`, or any runtime decode output."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/coefficient_scan.rs::tests::two_d_4x4_matches_the_spec_anti_diagonal_scan",
    "crates/splot-recon/src/coefficient_scan.rs::tests::vertical_scan_is_row_major_identity",
    "crates/splot-recon/src/coefficient_scan.rs::tests::horizontal_scan_is_column_major_transpose",
    "crates/splot-recon/src/coefficient_scan.rs::tests::every_shape_and_class_is_a_valid_permutation",
    "crates/splot-recon/src/coefficient_scan.rs::tests::rejects_invalid_shape_and_length",
]
commands = [
    "cargo test -p splot-recon coefficient_scan --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/coefficient_scan.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "RECON-GET-TX-CLASS"
name = "Transform class get_tx_class"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-recon"
module = "crates/splot-recon/src/coefficient_scan.rs"
change_id = "recon-get-tx-class"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds `tx_class(plane_tx_type) -> TransformClass` to the `splot-recon` `coefficient_scan` module, implementing the AV2 `get_tx_class(txType)` mapping (defined in §8.3.2, `08-parsing-process.md` lines 1549-1560): the vertical-only transforms `V_DCT`/`V_ADST`/`V_FLIPADST` (TX_TYPE 10/12/14) map to `TransformClass::Vertical`, the horizontal-only transforms `H_DCT`/`H_ADST`/`H_FLIPADST` (11/13/15) to `TransformClass::Horizontal`, and every other transform (`DCT_DCT`..`IDTX`, 0..9) to `TransformClass::TwoD` via the spec `else` branch. The TX_TYPE values are taken from `03-symbols.md`. It is a total `const fn` (the spec `else` maps any non-directional value, including out-of-range, to `TX_CLASS_2D`, so no error path is needed), and its result selects the scan in `coefficient_scan_order` (the §5.20.7.30 `get_scan`) and the coefficient base/br contexts. Self-contained pure prerequisite for the coefficient decode loop; not yet wired into a decode path (no caller), so no-output-change. It does not implement the coefficient decode loop, the `coeff_base`/`coeff_br` context derivations that consume the class, or any runtime decode output."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/coefficient_scan.rs::tests::tx_class_maps_every_plane_tx_type",
]
commands = [
    "cargo test -p splot-recon coefficient_scan --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-recon/src/coefficient_scan.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "DOC-DECODED-FRAME-PLANE-MODEL-CONTRACT"
name = "Decoded frame and plane model contract documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "decoded-frame-plane-model-contract"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Documents the future `DecodedFrame`, `Plane<T>`, `PixelFormat`, and `BitDepth` semantic contract before runtime frame/plane types exist. The contract separates cropped §7.21 output frames from padded §7.23 reference storage, requires explicit storage dimensions and `stride_samples`, records a splot-owned zero-based output emission index, excludes padding from hashes/Y4M/fixtures, requires checked sample and byte allocation arithmetic, charges full backing allocations against limits, requires immutable emitted output via immutable sharing, owned copies, or copy-on-write before mutation, and does not claim normative runtime implementation coverage."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = ["cargo xtask check-decoder-support"]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml"]
diagnostics = []

[[feature]]
id = "DOC-MINIMAL-DECODE-TIER-CONTRACT"
name = "Minimal decode tier contract documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "minimal-decode-tier-contract"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Documents the repository-owned `splot.decode.minimal_tier` v1 and `minimal-intra-8bit420-hash-v1` contract. The contract is a splot implementation subset, not an Annex A decoder conformance claim, and covers a committed IVF/DKIF-wrapped AV02 frame with Annex B length-delimited OBU payloads, single-layer selection, `seq_profile_idc == 0` Main 8-bit 4:2:0, `obu_type == OBU_CLOSED_LOOP_KEY` output, no crop, one tile, hash/raw/Y4M success, and explicit unsupported-feature or resource-limit rejection outside the tier. Raw Annex B shares this runtime success tier for hash/raw output and for Y4M when the caller provides a frame rate. The first hash runtime implementation is tracked by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`, raw output by `DECODE-MINIMAL-RAW-RUNTIME-OUTPUT`, and Y4M output by `DECODE-Y4M-RUNTIME-OUTPUT`; broad tile/CDF/intra/reconstruction/reference-refresh support remains partial or unsupported."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = ["cargo xtask check-decoder-support"]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml"]
diagnostics = []

[[feature]]
id = "DECODE-MINIMAL-TIER-RUNTIME-SUCCESS"
name = "Minimal-tier hash success"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.18.2", "5.19", "5.20.1", "5.20.3.1", "5.20.3.2", "5.17.12", "6.2", "6.4.1", "6.16.13", "6.17.2", "6.17.4.1", "6.17.7.2", "6.18", "6.19.1", "7.1", "7.21.1", "7.21.2", "8.2.2", "8.2.4", "8.2.6", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/output/hash.rs"
change_id = "decode-minimal-tier-runtime-success"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Current runtime success path for supported source-backed AV2 streams. Tiles enter `GeneralIntraMultiblockCursor` and its live `GeneralIntraPartitionTreeCursor`, which walks every supported superblock and partition leaf with one §8.2 `SymbolDecoder`, mutates tile CDF rows and MI/mode state as syntax is decoded and published, and follows SDP luma/chroma continuation with tree-type-specific loop-restoration plane ranges. Decoded leaf commands feed reconstruction. `GeneralIntraPartitionTreeOutput` carries active `WienerNsLrSourceBlock` records and unit-coded filters into `FrameFilterRecords`, and final filtering consumes those records during Wiener NS loop-restoration reconstruction. Supported fixtures produce hash, raw, and Y4M output with the reference evidence listed for their feature rows. The historical `DECODE-INACTIVE-LR-UNITS-FRONTIER` aggregate-only activity and `DECODE-LR-UNIT-SELECTIONS-FRONTIER` standalone selection milestones are superseded and are not current runtime state. The frozen `base_q_idx == 255` hand-traced path remains for its narrow supported syntax, while committed conformant fixtures use the general intra path. This row does not claim complete AV2 or Annex A conformance, every prediction and transform tool, or every output bit depth."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::tests::empty_ivf_decodes_to_empty_frame_set",
    "crates/splot-decode/src/output/hash.rs::tests::empty_ivf_decodes_to_deterministic_empty_hash_report",
    "crates/splot-decode/src/output/hash.rs::tests::truncated_empty_ivf_fails_as_malformed_source",
    "crates/splot-decode/src/output/hash.rs::tests::standalone_open_loop_key_matches_expected_output_hash",
    "crates/splot-decode/src/output/hash.rs::tests::minimal_fixture_decodes_to_hash_report",
    "crates/splot-decode/src/output/hash.rs::tests::zero_ivf_timebase_does_not_block_hash_output",
    "crates/splot-decode/src/output/hash.rs::tests::hash_report_records_resolved_thread_count",
    "crates/splot-decode/src/output/hash.rs::tests::raw_annex_b_payload_decodes_to_hash_report",
    "crates/splot-decode/src/output/hash.rs::tests::tile_trace_mismatch_fails_closed_as_unsupported",
    "crates/splot-decode/src/output/hash.rs::tests::tile_payload_byte_mutations_return_typed_results",
    "crates/splot-decode/src/output/hash.rs::tests::long_hash_decode_keeps_reference_storage_bounded",
    "crates/splot-decode/src/output/hash.rs::tests::decoded_hash_is_deterministic_across_thread_policies",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_json_success_for_minimal_fixture",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_json_success_creates_no_implicit_output_file",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_json_success_leaves_existing_output_path_untouched",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_json_success_hashes_are_thread_deterministic",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_empty_ivf_emits_success_report_without_frames",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask conformance",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "tests/conformance/vectors/valid/syn-empty-avmenc-64x64.ivf",
    "tests/conformance/manifest.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit", "decode/malformed-source"]

[[feature]]
id = "DECODE-LEADING-OBU-GATE"
name = "leading payload gate"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.18.2"]
sources = [
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-leading-obu-gate"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical leading-payload gate that advanced the local decoder mission past its former IVF-payload shape rejection. At this milestone, the runtime required `[OBU_TEMPORAL_DELIMITER, OBU_SEQUENCE_HEADER, OBU_CLOSED_LOOP_KEY]`, validated the leading sequence header, and exposed `unsupported_bit_depth` for the profile-0 10-bit 4:2:0 mission sequence. Follow-on frontier rows parsed the frame-level Wiener NS bank and LR-unit syntax; the current `DECODE-TILE-PARTITION-TRAVERSAL-BOUNDARY` supersedes the former aggregate-activity and standalone-selection milestones. This row did not implement 10-bit decode, extra frame units in the leading IVF payload, or additional prediction, filtering, residual, or output behavior."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_runtime_rejects_state_obu_before_following_inter_candidate",
]
commands = [
    "cargo test -p splot-decode prediction::inter --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-3frame-multiref-64x64.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-LUMA-TXTYPE-RESIDUAL-HANDOFF"
name = "luma transform-type residual handoff"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.3.1", "5.20.7.27", "5.20.8.2", "5.20.8.3", "7.20.4", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-luma-txtype-residual-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical luma transform-type handoff milestone now used directly by the generic residual and reconstruction path. The decoder resolves active §5.20.8.2/§5.20.8.3 syntax to `plane_tx_type`, uses its transform class for §5.20.7.30 scan order and §8.3.2 coefficient contexts, carries block-local TCQ/FSC facts, and invokes the matching inverse transform before residual addition. The current FSC branch derives IDTX and reconstructs supported FSC blocks, including committed 8-bit and 10-bit must-pass fixtures. Tx-skip records reuse the same decoded block facts, and unsupported transform shapes or tool combinations remain operation-specific fail-closed cases."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::luma_txtype_residual_lr_handoff_retains_non_dct_luma_tx_type",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::luma_txtype_residual_lr_handoff_skips_intra_ist_for_non_sec_tx_type",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::luma_txtype_residual_adst_adst_uses_reduced_ist_eob_limit",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::fsc_idtx_block_reconstructs_without_tcq_dequant_shift",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode tile_payload::general_intra_residual --locked",
    "cargo test -p splot-decode filters::wienerns_lr::tx_records --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-lr-switchable-768x256-8bit.ivf",
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/filters/wienerns_lr/diagnostics.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-10BIT-SEQUENCE-FRONTIER"
name = "10-bit sequence frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "7.21.2", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-10bit-sequence-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical frontier for the former 8-bit-only pre-decode boundary. AV2 §6.4.1 Table 6.3 maps `bit_depth_idc == 0` to a conformant 10-bit sequence, so this milestone moved rejection out of sequence validation and into the now-removed `ensure_8bit_runtime_storage` gate; its claim that no 10-bit stream decoded successfully described that earlier state only. The unified runtime now dispatches `BitDepthIdc::Eight` and `BitDepthIdc::Ten` through typed `u8`/`u16` decode scratch, `CurrentFrameWorkspace`/`DecodedFrame` storage, reference stores, output serialization, and the generic final-filter pipeline, superseding the old gate for supported 8- and 10-bit streams. The existing `cargo test -p splot-decode prediction::inter --locked` proof below includes `ten_bit_flex_mvres_inter_fixture_decodes_avm_bit_exact`, which successfully decodes typed 10-bit key and inter frames to AVM-pinned hashes. Unsupported 10-bit syntax and tool combinations continue to fail closed at their current operation-specific boundaries."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode prediction::inter --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-2frame-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-3frame-multiref-64x64.ivf",
    "local decoder mission fixture",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-SEQUENCE-CHROMA-FRONTIER"
name = "sequence chroma tool frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.4.5", "5.20.5.6"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-sequence-chroma-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical frontier for the former coarse sequence-level CfL/MHCCP gate. AV2 §5.4.5 carries `enable_cfl_intra` and `enable_mhccp`, while §5.20.5.6 reaches the corresponding mode-info syntax only when block conditions select those tools. The retired `ensure_sequence_chroma_tools_before_tile_decode` gate rejected a stream merely because either sequence capability flag was set; the unified `decode_frame` path supersedes that pre-tile veto by carrying the flags into block parsing and reconstruction. Supported CfL/MHCCP operations now run on the current path, while unsupported geometry fails closed at the consuming operation, such as `general_intra_mhccp_reference_geometry`. The existing `cargo test -p splot-decode prediction::chroma --locked` proof below covers the current per-operation CfL/MHCCP implementation and fail-closed cases. This row records the retired boundary rather than claiming complete CfL/MHCCP coverage."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode prediction::chroma --locked",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-intra-64x64-10bit.ivf",
    "tests/conformance/vectors/valid/syn-2frame-inter-64x64.ivf",
    "local decoder mission fixture",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-WIENERNS-FRONTIER"
name = "Wiener NS frame-header frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.7.11", "5.20.10.6"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-11",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters.rs"
change_id = "decode-wienerns-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Stable historical row for the retired parser-only Wiener NS key-frame boundary. AV2 §5.18.7.11 `lr_params()` now returns one complete `LrParams` value: it parses the frame-level Wiener bank when coded, or copies it only when the selected retained reference bank exists. Malformed or truncated syntax returns a typed parse error; when a locally coded class selects a retained reference filter whose taps are unavailable, the inter frame-header path preserves the facts parsed before LR and stops with `UnsupportedUntilFeature`. A temporal copy with no available active bank remains absent so downstream consumers fail closed when the operation requires it. The obsolete partial LR model, `StoppedBeforeWienerNsFilter` status, and `unsupported_wienerns_filter` lifecycle fallback are deleted. Parsed frame banks and LR-unit records feed the live `WienerNsLrReconSink` final-filter chain, which filters reconstructed frames and publishes typed `u8` or `u16` output. The committed Wiener NS oracle fixtures decode successfully and match AVM; downstream unsupported tool combinations remain operation-specific boundaries tracked by their own rows."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/restoration.rs::tests::lr_frame_filters_on_parses_wienerns_bank",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_frame_filters_on_parses_wienerns_bank",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_inter_missing_lr_reference_taps_is_a_coverage_stop",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier_tests.rs::temporal_frame_wiener_ns_without_local_bank_keeps_lr_unit_syntax_enabled",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::owned_multi_stripe_10bit_filter_matches_monolithic_and_publishes_contiguously",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-core lr_frame_filters_on_parses_wienerns_bank --locked",
    "cargo test -p splot-core frame_header_core_inter_missing_lr_reference_taps_is_a_coverage_stop --locked",
    "cargo test -p splot-decode wiener --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-wienerns-intra-128x128.ivf",
    "tests/conformance/vectors/valid/syn-wienerns-422-intra-128x128.ivf",
    "tests/conformance/vectors/valid/syn-wienerns-444-intra-128x128.ivf",
    "tests/conformance/vectors/valid/syn-2frame-lr-switchable-768x256-8bit.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-WIENERNS-BANK-FRONTIER"
name = "Wiener NS frame-filter bank frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.7.11", "5.20.10.6"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-11",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
]
crate = "splot-core"
module = "crates/splot-core/src/headers/frame/restoration/wienerns.rs"
change_id = "decode-wienerns-bank-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical fail-closed support row for parsing and preserving the AV2 §5.20.10.6 fixed-coded frame-level Wiener NS bank reached from §5.18.7.11 `lr_params()`. At that milestone the parser stored each signalled plane's `WienerNsFrameFilterBank` on `LrPlaneParams::frame_filter_bank`, including class matches, merge flags, subsets, chroma symmetry, and tap coefficients; fixed-coded luma PC-Wiener match translation used the generated table. `DECODE-LR-UNIT-SYNTAX-FRONTIER` superseded the parser-only milestone by consuming LR-unit syntax, and the former aggregate-activity and standalone-selection stages were superseded by the current tile traversal. `DECODE-LR-SOURCE-READ-FRONTIER` is also historical and superseded: the generic `WienerNsLrReconSink::into_filtered_frame` / `into_filtered_frame_from_deblocked` path now consumes retained filter records, materializes bounded current-frame or CDEF-frame source windows in `final_filters::LrSourceWindow`, and publishes each filtered block through its exact output rectangle. The writer still rejects serialized models carrying a frame-filter bank; this row claims the historical parser milestone, while current reconstruction, filtering, bit-depth, and output support is documented by active decoder rows."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/restoration/wienerns.rs::tests::luma_two_class_merged_bank_shape",
    "crates/splot-core/src/headers/frame/restoration/wienerns.rs::tests::luma_nonmerged_subset_zero_reads_present_taps",
    "crates/splot-core/src/headers/frame/restoration/wienerns.rs::tests::eof_inside_frame_bank_is_structured_error",
    "crates/splot-core/src/headers/frame/restoration.rs::tests::lr_frame_filters_on_parses_wienerns_bank",
    "crates/splot-core/src/headers/frame/info.rs::tests::frame_header_core_frame_filters_on_parses_wienerns_bank",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-core wiener --locked",
    "cargo test -p splot-decode wienerns --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-core/src/headers/frame/restoration.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-LR-UNIT-SYNTAX-FRONTIER"
name = "Loop-restoration unit syntax frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "7.20.1", "7.20.3", "7.20.4", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-lr-unit-syntax-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current AV2 §5.20.10.4-.6 handling for fixed Wiener NS and PC-Wiener plus luma `RESTORE_SWITCHABLE`, with or without frame-level Wiener filters. The generated FlexRestorationType CDF rows resolve every switchable unit transiently in tool order. Active PC-Wiener and Wiener NS outcomes become §7.20 source records; a Wiener NS outcome without a frame-level bank also becomes a unit-filter record. Inactive None outcomes are not recorded. The live cursor applies LR syntax over Shared `[0, NumPlanes)`, Luma `[0, 1)`, and Chroma `[1, NumPlanes)` SDP tree ranges, and final filtering dispatches active luma runs from a shared pre-restoration snapshot. The 32-unit reference stream resolves to 25 Wiener NS, 5 None, and 2 PC-Wiener outcomes and emits byte-identical output. Chroma `RESTORE_SWITCHABLE` remains explicitly fail-closed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_cdf_subset_copies_generated_defaults_without_aliasing",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::reads_supported_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::update_mode_controls_only_selected_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::frame_level_lr_symbol_precedes_partition_read",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::inactive_frame_level_wiener_ns_advances_current_cursor_without_source_output",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_advances_every_covered_inactive_wiener_ns_unit",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::frame_level_wiener_ns_rejects_invalid_unit_size_before_partition_read",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::switchable_luma_selection_controls_current_source_output",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::switchable_luma_without_frame_filter_reads_unit_wiener_filter",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::switchable_lr_rejects_chroma_before_symbol_read",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier_tests.rs::switchable_luma_without_frame_filter_keeps_lr_unit_syntax_enabled",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::keeps_switchable_restoration_types_in_separate_runs",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::switchable_luma_dispatches_mixed_units_from_one_snapshot",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode partition_traversal --locked",
    "cargo test -p splot-decode wienerns --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output -- --ignored",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-lr-switchable-unit-wienerns-128x64-q100.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-INACTIVE-LR-UNITS-FRONTIER"
name = "inactive Wiener NS LR units frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-inactive-lr-units-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical aggregate-activity milestone superseded by the current LR-unit cursor and generic final-filter path. The selector resolves each supported unit transiently: `RESTORE_NONE` advances without a downstream record, while active PC-Wiener or Wiener NS selections produce bounded `WienerNsLrSourceBlock` records and, when required, unit-filter records. Those active records flow directly through source derivation, tx-skip lookup, classification, filtering, and typed frame publication; no aggregate activity summary or handoff-specific diagnostic owns the live path. Current tests cover inactive omission, active retention, SDP plane ranges, switchable selection, resource limits, and successful filtered output."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::inactive_frame_level_wiener_ns_advances_current_cursor_without_source_output",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_advances_every_covered_inactive_wiener_ns_unit",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_honors_zero_step_limit_before_lr_symbol",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_sdp_chroma_lr_source_blocks",
]
commands = [
    "cargo test -p splot-decode partition_traversal --locked",
    "cargo test -p splot-decode wienerns --locked",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-UNIT-SELECTIONS-FRONTIER"
name = "Wiener NS LR unit selection frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-lr-unit-selections-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical per-unit selection stage for the proven frame-level Wiener NS LR syntax. The source-bounds stage superseded the standalone selection list: the live partition-tree cursor carries active unit type and coordinates directly in WienerNsLrSourceBlock, while inactive units leave no downstream record. The obsolete selection-only root summary was removed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::inactive_frame_level_wiener_ns_advances_current_cursor_without_source_output",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_advances_every_covered_inactive_wiener_ns_unit",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode partition_traversal --locked",
    "cargo test -p splot-decode wienerns --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-SOURCE-BOUNDS-FRONTIER"
name = "Wiener NS LR source-bounds frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "7.20.1", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-lr-source-bounds-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical local-decoder-mission frontier for retaining active Wiener NS LR source-bound facts. Its standalone frontier role is superseded by the generic bounded-source/final-filter path tracked by `DECODE-LR-SOURCE-READ-FRONTIER`: live traversal retains `WienerNsLrSourceBlock` entries with plane, selected LR-unit coordinates, current-plane block geometry, and tile-local source and stripe bounds; the final-filter sink materializes bounded source windows and publishes typed output for supported active-LR streams. Inactive units are consumed without downstream records. The proof below preserves the source-bound fact coverage, while the successor row carries current final-filter coverage."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::wiener_ns_unit_filter_reads_use_bank_for_merged_units_after_bank_growth",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::active_lr_source_blocks_track_stripe_bounds",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::active_lr_source_bounds_clamp_to_tile_when_loopfilters_across_tiles_disabled",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_cdf_subset_copies_generated_defaults_without_aliasing",
]
commands = [
    "cargo test -p splot-decode wiener_ns_unit_filter_reads_use_bank_for_merged_units_after_bank_growth --locked",
    "cargo test -p splot-decode partition_traversal --locked",
    "cargo test -p splot-decode frame_cdf_subset_copies_generated_defaults_without_aliasing --locked",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
]
diagnostics = ["decode/resource-limit"]

[[feature]]
id = "DECODE-LR-SOURCE-READ-FRONTIER"
name = "Wiener NS LR source-read frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "7.20.1", "7.20.2", "7.20.3", "7.20.4", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters.rs"
change_id = "decode-lr-source-read-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical source-read frontier now represented by the generic final-filter path. The current path reads bounded LR source windows from CurrentFrameWorkspace, reuses source scratch after errors, writes each block only to its output rectangle, and produces the same predeblocked tail as the combined final-filter path. Deleted dedicated frontier tests are not current evidence."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::lr_source_window_reuses_storage_after_an_error",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::lr_block_output_lands_in_its_rectangle_for_both_storage_widths",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::predeblocked_filter_tail_matches_the_combined_path",
]
commands = [
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode lr_source_window_reuses_storage_after_an_error --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-recon/src/loop_restoration.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-CLASSIFIED-WIENER-FRONTIER"
name = "Wiener NS LR classified-Wiener frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.10.4", "5.20.10.5", "5.20.10.6", "7.20.1", "7.20.2", "7.20.3", "7.20.4", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-recon/src/pc_wiener.rs"
change_id = "decode-lr-classified-wiener-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical coordinate-only frontier superseded by the current source-backed PC-Wiener classifier. Retained source-block bounds feed current 10-bit feature accumulation and clipped tx-skip lookup; current PC-Wiener tests cover feature accumulation and tile, block, and stripe clipping. Deleted dedicated frontier tests are not current evidence."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-recon/src/pc_wiener/tests.rs::quadratic_ten_bit_source_accumulates_features_and_tx_skip",
    "crates/splot-recon/src/pc_wiener/tests.rs::tx_skip_lookup_clips_to_block_stripe_and_tile_bounds",
]
commands = [
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-recon quadratic_ten_bit_source_accumulates_features_and_tx_skip --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-recon/src/loop_restoration.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-TX-SKIP-GRID-RETENTION"
name = "Wiener NS LR tx-skip grid-retention frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.24", "5.20.7.27", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-24",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr.rs"
change_id = "decode-lr-tx-skip-grid-retention"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current value-backed LrTxSkip retention. The decoder derives each bounded luma grid cell from retained transform records with the AV2 §5.20.7.24 skip_flag || (eob == 0) rule, fills transform extents, and rejects incomplete, out-of-range, or conflicting records without fabricating defaults. Current tx-record grid tests cover value preservation, clamped edges, bounds rejection, and selectable-record handoff."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::tx_skip_grid_retention_preserves_skip_flag_for_nonzero_eob_record",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::tx_skip_grid_retention_clamps_bottom_edge_overhang",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::tx_skip_grid_retention_rejects_out_of_frame_origin",
]
commands = [
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode tx_skip_grid_retention --locked",
]
fixtures = [
    "crates/splot-decode/src/filters/wienerns_lr.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-LR-RUNTIME-STORAGE-RETENTION"
name = "Wiener NS LR runtime storage-retention frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "6.17.4.1", "7.20.1", "7.20.2", "7.20.3", "7.20.4", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon.rs"
change_id = "decode-lr-runtime-storage-retention"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical helper-planning milestone. The private shell and preflight tree was deleted; current runtime retention is owned by CurrentFrameWorkspace plus bounded per-worker LR and source scratch in the generic final-filter path. Current tests prove worker scratch remains bounded and oversized source buffers are not retained."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::wiener_ns_luma_worker_scratch_retention_is_bounded",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::lr_source_scratch_does_not_retain_oversized_buffers",
]
commands = [
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode wiener_ns_luma_worker_scratch_retention_is_bounded --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-LIVE-STORAGE-ALLOCATION"
name = "Wiener NS LR live storage-allocation frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "6.17.4.1", "7.20.2", "7.20.3", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon.rs"
change_id = "decode-lr-live-storage-allocation"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical shell-allocation milestone superseded by direct CurrentFrameWorkspace ownership and the generic owned final-filter path. Current tests cover workspace setup before deblock records move and bit-exact multi-stripe 10-bit filtering; no private unpopulated Option shell is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::owned_setup_derives_lossless_grid_before_deblock_records_move",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::owned_multi_stripe_10bit_filter_matches_monolithic_and_publishes_contiguously",
]
commands = [
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode owned_multi_stripe_10bit_filter_matches_monolithic_and_publishes_contiguously --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-LR-LIVE-TX-SKIP-GRID"
name = "Wiener NS LR live tx-skip grid frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.24", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-24",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr.rs"
change_id = "decode-lr-live-tx-skip-grid"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Retired helper-only row. The live-storage shell tree was deleted in favor of the generic decode path retaining transform facts directly; this row no longer tracks active implementation. DECODE-LR-TX-SKIP-GRID-RETENTION remains the durable value-backed LrTxSkip grid model."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "not-applicable"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = []
fixtures = []
diagnostics = []

[[feature]]
id = "DECODE-LR-LIVE-TRANSFORM-RECORD-HANDOFF"
name = "Wiener NS LR live transform-record handoff frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.6.1", "5.20.7.27", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr.rs"
change_id = "decode-lr-live-transform-record-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical fixed-largest handoff milestone superseded by generic transform-record retention. Current residual decode appends skip and EOB transform records, reconstruction derives the bounded tx-skip grid, and the current selectable-record test proves grid population. No deleted live-storage shell is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_records_populate_live_tx_skip_grid",
]
commands = [
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode selectable_tx_records_populate_live_tx_skip_grid --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-SELECTABLE-TRANSFORM-RECORDS"
name = "selectable transform-record frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = [
    "5.4.8",
    "5.20.5.3",
    "5.20.5.4",
    "5.20.6.1",
    "5.20.6.3",
    "5.20.7.13",
    "5.20.7.20",
    "5.20.7.24",
    "5.20.7.25",
    "5.20.7.27",
    "5.20.7.30",
    "5.20.8.2",
    "6.4.8",
    "6.19.2.1",
    "6.19.6.3",
    "6.19.7.12",
    "7.12.2",
    "7.13.3.18",
    "7.20.4",
    "8.3.2",
]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-8",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-13",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-20",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-24",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-8",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-6-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-7-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs"
change_id = "decode-intra-recon-bridge"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Partial current-path support for IntrABC mode-info and TX_MODE_SELECT luma transform records. The duplicated inter §5.20.6.3 reader independently uses SequenceHeader.reduced_tx_part_set for the full/reduced TxPartitionType CDF and one-axis tx_2or3 suppression; FrameHeader.reduced_tx_set remains a transform-type restriction. Writer tests prove both CDFs, suppression, flag independence, and offset-aware §6.19.6.3 malformed classification for entropy-valid narrow four/five-way geometry. The pinned 104-byte two-frame witness reaches only the inter reader and fails exact base at terminal entropy validation; fixed splot at one and eight threads matches pinned/current AVM raw SHA-256 9e9707974cbbb48461ae6e9e4e512918ff5057e2d3ed068c862f67a5881074d3. Its byte-distinct full-set control decodes on exact base with identical output. Official AVMenc sweeps did not isolate one-axis syntax, so that branch is writer-proven. Unsupported transform or reconstruction subsets still fail closed."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::inter_partition_reader_uses_sequence_selected_reduced_cdf",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::inter_reduced_partition_set_suppresses_one_axis_four_way_symbol",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::writer_produced_inter_narrow_four_and_five_way_partitions_are_malformed",
    "crates/splot-decode/src/output/hash_tests.rs::reduced_tx_partition_fixtures_match_reference_output_hashes_at_one_and_eight_threads",
    "crates/splot-decode/src/output/hash_tests.rs::reduced_tx_partition_fixtures_eof_fail_closed",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_grid_records_observed_luma_only_block_8x32_region",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_grid_rejects_empty_transform_dimensions",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs::tests::tx_skip_grid_retention_preserves_skip_flag_for_nonzero_eob_record",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::active_intrabc_nearmv_skip_reads_use_skip_mode_and_drl_then_advances",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_newmv_geometry_derives_integer_luma_copy_rectangles",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_nearmv_geometry_derives_integer_luma_copy_rectangles",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_derives_bilinear_fractional_luma_prediction_region",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_rejects_source_outside_current_tile",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_uses_mi_domain_for_partial_edge_frame",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_rejects_out_of_frame_source",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_rejects_out_of_frame_target",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_geometry_rejects_missing_frame_size",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::intrabc_newmv_one_pel_record_shifts_shell_delta",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::non_intrabc_path_reads_only_use_intrabc_symbol",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs::tests::contexts_use_intrabc_npos_and_skip_nposbuf_boundaries",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::active_fsc_mode_metadata_is_retained",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::fsc_mode_luma_transform_handoff_derives_idtx_without_luma_context",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::non_fsc_luma_transform_handoff_still_requires_luma_context",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::nonzero_eob_covers_direct_refined_and_max_values",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_records_populate_live_tx_skip_grid",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode filters::wienerns_lr::tx_records --locked",
    "cargo test -p splot-decode intrabc_records --locked",
    "cargo test -p splot-decode --tests active_fsc_mode_metadata_is_retained --locked",
    "cargo test -p splot-decode --tests luma_transform_handoff --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo test -p splot-decode filters::wienerns_lr::recon::final_filters::tests --locked",
    "cargo test -p splot-decode intra_joint_modes --locked",
    "cargo test -p splot-decode coeff_loop --locked",
    "cargo test -p splot-decode general_intra_residual --locked",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-reduced-txpart-inter-64x64-q160.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/filters/wienerns_lr/recon.rs",
    "crates/splot-decode/src/filters/wienerns_lr/intrabc_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit", "decode/malformed-source"]

[[feature]]
id = "DECODE-SELECTABLE-NARROW-LUMA-RECORDS"
name = "selectable narrow luma records"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.6.1", "5.20.6.3", "5.20.7.24", "5.20.7.27", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-24",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs"
change_id = "decode-selectable-narrow-record-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Partial current-path support for admitted luma-only narrow selectable transforms. Current tx-record grid tests cover 8x32 and 4x32 records, empty-geometry rejection, and completeness for clamped edge regions. Chroma-bearing or otherwise unsupported shapes remain fail-closed."

[feature.status]
mapped = "done"
types = "partial"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_grid_records_observed_luma_only_block_8x32_region",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_grid_records_luma_only_block_4x32_region",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::selectable_tx_grid_rejects_empty_transform_dimensions",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::records_for_region_is_complete_for_clamped_edge_region",
]
commands = [
    "cargo test -p splot-decode filters::wienerns_lr::tx_records --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-CFL-CHROMA-MODE-FRONTIER"
name = "CfL chroma mode frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.5.6", "5.20.7.32", "6.19.7.4", "6.19.7.25", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-32",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-7-4",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-7-25",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs"
change_id = "decode-cfl-chroma-mode-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical CfL syntax-frontier milestone now exercised through the current general-intra path. The decoder consumes AV2 §5.20.5.6 `is_cfl` and `UV_CFL_PRED`, reads §5.20.7.32 alpha syntax with the generated §9.3 CDF rows and §8.3.2 contexts, retains the typed mode through residual parsing, and passes it to the shared CfL reconstruction path for `CflRef` capture, alpha application, and chroma sample output. Committed 4:2:0, 4:2:2, and 4:4:4 must-pass oracle fixtures cover successful CfL decode, including 10-bit 4:2:0 output. Unsupported parameter or geometry combinations fail closed at their current operation-specific boundaries; the removed live-transform frontier diagnostics are no longer reachable evidence."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::active_cfl_chroma_mode_returns_typed_uv_cfl_pred",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::read_cfl_alphas_consumes_explicit_sign_and_alpha_contexts",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::read_cfl_alphas_empty_payload_fails_exit_symbol_validation",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::cfl_alpha_contexts_match_spec_tables",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::cfl_cdf_selectors_match_generated_defaults_and_check_bounds",
]
commands = [
    "cargo test -p splot-decode tile_payload::general_intra_block --locked",
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-CFL-NON-420"
name = "General-intra CfL reconstruction for 4:2:2 and 4:4:4 chroma"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.6", "5.20.7.32", "6.4.5", "7.13.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-32",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/chroma/cfl.rs"
change_id = "decode-general-intra-cfl-non-420"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements the complete legal chroma-format dispatch for the existing CfL reconstruction path. PixelFormat supplies subsampling_x/subsampling_y to coordinate mapping, alpha/reference sampling, superblock reference bounds, and MHCCP reference extents. The §7.13.5 sample filter chooses the existing Cfl_Filters_420 table when both axes are subsampled, Cfl_Filters_422 when only X is subsampled, and 8*v when neither axis is subsampled. The lumaAbove/lumaLeft average and implicit-alpha reference filters clamp negative taps only at the transform-local left/top edge (`i == 0` / `j == 0`); internal absolute 64-luma-sample crossings retain the negative tap. The separate current-block L derivation keeps the §7.13.5 `lumaX % 64` / `lumaY % 64` clamps. At right and bottom frame overhang, one shared coordinate rule repeats the last valid downsampled luma sample, matching the §7.13.5 `prevSample` rule before implicit-alpha correlation rather than independently clamping each luma filter tap. Focused wide-above, symmetric tall-left, and frame-bottom 4:2:0 regressions prove block-edge controls, internal-boundary source samples, frame-edge repetition, and implicit-alpha derivation; the tall case additionally proves the corrected luma average, AC, and final prediction. Two committed AVMenc oracles prove both newly admitted non-4:2:0 branches: syn-cfl-444-intra-64x64-q255.ivf is a minimal 73-byte 4:4:4 CfL stream, while syn-cfl-422-intra-64x64-q128.ivf is an active-CfL 4:2:2 stream. Pinned AVM, current AVM, and fixed splot agree byte-for-byte on each complete native planar raw output."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_above_average_uses_block_left_not_internal_64_pixel_boundaries",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_left_average_uses_block_top_not_internal_64_pixel_boundaries",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_derived_alpha_above_uses_transform_local_boundary",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_luma_sample_repeats_last_downsampled_row_at_frame_edge",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_reconstruction_supports_non_420_chroma_geometry",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::cfl_luma_sample_uses_422_and_444_filters",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_reference_extensions_use_non_420_subsampling",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_reference_extensions_use_luma_pixel_threshold_for_420",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode prediction::chroma::cfl --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-cfl-444-intra-64x64-q255.ivf",
    "tests/conformance/vectors/valid/syn-cfl-422-intra-64x64-q128.ivf",
    "crates/splot-decode/src/prediction/chroma/cfl.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-MHCCP-FRAME-EDGE-REFERENCE-EXTENT"
name = "MHCCP frame-edge reference extent"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.2.1", "7.13.6", "7.13.7"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-7",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/chroma/cfl.rs"
change_id = "decode-mhccp-frame-edge-reference-extent"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Separates the full MHCCP prediction buffer geometry from the captured in-frame CflRef extent at the right and bottom frame edges. AV2 §7.13.2.1 initializes enough CflRef storage for the whole transform prediction but limits CflRefWidth/CflRefHeight to the available coded-frame region; §7.13.7 derives multiParams only from that captured extent. The decoder now retains the full transform-sized zero-initialized buffer required by §7.13.6 prediction, fills and derives parameters from only the captured extent, and continues relying on CurrentFrameWorkspace to drop out-of-frame output samples. The external 1920x1080 10-bit local mission stream's bottom-edge U-plane TX_32X16 MHCCP block at chroma (128,528) advances from `general_intra_mhccp_reference_geometry` at byte 1195349 to a malformed §8.2.4 exit-symbol diagnostic at byte 1242698. Focused tests prove full bottom-edge prediction, ignored uncaptured padding during parameter derivation, retained non-edge behavior, and rejection when the transform origin itself is outside the frame. This row does not claim padded current-frame storage, out-of-frame sample retention, a committed AVMenc fixture, the following compound exit-symbol branch, or full-file decode success."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/mhccp.rs::tests::parameter_derivation_ignores_uncaptured_prediction_padding",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_bottom_edge_keeps_full_prediction_with_clipped_reference_extent",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_rejects_reference_origin_outside_frame",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_reference_extensions_use_luma_pixel_threshold_for_420",
    "crates/splot-decode/src/prediction/chroma/cfl_tests.rs::mhccp_reference_extensions_use_non_420_subsampling",
]
commands = [
    "cargo test -p splot-recon mhccp --locked",
    "cargo test -p splot-decode --lib 'prediction::chroma::cfl::tests::mhccp_' --locked",
    "cargo xtask check-feature-status",
]
fixtures = []
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-SDP-CFL-ALLOWED-FRONTIER"
name = "SDP CflAllowedInSdp frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.3.1", "5.20.5.3", "5.20.5.6", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-sdp-cfl-allowed-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current partial support for retaining AV2 §5.20.3.1 `CflAllowedInSdp` through SDP partition traversal and applying it to §5.20.5.6 chroma mode-info. The private decode frontier represents a `CHROMA_PART` only together with the collocated §5.20.5.3 luma mode and angle facts: ordinary SDP pushes chroma then luma onto its LIFO stack, extended SDP schedules the chroma continuation below all luma children, and successful ordinary-intra or IntraBC luma publication records the required mode before chroma construction. A missing collocation is a typed internal header-state error, never an unsupported feature or malformed source. `CHROMA_PART` leaves whose state disables CfL/MHCCP omit `is_cfl` before `uv_mode`, while admitted leaves carry the selected chroma mode into the same CfL or MHCCP reconstruction used by the generic path. Focused traversal and mode-info tests protect ordering, publication, failure isolation, and state propagation; unsupported SDP, CfL, or MHCCP combinations continue to fail closed at the consuming operation."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs::tests::sdp_cfl_allowed_state_tracks_top_luma_and_chroma_partitions",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs::tests::sdp_cfl_allowed_state_propagates_to_chroma_children",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal/tree_walk.rs::row_cursor_tests::ordinary_sdp_publishes_luma_mode_before_chroma_frontier",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal/tree_walk.rs::row_cursor_tests::extended_sdp_schedules_luma_children_before_collocated_chroma",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal/tree_walk.rs::row_cursor_tests::sdp_intrabc_publishes_dc_mode_before_chroma_frontier",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal/tree_walk.rs::row_cursor_tests::failed_sdp_luma_leaf_neither_publishes_mode_nor_reaches_chroma",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::partition_walk_failures_keep_typed_boundaries",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::sdp_chroma_part_cfl_disallowed_reads_uv_mode_without_is_cfl",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode sdp_cfl_allowed_state --locked",
    "cargo test -p splot-decode bitstream::tile_payload::partition_traversal::tree_walk::row_cursor_tests --locked",
    "cargo test -p splot-decode tile_payload::general_intra_block::tests::sdp_chroma_part_cfl_disallowed_reads_uv_mode_without_is_cfl --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTRA-PRELUDE-TX-FRONTIER"
name = "intra prelude transform frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.5.3", "5.20.5.11", "5.20.6.1", "5.20.6.3", "5.20.10.1", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-11",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-6-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs"
change_id = "decode-sdp-cfl-allowed-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical prelude-admission milestone now folded into the shared partition, block, residual, reconstruction, and final-filter path. The current decoder consumes each admitted prelude symbol in AV2 §5.20.5.3 order, retains CDEF and delta-Q state, parses §5.20.6 transform partitions, and dispatches supported IntrABC, MRL, DIP, intra-edge, IBP, coefficient, and filter operations to their owning implementations. Reconstructed samples and filter records continue through the generic frame sink and output pipeline. Unsupported tool or geometry combinations fail closed at operation-specific diagnostics rather than a mission-only pre-tile or transform-record frontier."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs::tests::cdef_index0_context_uses_zero_strength_neighbours_in_same_superblock",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs::tests::cdef_fill_units_uses_cdef_aligned_origin_and_block_extent",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
    "cargo test -p splot-decode filters::wienerns_lr::tx_records --locked",
    "cargo test -p splot-decode tx_records::cdef_tests --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/lifecycle.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-ACTIVE-INTRA-TOOL-FRONTIER"
name = "active intra tool frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.5.3", "5.20.5.5", "5.20.7.27", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs"
change_id = "decode-active-mrl-records"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical MRL-metadata frontier now integrated with current prediction and reconstruction. The tile CDF lifecycle exposes the generated MRL index and secondary-index rows; mode-info derives `UsesMrls`, uses retained left/above values for §8.3.2 contexts, and carries the selected indices into the one-sided directional prediction helpers. Transform and tx-skip records remain ordinary outputs of the same decoded blocks, and the generic final-filter/output path consumes the resulting frame. Focused context and prediction tests plus committed MRL must-pass oracle fixtures protect active primary and secondary MRL behavior; unsupported combinations remain operation-specific fail-closed cases."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_cdf_subset_copies_generated_defaults_without_aliasing",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::saved_copy_and_average_are_exact_for_supported_subset",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_end_update_copies_saved_rows_and_scales_counts",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::tx_partition_rows_load_defaults_and_report_selector_errors",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::uses_mrls_neighbours_select_index_and_secondary_contexts",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::directional_luma_mrl_zero_is_consumed_when_mrls_are_enabled",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::active_mrl_metadata_is_retained_after_mrl_sec_index_is_consumed",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::mrl_symbols_use_retained_neighbour_contexts",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode intra_joint_modes --locked",
    "cargo test -p splot-decode tile_payload::general_intra_block --locked",
    "cargo test -p splot-decode filters::wienerns_lr::tx_records --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/lifecycle.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-DCTONLY-RESIDUAL-FRONTIER"
name = "DCT-only residual frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.7.27", "5.20.8.2", "5.20.8.3", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-dctonly-residual-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical DCT-only admission frontier superseded by the shared transform-tool residual path. The decoder threads sequence facts into tile coefficient state, consumes `all_zero` and EOB syntax, resolves the active luma or chroma transform type from the generated tables and CDF rows, then sends FSC or ordinary coefficient output to reconstruction. DCT_DCT remains the forced result for the spec-defined DCT-only cases, while supported non-DCT, CCTX, and IST cases now continue through inverse transforms and sample output. Focused syntax/reconstruction tests and the decoder-oracle corpus protect the current path; unsupported transform combinations still fail closed at their owning operation."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_admits_luma_when_ist_cannot_read_after_eob_limit",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_maps_nonzero_intra_tx_type_to_non_dct",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_maps_intra_tx_type_zero_to_dct_dct",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_long_set_maps_dct_symbol_only_for_long_side_dct",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::tx_partition_rows_load_defaults_and_report_selector_errors",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::reads_supported_block_symbol_rows",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode tile_payload::general_intra_residual --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTRA-IST-ZERO-FRONTIER"
name = "intra IST zero frontier"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.7.29", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-intra-ist-zero-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical zero-IST frontier retained as a syntax/CDF milestone. The tile CDF subset still lifecycle-manages `TileSecTxTypeCdf`, `TileMostProbableStxSetCdf`, and `TileMostProbableStxSetAdstCdf`, and §5.20.7.29 still admits `sec_tx_type == 0` without a secondary transform. Active values now continue through `resolve_secondary_inverse_transform` and the reconstruction kernels rather than a syntax-only LR handoff. Focused CDF and residual tests protect the zero case, while committed IST oracle fixtures protect successful active secondary-transform reconstruction and output."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_cdf_subset_copies_generated_defaults_without_aliasing",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::tx_partition_rows_load_defaults_and_report_selector_errors",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::reads_supported_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::update_mode_controls_only_selected_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_admits_luma_when_intra_ist_reads_zero_sec_tx_type",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_rejects_intra_ist_without_luma_context",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode tile_payload::general_intra_residual --locked",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-ACTIVE-INTRA-IST-HANDOFF"
name = "active intra IST handoff"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.7.27", "5.20.7.29", "7.20.4", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-active-intra-ist-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical active-IST handoff milestone now part of reconstruction. The residual path reads AV2 §5.20.7.29 `sec_tx_type` and `most_probable_stx_set`, stores the typed metadata on `LumaCoeffBlock`, and `resolve_secondary_inverse_transform` derives the §7.15.3 kernel used by `secondary_inverse_transform` before residual addition. Transform records and §7.20.4 tx-skip derivation reuse the same parsed block facts instead of taking a syntax-only side path. Focused syntax and reconstruction tests plus the committed `syn-ist-intra-128x128` and `syn-ist-largest-intra-128x128` fixtures protect successful output; unsupported shapes or contexts remain fail closed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_lr_handoff_admits_active_intra_ist_metadata",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode tile_payload::general_intra_residual --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-CHROMA-CCTX-HANDOFF"
name = "chroma CCTX handoff"
category = "infrastructure"
kind = "bitstream-syntax"
spec_sections = ["5.20.7.27", "5.20.8.3", "7.20.4", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-chroma-cctx-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical CCTX syntax-handoff milestone now integrated with chroma reconstruction. The tile CDF subset lifecycle-manages `TileCctxTypeCdf`; the residual path reads active U-plane §5.20.7.27 `cctx_type`, derives the chroma transform type, and retains paired U/V coefficient blocks. `reconstruct_general_intra_chroma_cctx_pair_into` dequantizes both blocks, applies the cross-chroma matrix, runs the inverse transforms, and adds both residuals to their predictions. Tx-skip derivation reuses the same block facts. Focused syntax and reconstruction tests plus the committed `syn-cctx-444-intra-128x128` fixture protect successful output; unsupported CCTX geometry remains fail closed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_cdf_subset_copies_generated_defaults_without_aliasing",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::reads_supported_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs::tests::update_mode_controls_only_selected_block_symbol_rows",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs::tests::dctonly_residual_lr_handoff_admits_chroma_non_dct_tx_set",
    "crates/splot-cli/tests/decode_cli.rs::local_decoder_mission_reaches_current_runtime_gate_without_output",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode tile_payload::general_intra_residual --locked",
    "cargo test -p splot-cli --test decode_cli local_decoder_mission_reaches_current_runtime_gate_without_output --locked -- --ignored",
    "SPLOT_LOCAL_DECODER_MISSION_IVF=<local mission fixture> cargo run -p splot-cli --locked -- decode --json --output-format hash $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "cargo xtask check-feature-status",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "local decoder mission fixture",
    "crates/splot-cli/tests/decode_cli.rs",
    "crates/splot-decode/src/filters/wienerns_lr.rs",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-Y4M-RUNTIME-OUTPUT"
name = "Minimal-tier Y4M output"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.18.2", "5.19", "5.20.1", "5.20.3.1", "5.20.3.2", "5.17.12", "6.2", "6.4.1", "6.16.13", "6.17.2", "6.17.4.1", "6.17.4.4", "6.17.7.2", "6.18", "6.19.1", "7.1", "7.3.8.10", "7.3.8.11", "7.21.1", "7.21.2", "8.2.2", "8.2.4", "8.2.6", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/output/y4m.rs"
change_id = "decode-y4m-runtime-output"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Extends runtime Y4M output with a typed caller-provided frame-rate override. `DecodeOptions` reuses `splot_recon::Y4mFrameRate`; the output adapter validates IVF timing by default, requires an override for raw Annex B without external timing, and applies an override consistently to every emitted frame. The CLI exposes `--frame-rate NUM:DEN`, rejects malformed or zero components during argument parsing, and rejects non-Y4M use before reading input or touching output. The committed `syn-2seq-compatible-intra-64x64.obu` stream is composed from two unchanged AVMenc frame units; existing reference evidence records pinned AVM, current AVM, and splot agreement on all 12,288 raw bytes. The exact regression wraps those two frames in `F24000:1001` Y4M without changing default `F30:1` IVF behavior. Missing Annex B timing and invalid IVF timing are transactional `decode/output-error` failures unless a valid override is supplied. This row does not claim that AV2 Annex B carries a normative frame rate or invoke AVM at runtime."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/output/y4m.rs::tests::empty_ivf_y4m_fails_with_typed_empty_frame_set_error",
    "crates/splot-decode/src/output/y4m.rs::tests::minimal_fixture_decodes_to_exact_y4m_bytes",
    "crates/splot-decode/src/output/y4m.rs::tests::raw_annex_b_payload_y4m_requires_frame_rate",
    "crates/splot-decode/src/output/y4m.rs::tests::raw_annex_b_payload_decodes_to_exact_y4m_with_frame_rate",
    "crates/splot-decode/src/output/y4m.rs::tests::caller_frame_rate_overrides_ivf_timebase",
    "crates/splot-decode/src/output/y4m.rs::tests::caller_frame_rate_rescues_zero_ivf_timebase",
    "crates/splot-decode/src/output/y4m.rs::tests::y4m_output_is_deterministic_across_thread_policies",
    "crates/splot-decode/src/output/y4m.rs::tests::caller_writer_io_error_maps_to_output_diagnostic",
    "crates/splot-decode/src/output/y4m.rs::tests::zero_ivf_timebase_fails_as_y4m_output_diagnostic_before_writer_success",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_explicit_y4m_success_for_minimal_fixture",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_annex_b_y4m_uses_explicit_frame_rate_and_exact_oracle_payload",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_frame_rate_rejects_invalid_values_before_reading_input",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_frame_rate_rejects_non_y4m_output_before_reading_input",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_implicit_y4m_success_matches_explicit_output",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_success_replaces_existing_output_and_cleans_temp_file",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_skips_temp_name_that_matches_requested_output",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_outputs_are_thread_deterministic",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_missing_output_parent_emits_output_error_json",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_directory_output_path_emits_output_error_text_and_cleans_temp_file",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_writes_to_null_device",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_out_of_tier_y4m_text_mode_emits_unsupported_feature_without_touching_output",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_out_of_tier_y4m_json_mode_emits_unsupported_feature_object",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_source_error_wins_before_missing_output_parent",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_empty_ivf_reports_typed_frame_set_error_without_touching_output",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test decode_y4m_cli --locked",
    "cargo xtask check-diagnostic-registry",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "tests/conformance/vectors/valid/syn-2seq-compatible-intra-64x64.obu",
    "tests/conformance/vectors/valid/syn-empty-avmenc-64x64.ivf",
    "tests/conformance/manifest.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit", "decode/malformed-source", "decode/output-error"]

[[feature]]
id = "DECODE-MINIMAL-RAW-RUNTIME-OUTPUT"
name = "Minimal-tier raw output"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["Annex B.2", "Annex B.3", "5.2", "5.18.2", "5.19", "5.20.1", "5.20.3.1", "5.20.3.2", "5.17.12", "6.2", "6.4.1", "6.16.13", "6.17.2", "6.17.4.1", "6.17.4.4", "6.17.7.2", "6.18", "6.19.1", "7.1", "7.21.1", "7.21.2", "8.2.2", "8.2.4", "8.2.6", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/output/raw.rs"
change_id = "decode-minimal-tier-raw-output"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds the first runtime raw sample-byte output path for the existing `minimal-intra-8bit420-hash-v1` fixture-trace tier. `DecodeContext::decode_raw_bytes` reuses `plan_bytes` plus the shared minimal tier validation and, for a `base_q_idx == 255` frame, tile-payload trace verification through the root partition frontier plus the `DECODE-MINIMAL-INTRA-RECONSTRUCTION-FRONTIER` frame handoff; the committed fixture now uses base_q_idx 210 and routes through the general intra path (`DECODE-GENERAL-INTRA-FRAME-RECON`). It serializes exactly one raw-intermediate-output frame as the existing `av2-output-samples-v1` byte stream through `splot-recon::DecodedFrameHashInput::write_to`: visible Y samples, then visible U samples, then visible V samples, with no header, metadata, OBU bytes, or container bytes. A truthfully empty zero-record IVF serializes successfully to zero raw bytes without charging frame limits. IVF width, height, and declared frame count are advisory transport metadata; complete zero-size records are discarded as runtime no-ops only after bounded planning charges them to `max_ivf_frame_records`. The AV02 codec boundary and malformed/truncated record failures remain typed. Raw output shares the hash path's zero-IVF-timebase tolerance because it does not serialize frame-rate metadata, and the same committed frame-unit payload decodes when supplied as raw Annex B. The committed advisory-IVF derivative decodes byte-identically through pinned AVMdec, current AVMdec, and splot. `splot decode --output-format raw -o` streams decoded frames through a bounded writer buffer into a lazily-created same-directory temporary file, then publishes atomically after flush, file sync, rename, and best-effort parent-directory sync; the explicitly supported Unix null device is opened and written directly because a device node cannot be atomically replaced. Other special paths are not direct-write targets. Out-of-tier, malformed, and resource-limit inputs fail closed without touching the requested output path, and output serialization/publication failures emit `decode/output-error` with raw-specific operation identifiers and without nondeterministic temporary filename suffixes. This row does not claim broad stream-like paths, broad AV2 decode, Annex A conformance, post-film-grain output, film grain, show-existing/flush scheduling, reference refresh, metadata MD5 verification, AVM/dav2d invocation at runtime, or raw output beyond the committed minimal tier frame-unit shape."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/output/raw.rs::tests::empty_ivf_decodes_to_zero_raw_bytes_with_zero_output_limits",
    "crates/splot-decode/src/output/raw.rs::tests::minimal_fixture_decodes_to_exact_raw_bytes",
    "crates/splot-decode/src/output/raw.rs::tests::raw_annex_b_payload_decodes_to_exact_raw_bytes",
    "crates/splot-decode/src/output/raw.rs::tests::zero_ivf_timebase_does_not_block_raw_output",
    "crates/splot-decode/src/output/raw.rs::tests::long_raw_decode_keeps_output_frames_out_of_reference_storage",
    "crates/splot-decode/src/output/raw.rs::tests::raw_output_is_deterministic_across_thread_policies",
    "crates/splot-decode/src/output/raw.rs::tests::caller_writer_io_error_maps_to_raw_output_diagnostic",
    "crates/splot-decode/src/output/raw.rs::tests::advisory_ivf_metadata_and_empty_record_preserve_raw_output",
    "crates/splot-decode/src/output/raw.rs::tests::empty_ivf_record_is_charged_to_the_record_limit",
    "crates/splot-decode/src/output/raw.rs::tests::non_av2_ivf_codec_is_a_typed_source_rejection",
    "crates/splot-decode/src/output/raw.rs::tests::trailing_partial_ivf_header_preserves_complete_frame_output",
    "crates/splot-decode/src/output/raw.rs::tests::truncated_ivf_payload_fails_before_raw_output",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_explicit_raw_output_format_requires_output_path",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_explicit_raw_success_for_minimal_fixture",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_success_replaces_existing_output_and_cleans_temp_file",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_out_of_tier_raw_text_mode_emits_unsupported_feature_without_touching_output",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_source_error_wins_before_missing_output_parent",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_outputs_are_thread_deterministic",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_missing_output_parent_emits_output_error_json",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_directory_output_path_emits_output_error_text_and_cleans_temp_file",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_empty_ivf_creates_empty_raw_output",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_writes_to_null_device",
]
commands = [
    "cargo test -p splot-decode output::raw --locked",
    "cargo test -p splot-cli --test decode_raw_cli --locked",
    "cargo xtask check-diagnostic-registry",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "tests/conformance/vectors/valid/syn-empty-avmenc-64x64.ivf",
    "tests/conformance/vectors/valid/syn-ivf-advisory-empty-record-64x64.ivf",
    "tests/conformance/manifest.toml",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit", "decode/malformed-source", "decode/output-error"]

[[feature]]
id = "INFRA-DECODER-CRATE-SCAFFOLDING"
name = "Decoder and reconstruction crate scaffolding"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-decode"
module = "crates/splot-decode/src/lib.rs"
change_id = "decoder-crate-scaffolding"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds approved workspace scaffolds for `splot-recon` and `splot-decode`, plus dependency-direction and coverage-scope automation updates. `splot-recon` has no `splot-*` dependencies and stays scheduler-free. `splot-decode` depends on `splot-core` for source-backed stream planning, `splot-parallel` for `DecodeRuntimeConfig`/`DecodeContext` plus its single owned `WorkerPool`, and `splot-recon` for the narrow runtime decode/reconstruction/hash/raw/Y4M handoff tracked by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`, `DECODE-MINIMAL-INTRA-RECONSTRUCTION-FRONTIER`, `DECODE-MINIMAL-RAW-RUNTIME-OUTPUT`, and `DECODE-Y4M-RUNTIME-OUTPUT`. Decoded-frame model types are tracked by `INFRA-RECON-FRAME-PLANE-TYPES`; this scaffold row itself exposes no broad runtime reconstruction, broad raw/Y4M writer use, or broad byte-consuming decoded-frame production path. The raw byte planner is tracked separately by `DECODE-BYTE-STREAM-PLANNER`. The scaffolds emit no decoder diagnostics beyond separately tracked decode diagnostics and do not invoke or require AVM/dav2d."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo check -p splot-recon --locked",
    "cargo check -p splot-decode --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/lib.rs",
    "crates/splot-decode/Cargo.toml",
    "crates/splot-decode/src/lib.rs",
    "crates/splot-decode/src/context.rs",
    "crates/splot-decode/src/runtime.rs",
]
diagnostics = []

[[feature]]
id = "INFRA-PARALLEL-RUNTIME-POLICY"
name = "Parallel runtime policy (Rayon worker pool)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-parallel"
module = "crates/splot-parallel/src/lib.rs"
change_id = "parallel-runtime-policy"
tracking_issue = ""
owner = "core"
risk = "low"
notes = "Adds the maintainer-authorized `splot-parallel` crate owning the only approved concurrency primitive: a local Rayon `WorkerPool` (never the global pool or `build_global`). Introduces a `ThreadCount` policy (`auto` default, fixed positive integer, `0` aliases to `auto`) wired through `EncoderRuntimeConfig`/`DecodeRuntimeConfig`; the CLI exposes it through `splot decode --threads`, and each encoder/decoder context owns exactly one `WorkerPool`. Enforced by `cargo xtask check-concurrency-policy`. This infrastructure policy makes no codec-algorithm claim; tokio/async-std/futures/threadpool and other competing runtimes are banned."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo test -p splot-parallel --all-targets --locked",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-dependency-direction",
]
fixtures = [
    "crates/splot-parallel/src/lib.rs",
    "crates/splot-parallel/src/pool.rs",
    "crates/splot-parallel/src/thread_count.rs",
    "crates/splot-parallel/src/queue.rs",
    "xtask/src/concurrency_policy.rs",
]
diagnostics = []

[[feature]]
id = "INFRA-RECON-FRAME-PLANE-TYPES"
name = "Decoded frame and plane runtime types"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "6.17.4.1", "6.17.4.4", "7.1", "7.21.1", "7.21.2", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-recon"
module = "crates/splot-recon/src/lib.rs"
change_id = "decoded-frame-plane-runtime-types"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds the first source-backed immutable decoded output model in `splot-recon`: AV2-derived `BitDepth`, `PixelFormat`, `PlaneId`, `OutputIndex`, `PlaneSize`, `PlaneRect`, `Plane<T>`, `FramePlanes<T>`, `DecodedFrameInfo`, `DecodedFrame<T>`, sealed `ReconSample` for `u8`/`u16`, and typed `ReconError`. Constructors enforce reserved bit-depth/chroma idc rejection, positive dimensions, crop bounds/alignment, stride, checked sample and byte accounting, exact backing length, visible-row access excluding padding, monochrome/non-monochrome plane presence, AV2-derived chroma visible sizes, sample-type compatibility, and sample-range checks. This is output model infrastructure only: no byte-consuming decode, reconstruction algorithm, frame hash computation, Y4M output, reference-frame-store behavior, external decoder invocation, or dependency graph change."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/format.rs::tests",
    "crates/splot-recon/src/geometry.rs::tests",
    "crates/splot-recon/src/plane.rs::tests",
    "crates/splot-recon/src/frame.rs::tests",
]
commands = [
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/lib.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/format.rs",
    "crates/splot-recon/src/geometry.rs",
    "crates/splot-recon/src/plane.rs",
    "crates/splot-recon/src/frame.rs",
]
diagnostics = []

[[feature]]
id = "INFRA-ZERO-COPY-MEDIA-POLICY"
name = "Zero-copy media-buffer ownership policy"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-recon"
module = "crates/splot-recon/src/lib.rs"
change_id = "zero-copy-media-policy"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Makes the media-buffer ownership model explicit, documented, and CI-enforced before decoder/reconstruction/encoder code exists: `docs/ARCHITECTURE.md` defines borrow / mutable view / share / materialize / copy-ok marker and the default view-first model; `splot-recon` adds borrowed `PlaneRef`/`PlaneMut`/`FrameRef`/`FrameMut` views and an explicit `SharedFrame` (Arc-backed, `.share()` only, no `Clone`) and drops dangerous `Clone` from `Plane`/`FramePlanes`/`DecodedFrame`/`ReferenceFrameStore`/`CurrentFrameWorkspace`/`CurrentFramePlane`; every intentional copy carries a specific `splot-copy-ok:` marker; and `cargo xtask check-zero-copy-policy` (run in `cargo xtask ci`) flags large-media `Clone`, unmarked media copies, `make_mut`, `unsafe`/`transmute`/`from_raw_parts`, `include!` bypasses, and banned/wrong-crate `zerocopy`. `zerocopy` is approved only for private fixed-layout byte/wire view structs (e.g. IVF headers), never public APIs or AV2 bit-level/entropy/variable-length parsing. Infrastructure / codec-runtime API policy only: no AV2 conformance behavior change and no decoder/reconstruction/encoder algorithmic stage is implemented."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/views.rs::tests",
    "crates/splot-recon/src/frame.rs::tests",
    "crates/splot-recon/src/reference.rs::tests",
    "xtask/src/zero_copy.rs::tests",
]
commands = [
    "cargo test -p splot-recon --all-targets --locked",
    "cargo test -p xtask --all-targets --locked",
    "cargo xtask check-zero-copy-policy",
    "cargo xtask check-dependency-direction",
]
fixtures = [
    "docs/ARCHITECTURE.md",
    "crates/splot-recon/src/views.rs",
    "crates/splot-recon/src/frame.rs",
    "crates/splot-recon/src/plane.rs",
    "crates/splot-recon/src/reference.rs",
    "crates/splot-recon/src/workspace.rs",
    "xtask/src/zero_copy.rs",
]
diagnostics = []

[[feature]]
id = "INFRA-COMMENT-DENSITY-RATCHET"
name = "Comment-density ratchet"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/comment_density.rs"
change_id = "comment-density-ratchet"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Applies the Comment Diet mission to source comments and adds `cargo xtask check-comment-density` as a permanent implementation-comment budget gate. The gate counts Rust files under `crates`, `xtask`, and `fuzz/fuzz_targets` (including untracked non-ignored files during local runs), excludes SPDX lines, Rustdoc, xtask-generated sources identified by the `@generated by `cargo xtask` marker, and comment-looking lines inside multiline string literals, and fails when full-line implementation comments exceed `tools/comments/budget.toml`. The pre-cleanup baseline was 21,975 implementation comment lines and 34,868 Rustdoc lines across 531 Rust files. The post-cleanup source-aware gate count is 460 implementation comment lines across 532 scoped Rust files (47.8x reduction); the ratchet budget is 460. The cleanup preserves required Rustdoc, SPDX headers, generated markers, tracked `TODO(spec: ...)` comments, and zero-copy `splot-copy-ok` markers while removing redundant implementation comments and process-history comments. Pure repository hygiene: no AV2 conformance behavior, no diagnostic semantics, no crate dependency graph, and no CLI contract changes."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/comment_density.rs::tests",
]
commands = [
    "cargo test -p xtask comment_density --locked",
    "cargo xtask check-comment-density",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "tools/comments/budget.toml",
    "xtask/src/comment_density.rs",
    ".github/workflows/ci.yml",
    "AGENTS.md",
]
diagnostics = []

[[feature]]
id = "INFRA-AI-SLOP-GATE"
name = "AI-slop comment gate"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/ai_slop.rs"
change_id = ""
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds `cargo xtask check-ai-slop`, a hard-zero gate that fails when a tracked, non-generated Rust source comment carries a banned process-history or fixture-diary phrase (`formerly`, `round-<n>`, `PR #<n>`, `this refactor`, `this used to`, `used to be`, `old behavior`/`old behaviour`, `oracle fixture`, `verified fixture`, `pinned by fixture`, `not yet fixtured`). It reuses the `comment_density` string-aware comment scanner (including nested and trailing block comments), skips generated files and SPDX lines, matches case-insensitively at word boundaries, and intentionally excludes established domain vocabulary (`frontier`, `previously`, `only supports`, `deferred until`). Wired into `cargo xtask ci` and `.github/workflows/ci.yml`. Codifies the recurring AI-slop policy in AGENTS.md §11, AGENTS.md, and AGENTS.md. Pure repository hygiene: no AV2 conformance behavior, no diagnostic semantics, no crate dependency graph, and no CLI contract changes."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/ai_slop.rs::tests",
]
commands = [
    "cargo test -p xtask ai_slop --locked",
    "cargo xtask check-ai-slop",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "xtask/src/ai_slop.rs",
    "xtask/src/comment_density.rs",
    ".github/workflows/ci.yml",
    "AGENTS.md",
]
diagnostics = []

[[feature]]
id = "INFRA-DUPEHOUND-DUPLICATION-GATE"
name = "Duplicate-code budget gate"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/dupehound.rs"
change_id = "dupehound-duplication-gate"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Integrates dupehound (https://github.com/Rafaelpta/dupehound), a structural duplicate-function detector, as a CI gate with the two modes the maintainer chose. The absolute-budget gate `cargo xtask check-duplication` runs `dupehound scan --json` (dupehound's DEFAULT scope, which excludes `#[test]` bodies), parses `score.deletable_lines`, and fails when it exceeds the committed ceiling in `tools/dupehound/budget.toml`; the ceiling is a ratchet (lowered when a cluster is removed, never raised). The default scope gates PRODUCTION duplication (the kind that costs maintainability); deliberately-explicit per-scenario tests are intentional in this repo and are not gated — this is a ratchet, not a zero mandate. The pure `enforce_budget` threshold helper is unit-tested for over/at/under-budget. The gate follows the run-if-present policy (mandatory in CI, skipped locally with an install hint) and runs inside `cargo xtask ci` next to the other repository gates. `.github/workflows/ci.yml` installs dupehound (pinned 0.1.2 for a reproducible count), runs the budget gate, and runs the PR-only `dupehound check --diff <base>` ratchet that blocks newly introduced duplication (base bound through env for workflow-injection hygiene). `AGENTS.md` + `AGENTS.md` record the reuse-before-reimplement discipline. Non-normative repository tooling: no AV2 conformance behavior, no Cargo dependency (dupehound is an external CLI like typos/cargo-machete/cargo-deny; JSON parsed with the `serde_json` xtask already uses), and no decoder/encoder algorithmic stage. Subsequent dedup commits lower the production budget under this same change."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/dupehound.rs::tests",
]
commands = [
    "cargo test -p xtask --all-targets --locked",
    "cargo xtask check-duplication",
    "cargo xtask ci",
]
fixtures = [
    "tools/dupehound/budget.toml",
    "xtask/src/dupehound.rs",
    ".github/workflows/ci.yml",
    "AGENTS.md",
]
diagnostics = []

[[feature]]
id = "XTASK-DOC-BUDGET"
name = "Manual documentation budget gate"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/doc_budget.rs"
change_id = "doc-budget-gate"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Adds `cargo xtask check-doc-budget` and `tools/docs/budget.toml` as a CI-enforced budget for committed manual markdown. The gate excludes the AV2 spec mirror, legal notices, test fixture docs, and third-party notices; it rejects generated status/coverage markdown, roadmap/gap/status novels, and docs/archive/old/notes hiding places. Generated status renders remain available on demand through their xtask commands and are drift-checked when present, but `check-doc-budget` prevents committing them."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/doc_budget.rs::tests",
]
commands = [
    "cargo test -p xtask doc_budget --locked",
    "cargo xtask check-doc-budget",
    "cargo xtask ci",
]
fixtures = [
    "tools/docs/budget.toml",
    "xtask/src/doc_budget.rs",
    ".github/workflows/ci.yml",
    "docs/README.md",
]
diagnostics = []

[[feature]]
id = "INFRA-FUZZ-CORPUS-SEEDING"
name = "Fuzz corpus seeding xtask"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/seed_fuzz_corpus.rs"
change_id = "extract-fuzz-corpus-seeding"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Moves the CI fuzz-smoke corpus seeding out of ~100 lines of embedded shell and Python in `.github/workflows/ci.yml` into `cargo xtask seed-fuzz-corpus` (xtask/src/seed_fuzz_corpus.rs). The subcommand reads the committed `tests/fixtures/*.av2` corpus and `tests/conformance/vectors/**/*.ivf` AVM streams and writes the `fuzz/corpus/<target>/` seeds the per-target fuzz matrix relies on: a raw copy of each fixture into every target dir, config-prefixed copies for the validator/planner/decode-runtime targets, hand-tuned minimal symbol-decoder/tile-payload/recon seeds, synthesized IVF wrappers for parse_ivf, and de-wrapped raw OBU streams for parse_obu/parse_bitstream. Target enumeration reuses the existing `fuzz_targets()` helper (filesystem-derived, no nightly cargo-fuzz needed). Pure repository tooling: the seed bytes are byte-identical to the former inline script (verified by a full `diff -r` over all 974 seeds), so it changes no AV2 conformance behavior and implements no decoder/encoder algorithmic stage. The byte layouts (IVF header/frame synthesis, IVF de-wrap, config prefixes) are unit-tested in-process so the logic is testable and locally reproducible instead of buried in YAML. CI replaces the inline block with the single `cargo xtask seed-fuzz-corpus` command."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/seed_fuzz_corpus.rs::tests",
]
commands = [
    "cargo test -p xtask --all-targets --locked",
    "cargo xtask seed-fuzz-corpus",
    "cargo xtask ci",
]
fixtures = [
    "xtask/src/seed_fuzz_corpus.rs",
    ".github/workflows/ci.yml",
]
diagnostics = []

[[feature]]
id = "RECON-REFERENCE-FRAME-STORE"
name = "Reference frame store runtime model"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.4.6", "6.4.6", "5.18.2", "6.17.2", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-6",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-recon"
module = "crates/splot-recon/src/reference.rs"
change_id = "recon-reference-frame-store-refresh-flags"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a source-backed dependency-free runtime reference-slot container in `splot-recon`: `ReferenceSlot` with `MAX_SLOTS == 16` (AV2 §3 NUM_REF_FRAMES), `ReferenceRefreshMask` for caller-derived refresh masks within that slot ceiling, and generic `ReferenceFrameStore<F>` with validated `1..=16` capacity, bounds-checked `put`/`get`/`take`, preflighted `refresh_slots_with`, `clear`, occupancy, immutable ascending slot-order entries, and replacement reporting over caller-owned frame payloads. `refresh_slots_with` validates every selected mask bit against the store capacity before invoking the payload producer, treats zero masks as no-ops, and visits selected slots in ascending order while avoiding `F: Clone`. The generic payload avoids coupling future reconstructed-reference storage to output-emission metadata. This is storage infrastructure only: it does not parse or infer `refresh_frame_flags`, derive active NumRefFrames/ActiveNumRefFrames, model RefValid or key/switch first-reference validity, output scheduling, show-existing deduplication, counters, order hints, dimensions, crop metadata, motion vectors, CDFs, grain params, segment IDs, global motion state, CCSO params, byte-consuming decode, resource diagnostics, external decoder invocation, or dependency graph changes."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-recon/src/reference.rs::tests"]
commands = [
    "cargo test -p splot-recon reference --locked",
    "cargo test -p splot-recon --locked",
    "cargo clippy -p splot-recon --all-targets --locked -- -D warnings",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-recon/src/reference.rs",
    "crates/splot-recon/src/error.rs",
    "crates/splot-recon/src/lib.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-LIMITS-RUNTIME-API"
name = "Decode limits runtime API"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.11.6", "Annex B.2", "Annex B.3", "5.2.1", "6.4.1", "6.4.6", "5.18.4.1", "6.17.4.1", "5.18.4.4", "6.17.4.4", "5.18.7.2", "6.17.7.2", "5.19", "6.18", "5.20.1", "6.19.1", "7.1", "7.20.2", "7.21", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-11-6",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-4-4",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-18",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-decode"
module = "crates/splot-decode/src/limits.rs"
change_id = "decode-limits-runtime-api"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a source-backed, dependency-free `splot-decode` runtime API for caller-provided decode resource policy: `DecodeOptions`, `DecodeLimits`, typed limit names and units, thresholds, inclusive pure checks, local typed limit errors, checked arithmetic helpers, and allocation-size handoff checks. The API uses `max_ivf_frame_records`, `max_reference_slots`, `max_reference_store_bytes`, `max_tile_partition_steps`, `max_tile_payload_bytes`, and `max_loop_restoration_source_reads` to avoid overclaiming AV2 semantics, provides finite defaults plus explicit zero/unlimited constructors, and keeps all thresholds as repository policy rather than AV2 conformance limits. The pinned finite OBU/frame defaults are sized for the current local decoder mission target's inspected 12964 OBU stream so the byte planner reaches the next runtime gate instead of stopping at the old 128-frame policy cap. This is policy infrastructure only: it does not consume bytes, allocate from bitstream-derived values, emit the planned resource-limit diagnostic, change CLI behavior, compute frame hashes, write Y4M, reconstruct pixels, store references, invoke external decoders, or change the dependency graph."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/limits.rs::tests",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo clippy -p splot-decode --all-targets --locked -- -D warnings",
    "cargo xtask check-diagnostic-registry",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-decoder-support",
]
fixtures = ["crates/splot-decode/src/limits.rs", "crates/splot-decode/src/lib.rs"]
diagnostics = []

[[feature]]
id = "DECODE-STREAM-STATE-PLANNER"
name = "Parsed decode stream planner"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.2.1", "5.4.1", "6.2.2", "6.4.1", "7.1", "7.3", "7.4", "Annex F.3.2", "Annex F.3.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-4",
    "docs/spec/av2/1.0.0/annex-f-sub-bitstream-extraction-informative.md#s-annex-f-3-2",
    "docs/spec/av2/1.0.0/annex-f-sub-bitstream-extraction-informative.md#s-annex-f-3-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/stream_plan.rs"
change_id = "decode-stream-state-planner"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a source-backed parsed-input stream planner in `splot-decode`: `DecodeContext::plan_stream` consumes already parsed `splot_core::stream::ParsedBitstream` plus caller-supplied input length, preserves raw Annex B and IVF OBU order/offset metadata, selects the fixed base layer while enforcing §6.2.2 global/local xlayer scope rules, treats selected frame OBUs as frame candidates, applies all input/container/OBU/frame limits, and rejects malformed sources transactionally. A §5.4.1 sequence declaration may advertise temporal enhancement layers without forcing their selection. Per Annex F.3.2, actual TL>0 OBUs are retained in the source plan with truthful metadata and OBU accounting but marked unselected, so they do not consume frame limits or reach runtime/output; Annex F.3.2/F.3.3 temporal delimiters remain global regardless of temporal id. Non-base embedded/extended layers and selected multistream structures remain rejected as separate selection gaps. The 49-byte validator-clean current-AVM scalable fixture carries one TL0 and one TL1 frame. Fixed splot emits only the 6,144-byte TL0 raw I420 frame at SHA-256 ce03721c5041e45d02106592c6c55db2bcda0d651568729b92f4ef5d4a665774, byte-identical to pinned avmdec revision 457cd58681a747465661baccb1f32095bc5b7774 and current avmdec revision 3935f4bf31ee774177d9300341bbbaf5b6116500 with `--limit=1`; unrestricted pinned/current AVM agree on the complete two-frame output SHA-256 5b7d078283444422337c3e7f71623fd16a0ea5c7193bc45ca7e4c1307465598b. Operating-point, embedded-layer, extended-layer, and multistream selection remain unsupported. The planner runs through the context-owned `WorkerPool` boundary and tests deterministic metadata across thread policies, but the implementation is serial and uses no direct Rayon/crossbeam APIs."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/stream_plan.rs::tests",
    "crates/splot-decode/src/output/raw_tests.rs::higher_declared_temporal_layer_decodes_selected_base_layer_to_reference_output",
    "crates/splot-decode/src/output/raw_tests.rs::non_base_temporal_layer_is_retained_without_runtime_output",
    "crates/splot-decode/src/output/raw_tests.rs::higher_declared_temporal_layer_fixture_eof_fails_closed",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-decode/Cargo.toml",
    "crates/splot-decode/src/context.rs",
    "crates/splot-decode/src/error.rs",
    "crates/splot-decode/src/lib.rs",
    "crates/splot-decode/src/bitstream/stream_plan.rs",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs",
    "crates/splot-decode/src/pipeline/frame_lifecycle.rs",
    "crates/splot-decode/src/output/raw_tests.rs",
    "tests/conformance/vectors/valid/syn-tlayer2-base-only-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-tlayer2-base-and-enhancement-64x64.obu",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-BYTE-STREAM-PLANNER"
name = "Byte-consuming decode stream planner"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["4.11.6", "Annex B.2", "Annex B.3", "5.2.1", "5.2.2", "5.3", "6.2.1", "6.2.2", "7.1", "7.3"]
sources = [
    "docs/spec/av2/1.0.0/04-conventions.md#s-4-11-6",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-2",
    "docs/spec/av2/1.0.0/annex-b-length-delimited-bitstream-format.md#s-annex-b-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/byte_stream.rs"
change_id = "decode-byte-stream-planner"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a bounded byte-consuming stream planner in `splot-decode`: `DecodeContext::plan_bytes` accepts raw Annex B or IVF/DKIF bytes, checks `max_input_bytes` before traversal, checks `max_obus` before retaining each OBU across the whole stream, checks `max_ivf_frame_records` before processing each complete IVF frame record, checks `max_frames_to_decode` before retaining each accepted selected frame candidate (`OBU_CLOSED_LOOP_KEY`, `OBU_REGULAR_TILE_GROUP`, or `OBU_REGULAR_TIP`), builds bounded `splot-core` parsed stream structures by driving `AnnexBObuCursor` and `IvfFrameCursor`, and reuses the existing parsed planner for base-layer selection, typed malformed-source errors, and typed unsupported-structure metadata. IVF width, height, and declared frame count stay advisory; actual complete records drive traversal and record-limit accounting, including zero-size records that the runtime later treats as no-ops. Non-AV02 FourCC metadata is a typed input-domain rejection (`decode/unsupported-ivf-codec`), while truncated record headers and payloads stay malformed-source failures. Reserved types 0 and 26-31 are retained, counted against `max_obus`, and classified global regardless of extension-layer ids per §5.3/§6.2.2; the one-pass byte-stream preparation path plans from a single `FlatParsedBitstream`, then `FlatParsedBitstream::discard_runtime_noops` removes reserved OBUs before syntax scheduling. The API runs inside the context-owned `splot_parallel::WorkerPool` and tests deterministic results across thread policies. It also adds the `decode_plan_bytes` cargo-fuzz target with finite limits. `splot decode` now reads files and calls this planner before rendering malformed-source, resource-limit, planner unsupported, or runtime-deferral diagnostics. This remains plan-only: it does not parse tile payloads, call `payload_status()`, reconstruct pixels, compute hashes, write Y4M, refresh references, or invoke AVM/dav2d at runtime; local AVM output agreement for downstream raw decode is recorded separately."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/byte_stream.rs::tests",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::byte_planner_raw_annex_b_matches_parsed_plan",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::ivf_plan_preserves_frame_context_and_warning_metadata",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::byte_planner_malformed_sources_are_transactional",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::byte_planner_limits_reject_before_unbounded_traversal",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::byte_planner_propagates_unsupported_structures",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::byte_planning_is_deterministic_across_thread_policies",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::tests::reserved_obus_are_retained_without_layer_selection",
    "crates/splot-decode/src/pipeline/mod_tests.rs::prepared_byte_stream_discards_reserved_obus_from_annex_b_and_ivf",
    "crates/splot-decode/src/output/raw_tests.rs::advisory_ivf_metadata_and_empty_record_preserve_raw_output",
    "crates/splot-decode/src/output/raw_tests.rs::empty_ivf_record_is_charged_to_the_record_limit",
    "crates/splot-decode/src/output/raw_tests.rs::non_av2_ivf_codec_is_a_typed_source_rejection",
]
commands = [
    "cargo test -p splot-core annexb --locked",
    "cargo test -p splot-core ivf --locked",
    "cargo test -p splot-decode --locked",
    "cargo check --manifest-path fuzz/Cargo.toml --bins",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-core/src/annexb.rs",
    "crates/splot-core/src/ivf.rs",
    "crates/splot-decode/src/bitstream/byte_stream.rs",
    "crates/splot-decode/src/context.rs",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs",
    "tests/conformance/vectors/valid/syn-2frame-reserved-obu-64x64.ivf",
    "tests/conformance/vectors/valid/syn-ivf-advisory-empty-record-64x64.ivf",
    "fuzz/fuzz_targets/decode_plan_bytes.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/malformed-source", "decode/resource-limit"]

[[feature]]
id = "DECODE-TILE-PAYLOAD-BOUNDARY"
name = "Tile payload decode boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.1", "5.20.2.1", "6.19.1", "7.1", "8.2.2", "8.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload.rs"
change_id = "tile-payload-decode-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private `splot-decode` tile-payload boundary for the minimal single-tile closed-loop-key tier: it accepts already-framed §5.20.1 tile-group payload metadata, enforces `max_tile_payload_bytes` and `max_tile_count`, derives deterministic tile work units with exact source/layer/tile/MI-range/byte-span provenance, initializes §8.2 symbol state over the bounded tile slice, and then returns structured `decode/unsupported-feature` metadata for the unimplemented `decode_tile()` / §8.3 CDF-selection boundary. The boundary intentionally does not call `exit_symbol()`, mutate CDF banks, copy/average CDFs, reconstruct pixels, compute hashes, write runtime Y4M, refresh references, support multi-tile/multi-tile-group/bridge/BRU paths, invoke AVM/dav2d, or add scheduler use outside the `splot_parallel::WorkerPool` policy."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
    "fuzz/fuzz_targets/tile_payload_decode_bytes.rs",
]
commands = [
    "cargo check --manifest-path fuzz/Cargo.toml --bins --locked",
    "cargo xtask check-fuzz-targets",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-decode/src/lib.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
    "fuzz/decode_tile_payload_harness.rs",
    "fuzz/fuzz_targets/tile_payload_decode_bytes.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-MULTIPLE-TILE-GROUPS"
name = "Multiple tile-group OBUs per coded frame"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.19", "5.20.1", "7.3.3", "7.3.4", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-multiple-tile-groups"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements one coded frame spread across multiple §5.19 tile-group OBUs. The planner labels non-first groups as FrameContinuation, so they do not consume frame-count limits. The runtime records the first group's frame-header bits, validates a continuation's optional frame-header copy, parses each group with its own source-backed byte range, and appends only contiguous tile-number work units; frame-end CDF update and wrap-up remain deferred until the group containing the final tile. The 76-byte AVMenc fixture splits two 64x64 tiles across tg 0..0 and tg 1..1. Fixed splot, pinned avmdec revision 457cd58681a747465661baccb1f32095bc5b7774, and current avmdec revision 3935f4bf31ee774177d9300341bbbaf5b6116500 agree on the complete 12,288-byte raw output SHA-256 2b0affadf536e7decb3e6899e173ffb289127c131a1f676cebcf79169f714fe2."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod_tests.rs::multiple_tile_groups_decode_bit_exact",
    "crates/splot-decode/src/pipeline/mod_tests.rs::continuation_prefix_locates_structure_with_and_without_header_copy",
    "crates/splot-decode/src/pipeline/mod_tests.rs::continuation_prefix_rejects_mismatch_and_eof_in_header_copy",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::non_first_tile_groups_continue_one_frame_candidate",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2tile-2group-intra-128x64-q80.ivf",
    "crates/splot-decode/src/bitstream/stream_plan.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-CONTEXT-TILE-PAYLOAD-HANDOFF"
name = "DecodeContext tile-payload handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.1", "5.20.2.1", "6.19.1", "7.1", "8.2.2", "8.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3",
    "docs/ARCHITECTURE.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/context.rs"
change_id = "decode-context-tile-payload-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a crate-private `DecodeContext` handoff for the existing tile-payload boundary: `DecodeContext::plan_tile_payload_boundary` runs the crate-private `plan_tile_payload_boundary` inside the context-owned `splot_parallel::WorkerPool`, preserving deterministic tile work-unit metadata and internal tile-boundary errors across `auto`, `1`, and fixed positive thread policies. The tile payload facts remain caller-provided and crate-private, with conditional non-test dead-code allowances documenting that runtime decode fact derivation was future work for this row. This row itself does not expose public tile-payload API support, derive tile facts from raw byte streams, implement `decode_tile()`, reconstruct pixels, compute hashes, write Y4M, refresh references, add a `splot-decode -> splot-recon` dependency, invoke AVM/dav2d, or use direct Rayon/crossbeam/global pools/ad-hoc threads. The later runtime hash handoff is tracked by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
]
commands = [
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-decode/src/context.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-TILE-PAYLOAD-INPUT-DERIVATION"
name = "Tile payload input derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.2.1", "5.18.1", "5.18.2", "5.18.6.1", "5.18.7.2", "5.19", "5.20.1", "5.20.2.1", "6.17.1", "6.17.7.2", "6.18", "6.19.1", "8.2.2", "8.2.4", "8.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-7-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-18",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/input.rs"
change_id = "decode-tile-payload-input-derivation"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private source-backed derivation bridge for the minimal one-tile closed-loop-key tile-payload tier: it validates a selected `DecodePlannedObu` against the borrowed `splot-core` `ObuEnvelope`, rejects forged source/container metadata before slicing, requires complete intra first-and-only tile-group facts, slices only the §5.20 tile_group_payload() region after a complete §5.19 structure with checked arithmetic, exposes and consumes `FrameHeaderCore::disable_cdf_update` instead of hardcoding CDF update mode, reuses the existing tile-payload boundary, and runs through `DecodeContext`'s context-owned `splot_parallel::WorkerPool`. It remains plan-only and crate-private: no public tile-payload API, multi-tile/multi-tile-group/bridge/BRU support, `decode_tile()`, reconstruction, hashes, Y4M output, reference refresh, AVM/dav2d invocation, or direct Rayon/crossbeam/global-pool/ad-hoc thread use is added."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_rejects_candidate_envelope_mismatch_before_slicing",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_rejects_absent_frame_header_facts_without_guessing",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_rejects_incomplete_tile_group_structure",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_rejects_invalid_locally_parsed_tile_group_structure",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_enforces_tile_count_and_payload_limits",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_rejects_unsupported_position_and_frame_paths",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_is_deterministic_across_decode_context_thread_policies",
]
commands = [
    "cargo test -p splot-core frame_header_core --locked",
    "cargo test -p splot-core reached_shared_tail_consumes_disable_cdf_update --locked",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo check -p splot-decode --locked",
    "cargo xtask check-dependency-direction",
    "cargo xtask check-concurrency-policy",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-core/src/headers/frame/info.rs",
    "crates/splot-decode/src/context.rs",
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/input.rs",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-TILE-CDF-SELECTION-BOUNDARY"
name = "Tile CDF selection boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.1", "5.20.2.1", "5.20.3.2", "6.19.1", "8.2.2", "8.2.4", "8.2.6", "8.3.1", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf.rs"
change_id = "tile-idtx-sign-context"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private `splot-decode` tile CDF selection boundary for the minimal single-tile closed-loop-key tier: a small partition-entry CDF subset (`TileDoSplitCdf`, `TileDoSquareSplitCdf`, `TileRectTypeCdf`, `TileDoExtPartitionCdf`, and `TileDoUneven4wayPartitionCdf`) is copied from generated §9.3 defaults, typed selector validation and closure-scoped mutable row access prove §8.3 handoff into `SymbolDecoder::read_symbol(cdf)`, the tile work unit records CDF update mode from `disable_cdf_update`, the §8.2 copy/average decision is computed from `enable_avg_cdf`, `avg_cdf_type`, `context_update_tile_id`, `TileNum`, and `TileCols * TileRows`, and bounded §8.3.2 contexts are derived for `do_split`, `do_square_split`, `rect_type`, `do_ext_partition`, and `do_uneven_4way_partition` using generated §9.2 conversion tables, availability-gated `MiSizes` lookups, and the local §8.3.2 adjustment arrays. Bounded §8.3.2 BLOCK-symbol context derivation covers `y_mode_index` and `uv_mode` (in `cdf/block_context.rs`). `TileIntraJointModeState::y_mode_index_ctx` computes `ctx = (left >= NON_DIRECTIONAL_MODES_COUNT) + (above >= NON_DIRECTIONAL_MODES_COUNT)` from the current per-MI joint-mode grid; out-of-frame neighbours contribute `DC_PRED`, so the tile origin is context 0 while directional left and above neighbours produce contexts 1 or 2. `decode_general_intra_luma_block_mode_with_fsc_context` uses that production value for the `YModeIndex` and `YModeOffset` selectors before reconstructing the mode. `reconstruct_minimal_y_mode` reconstructs the typed luma `YMode` from the decoded `y_mode_set`/`y_mode_index` for the supported non-directional subset (`y_mode_set == 0`, non-directional index -> `Reordered_Y_Mode[index]`), and `uv_mode_ctx` derives the `uv_mode` ctx = `is_directional_mode(YMode)` (DC_PRED -> 0). The `all_zero` (`txb_skip`/`v_txb_skip`) §8.3.2 context formula is also implemented: `txb_skip_ctx_luma` (plane 0: `fsc` -> `TXB_SKIP_CONTEXTS-1`; transform-fills-block -> 0; else `(Min(top,4)+Min(left,4)+3)>>1`) and `v_txb_skip_ctx` (plane 2: `(above!=0)+(left!=0)`, `+3` if the chroma block exceeds the transform, `+6` if `EobU!=0`), over caller-supplied level context and transform-block geometry. The minimal block-symbol trace `consume_trace` now decodes sequentially (not from a static table) so the `uv_mode` context depends on the reconstructed `YMode`, and the `txb_skip`/`v_txb_skip` contexts are computed from the *derived* zero level context of the first transform block (no prior decoded blocks; out-of-frame neighbours); both derived contexts equal the previous literals for the fixture, so the existing no-output-change snapshot proves the values match. The live in-frame `IntraJointModes` lookup and production CDF-selection handoff are exercised by the `TileIntraJointModeState` context tests; broader luma-mode reconstruction and coefficient-state runtime behavior are tracked by their dedicated features. The first COEFFICIENT-decode CDF bank, `TileEobExtraCdf`, is now wired into the block CDF subset: it is copied from the generated §9.3 `Default_Eob_Extra_Cdf` defaults, selected context-free by `coeff_cdf_q_ctx` per §8.3.2 (the cdf is given directly by `TileEobExtraCdf`, with no per-symbol context), and included in the tile copy/average and frame-end count-scaling paths. It is loaded but not yet read (the §5.20.7.27 `coeffs()` decode loop that would consume it is not wired), so the minimal-fixture decode output is unchanged. The `eob_pt` coefficient CDF family (the seven transform-size class banks `TileEobPt16Cdf` through `TileEobPt1024Cdf`) is wired the same way: each is copied from its generated §9.3 default and selected by an `EobPtSize` transform-size class plus `coeff_cdf_q_ctx` and the closed-form `eobCtx = (plane > 0) ? 2 : is_inter`, and the family joins the copy/average and frame-end count-scaling paths; it is likewise loaded but not yet read (also no-output-change). The `dc_sign` coefficient CDF bank (`TileDcSignCdf[coeff_cdf_q_ctx][plane_type][isHidden group][ctx]`) is wired the same way: it is copied from the generated §9.3 `Default_Dc_Sign_Cdf` defaults and the selector takes all four indices; the §8.3.2 `ctx` (0/1/2) is derived from the Above/Left DC-context buffers, which is deferred with the coeffs() loop, so this bank is also loaded-but-unread (no-output-change). The first COEFFICIENT-symbol §8.3.2 context derivations are also added, in `cdf/coeff_context.rs`: the two position-only coefficient base contexts `coeff_base_eob` (`coeff_base_eob_ctx(c, bwl, height)`, partitioning the scan position `c` by the adjusted block coefficient count `Tx_Height[adjTxSz] << Tx_Width_Log2[adjTxSz]` into the four `SIG_COEF_CONTEXTS_EOB - 4 ..= SIG_COEF_CONTEXTS_EOB - 1` contexts, total over an out-of-range shift) and `coeff_base_bob` (`coeff_base_bob_ctx(bob, segEob)`, partitioning the begin position by `segEob >> 3` and `segEob >> 2` into contexts 0/1/2). Both are pure functions of caller-supplied scan/segment scalars and caller-resolved adjusted geometry, needing no `Level[]` magnitude buffer; they are derivation-only and not yet read by any decode path (no-output-change). The `coeff_br` coefficient base-range context (`CoeffBrContext::ctx`) is also added in `cdf/coeff_context.rs`: it sums up to three caller-provided `Level[]` neighbour magnitudes (each clamped to `MAX_BASE_BR_RANGE - 1` = 5) at the §8.3.2 `Mag_Ref_Offset_With_Tx_Class` offsets for the transform class (only the first two offsets for non-2D chroma), halves and clamps the sum to 0..=6, then offsets it by plane (chroma `Min(mag, 3)`), DC position (non-2D `mag + 7`), or low-frequency (`mag + 7`). It is the first context to read the per-transform-block `Level[]` magnitudes, over a caller-provided row-major slice (neighbour reads beyond the block bounds or the slice contribute 0, with checked shifts and saturating geometry so it is total and panic-free for any caller-provided geometry). It takes the `txClass` as a caller-resolved scalar index (0 = 2D, 1 = HORIZ, 2 = VERT) rather than importing a reconstruction transform-class type, keeping the entropy CDF-selection layer off the `splot-decode` to `splot-recon` runtime-handoff edge. It is exercised by compile-time const-contract checks plus unit tests but not yet wired into a decode path (no-output-change). The two identity-transform magnitude contexts are also added: `coeff_base_idtx_ctx` (`mag = Min(3, Level[row][col-1]) + Min(3, Level[row-1][col])`) and `coeff_br_idtx_ctx` (the same with the `MAX_BASE_BR_RANGE - 1` = 5 clamp, then `Min(mag, 6)`), each reading only the left and above `Level[]` neighbours over a caller-provided slice with saturating geometry and a slice-bounds guard (total, panic-free). The main 2D significant-coefficient context `coeff_base` is also added (`CoeffBaseContext::select`): it sums the significant-neighbour `Level[]` magnitudes at the generated §9.2 `Sig_Ref_Diff_Offset` offsets for the transform class (num = 5 luma, 3 chroma-2D, 2 chroma-non-2D), each clamped by the position-dependent `magLimit` (5 for low-frequency near-DC samples unless parity-hidden DC, else 3), forms `ctx = (mag+1)>>1`, and returns a `CoeffBaseSelection` naming one of the five `coeff_base` banks with its bank-specific offset: `Ph` (`Min(ctx,4)`, parity-hidden DC, overriding the others), chroma `Uv`/`LfUv` (`Min(ctx,3)` plus the plane/2D offsets), luma low-frequency `Lf` (the c==0 / row+col / horiz-col-vert-row branches over `LF_SIG_COEF_CONTEXTS_2D`), and luma high-frequency `Hf` (`Min(ctx,4)` plus the row+col position buckets, or +15 non-2D). It reads a caller-provided row-major `Level[]` slice with checked shifts + saturating geometry + a slice guard (total, panic-free) and uses the generated `splot_core::tables::conversion::SIG_REF_DIFF_OFFSET` (no duplicate). The `dc_sign` sign context is also added (`dc_sign_ctx`): it nets the DC-sign votes of the block's above and left neighbours (`AboveDcContext[plane][x4+k]` for k in 0..w4 and `LeftDcContext[plane][y4+k]` for k in 0..h4, sign 1 decrementing and sign 2 incrementing a running dcSign), returning ctx 1 (dcSign<0), 2 (dcSign>0), or 0; the spec `x4+k < MiCols` / `y4+k < MiRows` bounds are the caller-supplied slice lengths, and the loop breaks once the monotonic index leaves the slice so a pathological w4/h4 cannot spin (total, panic-free). This completes the §8.3.2 `dc_sign` selection over the `dc_sign` CDF bank wired earlier, though it remains loaded-but-unread until the coeffs() loop supplies the DC-context buffers. The `idtx_sign` sign context is also added (`idtx_sign_ctx`): it nets the signs of the left (`QuantSign[row*txw + col-1]`), above (`QuantSign[(row-1)*txw + col]`), and above-left (`QuantSign[(row-1)*txw + col-1]`) coefficients into `signc`, maps it to a base ctx (5 for signc>2, 6 for signc<-2, 1 for signc>0, 2 for signc<0, else 0), then adds 2 when the current `Level[row][col]` exceeds `COEFF_BASE_RANGE` and the base ctx is non-zero; edge neighbours are gated by col>0/row>0 and the flat index is saturating + slice-bounds-guarded (total, panic-free). With this, ALL §8.3.2 coefficient-symbol CDF contexts are now derived (eob/bob position, coeff_br, the idtx magnitude variants, coeff_base, and the dc_sign/idtx_sign sign contexts). The runtime state these read — the per-transform-block Level[]/QuantSign[] and the Above/Left DC-context tile buffers — is tracked separately by `DECODE-TILE-COEFF-STATE-BUFFERS`; what remains here is wiring those buffers into the §5.20.7.27 coeffs() loop that fills them and consumes these contexts. The individual partition-entry `S()` read boundary is tracked separately by `DECODE-TILE-PARTITION-SYMBOL-READ-BOUNDARY`; the final one-decision consumer over caller-provided facts is tracked separately by `DECODE-TILE-PARTITION-DECISION-BOUNDARY`; supported-subset Tile-to-Saved and Saved-to-Frame lifecycle behavior is tracked separately by `DECODE-TILE-CDF-SAVE-LIFECYCLE-BOUNDARY`. This is a boundary only: it does not itself implement partition decisions, full §8.3 CDF selection, full Tile/Saved CDF banks, recursive `decode_tile()`/`read_partition()` traversal, broad `exit_symbol()` after real syntax, broad CDF lifecycle outside the supported subset, reconstruction, hashes, runtime Y4M output, reference refresh, public API support, AVM/dav2d invocation, or scheduler use outside the `splot_parallel::WorkerPool` policy."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::out_of_frame_neighbours_give_context_zero",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::non_directional_neighbour_keeps_context_zero",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::directional_left_neighbour_raises_context_to_one",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::directional_above_neighbour_raises_context_to_one",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::directional_both_neighbours_raise_context_to_two",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::last_non_directional_mode_does_not_raise_the_context",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::minimal_y_mode_reconstruction_maps_set0_index0_to_dc_pred",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::minimal_y_mode_reconstruction_covers_the_non_directional_subset",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::minimal_y_mode_reconstruction_rejects_unsupported_inputs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::uv_mode_ctx_is_zero_for_dc_pred_and_one_for_directional",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::luma_txb_skip_ctx_first_block_filling_transform_is_zero",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::luma_txb_skip_ctx_uses_min_clamped_level_sum_when_not_filling",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::luma_txb_skip_ctx_fsc_selects_last_context",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::v_txb_skip_ctx_first_block_larger_chroma_is_three",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::v_txb_skip_ctx_adds_neighbour_chroma_and_eob_contributions",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::eob_extra_selector_returns_rows_and_bounds_error",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::eob_extra_tile_copy_does_not_alias_the_frame",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::eob_pt_family_loads_defaults_and_selects_by_size_and_context",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::eob_pt_selector_rejects_out_of_range_contexts",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::eob_pt_tile_copy_does_not_alias_the_frame",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::dc_sign_loads_defaults_and_selects_by_all_indices",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::dc_sign_selector_rejects_out_of_range_indices",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::dc_sign_tile_copy_does_not_alias_the_frame",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::txb_skip_plane_type_error_still_names_txb_skip",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_eob_partitions_the_scan_position",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_eob_smallest_block",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_eob_is_total_for_out_of_range_shift",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_bob_partitions_the_begin_position",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_bob_zero_segment_eob",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_br_contexts_cover_neighbour_and_plane_rules",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_br_is_total_for_out_of_bounds_and_short_slices",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_br_is_total_for_pathological_geometry",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_idtx_sums_clamped_left_and_above",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_idtx_contexts_cover_missing_neighbours_and_clamps",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_idtx_is_total_for_short_slice_and_pathological_geometry",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_luma_hf_2d_position_buckets",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_luma_hf_non_2d_adds_fifteen",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_luma_lf_covers_2d_and_directional_branches",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_chroma_uv_branches",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_sums_clamped_neighbours_into_hf",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_low_frequency_maglimit_raises_to_five",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_parity_hidden_overrides_and_caps_maglimit",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_chroma_2d_reads_three_neighbours_not_five",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::coeff_base_is_total_for_short_slice_and_pathological_geometry",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::dc_sign_ctx_nets_above_and_left_votes",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::dc_sign_ctx_honours_the_position_offset_and_max_bounds",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::dc_sign_ctx_is_total_for_pathological_geometry",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::idtx_sign_ctx_maps_signc_to_base_context",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::idtx_sign_ctx_level_threshold_raises_nonzero_context",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::idtx_sign_ctx_skips_missing_edge_neighbours",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs::tests::idtx_sign_ctx_is_total_for_short_slices_and_pathological_geometry",
]
commands = [
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask ci",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-TILE-COEFF-STATE-BUFFERS"
name = "Tile coefficient state buffers"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.4.1", "5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs"
change_id = "tile-coeff-state-buffers"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient storage: TransformCoeffBlockState carries local Level, QuantSign, and Quant arrays, while TileCoeffContextState supplies and commits above/left level and DC contexts for both staged branches."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_state_tests.rs::update_after_coeffs_writes_above_and_left_ranges_only", "crates/splot-decode/src/bitstream/tile_payload/coeff_state_tests.rs::update_after_coeffs_rejects_bad_facts_without_mutation"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-COEFF-ALL-ZERO-CONTEXT-STATE"
name = "Coefficient all_zero context state handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-all-zero-context-state"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live all-zero path in decode_general_intra_plane_coeffs: it derives the txb-skip selector from tile level/DC context lines, reads the symbol, and commits zero context output directly."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::luma_skip_fixture_decodes_skip_branch_through_general_path", "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::cctx_pair_uses_u_transform_type_for_all_zero_v_block"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ALL-ZERO-BLOCK-STATE"
name = "Coefficient all_zero block state handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-all-zero-block-state"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient all_zero block state handoff milestone. Its predecessor wrapper was removed; the direct all-zero branch plus read_nonzero_coeff_block_start in decode_general_intra_plane_coeffs is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::luma_skip_fixture_decodes_skip_branch_through_general_path", "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::cctx_pair_uses_u_transform_type_for_all_zero_v_block"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-EOB-VALUE-STATE"
name = "Coefficient EOB value state"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs"
change_id = "coeff-eob-value-state"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live EOB arithmetic in nonzero_coeff_eob, with direct, refined, maximum, and rejected refinement cases covered by focused tests."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::nonzero_eob_covers_direct_refined_and_max_values", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::nonzero_eob_rejects_invalid_points_and_refinements"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-EOB-SYMBOL-READ"
name = "Coefficient EOB symbol read"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs"
change_id = "coeff-eob-symbol-read"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live EOB syntax in read_nonzero_coeff_eob. Focused tests cover selected CDF and literal reads, disabled updates, pre-consumption selector rejection, truncation, and wrapped offsets."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_uses_selected_cdfs_and_literal_refinement", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_honors_disabled_cdf_updates", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_rejects_selector_before_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_reports_truncation_and_wrapped_offsets"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_read.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-EOB-SIZE-CONTEXT"
name = "Coefficient EOB size context"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs"
change_id = "coeff-eob-size-context"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live EOB size/context derivation through read_nonzero_coeff_eob_from_context from real transform log2, plane, inter, and q-context facts before either staged branch."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_uses_selected_cdfs_and_literal_refinement", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-EOB-DERIVED-SYMBOL-READ"
name = "Coefficient EOB derived symbol read"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs"
change_id = "coeff-eob-derived-symbol-read"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live composition from transform facts to selected EOB syntax through read_nonzero_coeff_eob_from_context; focused EOB and staged branch tests protect the surviving path."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs::live_eob_read_uses_selected_cdfs_and_literal_refinement", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-EOB-BRANCH-HANDOFF"
name = "Coefficient EOB branch handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs"
change_id = "coeff-eob-branch-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient eob branch handoff milestone. Its predecessor wrapper was removed; the direct all-zero branch plus read_nonzero_coeff_block_start in decode_general_intra_plane_coeffs is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::luma_skip_fixture_decodes_skip_branch_through_general_path", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-NONZERO-BLOCK-STATE"
name = "Coefficient nonzero block state"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs"
change_id = "coeff-nonzero-block-state"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live nonzero start in read_nonzero_coeff_block_start: checked local state and EOB feed both staged ordinary and FSC branches."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-SCAN-WALK"
name = "Coefficient scan walk"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs"
change_id = "coeff-scan-walk"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient scan walk in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-SEQUENCE-CROP-WINDOW"
name = "Sequence crop-window decode and output"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.4.1", "6.4.1", "5.18.4.4", "6.17.4.4", "7.21.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-4-4",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/frame_lifecycle.rs"
change_id = "decode-sequence-crop-window"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements sequence crop windows through decoded output. The runtime derives per-frame CropLeft, CropRight, CropTop, CropBottom, CropWidth, and CropHeight with the proportional integer formulas in AV2 §5.18.4.4, validates positive extents and subsampling-aligned crop origins through the existing splot-recon frame model per §6.17.4.4, retains full coded storage for reconstruction/reference use, and exposes only the §7.21.2 visible luma/chroma rectangles to hash, raw, and Y4M output. The committed 64x64 8-bit 4:2:0 fixture signals two samples of crop on each side, produces a 60x60 visible luma rectangle at origin (2,2), and matches both the pinned and current avmdec raw output byte-for-byte."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/frame_lifecycle.rs::crop_tests",
    "crates/splot-decode/src/output/hash.rs::tests::crop_window_fixture_reports_and_hashes_only_visible_samples",
]
commands = [
    "cargo test -p splot-decode crop_window --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-crop-intra-64x64-q80.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = []

[[feature]]
id = "DECODE-GENERAL-INTRA-FRAME-FRONTIER"
name = "General intra frame decode frontier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.2", "5.20.1", "5.20.3.1", "5.20.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-general-intra-frame-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private general intra decode frontier in `splot-decode` that runs alongside the frozen minimal hash tier. `route_general_minimal_intra` plus `is_general_minimal_intra` accept a single-tile 64x64 8-bit 4:2:0 intra key frame with any `base_q_idx` other than the frozen fixture's 255 and with segmentation, quant matrices, delta-Q, in-loop filters, CCSO, GDF, and film grain all disabled; `decode_general_minimal_intra_frame` derives the one tile work unit and runs the real AV2 §5.20.3.1 root partition traversal to the single-block frontier. It then decodes the AV2 §5.20.5.3 block mode info (`DECODE-GENERAL-INTRA-BLOCK-MODES`), the §5.20.7.27 luma transform-block coefficients (`DECODE-GENERAL-INTRA-LUMA-COEFFS`), and the chroma coefficients plus dequant / inverse-transform / residual-add / frame assembly (`DECODE-GENERAL-INTRA-FRAME-RECON`), reconstructing the full frame; it never mutates the frozen `base_q_idx == 255` minimal hash contract. The committed `syn-flat-intra-64x64-q80.ivf` fixture carries a real nonzero DC residual and decodes bit-exactly to the avmdec/dav2d oracle (flat Y=100, U=120, V=130). The frontier does not yet handle split partitions, multiple blocks, multiple tiles, non-64x64 frames, inter prediction, in-loop filters, or invoke AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::decode_general_intra_fixture_reconstructs_full_frame",
    "crates/splot-cli/tests/decode_cli.rs::general_intra_fixtures_decode_to_distinct_pinned_hashes",
]
commands = [
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-cli/tests/decode_cli.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-BLOCK-MODES"
name = "General intra block mode-info decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.9", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-9",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs"
change_id = "decode-general-intra-block-modes"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds crate-private general intra block mode-info decode in `splot-decode` for the AVM-oracle general intra path, decoding the AV2 §5.20.5.3 `intra_frame_mode_info` mode symbols in spec order without the frozen minimal-tier trace's value assertions. `decode_general_intra_block_modes` reads `read_intra_y_mode` (`y_mode_set`, then `y_mode_index` with the §8.3.2 tile-origin context) and reconstructs the typed non-directional luma `YMode`, then `read_intra_uv_mode` (`uv_mode` with the §8.3.2 `is_directional_mode(YMode)` context, plus the `uv_mode == CHROMA_MODE_COUNT - 1` `uv_mode_idx` escape). It is wired into the general intra frame path after the §5.20.3.1 partition frontier, advancing the rejection from the partition frontier to the residual step. For the minimal-tool subset (no intra block copy, segmentation, GDF, CDEF, CCSO, delta-Q, lossless DPCM, palette, DIP, FSC, MRL, CfL, or MHCCP) those tool branches read no symbols. Follow-on DIP syntax support exposes generated `TileUseDipCdf` / `TileDipModeCdf` rows, retains tile-local `UseDip` NPos context, carries `enable_dip` from `sequence_intra_config()`, and consumes AV2 §5.20.7.9 `dip_mode_info()` after palette for enabled DC luma blocks with `PaletteSizeY == 0`, reading `use_dip`, `dip_transpose`, and `dip_mode` as metadata so active DIP streams remain entropy-synchronized. It does not implement the §7.13.2.4-§7.13.2.6 data-driven intra prediction process, reconstruct the typed `UVMode`, decode the residual or transform-block syntax, read coefficient symbols, write `Quant`, dequantize, inverse transform, add residuals, reconstruct pixels, handle the directional / escape / second-mode `YMode` reconstruction paths, or invoke AVM/dav2d."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::decodes_dc_luma_mode_and_a_chroma_mode_in_spec_order",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::active_dip_mode_info_reads_flag_transpose_and_mode_after_palette",
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs::tests::dip_cdf_selectors_match_generated_defaults_and_check_bounds",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::fsc_and_use_dip_neighbours_select_context_sum",
]
commands = [
    "cargo test -p splot-decode general_intra_block --locked",
    "cargo test -p splot-decode dip_cdf_selectors_match_generated_defaults_and_check_bounds --locked",
    "cargo test -p splot-decode use_dip --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-q80.ivf",
]
diagnostics = ["decode/unsupported-feature", "decode/malformed-source"]

[[feature]]
id = "DECODE-GENERAL-INTRA-LUMA-PALETTE"
name = "General intra luma palette token decode and prediction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.8.1", "5.20.8.4", "6.19.8.1", "6.19.8.3", "7.13.4", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-4",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-8-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/residual/pipeline/palette.rs"
change_id = "decode-general-intra-luma-palette"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "The existing general-intra path decodes §5.20.8.4 luma palette direction, identity-row, first-sample nonsymmetric literal, context-selected color-index symbols, edge padding, and §7.13.4 palette prediction. This change removes the former mixed palette unsupported marker: input-origin palette token failures are typed malformed source under §5.20.8.4; the forbidden first-line identity_row_y == 2 value is a typed §6.19.8.3 conformance error; CDF/arithmetic-state failures and color-index escapes are typed header-state invariants without global reason ids. The selector proof exhaustively checks the 2..8 palette-size alphabet and every neighbor-derived ColorOrder prefix. The pinned 68-byte AVMenc fixture is a strict-clean 16x16 monochrome, 2-color, row-major palette stream; it is byte-distinct from the palette-disabled encode and splot matches pinned AVM at one and eight threads over the complete 256-byte native monochrome output SHA-256 418ecf79e4b2564419678cb25c28cbbf0259bf4602dd54f3e2868529d4df261d. This proof does not claim every palette size, palette direction, identity-row mode, chroma format, or partition shape."
replaces = []

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/general_intra_unit_tests.rs::palette_source_failures_are_malformed_at_the_palette_token_boundary",
    "crates/splot-decode/src/pipeline/general_intra_unit_tests.rs::palette_first_line_copy_is_a_typed_conformance_error",
    "crates/splot-decode/src/pipeline/general_intra_unit_tests.rs::palette_entropy_failures_are_typed_internal_and_fail_atomic",
    "crates/splot-decode/src/residual/pipeline/palette.rs::tests::palette_cdf_alphabets_and_color_order_stay_inside_palette_size",
    "crates/splot-decode/src/output/raw_tests.rs::truncated_palette_tile_terminal_fails_closed_before_raw_output",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode palette --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-luma-palette2-row-mono-16x16-q0.ivf",
    "tests/conformance/decoder-oracle.toml",
    "tools/decoder-fixtures/generate.py",
]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-GENERAL-INTRA-LUMA-COEFFS"
name = "General intra luma coefficient decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-general-intra-luma-coeffs"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current runtime plane coefficient decode handles the direct all-zero path and routes nonzero blocks through staged ordinary or FSC decoding; oracle tests protect reconstructed output."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::luma_skip_fixture_decodes_skip_branch_through_general_path", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-q80.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-FRAME-RECON"
name = "General intra full frame reconstruction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "7.13.2", "7.14.2", "7.14.4", "7.15.4", "7.14.3", "8.2.4", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "decode-general-intra-frame-recon"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical predecessor milestone (the helper named below was later removed after staged runtime integration; the live proof now points to the current staged/direct path). Completes the first end-to-end AVM-oracle general intra frame decode. After the §5.20.5.3 mode info and the §5.20.7.27 luma transform-block coefficients, `decode_general_intra_chroma_coeffs` decodes the U then V 32x32 chroma blocks: the `all_zero` (`txb_skip`) symbol uses the §8 parsing CDF `TileTxbSkipCdf[is_inter || fsc_mode][txSzCtx][ctx]` for plane 0/1 (the second index is is_inter||fsc_mode == 0 here, NOT plane_type; the U-plane offset lives in ctx == (above!=0)+(left!=0)+6 == 6 for the first block) and `TileVTxbSkipCdf[ctx]` for plane 2 (ctx == (EobU!=0)?6:0); the nonzero pass reuses `apply_coeff_use_fsc_branch_from_frame_facts` with the chroma plane. `reconstruct_general_intra_block` then composes §7.14.4 dequant (with `dqDenom = 1 << ((pels>256)+(pels>1024)+useTcq)`, the §7.14.4 TCQ term applying to the luma DCT_DCT block only), the §7.15.4 inverse transform (64x64 reconstructed via the adjusted 32x32 inverse plus duplication for luma, native 32x32 for chroma), and §7.14.3 residual add over the §7.13.2 no-neighbour DC prediction (128 for 8-bit). The single 64x64 block consumes the whole tile payload, so §8.2.4 `exit_symbol()` is validated after the coefficients (a failure is reported as `general_intra_exit_symbol`). `assemble_general_intra_frame` builds the decoded 8-bit 4:2:0 frame from the three reconstructed planes. The committed `syn-flat-intra-64x64-q80.ivf` (single DC coefficient per plane) and `syn-cos-intra-64x64-q180.ivf` (a low-frequency half-cosine giving the luma block multiple `eob > 1` AC coefficients) both decode bit-exactly to the avmdec/dav2d oracle (q80 flat Y=100/U=120/V=130, pinned hash `ce9c46b1078b9dd593254837ead7dcd6cee8b3ec6cc3c7d34f54fb08df703979`; q180 cosine luma + flat chroma, pinned hash `8a6751d4517073bad0bbe71f4b5537df8e8b0bfee85fcd6af1ac2d5878dd59e8`) end-to-end through the CLI. Multi-coefficient (`eob > 1`) blocks are decoded by the same §5.20.7.27 coefficient loop: with the transform-type and cross-component tools rejected at admission the inferred DCT_DCT transform applies and no `intra_tx_type` / `cctx_type` symbol is coded, so the AC levels are read directly; §8.2.4 `exit_symbol()` is the bit-exactness guard that rejects any unconsumed syntax rather than returning a wrong hash. To avoid a confident-but-wrong hash, admission and decode reject everything outside the verified subset (reconstruction passes zero deltas, so all resolved quantizer deltas must be zero): nonzero §5.18.6.1 frame DeltaQ values, nonzero §5.4.8 base offsets (BaseYDcDeltaQ via `!equal_ac_dc_q`; BaseUVDcDeltaQ / BaseUVAcDeltaQ derived independently), `enable_fsc`/`enable_cctx`/`enable_idtx_intra`/`enable_intra_ist` (transform-type and cross-component syntax), `TX_MODE_SELECT`, and non-DC luma/chroma prediction modes (rejected after mode decode). The configured decode limits are enforced before any reconstruction buffer is allocated. It does not yet handle split partitions, multiple blocks, multiple tiles, non-64x64 frames, those rejected tools, inter prediction, in-loop filters, or invoke AVM/dav2d live in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q80_intra_frame_reconstructs_flat_planes",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q80_intra_frame_hash_is_stable",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q180_cos_intra_frame_decodes_multi_coefficient_luma",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::luma_skip_fixture_decodes_skip_branch_through_general_path",
    "crates/splot-cli/tests/decode_cli.rs::decode_general_intra_fixture_reconstructs_full_frame",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-cos-intra-64x64-q180.ivf",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-cli/tests/decode_cli.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-MULTIBLOCK"
name = "General intra multi-block partition decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.1", "5.20.4.1", "5.20.5.3", "5.20.7.27", "7.13.2", "8.2.4", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-general-intra-multiblock"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the general intra decode from a single block to a SPLIT-partition multi-block frame. `decode_general_intra_partition_tree` walks the full AV2 §5.20.3.1 partition tree (depth-first), reading partition-split symbols and per-block syntax interleaved on one live symbol decoder and the tile CDFs, maintaining the §5.20.4.1 MI-size partition context across blocks (`TileMiSizeState`). At each PARTITION_NONE leaf, `decode_one_general_intra_block` decodes the §5.20.5.3 mode info and the §5.20.7.27 Y/U/V coefficients and reconstructs the block into a persistent `CurrentFrameWorkspace` in decode order, so each non-first block's §7.13.2 DC prediction reads its already-reconstructed above/left neighbours. The §8.3.2 coefficient contexts are derived from the persistent `TileCoeffContextState` neighbour lines (txb_skip from the above/left level/DC OR-reductions; dc_sign and the per-block context commit are handled by the nonzero coefficient pass). The single-block path is unified through this driver. The committed `syn-quad-intra-64x64-q80.ivf` (four flat quadrants -> Horz/Vert/Vert -> four square 32x32 DC blocks) decodes bit-exactly to the avmdec/dav2d oracle (pinned frame hash `c54ed4e996841e2178e74033d765dda1e1127d5d89c3012be3266c3e24a7fd28`), and the single-block q80/cos fixtures are unchanged. Gated to DC_PRED square blocks of at least 8x8 luma with chroma present (so mode contexts reduce to the tile-origin ctx 0) with the §8.2.4 `exit_symbol()` bit-exactness guard; non-DC modes, non-square (rectangular leaf) partitions, sub-8x8 luma leaves (deferred 4:2:0 chroma sizing) and luma-only blocks (both rejected before the mode read so an absent `uv_mode` symbol is never consumed), multiple tiles, non-64x64 frames, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::quad_multiblock_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q80_intra_frame_reconstructs_flat_planes",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-quad-intra-64x64-q80.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-DEEP-SPLIT"
name = "General intra deeper (sub-32x32) square SPLIT partition decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.3", "5.20.3.1", "5.20.4.1", "7.13.2.10"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-general-intra-deep-split"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Proves the general intra decode handles a partition tree TWO levels deep: a 64x64 superblock SPLITs into four 32x32 quadrants and one 32x32 SPLITs AGAIN into four square 16x16 DC_PRED leaves (the other three quadrants stay 32x32 DC_PRED). One level deeper than the `syn-quad` merged-quad case (a single 32x32 split). The §5.20.4.1 partition recursion (`child_calls` PARTITION_SPLIT) already pushes the 16x16 children depth-first, and each 16x16 leaf's §7.13.2.10 DC intra prediction reads its in-frame left column and above row from the persistent `CurrentFrameWorkspace`; the §5.20.2.3 left-and-above availability for the DC predictor is frame-position-based (`x==0` and `y==0`), so a sub-32x32 16x16 leaf correctly DC-predicts from the already-reconstructed sibling 16x16 neighbour INSIDE the parent 32x32 sub-block with no §5.20.2.3 `BlockDecoded` flag state required (the DC predictor never reads the above-right or below-left sentinels that `BlockDecoded` gates). No production code changed: the deeper square DC split is bit-identical to the verified shared `reconstruct_general_intra_block_into` and partition-walk code, just at a smaller transform log2 and one recursion level deeper. The committed `syn-deep-intra-64x64-q120.ivf` decodes bit-exactly to the avmdec and dav2d oracle (raw md5 5e3484135608ea2cd1457d4c50df7855, pinned frame hash `73123e51c66787b59fb6b93a6221e9d78a550c6e0d1c4e0c1adfd21a41ed39ab`), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Still deferred: the §5.20.2.3 per-block `BlockDecoded` flag state itself, which is only needed for non-DC or SMOOTH sub-superblock blocks that read the §7.13.2.1 above-right or below-left sentinels (those modes stay gated to the full 64x64 superblock); rectangular-leaf and non-DC sub-32x32 partitions still reject with `decode/unsupported-feature`; non-64x64 frames, inter prediction, in-loop filters, and live AVM and dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::deep_split_sub_32x32_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-deep-intra-64x64-q120.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-BLOCK-DECODED"
name = "General intra per-block BlockDecoded state for SMOOTH_H split sub-block above-right"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.3", "5.20.7.25", "7.13.2.1", "7.13.2.13", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/block_decoded_state.rs"
change_id = "decode-general-intra-block-decoded"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements the AV2 §5.20.2.3 per-block `BlockDecoded` flag state that the DECODE-GENERAL-INTRA-DEEP-SPLIT brick explicitly deferred (DC reads only the immediate left/above so it needs no `BlockDecoded`; SMOOTH reads the §7.13.2.1 above-right `AboveRow[w]` / below-left `LeftCol[h]` sentinels, whose availability §7.13.2.1 derives from `BlockDecoded`). New crate-private `crates/splot-decode/src/bitstream/tile_payload/block_decoded_state.rs` (`TileBlockDecodedState`) holds a superblock-relative per-plane decoded-flag grid (index ranges y in [-1, sbSize4>>subY], x in [-1, (2*sbSize4)>>subX], the -1 edge stored at offset +1), with `clear_superblock` faithful to §5.20.2.3 `clear_block_decoded_flags(r, c, sbSize4)` (incl the line-8830 below-left corner-zero override) (above row decoded for x < sbWidth4 = (MiColEnd - c)>>subX, left column for y < sbHeight4 = (MiRowEnd - r)>>subY, the corner [-1][-1] = 1), `count_top_right_avail`/`count_bottom_left_avail` (§5.20.7.25), and `set_block` (§5.20.4 `BlockDecoded[plane][(subBlockMiRow>>subY)+i][(subBlockMiCol>>subX)+j] = 1`). The §5.20.3.1 partition walk (`decode_general_intra_partition_tree`) creates the grid, calls `clear_superblock` at each §5.20.2.1 superblock, passes it read-only to the leaf callback, and marks each decoded transform block's plane 4x4 units after the leaf (single full-block transform under TX_MODE_LARGEST). The general intra leaf now derives the luma §7.13.2.1 `num4AboveRight` from `count_top_right_avail` over the real grid (new `luma_num4_above_right_from_block_decoded`) instead of the full-superblock-only `full_sb_num4_above_right` approximation, so a SPLIT child's above-right sibling is counted: this lifts the prior `general_intra_multiblock_non_dc_subblock` reject for a SMOOTH_H luma SPLIT sub-block of size >= 32x32 (TX_SET_DCTONLY) that reads a real reconstructed above-right sentinel. The committed `syn-shsplit-intra-64x64-q80.ivf` (64x64 superblock SPLIT into four 32x32: TL flat 50, TR flat 210, BR flat 130 all DC_PRED, BL 32x32 horizontal-ramp SMOOTH_H_PRED, distinct flat chroma) decodes bit-exactly to the avmdec AND dav2d oracle (raw md5 88ea298073104752646aab5f718fdc31, pinned frame hash `296f15949d88b26b5797bffdb15c6c36dc46bf6976bad59f7995e2443e1b418a`), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. The bottom-left 32x32 SMOOTH_H block (superblock-relative MI (8, 0), x4=0 y4=8 w4=8) reads `count_top_right_avail` = 8 over the decoded top-right 32x32 sibling, so its §7.13.2.1 `AboveRow[w]` sentinel is the real reconstructed top-right corner (210); the reconstructed right column is 211, NOT the ~51 the OLD edge-clamp would produce (empirically confirmed, proving the BlockDecoded path materially changed the output). This is also the first luma (sub_x=0) above-right VALUE oracle anchor (the prior `general_intra_smooth_h_above_right_unverified` deferral noted only the sub_x=1 SMOOTH chroma grid and the value-ignoring SMOOTH_V were verified). VERIFIED-SUBSET DISCIPLINE: only SMOOTH_H luma SPLIT sub-blocks at superblock-relative MI row > 0 (frontier.r % FULL_SB_N4_LUMA != 0, a WITHIN-superblock above-right sibling, the case the syn-shsplit fixture verifies) are admitted; a superblock-relative-row-0 SMOOTH_H split sub-block reads a cross-superblock above-right (the deferred row>0 luma above-right VALUE path, general_intra_smooth_h_above_right_unverified) and is rejected; SMOOTH_V luma sub-blocks (which read the below-left sentinel `LeftCol[h]`, a separate not-yet-fixtured path) remain deferred. SMOOTH chroma sub-blocks already decoded on main through the same BlockDecoded sentinel path. The earlier negative note for `syn-svsplit-intra-64x64-q140.ivf` was stale; the committed fixture decodes bit-exactly with pinned AVM, current AVM, dav2d, and splot; splot's complete raw SHA-256 is `c2b46d534744db9fc146b460f438a6ae450c711e43e537f80a13886297ddbaa5`. The large-chroma §7.13.2.1 override is now applied centrally after raw corner availability is derived: for U/V transforms wider than 32, `num4AboveRight` is zero; for U/V transforms taller than 32, `num4BelowLeft` is zero; the two axes are independent and luma is unchanged. A separate uncapped above-right query is used only by the existing 10-bit D45/D67 capability guard so this correction does not broaden its verified subset. The committed 105-byte `syn-chroma-corner-cap-256x256-q140.ivf` contains a decisive 64x64 all-zero-residual U/V SMOOTH block with distinct in-block versus decoded above-right sentinels; pre-fix splot differed across 16,192 chroma samples, while fixed splot, pinned AVM, and dav2d agree at raw SHA-256 `9d1331b7f113bcd0de59adfd04681009dd8a8c03b18d7ddbd651058fb406435c`. Focused tests pin 64x64, 64x32, 32x64, 32x32, and luma cap behavior, repeated-edge prediction, fixture byte-exactness, and the unchanged right-edge directional admission boundary. All existing general-intra fixtures still decode bit-exact. Out of scope / deferred: SMOOTH_V below-left sub-block sentinels, directional (D135) sub-blocks (need the mode-dependent transform type too), non-DCTONLY-size (<32x32) non-DC sub-blocks, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::smooth_chroma_split_subblock_decodes_to_oracle",
    "crates/splot-decode/src/tile/block_context.rs::tests::large_chroma_smooth_corner_cap_fixture_decodes_to_oracle",
    "crates/splot-decode/src/tile/block_context.rs::tests::chroma_corner_counts_follow_large_transform_dimension_caps",
    "crates/splot-decode/src/tile/block_context.rs::tests::large_chroma_smooth_repeats_top_edge_instead_of_decoded_above_right",
    "crates/splot-decode/src/bitstream/tile_payload.rs::block_decoded_state::tests::split_bottom_left_reads_decoded_top_right_sibling",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-decode block_decoded --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-shsplit-intra-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-svsplit-intra-64x64-q140.ivf",
    "tests/conformance/vectors/valid/syn-chroma-corner-cap-256x256-q140.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/block_decoded_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-NONDC-LUMA-SMOOTH"
name = "General intra single-block non-DC luma (smooth) decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.13", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-nondc-smooth"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the general intra decode from DC-only to the first non-DC luma prediction modes: §7.13.2.13 SMOOTH_V_PRED and SMOOTH_H_PRED. `reconstruct_general_intra_luma_nondc_first_block_into` builds the smooth prediction over the §7.13.2.1 no-neighbour fallback edges (8-bit: AboveRow `127`, LeftCol `129`, the smooth sentinels sharing those fallbacks) via the shared `splot-recon` `predict_intra_smooth_rect_into`, then adds the §5.20.7.27 AC residual through the refactored `reconstruct_general_intra_block_with_prediction` (the DC path is now the flat-prediction special case). The committed `syn-vsmooth-intra-64x64-q120.ivf` (vertical-gradient -> one 64x64 SMOOTH_V luma block) and `syn-hsmooth-intra-64x64-q120.ivf` (horizontal-gradient -> SMOOTH_H) decode bit-exactly to the avmdec/dav2d oracle (pinned frame hashes `3aebe2eb215d4878bbc40aa2f97e2178b6140ef51c03afaaae478e69dbbf6bcd` and `cfc6debd26760cdebf1d1a4497792461f0f68bc7e7773741ddf2cbc34561e702`), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Gated to the top-left (no-neighbour) block of size >= 32x32 with DC chroma: for square intra blocks of size >= 32x32 the §5.20.8.2 `get_tx_set` returns TX_SET_DCTONLY (`!is_inter && txSzSqrUp >= TX_32X32`), so the transform is forced DCT_DCT with no `intra_tx_type` signaled, matching the DCT_DCT residual path; smaller non-DC blocks (8x8/16x16) can carry a mode-dependent (non-DCT) TxType and are rejected. The remaining non-DC modes (SMOOTH, PAETH), directional modes, multi-block non-DC prediction (which reads reconstructed neighbours), non-DC chroma, sub-32x32 non-DC blocks, non-64x64 frames, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::vsmooth_single_block_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::hsmooth_single_block_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-vsmooth-intra-64x64-q120.ivf",
    "tests/conformance/vectors/valid/syn-hsmooth-intra-64x64-q120.ivf",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-NONDC-LUMA-SMOOTH-PLAIN"
name = "General intra single-block plain SMOOTH (2-D) luma decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.13", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-nondc-smooth-plain"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the general intra non-DC luma decode from SMOOTH_V/H to the plain §7.13.2.13 SMOOTH_PRED mode (canonical §9.2 mode 9, §5.20.5.5 modeIdx 1 in the non-directional set), decoded as a non-escape `y_mode_index == 1` (`Reordered_Y_Mode[1] == SMOOTH_PRED`). Unlike SMOOTH_V (`predV2` only) and SMOOTH_H (`predH2` only), plain SMOOTH outputs `Round2(predV2 + predH2, 1)`, blending BOTH the vertical predictor (above row + bottom-left sentinel `LeftCol[h]`) and the horizontal predictor (left column + top-right sentinel `AboveRow[w]`). `block_context.rs` maps `SMOOTH_PRED` -> `SupportedNonDcLumaMode::Smooth`; `pipeline::reconstruct.rs` maps that to `splot-recon` `IntraSmoothMode::Smooth` and reuses `reconstruct_general_intra_luma_nondc_first_block_into` -> `predict_intra_smooth_rect_into` over the §7.13.2.1 no-neighbour fallback edges (8-bit AboveRow `127`, LeftCol `129`, the sentinels sharing those fallbacks), then adds the §5.20.7.27 AC residual. The committed `syn-smooth-intra-64x64-q124.ivf` (a 2-D smooth surface -> one 64x64 plain SMOOTH luma block, DC chroma) decodes bit-exactly to the avmdec/dav2d oracle (md5 82d0f23be478479c9835e9a76e4a879c, pinned frame hash 9b054c6fff47397fbe88a9eb45a34fac018efc7748fc697edebddd3f14bd88d3), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Gated to the top-left (no-neighbour) 64x64 superblock (`n4w == 16`, TX_64X64 -> §5.20.8.2 `get_tx_set` returns TX_SET_DCTONLY -> forced DCT_DCT, no `intra_tx_type`) with DC chroma. The `syn-smoothnondc-intra-64x64-q132.ivf` fixture (plain SMOOTH luma + non-DC H_PRED chroma) now ALSO decodes bit-exactly: its non-follow H_PRED chroma at the no-neighbour top-left 64x64 block reconstructs via DECODE-FIRST-INTER-FRAME-FRONTIER's non-follow-H_PRED-chroma support (a §7.13.2.8 horizontal copy of the §7.13.2.1 flat fallback left column), matching the avmdec/dav2d oracle (md5 70494255beb63103c97422e327243319, pinned frame hash f1621607dfcd2737e8a4c308fc26cd1596cb001444437f0440e34883a59b519b). Neighbour-having plain-SMOOTH blocks (which read the real §7.13.2.1 reconstructed above-right `AboveRow[w]` and below-left `LeftCol[h]` sentinels), sub-64x64 plain-SMOOTH blocks (mode-dependent non-DCT TxType), neighbour-having non-follow H_PRED chroma, plain-SMOOTH chroma, PAETH, non-64x64 frames, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::smooth_single_block_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::smooth_luma_with_non_dc_h_pred_chroma_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-smooth-intra-64x64-q124.ivf",
    "tests/conformance/vectors/valid/syn-smoothnondc-intra-64x64-q132.ivf",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-ANGLE"
name = "General intra single-block directional-angle luma (D135) decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-angle"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the general intra decode to the first directional-angle luma mode: §7.13.2.8 D135_PRED (pAngle 135). Reconstructs the §5.20.5.3 `y_mode_offset` escape (`y_mode_set == 0`, `y_mode_index == MODE_INDEX_COUNT - 1`): the new `TileYModeOffsetCdf` selector reads `y_mode_offset` (sharing the `y_mode_index` §8.3.2 context, defaults from `DEFAULT_Y_MODE_OFFSET_CDF`), then `reconstruct_y_mode_offset_escape_top_left` runs `modeIdx = (MODE_INDEX_COUNT - 1) + y_mode_offset` through `get_intra_y_mode_set` for the top-left no-directional-neighbour case (`Default_Mode_List_Y`) and the directional reorder (`Reordered_Y_Mode`, `TOTAL_ANGLE_DELTA_COUNT`, `MAX_ANGLE_DELTA`) to the typed `YMode`/`AngleDeltaY`. For the hedge fixture: `y_mode_offset == 3` -> modeIdx 10 -> D135_PRED, AngleDeltaY 0. `reconstruct_general_intra_luma_directional_first_block_into` builds the §7.13.2.8 prediction over the §7.13.2.1 no-neighbour fallback edges (8-bit: AboveRow `127`, LeftCol `129`, shared corner `128`) via the shared `splot-recon` `predict_intra_middle_directional_angle_rect_into`, then adds the §5.20.7.27 residual through `reconstruct_general_intra_block_with_prediction`. The committed `syn-hedge-intra-64x64-q80.ivf` (one 64x64 D135 luma block, top half near 40, bottom half near 210, DC chroma U 120 V 130) decodes bit-exactly to the avmdec/dav2d oracle (md5 1179bcc873c1d1ac49c2c032f11ca44d, pinned frame hash b15f267ec6e99ca4d96a70f38bffe5f798ee4c33ad3aaec23761a1ea74b0be33), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Gated to the top-left (no-neighbour) 64x64 superblock (`n4w == 16`, TX_64X64 -> §5.20.8.2 `get_tx_set` returns TX_SET_DCTONLY -> forced DCT_DCT) at pAngle 135 with `AngleDeltaY == 0`: this is the case where `enable_intra_edge_filter == 0`, the IDIF/upsample edge synthesis is a no-op, and pAngle 135's `dx = dy = Dr_Intra_Derivative[45] = 64` give `shift == 0` so the luma IDIF reduces to a sample copy (bit-identical to bilinear). Non-zero angle deltas, the other directional modes, sub-64x64 directional blocks (mode-dependent non-DCT TxType), neighbour-having directional blocks (full §7.13.2.8 IDIF edge synthesis with `shift != 0`), directional chroma beyond the top-left D135-follow case (now supported by DECODE-GENERAL-INTRA-DIRECTIONAL-FOLLOW-CHROMA), non-64x64 frames, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::hedge_directional_d135_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::y_mode_offset_escape_reconstructs_d135",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-decode block_context --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-hedge-intra-64x64-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-MB-NEIGHBOUR-SMOOTH"
name = "General intra multi-block non-DC luma over a reconstructed neighbour edge"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.13", "8.2.4", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-mb-neighbour-smooth"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the general intra decode to the first MULTI-BLOCK non-DC luma prediction that reads the REAL reconstructed neighbour edge (previous non-DC / directional luma was gated to the no-neighbour top-left block over §7.13.2.1 flat fallbacks). The brief expected a cardinal V_PRED/H_PRED right superblock; the committed `syn-mbvg-128x64-q80.ivf` instead codes BOTH 64x64 superblocks as §7.13.2.13 SMOOTH_V_PRED (confirmed by temporary mode instrumentation: `y_mode == SMOOTH_V_PRED == 10` for both, decoded via the §5.20.5.3 `y_mode_offset` escape). SMOOTH_V is not a cardinal copy nor an §7.13.2.8 angle mode — it is linear interpolation over §7.13.2.13, so it reads the real neighbour edge with NO IDIF / edge-filter synthesis, and is bit-exact over a non-flat neighbour edge. The left (top-left, no-neighbour) superblock uses the existing `reconstruct_general_intra_luma_nondc_first_block_into` (flat fallback edges). The right superblock has a left neighbour (`!is_top_left`), so the new `reconstruct_general_intra_luma_nondc_neighbour_block_into` builds the §7.13.2.1 edges from the partially-built frame via the shared (luma/chroma-general) `build_smooth_edges`: `haveLeft == 1, haveAbove == 0` -> `LeftCol[i] = CurrFrame[0][Min(leftLimit, y+i)][x-1]` (the reconstructed left column, bottom-left sentinel `LeftCol[h]` clamped to the last in-block sample since `num4BelowLeft == 0` in raster order) and `AboveRow[i] = CurrFrame[0][y][x-1]` (repeated first left sample); then runs the shared `predict_intra_smooth_rect_into` SMOOTH_V mode and adds the §5.20.7.27 residual. The production `TileIntraJointModeState::y_mode_index_ctx` derives the §8.3.2 context from the current per-MI left and above joint modes: non-directional or out-of-frame neighbours contribute zero, while directional neighbours raise the context to one or two. `decode_general_intra_luma_block_mode_with_fsc_context` uses that context for the live `YModeIndex` and `YModeOffset` CDF selectors and passes the same neighbour joint modes to the current reorder reconstruction. The mbvg left neighbour is SMOOTH_V (`modeDelta == 2`), so its context remains zero; the dedicated directional-neighbour feature covers nonzero-context decoding. The committed `syn-mbvg-intra-128x64-q80.ivf` (vertical gradient top 30 -> bottom 210, flat chroma U=120 V=130) decodes bit-exactly to the avmdec AND dav2d oracle (md5 3e57ba0c8cbdbe1d3400b0ae365c5d8e, pinned frame hash 269b4969800751c63f7f0605f1f7b8f178f7bf85590ec62fe64313ff394d6dfd), guarded by the §8.2.4 `exit_symbol()` bit-exactness check; all existing general-intra fixtures still decode bit-exact. Gated to a full 64x64 superblock (`n4w == 16`): sub-superblock non-DC blocks need the per-block §5.20.2.3 `BlockDecoded` update for the intra-superblock above-right / below-left split neighbours (not yet modelled). Neighbour-having §7.13.2.8 directional (D135) luma over a real non-flat edge (which needs the real IDIF 4-tap interpolation, since `enableIdif == 0` bilinear equals IDIF only for a flat edge), SMOOTH / PAETH luma, non-DC chroma neighbour edges beyond the existing SMOOTH chroma path, sub-superblock non-DC, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work. When this brick landed, the neighbour-having SMOOTH path was gated to the FIRST superblock row (`frontier.r == 0`, `haveAbove == 0`); that gate was subsequently LIFTED by DECODE-GENERAL-INTRA-GRID-NEIGHBOUR-NONDC, which admits a row>0 SMOOTH_V luma block over the real reconstructed above row (SMOOTH_H only where the above-right sentinel is the no-neighbour fallback), so the `general_intra_multirow_neighbour_non_dc` diagnostic is no longer emitted."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::mbvg_multiblock_smooth_v_neighbour_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::directional_neighbour_ctx_reads_with_the_real_context",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::non_directional_left_neighbour_keeps_ctx_zero_and_decodes",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::directional_left_neighbour_raises_context_to_one",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-decode intra_joint_modes --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-mbvg-intra-128x64-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-NON64-MULTISB"
name = "General intra multi-superblock (non-64x64) DC decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.1", "5.20.3.1", "5.20.5.3", "7.13.2.1", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "decode-general-intra-non64-multisb"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Generalizes the general intra decode from a single 64x64 superblock to a single ROW of superblocks: single-tile 8-bit 4:2:0 intra key frames whose width is any positive multiple of 64 and whose height is exactly 64 (e.g. 128x64). The IVF size gate (`require_minimal_ivf`) now admits any positive-sized AV02 frame to routing, and `is_general_minimal_intra` accepts width a multiple of 64 with height 64; the frozen `base_q_idx == 255` hash tier still re-imposes its strict 64x64 requirement in `validate_frame_core`. Height is restricted to 64 (one superblock row) because the §7.13.2.13 SMOOTH chroma path builds its §7.13.2.1 bottom-left sentinel `LeftCol[h]` by edge-clamping (repeating the last neighbour sample), which equals the spec value `CurrFrame[Min(maxY, y+h)][x-1]` only when the chroma block sits at the bottom frame edge with no above neighbour (always true at height 64); taller frames are deferred until that sentinel reads the real below-block reconstructed sample. `new_general_intra_workspace` is parameterized by the real frame size (chroma = half for 4:2:0), and `decode_general_intra_partition_tree` now iterates every superblock in the tile's MI range in raster order per AV2 §5.20.2.1 `decode_tile()` (`sbSize4 = Num_4x4_Blocks_Wide[SbSize]` steps, `clear_left_context()` at each superblock-row start), reusing one `SymbolDecoder`, tile CDFs, `TileMiSizeState`, and frame-spanning `TileCoeffContextState` + workspace so later superblocks DC-predict from already-reconstructed neighbours. Chroma reconstruction adds the §7.13.2.13 SMOOTH_PRED case (resolved from `uv_mode` via §5.20.5.3 `get_intra_uv_mode_set` for the non-directional luma subset) over §7.13.2.1 neighbour edges read from the partially-built frame, since the multi-SB encoder codes the second superblock's (residual-free) chroma as SMOOTH_PRED. The committed `syn-2sb-intra-128x64-q80.ivf` (left flat luma 80, right flat luma 180, chroma flat U=120 V=130) decodes bit-exactly to the avmdec and dav2d oracle (md5 88cf94a2d7b2a20c3212d96acdd456ef, pinned frame hash 18ba32ffb8d818689cbded3dbd5c44602bb091c1f9750c1bb062e6f80498540f), guarded by the §8.2.4 `exit_symbol()` bit-exactness check; existing 64x64 fixtures still decode bit-exact. Multi-superblock-ROW frames (height > 64, deferred for the SMOOTH chroma sentinel reason above), partial frames (non-multiple-of-64 widths needing edge clamping), non-DC/non-SMOOTH chroma, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::two_superblock_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::quad_multiblock_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2sb-intra-128x64-q80.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/mi_size_state.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-MULTIROW"
name = "General intra multi-superblock-row (grid) decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.3", "5.20.2.1", "5.20.4.1", "7.13.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-general-intra-multirow"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Relaxes the general intra frame-size admission from a single superblock ROW (height exactly 64) to also a single superblock COLUMN: `is_general_minimal_intra` now accepts frames whose width and height are positive multiples of 64 AND (width == 64 OR height == 64) — a single row or single column of 64x64 superblocks. The §5.20.2.1 superblock raster loop already iterates rows and columns with `clear_left_context()` at each superblock-row start, and the DC path already reads above neighbours, so a second-row superblock DC-predicts its luma from the reconstructed first-row neighbour. The single-row-or-column restriction keeps every full-superblock §7.13.2.13 SMOOTH chroma block free of a decoded above-right neighbour (a row-0 block has no above; a rightmost-column block has no above-right), so the §7.13.2.1 `AboveRow[w]` sentinel this path builds by edge-clamping (repeat-last) equals the spec value. A 2-D grid is rejected because a non-rightmost row>0 superblock's above-right IS decoded (`clear_block_decoded_flags` marks `BlockDecoded[-1][x] = 1` up to `(MiColEnd - c) >> subX`, which exceeds the superblock width), so its SMOOTH chroma sentinel would need the real `CurrFrame[y-1][x+w]` sample. The committed `syn-2sbcol-intra-64x128-q80.ivf` (two vertically stacked 64x64 DC superblocks, top luma 80, bottom luma 180, chroma 120/130) decodes bit-exactly to the avmdec/dav2d oracle (md5 bd09ea82..., pinned frame hash 3ee739a8...); all single-row 64x64 and 128x64 fixtures are unchanged. 2-D grid frames, distinct content needing directional luma, SMOOTH chroma on sub-partitioned blocks, partial (non-multiple-of-64) frames, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work (a 2-D grid needs the §7.13.2.1 above-right sentinel to read the real reconstructed neighbour)."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::multi_row_superblock_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::two_superblock_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2sbcol-intra-64x128-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-GRID"
name = "General intra full 2-D superblock grid decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.3", "5.20.2.3", "5.20.7.25", "7.13.2.1", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-grid"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Relaxes the general intra frame-size admission from a single superblock ROW or COLUMN to a full 2-D grid: `is_general_minimal_intra` now accepts any frame whose width and height are positive multiples of 64 (dropping the `width == 64 || height == 64` restriction). The enabling fix is the §7.13.2.13 SMOOTH chroma top-right sentinel `AboveRow[w]`: for a full-superblock block at a non-rightmost row>0 superblock the above-right neighbour IS already decoded (`clear_block_decoded_flags` (§5.20.2.3) marks the above row `BlockDecoded[plane][-1][x] = 1` out to `(MiColEnd - c) >> SubsamplingX`, exceeding the superblock width), so the sentinel must read the real reconstructed `CurrFrame[plane][y-1][Min(aboveLimit, x+w)]` rather than the edge-clamped own-top sample. `full_sb_chroma_num4_above_right` (pipeline/mod.rs) derives §7.13.2.1 `num4AboveRight` faithfully to §5.20.7.25 `count_top_right_avail` over that `BlockDecoded` state for the full-superblock case (chroma `w4 = n4w >> SubsamplingX`, sub-block MI position `(0,0)`); `resolve_smooth_above_right_sentinel` (pipeline::reconstruct.rs) then reads the sentinel via the new total/checked `CurrentFrameWorkspace::reconstructed_sample` accessor, with `aboveLimit = Min(maxX, x + w + 4*num4AboveRight - 1)` simplifying to the column `Min(maxX, x+w)` for `num4AboveRight >= 1`. The bottom-left §7.13.2.13 `LeftCol[h]` sentinel is unchanged (in raster decode order the below-left is never decoded yet, `num4BelowLeft == 0`, so the clamp equals the spec value). SMOOTH chroma stays gated to full 64x64 superblock blocks (`n4w == 16`); sub-partitioned SMOOTH chroma needs the per-block §5.20.2.3 `BlockDecoded` update, still future. The committed `syn-grid-intra-128x128-q80.ivf` (uniform luma 100; chroma distinct flat per quadrant except a SMOOTH bottom-left superblock whose above-right is the distinct top-right superblock) decodes bit-exactly to the avmdec/dav2d oracle (md5 dd2fa84f..., pinned frame hash 42bd99fa...); the bottom-left SMOOTH block reads the real above-right (its top-right corner is pulled toward 200, not the clamped own-top 110), and the old repeat-last sentinel mismatched this fixture by 4096 bytes. All single-row/column 64x64, 128x64, and 64x128 fixtures are unchanged. Distinct content needing directional luma, SMOOTH chroma on sub-partitioned blocks, partial (non-multiple-of-64) frames, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI remain future work."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::grid_2d_intra_frame_decodes_to_oracle",
    "crates/splot-recon/src/workspace_tests.rs::workspace_reconstructed_sample_reads_arbitrary_in_storage_samples",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-recon workspace_reconstructed --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-grid-intra-128x128-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-recon/src/workspace.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-GRID-NEIGHBOUR-NONDC"
name = "General intra 2-D grid non-DC SMOOTH luma over a real reconstructed above row"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.3", "5.20.5.3", "5.20.7.25", "5.20.7.27", "7.13.2.1", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-general-intra-grid-neighbour-nondc"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Lifts the prior `DECODE-GENERAL-INTRA-MB-NEIGHBOUR-SMOOTH` restriction that gated the §7.13.2.13 SMOOTH_V/H luma neighbour-edge decode to the FIRST superblock row (`frontier.r == 0`, `haveAbove == 0`, rejecting a row>0 block with the `general_intra_multirow_neighbour_non_dc` diagnostic). A full-superblock SMOOTH_V luma block at ANY 2-D grid position now decodes (SMOOTH_H only in the first superblock row, where its above-right sentinel is the no-neighbour fallback — see Out of scope): the admission match collapses the two prior arms (first-row admit + row>0 reject) into one `n4w == FULL_SB_N4_LUMA` admit, and `nondc_luma_has_neighbour` drops its `&& frontier.r == 0`. The reconstruction is unchanged — `reconstruct_general_intra_luma_nondc_neighbour_block_into` already delegates to the plane-general `reconstruct_general_intra_smooth_over_edges_into`, which builds the §7.13.2.1 edges from the partially-built frame (`haveAbove`/`haveLeft` per position) and resolves the top-right sentinel via `resolve_smooth_above_right_sentinel` over `full_sb_num4_above_right` (§5.20.7.25 `count_top_right_avail` over the §5.20.2.3 `BlockDecoded` state) — the SAME machinery the SMOOTH chroma grid path (`DECODE-GENERAL-INTRA-GRID`) already uses bit-exact. For a row>0 block §7.13.2.1 supplies the REAL reconstructed above row `CurrFrame[0][y-1][...]` (the bottom row of the already-decoded above superblock); SMOOTH_V's predictor reads `AboveRow[j]` and the bottom-left sentinel `LeftCol[h]`, so this is the first general-intra path to read a real reconstructed above row for non-DC luma. SMOOTH prediction is linear interpolation over those edges (no `enable_intra_edge_filter` / IDIF / upsample synthesis), so the non-flat real above-row edge reconstructs bit-exact. The `y_mode_index` §8.3.2 ctx is unchanged (the per-MI `IntraJointModes` grid from `DECODE-GENERAL-INTRA-MB-NEIGHBOUR-SMOOTH`): SMOOTH_V/H are non-directional (`modeDelta < NON_DIRECTIONAL_MODES_COUNT`), so ctx stays 0 and they are admitted; a directional neighbour (`ctx != 0`) is still REJECTED (`general_intra_directional_neighbour_y_mode_index_ctx`). The committed `syn-vgrid-intra-192x128-q120.ivf` (192x128 = three superblock columns by two rows, vertical gradient top 20 -> bottom 230 with a small per-superblock-column tint, flat chroma U=120 V=130) codes the right two columns as SMOOTH_V_PRED luma; the decisive block is the row>0 SMOOTH_V luma superblock at the MIDDLE (non-rightmost) column (`frontier.r == 16`, `frontier.c == 16`), which reads the real reconstructed above row and (being non-rightmost) has a decoded above-right superblock so the `full_sb_num4_above_right` / `AboveRow[w]` resolver runs (SMOOTH_V ignores the top-right sentinel value). It decodes bit-exactly to the avmdec AND dav2d oracle (md5 136a87190eeecb1ccd32e7cf27861c9c, pinned frame hash c62dd0eb74ab1129e9cd4d6a326cfef9026f62ab4144a378b38cb325b45462d2), guarded by the §8.2.4 `exit_symbol()` bit-exactness check; all existing general-intra fixtures still decode bit-exact and the OLD code rejected this frame (verified). Scope: 2-D grid SMOOTH_V luma over a real reconstructed above row at any superblock position, plus SMOOTH_H only in the first superblock row (`frontier.r == 0`, above-right is the no-neighbour fallback), gated to full 64x64 superblock blocks (`n4w == 16`). Out of scope (still rejected / deferred): neighbour-having §7.13.2.8 directional D135 luma over a real edge (now supported by DECODE-GENERAL-INTRA-NEIGHBOUR-DIRECTIONAL: pAngle 135 shift=0 makes bilinear == IDIF == a sample copy even over a non-flat edge); non-D135 real-edge modes, completed by later directional-mode rows, SMOOTH_H luma at superblock row>0 (its §7.13.2.13 predictor reads the real reconstructed above-right sentinel value `AboveRow[w]`, a luma `sub_x == 0` path not yet oracle-fixtured — only the `sub_x == 1` SMOOTH chroma grid and the value-ignoring SMOOTH_V are verified — `general_intra_smooth_h_above_right_unverified`), directional-neighbour `y_mode_index` reorder, SMOOTH / PAETH luma, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::vgrid_multirow_smooth_v_above_row_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::mbvg_multiblock_smooth_v_neighbour_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::grid_2d_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::directional_neighbour_ctx_reads_with_the_real_context",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-vgrid-intra-192x128-q120.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-GRID-SMOOTH-H-ABOVE-RIGHT"
name = "General intra full-superblock SMOOTH_H luma cross-superblock above-right (row>0)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.3", "5.20.5.3", "5.20.7.25", "5.20.7.27", "7.13.2.1", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-general-intra-grid-smooth-h-above-right"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Lifts the prior `DECODE-GENERAL-INTRA-GRID-NEIGHBOUR-NONDC` restriction that gated the full-superblock §7.13.2.13 SMOOTH_H luma neighbour-edge decode to the FIRST superblock row (`frontier.r == 0`, where the §7.13.2.1 above-right sentinel `AboveRow[w]` is the no-neighbour fallback), rejecting a row>0 SMOOTH_H block with `general_intra_smooth_h_above_right_unverified`. A full-superblock SMOOTH_H luma block at ANY 2-D grid position now decodes: the admission match collapses the two prior arms (first-row admit + row>0 reject) into one `n4w == FULL_SB_N4_LUMA` admit. The reconstruction is UNCHANGED — `reconstruct_general_intra_luma_nondc_neighbour_block_into` already delegates to the plane-general `reconstruct_general_intra_smooth_over_edges_into`, which derives the luma §7.13.2.1 `num4AboveRight` from `luma_num4_above_right_from_block_decoded` (§5.20.7.25 `count_top_right_avail` over the §5.20.2.3 `BlockDecoded` state) and resolves the top-right sentinel `AboveRow[w]` via `resolve_smooth_above_right_sentinel` — the SAME plane-general machinery the SMOOTH chroma grid path (`DECODE-GENERAL-INTRA-GRID`, `sub_x == 1`) and the SMOOTH_V grid path (`DECODE-GENERAL-INTRA-GRID-NEIGHBOUR-NONDC`) already exercise; this is the first time it runs for the luma (`sub_x == 0`) above-right VALUE that the SMOOTH_H §7.13.2.13 `predH2` actually reads. For a row>0 superblock the §7.13.2.1 top-right sentinel is the REAL reconstructed bottom row of the already-decoded diagonally-above-right superblock; SMOOTH prediction is linear interpolation over those edges (no `enable_intra_edge_filter` / IDIF / upsample synthesis), so the non-flat real above-right reconstructs bit-exact. The committed `syn-shgrid-intra-128x128-q80.ivf` (128x128 = two superblock columns by two rows: bottom-left 64x64 superblock SMOOTH_H_PRED luma over a horizontal gradient with DC chroma; top-left/bottom-right DC luma; top-right DC luma with full-superblock SMOOTH chroma) codes the decisive bottom-left row>0 non-rightmost SMOOTH_H superblock (`frontier.r == 16`, `frontier.c == 0`): `count_top_right_avail` yields `num4AboveRight == 16`, so the §7.13.2.1 `AboveRow[w]` resolver returns the REAL above-right value 200 (the top-right superblock's reconstructed bottom row at column 64), NOT the edge-clamp 100 (the bottom-left's own above sample at column 63) — empirically confirmed via temporary instrumentation (sentinel=200 vs clamp=100), and the SMOOTH_H rightmost column reconstructs above 200. It decodes bit-exactly to the avmdec AND dav2d oracle (md5 fe420ce870c13a8055aa83fd5aa64740, pinned frame hash d1ce39cc3d79f5c46fdea67ad57ec4edd5dfed088ee39fd7029fda1bbb11e0e8), guarded by the §8.2.4 `exit_symbol()` bit-exactness check; all existing general-intra fixtures still decode bit-exact and the OLD code rejected this frame with `general_intra_smooth_h_above_right_unverified` (verified by temporarily restoring the reject arm). The earlier negative note for `syn-svsplit-intra-64x64-q140.ivf` was stale: the fixture already decoded byte-exactly on main through the shared SMOOTH chroma BlockDecoded path. VERIFIED-SUBSET DISCIPLINE: only the full-superblock (`n4w == FULL_SB_N4_LUMA == 16`) SMOOTH_H row>0 cross-superblock above-right is admitted. Still rejected / deferred: SMOOTH_H sub-partitioned (SPLIT-child) blocks at superblock-relative row 0 (their cross-superblock above-right path remains `general_intra_smooth_h_above_right_unverified`); SMOOTH_V below-left sub-block sentinels; directional / PAETH neighbour cases; multiple tiles; inter prediction; in-loop filters; and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::shgrid_multirow_smooth_h_above_right_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::vgrid_multirow_smooth_v_above_row_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::grid_2d_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::smooth_chroma_split_subblock_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-shgrid-intra-128x128-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-DIRECTIONAL-NEIGHBOUR-CTX"
name = "General intra directional-neighbour Y-mode decode and total reorder"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.5.5", "5.20.7.27", "7.13.2.13", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-5",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs"
change_id = "decode-general-intra-directional-neighbour-ctx"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Completes AV2 §5.20.5.3 directional-neighbour Y-mode selection with bounded ModeIndex and IntraJointMode values 0 through 60. The total reorder follows neighbour order left then above, suppresses duplicate directional modes, expands deltas plus or minus 1 through 4 with wrap, and completes from Default_Mode_List_Y; §5.20.5.5 MRL syntax is retained as a typed MrlSelection. The pinned, conforming syn-dirneigh-reorder-128x64-q196 fixture reaches MI(0,16), ctx 1, set 0, modeIdx 5, left joint mode 8, above 0, selecting 8 instead of the ctx-0 list value 22; pinned AVM and splot raw I420 SHA-256 is a1500046842354466967cd766ae569ed61927ffcf4617cbc471a3c7644d18edd at one and eight threads. Base HEAD already passed that conforming stream without a gap reason, proving the process is live while the removed fallback is invariant. Exhaustive tests cover every mode and neighbour pair. Invalid typed/CDF state is an internal header-state error with no diagnostic and no CDF mutation; input failures remain malformed. SymbolDecoder zero-padding makes branch-local EOF unreachable, so validated writer truncations are rejected by exit_symbol without neighbour-state publication."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::dirneigh_directional_neighbour_ctx_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::directional_neighbour_ctx_reads_with_the_real_context",
    "crates/splot-decode/src/bitstream/tile_payload/intra_joint_modes.rs::tests::directional_left_neighbour_raises_context_to_one",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::neighbour_reorder_is_total_for_every_typed_mode_and_neighbour_pair",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs::tests::writer_produced_luma_path_truncations_fail_at_exit_without_neighbour_publication",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::general_intra_cdf_failures_are_typed_internal_and_do_not_mutate_rows",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-dirneigh-intra-128x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-dirneigh-reorder-128x64-q196.ivf",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_block.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-DIRECTIONAL-FOLLOW-CHROMA"
name = "General intra D135 and directional-follow D135 chroma decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.8", "7.13.2.14", "7.13.2.17", "7.13.2.18", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-17",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-directional-follow-chroma"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Extends the general intra decode to the directional-follow chroma mode the minimal-tool avmenc pairs with a §7.13.2.8 D135_PRED luma block, which the prior chroma admission REJECTED (`general_intra_non_dc_chroma_mode`). When the §5.20.5.3 `read_intra_uv_mode` decodes `uv_mode == 0` over a directional luma, `get_intra_uv_mode_set(0)` returns `YMode` (the directional-follow branch, line 11316 of the §5.20.5.3 mirror), and the spec then sets `AngleDeltaUV = AngleDeltaY` (line 11288). For the supported luma D135 (`AngleDeltaY == 0`) this resolves the chroma to `UVMode == D135_PRED`, `AngleDeltaUV == 0` — a plain §7.13.2.8 middle-angle (pAngle 135) chroma intra prediction, NOT CfL / CCTX / MHCCP (those return UV_CFL_PRED at §5.20.5.3 line 11268 / read separate cross-component syntax and are explicitly out of scope and still rejected). `supported_chroma_mode` distinguishes directional-follow `D135Follow` from explicit non-follow `D135`, and both variants now share the same total §7.13.2.1 edge preparation and §7.13.2.8 bilinear reconstruction. The runtime admits `D135Follow` and explicit `D135` for every valid block position and neighbour state. In the no-neighbour case the chroma plane has no above/left neighbour so the §7.13.2.1 prediction edges reduce to the flat fallbacks (8-bit: AboveRow `127`, LeftCol `129`, shared corner `128`) and the `enable_intra_edge_filter` / IDIF / upsample synthesis is a no-op; pAngle 135's `dx = dy = Dr_Intra_Derivative[45] = 64` give `shift == 0` so the §7.13.2.8 bilinear middle-angle predictor (`enableIdif == 0` for chroma, since `enableIdif = plane == 0`) is a sample copy of the flat fallback edge. The new `reconstruct_general_intra_chroma_directional_first_into` builds that prediction via the shared `splot-recon` `predict_intra_middle_directional_angle_rect_into` (the SAME `predict_directional_noneighbour` helper the luma D135 path uses) and adds the §5.20.7.27 chroma residual through `reconstruct_general_intra_block_with_prediction`. Over the flat fallback edges the D135 chroma prediction is a genuine 135-degree anti-diagonal split (upper-right triangle `127`, lower-left `129`, anti-diagonal `128`), so with the diagonal chroma residual both U and V reconstruct as a genuine directional pattern (17 distinct values each), NOT a flat DC level. The committed `syn-dfchroma-intra-64x64-q80.ivf` (one 64x64 D135 luma block, top half near 40 / bottom half near 210, 135-degree anti-diagonal chroma) decodes bit-exactly to the avmdec AND dav2d oracle (md5 09fc23f0bced8ab5b9562d6d2478af1c, pinned frame hash 628b759dcb63356ad3174063652c54d7ebf6f54d1566ab9f1b64b3a74542154f), guarded by the §8.2.4 `exit_symbol()` bit-exactness check; all existing general-intra fixtures still decode bit-exact (the original `syn-hedge` fixture, which uses the `uv_mode == 1` DC chroma, is unchanged). The follow-up `syn-d135-chroma-nonfull-32x32-q7.ivf` is a deterministic 66-byte current-AVMenc fixture whose only non-full-superblock 32x32 block has DC luma, explicit D135 chroma, and neither neighbour available. Pre-fix splot rejected it at byte 64 with `general_intra_directional_chroma_neighbour`; fixed splot, pinned avmdec, and current avmdec emit byte-identical 1,536-byte raw I420 SHA-256 `9a1caf04eeed5402053edc129525d145477fcac645984e84ea2ec735ca4d47fa`. A later external-stream correction applies the same normative edge preparation to chroma before the bilinear D135 prediction: §7.13.2.14 corner filtering and the §7.13.2.17-.18 selected edge filters. The focused 10-bit top-row regression has no available above edge and a strength-3, 65-sample available left edge; filtering that left edge changes the checked U sample to the AVM-observed value 513. Together with the existing real-edge fixtures and 8/10-bit frame-origin/tile-origin unit coverage, this removes the D135/D135Follow neighbour/position gate. Out of scope: other directional chroma angles, non-zero `AngleDeltaUV`, CfL/CCTX/MHCCP, unsupported transform and partition syntax, inter prediction, in-loop filters, and live AVM invocation in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::dfchroma_directional_follow_chroma_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_d135_tests::d135_chroma_nonfull_no_neighbour_frame_decodes_to_oracle",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::supported_chroma_mode_directional_follow_resolves_d135_for_uv_mode_zero",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::supported_chroma_mode_explicit_d135_uses_non_follow_mode",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::chroma_d135_top_row_filters_available_left_edge",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-decode block_context --locked",
    "cargo test -p splot-decode --lib chroma_d135_top_row_filters_available_left_edge --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-dfchroma-intra-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-d135-chroma-nonfull-32x32-q7.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-NEIGHBOUR-DIRECTIONAL"
name = "General intra neighbour-having directional (D135) luma + follow chroma over a real edge"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-neighbour-directional"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Lifts the deferred rejects for a §7.13.2.8 D135_PRED luma block (and its `uv_mode == 0` directional-follow D135 chroma) that reads a REAL reconstructed neighbour edge: previously `general_intra_multiblock_directional_luma` (luma) and `general_intra_directional_chroma_neighbour` (chroma) gated D135 to the no-neighbour top-left block over §7.13.2.1 flat fallbacks. KEY SPEC FINDING: pAngle 135 is a §7.13.2.8 middle angle (`90 < pAngle < 180`) whose derivatives are `dx = dy = Dr_Intra_Derivative[45] = 64`, so EVERY projection has `idx = 64*(j - i - 1)` (above branch) / `64*(i - j - 1)` (left branch) and thus `shift = (idx >> 1) & 0x1F == 0`. At `shift == 0` the §7.13.2.8 luma IDIF 4-tap (`enableIdif == 1`, plane 0) collapses to `Dr_Interp_Filter[0] = {0, 128, 0, 0}`, i.e. `Clip1(Round2(128 * Edge[base], 7)) == Edge[base]`, which is BIT-IDENTICAL to the chroma bilinear branch (`enableIdif == 0`: `Round2(Edge[base] * 32 + Edge[base+1] * 0, 5) == Edge[base]`) — a pure sample copy `Edge[base]` EVEN OVER A NON-FLAT reconstructed edge (verified numerically and against the oracle). So no genuine IDIF 4-tap kernel was needed for D135; the existing shared bilinear `predict_intra_middle_directional_angle_rect_into` is exact for D135 in both planes, and the only new work is reading the REAL §7.13.2.1 edges instead of the flat fallback. (Other angles, where `shift != 0`, genuinely differ between IDIF and bilinear and remain deferred.) The new `reconstruct_general_intra_directional_neighbour_block_into` (plane-general) builds the logical `AboveRow[-1..w)` / `LeftCol[-1..h)` edges from the partially-built frame via `build_directional_middle_edges`, faithful to §7.13.2.1 for the minimal-tool subset (`MrlIndex == 0`, `enable_intra_edge_filter == 0`, no DIP/upsample): for the verified first-superblock-row block (`frontier.r == 0`, `haveLeft == 1, haveAbove == 0`) `LeftCol[i] = CurrFrame[plane][Min(leftLimit, y+i)][x-1]` (the real reconstructed left column, bottom-left sentinel clamped since `num4BelowLeft == 0`), `AboveRow[i] = CurrFrame[plane][y][x-1]` (the repeated first left sample), and the corner `AboveRow[-1] = LeftCol[-1] = CurrFrame[plane][y][x-1]`. D135 never reads the above-right sentinel value (its above/left projections stay within `AboveRow[0..w)` / `LeftCol[0..h)`), so no above-right resolver is needed. The D135 escape was decoded with a NON-directional joint-mode neighbour (`ctx == 0`; a directional neighbour would have hit the deferred §5.20.5.3 directional-neighbour reorder reject earlier), so the left neighbour is DC/SMOOTH and supplies the real left column. The chroma is the `uv_mode == 0` directional-follow of the luma D135 (`UVMode == D135_PRED`, `AngleDeltaUV == AngleDeltaY == 0`); `reconstruct_general_intra_chroma_block_into` routes `D135Follow` with `x > 0 || y > 0` to the same neighbour-having path (chroma never uses the §7.14.4 TCQ dqDenom term). The committed `syn-rdir-intra-128x64-q80.ivf` (two 64x64 superblocks: LEFT codes SMOOTH_V_PRED luma over a vertical gradient with DC chroma; RIGHT codes D135_PRED luma + directional-follow D135 chroma, reading the LEFT superblock's real reconstructed right column — a non-flat 34-distinct-value vertical gradient — as its §7.13.2.1 left edge; flat chroma U=120 V=130) decodes bit-exactly to the avmdec AND dav2d oracle (raw output md5 9ff7e4d46c0dd4fa979070ce4ca4dd1c; raw outputs agree, recorded in LOCAL-REFERENCE-EVIDENCE.toml; pinned splot frame hash 9ea9254abc7d7507558099d5ae3e78eaf5d88625e1cc8184038321650b2b54a4 asserted by the rdir oracle test), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. The OLD code rejected this exact frame (verified: `general_intra_directional_chroma_neighbour`, the chroma reject firing first). All existing general-intra fixtures remain byte-exact; later rows made the other directional luma modes and the directional-neighbour reorder total. Scope: a first-superblock-row (`frontier.r == 0`, `haveAbove == 0`), non-top-left, full 64x64 superblock (`n4w == 16`) D135 luma block reading the real reconstructed LEFT column, plus its directional-follow D135 chroma. Out of scope (still rejected / deferred): a row>0 D135 block reading the real reconstructed above row (`general_intra_multirow_directional_luma` — bit-exact by the same `shift == 0` argument but not yet oracle-fixtured), other directional angles and non-zero angle deltas (where `shift != 0` so the real §7.13.2.8 IDIF 4-tap genuinely differs from bilinear), non-64x64 frames, inter prediction, in-loop filters, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::rdir_neighbour_directional_d135_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-rdir-intra-128x64-q80.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-CARDINAL"
name = "General intra total luma-mode domain with cardinal V_PRED / H_PRED"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.5.6", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-cardinal"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "The general-intra luma domain is now an exhaustive 13-variant IntraYMode classification covering DC, the eight directional modes, the three smooth modes, and Paeth. Planning is total for every valid mode: V_PRED and H_PRED retain their §7.13.2.8 cardinal sample-copy paths, every other directional mode retains pAngle, wide-angle, MRL, and reconstruction routing, and smooth/Paeth retain their existing plans. Bounded joint modes and typed MRL state remove raw fallbacks. Existing cardinal AVM fixtures remain byte-exact; syn-dirneigh-reorder-128x64-q196 additionally selects D45_PRED joint mode 8 through the directional-neighbour reorder. Forged values are typed internal errors rather than malformed or unsupported syntax."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::vpred_cardinal_multirow_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::hpred_cardinal_multicolumn_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::lossless_cardinal_y_and_top_left_angle_delta_variants_decode_to_oracle",
    "crates/splot-decode/src/pipeline/general_intra_horizontal_tests.rs::horizontal_chroma_tile_origin_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::cardinal_horizontal_follow_chroma_top_left_uses_no_neighbour_left_fallback",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::intra_y_mode_encodes_the_complete_thirteen_mode_domain",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::mode_and_joint_domains_are_bounded_to_zero_through_sixty",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-vpred-intra-64x128-q160.ivf",
    "tests/conformance/vectors/valid/syn-vpred-top-left-intra-64x64-q96.ivf",
    "tests/conformance/vectors/valid/syn-hpred-top-left-intra-64x64-q96.ivf",
    "tests/conformance/vectors/valid/syn-hpred-intra-128x64-q180.ivf",
    "tests/conformance/vectors/valid/syn-cardinal-v-chroma-top-left-64x64-q64.ivf",
    "tests/conformance/vectors/valid/syn-cardinal-h-chroma-top-left-64x64-q192.ivf",
    "tests/conformance/vectors/valid/syn-cardinal-h-chroma-tile-origin-128x64-q1.ivf",
    "tests/conformance/vectors/valid/syn-dirneigh-reorder-128x64-q196.ivf",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/general_intra_horizontal_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-IDIF-D157"
name = "General intra D157 luma IDIF + total explicit/follow chroma"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-idif-d157"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "First general-intra decode to ORACLE-PROVE the §7.13.2.8 luma IDIF 4-tap kernel: the §7.13.2.8 D157_PRED directional luma mode (pAngle 157, `AngleDeltaY == 0`) over a REAL reconstructed neighbour edge plus its `uv_mode == 0` directional-follow D157 chroma. D157 is a §7.13.2.8 MIDDLE angle (`90 < pAngle < 180`) with `dx = Dr_Intra_Derivative[23] = 170` and `dy = Dr_Intra_Derivative[67] = 24` (§9.2). Unlike D135 (`dx = dy = Dr[45] = 64`, every projection `shift == 0` so the IDIF reduces to a sample copy bit-identical to bilinear), D157's projection lands on a NONZERO `shift` for 2940 of its 3344 left-branch samples (confirmed by temporary instrumentation, since removed), so it genuinely exercises the luma IDIF 4-tap. This brick adds the §7.13.2.8 / §9.2 `Dr_Interp_Filter[32][4]` table to `splot-recon` (copied VERBATIM from `docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8`) and the `enableIdif == 1` luma 4-tap path `predict_intra_middle_directional_angle_rect_idif_into` (`s = Σ(t=0..3) Dr_Interp_Filter[shift][t] * Edge[base + t - 1]; pred = Clip1(Round2(s, 7))`, signed sum floored by Round2, Clip1 clamping negatives to 0), tracked by `RECON-INTRA-MIDDLE-DIRECTIONAL-ANGLE-PREDICTION`. The production luma route resolves mode value 6 through `IntraYMode::mode_to_angle`; `rect_luma_plan` applies `AngleDeltaY` and rectangular wide-angle mapping and dispatches pAngle 157 as `RectLumaPlan::Middle`. The middle-angle reconstruction path builds the wider §7.13.2.8 IDIF edges `AboveRow[-2..=w+1]` / `LeftCol[-2..=h+1]`, runs the luma 4-tap IDIF kernel, and keeps the chroma branch bilinear. The D157-follow chroma (`SupportedChromaMode::D157Follow`, resolved when `uv_mode == 0` over the D157 luma makes §5.20.5.3 return `UVMode == D157_PRED`, `AngleDeltaUV == 0`) routes to the same function on the chroma plane (bilinear over the flat real chroma edge). D157 was decoded via the §5.20.5.3 `y_mode_offset` escape (`y_mode_index == MODE_INDEX_COUNT - 1`, `y_mode_offset == 3` resolving through `Reordered_Y_Mode[10] == D157_PRED`) at §8.3.2 ctx == 0 (the SMOOTH_V left neighbour stored a non-directional `IntraJointMode 2`). The committed `syn-d157-intra-128x64-q80.ivf` (128x64: LEFT 64x64 SMOOTH_V_PRED smooth vertical gradient with DC chroma; RIGHT 64x64 D157-exact construction — input built by running the §7.13.2.8 D157 predictor over the gradient left column — reading the LEFT superblock's real reconstructed right column as its §7.13.2.1 left edge at `frontier.r == 0, frontier.c == 16, haveLeft && !haveAbove`; flat chroma U=120 V=130) decodes bit-exactly to the avmdec AND dav2d oracle (raw output md5 c8698fdb7628843971bc9e37a82391ae, sha256 bf93ca6b8f55e1fb7db2584f3e3821ad67f21018b774c6e326634362ee5ef046; pinned splot frame hash bf93ca6b8f55e1fb7db2584f3e3821ad67f21018b774c6e326634362ee5ef046), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. The OLD code rejected this exact frame (`general_intra_non_dc_chroma_mode`, verified by building origin/main and decoding the fixture). The `directional_luma_always_plans_through_the_rect_planner` test covers every canonical directional luma mode and angle delta through this production route, while the D157 oracle and IDIF reconstruction tests pin the behavior claimed here. The follow-up `syn-d157-chroma-nonfull-32x32-q1.ivf` is a deterministic 66-byte current-AVMenc fixture whose only non-full-superblock 32x32 block has DC luma, explicit D157 chroma, and neither neighbour available. Pre-fix splot rejected it at byte 64 with `general_intra_directional_d157_chroma_neighbour`; fixed splot, pinned avmdec, and current avmdec emit byte-identical 1,536-byte raw I420 SHA-256 `9084f7311718d3558b0e9b5ed09315a989f39b55b585d3e387fe878c436d40e7`. D157 and D157Follow chroma admission is now total across every valid block position, neighbour state, and transform rectangle: `rect_chroma_plan_for_mode` retains D157 plus AngleDeltaUV and `unit_directional_replan` resolves pAngle 157 to the already-total middle-angle handoff, whose §7.13.2.1 edge builder covers neither, above-only, left-only, and both edges. The original D157 luma oracle evidence remains the first-superblock-row, non-first-column full 64x64 superblock pinned by `syn-d157-intra-128x64-q80.ivf`; later feature rows own broader positions, subpartitions, angle deltas, MRL, IBP, wide-angle, neighbour reorder, inter prediction, and filters."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d157_neighbour_directional_idif_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/general_intra_d157_tests.rs::d157_chroma_nonfull_no_neighbour_frame_decodes_to_oracle",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d157_idif_middle_prediction_applies_the_4_tap_filter_and_differs_from_bilinear",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d135_idif_middle_prediction_is_a_sample_copy_identical_to_bilinear",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-recon intra_directional_angle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-d157-intra-128x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-d157-chroma-nonfull-32x32-q1.ivf",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-recon/src/intra_directional_angle.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-DIRECTIONAL-CORNER"
name = "General intra row>0 directional reconstruction reads the real §7.13.2.1 corner"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/reconstruct.rs"
change_id = "decode-general-intra-directional-corner"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Lifts the deferred row>0 directional reject by building the REAL §7.13.2.1 top-left corner `AboveRow[-1] == LeftCol[-1] == CurrFrame[plane][y-1][x-1]` for the `haveAbove == 1` middle-angle reconstruction, unblocking the first general-intra ROW>0 directional decode. The prior `build_directional_middle_edges` `(true, true)` and `(false, true)` arms returned `GeneralIntraResidualError::UnsupportedDirectionalAboveEdge` (runtime `general_intra_directional_above_edge`) because the real diagonally-above-left corner is not returned by `intra_dc_edges_for_rect`; the admission gate rejected the whole row>0 directional path (`general_intra_multirow_directional_luma` for luma / `general_intra_directional_chroma_neighbour` for chroma). §7.13.2.8 D135 reads that corner on its main diagonal (`column == row`, `above_base == -1`, `shift == 0`, a sample copy); the first-row bricks only worked because at `haveLeft && !haveAbove` the corner is the (correct) repeated-first-left sample. This brick reads the real corner via the current-frame workspace `reconstructed_sample(plane, x-1, y-1)` accessor (added by DECODE-GENERAL-INTRA-GRID) in `reconstruct_general_intra_directional_neighbour_block_into` and threads it into the now-total `build_directional_middle_edges`: the `(true, true)` arm uses `CurrFrame[plane][y-1][x-1]` (aboveMrlIndex == 0 at the superblock boundary, MrlIndex == 0) + the real above row (`AboveRow[i] = CurrFrame[plane][y-1][x+i]`) + the real left column (`LeftCol[i] = CurrFrame[plane][y+i][x-1]`); the `(false, true)` arm uses the real above row and derives `LeftCol[i] = AboveRow[-1] = CurrFrame[plane][y-1][x] = AboveRow[0]` per §7.13.2.1. The IDIF luma 4-tap / bilinear chroma kernel (DECODE-GENERAL-INTRA-IDIF-D157) is reused unchanged; only the edge construction differs by arm. pAngle 135's `shift == 0` makes the IDIF a sample copy `Edge[base]` bit-identical to bilinear over the non-flat real edge, so the corner read is the new piece and D135's row>0 reconstruction is bit-exact. The committed `syn-d135row-intra-128x128-q80.ivf` (128x128 = two superblock columns by two rows: top-left / top-right / bottom-left 64x64 superblocks DC_PRED — uniform top 100, bottom-left a vertical gradient coded DC; BOTTOM-RIGHT 64x64 superblock at `frontier.r == 16, frontier.c == 16, haveLeft && haveAbove` codes D135_PRED luma via the §5.20.5.3 y_mode_offset escape at §8.3.2 ctx == 0 plus `uv_mode == 0` directional-follow D135 chroma) decodes bit-exactly to the avmdec AND dav2d oracle (raw output md5 79bd663383515e37b75b1ad7054c84d6; pinned splot frame hash 85583e5a46ac6a2db97854b86f643735c1b9710bee2c2d2bc65d1aa5a16fe3a1), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Confirmed by temporary instrumentation (since removed): the bottom-right luma block reads `have_left == true, have_above == true` and corner == 100 (= `CurrFrame[Y][63][63]`, the top-left superblock's bottom-right sample, NOT the 128 no-neighbour fallback); chroma U/V read corner 120/130. The OLD code rejected this exact frame (`general_intra_directional_chroma_neighbour` for the bottom-right chroma). All existing general-intra fixtures still decode bit-exact (the no-corner first-row / no-neighbour D135/D157 arms are byte-identical), and a still-unsupported row>0 case rejects: a row>0 FIRST-column (`!haveLeft && haveAbove`) D135 block still rejects (`general_intra_directional_chroma_neighbour` for the row>0 first-column chroma; verified on the 64x128 single-column SMOOTH_H-top + D135-bottom companion). Scope: row>0, NON-first-column full 64x64 superblock (`frontier.r != 0 && frontier.c != 0`, `n4w == 16`, `haveLeft && haveAbove`) D135 luma + `uv_mode == 0` directional-follow D135 chroma reading the real §7.13.2.1 corner + above row + left column. Out of scope (still rejected / deferred): the row>0 FIRST-column (`!haveLeft && haveAbove`) D135 position, any row>0 D157 position (no fixture pins the real-corner D157 IDIF yet), sub-superblock (split-child) directional blocks (need the per-block §5.20.2.3 BlockDecoded update + mode-dependent transform type), non-zero angle deltas, the directional-neighbour (`ctx != 0`) escape reorder, the other middle angle D113 and the one-sided angles D45/D67/D203, non-64x64 superblock blocks, multiple tiles, inter prediction, in-loop filters, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d135row_neighbour_directional_row_gt0_corner_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::rdir_neighbour_directional_d135_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d157_neighbour_directional_idif_intra_frame_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-d135row-intra-128x128-q80.ivf",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-ANGLE-D113"
name = "General intra D113 vertical-leaning luma IDIF + follow chroma over a real above edge"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-angle-d113"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Completes the §7.13.2.8 MIDDLE-angle zone (`90 < pAngle < 180`, D135 + D157 already done) by admitting the §7.13.2.8 D113_PRED directional luma mode (pAngle 113, canonical §9.2 mode 5, `AngleDeltaY == 0`) over a REAL reconstructed above edge plus its `uv_mode == 0` directional-follow D113 chroma. D113 is VERTICAL-LEANING (pAngle near 90): `dx = Dr_Intra_Derivative[180 - 113] = Dr_Intra_Derivative[67] = 24` and `dy = Dr_Intra_Derivative[113 - 90] = Dr_Intra_Derivative[23] = 170` (§9.2, confirmed against the committed `Mode_To_Angle[5] == 113` and the `Dr_Intra_Derivative[90]` table). Most projections take the §7.13.2.8 ABOVE branch (`base >= -(1 + mrlIndex)`) and land on a NONZERO `shift` for 2940 of the 4096 luma samples (confirmed by temporary instrumentation, since removed), so it genuinely exercises the luma §7.13.2.8 IDIF 4-tap `Dr_Interp_Filter` over the real reconstructed above row + diagonally-above-left corner — unlike D135 (every `shift == 0`, the IDIF reduces to a sample copy) and complementary to D157 (mostly LEFT-branch). This is purely a wiring + fixture brick: the §7.13.2.8 luma IDIF 4-tap kernel (`predict_intra_middle_directional_angle_rect_idif_into`, DECODE-GENERAL-INTRA-IDIF-D157 / RECON-INTRA-MIDDLE-DIRECTIONAL-ANGLE-PREDICTION) and the real §7.13.2.1 corner builder (`build_directional_middle_edges` `(true, true)` arm reading `CurrFrame[plane][y-1][x-1]`, DECODE-GENERAL-INTRA-DIRECTIONAL-CORNER) both already exist and are reused UNCHANGED. The production luma route resolves mode value 5 through `IntraYMode::mode_to_angle`; `rect_luma_plan` applies `AngleDeltaY` and rectangular wide-angle mapping and dispatches pAngle 113 as `RectLumaPlan::Middle` to the middle-angle reconstruction path, while the D113-follow chroma (`SupportedChromaMode::D113Follow`, resolved when `uv_mode == 0` over the D113 luma makes §5.20.5.3 return `UVMode == D113_PRED`, `AngleDeltaUV == 0`) routes to the same function on the chroma plane (the `enableIdif == 0` bilinear branch). D113 was decoded via the §5.20.5.3 `y_mode_offset` escape (`y_mode_index == MODE_INDEX_COUNT - 1`, `y_mode_offset == 2` -> modeIdx 9 -> modeDelta 29 -> `Reordered_Y_Mode[8] == D113_PRED`) at §8.3.2 ctx == 0 (the DC neighbour stored a non-directional `IntraJointMode 0`). The committed `syn-d113-intra-128x128-q80.ivf` (128x128 = two superblock columns by two rows: top-left / top-right / bottom-left 64x64 superblocks DC_PRED — uniform top 100, bottom-left a vertical gradient coded DC; BOTTOM-RIGHT 64x64 superblock at `frontier.r == 16, frontier.c == 16, haveLeft && haveAbove` codes D113_PRED luma via the §5.20.5.3 y_mode_offset escape at §8.3.2 ctx == 0 plus `uv_mode == 0` directional-follow D113 chroma) decodes bit-exactly to the avmdec AND dav2d oracle (raw output md5 ba857e73ad624d0409d1189b387d1ef7; pinned splot frame hash d32bc2b11585e7ea55f0d2401f18402c55e781c0a861bb613b55f5dc26a2a395), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. Confirmed by temporary instrumentation (since removed): the bottom-right block decodes `y_mode == IntraYMode(5)` (canonical D113), `angle_delta_y == 0`, `frontier.r == 16` (row>0), `dir == Some(D113)`, `chroma == Some(D113Follow)`. The OLD code rejected this exact frame (`general_intra_non_dc_chroma_mode`, verified by building origin/main and decoding the fixture). The `directional_luma_always_plans_through_the_rect_planner` and `classifies_rect_directional_luma_angles` tests cover D113 through the current planner, while the D113 reconstruction and oracle tests pin the claimed edge and output behavior. Scope: D113 luma + D113-follow chroma on a row>0, NON-first-column full 64x64 superblock (`frontier.r != 0 && frontier.c != 0`, `n4w == 16`, `haveLeft && haveAbove`) reading the real reconstructed §7.13.2.1 above row + left column + diagonally-above-left corner. The committed 77-byte `syn-d113-chroma-top-left-64x64-q192.ivf` additionally proves explicit D113 chroma over DC luma at the no-neighbour top-left; both AVM decoders and Splot agree on raw SHA-256 `991b1a40edefbde26e76a81a37e2221048880636238eb93f44574d801e86ca35`, and the obsolete `general_intra_directional_d113_chroma_neighbour` reason is removed. D113 luma and D113-follow chroma at every top-left, first-row, first-column, sub-partitioned, and non-64x64 position are now planned by the generic §7.13.2.8 rectangular directional planner; the `general_intra_d113_unverified_position` whitelist is retired. The original D113 oracle evidence is scoped to the cited neighbour and top-left fixtures; later feature rows own one-sided angles, non-zero angle deltas, MRL, IBP, wide-angle, neighbour reorder, subpartitions, inter prediction, and filters."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d113_neighbour_directional_idif_above_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::directional_luma_always_plans_through_the_rect_planner",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::d113_chroma_top_left_uses_no_neighbour_middle_edges",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::classifies_rect_directional_luma_angles",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::y_mode_offset_escape_reconstructs_d113",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d113_middle_prediction_matches_non_idif_bilinear_formula",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-decode block_context --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-d113-intra-128x128-q80.ivf",
    "tests/conformance/vectors/valid/syn-d113-chroma-top-left-64x64-q192.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-ANGLE-D45"
name = "General intra D45 zone-1 one-sided luma + follow chroma reading the real above-right"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.25", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-angle-d45"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Opens the §7.13.2.8 ZONE-1 one-sided angle zone (`pAngle < 90`, `needRight`) by admitting the §7.13.2.8 D45_PRED directional luma mode (pAngle 45, canonical §9.2 mode 3, `AngleDeltaY == 0`) over a REAL reconstructed above row + ABOVE-RIGHT plus its `uv_mode == 0` directional-follow D45 chroma. Unlike the §7.13.2.8 MIDDLE angles (D113/D135/D157, `90 < pAngle < 180`, which read `AboveRow[0..w)` / `LeftCol[0..h)`), the zone-1 step-1 projection reads UP-AND-RIGHT into the ABOVE-RIGHT: `pred[i][j] = AboveRow[base]` with `base = (i + 1) + j` (`dx = Dr_Intra_Derivative[45] = 64`, §9.2), up to `maxBaseX = w + h - 1 = 127` for a 64x64 block — the upper-right triangle reads `h` real reconstructed above-right samples the middle angles never touch. This brick adds the zone-1 luma IDIF kernel to `splot-recon` (`predict_intra_directional_angle_rect_one_sided_idif_into` / `IntraDirectionalAngleIdifEdges`, RECON-INTRA-ONE-SIDED-DIRECTIONAL-ANGLE-PREDICTION): it implements §7.13.2.8 step 1 (`idx = (i + 1 + mrlIndex) * dx`, `base = (idx >> 6) + j`, `shift = (idx >> 1) & 0x1F`; for `base < maxBaseX + 1` the 4-tap `s = Σ(t=0..3) Dr_Interp_Filter[shift][t] * AboveRow[base + t - 1]; pred = Clip1(Round2(s, 7))`, else `pred = AboveRow[maxBaseX]`) over the wider above edge `AboveRow[-2 ..= w + h + 1]` (length `w + h + 4`, slice index 0 = logical `-2`), reusing the existing §7.13.2.8 / §9.2 `Dr_Interp_Filter[32][4]` table UNCHANGED. Every D45 projection has `shift == 0` (`(i + 1) * 64 >> 1 & 0x1F == 0`), so the IDIF reduces to the sample copy `AboveRow[base]` — but it still reads far into the real reconstructed above-right (the one-sided zone). The production luma route resolves mode value 3 through `IntraYMode::mode_to_angle`; `rect_luma_plan` applies `AngleDeltaY` and rectangular wide-angle mapping and dispatches pAngle 45 as `RectLumaPlan::OneSidedAbove` to the zone-1 reconstruction path, while the D45-follow chroma (`SupportedChromaMode::D45Follow`, resolved when `uv_mode == 0` over the D45 luma makes §5.20.5.3 return `UVMode == D45_PRED`, `AngleDeltaUV == 0`) routes to the same function on the chroma plane (the `enableIdif == 0` bilinear one-sided branch, bit-identical for D45's `shift == 0`). D45 was decoded via the §5.20.5.3 `y_mode_offset` escape (`y_mode_index == MODE_INDEX_COUNT - 1`, `y_mode_offset == 0` -> modeIdx 7 -> modeDelta 8 -> `Reordered_Y_Mode[5] == D45_PRED`) at §8.3.2 ctx == 0. `enable_ibp == 0` keeps `useIBP == 0` (§7.13.2.7 gates `useIBP` on `pAngle < 90`); `enable_intra_edge_filter == 0` / `MrlIndex == 0` keep the §7.13.2.x edge-filter / upsample synthesis a no-op. The committed `syn-d45-intra-192x128-q80.ivf` (192x128 = three superblock columns by two rows: top-left / top-middle / bottom-left / bottom-right 64x64 superblocks DC_PRED, top-right DC superblock carrying a horizontal-gradient residual so it reconstructs NON-FLAT; BOTTOM-MIDDLE 64x64 superblock at `frontier.r == 16, frontier.c == 16, haveLeft && haveAbove`, NON-rightmost, codes D45_PRED luma via the §5.20.5.3 y_mode_offset escape at §8.3.2 ctx == 0 plus `uv_mode == 0` directional-follow D45 chroma) decodes bit-exactly to the avmdec AND dav2d oracle (raw output md5 8fe6a134c01b0d20be4016348ccd3b99; pinned splot frame hash d08056c0d1ed3f379e3072c7f1ebced04da0f6df994efd0b5f8d39b76c0b683f), guarded by the §8.2.4 `exit_symbol()` bit-exactness check. The follow-up `syn-d45-top-left-intra-64x64-q96.ivf` pins a top-left no-neighbour `D45_PRED` syntax case with `AngleDeltaY == 0` (`pAngle == 45`), routed over the §7.13.2.1 no-neighbour above fallback; AVM raw SHA-256 `6a8317f2ca517cb85a9157c9e8e86ffe7b4ad9580374cbfee7394e974a5cf06b` is reproduced byte-exactly. The follow-up `syn-d45-right-edge-intra-128x128-q80.ivf` pins a rightmost full-SB D45 syntax case through the §5.20.5.3 y_second_mode path with `num4AboveRight == 0`, proving the existing square-to-rect residual fallback and §7.13.2.1 `aboveLimit` clamp reproduce AVM raw SHA-256 `fd131679dbf64ae6ad63cf2b51894995c86bda529ff5299545225b00d478ba76` byte-exactly. The committed 106-byte `syn-d45-follow-chroma-top-left-64x64-q182.ivf` additionally pins `D45_PRED` luma with directional-follow D45 chroma over the §7.13.2.1 no-neighbour `AboveRow` fallback: the old decoder rejected it at byte 65 with `general_intra_directional_d45_chroma_neighbour`, while pinned/current avmdec and fixed Splot agree on the complete 6,144-byte raw I420 SHA-256 `5f048c577d2fd6a392302aad8be4dbdb070d5dd33ccaecfe4cbadb5374aec0c3`. The square-path D45/D45-follow/D67/D67-follow chroma admission is now total over valid block geometry, reusing the existing generic zone-1 reconstruction, and the obsolete reason is removed. The 192x128 D45 block's upper-right triangle reads the real reconstructed above-right (the top-right superblock's non-flat bottom row, 32 distinct values 42..228); the block reconstructs as 33 distinct values, genuinely directional. The old code rejected the real above-right frame with `the former unsupported-Y-mode gate` and the top-left frame with `general_intra_d45_unverified_position` at byte offset 64. The `directional_luma_always_plans_through_the_rect_planner` test covers D45 and every other canonical directional luma mode through the current planner, while the D45 reconstruction and oracle tests pin the claimed above-right and output behavior. Scope: D45 luma + D45-follow chroma on a row>0, NON-first-column, NON-rightmost full 64x64 superblock (`frontier.r != 0 && frontier.c != 0`, `n4w == 16`, `full_sb_num4_above_right > 0`, `haveLeft && haveAbove`) reading the real reconstructed §7.13.2.1 above row + above-right + diagonally-above-left corner; and 8-bit top-left no-neighbour full-SB D45 luma with DC chroma over the §7.13.2.1 no-neighbour above fallback. Every D45 luma position, including first-row, first-column, rightmost, sub-partitioned, and non-64x64 blocks, is now planned by the generic §7.13.2.8 rectangular directional planner; the `general_intra_d45_unverified_position` whitelist is retired. The original D45 oracle evidence is scoped to the cited top-left, right-edge, and real-above-right fixtures; later feature rows own D67, D203, non-zero angle deltas, MRL, IBP, wide-angle, neighbour reorder, subpartitions, inter prediction, and filters."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d45_neighbour_one_sided_above_right_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d45_top_left_no_neighbour_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d45_right_edge_clamped_above_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::d45_follow_chroma_top_left_uses_no_neighbour_above_fallback",
    "crates/splot-decode/src/pipeline/reconstruct.rs::tests::zone1_d45_top_left_synthesizes_no_neighbour_above_fallback",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::directional_luma_always_plans_through_the_rect_planner",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_one_sided_idif_matches_bilinear_copy_for_shift_zero",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d45_one_sided_idif_clamps_base_at_max_base_x",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-recon d45_one_sided --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-d45-intra-192x128-q80.ivf",
    "tests/conformance/vectors/valid/syn-d45-follow-chroma-top-left-64x64-q182.ivf",
    "tests/conformance/vectors/valid/syn-d45-top-left-intra-64x64-q96.ivf",
    "tests/conformance/vectors/valid/syn-d45-right-edge-intra-128x128-q80.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-recon/src/intra_directional_angle.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-ANGLE-D203"
name = "General intra zone-3 one-sided luma, including D203 and no-edge IBP"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.5.3", "5.20.7.25", "5.20.7.27", "7.13.2.1", "7.13.2.7", "7.13.2.8", "7.13.2.9", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-5-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-25",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-9",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-angle-d203"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Completes the AV2 §7.13.2.1, §7.13.2.7, §7.13.2.8, and §7.13.2.9 zone-3 luma path for square and rectangular leaves across top-left, first-column, interior, row-greater-than-zero, subpartition, non-64x64, angle-delta, IBP, and MRL cases. The planner and reconstruction matrices cover all neighbour-availability arms. A successfully reconstructed Single luma transform chunk publishes its §5.20.2.3 BlockDecoded flags immediately, before the outer leaf publication, so a later chunk's §5.20.7.25 count_top_right_avail sees the already reconstructed top-right chunk; `single_luma_chunk_publication_exposes_top_right_to_later_chunk` pins the 64x64-to-64x64 16-unit handoff used by D203 IBP's opposite-zone secondary. The committed q80 D203 real-left-edge fixture and q120 pAngle-186 no-edge IBP fixture decode byte-identically to AVM; the active syn-mrl-intra-128x128 fixture also matches AVM. For the no-edge nonzero-MRL subcase, §7.13.2.1 synthesizes a constant reference edge, so changing the MRL index does not change prediction and AVM RDO does not select the costlier syntax; that subcase is proven by unit tests and an inline AVM zone-3 reference calculation, not claimed as an AVMenc hit. The obsolete general_intra_d203_unverified_position guard is removed. D67, chroma-only gates, and other independently named features remain separate."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::d203_neighbour_one_sided_left_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::classifies_rect_directional_luma_angles",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::directional_luma_always_plans_through_the_rect_planner",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::admits_rect_luma_mrl_cases",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::zone3_d203_interior_leaf_reads_diagonal_above_left_corner",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::zone3_d203_mrl_index_1_matches_inline_avm_z3_idif_reference",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::zone3_d203_top_left_synthesizes_no_neighbour_left_fallback",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::one_sided_ibp_p45_synthesizes_both_missing_edges",
    "crates/splot-decode/src/residual/pipeline/tests.rs::single_luma_chunk_publication_exposes_top_right_to_later_chunk",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::plans_rect_directional_luma_from_modes_and_geometry",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_context.rs::tests::supported_chroma_mode_directional_follow_resolves_d203_for_uv_mode_zero",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d203_one_sided_idif_interpolates_real_left_column",
    "crates/splot-recon/src/intra_directional_angle.rs::tests::d203_one_sided_idif_rejects_above_edge",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-recon d203_one_sided --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-d203-intra-128x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-noedge-zone3-p186-intra-128x64-q120.ivf",
    "tests/conformance/vectors/valid/syn-mrl-intra-128x128.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/residual/pipeline/plane_execution.rs",
    "crates/splot-recon/src/intra_directional_angle.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-RECT-PARTITION"
name = "General intra rectangular (non-square) luma leaf decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.1", "5.20.4.1", "5.20.7.27", "7.13.2.1", "7.13.2.4", "7.13.2.8", "7.14.4", "7.15.4", "8.2.4", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-rect-partition"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Completes general-intra rectangular luma dispatch and reconstruction for DC, PAETH, SMOOTH, cardinal, middle-angle, and one-sided directional modes, including angle deltas, IBP, MRL, square-to-rect residual routing, and every real or synthesized §7.13.2.1 edge-availability combination exercised by the planner and reconstruction matrices. The q120 horizontal-rectangle fixture and the q116 two-tile no-neighbour 16x64 D157 fixture decode byte-identically to pinned AVM, current AVM, and Splot. The obsolete general_intra_rect_non_dc_luma guard is removed. Rectangular non-DC chroma, invalid syntax, inter prediction, and in-loop filters remain independently gated."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::horz_rectangular_partition_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::plans_rect_cardinal_luma_from_modes_and_geometry",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::classifies_rect_directional_luma_angles",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::plans_rect_directional_luma_from_modes_and_geometry",
    "crates/splot-decode/src/pipeline/general_intra.rs::tests::admits_rect_luma_mrl_cases",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::rect_cardinal_horizontal_32x64_fills_each_row_from_tall_left_column",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::rect_cardinal_horizontal_32x64_no_left_fallback_is_flat_above_corner",
    "crates/splot-decode/src/pipeline/reconstruct_tests.rs::one_sided_ibp_p45_synthesizes_both_missing_edges",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-hrect-intra-64x64-q120.ivf",
    "tests/conformance/vectors/valid/syn-rect-noedge-d157-intra-128x64-q116.ivf",
    "tests/conformance/vectors/valid/syn-mrl-intra-128x128.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-10BIT"
name = "General intra 10-bit reconstruction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "7.13.2", "7.14.3", "7.14.4", "7.15.4", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-10bit-recon"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "General-intra reconstruction and output storage are generic across 8-bit u8 and 10-bit u16 samples. The duplicate 10-bit-only luma/chroma admission gates were deleted: ordinary mode, partition, transform, chroma, and filter capability checks now decide both bit depths at their real boundaries. Oracle coverage includes DC, nonzero residual, SMOOTH luma/chroma, split leaves, base_q_idx 255, a multi-superblock SMOOTH-chroma/DC mix, exact top-left D45 luma in monochrome, and exact D45 luma plus D45-follow chroma in 4:2:0. The two q128 D45 fixtures separately prove the former luma-only and luma-plus-chroma broad gates; pinned avmdec 457cd586, current avmdec 3935f4bf, and Splot match byte-for-byte at raw SHA-256 2efc7a2cdb39694755fb06ff31e70bc06b5711e26196d5a756daad43e546d6cc (8,192-byte monochrome) and 176168529b5970cafe650525f68cdd34df4a9bd9cca1e784005248ec71aa7ed7 (12,288-byte I420). The §7.13.2.1 fallback values derive from BitDepth, and the §7.13.2.8 predictor and residual graph use ReconSample, so the implementation is shared rather than duplicated. Independently gated features such as unsupported CfL parameters or unimplemented syntax remain fail-closed. Existing 8-bit fixtures and planner tests remain byte-identical."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q80_10bit_intra_frame_reconstructs_flat_planes",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::q80_10bit_intra_frame_hash_is_stable",
    "crates/splot-decode/src/output/hash_tests.rs::ten_bit_d45_fixtures_match_reference_output_hashes",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo xtask conformance",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-10bit-q80.ivf",
    "tests/conformance/vectors/valid/syn-d45-follow-intra-64x64-10bit-q128.ivf",
    "tests/conformance/vectors/valid/syn-d45-mono-intra-64x64-10bit-q128.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/pipeline/reconstruct.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-10BIT-SMOOTH-CHROMA"
name = "General intra 10-bit DC luma + SMOOTH chroma reconstruction"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.4.1", "7.13.2.1", "7.13.2.13"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-2-13",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/general_intra.rs"
change_id = "decode-general-intra-10bit-smooth-chroma"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends DECODE-GENERAL-INTRA-10BIT with §7.13.2.13 SMOOTH chroma. Both the single 64x64 no-neighbour fixture and the 128x64 mixed case (top-left SMOOTH chroma followed by DC chroma over a reconstructed left edge) decode byte-exactly to AVM; the multi-superblock raw SHA-256 is dd96ae02ba422d0da6c38f9f748ee06c74d89a5d70170db29550c6916cb5fbaa. Admission uses the ordinary bit-depth-generic chroma planner; the obsolete frame-size-specific 10-bit policy gate is gone. Independently unsupported chroma syntax remains fail-closed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::ten_bit_dc_luma_smooth_chroma_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::ten_bit_multi_sb_smooth_chroma_decodes_to_oracle",
]
commands = [
    "cargo test -p splot-decode general_intra --locked",
    "cargo xtask conformance",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-smchroma-intra-64x64-10bit-q160.ivf",
    "tests/conformance/vectors/valid/syn-2sb-smchroma-intra-128x64-10bit-q160.ivf",
    "crates/splot-decode/src/pipeline/general_intra.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-DEBLOCK"
name = "General intra §7.17 deblocking-filter orchestration"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17.1", "7.17.2", "7.17.5", "7.17.6", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-6",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/deblock.rs"
change_id = "decode-general-intra-deblock"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "First in-loop-filter brick: builds the §7.17.1/§7.17.2 deblocking-filter edge-traversal orchestration over the `splot-recon` per-edge primitives (DECODE-RECON-DEBLOCK-* leaf math), relaxing the general intra route gate to admit deblock-ACTIVE frames (previously the route rejected any `apply_deblocking_filter != [false; 4]`). The new `crates/splot-decode/src/filters/deblock.rs` module: (1) derives the per-(plane, pass) §7.17.6 filter LEVEL — with segmentation disabled, `df_delta_q` all zero, and the chroma base offsets resolving to zero (all enforced by the route gate), `lvl = q_clamped(base_q_idx, 0)` for every plane; (2) derives the §7.17.5 (qThr, side) strengths via `deblock_side_threshold_index` + `deblock_adaptive_filter_strength` over the §9.2 `Side_Thresholds` table; (3) iterates the §7.17.1 plane (0,1,2) × pass (0=vertical, 1=horizontal) × MI (row, col with 4:2:0 chroma `rowStep`/`colStep`) loop, gating each pass on `apply_deblocking_filter[plane==0 ? pass : plane+1]`; (4) for each edge runs the §7.17.2 derivation (sbEdge / onScreen / isBlockEdge / isTxEdge / applyFilter cascade with `isSubPuEdge == 0` since `allow_df_sub_pu == 0` for an intra key frame, and `skip == 0` for the residual-coded intra blocks), the §7.17.4 filterSize (Min over the two transform dims in the pass direction, screen-edge clipped), the §7.17.3 per-side `maxWidthNeg`/`maxWidthPos` via `deblock_filter_max_width`, the §7.17.7.2 `deblock_filter_choice` over the gathered perpendicular s/t sample lines, then the §7.17.7.1 `deblock_sample_filter` for each of the MI_SIZE edge positions; the modified samples are written back in place via the new checked `CurrentFrameWorkspace::set_reconstructed_sample`. Applied after the block walk and BEFORE `workspace.freeze()`; a deblock-OFF (`apply == [false; 4]`) frame runs the pass as a NO-OP, so all existing general-intra fixtures (8-bit and 10-bit) stay byte-identical (verified). The decoded-block geometry (luma/chroma transform sizes, base MI position) is recorded per leaf during the partition walk into a luma-MI covering-block grid. The committed `syn-2sb-deblock-intra-128x64-q100.ivf` (128x64 = two 64x64 superblocks each PARTITION_SPLIT into four 32x32 DC_PRED luma + DC chroma blocks; `apply_deblocking_filter == [false, true, true, true]`, `df_delta_q == [0,0,0,0]`, base_q_idx 100) decodes bit-exactly to the avmdec AND dav2d oracle (raw I420 md5 ca302adc8641007251c5947b3d5c73ba). The deblock effect is the luma horizontal pass at the y=32 transform/block boundary: 32 edges are visited (xP 0..124 step 4, all yP=32), of which only the column at xP=4 changes samples (2 luma samples, ±1 at rows 31/32); the chroma passes visit 80 edges over the flat U=120/V=135 planes and the §7.17.7.2 filter choice + zero `deltaM2` leave them unchanged. Two additional fixtures with LARGER luma-horizontal effects (still all-DC multi-block, `apply == [false, true, true, true]`): `syn-2sb-deblock-intra-128x64-q98.ivf` (24 luma samples change, raw md5 5cb616189cf501809d6986a9ef1058a7) and `syn-2sb-deblockwide-intra-128x64-q100.ivf` (38 luma samples change, raw md5 82d8be50486d46fdd771e73a214bf5c2), both bit-exact vs avmdec AND dav2d. VERIFIED SUBSET: 8-bit 4:2:0 only (a deblock-ACTIVE 10-bit frame is rejected — the §7.17 math is bit-depth-generic but no 10-bit deblock oracle fixture pins it; a 10-bit deblock-OFF frame is unaffected), `df_delta_q` all zero (a nonzero per-pass §7.17.6 `DfDeltaQ[i]` shifts the filter level and is rejected), single tile, segmentation disabled. Coverage limit: only the luma-HORIZONTAL (apply index 1) deblock effect is exercisable in the admitted DC-multi-block subset — the encoder switches to non-DC / rectangular / larger partitions (outside the admitted subset) whenever the content has the structure needed to make the luma-vertical or a chroma deblock pass change samples, so those passes run faithfully over the decoded grid but are not yet pinned by a sample-changing oracle fixture. Out of scope / deferred: the other in-loop filters (GDF/CDEF/CCSO/loop-restoration, still rejected by the route gate), nonzero `df_delta_q`, `allow_df_sub_pu` sub-PU edges (inter only), 10-bit deblock, multiple tiles, segmentation/lossless segments, inter frames, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::deblock_active_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::deblock_off_frame_is_byte_identical",
    "crates/splot-decode/src/filters/deblock.rs::tests::adaptive_strength_for_lvl_100_8bit",
    "crates/splot-recon/src/workspace_tests.rs::workspace_set_reconstructed_sample_writes_and_validates",
]
commands = [
    "cargo test -p splot-decode deblock --locked",
    "cargo test -p splot-recon workspace_set_reconstructed --locked",
    "cargo xtask conformance",
    "cargo xtask check-fixtures",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2sb-deblock-intra-128x64-q100.ivf",
    "tests/conformance/vectors/valid/syn-2sb-deblock-intra-128x64-q98.ivf",
    "tests/conformance/vectors/valid/syn-2sb-deblockwide-intra-128x64-q100.ivf",
    "crates/splot-decode/src/filters/deblock.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-recon/src/workspace.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GENERAL-INTRA-CDEF"
name = "General intra §7.18 CDEF orchestration"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.18", "7.18.1", "7.18.2", "7.18.3", "5.20.10.1", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-18-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-1",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/cdef.rs"
change_id = "decode-general-intra-cdef"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Second in-loop-filter brick (after DECODE-GENERAL-INTRA-DEBLOCK): builds the §7.18/§7.18.1 CDEF 64x64-unit to 8x8-block orchestration over the `splot-recon` per-block primitives (RECON-CDEF-FILTER leaf math), relaxing the general intra route gate to admit a verified CDEF-ACTIVE subset (previously the route rejected any `cdef_frame_enable`). The new `crates/splot-decode/src/filters/cdef.rs` module, applied AFTER §7.17 deblocking and reading the deblocked CurrFrame in place before `workspace.freeze()` (filter order deblock then CDEF): (1) snapshots the deblocked three-plane frame (§7.18 filters CurrFrame INTO CdefFrame, so every tap must read a pre-CDEF sample regardless of raster write order); (2) iterates the §7.18 8x8 blocks (`step4 = 2` MI units) — `cdef_idx == 0` everywhere in the admitted subset, so every block uses the single strength set; (3) per 8x8 block runs the §7.18.2 `cdef_direction` over the deblocked luma 8x8, derives the §7.18.1 ordered steps 1-6 luma priStr (`(priStr * (4 + Min(FloorLog2(var>>6),12)) + 8) >> 4` when var nonzero, else 0), secStr, dir (`priStr==0 ? 0 : yDir`), and damping (`CdefDamping + coeffShift`), and runs the §7.18.3 filter over the 8x8 luma; then the §7.18.1 steps 9-14 chroma (priStr/secStr from `cdef_uv_*_strength`, dir from `Cdef_Uv_Dir[subX][subY][yDir]`, damping `CdefDamping + coeffShift - 1`) over the two 4:2:0 4x4 chroma blocks; (4) for each output sample fetches the six §7.18.3 directional taps via `cdef_get_at` with the §5.20.9.3 `is_inside_filter_region` (single tile -> `is_inside_frame`: the candidate luma MI must be inside the frame MI grid) availability check, calls `cdef_filter_sample`, clamps to the bit-depth range, and writes the deringed sample back via `CurrentFrameWorkspace::set_reconstructed_sample`. A CDEF-OFF frame leaves the params `None`, so the pass is SKIPPED (existing 8-bit and 10-bit fixtures stay byte-identical, verified). VERIFIED SUBSET: 8-bit 4:2:0 only (a CDEF-active 10-bit frame is rejected — the §7.18 math is bit-depth-generic but no 10-bit CDEF oracle fixture pins it), `CdefStrengths == 1` (so §5.20.10.1 `read_cdef` reads NO per-block symbol: `cdef_idx[r][c] == 0` for the whole frame — a multi-strength frame needs the per-block `cdef_index0`/`cdef_index_minus_1` reads, not modeled on this path), `cdef_on_skip_txfm_frame_enable == 1` (so the §7.18.1 `skip` is 0 with no Skips/LosslessArray lookup), single tile, segmentation disabled (no lossless segment). BOTH luma and chroma (uv) strengths are admitted and oracle-pinned. The committed fixtures (avm==dav2d, splot bit-exact): three luma-only-CDEF (uv strengths 0) 128x64 frames whose two 64x64 superblocks PARTITION_SPLIT into all-DC 32x32 blocks — `syn-2sb-cdef-intra-128x64-q130.ivf` (CdefDamping 5, y_pri 1 y_sec 4, raw md5 192e3935f9892345a14e02cb4baf4ba5), `syn-2sb-cdef-intra-128x64-q120.ivf` (CdefDamping 4, y_pri 2 y_sec 4, raw md5 2319a8f00af1ebb919a52ba18d90f4a1), and `syn-2sb-cdefdeblock-intra-128x64-q100.ivf` (deblock `apply [false,true,true,true]` AND CDEF both active, CdefDamping 4 y_pri 1 y_sec 4, raw md5 472d95801ce2a112160bcdfee93957d5 — pins the deblock-then-CDEF order); and the NONZERO-chroma-CDEF `syn-2sb-cdefuv-intra-128x64-q170.ivf` (two 64x64 PARTITION_NONE superblocks, both DC_PRED luma with left non-follow H_PRED chroma and right DC chroma, CdefDamping 5 y_pri 10 y_sec 4 AND uv_pri 2 uv_sec 4, raw md5 d783f353078cf156ba23dcfd3b2b50ad — the first to exercise the §7.18.1 chroma steps 9-14 sample-changingly: the `Cdef_Uv_Dir[1][1][yDir]` selection (engaged because uv_pri != 0), the 4:2:0 subsampled 4x4 chroma tap addressing, and the `CdefDamping - 1` chroma damping; over a thousand U and V samples each change). The §7.18.2 direction search drives a real per-block dering (yDir varies 0/2/4/6, var positive over the cosine-AC content), changing thousands of luma samples (and, for the nonzero-uv fixture, over a thousand chroma samples per plane) from the CDEF-off reconstruction. Out of scope / deferred: multi-strength (`CdefStrengths > 1`) frames and their per-block read_cdef symbols, `cdef_on_skip_txfm_frame_enable == 0` skip handling, lossless/segmentation skipChroma, 10-bit CDEF, multiple tiles, the other in-loop filters (GDF/CCSO/loop-restoration, still rejected), inter frames, and live AVM/dav2d in CI."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::general_intra_cdef_tests::cdef_active_intra_frame_decodes_to_oracle",
    "crates/splot-decode/src/filters/cdef.rs::tests::small_ringing_step_is_deringed_within_bounds",
    "crates/splot-decode/src/filters/cdef.rs::tests::flat_frame_is_unchanged",
    "crates/splot-decode/src/filters/cdef.rs::tests::nonzero_uv_strengths_dering_chroma_only",
    "crates/splot-decode/src/filters/cdef.rs::tests::uv_dir_selection_tracks_luma_direction_only_when_uv_pri_nonzero",
]
commands = [
    "cargo test -p splot-decode cdef --locked",
    "cargo test -p splot-recon cdef_filter --locked",
    "cargo xtask conformance",
    "cargo xtask check-fixtures",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2sb-cdef-intra-128x64-q130.ivf",
    "tests/conformance/vectors/valid/syn-2sb-cdef-intra-128x64-q120.ivf",
    "tests/conformance/vectors/valid/syn-2sb-cdefdeblock-intra-128x64-q100.ivf",
    "tests/conformance/vectors/valid/syn-2sb-cdefuv-intra-128x64-q170.ivf",
    "crates/splot-decode/src/filters/cdef.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-recon/src/cdef_filter.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTRA-ONLY-TILE-GROUP"
name = "Intra-only frame in Leading/Regular tile-group carrier"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.2", "5.19", "6.2.1", "7.2", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter.rs"
change_id = "decode-intra-only-tile-group"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Carrier/routing-specific support that depends on DECODE-GENERAL-INTRA-FRAME-FRONTIER for block parsing, reconstruction, filters, and output. FrameHeaderParseStatus is validated before frame-kind routing: only IntraHeaderComplete + INTRA_ONLY + frame_is_intra in OBU_REGULAR_TILE_GROUP or OBU_LEADING_TILE_GROUP is admitted; contradictory complete state is typed internal header state, modeled-region truncation is decode/malformed-source at §6.2.1, and UnsupportedUntilFeature remains a coverage stop. The split scheduler explicitly requires InterHeaderComplete and non-intra facts, while the fused 8/10-bit path selects FrameSetup::Intra for the validated intra status and preserves the existing common filter, CDF, reference-refresh, and output lifecycle. The pinned instrumented AVM RegularTileGroup fixture decodes byte-exact to the 512-byte raw oracle at one and eight threads. LeadingTileGroup shares the spec-defined intra header/tile carrier path and has positive status and boundary tests, but no AVM output claim. The fixture does not claim coeff.all_zero or expand the inherited general-intra tool frontier."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/prediction/inter/tests/header_state.rs::complete_intra_only_tile_group_is_admitted_for_regular_and_leading_carriers", "crates/splot-decode/src/prediction/inter/tests/header_state.rs::contradictory_intra_only_tile_group_state_is_typed", "crates/splot-decode/src/prediction/inter/tests/header_state.rs::truncated_inter_header_regions_are_malformed_source_diagnostics", "crates/splot-decode/src/prediction/inter/tests/header_state.rs::intra_only_tile_group_never_enters_split_inter_walk", "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs::derived_boundary_admits_intra_only_regular_and_leading_tile_groups", "crates/splot-decode/src/output/raw.rs::tests::intra_only_tile_group_fixture_matches_exact_raw_bytes", "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest"]
commands = ["cargo test -p splot-decode intra_only --locked", "cargo xtask conformance", "cargo xtask decoder-fixtures verify", "cargo test -p splot-cli --test decoder_oracle --locked", "cargo xtask check-decoder-support", "cargo xtask check-feature-status"]
fixtures = ["tests/conformance/vectors/valid/syn-2frame-intra-only-mono-16x16-q255.ivf"]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-FIRST-INTER-FRAME-FRONTIER"
name = "First inter frame decode frontier (verified target)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.2.1", "5.18.2", "5.19", "5.20", "5.20.7.2", "5.20.7.6", "5.20.7.10", "6.17.2", "6.18", "7.2", "7.7", "7.12.3.2", "7.13.3.18", "7.13.3.24", "7.23", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-10",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-3-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-24",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/mod.rs"
change_id = "decode-first-inter-frame-frontier"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Opens the INTER decode arc (the gate to real content). The committed `syn-2frame-inter-64x64.ivf` (project-owned flat synthetic input — luma 100, U 120, V 130, both frames identical; broad decode tools disabled; `--enable-global-motion=1 --qp=80 --sb-size=64 --min/max-partition-size=64`) is a minimal two-frame stream: frame 0 = OBU_TEMPORAL_DELIMITER + OBU_SEQUENCE_HEADER + OBU_CLOSED_LOOP_KEY (a single flat 64x64 intra key block), frame 1 = OBU_TEMPORAL_DELIMITER + OBU_REGULAR_TILE_GROUP (a single 64x64 inter block, single reference, GLOBALMV/NEARESTMV with zero MV and skip=1, so §7.13.3.18 zero-fraction motion compensation reduces to a straight copy of the co-located key block; no residual). avmdec --rawvideo --i420 and dav2d --demuxer ivf decode the whole stream byte-for-byte identically (decoded-output md5 4e1bd39f0b541ef1f479cff049e6985c, 12288 bytes; frame 1 == a copy of frame 0), recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml` (`lref-avm-dav2d-syn-2frame-inter-64x64`). The fixture validates clean and is registered in the conformance corpus. DONE (the first inter frame decodes BIT-EXACT): `splot decode syn-2frame-inter-64x64.ivf --output-format raw` reproduces the whole-stream md5 4e1bd39f0b541ef1f479cff049e6985c (12288 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf; frame 1 is a byte-exact copy of frame 0 (both flat Y=100/U=120/V=130). The actual decoded mode is single_mode==0 -> NEARMV (NOT GLOBALMV): the encoder chose NEARMV, whose §7.11 MV over the empty no-neighbour MV stack is the zero vector. NO step is hardcoded — the §8.2.4 exit_symbol() check over the inter tile guards bit-exactness, so a wrong symbol read would reject, never emit a confident-wrong copy. Pipeline: the stream planner admits the inter OBU_REGULAR_TILE_GROUP as a `DecodePlannedObuRole::InterFrameCandidate` (§5.2.1); the multi-frame runtime (`decode_minimal_frames_from_plan*`) decodes the key frame via the general-intra frontier, then (§7.23) retains its planes in a splot-recon `ReferenceFrameStore<&DecodedFrame<u8>>` (one valid slot modeled, so the §7.7 implicit reference-map derivation is exact) plus the §7.7/§7.23 `FrameReferenceStateView` metadata, then `decode_following_inter_frame` -> `crates/splot-decode/src/prediction/inter.rs`: parse §5.18.2 to InterHeaderComplete (reaching skip_mode_present==0, reference_select==0, use_global_motion==0, NumTotalRefs==1, ref_frame_idx==[0]); walk the §5.20.3 partition tree to the single 64x64 NONE block; read §5.20.7.6 inter mode_info — is_inter==1 (TileIsInterCdf[0]), skip==1 (TileSkipCdf[0]), single_mode==0/NEARMV (TileSingleModeCdf[0]), then read_drl_idx over TileDrlModeCdf[Min(idx,2)][0] (read_skip_mode/read_ref_frames single_ref/read_motion_mode read NO symbols in this subset); derive §7.11 MV=(0,0); §7.13.3.18 zero-fraction motion-compensation copy of the co-located reference planes; validate exit_symbol(). New inter CDF rows/selectors (is_inter/skip/single_mode/drl_mode) thread through the Block/Tile CDF subsets; the tile-payload boundary + §5.20.3 partition traversal are relaxed to admit an inter OBU_REGULAR_TILE_GROUP (the partition syntax is frame-type agnostic); `FrameCandidateTileFacts::from_inter_frame_core` derives inter tile facts. Pinned by `two_frame_inter_fixture_decodes_both_frames_bit_exact`, `inter_frame_is_a_bit_exact_copy_of_the_key_frame`, `two_frame_inter_fixture_per_frame_hash_is_stable` (splot-decode) and `decode_two_frame_inter_fixture_decodes_both_frames_bit_exact` (splot-cli). The 38 general-intra fixtures still decode byte-identical (no regression). VERIFIED-SUBSET DISCIPLINE: the admitted inter subset now also covers \u00a75.20.7.13 flexible MV resolution (per-block use_most_probable_precision / pb_mv_precision reads, sub-one-pel MV shells, \u00a75.20.7.13 lower_mv_precision predictor rounding), the \u00a75.20.7.14 SIMPLE-path inter_intra flag read (frontier-rejects a set flag), the \u00a75.20.7.2 dual neighbour lists (NPosBuf for is_inter/skip/use_amvd/comp_mode/single_ref contexts incl. both-list count_refs; NPos for interp_filter and the precision contexts), 10-bit inter frames, NEWMV with residual, and multi-superblock inter frames (syn-2frame-inter-64x64-10bit and syn-grid-inter-128x128-q80 decode byte-identical to avmdec --i420 --rawvideo; local decoder mission output frame 0 is byte-identical to avmdec, pinned by the ignored local_decoder_mission_full_stream_avm_compare harness with SPLOT_LOCAL_DECODER_MISSION_LIMIT=1). The admitted subset further covers the \u00a77.2 in-loop filter chain on inter frames (deblock \u2192 CDEF \u2192 CCSO \u2192 LR via the shared final-filter sink; deblock geometry recorded per decoded transform with \u00a75.20.6.2 max-rect skip tiling; syn-2frame-deblock-inter-32x32-10bit-q100, syn-2frame-cdef-inter-64x32-10bit-q120 and syn-2frame-ccso-inter-32x32-10bit-q100 decode byte-identical to avmdec), the \u00a75.20.7.14 use_extend_warp/use_local_warp reads over \u00a77.11.4 WarpSampleFound, and the \u00a75.20.7.27 inter chroma cctx_type read. Everything outside it (compound beyond skip NEAR_NEARMV, same-ref compound on reference_select frames, interintra/extend-warp/local-warp PREDICTION, nonzero inter cctx_type, \u00a75.18.7.12 CCSO reference reuse, multi-transform-unit intra prediction beyond the block-equivalent cardinal splits, temporal MVs with retained inter references, >2 valid reference slots, num_total_refs > 2, global motion, GDF, film grain, skip-mode) is rejected with a structured decode/unsupported-feature diagnostic BEFORE any output. avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml` (`lref-avm-dav2d-syn-2frame-inter-64x64`). b03 additions: the block mode context is derived AFTER reference selection per §5.20.7.6 (compound reader split into a reference-pair stage and a pair-context mode stage; provably behavior-neutral on the previously admitted surface); WARPMV smooth-mask interintra prediction (§5.20.7.15 parse + §7.13.3.29 Ii_Weights_1d mask + §7.13.3.30 blend, DC with the §7.13.2.12 IBP modifier / V / H per plane before-MC-predict then post-MC-blend; wedge interintra, II_SMOOTH, and the SIMPLE-path tail stay fail-closed defers pinned by `syn-3frame-simple-interintra-64x32-10bit.ivf` — interintra blend output is unreachable on every known stream until the EXTENDWARP/ext-block-warp/CCSO-reuse families land, so the blend is entropy- and unit-verified, output-pending); and §5.20.7.24 per-transform-unit intra prediction for intra-in-inter blocks (per-unit re-scoped prediction over just-reconstructed sibling samples, BlockDecoded marked per unit, per-unit above-MRL offsets; `syn-2frame-txsplit-intra-inter-64x64-10bit-q100.ivf` converted from a defer pin to a POSITIVE byte-exact pin, and the intra-in-inter DC path gained the previously missing IBP-DC arm). The local decoder mission frontier moved from the interintra defer through the per-unit defers to the first EXTENDWARP block (`inter_warp_extend_unimplemented`, still byte 8371, coded frame 2). b04 additions: the full warp motion family — retained warp neighbour state, §7.13.3.24 EXTENDWARP estimation (parameters verified identical to AVM on the stream's first EXTENDWARP block), §7.12.3 + §7.13.3.23 LOCALWARP least squares, the §7.13.3.20 extended block warp for §7.13.3.15 skipPred geometry (invalid shear / sub-8x8 planes), and non-NEWMV warp neighbour modes per §7.11.3 (the DRL-context divergence pinned against AVM accounting); §7.13.3.25 block adaptive weighted prediction (implicit template fit + explicit scales, chroma reusing the luma alpha); the remaining per-unit intra arms (kernel-identical square-to-rect directional/smooth mappings, §5.20.7.24 allowCorners = 0 counts on middle units); §7.21 display-order output (held implicit-output frames released by immediate/refresh/successive/end-of-stream events — the decoder previously emitted decode order); the §7.12.2.21 reference MV bank with the §5.20.2.2 per-superblock reset and row re-seed; and the §5.18.2 tip_frame_mode read (TIP frames stay fail-closed). Coded frame 2 decodes to completion (its recon divergence from AVM is tracked mechanism-by-mechanism in the mission workpapers; the reference warp-parameter bank is the next known gap), and the frontier is coded frame 3's frame-tool gate (temporal MVs with retained inter references)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::decode_two_frame_inter_fixture_decodes_both_frames_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::two_frame_inter_fixture_decodes_both_frames_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests/segmentation.rs::inherited_inter_segmentation_map_decodes_expected_frames",
    "crates/splot-decode/src/prediction/inter/tests/segmentation.rs::skip_segment_forces_skip_and_globalmv_decode_paths",
    "crates/splot-decode/src/prediction/inter/tests.rs::inter_frame_is_a_bit_exact_copy_of_the_key_frame",
    "crates/splot-decode/src/prediction/inter/tests.rs::two_frame_inter_fixture_per_frame_hash_is_stable",
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::regular_tile_group_is_admitted_as_inter_frame_candidate",
    "crates/splot-decode/src/pipeline/general_intra_tests.rs::two_frame_key_frame_reconstructs_flat_h_pred_chroma",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::extend_warp_uses_retained_compound_list1_mv",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/global_motion_tests.rs::globalmv_neighbour_uses_current_global_mv_and_retains_list0_model",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/global_motion_tests.rs::global_globalmv_neighbour_uses_current_global_mv_and_retains_list1_model",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/global_motion_tests.rs::nonmatching_globalmv_neighbour_derives_from_its_stored_sub_mv",
    "crates/splot-decode/src/prediction/inter/global_motion_tests.rs::global_candidate_fact_survives_force_integer_reconstruction_gating",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::local_warp_samples_include_retained_compound_list1_mv",
    "crates/splot-decode/src/output/hash.rs::tests::standalone_open_loop_key_matches_expected_output_hash",
    "crates/splot-decode/src/output/hash.rs::tests::bridge_celu_matches_expected_output_hashes",
    "crates/splot-decode/src/output/hash.rs::tests::scaled_bridge_matches_expected_output_hashes",
    "crates/splot-decode/src/output/hash.rs::tests::single_picture_bridge_matches_expected_output_hashes",
    "crates/splot-decode/src/pipeline/mod_tests.rs::in_band_long_term_prelude_accepts_hidden_clk_then_olk_in_sequential_slots",
    "crates/splot-decode/src/pipeline/mod_tests.rs::in_band_long_term_prelude_rejects_missing_or_visible_reference",
    "crates/splot-decode/src/pipeline/mod_tests.rs::in_band_long_term_prelude_rejects_overwritten_slot_and_clk_after_olk",
    "crates/splot-decode/src/pipeline/mod_tests.rs::in_band_long_term_prelude_does_not_cross_temporal_unit_boundary",
    "crates/splot-core/src/headers/frame/inter.rs::tests::mv_precision_av2_codes_match_table_6_19",
    "crates/splot-core/src/headers/frame/inter.rs::tests::inter_mv_precision_paths_preserve_following_bit_alignment",
    "crates/splot-core/src/headers/frame/info/tests/tail.rs::frame_header_core_inter_mv_precision_eof_boundaries_are_truncation",
    "crates/splot-decode/src/prediction/inter/tests/header_state.rs::avm_inter_fixtures_parse_quarter_and_eighth_pel_precision",
    "crates/splot-decode/src/prediction/inter/tests/header_state.rs::missing_inter_header_regions_are_typed_header_state_errors",
    "crates/splot-core/src/headers/frame/info/tests.rs::frame_header_core_eof_in_ref_long_term_id_loop",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-subpel-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-lr-switchable-768x256-8bit.ivf",
    "tests/conformance/vectors/valid/syn-3frame-seg-inherit-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-seg-skip-inter-64x64.ivf",
    "tests/conformance/vectors/valid/syn-orderhint-wrap-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-warp-sample-list1-48x48-q90.ivf",
    "tests/conformance/vectors/valid/syn-2frame-extendwarp-global-48x48-q185.ivf",
    "tests/conformance/vectors/valid/syn-bridge-celu-64x64.ivf",
    "tests/conformance/vectors/valid/syn-bridge-scaled-box-64x64.ivf",
    "tests/conformance/vectors/valid/syn-bridge-single-picture-32x32.ivf",
    "tests/conformance/vectors/valid/syn-frame-olk-leading-64x64.ivf",
    "tests/conformance/vectors/valid/syn-frame-ras-64x64.ivf",
    "tests/conformance/vectors/valid/syn-frame-sef-families-64x64.ivf",
    "tests/conformance/vectors/valid/syn-frame-switch-64x64.ivf",
    "tests/conformance/vectors/valid/syn-frame-tip-families-64x64.ivf",
    "tests/conformance/vectors/valid/syn-hidden-clk-ras-initial-tu-64x64.ivf",
    "tests/conformance/vectors/valid/syn-standalone-olk-64x64.ivf",
    "crates/splot-cli/tests/decode_cli.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-MULTI-TILE"
name = "Multi-tile inter frame decode"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.7.2", "5.19", "5.20.1", "5.20.2.1", "7.13.3.18", "7.23", "8.2.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-19",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter.rs"
change_id = "decode-inter-multi-tile"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Removes the inter_multi_tile_frame frontier by decoding every tile work unit derived from the §5.18.7.2 tile geometry and §5.19/§5.20.1 payload. Resource validation uses the largest tile payload and the declared tile count, while each tile retains independent entropy and tile-local neighbour bounds. The 125-byte pinned-AVMenc syn-2tile-inter-128x64-q80.ivf contains two tile columns across a key frame and one inter frame. Pre-fix splot rejected it at byte 112; fixed splot, pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774, and current AVM agree byte-for-byte on the complete 24,576-byte raw I420 SHA-256 acf7dcf80a387ad21064ad9d18016265c301059fa68ed684a9a90fba937aa143. Unsupported multi-tile in-loop-filter combinations continue to fail closed at their operation-specific consumer."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_tile_inter_fixture_decodes_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_tile_inter_fixture_enforces_tile_count_limit",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2tile-inter-128x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-2tile-zone1-edge-128x64-q192.ivf",
    "tests/conformance/vectors/valid/syn-2frame-lr-switchable-768x256-8bit.ivf",
    "crates/splot-decode/src/prediction/inter.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-SUBPEL-MV"
name = "Sub-pel inter frame decode (NEWMV + interpolation-filter convolution)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.6", "5.20.7.13", "5.20.7.20", "7.13.3.17", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-13",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-20",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-17",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/read_mv.rs"
change_id = "decode-inter-subpel-mv"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the inter decode arc from the zero-MV copy (DECODE-FIRST-INTER-FRAME-FRONTIER) to a real sub-pel NEWMV block, wiring the merged §7.13.3.18 convolution kernel (RECON-SUBPEL-MC) into the decode path. The committed `syn-2frame-subpel-inter-64x64.ivf` (project-owned synthetic horizontal half-cosine luma + flat chroma; frame 1 shifted right by half a sample; broad decode tools incl. GDF/intra-dip/bawp/cwp/flex-mvres/adaptive-mvd/global-motion/warp/tip/refinemv/opfl-refine/masked-interintra-compound/joint-mvd/ref-frame-mvs disabled; `--qp 180 --sb-size 64 --min/max-partition-size 64`) is a two-frame stream: frame 0 = OBU_CLOSED_LOOP_KEY (a single 64x64 DC_PRED intra key block with a low-frequency AC residual, decoded by the general-intra frontier), frame 1 = OBU_REGULAR_TILE_GROUP (a single 64x64 single-reference NEWMV inter block, EighthPel (0, -4) horizontal half-sample sub-pel MV, SWITCHABLE EIGHTTAP_SHARP interpolation filter, skip=1, no residual). DONE (the first sub-pel inter frame decodes BIT-EXACT): `splot decode syn-2frame-subpel-inter-64x64.ivf --output-format raw` reproduces the whole-stream md5 a0e82de3a95bb4b519c4c84ffa2ba816 (12288 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-2frame-subpel-inter-64x64`). NO step is hardcoded — the §8.2.4 exit_symbol() check over the inter tile guards bit-exactness, so a wrong MV/filter symbol read would reject, never emit a confident-wrong frame. Pipeline (on top of DECODE-FIRST-INTER-FRAME-FRONTIER): the §5.20.7.6 block decode admits single_mode==2 (NEWMV), reads its DRL (read_drl_idx over TileDrlModeCdf[Min(idx,2)][0]), then the §5.20.7.20 SHELL-coded read_mv (shell_set -> TileJointShellSetCdf; shell_class -> TileJointShell6ClassCdf[shell_set] for the EighthPel P==6 precision; shell_offset_low_class/class2/other_class; col_mv_greater/col_remainder NS(n)/col_mv_index; with one §5.20.7.13 mv_sign L(1) bypass bit per nonzero component — sign derivation is disabled for EighthPel) over the zero no-neighbour predictor, then the §5.20.7.6 interp_filter symbol (TileInterpFilterCdf[ctx=3] when the frame filter is SWITCHABLE and needs_interp_filter() is 1). The decoded MV (0, -4) drives the §7.13.3.17 motion_vector_scaling (identity scale -> startX/startY with the eighth-pel fractional phase, stepX==stepY==1024) and §7.13.3.18 firstX/firstY/lastX/lastY clip bounds per plane (luma + 4:2:0 chroma via the (2*mv)>>sub adjustment), fed to splot-recon `subpel_predict_block` (the merged separable interpolation-filter convolution). New tile/block CDF banks (joint_shell_set, the P==6 shell_class pair, joint_shell_last_two_classes, shell_offset_low_class/class2/other_class, col_mv_greater, col_mv_index, interp_filter) thread through the Block/Tile CDF subsets from the generated §9.3 defaults. Pinned by `subpel_fixture_decodes_two_frames`, `subpel_inter_frame_differs_from_key_frame`, `subpel_fixture_per_frame_hash_is_stable` (splot-decode) plus the read_mv/mv_scaling unit tests; the zero-MV inter (4e1bd39f) and the general-intra fixtures stay byte-identical (no regression). VERIFIED-SUBSET DISCIPLINE: everything outside the single-reference NEWMV (or zero-MV NEARMV/GLOBALMV) skip==1 EighthPel SWITCHABLE-or-fixed-filter subset (residual skip==0, compound, multi-reference, non-64x64, motion modes, OBMC, warp, flex-mvres, adaptive-mvd, bawp, cwp) is rejected with a structured decode/unsupported-feature diagnostic BEFORE any output. avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::subpel_fixture_decodes_two_frames",
    "crates/splot-decode/src/prediction/inter/tests.rs::subpel_inter_frame_differs_from_key_frame",
    "crates/splot-decode/src/prediction/inter/tests.rs::subpel_fixture_per_frame_hash_is_stable",
    "crates/splot-decode/src/prediction/inter/read_mv/tests.rs::read_ns_returns_values_in_range",
    "crates/splot-decode/src/prediction/inter/mv_scaling.rs::fractional_mv_produces_subpel_phase",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-subpel-inter-64x64.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-RESIDUAL-DCT"
name = "Inter block residual decode (skip == 0, DCT_DCT)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.6.1", "5.20.7", "5.20.7.27", "5.20.8.2", "5.20.8.3", "6.17.6.1", "7.14.2", "7.14.3", "7.14.4", "7.15.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-6-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-6-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-15-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block.rs"
change_id = "decode-inter-residual-dct"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Extends the inter decode arc from the skip == 1 no-residual cases (DECODE-FIRST-INTER-FRAME-FRONTIER zero-MV copy, DECODE-INTER-SUBPEL-MV sub-pel) to a real skip == 0 inter block carrying a §5.20.7.27 coded residual added over the §7.13.3.18 motion-compensated prediction — the most fundamental remaining inter piece. The committed `syn-2frame-inter-residual-64x64.ivf` (project-owned flat-100 luma + flat chroma frame 0 plus a smooth low-frequency luma delta on frame 1; broad decode tools incl. GDF/intra-dip/intra-edge-filter/bawp/cwp/flex-mvres/adaptive-mvd/warp/tip/refinemv/opfl-refine/masked-wedge-interintra-onesided-diffwtd-compound/joint-mvd/ref-frame-mvs/inter-ist/inter-ddt/cctx/fsc/idtx-intra disabled; `--enable-global-motion=1 --qp 80 --sb-size 64 --min/max-partition-size 64`) is a two-frame stream: frame 0 = OBU_CLOSED_LOOP_KEY (a single flat 64x64 DC_PRED intra key block, decoded by the general-intra frontier), frame 1 = OBU_REGULAR_TILE_GROUP (a single 64x64 single-reference zero-MV NEARMV inter block with skip == 0, a §5.20.7.27 luma DCT_DCT residual, flat chroma so no chroma residual). The additional 111-byte `syn-2frame-flatstep-inter-y-dc-delta1-64x64-q80.ivf` fixture signals `DeltaQYDc=1` and decodes to the same 12,288-byte raw SHA-256 `a3109f9f21810ac7dc529212e5978fd9a038aedf4f1c9e680fb1990581c91f6d` in Splot and AVM; frame 1 changes all 4,096 luma samples while chroma remains flat. DONE (the first inter-residual frame decodes BIT-EXACT): `splot decode syn-2frame-inter-residual-64x64.ivf --output-format raw` reproduces the whole-stream md5 ab2b067aed48cf46035fa031cefb3ab1 (12288 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-2frame-inter-residual-64x64`). NO step is hardcoded — the §8.2.4 exit_symbol() check over the inter tile guards bit-exactness, so a wrong residual coefficient / sign / eob symbol read would reject, never emit a confident-wrong frame (frame 1 luma differs from frame 0 by 3348 of 4096 samples, proving the residual is genuinely decoded and added, not a copy). Pipeline (on top of DECODE-FIRST-INTER-FRAME-FRONTIER): §5.20.7 decode_block reads mode_info (is_inter==1, skip==0, single_mode NEARMV, DRL, interp_filter), then under TX_MODE_LARGEST read_block_tx_size() reads NO symbol (read_tx_size returns 0 -> TxSize = maxRectTxSize = TX_64X64); §5.20.8.3 get_tx_set(TX_64X64, 0) returns TX_SET_DCTONLY (txSzSqrUp > TX_32X32 && txSzSqr >= TX_32X32), so §5.20.8.2 transform_type() reads NO inter_tx_type symbol and PlaneTxType == DCT_DCT (the chroma tx type is DERIVED, not signalled — §5.20.8.2 reads no chroma tx_type symbol; for an inter TX_32X32 chroma block §5.20.8.3 get_tx_set is TX_SET_DCT_IDTX, not the DCT-only branch, but chroma forces DCT_DCT); §5.20.7.27 residual() reads the luma TX_64X64 then U/V TX_32X32 coefficients via the SHARED intra coefficient loop (decode_general_intra_plane_coeffs) with is_inter == true — the only inter-specific contexts are the §8.3.2 TileTxbSkipCdf[is_inter || fsc_mode] all_zero bank (index 1 for inter; fsc_mode false here) and the luma eobCtx = is_inter (=1); the coeff_base / coeff_br CDF banks are unified across inter/intra. The decoded per-plane Quant[] feeds §7.14.4 dequant (the luma DCT_DCT TCQ dqDenom +1 term applies only when the frame's allow_tcq is set; chroma never), §7.15.4 inverse transform, and §7.14.3 residual add over the §7.13.3.18 MC prediction held in the workspace (reconstruct_inter_block_residual_into reads the predicted block, adds the residual, writes back), then Clip1; an all_zero plane leaves the prediction. Pinned by `residual_fixture_decodes_two_frames`, `residual_inter_frame_differs_from_key_frame`, `residual_fixture_per_frame_hash_is_stable`, `effective_quantizer_deltas_include_frame_and_sequence_offsets` (splot-decode) and `decode_two_frame_inter_residual_fixture_decodes_bit_exact` (splot-cli). The skip == 1 inter fixtures (zero-MV 4e1bd39f, sub-pel a0e82de3) and the general-intra fixtures stay byte-identical (no regression); the is_inter coeff plumbing is an exact no-op for the intra callers. VERIFIED-SUBSET DISCIPLINE: everything outside the single-reference zero-MV-or-sub-pel skip ∈ {0,1} 64x64 DCT-only-residual subset is rejected with a structured decode/unsupported-feature diagnostic BEFORE any output — the residual decode still rejects unsupported transform and entropy tools such as inter-IST (sec_tx_type), inter-DDT, CCTX, FSC, and IDTX-intra. Frame and sequence quantizer deltas are combined per plane exactly as §7.14.2 requires and installed once for the whole inter tile decode, so ordinary, compound, warp, intra-in-inter, and deferred residual reconstruction share the same state; unrelated inter limitations retain their own fail-closed gates. The fixture's chroma is all_zero (inter chroma == flat key chroma), so this luma-residual + chroma-skip path is oracle-exact; a coded chroma inter residual (the chroma nonzero coeff pass) and the chroma inter tx-type derivation are NOT yet oracle-exercised (a future non-zero-chroma-residual fixture would test that path). avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::residual_fixture_decodes_two_frames",
    "crates/splot-decode/src/prediction/inter/tests.rs::residual_inter_frame_differs_from_key_frame",
    "crates/splot-decode/src/prediction/inter/tests.rs::residual_fixture_per_frame_hash_is_stable",
    "crates/splot-decode/src/prediction/inter/tests.rs::effective_quantizer_deltas_include_frame_and_sequence_offsets",
    "crates/splot-decode/src/prediction/inter/tests.rs::residual_reconstruction_uses_nonzero_luma_dc_quantizer_delta",
    "crates/splot-decode/src/prediction/inter/tests.rs::skip_one_inter_allows_nonzero_effective_quantizer_deltas",
    "crates/splot-decode/src/prediction/inter/tests.rs::nonzero_y_dc_quantizer_delta_fixture_is_bit_exact",
    "crates/splot-cli/tests/decode_cli.rs::decode_two_frame_inter_residual_fixture_decodes_bit_exact",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-inter-residual-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-flatstep-inter-y-dc-delta1-64x64-q80.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-MVSTACK-SPATIAL"
name = "Multi-block inter MV stack (spatial neighbour prediction)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3", "5.20.7.2", "5.20.7.6", "5.20.7.8", "7.11.2", "7.11.3", "7.12.2", "7.12.2.16", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-11-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-11-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-16",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/find_mv_stack.rs"
change_id = "decode-inter-mvstack-spatial"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Widens the inter decode arc from a single 64x64 inter block (DECODE-FIRST-INTER-FRAME-FRONTIER / DECODE-INTER-SUBPEL-MV / DECODE-INTER-RESIDUAL-DCT all decode exactly one top-left 64x64 block) to a MULTI-BLOCK inter frame where a later block's motion vector is PREDICTED from a decoded neighbour block via the §7.11/§7.12 spatial MV-context + MV-stack processes — the inter analog of the intra partition-depth unlock. The committed `syn-2frame-inter-mvstack-64x64.ivf` (project-owned synthetic input: frame 0 four flat 32x32 luma quadrants 100/150/180/60 + flat chroma, frame 1 shifted left 6 luma samples; broad decode tools incl. GDF/cdef/deblocking/restoration/ccso/cfl/mhccp/cctx/qm/ibp/angle-delta/ist/inter-ist/intra-dip/idtx-intra/smooth-intra/paeth-intra/bawp/cwp/tcq/trellis/all-warp/tip/opfl-refine/refinemv/masked-wedge-interintra-onesided-diffwtd-compound/joint-mvd/adaptive-mvd/flex-mvres/ref-frame-mvs/refmvbank/drl-reorder/mv-traj/high-motion/inter-ddt/skip-mode/bru/global-motion/tx-partition/ext-uneven-partitions disabled; `--qp 80 --sb-size 64 --min-partition-size 32 --max-partition-size 32 --enable-rect-partitions=1`) is a two-frame stream: frame 0 = OBU_CLOSED_LOOP_KEY (DC_PRED intra blocks, decoded by the general-intra frontier), frame 1 = OBU_REGULAR_TILE_GROUP whose 64x64 superblock is §5.20.3 SPLIT into four 32x32 single-reference inter blocks. DONE (the first multi-block neighbour-predicted-MV frame decodes BIT-EXACT): `splot decode syn-2frame-inter-mvstack-64x64.ivf --output-format raw` reproduces the whole-stream md5 e5b581a55433785c0071b635d5642083 (12288 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-2frame-inter-mvstack-64x64`). The additional `syn-3frame-derived-smvp-96x64-q100.ivf` oracle proves §7.12.2.16 same-side order-hint projection is spatial state and remains active when `use_ref_frame_mvs` disables only the temporal scan: at MI(12,13), the reference-0 DeltaWarp sub-MV (-12,9) projects to the reference-1 predictor (-24,16), yielding final MV (-16,24); the pinned AVM and dav2d raw MD5 is 7b5f9068d515351d82ae3363166d8052 (55296 bytes), recorded as `lref-avm-dav2d-syn-3frame-derived-smvp-96x64-q100`. Confirmed via the AVM inspection oracle: block 0 @ MI(0,0) is NEWMV with mv (col 48 = +6 full pels, eighth-pel units, ref 0); blocks 1-3 are NEARMV that reconstruct (col 48) from the §7.12.2 stack (NOT the zero fallback). NO step is hardcoded — the §8.2.4 exit_symbol() check guards bit-exactness, so a wrong §5.20.7.6 mode / §5.20.7.8 DRL / §5.20.7.20 MV / neighbour-context read rejects rather than emitting a confident-wrong frame. Pipeline: the §5.20.3 partition walk (already frame-type-agnostic) invokes a per-leaf inter closure; each block derives its §5.20.7.2 neighbour-buffer §8.3.2 contexts (is_inter ctx from NNumBuf + NIntra, skip_flag ctx from the neighbour Skips[] sum) and §7.11.2 find_mode_ctx NewMvContext (the leftA/aboveA/leftB/aboveB scan-point probes -> single_mode / DRL CDF context), reads mode_info, builds the §7.12.2 find_mv_stack (the ordered §7.12.2.6 spatial scan-point steps -> §7.12.2.10 add-reference -> §7.12.2.12 search-stack dedupe, then §7.12.2.20 extra-search global-MV fallback + §7.12.2.23 clamp), and resolves the §5.20.7.13 assign_mv MV (NEARMV = RefStackMv[RefMvIdx]; NEWMV = clamp(predictor + read_mv delta); GLOBALMV = zero). Each block records its IsInters/RefFrames[0]/YModes/Mvs[0]/Skips into the per-MI neighbour grid so a later block in decode order predicts from it, then is §7.13.3.18 motion-compensated at its own luma-space rectangle into the shared frame workspace. The single-block inter fixtures (zero-MV 4e1bd39f, sub-pel a0e82de3, residual ab2b067a) and all general-intra fixtures stay byte-identical (no regression). VERIFIED-SUBSET DISCIPLINE: the find_mv_stack kernel models only the spatial single-prediction subset the fixture proves; temporal candidates (§7.12.2.7/§7.12.2.8 — caller requires use_ref_frame_mvs == 0), compound (§7.12.2.13-§7.12.2.18), warp candidates (§7.12.2.2-§7.12.2.4/§7.12.2.9/§7.12.2.11), the ref-MV bank (§7.12.2.21) and its derived-SMVP insertion (§7.12.2.22), the DRL reorder sort (§7.12.2.19), the §7.12.2.5 scan-col wider reach, and the large-block (> 32x32) extra MVP combinations are deferred with explicit TODO(spec: DECODE-INTER-MVSTACK-SPATIAL) markers. The temporal scan (enable_ref_frame_mvs), the ref-MV bank (enable_refmvbank), and the DRL reorder (DrlReorder != Disabled) are provable no-ops for a NO-NEIGHBOUR block (empty motion field on the first inter frame, empty per-tile bank, no sort without >= 4 neighbours), so a single-block inter frame that enables them still decodes correctly; a block WITH a decoded neighbour rejects them (inter_block_mv_stack_tools_with_neighbour) before any output, because the §8.2.4 exit_symbol() bit-count check cannot catch a wrong MV value. The remaining deferred candidates (compound, warp, ref-MV-bank-derived SMVP, scan-col, large-block MVP combos) stay gated absent by the inter frame-header tool rejections + the single-reference subset gates. A multi-block skip == 0 residual (per-block transform sizes) is also deferred (rejected before output, inter_block_multiblock_residual). avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::block1_predicts_block0_mv_via_left_neighbour",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::block2_predicts_block0_mv_via_above_neighbour",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::same_side_spatial_projection_survives_disabled_temporal_scan",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::single_mv_stack_derives_tip_neighbour_for_another_reference",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::single_mv_storage_keeps_the_first_six_candidates",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/derived.rs::tests::derived_mv_storage_keeps_the_first_four_candidates",
    "crates/splot-decode/src/prediction/inter/tests.rs::mvstack_fixture_decodes_two_frames",
    "crates/splot-decode/src/prediction/inter/tests.rs::mvstack_fixture_per_frame_hash_is_stable",
    "crates/splot-cli/tests/decode_cli.rs::decode_two_frame_inter_mvstack_fixture_decodes_bit_exact",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-inter-mvstack-64x64.ivf",
    "tests/conformance/vectors/valid/syn-3frame-derived-smvp-96x64-q100.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-MULTI-SB-SPATIAL"
name = "Multi-superblock inter frame (cross-superblock MV prediction)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.3", "5.20.2.1", "5.20.3", "5.20.7.6", "7.11.2", "7.12.2", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-11-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter.rs"
change_id = "decode-inter-multi-sb-spatial"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Widens the inter decode arc from a SINGLE 64x64 superblock (DECODE-INTER-MVSTACK-SPATIAL decodes a multi-BLOCK frame but still inside one 64x64 superblock) to a MULTI-SUPERBLOCK inter frame: a single superblock ROW (height 64, width a positive multiple of 64) of 64x64 superblocks, iterated by the §5.20.2.1 decode_tile() raster loop (already in the shared partition walker), where a block in the SECOND superblock predicts its motion vector from the immediately-prior superblock's reconstructed-edge neighbour via the frame-wide §7.11/§7.12 find_mv_stack grid — a prerequisite for real-content scale (local decoder mission is 1920x1080). The committed `syn-2sb-inter-128x64-q80.ivf` (project-owned synthetic input: frame 0 two flat 64x64 luma superblocks 100/150 + flat chroma, frame 1 shifted left 6 luma samples; broad decode tools incl. GDF/cdef/deblocking/restoration/ccso/cfl/mhccp/cctx/qm/ibp/mrls/intra-edge-filter/angle-delta/ist/inter-ist/intra-dip/idtx-intra/smooth-intra/paeth-intra/bawp/cwp/tcq/trellis/all-warp/tip/opfl-refine/refinemv/masked-wedge-interintra-onesided-diffwtd-compound/joint-mvd/adaptive-mvd/flex-mvres/ref-frame-mvs/refmvbank/drl-reorder/mv-traj/high-motion/inter-ddt/skip-mode/bru/global-motion/tx-partition/rect/ext/uneven-partitions/parity-hiding/mvd-sign-derive disabled; `--qp 80 --sb-size 64 --min-partition-size 64 --max-partition-size 64`) is a two-frame 128x64 stream: frame 0 = OBU_CLOSED_LOOP_KEY (two DC_PRED intra superblocks, decoded by the general-intra frontier), frame 1 = OBU_REGULAR_TILE_GROUP whose two 64x64 superblocks are each a single 64x64 single-reference inter block. DONE (the first multi-superblock inter frame decodes BIT-EXACT): `splot decode syn-2sb-inter-128x64-q80.ivf --output-format raw` reproduces the whole-stream md5 477a993d671e93d37b92a0d368c238ff (24576 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-2sb-inter-128x64-q80`). Confirmed via splot's own instrumentation: SB0 @ MI(0,0) is NEWMV with mv (col 48 = +6 full pels, eighth-pel units, ref 0, has_neighbour=false); SB1 @ MI(0,16) — in the SECOND superblock — is NEARMV (has_neighbour=true, NewMvContext 3) that reconstructs (col 48) from SB0 across the superblock boundary via the §7.12.2 stack (NOT the zero fallback). NO step is hardcoded — the §8.2.4 exit_symbol() check guards bit-exactness, so a wrong §5.20.7.6 mode / §5.20.7.8 DRL / §5.20.7.20 MV / neighbour-context read rejects rather than emitting a confident-wrong frame. Pipeline: the only decoder change over DECODE-INTER-MVSTACK-SPATIAL is the §5.18.3 frame-size gate in validate_inter_frame_core — it now admits a single superblock ROW (height 64, width a multiple of 64) of 64x64 superblocks instead of exactly 64x64 (the §5.20.2.1 SB raster loop, the frame-wide find_mv_stack grid, decode_inter_blocks, and the tile-payload boundary derivation were already geometry-agnostic and unchanged). VERIFIED-SUBSET DISCIPLINE: a full 2-D superblock grid (both dimensions > 64) is REJECTED (inter_unsupported_frame_size) because a non-leftmost non-top superblock's §7.12.2 above-right / below-left scan positions reach a not-yet-decoded superblock, a case this kernel does not yet model; the existing >= 32x32-leaf / single-reference / no-tools / spatial-only-find_mv_stack gates are unchanged, so the multi-block-within-a-superblock and single-superblock fixtures stay byte-identical. The skip == 0 residual is still gated to the top-left 64x64 block (inter_block_multiblock_residual), so the admitted multi-SB frame is skip == 1; all existing inter (zero-MV 4e1bd39f, sub-pel a0e82de3, residual ab2b067a, mvstack e5b581a5) and general-intra fixtures stay byte-identical (no regression). The single-superblock COLUMN case (64x128, two vertically-stacked superblocks, SB1 @ MI(16,0) NEARMV predicting SB0's vertical MV) is analytically correct and was confirmed bit-exact locally, but it is REJECTED by the gate (inter_unsupported_frame_size) — NOT admitted — pending its own committed 3-oracle fixture (deferred follow-on); only the ROW is admitted and committed. avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_sb_fixture_decodes_two_frames",
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_sb_fixture_per_frame_hash_is_stable",
    "crates/splot-cli/tests/decode_cli.rs::decode_multi_sb_inter_fixture_decodes_bit_exact",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2sb-inter-128x64-q80.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-GRID-SPATIAL"
name = "2-D-grid inter frame (cross-superblock-row MV prediction)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.3", "5.20.2.1", "5.20.3", "5.20.7.6", "7.11.2", "7.12.2", "7.12.2.6", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-11-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter.rs"
change_id = "decode-inter-grid-spatial"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Widens the inter decode arc from a single superblock ROW (DECODE-INTER-MULTI-SB-SPATIAL admitted the ROW only; the single-SB column was analytically verified but REJECTED pending its own fixture) to a full 2-D superblock GRID (this brick is also the first to admit the single-SB column, as a degenerate 1-D case of the grid): width AND height each a positive multiple of 64, iterated by the §5.20.2.1 decode_tile() raster loop (sb_row outer, sb_col inner), where a block in the SECOND superblock ROW predicts its motion vector from an already-decoded SB-row-0 superblock ACROSS the superblock-row boundary via the frame-wide §7.11/§7.12 find_mv_stack grid — a further prerequisite for real-content scale (local decoder mission is 1920x1080). The committed `syn-grid-inter-128x128-q80.ivf` (project-owned synthetic input: frame 0 four flat 64x64 luma superblocks 100/150/80/200 + flat chroma, frame 1 shifted left 6 luma samples; broad decode tools incl. GDF/cdef/deblocking/restoration/ccso/cfl/mhccp/cctx/qm/ibp/mrls/intra-edge-filter/angle-delta/ist/inter-ist/intra-dip/idtx-intra/smooth-intra/paeth-intra/bawp/cwp/tcq/trellis/all-warp/tip/opfl-refine/refinemv/masked-wedge-interintra-onesided-diffwtd-compound/joint-mvd/adaptive-mvd/flex-mvres/ref-frame-mvs/refmvbank/drl-reorder/mv-traj/high-motion/inter-ddt/skip-mode/bru/global-motion/tx-partition/rect/ext/uneven-partitions/parity-hiding/mvd-sign-derive disabled; `--qp 80 --sb-size 64 --min-partition-size 64 --max-partition-size 64`) is a two-frame 128x128 stream: frame 0 = OBU_CLOSED_LOOP_KEY (four DC_PRED intra superblocks, decoded by the general-intra 2-D grid frontier), frame 1 = OBU_REGULAR_TILE_GROUP whose four 64x64 superblocks are each a single 64x64 single-reference inter block, all skip==1. DONE (the first 2-D-grid inter frame decodes BIT-EXACT): `splot decode syn-grid-inter-128x128-q80.ivf --output-format raw` reproduces the whole-stream md5 897bf67e72ec04cb7275fae08eab700c (49152 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-grid-inter-128x128-q80`). Confirmed via splot's own instrumentation: SB0 @ MI(0,0) is NEWMV with mv (col 48 = +6 full pels, eighth-pel units, ref 0, has_neighbour=false); SB1 @ MI(0,16), SB2 @ MI(16,0), SB3 @ MI(16,16) are NEARMV (has_neighbour=true, NewMvContext 3) that reconstruct (col 48) from SB0 via the §7.12.2 stack — SB2/SB3, in the SECOND superblock ROW, predict across the superblock-row boundary (NOT the zero fallback). NO step is hardcoded — the §8.2.4 exit_symbol() check guards bit-exactness, so a wrong §5.20.7.6 mode / §5.20.7.8 DRL / §5.20.7.20 MV / neighbour-context read rejects rather than emitting a confident-wrong frame. Pipeline: the ONLY decoder change over DECODE-INTER-MULTI-SB-SPATIAL is the §5.18.3 frame-size gate in validate_inter_frame_core — it now admits a full 2-D grid (width AND height each a multiple of 64) instead of a single SB row (and is the first to admit the single-SB column, a degenerate 1-D case). The wide grid gate is a geometric generalization of the committed 128x128 fixture, mirroring the intra DECODE-GENERAL-INTRA-GRID precedent (which admits arbitrary grids from a single 128x128 fixture); the 128x128 fixture exercises the qualitative neighbour relationships (left, above, above-left, above-right in-frame and out-of-frame, no-left corner) a larger grid repeats. NO find_mv_stack change was needed: the §7.12.2.6 Scan point process invokes the add-reference-MV step only when `is_inside(mvRow, mvCol)` AND `RefFrames[mvRow][mvCol][0] has been written for this frame (this checks that the candidate location has been decoded)` — both conditions are already modelled by `NeighbourMvGrid::get` returning None for an out-of-bounds position (is_inside == 0) OR an undecoded MI cell (RefFrames not yet written). Because the §5.20.2.1 loop decodes every superblock in raster order and `record_block` writes each block's MI cells before the next block's scan, a later superblock's above / above-right / below-left probe into a not-yet-decoded superblock reads an unwritten cell -> None -> no candidate, exactly as the spec's availability gate prescribes (verified by the new find_mv_stack unit tests `second_sb_row_block_predicts_above_sb_mv_across_sb_row_boundary` and `undecoded_later_sb_column_yields_no_candidate`). VERIFIED-SUBSET DISCIPLINE: a partial (non-multiple-of-64) frame size is REJECTED (inter_unsupported_frame_size); the skip == 0 residual is still gated to the top-left 64x64 block (inter_block_multiblock_residual), so the admitted grid frame is skip == 1 and a multi-superblock skip==0 residual stays rejected; the existing >= 32x32-leaf / single-reference / no-tools / spatial-only-find_mv_stack gates are unchanged, so all existing inter (zero-MV 4e1bd39f, sub-pel a0e82de3, residual ab2b067a, mvstack e5b581a5, SB-row 477a993d) and general-intra fixtures stay byte-identical (no regression). The deferred §7.12.2 temporal/compound/warp/ref-MV-bank-derived-SMVP/DRL-reorder candidates inherited from DECODE-INTER-MVSTACK-SPATIAL remain rejected once a block has a decoded neighbour. SCOPE OF PROOF (honest): the fixture proves the 2-D-grid geometry + cross-SB-row MV-stack AVAILABILITY (a decoded earlier-row superblock contributes a candidate, not the zero fallback). Because all four blocks reconstruct the SAME MV (col 48), the fixture does NOT discriminate the per-neighbour stack ORDERING (left vs above precedence with distinct MVs); and the §7.12.2.20 large-block (>32x32) mixed MVP candidates DO apply to the verified 64x64 leaves (>=32x32 lower gate only, no upper bound) but are deferred — kept safe only by the identical-MV fixture coincidence (mixed candidates coincide with existing entries, nothing appended) AND the §5.20.7.8 inter_block_drl_idx_out_of_range reject (a RefMvIdx into a mixed-only slot lands past the shorter stack). Both are spec-faithful but unproven, inherited from the single-row/SB bricks (find_mv_stack is unchanged here) and deferred to a committed distinct-neighbour-MV fixture (TODO(spec: DECODE-INTER-MVSTACK-SPATIAL) for §7.12.2.20 + the ordering discrimination). avm_diff remains todo (live AVM/dav2d differential in CI); the local oracle agreement is recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::second_sb_row_block_predicts_above_sb_mv_across_sb_row_boundary",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::undecoded_later_sb_column_yields_no_candidate",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-grid-inter-128x128-q80.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-MVORDER-SPATIAL"
name = "distinct-neighbour-MV §7.12.2 stack ordering (left-before-above)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3", "5.20.7.6", "5.20.7.8", "7.11.2", "7.12.2", "7.12.2.6", "7.12.2.12", "7.13.3.18"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-11-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/find_mv_stack.rs"
change_id = "decode-inter-mvorder-spatial"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "CLOSES the verified-subset honesty gap left by every prior inter fixture: DECODE-INTER-MVSTACK-SPATIAL / DECODE-INTER-MULTI-SB-SPATIAL / DECODE-INTER-GRID-SPATIAL all propagated ONE identical motion vector (SB0/block0 NEWMV col 48; later blocks NEARMV reusing col 48), so the §7.12.2 spatial MV stack collapsed to a single entry and the per-neighbour scan-point ORDERING (which-neighbour-wins, left vs above precedence with DISTINCT MVs) plus the §5.20.7.8 DRL slot selection were EXERCISED-BUT-NOT-DISCRIMINATED — a wrong order decoded identically and §8.2.4 exit_symbol() (bit-count-only) could not catch it. The committed `syn-2frame-inter-mvorder-64x64.ivf` (project-owned synthetic input: frame 0 four flat 32x32 luma quadrants 100/150/60/200 + flat chroma, frame 1 = each quadrant shifted by a DIFFERENT amount — top-left +8, top-right -4, bottom-left +4, bottom-right -4 luma samples, edge-clamped; same broad-decode-tools-disabled flags as the mvstack fixture: `--qp 80 --sb-size 64 --min-partition-size 32 --max-partition-size 32 --enable-rect-partitions=1`) is a two-frame 64x64 stream: frame 0 = OBU_CLOSED_LOOP_KEY (DC_PRED intra blocks), frame 1 = OBU_REGULAR_TILE_GROUP whose 64x64 superblock is §5.20.3 SPLIT into four 32x32 single-reference inter blocks, all skip==1, each carrying a DISTINCT MV (confirmed via temporary splot instrumentation, since removed): block 0 @ MI(0,0) NEWMV col 64 (+8 pel), block 1 @ MI(0,8) NEWMV col -32 (-4 pel), block 2 @ MI(8,0) NEWMV col 32 (+4 pel), and the INTERIOR block 3 @ MI(8,8) NEARMV RefMvIdx 1. DONE (the ordering is now oracle-PINNED): `splot decode syn-2frame-inter-mvorder-64x64.ivf --output-format raw` reproduces the whole-stream md5 284e1450b42180f02de7415ab0367bfe (12288 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf (recorded in `docs/LOCAL-REFERENCE-EVIDENCE.toml`, `lref-avm-dav2d-syn-2frame-inter-mvorder-64x64`). Block 3's §7.12.2 stack is [col 32 (slot 0 = the LEFT neighbour, block 2, from the step-7 scan_point(bh4-1,-1) probe), col -32 (slot 1 = the ABOVE neighbour, block 1, from the step-8 scan_point(-1,bw4-1) probe), col 64 (the (-1,-1) corner, block 0), col 0 (the zero global fallback)]: RefMvIdx 1 reconstructs col -32 (the ABOVE neighbour) DIRECTLY (NEARMV, no delta), so a reversed (above-before-left) scan order would reconstruct block 3 from col 32 and mis-decode it. NO decoder change was needed: find_mv_stack's §7.12.2.6 ordered spatial scan and the §7.12.2.12 search-stack dedupe were ALREADY CORRECT; this brick is a fixture + tests + honesty brick proving them. FALSIFIABILITY was verified locally: temporarily swapping scan steps 7 and 8 flips slots 0/1 (the find_mv_stack unit test `distinct_left_and_above_mvs_order_left_before_above` then fails: slot 0 becomes col -32) AND changes the inter-frame decode hash (the `mvorder_fixture_per_frame_hash_is_stable` test then fails: 889d89f6... instead of 3c2a8c85...), proving the fixture genuinely discriminates the ordering. SCOPE OF PROOF (honest): because every leaf is exactly 32x32 (Block_Width / Block_Height == 32, NOT > 32), the §7.12.2.20 large-block (> 32x32) extra MVP combinations are correctly INAPPLICABLE, so this brick pins the spatial ordering WITHOUT implementing §7.12.2.20; that step remains DEFERRED (TODO(spec: DECODE-INTER-MVSTACK-SPATIAL)) and guarded by the §5.20.7.8 inter_block_drl_idx_out_of_range reject for the > 32x32 leaves it does not yet model. All existing inter (zero-MV 4e1bd39f, sub-pel a0e82de3, residual ab2b067a, mvstack e5b581a5, SB-row 477a993d, grid 897bf67e) and general-intra fixtures stay byte-identical (no regression). The deferred §7.12.2 temporal/compound/warp/ref-MV-bank-derived-SMVP/DRL-reorder/scan-col candidates remain unmodelled; in-loop filters and live AVM/dav2d invocation in CI remain unimplemented."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::mvorder_fixture_decodes_two_frames",
    "crates/splot-decode/src/prediction/inter/tests.rs::mvorder_fixture_per_frame_hash_is_stable",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::distinct_left_and_above_mvs_order_left_before_above",
    "crates/splot-cli/tests/decode_cli.rs::decode_distinct_mv_inter_fixture_decodes_bit_exact",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-inter-mvorder-64x64.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-SINGLE-REF-SYMBOL"
name = "§5.20.7.12 single_ref entropy element (SymbolEncoder round-trip)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.12", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-12",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/single_ref.rs"
change_id = "decode-inter-single-ref-symbol"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds the AV2 §5.20.7.12 `read_single_ref` entropy element — the leaf prerequisite for multi-reference inter decode that two prior investigations found blocked on it (it is only read when `NumTotalRefs >= 2`, which needs >= 2 valid reference slots = a >= 3-frame runtime loop + reference retention + the §7.7 two-valid-slot feed, all separate larger bricks). `read_single_ref` (crates/splot-decode/src/prediction/inter/single_ref.rs) loops `ref` from 0 to `NumTotalRefs - 2`, reads one binary `single_ref` symbol per `ref` over `TileSingleRefCdf[ctx][ref]` (the §8.3.2 row, `ctx` computed as for `comp_ref` per docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2 line 1094), returns `ref` on the first `1` and `NumTotalRefs - 1` when all are `0` — matching the §5.20.7.12 mirror (docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-12) and AVM `read_single_ref` (av2/decoder/decodemv.c) + `av2_get_pred_cdf_single_ref` (av2/common/pred_common.h), which index `single_ref_cdf[ctx][ref]` identically. New `TileSingleRefCdf` rows from `DEFAULT_SINGLE_REF_CDF` ([REF_CONTEXTS=3][REFS_PER_FRAME - 1=6][3]) thread through the Block/Tile CDF subsets (selector, row/row_mut, §8.2.4 averaging, frame-end count scaling), mirroring `TileDrlModeCdf`. PROVEN bit-exact by a `SymbolEncoder` <-> `read_single_ref` round-trip over every selectable `RefFrame[0]` (0..=NumTotalRefs - 1) across NumTotalRefs 2..=7 with DISTINCT per-decision contexts (asymmetric, so a transposed tree decision or wrong CDF-row index changes the decoded value); `exit_symbol()` bit-count consistency is asserted. Falsifiability VERIFIED: transposing the CDF-row index (`ctx` <-> `ref`) breaks the round-trip tests. The §8.3.2 context derivation is caller-supplied here; the contexts are driven directly by the round-trip. The reader uses typed errors only and is panic-free (NumTotalRefs == 1 returns 0 via the empty §5.20.7.12 loop with no symbol read [the legal one-reference case, §6.19.7.11 requires NumTotalRefs > 0], NumTotalRefs == 0 typed-error [it would underflow NumTotalRefs - 1], missing-context, out-of-range-context, and short-buffer cases proven). This feature is now WIRED by DECODE-INTER-MULTIREF-RUNTIME: `read_single_ref` IS called from the runtime block decode (between read_skip and single_mode) when §7.7 yields `NumTotalRefs == 2`, the `NumTotalRefs == 1` gate is relaxed to admit NumTotalRefs ∈ {1, 2}, and the §8.3.2 neighbour-derived `single_ref` context is supplied by `BlockNeighbourContext::single_ref_ctx` (the comp_ref `count_refs` process, cross-checked vs AVM `av2_get_ref_pred_context`). The committed `syn-3frame-multiref-64x64.ivf` proves the runtime read bit-exact (frame 2 selects RefFrame[0] == 1 -> the retained inter frame). The SymbolEncoder round-trip still pins the entropy element. STILL out of scope (DECODE-INTER-MULTIREF-RUNTIME follow-on): NumTotalRefs > 2 (multi-decision single_ref), a neighbour-having single_ref context, and `read_compound_ref` (§5.20.7.11, the compound sibling)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_selection_roundtrips_through_symbol_encoder_for_every_value",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_roundtrips_across_num_total_refs_and_distinct_contexts",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_context_indexing_is_load_bearing",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_missing_context_is_a_typed_error_not_a_panic",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_out_of_range_context_is_a_typed_error_not_a_panic",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs::single_ref_short_buffer_does_not_panic",
]
commands = [
    "cargo test -p splot-decode --locked single_ref",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/prediction/inter/single_ref.rs",
    "crates/splot-decode/src/prediction/inter/single_ref/tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-MULTIREF-RUNTIME"
name = "multi-reference inter runtime (§7.7 two-valid-slot map + §7.20/§7.23 retention + §5.20.7.12 single_ref wiring)"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.6", "5.20.7.10", "5.20.7.12", "7.7", "7.20", "7.23", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-10",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/reference/buffer.rs"
change_id = "decode-inter-multiref-runtime"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "THE fundamental capability for real sequences: decode a frame that selects among TWO valid decoded references, retaining a prior decoded INTER frame as a reference. The committed `syn-3frame-multiref-64x64.ivf` (project-owned flat synthetic input: frame 0 luma 100 / U 120 / V 130; frames 1 AND 2 luma 160 / U 90 / V 70 — frames 1 and 2 identical to each other but DISTINCT from the key; `--qp 80 --sb-size 64 --min/max-partition-size 64 --enable-global-motion=1 --cdf-update-mode=0`, broad decode tools disabled) is a three-frame 64x64 stream: frame 0 = OBU_CLOSED_LOOP_KEY (flat DC_PRED intra; §7.20 refresh_frame_flags allFrames -> §7.23 :14100 marks ONLY slot 0 RefValid), frame 1 = OBU_REGULAR_TILE_GROUP (single-reference inter, §7.7 NumTotalRefs == 1 -> ref_frame_idx [0] == the key; refresh_frame_flags slot 1, so §7.23 retains frame 1 as a SECOND valid reference), frame 2 = OBU_REGULAR_TILE_GROUP over TWO valid slots (§7.7 ranks ref_frame_idx [0, 1], NumTotalRefs == 2) whose §5.20.7.6 block reads §5.20.7.12 single_ref (over the §8.3.2 no-neighbour comp_ref/count_refs context 1) -> RefFrame[0] == 1 -> ref_frame_idx[1] == slot 1, motion-compensating the RETAINED frame 1 (luma 160), NOT the key (luma 100). DONE: `splot decode syn-3frame-multiref-64x64.ivf --output-format raw` reproduces the whole-stream md5 861078138ab514bd847ccfe22ac44fa1 (18432 bytes) byte-for-byte vs avmdec --rawvideo --i420 and dav2d --demuxer ivf --muxer yuv (recorded in docs/LOCAL-REFERENCE-EVIDENCE.toml `lref-avm-dav2d-syn-3frame-multiref-64x64`). ASYMMETRIC PROOF: frame 2 reconstructs to 160 (frame 1), distinct from the key's 100, so a wrong slot-0 read is falsifiable; a reference-retention test asserts frame 2's luma equals frame 1's and differs from the key's. The runtime resolves following inter candidates by planned OBU offset, not by IVF record index; `multiref_fixture_decodes_when_two_frame_units_share_one_ivf_record` repacks the already oracle-proven frame-1 and frame-2 Annex B payloads into one IVF record and verifies the same three decoded frames. THREE pieces wired: (1) splot-core `FrameReferenceStateView` now models `RefBaseQIdx` (the new `from_slots_with_base_q_idx` constructor) — the ONLY §7.7 scoring input the single-spatial-layer minimal frame leaves non-deterministic (AllowedFrames == -1, all layers depend, layer ids 0; a shared RefCounter from a multi-bit refresh is harmless because §7.7 new_score_or_dist drops the duplicate by identical orderHint/score/mLayer), so `derive_implicit_ref_map` lifts its `valid_count > 1 -> UnmodeledDerivation` gate when RefBaseQIdx is modeled and runs the real §7.7 ranking over two valid slots (proven by `inter_implicit_map_two_valid_slots_with_base_q_idx_ranks_exact`); the historical `from_slots` view STAYS an UnmodeledDerivation stop. (2) splot-decode `RuntimeReferenceBuffer` (reference_buffer.rs) applies the §7.23 reference frame update per frame's refresh_frame_flags (KEY/SWITCH: RefValid[i] = first, mirror :14100; inter: RefValid[i] = 1), storing RefValid/RefOrderHint/RefFrameWidth/Height/RefBaseQIdx + the decoded frame index, and builds the borrowed ReferenceFrameStore for later frames. The multi-frame driver no longer rejects solely because more than three frame candidates were planned; `four_frame_multiref_reaches_too_many_valid_references_gate` repeats the already oracle-proven third-frame payload as a fourth candidate and proves the stream reaches the precise `inter_too_many_valid_references` gate before output once a third valid reference slot would be needed. (3) the block decode wires §5.20.7.12 read_single_ref between read_skip and single_mode when NumTotalRefs == 2 (DECODE-INTER-SINGLE-REF-SYMBOL is now WIRED), deriving the §8.3.2 single_ref context from the neighbour count_refs (`BlockNeighbourContext::single_ref_ctx`, cross-checked vs AVM `av2_get_ref_pred_context`); RefFrame[0] resolves the per-block reference slot via ref_frame_idx. VERIFIED-SUBSET DISCIPLINE (rejected before output): NumTotalRefs > 2 (more single_ref decisions), compound / reference_select, a fourth frame once it needs >2 valid references, a neighbour-having single_ref block (the §8.3.2 ctx is gated to the no-neighbour ctx 1 the fixture proves), more than two valid reference slots, and the cross-frame state hardening cases tracked under DECODE-INTER-MULTIREF-RUNTIME: (a) §7.23 cross-frame CDF save/load — the decoder decodes every frame from default init_*_cdfs and does not model load_cdfs. set_primary_ref_frame_and_ctx (§5 :5411-5430) is modelled PRECISELY, including the PRIMARY_REF_CHOOSE resolution (mirror :5414-5415) via choose_primary_secondary_ref_frame (mirror :5451-5510 — the inter-only candidate filter `RefFrameType == INTER_FRAME`, qpDiff scoring, is_ref_better tie-break; cross-checked vs AVM av2/common/pred_common.c). inter_cdf_inheritance_unmodeled rejects iff the §5-RESOLVED primary_ref_frame loads a PER-SLOT-ADAPTED reference slot (each §7.23 slot now records disable_cdf_update == 0 and RefFrameType, replacing the old coarse 'any prior frame adapted' + 'CHOOSE = no-load' shortcut, which let a CHOOSE frame resolving to a real adapted inter reference bypass the guard). The committed fixtures carry primary_ref == PRIMARY_REF_CHOOSE which resolves to PRIMARY_REF_NONE (their valid slots hold the CLK KEY frame, which the inter-only resolution skips) or to a NON-adapted inter slot, so all stay byte-identical. (b) temporal MV — inter_temporal_mvs_unmodeled rejects a frame using temporal MVs (enable_ref_frame_mvs / use_ref_frame_mvs) once an INTER reference has been retained (the buffer stores no §7.23 SavedMvs, so a §7.12 temporal candidate would predict from an empty stack). (c) order-hint wrap — DONE: §5.18.2 derives full OrderHint separately from coded OrderHintLsbs, §7.23 persists both values plus single-layer showability state, and all semantic decode consumers use the full hint; syn-orderhint-wrap-64x64.ivf decodes all 121 frames through the first wrapped frame byte-exact to AVM raw SHA-256 0d032cacd2c307f0d2b2c32fb3281742edcb03781fb81c4d3ae476c09ce8648e. (d) incomplete reference state — derive_implicit_ref_map keeps the valid_count > 1 UnmodeledDerivation stop unless ALL §7.7 ranking inputs (RefBaseQIdx / RefOrderHint / dims as COMPLETE parallel slices covering every active slot, + the current frame size when check_res) are actually supplied, so a caller with short/None slices can never derive ref_frame_idx from fabricated zeros. All existing inter (zero-MV 4e1bd39f, sub-pel a0e82de3, residual ab2b067a, mvstack e5b581a5, mvorder 284e1450, SB-row 477a993d, grid 897bf67e, 3-frame multiref 861078138) and general-intra (grid dd2fa84f) fixtures stay byte-identical (no regression). HASH RETENTION: the hash adapter emits in display order through a private callback and sparse frame slots are reclaimed once neither §7.23 references nor pending output own them; syn-orderhint-wrap-64x64 decodes 121 frames under a 110,592-byte reference-store cap while preserving the pinned wrapped-frame digest. Raw, Y4M, and hash adapters all release output-only frames after emission; only decoded frames still owned by references, in-flight work, or pending display order count against the live reference-store budget. NAMED FOLLOW-ON: §7.23 cross-frame CDF save/load (so an adapted reference can actually be loaded instead of rejected, and cdf-update-mode != 0 multiref streams decode), §7.23 SavedMvs (temporal MV prediction), NumTotalRefs > 2 (multi-decision single_ref), neighbour-having single_ref context, compound references (read_compound_ref §5.20.7.11), and the deferred §7.12.2 ref-MV-bank/DRL-reorder/warp candidates."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_fixture_decodes_three_frames_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_fixture_decodes_when_two_frame_units_share_one_ivf_record",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_fixture_rejects_when_inter_tile_group_starts_ivf_record",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_runtime_rejects_state_obu_before_following_inter_candidate",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_runtime_rejects_state_obu_after_inter_candidate_before_next_frame",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_runtime_enforces_cumulative_output_frame_limit",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_runtime_enforces_cumulative_reference_store_byte_limit",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_frame2_reads_retained_inter_frame_not_key",
    "crates/splot-decode/src/prediction/inter/tests.rs::multiref_fixture_per_frame_hash_is_stable",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::single_ref_ctx_no_neighbour_is_one",
    "crates/splot-decode/src/reference/buffer.rs::tests::key_refresh_marks_only_first_slot_valid",
    "crates/splot-decode/src/reference/buffer.rs::tests::inter_refresh_adds_a_second_valid_slot",
    "crates/splot-decode/src/reference/buffer.rs::tests::frame_index_is_retained_until_last_slot_is_overwritten",
    "crates/splot-decode/src/output/hash.rs::tests::long_hash_decode_keeps_reference_storage_bounded",
    "crates/splot-decode/src/output/hash.rs::tests::long_hash_decode_rejects_live_store_cap_below_peak",
    "crates/splot-decode/src/output/hash.rs::tests::requested_hash_output_limit_emits_one_frame",
    "crates/splot-decode/src/prediction/inter/tests.rs::resolve_cdf_load_models_choose_resolution_and_load_decision",
    "crates/splot-decode/src/prediction/inter/tests.rs::choose_primary_ref_frame_skips_non_inter_slots",
    "crates/splot-decode/src/prediction/inter/tests.rs::choose_primary_ref_frame_ranks_two_inter_slots_by_qp_diff",
    "crates/splot-core/src/headers/frame/info/tests.rs::wrapped_display_order_hint_drives_implicit_reference_ranking",
    "crates/splot-core/src/headers/frame/info/tests.rs::display_order_hint_extends_past_the_coded_lsb_window",
    "crates/splot-decode/src/reference/buffer.rs::tests::reference_refresh_preserves_full_and_lsb_order_hints",
    "crates/splot-decode/src/prediction/inter/tests.rs::floor_log2_matches_msb_index",
    "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_two_valid_slots_with_base_q_idx_ranks_exact",
    "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_two_valid_slots_without_base_q_idx_stops_unmodeled",
    "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_two_valid_slots_incomplete_ranking_inputs_stop_unmodeled",
    "crates/splot-cli/tests/decode_cli.rs::decode_multiref_three_frame_fixture_is_bit_exact",
    "crates/splot-cli/tests/conformance.rs::conformance_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-core --locked",
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo test -p splot-cli --test conformance --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-3frame-multiref-64x64.ivf",
    "tests/conformance/vectors/valid/syn-orderhint-wrap-64x64.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-ZERO-REFERENCE-INTRA-BLOCKS"
name = "zero-reference inter frames with intra-coded blocks"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.2", "5.18.4.3", "5.20.7.6", "5.20.7.10", "5.20.7.11", "5.20.7.12", "6.17.2", "6.19.7.10", "6.19.7.11"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-4-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-10",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-11",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-12",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-7-10",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-7-11",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/frame_walk.rs"
change_id = "decode-inter-zero-reference-intra-blocks"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Admits a complete regular-inter header with NumTotalRefs == 0 and an empty ref_frame_idx, as permitted by §§5.18.2 and 6.17.2. Intra-coded blocks continue through the existing general-intra path. If tile syntax selects an inter-coded block, the runtime reports DecodeError::MalformedSource with TilePayloadParseError before skip, single-reference, or compound-reference parsing can fabricate reference slot zero; §§6.19.7.10 and 6.19.7.11 require a nonzero reference count for those readers. The positive unit seed is a 115-byte AVMenc-generated two-frame IVF (SHA-256 133166055845472c2362f53dde58328912a1fdf4a376148a4c2227c04dfbe32e) whose second frame is one intra-coded block; mutating its parsed reference count/map to zero still reconstructs successfully. Stock AVMenc cannot encode the exact header because --max-reference-frames is restricted to 1..=7, so the exact zero-reference case uses the requested unit-test fallback."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/inter.rs::tests::inter_implicit_map_no_valid_slot_reaches_shared_tail_with_zero_refs",
    "crates/splot-decode/src/prediction/inter/tests/zero_reference.rs::zero_reference_inter_frame_decodes_when_every_block_is_intra",
    "crates/splot-decode/src/prediction/inter/tests/zero_reference.rs::zero_reference_inter_frame_reaches_the_inter_block_boundary",
    "crates/splot-decode/src/prediction/inter/tests/zero_reference.rs::zero_reference_inter_frame_requires_an_empty_reference_map",
]
commands = [
    "cargo test -p splot-core --locked inter_implicit_map_no_valid_slot_reaches_shared_tail_with_zero_refs",
    "cargo test -p splot-decode --locked zero_reference_inter_frame",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = ["crates/splot-decode/src/prediction/inter/tests/zero_reference.rs"]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-INTER-COMPOUND-AVERAGE"
name = "two-reference compound-average inter runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.8.2", "5.20.7.2", "5.20.7.3", "5.20.7.6", "5.20.7.8", "5.20.7.10", "5.20.7.11", "5.20.7.13", "5.20.7.16", "5.20.7.17", "6.4.6", "6.17.3.1", "7.12.2.10", "7.12.2.14", "7.12.2.15", "7.13.3.1", "7.13.3.7", "7.13.3.8", "7.13.3.9", "7.13.3.10", "7.13.3.11", "7.13.3.12", "7.13.3.16", "7.13.3.18", "7.22", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-8-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-8",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-10",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-11",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-13",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-16",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-17",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-6",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-3-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-15",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-9",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-12",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-16",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/compound.rs"
change_id = "decode-inter-compound-average"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements the smallest oracle-proven compound-reference decode subset: NumTotalRefs == 2, reference_select chooses compound, read_compound_ref yields RefFrame[0..1] == [0, 1] for the admitted fixture, non-joint NEAR_NEARMV, zero MVs, skipped residual, identity-scaled references, and equal-weight COMPOUND_AVERAGE with CWP/masks/implicit masked blend/refinemv/TIP/warp/temporal-MV disabled. The runtime adds private minimal-inter compound syntax helpers, the needed §8.3.2 CDF rows for comp_mode/is_joint/compound_mode_non_joint plus the fixture-required comp_ref selectors, a `splot-recon` compound intermediate helper that preserves §7.13.3.18 signed intermediates, and a CWP_EQUAL blend using the §7.13.3.16 equal-weight formula. The committed syn-3frame-compound-average-64x64.ivf fixture now decodes with `splot decode --output-format raw` to the recorded avmdec/dav2d raw SHA-256 2b4f716243d9f5c30a244ecc6f7fdcb5bef804d2ba353a21d670d686cfe63ff4 (MD5 34074c6945348b146f84551a20d9affd). The compound path also implements §7.13.3.8-§7.13.3.12 explicit optical-flow prediction: ordinary clipped luma predictors feed distance reduction, 16-sample gradient units, regularized least-squares deltas, and 4x4/8x8 refined 1/16-pel motion compensation reused by chroma. The rebuilt predictors apply the §7.13.3.8 per-subblock §7.13.3.4 reference area for every 8x8 luma unit and every chroma unit, so refined MVs cannot sample beyond the area derived from the unrefined candidate. Its §7.22 temporal storage now records the reconstructed optical-flow MV for each 8x8 motion-field cell instead of splatting the unrefined block MV; 4x4 optical-flow blocks are reduced with the specified four-delta signed average. Wedge compound storage evaluates the shared luma mask per 8x8 motion-field unit and suppresses a reference when at least 60 of 64 samples are dominated before the surviving slot is normalized. Skip mode now uses the upper skip_txfm CDF contexts, derives or inherits the §5.20.7.10 reference pair, reads one shared DRL index, resolves both NEAR_NEARMV vectors from one compound candidate, preserves that candidate's CWP weight, records the inferred most-probable precision state, forces EIGHTTAP_SHARP, and records skip-mode neighbour state through the shared compound reconstruction sink. Non-skip optical-flow NEAR_NEARMV also uses its one signalled §5.20.7.8 DRL index to select one paired compound-stack entry; the deltaq1 oracle block at MI(28,64) now selects (-4,-149) and (4,187), matching AVM, instead of incorrectly taking index 0 from two independent stacks. The compound DRL implements §7.12.2.14 with bounded single and derived stacks, target-reference precedence, first-opposite-reference pairing, trajectory-adjusted candidates from the current block's two target-reference fields, and final insertion before the ref-MV bank. On deltaq1 the MI(48,8) ordinary spatial stack matches AVM exactly through DRL slot 3: [(19,-243)/(-24,286), (28,-242)/(-35,302), (16,-263)/(-24,316), (16,-263)/(-24,300)]. The first trajectory-applicable frame executes 11 candidates across MI(16,80), MI(24,80), and MI(36,24); every candidate MV and both derived MVs match AVM. The trajectory path advances the decoded raw MD5 from f10ae814b6fda4a10eb150853bd85354 to c12e7fc9eac3c065c4fde249d9af8c59 versus AVM a51faffac365722f94697051af0d9e00. Identity GLOBAL_GLOBALMV blocks are admitted when frame global motion is disabled: both MVs remain zero and the path reads no DRL, AMVD, optical-flow, refine-MV, local-warp, or interpolation-filter symbol. This closes decode admission for the 64x64 l5 and opfl0-refinemv0 corpus streams, producing raw MD5 c98f2490fd026bf51255eb06a7865e6a versus AVM c7e65cb31f675f460daf745a42bf9d0e and 9b09f32efd82a4ed45a1349a469522f0 versus AVM ae41842a250d29146e5316a4f77271ed. Non-identity global motion and global-mode replacement in the broader derived process remain partial, as do switchable refine-MV signalling, residual compound, and full AV2 decoder conformance."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/compound.rs::tests",
    "crates/splot-decode/src/prediction/inter/tests.rs::compound_average_fixture_decodes_three_frames_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::compound_average_fixture_per_frame_hash_is_stable",
    "crates/splot-recon/src/subpel_mc/tests.rs::compound_intermediate_keeps_unclipped_prescaled_predictor",
    "crates/splot-recon/src/subpel_mc/tests.rs::compound_equal_average_blend_rounds_and_clips",
    "crates/splot-recon/src/subpel_mc/tests.rs::compound_blend_rejects_length_mismatch",
    "crates/splot-recon/src/optflow.rs::tests",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::dispatcher_rebuilds_optflow_compound_planes_from_refined_grid",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::optflow_reuses_the_luma_motion_grid_for_every_chroma_format",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::refine_all_requires_equal_weight_non_global_average",
    "crates/splot-decode/src/prediction/inter/tests.rs::opfl_refine_all_fixture_decodes_eight_frames_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::opfl_refine_all_fixture_rejects_truncated_payload",
    "crates/splot-recon/src/optflow.rs::tests::ten_bit_normalization_matches_eight_bit_deltas",
    "crates/splot-decode/src/prediction/inter/mc/optflow.rs::tests::temporal_mvs_average_four_by_four_optflow_deltas_over_eight_by_eight",
    "crates/splot-decode/src/prediction/inter/mc/optflow.rs::tests::temporal_mvs_treat_cropped_four_by_four_units_as_zero_delta",
    "crates/splot-decode/src/prediction/inter/mc/optflow.rs::tests::reference_areas_keep_nominal_luma_and_chroma_subblock_sizes",
    "crates/splot-decode/src/prediction/inter/block_tests.rs::skip_mode_selects_the_upper_skip_txfm_context_bank",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::skip_mode_default_pair_treats_restricted_order_hint_as_zero_distance",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::wedge_temporal_storage_keeps_only_a_dominant_reference",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_opfl_near_near_uses_one_paired_drl_idx",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_opfl_near_near_selects_the_indexed_paired_candidate",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::skip_mode_reference_pair_accepts_the_active_reference_range",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::skip_mode_reference_pair_rejects_out_of_range_state_as_malformed_syntax",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::skip_mode_reference_pair_inherits_compound_neighbour_or_keeps_default",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::skip_mode_reference_pair_skips_intrabc_neighbour_before_compound",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::skip_mode_reference_pair_projects_tip_neighbour_to_sorted_tip_pair",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::recorded_skip_mode_contributes_to_the_next_block_context",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::compound_mv_stack_keeps_paired_vectors_cwp_and_precision_state",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::compound_mv_stack_pairs_single_ref_spatial_neighbours_in_probe_order",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::compound_mv_stack_projects_tip_neighbour_to_its_reference_pair",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::compound_mv_storage_keeps_the_first_six_candidates",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::compound_spatial_derivation_uses_both_reference_trajectories",
    "crates/splot-decode/src/prediction/inter/compound.rs::tests::compound_average_reads_global_global_mode",
    "crates/splot-decode/src/prediction/inter/compound.rs::tests::same_reference_compound_mode_mapping_is_complete",
    "crates/splot-decode/src/prediction/inter/compound.rs::tests::same_reference_compound_mode_rejects_out_of_range_context",
    "crates/splot-decode/src/prediction/inter/tests.rs::same_ref_global_global_fixture_decodes_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::same_ref_global_global_fixture_rejects_truncated_payload",
    "crates/splot-cli/tests/decode_cli.rs::decode_compound_average_three_frame_fixture_is_bit_exact",
]
commands = [
    "cargo test -p splot-decode prediction::inter --locked",
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-recon subpel_mc --locked",
    "cargo test -p splot-recon optflow --locked",
    "cargo test -p splot-cli decode_compound_average_three_frame_fixture_is_bit_exact --locked",
    "cargo test -p splot-decode same_ref --locked",
    "target/debug/splot decode --output-format raw tests/conformance/vectors/valid/syn-2frame-same-ref-global-global-64x64-q80.ivf -o /tmp/splot-same-ref-global-global.raw && shasum -a 256 /tmp/splot-same-ref-global-global.raw",
    "target/debug/splot decode --output-format raw tests/conformance/vectors/valid/syn-3frame-compound-average-64x64.ivf -o /tmp/splot-compound.raw && shasum -a 256 /tmp/splot-compound.raw",
    "cargo xtask conformance",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-3frame-compound-average-64x64.ivf",
    "tests/conformance/vectors/valid/syn-2frame-same-ref-global-global-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-8frame-opfl-refine-all-64x64-q120.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-COMPOUND-REFINEMV"
name = "compound refine-MV runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.17", "7.13.3.3", "7.13.3.4", "7.13.3.5", "7.13.3.6", "7.13.3.7", "7.22"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-17",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/mc/refinemv.rs"
change_id = "decode-inter-compound-refinemv"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements compound refine-MV after §5.20.7.17 enables or signals use_refinemv. Each luma block is divided into at-most-16x16 units; a bilinear reference-area prediction searches the 24 ordered opposing full-pixel offsets with the §7.13.3.6 downsampled SAD and MV range guard. Non-switchable refine-MV permits the centre candidate, applies its 7/8 SAD bias, and may take the low-SAD threshold exit. Explicitly signalled switchable refine-MV now follows allowCentre = 0: it skips the centre and threshold and initializes the ordered search from neighbor 0. This makes the target stream's hidden order-hint-4 prefilter SHA-256 byte-exact to pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774 at eddb393504057be0e39f6aa5c01f74dc9188f402fb8f298b83a3f4266b124dbe; the next mismatch is independently in chroma deblocking. One shared compound motion grid reuses the refined luma MVs for chroma, composes optical-flow deltas over each refined unit when use_optflow is active, applies the original candidate's §7.13.3.4 clipping area, and stores the final per-8x8 motion through §7.22. The deltaq1 AVM corpus vector advances from compound_refinemv_enabled to an independent malformed §8.2.4 exit-symbol diagnostic at byte 692. Switchable use_refinemv is read from TileUseRefinemvCdf with the §8.3.2 context 1 + (YMode - NEAR_NEARMV) + 6*use_optflow (minus one for optical-flow modes past GLOBAL_GLOBALMV), and DecidedAgainstRefinemv folds through the existing post-blend deactivation and pre-blend sharp-filter forcing. Full-stream AVM agreement remains partial because later decode and filter gaps remain fail-closed or non-exact."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/mc/refinemv.rs::tests::default_search_keeps_a_low_sad_centre",
    "crates/splot-decode/src/prediction/inter/mc/refinemv.rs::tests::default_search_selects_opposing_full_pixel_offsets",
    "crates/splot-decode/src/prediction/inter/mc/refinemv.rs::tests::switchable_search_rejects_low_sad_center_and_starts_with_first_neighbor",
    "crates/splot-decode/src/prediction/inter/mc/refinemv.rs::tests::reference_area_uses_the_refinemv_extension",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::dispatcher_returns_default_refinemv_motion_grid",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::dispatcher_switchable_refinemv_excludes_low_sad_center",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_refinemv_suppresses_cwp_symbols",
]
commands = [
    "cargo test -p splot-decode prediction::inter::mc --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = []
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-COMPOUND-LOCALWARP"
name = "compound LOCALWARP runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.14", "7.12.3", "7.12.3.2", "7.13.3.14", "7.13.3.19", "7.13.3.20", "7.13.3.23", "7.22"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-3-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-19",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-20",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-23",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block/compound_path.rs"
change_id = "decode-compound-local-warp"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Retires the inter.compound.local_warp fail-closed gate: when §5.20.7.14 read_motion_mode returns LOCALWARP for a NEW_NEWMV compound block (AVM WARP_CAUSAL), the §7.12.3 warp-sample search and §7.13.3.23 least-squares estimation run once per reference list (a signalled list that gathers no samples fails closed as inter.compound.local_warp.empty_sample_list: the \u00a75.20.7.14 WarpSampleFound scan is wider than the \u00a77.12.3.1 gathering, and the spec det==0 identity model diverges from AVM wm_params[ref].invalid in that corner). Prediction warps each list with its own model through §7.13.3.19 block warp / §7.13.3.20 extended block warp emitting the §7.13.3.16 compound InterRound1=7 intermediate, then reuses the ordinary compound average / wedge / diff-weighted blend; compoundWarp=1 forces the plain average (the implicit-mask branch is disabled). Downstream syntax follows §5.20.7.14/16/17: refine-MV and CWP are suppressed (motion_mode != SIMPLE) and the interpolation filter is not signalled (needs_interp_filter=0 -> EIGHTTAP), while masked wedge/diff-weighted stay reachable. Per-8x8 SubMvs are stored for both lists into the neighbour grid (mv-stack) and the §7.22 motion field. Frontier: on the pre-#774 base, ac0ej3 --limit=2 tripped this gate at byte 12463 via a use_local_warp read produced by the since-fixed frame-2 entropy divergence; on the current base (#774-#778) no compound LOCALWARP block is signalled in the reachable prefix, so the stream fails with decode/malformed-source (source_issue_kind tile_payload_parse_error) for an undefined UV mode at byte 12463 identically with and without this feature. The committed 550-byte syn-2frame-warp-sample-list1-48x48-q90.ivf reaches §7.12.3.2 with the matching neighbouring reference and MV in list 1 of a compound block; the neighbour grid now stores every compound mv1 concretely, so the former List1MvUnretained defensive state is impossible by construction. Splot and current plus pinned AVM decoders produce identical complete 13,824-byte raw I420 SHA-256 d1c84d805c7e01737a88779051a6e9363fac552c5c543d21b8bb99fd3388a787. The bit-exact §7.13.3.19 round-7 kernel, per-list model derivation, parse flow, and list-1 sample routing are all covered; broader compound LOCALWARP full-stream agreement remains partial."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-recon/src/warp_prediction.rs::tests::compound_intermediate_flat_reference_is_scaled_by_filter_gain",
    "crates/splot-recon/src/warp_prediction.rs::tests::compound_intermediate_affine_matches_round7_spec_trace",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_local_warp_symbol_reports_localwarp_when_set",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_local_warp_derives_a_model_per_reference_list",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_warp_blend_forces_plain_average_but_keeps_masks",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::compound_local_warp_motion_suppresses_cwp",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::warp_sample_storage_keeps_the_first_eight_samples",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::local_warp_samples_include_retained_compound_list1_mv",
]
commands = [
    "cargo test -p splot-recon warp_prediction --locked",
    "cargo test -p splot-decode prediction::inter --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-warp-sample-list1-48x48-q90.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-TIP-REFERENCE"
name = "TIP reference block runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.6", "5.20.7.10", "5.20.7.13", "7.9.1", "7.9.3", "7.9.5", "7.9.8", "7.10.2", "7.12.2.10", "7.12.2.15", "7.12.2.18", "7.13.3", "7.22", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-6",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-10",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-13",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-8",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-10-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-15",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block.rs"
change_id = "decode-inter-tip-reference"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Implements TIP-as-reference block selection with the two-neighbour tip context, TIP prediction-mode and dedicated DRL syntax, the TIP reference sentinel in spatial state, sharp interpolation inference, and projected temporal motion over 8x8 prediction units. The §7.9.1 selector persists each source frame's reference hints, type, and exact size, then prioritizes eligible side projections by the TIP pair, interpolation distance, and reference topology; target-specific replacement and the reduced-reference cap follow the same shared queue. On the external deltaq1 vector all 30 ordered projections across eight motion-field frames match AVM, improving frame-two luma MAE from 5.7045 to 5.3567 and exact luma samples from 46419 to 50729. The §7.9.3 reference projection samples source cells at the signaled ProjStep and aligns projected destinations to the same grid. The §7.9.8 position gate rejects source cells outside the projected destination's TMVP unit bounds. TIP-as-reference reconstruction passes the current frame's §7.22 motion-field sink through the existing TIP prediction units instead of discarding those stored MVs; on deltaq1 the saved order-hint-2 field grows from 264 valid cells per side to 1584 on list zero and 1352 on list one, and §7.22 single-reference normalization then fills list one to the same 1584 cells. Two-reference storage orders both motion-vector slots around the current order hint before they are projected. TIP-coded neighbours also match their underlying two-reference pair when deriving a following compound block's mode context; this keeps the shared DRL CDF context aligned instead of adding a target-specific exception. Compound DRL construction projects TIP-coded spatial neighbours through that pair after snapping the probe to its recorded 8x8 or 16x16 prediction-unit base and clamping the temporal-field coordinates per §7.12.2. The §7.12.2.10 candidate loop invokes the §7.12.2.18 TIP add-derived process only for candList 0, so each qualifying compound neighbour contributes once and cannot exhaust the bounded derived stack with duplicate candidates before a later distinct neighbour is scanned; the A/A/A/B regression pins this ordering and capacity behavior. Reconstruction reuses the existing compound motion-compensation and residual sinks with the frame TIP weight and optional projected-motion hole filling. Full-stream conformance remains partial: deltaq1 still differs from AVM (`c12e7fc9eac3c065c4fde249d9af8c59` versus `a51faffac365722f94697051af0d9e00`), later frame kinds still reject through decode/unsupported-feature, and no committed TIP vector yet proves an end-to-end AVM checksum."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/prediction/inter/block/syntax.rs::tests::skip_and_tip_drl_idx_use_dedicated_cdf_banks",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::tip_neighbour_matches_its_underlying_compound_reference_pair",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::compound_mv_stack_projects_tip_neighbour_to_its_reference_pair",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/tests.rs::tip_mv_stack_keeps_a_later_distinct_compound_candidate",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::tip_reference_pair_uses_the_nearest_past_and_future_references",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::tip_projection_fills_unsampled_units_and_adds_the_block_mv",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::tip_temporal_scaling_clamps_to_the_reference_mv_domain",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::step_two_projection_samples_and_stores_on_the_even_grid",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::step_one_projection_uses_64_pixel_tmvp_unit_bounds",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::temporal_projection_stays_within_the_vertical_tmvp_unit",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::compound_references_are_stored_in_temporal_slot_order",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::side_rejected_projection_still_extends_existing_trajectory",
    "crates/splot-decode/src/prediction/inter/block/tip.rs::tests::tip_reference_unit_size_follows_refinement_and_large_block_gates",
    "crates/splot-decode/src/prediction/inter/block/tip.rs::tests::tmvp_unit_size_uses_64_pixels_for_step_one_or_64_pixel_superblocks",
]
commands = [
    "cargo test -p splot-decode tip_ --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo xtask check-duplication",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/prediction/inter/block.rs",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs",
    "crates/splot-decode/src/prediction/inter/block/syntax.rs",
    "crates/splot-decode/src/prediction/inter/block/tip.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-TIP-OUTPUT"
name = "TIP output frame runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.2", "7.9.1", "7.9.3", "7.10.6", "7.16", "7.22", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-9-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-10-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-16",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block/tip.rs"
change_id = "decode-inter-tip-output"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Admits regular and leading TIP OBUs in the multi-frame runtime and reconstructs TIP_FRAME_AS_OUTPUT from the normative reference pair through shared temporal projection and compound prediction. §7.9.1 reference sorting now applies the spec's in-place equal-hint ordering, and §7.9.3 projection accepts the zero-offset match required to retain the scaled trajectory. The runtime preserves per-slot U/V AC deltas for implicit TIP QP, stores optical-flow-refined output MVs with the §7.22 single-reference weighting rule, forwards parsed TIP film-grain selection, and applies §7.16 deblocking per tile and per native plane geometry, including monochrome, 4:2:0, 4:2:2, and 4:4:4. Four committed AVMenc fixtures cover a natural two-tile monochrome open GOP with leading and regular TIP, regular TIP in 4:2:2 and 4:4:4, and active sample-changing two-tile leading-TIP deblocking. Fixed splot, pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774, and current AVM revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce byte-identical complete output for every fixture; their lexical concatenation is 647,168 bytes with SHA-256 c7f5008940bc34e84b47588102732a13eeef45c6a2b5d22f3c7a7316b8ac5feb."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/quant.rs::tests::inferred_tip_quantization_retains_independent_ac_deltas",
    "crates/splot-decode/src/filters/deblock_tests.rs::tip_deblocking_smooths_prediction_unit_boundaries",
    "crates/splot-decode/src/filters/deblock_tests.rs::tip_filter_widths_follow_unit_and_chroma_superblock_edges",
    "crates/splot-decode/src/filters/deblock_tests.rs::tip_tile_edges_map_subsampled_plane_coordinates_to_luma_mi",
    "crates/splot-decode/src/filters/deblock_tests.rs::tip_deblocking_handles_yuv422_and_yuv444_chroma_geometry",
    "crates/splot-decode/src/filters/deblock_tests.rs::tip_deblocking_obeys_cross_tile_filtering_flag",
    "crates/splot-decode/src/prediction/inter/block/tip.rs::tests::tip_output_edge_units_keep_nominal_prediction_size",
    "crates/splot-decode/src/prediction/inter/block/tip.rs::tests::tip_output_unit_size_requires_sharp_refinement",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs::tests::tip_reference_pair_matches_sort_ref_order_for_equal_future_hints",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal/selection.rs::tests::tip_projection_pair_matches_sort_ref_order_for_equal_future_hints",
    "crates/splot-decode/src/reference/buffer_tests.rs::inter_refresh_adds_a_second_valid_slot",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode tip_ --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask conformance",
    "cargo xtask check-duplication",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/filters/deblock.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
    "crates/splot-decode/src/prediction/inter.rs",
    "crates/splot-decode/src/prediction/inter/block/tip.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal/selection.rs",
    "crates/splot-decode/src/prediction/inter/find_mv_stack/temporal/trajectory.rs",
    "crates/splot-decode/src/reference/buffer.rs",
    "tests/conformance/vectors/valid/syn-tip-output-2tile-mono-128x64-q160.ivf",
    "tests/conformance/vectors/valid/syn-tip-output-422-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-tip-output-444-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-tip-output-deblock-2tile-mono-128x64-q160.ivf",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-SCAN-WALK"
name = "Coefficient FSC scan walk"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs"
change_id = "coeff-fsc-scan-walk"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient fsc scan walk in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-BASE-CDF-ROWS"
name = "Coefficient base CDF rows"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs"
change_id = "coeff-base-cdf-rows"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live ordinary base/base-range CDF storage and selection. CDF tests cover defaults, bounds, copying, and update handoff; staged ordinary coverage proves live consumption."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_rows_load_defaults_and_select_by_family", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_selectors_reject_out_of_range_axes", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_tile_copy_does_not_alias_the_frame", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_row_hands_off_to_symbol_decoder_update_mode"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-IDTX-CDF-ROWS"
name = "Coefficient IDTX CDF rows"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs"
change_id = "coeff-idtx-cdf-rows"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live FSC BaseBob, BaseIdtx, BrIdtx, and IdtxSign CDF storage. CDF lifecycle and staged FSC tests protect the surviving path."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::saved_copy_and_average_are_exact_for_supported_subset", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_end_update_copies_saved_rows_and_scales_counts", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-LEVEL-PASS"
name = "Coefficient FSC level pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs"
change_id = "coeff-fsc-level-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live FSC first loop reads BaseBob, BaseIdtx, and conditional BrIdtx rows while evolving local Level state inside the staged FSC path."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-SIGN-PASS"
name = "Coefficient FSC sign pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_sign_pass.rs"
change_id = "coeff-fsc-sign-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc sign pass milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_sign_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-QUANT-PASS"
name = "Coefficient FSC quant pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-quant-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc quant pass milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_sign_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-CONTEXT-COMMIT"
name = "Coefficient FSC context commit"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-context-commit"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live FSC context commit writes culLevel and dcCategory after successful reads and preserves context on selector or commit failure."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_context_commit_failure_preserves_context"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-BRANCH-HANDOFF"
name = "Coefficient FSC branch handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-branch-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc branch handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-BRANCH-SEG-EOB-HANDOFF"
name = "Coefficient FSC branch segment extent handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "5.20.7.30", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-branch-seg-eob-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc branch segment extent handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-BRANCH-SCAN-ORDER"
name = "Coefficient FSC branch scan-order handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.30", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-branch-scan-order-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc branch scan-order handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FSC-BRANCH-TX-SIZE-HANDOFF"
name = "Coefficient FSC branch tx-size handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.30", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs"
change_id = "coeff-fsc-branch-tx-size-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient fsc branch tx-size handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_fsc_branch_from_tx_size is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_preflights_selector_geometry_and_plane_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_invalid_cdf_selector_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-USE-FSC-BRANCH-HANDOFF"
name = "Coefficient useFsc branch handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-use-fsc-branch-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient usefsc branch handoff milestone. Its predecessor wrapper was removed; decode_staged_transform_tool_nonzero_coeffs is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_fsc_d135_10bit_frame_decodes_to_oracle", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-USE-FSC-CONDITION-HANDOFF"
name = "Coefficient useFsc condition handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-use-fsc-condition-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient usefsc condition handoff milestone. Its predecessor wrapper was removed; decode_staged_transform_tool_nonzero_coeffs is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_fsc_d135_10bit_frame_decodes_to_oracle", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-USE-FSC-SHARED-FACTS-HANDOFF"
name = "Coefficient useFsc shared-facts handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-use-fsc-shared-facts-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient usefsc shared-facts handoff milestone. Its predecessor wrapper was removed; decode_staged_transform_tool_nonzero_coeffs is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_fsc_d135_10bit_frame_decodes_to_oracle", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-CDF-Q-CONTEXT-HANDOFF"
name = "Coefficient CDF q-context handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "6.17.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf.rs"
change_id = "coeff-cdf-q-context-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live q-context derivation: coeff_cdf_q_ctx_from_base_q_idx implements the four thresholds and decode_general_intra_plane_coeffs passes the result into EOB and the selected branch."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_cdf_q_context_boundaries_select_expected_contexts", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/fsc_quant_pass_tests.rs::staged_fsc_live_path_reads_bob_idtx_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-FRAME-FACTS-HANDOFF"
name = "Coefficient frame-facts handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.4.8", "5.18.2", "5.20.7.27", "6.4.8", "6.17.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-4-8",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-8",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-frame-facts-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live frame facts are read directly by decode_staged_transform_tool_nonzero_coeffs; deleted frame-facts wrappers and tests are not claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_derives_coeff_tool_flags", "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::lossless_fsc_staged_plane_tx_type_remains_idtx", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_fsc_d135_10bit_frame_decodes_to_oracle"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/input.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs",
    "crates/splot-decode/src/pipeline/mod.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-PARITY-TCQ-HANDOFF"
name = "Coefficient parity-hiding and TCQ handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.18.2", "5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs"
change_id = "coeff-parity-tcq-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live parity-hiding and TCQ derivation occurs in staged_transform_tool_lossless_base_config, followed by ordinary quant-state application; FSC disables TCQ."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_derives_coeff_tool_flags", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::fsc_idtx_block_reconstructs_without_tcq_dequant_shift"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/input.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/derived_tests.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-BASE-SYMBOL-READ"
name = "Coefficient base symbol read"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs"
change_id = "coeff-base-symbol-read"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient base symbol read in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-LEVEL-STATE-WRITE"
name = "Coefficient level state write"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs"
change_id = "coeff-level-state-write"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient level state write milestone. Its predecessor wrapper was removed; the live staged ordinary first and interleaved sign/quant loops is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-SIGN-SYMBOL-READ"
name = "Coefficient sign symbol read"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs"
change_id = "coeff-sign-symbol-read"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient sign symbol read in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol_tests.rs::reads_none_and_literal_sources_in_syntax_order", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-SIGN-SOURCE-DERIVE"
name = "Coefficient sign source derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs"
change_id = "coeff-sign-source-derive"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient sign source derivation in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol_tests.rs::derives_dc_axis_hidden_and_zero_sources", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol_tests.rs::reads_none_and_literal_sources_in_syntax_order", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-MAX-LEVEL-DERIVE"
name = "Coefficient maxLevel derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md#s-3",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs"
change_id = "coeff-max-level-derive"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient maxlevel derivation in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_max_level_derives_low_frequency_limits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_max_level_hidden_final_scan_entry_overrides_limit", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_max_level_pathological_coordinates_are_total", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-TX-CLASS-DERIVE"
name = "Coefficient transform class derivation"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs"
change_id = "coeff-tx-class-derive"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient transform class derivation in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_transform_class_derives_from_plane_tx_type", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-READ-QUANT-SYNTAX"
name = "Coefficient read_quant syntax"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs"
change_id = "coeff-read-quant-syntax"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live stateful read_quant path has focused no-bit, finite, Golomb, HR-average carry, invalid maxLevel, and overflow proof."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::below_threshold_consumes_no_bits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::finite_and_golomb_extensions_decode_expected_quant", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::state_carries_hr_average_between_entries", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::invalid_max_level_and_oversized_golomb_width_are_rejected"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-core/src/symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-QUANT-STATE-WRITE"
name = "Coefficient quantized state write"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs"
change_id = "coeff-quant-state-write"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live quant-state accumulator has focused signed-write, context-summary, hidden-parity, TCQ, and overflow proof."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state_tests.rs::accumulator_applies_sign_and_updates_context_summary", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state_tests.rs::hidden_parity_and_tcq_adjust_quant", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state_tests.rs::hidden_parity_overflow_is_reported"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-QUANT-PASS-COMPOSE"
name = "Coefficient quant pass composition"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-quant-pass-compose"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient quant pass composition milestone. Its predecessor wrapper was removed; the live staged ordinary first and interleaved sign/quant loops is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_max_level_derives_low_frequency_limits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::state_carries_hr_average_between_entries"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-core/src/symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-QUANT-PASS-MAXLEVEL-HANDOFF"
name = "Coefficient quant pass maxLevel handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-quant-pass-maxlevel-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient quant pass maxlevel handoff milestone. Its predecessor wrapper was removed; the live staged ordinary first and interleaved sign/quant loops is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level_tests.rs::coefficient_max_level_derives_low_frequency_limits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant_tests.rs::state_carries_hr_average_between_entries"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-core/src/symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-BASE-DERIVED-LEVEL-PASS"
name = "Coefficient base derived level pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs"
change_id = "coeff-base-derived-level-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient base derived level pass in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_context.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-BASE-PH-CDF-ROW"
name = "Coefficient base Ph CDF row"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.2.5", "8.2.6", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs"
change_id = "coeff-base-ph-cdf-row"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current BasePh CDF storage and selector support. Focused CDF tests protect row mechanics; this row does not claim that every BasePh runtime condition is forced by a surviving focused test."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_rows_load_defaults_and_select_by_family", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_selectors_reject_out_of_range_axes", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_tile_copy_does_not_alias_the_frame", "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::coeff_base_row_hands_off_to_symbol_decoder_update_mode"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/coeff_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-PASS-COMPOSE"
name = "Coefficient ordinary pass composition"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-ordinary-pass-compose"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient ordinary pass composition milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_ordinary_branch_from_lossless is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/scan_walk.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
    "crates/splot-core/src/symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-DERIVED-BASE-PASS"
name = "Coefficient ordinary derived-base pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-ordinary-derived-base-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient ordinary derived-base pass in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-DERIVED-SIGN-PASS"
name = "Coefficient ordinary derived-sign pass"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-ordinary-derived-sign-pass"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient ordinary derived-sign pass in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_applies_evolving_tcq_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/read_quant.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-NONZERO-CONTEXT-COMMIT"
name = "Coefficient nonzero context commit"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-nonzero-context-commit"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live ordinary context commit is protected by success, selector-atomicity, and post-read commit-failure tests."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_context_commit_failure_preserves_context_after_reads"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-STATE-CONTEXT-HANDOFF"
name = "Coefficient state context handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs"
change_id = "coeff-state-context-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current live coefficient state context handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_context_commit_failure_preserves_context_after_reads"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/base_level_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/quant_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/sign_symbol.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-HANDOFF"
name = "Coefficient ordinary branch handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.28", "8.2.5", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-28",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-handoff"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Historical and superseded coefficient ordinary branch handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_ordinary_branch_from_lossless is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/branch.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-TX-CLASS-HANDOFF"
name = "Coefficient ordinary branch transform-class handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-tx-class-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch transform-class handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/max_level.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-PLANE-TYPE-HANDOFF"
name = "Coefficient ordinary branch plane-type handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-plane-type-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch plane-type handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-GEOMETRY-HANDOFF"
name = "Coefficient ordinary branch geometry handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-geometry-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Historical and superseded coefficient ordinary branch geometry handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_ordinary_branch_from_lossless is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-COEFFS-GEOMETRY-HANDOFF"
name = "Coefficient ordinary branch coeffs geometry handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-coeffs-geometry-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Historical and superseded coefficient ordinary branch coeffs geometry handoff milestone. Its predecessor wrapper was removed; apply_staged_nonzero_coeff_ordinary_branch_from_lossless is the current successor, and the cited proof covers that successor without claiming deleted tests still run."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-TX-SIZE-DIMENSIONS"
name = "Coefficient ordinary branch tx size dimensions"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-tx-size-dimensions"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch tx size dimensions in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-ADJUSTED-TX-SIZE"
name = "Coefficient ordinary branch adjusted tx size"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-adjusted-tx-size"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch adjusted tx size in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-TX-SIZE-CONTEXT"
name = "Coefficient ordinary branch tx size context"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-tx-size-context"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch tx size context in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-SCAN-ORDER"
name = "Coefficient ordinary branch scan order"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.30", "8.3.2", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-30",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-scan-order"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch scan order in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_selector_error_preserves_context_and_reader_state"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-MODE-TO-TXFM-HANDOFF"
name = "Coefficient ordinary branch Mode_To_Txfm handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.29", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-mode-to-txfm-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch mode_to_txfm handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-DIRECTIONAL-UV-HANDOFF"
name = "Coefficient ordinary branch directional UV handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "5.20.7.29", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-directional-uv-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch directional uv handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-LUMA-TXTYPES-HANDOFF"
name = "Coefficient ordinary branch luma TxTypes handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "5.20.7.29"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-luma-txtypes-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch luma txtypes handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-CHROMA-INTER-TXTYPES-HANDOFF"
name = "Coefficient ordinary branch chroma inter TxTypes handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "5.20.7.27", "5.20.7.29"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-chroma-inter-txtypes-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch chroma inter txtypes handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-TX-SET-HANDOFF"
name = "Coefficient ordinary branch txSet handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.29", "5.20.8.3", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-tx-set-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live coefficient ordinary branch txset handoff in the staged coefficient path. The cited focused or oracle tests protect the surviving behavior named by this row; no deleted helper or test is claimed."

[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::luma_txtype_residual_staged_base_config_uses_retained_luma_tx_type", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle", "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_preflights_geometry_and_bounds_without_consumption"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-core/src/tables/conversion.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-COEFF-ORDINARY-BRANCH-LOSSLESS-HANDOFF"
name = "Coefficient ordinary branch lossless handoff"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.27", "5.20.7.29", "5.20.8.3", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-27",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-29",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-8-3",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs"
change_id = "coeff-ordinary-branch-lossless-handoff"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Current live lossless ordinary path derives lossless transform/base configuration, reads coefficients, commits context, and has focused plus oracle proof."
[feature.status]
mapped = "done"
types = "done"
parse = "partial"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "partial"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass_tests.rs::staged_ordinary_live_path_reads_base_br_sign_quant_and_commits", "crates/splot-decode/src/pipeline/general_intra_tests.rs::lossless_nonzero_intra_frame_decodes_to_oracle"]
commands = ["cargo test -p splot-decode coeff_loop --locked", "cargo test -p splot-decode general_intra_residual --locked", "cargo xtask check-feature-status"]
fixtures = [
    "tests/conformance/vectors/valid/syn-lossless-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nonzero-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-cardinal-y-v-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-cardinal-y-h-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-paeth-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d135-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d45-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d67-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d203-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d203-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d113-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d45-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d45-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d67-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d113-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d135-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d157-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-luma-d203-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d45-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d157-chroma-follow-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d113-chroma-follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d157-chroma-follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-luma-d203-chroma-follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-h-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-v-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-h-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-v-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d45-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d67-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d113-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d135-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d157-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d203-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-smooth-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d45-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d45follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d67-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d67follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d113follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d135follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d157follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d203follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-h-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-v-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-vfollow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-hfollow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-paeth-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d135-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d113-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d157-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-d203-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-nondc-chroma-paeth-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d135-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d157-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d45-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d67-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-smooth-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-smooth-v-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d113-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d203-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-paeth-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d45-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d67-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d67follow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-v-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-vfollow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-h-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-paeth-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-hfollow-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d113-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d135-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d157-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-nondc-chroma-d203-leftedge-128x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-dpcm-y-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-dpcm-uv-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-dpcm-uv-intra-64x64.ivf",
    "tests/conformance/vectors/valid/syn-lossless-sdp-dpcm-uv-h-intra-64x64.ivf",
    "tests/conformance/decoder-oracle.toml",
    "crates/splot-decode/src/pipeline/frame_engine/intra.rs",
    "crates/splot-decode/src/pipeline/general_intra.rs",
    "crates/splot-decode/src/residual/pipeline.rs",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass/geometry.rs",
    "crates/splot-decode/src/bitstream/tile_payload/coeff_loop/ordinary_pass.rs",
    "crates/splot-core/src/tables/conversion.rs",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-TX-SIZE-SYMBOLIC-TABLES"
name = "Decode TxSize symbolic tables"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["6.19.6.1", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-6-1",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
    "docs/spec/av2/1.0.0/attachments/all_tables.h",
]
crate = "splot-core"
module = "crates/splot-core/src/tables/conversion.rs"
change_id = "decode-tx-size-symbolic-tables"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Extends `cargo xtask gen-tables` with a narrow AV2 §6.19.6.1 TxSize ordinal resolver for the §9.2 symbolic transform-size conversion tables `Adjusted_Tx_Size`, `Tx_Size_Sqr`, and `Tx_Size_Sqr_Up`. The tables are emitted into generated `splot-core::tables::conversion` from the committed all_tables.h mirror as numeric `i32` arrays, reducing the skip allowlist from 9 to 6 symbolic tables. Focused generator tests prove edge TxSize ordinals and loud failure for unsupported symbols; generated table spot tests prove representative `Adjusted_Tx_Size`, `Tx_Size_Sqr`, and `Tx_Size_Sqr_Up` values against the §9.2 mirror. This feature is infrastructure only: no runtime `coeffs()` path consumes the tables yet, `txSzCtx` remains caller-resolved, and `compute_tx_type`, scan derivation, dequantization, reconstruction, output, reference refresh, public APIs, AVM/dav2d evidence, broad `decode_block()`, and broad `decode_tile()` remain unimplemented."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/gen_tables/block_symbols.rs::tests::resolves_supported_tx_size_table_body",
    "xtask/src/gen_tables/block_symbols.rs::tests::rejects_non_tx_size_symbol_in_supported_table",
    "crates/splot-core/tests/tables_spot.rs::conversion_tx_size_symbolic_tables_match_mirror",
]
commands = [
    "cargo xtask gen-tables",
    "cargo xtask gen-tables --check",
    "cargo test -p xtask gen_tables --locked",
    "cargo test -p splot-core --test tables_spot conversion_tx_size_symbolic_tables_match_mirror --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "xtask/src/gen_tables.rs",
    "xtask/src/gen_tables/block_symbols.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-core/tests/tables_spot.rs",
    "docs/spec/av2/1.0.0/attachments/all_tables.h",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-MODE-TO-TXFM-SYMBOLIC-TABLE"
name = "Decode Mode_To_Txfm symbolic table"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["3", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/03-symbols.md",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
    "docs/spec/av2/1.0.0/attachments/all_tables.h",
]
crate = "splot-core"
module = "crates/splot-core/src/tables/conversion.rs"
change_id = "decode-mode-to-txfm-table"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Extends `cargo xtask gen-tables` with a narrow AV2 §3 TxType ordinal resolver for the §9.2 symbolic `Mode_To_Txfm[UV_INTRA_MODES_CFL_ALLOWED]` conversion table. The table is emitted into generated `splot-core::tables::conversion` from the committed all_tables.h mirror as a numeric `i32` array, reducing the skip allowlist from 6 to 5 symbolic tables. Focused generator tests prove representative TxType ordinals and loud failure for unsupported symbols; generated table spot tests prove every `Mode_To_Txfm` value against the §9.2 mirror and §3 TxType assignments. This feature is infrastructure only: no runtime `coeffs()` path consumes the table yet, §5.20.7.29 `compute_tx_type` remains unwired, `TxTypes` tile state, transform-set membership, luma tx-size state, mode syntax, wide-angle mapping, dequantization, reconstruction, output, reference refresh, public APIs, AVM/dav2d evidence, broad `decode_block()`, and broad `decode_tile()` remain unimplemented."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/gen_tables/block_symbols.rs::tests::resolves_supported_tx_type_table_body",
    "xtask/src/gen_tables/block_symbols.rs::tests::rejects_non_tx_type_symbol_in_supported_table",
    "crates/splot-core/tests/tables_spot.rs::conversion_mode_to_txfm_matches_mirror",
]
commands = [
    "cargo xtask gen-tables",
    "cargo xtask gen-tables --check",
    "cargo test -p xtask gen_tables --locked",
    "cargo test -p splot-core --test tables_spot conversion_mode_to_txfm_matches_mirror --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask check-feature-status",
]
fixtures = [
    "xtask/src/gen_tables.rs",
    "xtask/src/gen_tables/block_symbols.rs",
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-core/tests/tables_spot.rs",
    "docs/spec/av2/1.0.0/attachments/all_tables.h",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-TILE-CDF-SAVE-LIFECYCLE-BOUNDARY"
name = "Tile CDF save lifecycle boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.1", "5.20.2.1", "6.19.1", "7.5", "8.2.2", "8.2.4", "8.2.6", "8.3.1", "8.3.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-4",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf/lifecycle.rs"
change_id = "tile-cdf-save-lifecycle-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Supported crate-private AV2 tile CDF lifecycle boundary for the currently supported CDF subset only: partition-entry rows (`TileDoSplitCdf`, `TileDoSquareSplitCdf`, `TileRectTypeCdf`, `TileDoExtPartitionCdf`, `TileDoUneven4wayPartitionCdf`) plus the minimal flat-intra block-symbol rows (`TileYModeSetCdf`, `TileYModeIndexCdf`, `TileTxbSkipCdf`, `TileUvModeCflNotAllowedCdf`, and `TileVTxbSkipCdf`). `TileCdfWorkUnitBoundary` carries Frame, Saved, and Tile CDF subset state; successful minimal tier tile completion applies final tile-local rows to Saved CDF rows only after the traced block frontier and `exit_symbol()` pass, honors copy versus average policy, then applies the supported-subset `frame_end_update_cdf()` path by copying Saved rows into Frame rows while scaling row counts per AV2 §7.5. Symbol mismatch, CDF/symbol parse failure, resource-limit failure, and exit-padding failure leave Saved and Frame rows unchanged, and minimal hash/raw/Y4M output bytes remain stable. This row is not full CDF lifecycle support: broad §8.3 selector coverage, all §9.3 banks, multi-tile or multi-tile-group averaging, reference-frame CDF persistence, `load_cdfs`, `save_cdfs`, `blend_cdfs`, broad `decode_tile()`, reconstruction, output, reference refresh, public APIs, and external decoder integration remain out of scope."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::saved_copy_and_average_are_exact_for_supported_subset",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::disabled_cdf_update_keeps_saved_subset_at_initial_rows",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::frame_end_update_copies_saved_rows_and_scales_counts",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::work_unit_boundary_applies_saved_and_frame_updates_transactionally",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::luma_skip_fixture_decodes_skip_branch_through_general_path",
]
commands = [
    "cargo test -p splot-decode tile_payload::cdf --locked",
    "cargo test -p splot-decode tile_payload::tile_frontier --locked",
    "cargo test -p splot-decode output::hash --locked",
    "cargo test -p splot-decode output::y4m --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/lifecycle.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/block_rows.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
]
diagnostics = ["decode/unsupported-feature", "decode/resource-limit"]

[[feature]]
id = "DECODE-TILE-PARTITION-SYMBOL-READ-BOUNDARY"
name = "Tile partition symbol read boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.2", "8.2.6", "8.3.1", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/cdf/partition_read.rs"
change_id = "tile-partition-symbol-read-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private `splot-decode` boundary for individual AV2 §5.20.3.2 partition-entry `S()` reads over the existing `DECODE-TILE-CDF-SELECTION-BOUNDARY` selectors. `TileCdfSubset::read_partition_entry_symbol` validates a `TileCdfSelector`, passes the selected mutable CDF row to a caller-owned `SymbolDecoder::read_symbol(cdf)`, returns the raw `Symbol`, preserves selector errors separately from `splot-core` symbol/CDF errors, and keeps sequential symbol state owned by the caller. The `Rect_Part_Table`, final outcome, and `uneven_4way_partition_type L(1)` consumer is tracked separately by `DECODE-TILE-PARTITION-DECISION-BOUNDARY`; implied and allowed-partition derivation is tracked separately by `DECODE-TILE-PARTITION-ALLOWED-BOUNDARY`. This row does not implement recursive `read_partition()`/`decode_tile()` traversal, `exit_symbol()` after real syntax, full §8.3 CDF selection, full Tile/Saved CDF banks, reconstruction, hashes, Y4M output, reference refresh, public API support, AVM/dav2d invocation, or scheduler/dependency changes."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf/partition_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs::selected_row_hands_off_to_symbol_decoder_update_modes",
]
commands = [
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload/cdf.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/partition_read.rs",
    "crates/splot-decode/src/bitstream/tile_payload/cdf/tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tests.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-TILE-PARTITION-DECISION-BOUNDARY"
name = "Tile partition decision boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.2", "6.19.3", "8.2.5", "8.2.6", "8.3.1", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition.rs"
change_id = "tile-partition-decision-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private `splot-decode` AV2 §5.20.3.2 partition decision boundary over caller-provided facts. `read_partition_decision` accepts a typed allowed partition set, optional implied partition, BRU-active state, optional rect-type implication, and already-bounded CDF context inputs; returns a typed `PartitionType`; follows the spec fall-through when an implied partition is not allowed; returns `PARTITION_NONE` on the inactive-BRU branch after the single-allowed branch even when `allowed[PARTITION_NONE]` is false; uses the existing `DECODE-TILE-PARTITION-SYMBOL-READ-BOUNDARY` for reached `do_split`, `do_square_split`, `rect_type`, `do_ext_partition`, and `do_uneven_4way_partition` `S()` reads; consumes `uneven_4way_partition_type L(1)` only when the branch order reaches it; and validates empty allowed sets or impossible final table results with typed crate-private errors. Implied and allowed-partition derivation is tracked separately by `DECODE-TILE-PARTITION-ALLOWED-BOUNDARY`. This row does not implement recursive `read_partition()`/`decode_tile()` traversal, `exit_symbol()` after real syntax, Saved CDF mutation, reconstruction, hashes, Y4M output, reference refresh, public API support, AVM/dav2d invocation, or scheduler/dependency changes. The generated §9.2 `Partition_Subsize` / `H_Partition_Midsize` lookup wrapper is tracked separately by `DECODE-TILE-PARTITION-SIZE-TABLE-BOUNDARY`."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition.rs",
]
commands = [
    "cargo test -p splot-core symbol --locked",
    "cargo test -p splot-core --test tables_spot --locked",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-core --all-targets --all-features --locked -- -D warnings",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
    "cargo xtask ci",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-TILE-PARTITION-SIZE-TABLE-BOUNDARY"
name = "Tile partition size table boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.1", "5.20.3.2", "6.19.3", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-3",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
    "docs/spec/av2/1.0.0/attachments/all_tables.h",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs"
change_id = "tile-partition-size-tables"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Adds a crate-private AV2 §9.2 partition-size table boundary for future §5.20.3.1 traversal and §5.20.3.2 allowed-partition derivation. `cargo xtask gen-tables` now resolves the two `BLOCK_*` symbolic tables `Partition_Subsize[EXT_PARTITION_TYPES][BLOCK_SIZES]` and `H_Partition_Midsize[BLOCK_SIZES]` from the committed all_tables.h attachment, while leaving unrelated symbolic/reserved tables skip-allowlisted. `splot-decode` wraps the generated arrays with a checked `BlockSize` newtype, `PartitionSubsize::Valid` / `PartitionSubsize::Invalid` results so `BLOCK_INVALID` is never a valid block-size index, table-backed 4x4 geometry helpers, and typed `PartitionSizeError` paths for invalid caller inputs or impossible generated values. The allowed-partition derivation over these helpers is tracked separately by `DECODE-TILE-PARTITION-ALLOWED-BOUNDARY`. This row does not implement recursive `read_partition()` / `decode_partition()` / `decode_tile()` traversal, `MiSizes` mutation, `exit_symbol()` after real syntax, CDF mutation, reconstruction, output, reference refresh, public API behavior, AVM/dav2d invocation, or scheduler/dependency changes."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/tests/tables_spot.rs::conversion_partition_size_tables_match_mirror",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs",
    "xtask/src/gen_tables.rs::tests",
    "xtask/src/gen_tables/block_symbols.rs::tests",
]
commands = [
    "cargo xtask gen-tables --check",
    "cargo test -p splot-core --test tables_spot --locked",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-core/src/tables/conversion.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs",
    "xtask/src/gen_tables.rs",
    "xtask/src/gen_tables/block_symbols.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-TILE-PARTITION-ALLOWED-BOUNDARY"
name = "Tile partition allowed set boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.3.2", "5.20.7.26", "6.19.3", "9.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-26",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-3",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_allowed.rs"
change_id = "tile-partition-allowed-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private AV2 §5.20.3.2 partition implied and allowed-set derivation boundary for future tile traversal. `partition_allowed.rs` derives `partition_implied_at_boundary`, `partition_implied`, `rect_type_implied_by_bsize`, `is_partition_allowed`, `init_allowed_partitions`, and decision facts from explicit MI geometry, tree type, chroma, subsampling, feature-flag, region, and known luma-partition inputs. It uses generated §9.2 block geometry helpers, derives §5.20.7.26 residual sizes without treating `BLOCK_INVALID` as a valid block size, locks that derivation against the literal `Subsampled_Size` table, preserves typed crate-private errors for caller overflow and impossible table facts, and reuses the existing partition-size, CDF-context, and partition-decision boundaries without public API exposure. This row does not implement recursive `read_partition()` / `decode_partition()` / `decode_tile()` traversal, `MiSizes`, `ChromaPartitionKnown`, or `LumaPartitions` array mutation, symbol reads, CDF mutation, `exit_symbol()` after real syntax, reconstruction, output, reference refresh, AVM/dav2d invocation, or scheduler/dependency changes."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed/spec_table_tests.rs::get_plane_residual_size_matches_subsampled_size_table",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs::tests::block_geometry_helpers_use_generated_tables",
]
commands = [
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo test -p splot-decode --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed/spec_table_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs",
    "change_id:tile-partition-allowed-boundary",
]
diagnostics = []

[[feature]]
id = "DECODE-TILE-PARTITION-TRAVERSAL-BOUNDARY"
name = "Tile partition traversal"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.2.1", "5.20.3.1", "5.20.3.2", "5.20.9.1", "5.20.10.4", "5.20.10.5", "6.19.2.1", "6.19.3", "8.2.5", "8.2.6", "8.3.1", "8.3.2", "9.2", "9.3"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-2-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-3-2",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-9-1",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-4",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-5",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-3",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-2-6",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-02-conversion-tables.md#s-9-2",
    "docs/spec/av2/1.0.0/09-additional-tables/09-03-default-cdf-tables.md#s-9-3",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs"
change_id = "tile-partition-traversal-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Current crate-private AV2 §5.20.3.1 traversal used by the live tile parser. `GeneralIntraPartitionTreeCursor` walks every in-frame superblock and partition leaf, keeps one §8.2 `SymbolDecoder` across leaf callbacks, mutates the work unit tile CDF rows as syntax is consumed, and reads and updates `TileMiSizeState` plus adjacent mode state after each published leaf. The traversal derives `AvailU` and `AvailL` from tile bounds, enforces `max_tile_partition_steps`, and returns typed coordinate and resource errors. It supports intra SDP and active extended-SDP luma/chroma continuation. Loop-restoration reads use the live tree-type plane ranges: shared trees cover `[0, NumPlanes)`, luma parts cover `[0, 1)`, and chroma parts cover `[1, NumPlanes)`. Active Wiener NS units become `WienerNsLrSourceBlock` records, and unit-coded filters are retained in `GeneralIntraPartitionTreeOutput`; the tile parser merges both into `FrameFilterRecords`, and final filtering consumes them during reconstruction. Unsupported loop-restoration plane/tool combinations and BRU/bridge remain explicit gates. The boundary is crate-private and does not claim complete AV2 syntax coverage or a public `decode_tile` API."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::frame_level_lr_symbol_precedes_partition_read",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::inactive_frame_level_wiener_ns_advances_current_cursor_without_source_output",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_advances_every_covered_inactive_wiener_ns_unit",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::frame_level_wiener_ns_rejects_invalid_unit_size_before_partition_read",
]
commands = [
    "cargo test -p splot-decode partition_traversal --locked",
    "cargo test -p splot-decode tile_payload --locked",
    "cargo clippy -p splot-decode --all-targets --all-features --locked -- -D warnings",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_allowed.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_size.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal.rs",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
]
diagnostics = []

[[feature]]
id = "DECODE-TILE-MI-SIZE-STATE-BOUNDARY"
name = "Tile MI-size state boundary"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.4.1", "6.19.2.1", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-2-1",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/bitstream/tile_payload/mi_size_state.rs"
change_id = "tile-mi-size-state-boundary"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds a crate-private AV2 §5.20.4.1 MI-size state boundary for the current tile payload frontier. `mi_size_state.rs` owns bounded luma/chroma `MiSizes`, `LeftMiSizes`, and `AboveMiSizes` state, initializes both planes with the §6.19.2.1 clear-context `BLOCK_256X256` sentinel over superblock-padded row and column extents, validates nonzero visible dimensions, padded allocation sizes, block-size table lookups, coordinate addition, visible start coordinates, and full padded block footprints before mutation, and exposes short-lived read-only `TilePartitionContextState` views for existing §8.3.2 partition context consumers. The minimal tier replaces its ad hoc synthetic context arrays with this state object, checks padded grid cells and total MI-state entry storage bytes against `DecodeLimits` before allocation, and applies the traced root luma update only after the minimal block-symbol frontier succeeds; chroma writes are supported only when a future caller supplies `ChromaMiRow`, `ChromaMiCol`, and `ChromaMiSize`. Hash/raw/Y4M output identity is unchanged. This row does not claim full `decode_block()`, deriving every `ChromaMiRow`, `ChromaMiCol`, and `ChromaMiSize`, recursive `read_partition()`, broad `decode_tile()` traversal, mode-info or transform/residual syntax, reconstruction expansion, reference refresh, public APIs, AVM/dav2d invocation, or external decoder integration."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/tile_payload/mi_size_state.rs::tests",
    "crates/splot-decode/src/pipeline/mod.rs::general_intra_tests::luma_skip_fixture_decodes_skip_branch_through_general_path",
    "crates/splot-decode/src/output/hash.rs::tests::minimal_fixture_decodes_to_hash_report",
    "crates/splot-decode/src/output/raw.rs::tests::minimal_fixture_decodes_to_exact_raw_bytes",
    "crates/splot-decode/src/output/y4m.rs::tests::minimal_fixture_decodes_to_exact_y4m_bytes",
]
commands = [
    "cargo test -p splot-decode tile_payload --locked",
    "cargo test -p splot-decode output::hash --locked",
    "cargo test -p splot-decode output::raw --locked",
    "cargo test -p splot-decode output::y4m --locked",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "crates/splot-decode/src/bitstream/tile_payload.rs",
    "crates/splot-decode/src/bitstream/tile_payload/mi_size_state.rs",
    "crates/splot-decode/src/bitstream/tile_payload/tile_frontier.rs",
    "change_id:tile-mi-size-state-boundary",
    "tests/conformance/vectors/valid/syn-flat-intra-64x64-minimal.ivf",
]
diagnostics = []

[[feature]]
id = "DECODE-UNSUPPORTED-DIAGNOSTIC-API"
name = "Decode unsupported diagnostic API"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.1"]
sources = ["docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1"]
crate = "splot-decode"
module = "crates/splot-decode/src/lib.rs"
change_id = "decode-unsupported-diagnostic-api"
tracking_issue = ""
owner = "decoder"
risk = "low"
notes = "Moves ownership of `decode/unsupported-feature` diagnostic metadata into `splot-decode` as public report APIs rendered by the CLI; planner unsupported structures reuse the rule with planner metadata. The API does not reconstruct pixels, compute hashes, produce Y4M, or invoke external decoders."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/lib.rs::tests::decode_severity_displays_stable_spelling",
    "crates/splot-decode/src/diagnostic.rs::tests::unsupported_structure_report_uses_planner_metadata",
]
commands = [
    "cargo test -p splot-decode --locked",
    "cargo test -p splot-cli --test decode_cli --locked",
    "cargo xtask check-diagnostic-registry",
    "cargo xtask check-dependency-direction",
]
fixtures = []
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DOC-DECODER-SUPPORT-MATRIX"
name = "Decoder support matrix documentation"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/DECODER-SUPPORT-MATRIX.toml"
change_id = "decoder-roadmap-matrix-boundary"
tracking_issue = ""
owner = "docs"
risk = "medium"
notes = "Adds docs/DECODER-SUPPORT-MATRIX.toml as the canonical decoder/reconstruction support matrix. docs/DECODER-SUPPORT-STATUS.md can be rendered on demand. The matrix records supported tier, status, tests, diagnostics, local reference evidence, and notes without executing AVM/dav2d."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["xtask/src/decoder_support.rs::tests::valid_matrix_renders_rows_deterministically"]
commands = ["cargo xtask check-decoder-support"]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml"]
diagnostics = []

[[feature]]
id = "XTASK-DECODER-SUPPORT-STATUS"
name = "Decoder support status reporting and checks"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/decoder_support.rs"
change_id = "decoder-roadmap-matrix-boundary"
tracking_issue = ""
owner = "automation"
risk = "medium"
notes = "Adds `cargo xtask decoder-support` and `cargo xtask check-decoder-support`, then wires the drift check into `cargo xtask ci`. The commands render decoder support docs on demand and validate any committed render without locating, building, invoking, or requiring AVM/dav2d."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/decoder_support.rs::tests::valid_matrix_renders_rows_deterministically",
    "xtask/src/decoder_support.rs::tests::invalid_status_and_missing_required_field_are_rejected",
    "xtask/src/decoder_support.rs::tests::supported_row_requires_test_or_fixture",
    "xtask/src/decoder_support.rs::tests::supported_proof_rejects_external_decoders",
    "xtask/src/decoder_support.rs::tests::reference_evidence_rejects_local_absolute_paths",
    "xtask/src/decoder_support.rs::tests::rendered_fields_reject_local_absolute_paths",
    "xtask/src/decoder_support.rs::tests::metadata_rejects_local_absolute_paths",
    "xtask/src/decoder_support.rs::tests::check_decoder_support_detects_drift",
]
commands = [
    "cargo test -p xtask decoder_support --locked",
    "cargo xtask check-decoder-support",
]
fixtures = ["docs/DECODER-SUPPORT-MATRIX.toml"]
diagnostics = []

[[feature]]
id = "XTASK-LOCAL-REFERENCE-EVIDENCE-MANIFEST"
name = "Portable local-reference evidence manifest"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/reference_evidence.rs"
change_id = "local-reference-evidence-manifest-contract"
tracking_issue = ""
owner = "automation"
risk = "medium"
notes = "Adds docs/LOCAL-REFERENCE-EVIDENCE.toml and `cargo xtask check-reference-evidence` for decoder local-reference metadata. The checker validates manifest versioning, evidence IDs, Feature IDs, decoder-support rows, repo-relative fixture paths, fixture SHA-256 and byte lengths, digest metadata, equality assertions, and local path leakage without locating or invoking AVM, dav2d, ffmpeg, the network, or `splot decode`. The manifest now contains two checked AVM/dav2d raw MD5 agreement entries for committed fixtures and does not claim runtime decode, reconstruction, hash, or Y4M support."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/reference_evidence.rs::tests::valid_manifest_with_fixture_and_digest_assertion_passes",
    "xtask/src/reference_evidence.rs::tests::duplicate_evidence_ids_are_rejected",
    "xtask/src/reference_evidence.rs::tests::invalid_feature_and_decoder_support_rows_are_rejected",
    "xtask/src/reference_evidence.rs::tests::local_and_absolute_paths_are_rejected",
    "xtask/src/reference_evidence.rs::tests::command_summary_rejects_paths_and_shell_composition",
    "xtask/src/reference_evidence.rs::tests::stale_fixture_hash_and_length_are_rejected",
    "xtask/src/reference_evidence.rs::tests::symlink_fixture_is_rejected",
    "xtask/src/reference_evidence.rs::tests::intermediate_symlink_fixture_is_rejected",
    "xtask/src/reference_evidence.rs::tests::git_worktree_requires_tracked_fixture",
    "xtask/src/reference_evidence.rs::tests::malformed_digest_and_broken_assertions_are_rejected",
    "xtask/src/reference_evidence.rs::tests::duplicate_reference_run_ids_are_rejected",
    "xtask/src/reference_evidence.rs::tests::tautological_digest_assertions_are_rejected",
    "xtask/src/reference_evidence.rs::tests::unequal_digest_assertions_are_rejected",
]
commands = [
    "cargo test -p xtask reference_evidence --locked",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
]
fixtures = ["docs/LOCAL-REFERENCE-EVIDENCE.toml"]
diagnostics = []

[[feature]]
id = "XTASK-DECODER-DIAGNOSTIC-REGISTRY"
name = "Decoder diagnostic registry enforcement"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/diagnostic_registry.rs"
change_id = "decoder-diagnostic-registry"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Extends `cargo xtask check-diagnostic-registry` so it enforces docs/DIAGNOSTICS.md against current decoder emission roots in addition to the existing validator registry. The decoder descriptor scans crates/splot-cli/src/commands/decode.rs and crates/splot-decode/src, only accepts `decode/*` rule IDs, and never locates or invokes external decoder, AVM, or dav2d tools."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/diagnostic_registry.rs::tests::decoder_registry_accepts_matching_source_and_doc",
    "xtask/src/diagnostic_registry.rs::tests::decoder_registry_reports_undocumented_id",
    "xtask/src/diagnostic_registry.rs::tests::decoder_registry_reports_unemitted_id",
    "xtask/src/diagnostic_registry.rs::tests::decoder_registry_rejects_wrong_prefix_in_source",
    "xtask/src/diagnostic_registry.rs::tests::decoder_registry_rejects_wrong_prefix_in_doc",
]
commands = [
    "cargo test -p xtask diagnostic_registry --locked",
    "cargo xtask check-diagnostic-registry",
]
fixtures = ["docs/DIAGNOSTICS.md"]
diagnostics = []

[[feature]]
id = "CLI-DECODE"
name = "splot decode command"
category = "cli"
kind = "cli"
spec_sections = ["7.1"]
sources = ["docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1"]
crate = "splot-cli"
module = "crates/splot-cli/src/commands/decode.rs"
change_id = "decode-cli-byte-planner-handoff"
tracking_issue = ""
owner = "cli"
risk = "medium"
notes = "Tracks the `splot decode` CLI surface. The command bounds file reads with the default `max_input_bytes` policy before constructing a full input buffer, constructs `DecodeContext` for in-policy inputs, and renders `splot-decode` structured diagnostics for malformed sources, resource limits, planner unsupported structures, runtime tier rejections, or output publication failures. Hash mode dispatches the supported minimal fixture hash path. Raw and Y4M modes atomically publish regular output files and write the explicitly supported Unix null device directly after complete validation and serialization. Broad stream-like paths, broad runtime decode, broad raw/Y4M output, broad reconstruction, and external decoder invocation remain unsupported."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = ["crates/splot-cli/tests/decode_y4m_cli.rs::decode_out_of_tier_y4m_text_mode_emits_unsupported_feature_without_touching_output", "crates/splot-cli/tests/decode_y4m_cli.rs::decode_out_of_tier_y4m_json_mode_emits_unsupported_feature_object", "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_source_error_wins_before_missing_output_parent", "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_writes_to_null_device", "crates/splot-cli/tests/decode_raw_cli.rs::decode_out_of_tier_raw_text_mode_emits_unsupported_feature_without_touching_output", "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_source_error_wins_before_missing_output_parent", "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_writes_to_null_device", "crates/splot-cli/tests/decode_cli.rs::decode_missing_input_is_operational_error_and_does_not_touch_files", "crates/splot-cli/tests/decode_cli.rs::decode_malformed_source_text_mode_emits_structured_diagnostic", "crates/splot-cli/tests/decode_cli.rs::decode_unsupported_structure_json_mode_uses_planner_metadata", "crates/splot-cli/tests/decode_cli.rs::decode_resource_limit_json_mode_reports_limit_values", "crates/splot-cli/tests/decode_cli.rs::decode_oversized_input_reports_resource_limit_without_touching_output", "crates/splot-cli/tests/decode_cli.rs::decode_thread_policies_emit_same_json_diagnostic", "crates/splot-cli/tests/decode_y4m_cli.rs::decode_explicit_y4m_success_for_minimal_fixture", "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_missing_output_parent_emits_output_error_json", "crates/splot-cli/tests/decode_raw_cli.rs::decode_explicit_raw_success_for_minimal_fixture", "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_missing_output_parent_emits_output_error_json"]
commands = ["cargo test -p splot-cli --test decode_cli --locked", "cargo test -p splot-cli --test decode_y4m_cli --locked", "cargo test -p splot-cli --test decode_raw_cli --locked", "cargo test -p splot-decode --locked", "cargo xtask check-concurrency-policy", "cargo xtask check-diagnostic-registry"]
fixtures = []
diagnostics = ["decode/unsupported-feature", "decode/malformed-source", "decode/resource-limit", "decode/output-error"]

[[feature]]
id = "CLI-DECODE-HASH-OUTPUT"
name = "splot decode non-file output CLI contract"
category = "cli"
kind = "cli"
spec_sections = ["7.1", "7.21"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21",
]
crate = "splot-cli"
module = "crates/splot-cli/src/commands/decode.rs"
change_id = "decode-hash-output-cli-contract"
tracking_issue = ""
owner = "cli"
risk = "medium"
notes = "Adds the CLI parse contract for non-file decode output: `--output-format hash` emits deterministic decoded-frame hashes, while `--output-format null` decodes and settles every requested output frame without hashing or serializing it. Neither mode requires a Y4M output path, `-o` alone remains implicit Y4M, and valid invocations read input bytes before emitting the relevant success artifact or structured diagnostic. Runtime hash computation for the committed minimal fixture is supported by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`; Y4M success is tracked separately by `DECODE-Y4M-RUNTIME-OUTPUT`. External decoder invocation remains unsupported."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_output_format_emits_unsupported_text_without_output_path",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_output_format_missing_input_is_operational_error",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_output_format_json_emits_same_diagnostic",
    "crates/splot-cli/tests/decode_cli.rs::decode_invalid_output_format_is_usage_error",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_output_format_with_output_path_does_not_touch_file",
    "crates/splot-cli/tests/decode_cli.rs::decode_null_output_format_discards_supported_frame",
    "crates/splot-cli/tests/decode_cli.rs::decode_null_output_format_with_output_path_does_not_touch_file",
    "crates/splot-cli/tests/decode_cli.rs::decode_null_output_format_reports_its_diagnostic_mode",
    "crates/splot-cli/tests/decode_cli.rs::decode_without_output_selection_is_usage_error",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_explicit_y4m_output_format_requires_output_path",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_explicit_y4m_output_format_matches_implicit_no_touch_behavior",
]
commands = ["cargo test -p splot-cli --test decode_cli --locked", "cargo test -p splot-cli --test decode_y4m_cli --locked"]
fixtures = []
diagnostics = ["decode/unsupported-feature", "decode/malformed-source", "decode/resource-limit"]

[[feature]]
id = "DOC-DECODER-OUTPUT-EQUIVALENCE-CONTRACT"
name = "Decoder output equivalence contract"
category = "docs"
kind = "docs"
spec_sections = ["5.17.12", "6.16.13", "6.4.1", "6.17.4.1", "6.17.4.4", "7.21", "7.21.1", "7.21.2", "7.21.3", "7.21.4", "7.21.5", "7.21.6", "7.21.7", "7.22", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-17-12",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16-13",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-1",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-4-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-4",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-5",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-6",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-21-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-22",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "docs"
module = "docs/CONFORMANCE.md"
change_id = "decoder-output-equivalence-contract"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Defines the decoder output identity contract: raw_intermediate_output and post_film_grain_output variant identity, splot-dfh-sha256-v1 raw-intermediate hash stability, hash JSON success schema, visible sample serialization, output event order including show-existing and flush output, raw/Y4M output policy, atomic regular-file publication, direct Unix-null-device output, metadata-hash separation, and local-only reference evidence boundaries. This row is documentation-only; runtime minimal hash output is tracked by `DECODE-MINIMAL-TIER-RUNTIME-SUCCESS`, runtime minimal raw output is tracked by `DECODE-MINIMAL-RAW-RUNTIME-OUTPUT`, runtime minimal Y4M output is tracked by `DECODE-Y4M-RUNTIME-OUTPUT`, and broad raw/Y4M output, film-grain synthesis, metadata verification, reference refresh, and external reference decoder integration remain unsupported."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
]
fixtures = [
    "docs/CONFORMANCE.md",
    "docs/DECODER-SUPPORT-MATRIX.toml",
]
diagnostics = []

[[feature]]
id = "DOC-DECODER-FULL-CONFORMANCE-CONTRACT"
name = "Full decoder conformance contract"
category = "docs"
kind = "docs"
spec_sections = []
sources = []
crate = "docs"
module = "docs/CONFORMANCE.md"
change_id = "decoder-full-conformance-contract"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Defines the public full AV2 decoder conformance contract, current non-claim, output variants, structured diagnostics, output-file safety, local-only AVM/dav2d evidence boundary, and final completion criteria. This row is documentation-only; runtime minimal hash, raw, and Y4M support are tracked by dedicated code rows, while broad runtime decode, broad output, reconstruction, reference refresh, and external reference decoder integration remain unsupported."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo xtask check-decoder-support",
    "cargo xtask check-decoder-conformance-coverage",
]
fixtures = ["docs/CONFORMANCE.md"]
diagnostics = []

[[feature]]
id = "XTASK-DECODER-CONFORMANCE-COVERAGE"
name = "Decoder conformance coverage gate"
category = "automation"
kind = "automation"
spec_sections = []
sources = []
crate = "xtask"
module = "xtask/src/decoder_conformance_coverage.rs"
change_id = "decoder-full-conformance-contract"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Adds an on-demand generated decoder-specific AV2 section-family coverage document and a self-contained drift/honesty gate. The gate validates committed coverage rows, support-row links, Feature IDs, diagnostic IDs, local-reference evidence pointers, and any committed render without invoking AVM, dav2d, ffmpeg, network access, or `splot decode`. It intentionally exposes unsupported and partial runtime decoder rows instead of treating this tooling as codec implementation proof."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "xtask/src/decoder_conformance_coverage.rs::tests",
]
commands = [
    "cargo test -p xtask decoder_conformance_coverage --locked",
    "cargo xtask decoder-conformance-coverage --format markdown --output docs/DECODER-SPEC-COVERAGE.md",
    "cargo xtask check-decoder-conformance-coverage",
    "cargo xtask ci",
]
fixtures = []
diagnostics = []

[[feature]]
id = "VALIDATOR-CONTEXT-SPLIT"
name = "Validator context module split"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-validate"
module = "crates/splot-validate/src/context"
change_id = "validator-context-split"
tracking_issue = ""
owner = "validator"
risk = "medium"
notes = "Refactors the private validator context state machine into cohesive modules without changing validation behavior, diagnostics, dependencies, or AV2 coverage."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo check -p splot-validate --all-targets --locked",
    "cargo test -p splot-validate --all-targets --locked",
    "cargo test -p xtask --all-targets --locked",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-MODULE-SIZE-REFACTOR"
name = "Oversized-module source-line refactor"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-validate"
module = "crates/splot-validate/src/celu"
change_id = "split-oversized-allowlisted-modules"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "Retires oversized-module source-line allowances by splitting allowlisted files along their real responsibility seams, sequenced by development activity (cold files first; the active-decoder-frontier files deferred with the deferral recorded in the allowance reason). Slice 1 (#519) moved the `celu.rs` in-file test module to `celu/tests.rs`, dropping the file 3693->1427 lines. Slice 2 (#520) moved the `sequence.rs` test + proptest modules to `sequence/tests.rs` + `sequence/proptests.rs`, dropping it 3282->2110 lines. Slice 3 moves the `frame/info.rs` test + proptest modules to `frame/info/tests.rs` + `frame/info/proptests.rs`, dropping it 5213->2357 lines (its header-parsing frontier had quieted — the active local decoder mission work is in splot-decode, not this splot-core header). Each removes its `xtask/src/source_lines.rs` allowance with no change to behavior, diagnostics, dependencies, or AV2 coverage (all relocated tests preserved). Slice 4 decomposes `sequence.rs`'s production code into value-seam submodules `sequence/{profile,layer_dependency,child_configs}.rs` (the `ProfileIdc` value model, the embedded/temporal layer-dependency maps, and the eight §5.4.x child-config structs + parsers), re-exporting every moved public item so no `crate::headers::sequence::…` call site changes; `sequence.rs` drops 2110->870 lines (each submodule under the 1000-line soft limit) with no behavior, diagnostic, API, or dependency change and no allowance to remove (already retired by Slice 2). Only `wienerns_lr/tx_records.rs` remains deferred under the `module-size-discipline` capability, while its local decoder mission frontier is active."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = []
commands = [
    "cargo test -p splot-validate --all-targets --locked",
    "cargo xtask check-source-lines",
    "cargo test -p xtask --all-targets --locked",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-STREAMING-TU-READER"
name = "Read-based temporal-unit demuxer"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-core"
module = "crates/splot-core/src/stream_reader.rs"
change_id = "streaming-validator-input"
tracking_issue = ""
owner = "core"
risk = "low"
notes = "Adds `TemporalUnitReader<R: Read>`: a forward-only (no `Seek`) container demuxer that frames an AV2 bitstream into one temporal unit at a time over a `std::io::Read` source, so consumers bound peak input memory to a single unit instead of the whole file. Annex B yields one OBU's bytes (`[leb128 length ++ OBU]`) per call at its absolute offset; IVF yields one frame payload per call at its payload offset; both reuse the existing slice parsers (`parse_ivf_header`, the leb128 reader, and `parse_annex_b_obus_partial_at` at the consumer) rather than reimplementing framing. Cross-read-boundary reassembly is correct down to one byte per `read`. A configurable per-unit byte cap (`DEFAULT_MAX_UNIT_BYTES` = 256 MiB) returns `ReaderError::UnitTooLarge` instead of allocating an over-cap unit; `splot-core` does NOT depend on `splot-decode`, so this mirrors decode's `max_input_bytes` guard without sharing a type across that forbidden edge. Stays in safe Rust (`unsafe_code = forbid`) via push-then-drop. Mechanism behind INFRA-VALIDATE-STREAMING-READER; reusable by future decode/inspect streaming."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/stream_reader.rs::tests",
    "crates/splot-core/src/stream_reader.rs::proptests",
]
commands = ["cargo test -p splot-core --lib stream_reader"]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-VALIDATE-STREAMING-READER"
name = "Streaming Read-based validator input"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-validate"
module = "crates/splot-validate/src/validator/streaming.rs"
change_id = "streaming-validator-input"
tracking_issue = ""
owner = "cli"
risk = "low"
notes = "Adds `Validator::validate_reader<R: Read>` (+ `_with_options`) and the streaming driver over `TemporalUnitReader` (INFRA-STREAMING-TU-READER). It drives the same per-OBU engine as the in-memory path (the shared `process_obu`: `observe_obu` + the check registry) and emits deferred diagnostics in the exact `validate_bytes` order (Annex B terminal error / IVF per-frame errors interleaved, then `finish()`, then IVF warnings, then the IVF container error), so the report is byte-identical to `validate_bytes` for the same bitstream. Equivalence is property-tested against `validate_bytes` over arbitrary raw and DKIF-prefixed input, plus a one-byte-at-a-time reader for reassembly. Truncated/malformed input stays a diagnostic report (never `Err`); `StreamValidateError` is reserved for genuine reader I/O failures and over-cap units (CLI exit 2). `splot validate <file>` now streams from a `BufReader<File>` and `-` reads stdin, bounding peak memory and adding stdin support (extends CLI-VALIDATE). `inspect` migration is deferred."

[feature.status]
mapped = "done"
types = "done"
parse = "not-applicable"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-validate/src/validator/streaming.rs::tests",
    "crates/splot-validate/src/validator/streaming.rs::tests::streamed_matches_in_memory",
    "crates/splot-cli/tests/cli.rs",
]
commands = [
    "cargo test -p splot-validate --lib streaming",
    "cargo test -p splot-cli --test cli",
    "cargo test -p splot-cli --test help_snapshots",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-DECODE-FILTER-HOT-PATHS"
name = "Decode filter hot-path optimization"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.18", "7.19", "7.20.2", "7.20.3", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters.rs"
change_id = "optimize-decode-filter-hot-paths"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Bit-exact first-frame latency work on the in-loop filter stage. The §7.20.2 source-sample selector is resolved once per restoration block into an LrSourceWindow (two x-extreme probes per row, since clipped x depends only on x and clipped y/source only on y), and the §7.20.3 luma filter, §7.20.4 PC-Wiener classification, chroma filter, and luma companion reads all consume the window directly; splot-recon gains a padded-source luma filter entry point (wiener_ns_filter_luma_block_padded, identical math, cross-checked bit-exact against the callback entry point) plus tap-table-tied read-reach constants WIENER_NS_LUMA_TAP_RADIUS / WIENER_NS_CHROMA_TAP_RADIUS / PC_WIENER_CLASSIFY_READ_RADIUS. CDEF captures plane snapshots by row slice, reduces the §7.18 CdefInside check to one precomputed plane rectangle, precomputes per-block signed tap offsets, hoists the per-strength constrain damping adjustment (constrain_with_adj proven equal to cdef_constrain by sweep test), and publishes each block with one validated write_rect; CCSO takes the same snapshot/batch shape. Block-independent filter stages (CDEF, CCSO, Wiener NS restoration blocks) run as data-parallel maps on the context's owned WorkerPool behind the new splot_parallel::on_multiworker_pool() guard with serial in-order publication, keeping output identical across --threads policies; direct callers (tests) take the serial path so Rayon's global pool is unreachable. An env-gated SPLOT_DECODE_TIMING stderr trace reports decode phase times. Motivating 1080p10 first-frame decode drops from 1240 ms to ~401 ms median (raw sha256 unchanged, full conformance-vector hash sweep unchanged)."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "done"

[feature.proof]
tests = [
    "crates/splot-recon/src/wienerns_filter.rs::tests::padded_and_callback_filters_match_bit_exactly",
    "crates/splot-recon/src/cdef_filter.rs::tests::constrain_with_adj_matches_cdef_constrain",
    "crates/splot-parallel/src/pool.rs::tests::on_multiworker_pool_tracks_install_scope_and_width",
]
commands = [
    "cargo test -p splot-recon --lib",
    "cargo test -p splot-decode --lib",
    "cargo test -p splot-parallel --lib",
    "cargo xtask check-concurrency-policy",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-DECODE-SERIAL-HOT-PATHS"
name = "Decode serial hot-path optimization"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.13.2.18", "7.13.3.18", "7.17", "7.18", "7.19", "7.20.1", "7.20.3", "7.20.5"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/timing.rs"
change_id = "optimize-decode-serial-hot-paths"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Bit-exact first-frame latency work on the serial decode spine, following INFRA-DECODE-FILTER-HOT-PATHS. The old diagnostic SPLOT_TRACE_* gates were later retired; SPLOT_DECODE_TIMING remains the supported timing surface, and the shared \u00a74.8 Round2/Clip3 helpers are #[inline]. The selectable-tx grid is a per-thread scratch reset from its own records instead of a ~2MB None-fill per coding block; active LR unit membership is derived from two per-axis matching scans with tile-constant geometry hoisted; LR source blocks coalesce while they are recorded into row runs, so runtime does not allocate thousands of 4x4 records only to sort and merge them before filtering; the Wiener NS luma kernel validates its padded window once then filters row slices; the chroma filter memoizes the luma downsample per coordinate; filter-stage plane snapshots are taken only for stages that run. CDEF gains a per-block interior kernel with hoisted tap constants over u16 snapshots; deblock gathers/scatters stack-array lines over direct plane slices with a per-pass adaptive-strength memo; CCSO precomputes a per-plane offset LUT over a borrowed source. The coefficient path drops the per-TU TransformCoeffBlockState clone, DC-context-line clones, preflight re-walks, and quant-buffer copies, and reuses per-thread residual scratch; the inverse transform selects its 1-D kernel once per pass, skips zero coefficients and all-zero rows (bit-exact: adding zero), and rounds via i64 shift algebra with a proven bound. The \u00a77.13.2.18 intra edge filter retains the two original look-behind samples while walking forward instead of allocating a full edge snapshot per call. Motion compensation borrows reference planes through a strided generic ReferencePlaneView instead of flattening each plane per block, and the \u00a77.13.3.18 convolution takes an unscaled-step contiguous-row fast path with the general path as fallback. Raw \u00a76.16.13 serialization and frame-hash digests batch per row. Both \u00a77.17 deblock passes band onto the owned pool behind the documented on_multiworker_pool() callee-side guard (pass 0 in disjoint 4-row bands, pass 1 in disjoint 4-column bands over row-slice contexts; MI-coverage guards fall back to the serial path), with a parallel-vs-serial equality test. Motivating 1080p10 first-frame decode drops from ~380 ms to ~170 ms median (raw sha256 unchanged); rewritten kernels carry old-vs-new equivalence tests. A later 13-pair record-time coalescing remeasurement on current main reduces decode-only median from 283.4 to 267.8 ms at one thread and from 122.0 to 110.3 ms at ten threads, with the decoded SHA-256 unchanged across 1/2/4/8/10 threads. Frame-level GDF computes independent four-row bands from immutable source snapshots on the owned pool, collects them in stable raster order, and publishes only after every band succeeds; the 30-frame 1080p10 hash stays bit-exact across 1/2/4/8/10 threads."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "done"

[feature.proof]
tests = [
    "crates/splot-recon/src/intra_directional_angle_tests.rs::intra_edge_filter_matches_owned_reference_for_all_sizes_and_strengths",
    "crates/splot-recon/src/subpel_mc/tests.rs::edge_positions_match_independent_reference",
    "crates/splot-recon/src/subpel_mc/tests.rs::strided_view_matches_contiguous_view",
    "crates/splot-recon/src/subpel_mc/tests.rs::zero_phase_copy_matches_independent_reference",
    "crates/splot-recon/src/inverse_transform.rs::tests::kernel_transform_matches_per_element_reference",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_grid_tests.rs::with_selectable_tx_grid_reuses_scratch_without_cross_block_state",
    "crates/splot-decode/src/bitstream/tile_payload/partition_traversal_tests.rs::current_cursor_retains_active_frame_level_wiener_ns_source_blocks",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::merges_contiguous_row_blocks_and_splits_on_filter_visible_fields",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::merges_compatible_adjacent_rows_into_rectangles",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::does_not_merge_rows_across_source_boundaries",
    "crates/splot-recon/src/pc_wiener.rs::tests::grid_classification_matches_scalar_cells_and_reuses_features",
    "crates/splot-decode/src/filters/deblock.rs::tests::banded_parallel_pass_matches_serial_output",
    "crates/splot-decode/src/pipeline/frame_engine/intra_tests.rs::frame_gdf_is_bit_exact_across_worker_counts",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::coalesces_all_planes_in_place_and_returns_ordered_partitions",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters_tests.rs::predeblocked_filter_tail_matches_the_combined_path",
]
commands = [
    "cargo test -p splot-core --lib",
    "cargo test -p splot-recon --lib",
    "cargo test -p splot-decode --lib",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-DECODE-FIRST-FRAME-LATENCY"
name = "Decode first-frame latency optimization"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.20.3", "7.20.4"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md",
]
crate = "splot-recon"
module = "crates/splot-recon/src/wienerns_filter.rs"
change_id = "optimize-decode-first-frame-latency"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Measured follow-up to INFRA-DECODE-SERIAL-HOT-PATHS for the 1080p10 first-frame decode frontier. Warmed baseline on the motivating IVF was 147.319 ms raw/default, 300.236 ms raw --threads 1, 135.324 ms hash/default, and 289.633 ms hash --threads 1; SPLOT_DECODE_TIMING attributed about 132 ms to runtime decode while input read, context construction, planning, and raw serialization were sub-2 ms each. This slice removes the luma Wiener NS in-range row segment Vec collection and adds hot PC-Wiener/Luma Wiener inline hints while preserving bit-exact output, fail-atomic helper behavior, owned-pool determinism, and no AV2 semantics changes. Final warmed medians were 144.664 ms raw/default, 284.383 ms raw --threads 1, 134.578 ms hash/default, and 276.009 ms hash --threads 1, so perf remains partial and the remaining hotspot is runtime reconstruction/final filtering rather than input/planning."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "partial"
tests = "done"
avm_diff = "not-applicable"
perf = "partial"

[feature.proof]
tests = [
    "crates/splot-recon/src/wienerns_filter.rs::tests::padded_and_callback_filters_match_bit_exactly",
    "crates/splot-recon/src/wienerns_filter.rs::tests::padded_and_callback_filters_match_bit_exactly_eight_bit_wide_block",
    "crates/splot-recon/src/pc_wiener.rs::tests",
]
commands = [
    "cargo test -p splot-recon wiener --locked",
    "cargo build --release --locked",
    "SPLOT_DECODE_TIMING=1 ./target/release/splot decode --quiet --output-format raw --limit=1 -o /tmp/splot-timing-1.raw $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "SPLOT_DECODE_TIMING=1 ./target/release/splot decode --quiet --output-format hash --limit=1 $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = []
diagnostics = []

[[feature]]
id = "INFRA-DECODE-PARALLEL-STAGES"
name = "Decode filter-stage worker-pool parallelization"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.17", "7.18", "7.19", "7.20.3"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/recon.rs"
change_id = "parallelize-decode-hot-stages"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Measured `--threads` scaling follow-up to INFRA-DECODE-FIRST-FRAME-LATENCY. The current filter pipeline runs independent restoration stripes as ready tasks on the context-owned WorkerPool. Each task carries one stripe through CDEF, CCSO, loop restoration, and GDF from immutable frame views, then ordered publication writes the validated stripe results to the frame. When a plane has no active restoration unit, the task moves its CDEF stripe directly to publication; GDF updates that owned luma stripe in place instead of forcing another stripe copy. CurrentFrameWorkspace can also partition disjoint rectangular Y/U/V surfaces into exclusive per-row slices, allowing column tiles to parse and precompute ReferenceOnly reconstruction concurrently without unsafe code or pixel copies. Completed tiles are ordered by tile number before dependent reconstruction, publication, temporal-motion commits, and tile-region merges of CDEF, GDF, and CCSO state. Global IntrABC retains the serial path. The row prepass schedules every ReferenceOnly entry whose write footprint stays inside its surface, including entries after a CurrentFrame dependency in the same superblock. Exact decode hashes match across one and ten threads on the canonical 30-frame performance stream and across one and four threads on the pinned multi-tile AVM fixtures."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "partial"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/cdef.rs::tests::stripe_frames_match_full_frame_across_restoration_boundaries",
    "crates/splot-decode/src/filters/ccso.rs::tests::stripe_outputs_match_full_frame_across_restoration_boundaries",
    "crates/splot-decode/src/filters/gdf.rs::tests::stripe_tasks_follow_restoration_boundaries_and_clip_to_tiles",
    "crates/splot-decode/src/filters/gdf.rs::tests::stripe_source_matches_per_block_windows_at_halo_boundaries",
    "crates/splot-decode/src/filters/wienerns_lr/recon/final_filters.rs::tests::inactive_filter_planes_reuse_cdef_storage",
    "crates/splot-decode/src/filters/deblock.rs::tests::banded_parallel_pass_matches_serial_output",
    "crates/splot-decode/src/prediction/inter/block/tile.rs::ready_row_tests::mixed_superblock_prepass_selects_every_independent_entry",
    "crates/splot-recon/src/workspace_tests.rs::rectangular_surfaces_write_adjacent_column_tiles_independently",
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_tile_inter_fixture_decodes_bit_exact",
    "crates/splot-decode/src/prediction/inter/tests.rs::multi_tile_lr_fixture_decodes_bit_exact",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records_cdef_tests.rs::cdef_tile_merge_copies_only_the_owned_region",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records/gdf_tests.rs::gdf_tile_merge_copies_only_the_owned_region",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records/ccso_tests.rs::ccso_tile_merge_copies_only_the_owned_region",
]
commands = [
    "cargo test -p splot-parallel --locked",
    "cargo test -p splot-recon --locked",
    "cargo test -p splot-decode --locked",
    "cargo build --release --locked",
    "SPLOT_DECODE_TIMING=1 ./target/release/splot decode --quiet --threads 10 --output-format raw --limit=1 -o /tmp/splot-t10.raw $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "./target/release/splot decode --threads 1 --output-format hash --limit=30 $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "./target/release/splot decode --threads 10 --output-format hash --limit=30 $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = []
diagnostics = []

[[feature]]
id = "DECODE-INTER-DEBLOCK-SUBPU-METADATA"
name = "Inter deblock sub-PU and inherited chroma metadata"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.4.1", "7.13.3.1", "7.13.3.14", "7.13.3.15", "7.17.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-4-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-15",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-17-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/block/filter_records.rs"
change_id = "fix-inter-deblock-subpu-metadata"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Preserves the separate luma and chroma prediction-unit bases, default sub-PU transform sizes, explicit sub-PU geometry, and chroma transform-unit overlays consumed by the section 7.17.2 deblocking edge process. A sub-8 luma leaf no longer replaces the larger ChromaMiSize / MiRowBase[1] / MiColBase[1] state saved by section 5.20.4.1 and section 7.13.3.15: inherited chroma metadata covers the chroma-reference geometry, while actual U/V transform records replace only transform-base and DeblockingTxSizes cells. Ordinary U/V residual-transform records retain the parent luma/chroma prediction bases and sub-PU geometry instead of resetting them at each transform. Explicit sub-PU geometry is rectangular: RefineMV records min(blockWidth,16) by min(blockHeight,16), including 8x16 and 16x8 cases, while the later OPFL subdivision takes precedence when both tools run. Pass-specific deblocking uses width for the horizontal pass and height for the vertical pass after plane subsampling. Frame-level allow_df_sub_pu is threaded into the edge gate instead of being inferred from whether the current record happens to carry a sub-PU size. Together these handoffs make the target stream's hidden order-hint-4 final SHA-256 byte-exact to pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774 at ed9720a741451bda28eb91e904c1756797913977fdb3a8a7358d37a32eebfb08. The committed 10-bit 4:2:0 three-frame fixture contains MI (0,3) BLOCK_4X8 inheriting MI (0,0) BLOCK_16X8 with all deblocking passes active and frame-2 allow_df_sub_pu=true; pre-fix output differed from AVM in eight U samples and the fixed decoder matches pinned AVM and independent dav2d byte-for-byte. The fixture's one-bit header normalization makes allow_df_sub_pu=true explicit but is not output-discriminating versus its stock AVM source; focused tests and the live decoder stream discriminate the allow gate, rectangular RefineMV/OPFL geometry, and ordinary chroma metadata handoff."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "todo"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/deblock_tests.rs::inherited_chroma_residual_transform_retains_prediction_metadata",
    "crates/splot-decode/src/filters/deblock_tests.rs::ordinary_chroma_overlay_replaces_full_block_metadata",
    "crates/splot-decode/src/filters/deblock_tests.rs::allow_df_sub_pu_gates_prediction_boundary_filtering",
    "crates/splot-decode/src/filters/deblock_tests.rs::explicit_sub_pu_dimensions_follow_pass_and_chroma_subsampling",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::refinemv_deblock_subpu_geometry_preserves_rectangular_blocks",
    "crates/splot-decode/src/prediction/inter/block/compound_path.rs::tests::optflow_deblock_subpu_geometry_takes_precedence_over_refinemv",
    "crates/splot-decode/src/prediction/inter/block/filter_records.rs::tests::ordinary_chroma_residual_retains_parent_prediction_and_rectangular_subpu",
    "crates/splot-decode/src/prediction/inter/block/filter_records.rs::tests::skipped_64x128_420_chroma_keeps_one_transform_across_luma_chunks",
    "crates/splot-core/src/write/frame_filters_tests.rs::deblocking_allow_df_sub_pu_rejected_before_write",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode inherited_chroma_residual_transform_retains_prediction_metadata --locked",
    "cargo test -p splot-decode allow_df_sub_pu_gates_prediction_boundary_filtering --locked",
    "cargo test -p splot-decode deblock_subpu --locked",
    "cargo test -p splot-decode ordinary_chroma_residual_retains_parent_prediction_and_rectangular_subpu --locked",
    "cargo test -p splot-decode skipped_64x128_420_chroma_keeps_one_transform_across_luma_chunks --locked",
    "cargo test -p splot-core deblocking_allow_df_sub_pu_rejected_before_write --locked",
    "cargo xtask decoder-fixtures verify",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-3frame-deblock-subpu-chroma-64x32-10bit-q90.ivf",
]
diagnostics = []

[[feature]]
id = "DECODE-GDF-PER-BLOCK"
name = "Per-block guided detail filter selection"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["5.18.7.9", "5.20.10.3", "6.19.9.3", "7.20.5", "8.3.2"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-7-9",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-10-3",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-19-9-3",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-5",
    "docs/spec/av2/1.0.0/08-parsing-process.md#s-8-3-2",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/wienerns_lr/tx_records/gdf.rs"
change_id = "decode-gdf-per-block"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Implements the complete per-block GDF selection handoff for the supported decoder runtime. The shared frame-header helper derives GdfBlkSize exactly as §5.18.7.9, including the sequence superblock override and 64x64 tile-start scan. The tile walk reads TileUseGdfCdf once at each aligned superblock/GDF-unit origin in the §5.20.10.3 order before CDEF, retains a validated dense GdfBlks grid across intra and inter block paths, and fails closed if any in-frame unit was not visited. The §7.20.5 filter selects each four-row output band with stripeRow = Min(MiRowEnd - 1, ((row + 2) >> 4) << 4), filters enabled units, and copies the deblocked/CDEF-stage base samples for disabled units before the existing lossless preservation step. The 270-byte AVMenc fixture codes four 64x64 units with use_gdf 1,1,0,1; fixed splot, pinned avmdec revision 457cd58681a747465661baccb1f32095bc5b7774, and current avmdec revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce the same 24,576 raw bytes with SHA-256 13af253aff07dbbdcd9cd9e904597376e5cdca89e4be02d6900419ad9dbaf599. Multi-tile source-boundary handling is implemented independently by DECODE-GDF-MULTI-TILE."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/wienerns_lr/tx_records/gdf_tests.rs::use_gdf_symbols_round_trip",
    "crates/splot-decode/src/filters/wienerns_lr/tx_records/gdf_tests.rs::unvisited_gdf_unit_fails_closed",
    "crates/splot-decode/src/filters/gdf.rs::tests::per_block_grid_selects_units_by_restoration_stripe_row",
    "crates/splot-decode/src/filters/gdf.rs::tests::per_block_grid_rejects_invalid_shape_and_values",
    "crates/splot-decode/src/filters/gdf.rs::tests::disabled_gdf_unit_copies_base_samples",
    "crates/splot-decode/src/output/hash_tests.rs::per_block_gdf_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::per_block_gdf_fixture_eof_fails_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode gdf --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-gdf-per-block-intra-128x128-q120.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/unsupported-feature"]

[[feature]]
id = "DECODE-GDF-MULTI-TILE"
name = "Guided detail filter across multiple tiles"
category = "infrastructure"
kind = "bitstream-semantics"
spec_sections = ["6.4.10", "7.20.1", "7.20.2", "7.20.5"]
sources = [
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-4-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-20-5",
]
crate = "splot-decode"
module = "crates/splot-decode/src/filters/gdf.rs"
change_id = "decode-gdf-multi-tile"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Implements GDF over arbitrary valid tile layouts for both cross-tile filtering modes. The runtime derives each containing tile from MiColStarts and MiRowStarts, restarts §7.20.1 restoration stripes relative to MiRowStart, clamps stripeRow to MiRowEnd - 1, and materializes each tile-column segment from immutable current/CDEF source planes. With disable_loopfilters_across_tiles equal to 1, §6.4.10 and §7.20.2 restrict source samples to the containing tile; otherwise the source bounds are the full frame. The 276-byte AVMenc fixture uses a 2-by-2 tile layout and disabled cross-tile filtering. Fixed splot, pinned avmdec revision 457cd58681a747465661baccb1f32095bc5b7774, and current avmdec revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce the same 24,576 raw bytes with SHA-256 3d74979b2e766b285c535c6ff68078b8175f8b835d6f8a4380f215cc19874107. A separate local two-tile cross-filter-enabled encode also matches current avmdec byte-for-byte at SHA-256 cdecee26844d99164a2e34da3a2f0fa4ea8d41c4a98a9d789b662195ab38e9."

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/filters/gdf.rs::tests::stripe_row_switches_before_the_next_sixty_four_pixel_band",
    "crates/splot-decode/src/filters/gdf.rs::tests::tile_axis_bounds_select_the_containing_tile",
    "crates/splot-decode/src/output/hash_tests.rs::multi_tile_gdf_fixture_matches_reference_output_hash",
    "crates/splot-decode/src/output/hash_tests.rs::multi_tile_gdf_fixture_eof_fails_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode multi_tile_gdf --locked",
    "cargo test -p splot-decode filters::gdf --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-4tile-gdf-intra-128x128-q120.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = []

[[feature]]
id = "DECODE-INTER-REFERENCE-SCALING"
name = "Inter reference-size scaling"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.20.7.12", "5.20.7.14", "5.20.7.17", "7.13.3.14", "7.13.3.17", "7.13.3.18", "7.13.3.20"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-12",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-14",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-20-7-17",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-14",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-17",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-18",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-20",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter/mv_scaling.rs"
change_id = "decode-inter-reference-scaling"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Removes the blanket inter_reference_resolution_mismatch rejection and implements the complete reference-size-dependent prediction plumbing. The §7.13.3.17 derivation computes independent rounded xScale/yScale, startX/startY, stepX/stepY, and per-plane sample bounds from retained-reference and current-frame dimensions; the same scale template is preserved when ordinary, refine-MV, and optical-flow motion vectors replace the starting MV. Single and compound sub-pel prediction use the scaled coordinates, scaled references disable the §7.13.3.16 implicit mask path, and the §7.13.3.14 skipPred rule routes scaled local-warp prediction through the scaled §7.13.3.20 extended-warp convolution. §5.20.7.12 BAWP eligibility excludes a scaled selected reference. TIP keeps the §7.13.3.1 base refine-MV invocation intact, gates only refine-MV search with the equal-distance unscaled-reference rule, and applies the distinct opposite-direction unscaled-reference rule to optical flow. The committed 111-byte syn-2frame-refscale-inter-64x64-51x51-q80.ivf is a validator-clean current-AVMenc stream: a retained 64x64 key frame is referenced by a 51x51 inter frame. It first hit inter_reference_resolution_mismatch at byte 102 before this change. Fixed splot, pinned AVM revision 457cd58681a747465661baccb1f32095bc5b7774, and current AVM revision 3935f4bf31ee774177d9300341bbbaf5b6116500 produce identical complete 10,097-byte raw I420 output SHA-256 4ca2e77c74dc68920ce0ba333e5a984b65836ac6f07e5d2d89e1f715e93ea590. The implementation is generic across both axes, chroma subsampling, up/down scaling, compound/refine/optflow motion replacement, and scaled extended warp; unsupported prediction tools remain governed by their independent support rows."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/output/hash_tests.rs::reference_scaling_fixture_matches_reference_output_hashes",
    "crates/splot-decode/src/output/hash_tests.rs::reference_scaling_fixture_eof_fails_closed",
    "crates/splot-decode/src/prediction/inter/mv_scaling_tests.rs::scaled_reference_uses_spec_ratio_and_independent_axis_steps",
    "crates/splot-decode/src/prediction/inter/mv_scaling_tests.rs::reference_scale_detection_uses_rounded_spec_scale",
    "crates/splot-decode/src/prediction/inter/mc_tests.rs::scaled_compound_references_disable_implicit_mask_blending",
    "crates/splot-decode/src/prediction/inter/block/tip.rs::tests::tip_optflow_accepts_unequal_opposite_direction_references",
    "crates/splot-recon/src/warp_prediction.rs::tests::scaled_ext_warp_identity_samples_the_reference_at_scaled_steps",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode reference_scaling --locked",
    "cargo test -p splot-decode mv_scaling --locked",
    "cargo test -p splot-decode scaled_compound_references_disable_implicit_mask_blending --locked",
    "cargo test -p splot-recon scaled_ext_warp_identity_samples_the_reference_at_scaled_steps --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-refscale-inter-64x64-51x51-q80.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/malformed-source"]

[[feature]]
id = "DECODE-OUTPUT-EFFECT-OBUS"
name = "Decoded output-effect OBU state"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.7", "5.12", "5.13", "5.15", "5.17", "6.7", "6.11", "6.12", "6.14", "6.16", "7.3.3", "7.3.4", "7.3.8.7", "7.3.8.9", "7.3.8.10", "7.3.8.11", "7.14.4"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-7",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-12",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-13",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-15",
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-17",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-7",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-11",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-12",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-14",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-16",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-7",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-9",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-10",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-3-8-11",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-14-4",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/output_effects.rs"
change_id = "decode-output-effect-obus"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Implements the selected-layer output-effect semantics used by the current decoder for multi-frame-header, buffer-removal-timing, content-interpretation, metadata-short, metadata-group, and quantizer-matrix OBUs. Stream planning retains each OBU as selected-layer state; the scheduler enforces §7.3.3/§7.3.4 prefix and suffix placement; per-temporal-unit and per-coded-video-sequence state applies the §7.3.8 lifetimes and identity constraints; multi-frame headers select protected reusable slots; BRT resolves the signaled operating-point set and preserves every BRT in source order. Coded output frame units admit any BRT count under §7.3.3, while a second BRT in a coded non-output frame unit is a typed malformed frame-unit conformance error under §7.3.4 after output classification and before frame effects. Metadata observes persistence, cancellation, frame association, and applicability to the selected base embedded-layer output; user-defined quantizer matrices feed §7.14.4 dequantization only for transform dimensions no greater than 8 by 8. Direct output-effect state tests protect metadata and BRT transitions, and seven minimal AVMenc-derived fixtures validate clean and decode byte-for-byte. The multi-BRT fixture's native 256-byte monochrome output is SHA-256 5a5f307aa9ce504d9235634f15cf382e8914c49fbd8dd4d4c47136c917886f7b in pinned AVM and splot at one and eight threads; AVM's separate forced-I420 projection is 384 bytes with SHA-256 f83545d43c6939ec393b6b8310959b6174fd764b08a12fc22d908408a7e6a43e."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "done"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-decode/src/bitstream/stream_plan/tests.rs::output_effect_obus_are_selected_layer_state",
    "crates/splot-decode/src/output/hash_tests.rs::output_effect_single_frame_fixtures_match_reference_hashes",
    "crates/splot-decode/src/output/hash_tests.rs::content_interpretation_fixture_decodes_both_frames",
    "crates/splot-decode/src/bitstream/tile_payload/general_intra_residual/tests.rs::resolve_block_qm_limits_user_matrix_to_eight_by_eight",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::no_persistence_metadata_expires_after_frame_snapshot",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::global_metadata_overwrites_global_and_survives_cancel",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::basic_and_enhanced_metadata_replace_same_scope_and_cancel",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::metadata_snapshot_applies_only_to_selected_base_layer",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::coded_video_sequence_start_prunes_metadata_from_earlier_temporal_units",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::multiple_brts_follow_output_classification_and_drain_in_source_order",
    "crates/splot-decode/src/pipeline/output_effects.rs::tests::malformed_brt_payloads_are_typed_and_fail_closed",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-decode output_effect --locked",
    "cargo test -p splot-decode content_interpretation_fixture_decodes_both_frames --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask decoder-fixtures verify",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-output-mfh-64x64.ivf",
    "tests/conformance/vectors/valid/syn-output-brt-64x64.ivf",
    "tests/conformance/vectors/valid/syn-output-multi-brt-16x16.ivf",
    "tests/conformance/vectors/valid/syn-output-ci-2frame-64x64.ivf",
    "tests/conformance/vectors/valid/syn-output-metadata-group-64x64.ivf",
    "tests/conformance/vectors/valid/syn-output-metadata-short-prefix-suffix-64x64.ivf",
    "tests/conformance/vectors/valid/syn-output-user-qm-64x64.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/malformed-source", "decode/unsupported-feature"]

[[feature]]
id = "DECODE-INTER-GLOBAL-MOTION"
name = "Frame global-motion inter runtime"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["5.18.9", "6.17.9", "7.12.2.1", "7.12.2.2", "7.13.3.1", "7.13.3.19", "7.20", "7.23"]
sources = [
    "docs/spec/av2/1.0.0/05-syntax-structures.md#s-5-18-9",
    "docs/spec/av2/1.0.0/06-syntax-structures-semantics.md#s-6-17-9",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-12-2-2",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-1",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-13-3-19",
    "docs/spec/av2/1.0.0/07-decoding-process.md#s-7-23",
]
crate = "splot-decode"
module = "crates/splot-decode/src/prediction/inter.rs"
change_id = "decode-inter-global-motion"
tracking_issue = ""
owner = "decoder"
risk = "high"
notes = "Removes the inter_use_global_motion fail-closed gate by implementing the complete frame-global runtime path. Frame parsing receives the current and saved §7.23 reference state, decodes every IDENTITY/ROTZOOM/AFFINE model, and retains NumTotalRefs, order hints, and global parameters on refresh. The §7.12.2.1 block-centre projection supplies the GLOBALMV motion vector and global fallback candidate for single and compound stacks. GLOBALMV/GLOBAL_GLOBALMV blocks attach their reference-specific non-identity model when the §7.13.3.1 eligibility conditions hold; ordinary warped reconstruction then executes through the shared §7.13.3.19 kernel, and neighbour/temporal warp state is retained. Three 64x64 AVMenc fixtures select GLOBALMV with translation-only ROTZOOM, genuine ROTZOOM, and AFFINE models. Their exact parsed parameters are asserted, every stream validates clean, and Splot matches both an isolated unmodified AVM decoder and the current local avmdec byte-for-byte (36,864 concatenated bytes, SHA-256 fcafae3a402b53ac3a550dafaf8417a929be7929cb479700389e808685341b1a)."

[feature.status]
mapped = "done"
types = "done"
parse = "done"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "done"
perf = "not-applicable"

[feature.proof]
tests = [
    "crates/splot-core/src/headers/frame/global_motion.rs::tests",
    "crates/splot-decode/src/reference/buffer_tests.rs::reference_refresh_preserves_global_motion_predictor_state",
    "crates/splot-decode/src/prediction/inter/tests.rs::global_motion_fixtures_match_reference_hashes",
    "crates/splot-cli/tests/decoder_oracle.rs::decoder_oracle_corpus_matches_manifest",
]
commands = [
    "cargo test -p splot-core global_motion --locked",
    "cargo test -p splot-decode global_motion --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo xtask conformance",
    "cargo xtask check-reference-evidence",
    "cargo xtask check-decoder-support",
    "cargo xtask check-feature-status",
]
fixtures = [
    "tests/conformance/vectors/valid/syn-2frame-global-translation-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-2frame-global-rotzoom-64x64-q80.ivf",
    "tests/conformance/vectors/valid/syn-2frame-global-affine-64x64-q80.ivf",
    "docs/LOCAL-REFERENCE-EVIDENCE.toml",
]
diagnostics = ["decode/malformed-source", "decode/unsupported-feature"]

[[feature]]
id = "PERF-X86-64-V3-BASELINE"
name = "x86-64-v3 (AVX2) compile-time CPU baseline for decode throughput"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-cli"
module = ".cargo/config.toml"
change_id = "perf-x86-64-v3-baseline"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Raises the x86-64 codegen floor to the `x86-64-v3` microarchitecture level (AVX2/FMA/BMI2, Intel Haswell 2013+ / AMD Excavator 2015+) via `.cargo/config.toml`, so the already auto-vectorizing loop-filter and prediction kernels widen from 128-bit SSE2 to 256-bit AVX2. Measured ~15% (-14.9%) faster whole-stream decode on the 1080p reference stream with byte-identical output. aarch64 is left at its default because 128-bit NEON is its mandatory baseline. Release binaries now require an AVX2 x86-64 CPU; the README Quick Start documents the `-C target-cpu=x86-64` portable-build override. First stage of the safe SIMD-acceleration program; the `perf` stage stays `pending` until the `cargo xtask perf` regression gate lands."
replaces = []

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "done"
perf = "pending"

[feature.proof]
tests = [
    "crates/splot-cli/tests/decoder_oracle.rs",
    "crates/splot-decode/tests/sequence_transition.rs",
]
commands = [
    "cargo build --release -p splot-cli --locked",
    "cargo test -p splot-cli --test decoder_oracle --locked",
    "cargo test -p splot-decode --test sequence_transition --locked",
]
fixtures = [
    ".cargo/config.toml",
    "README.md",
]
diagnostics = []

[[feature]]
id = "PERF-REGRESSION-GATE"
name = "Deterministic decode instruction-count (callgrind Ir) regression gate"
category = "infrastructure"
kind = "infrastructure"
spec_sections = []
sources = []
crate = "splot-cli"
module = "xtask/src/perf.rs"
change_id = "perf-regression-gate"
tracking_issue = ""
owner = "automation"
risk = "low"
notes = "`cargo xtask perf` decodes a curated subset of committed conformance vectors under `valgrind --tool=callgrind --cache-sim=no --branch-sim=no`, single-threaded, and compares each fixture's total retired-instruction count against `tests/perf/baseline.toml`. Callgrind Ir is deterministic for a fixed binary, so it is a runner-independent proxy for decode CPU work (unlike wall-clock). Fails on a >0.5% regression, reports improvements, and rewrites the baseline with `--bless`. Runs as its own `perf` CI job (installs valgrind); intentionally NOT part of `cargo xtask ci` because valgrind is a ~20-30x slowdown, mirroring how fuzz/coverage/audit live outside the fast local gate. Skips with a hint when valgrind is absent so a fresh checkout is unblocked. Fixtures are chosen to exercise the hot loop-filter/prediction kernels (gdf, wienerns-luma, pc-wiener, deblock/cdef, inter subpel/optflow, warp/subpel), giving the upcoming SIMD-kernel ports a measured Ir number per PR."
replaces = []

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "not-applicable"
tests = "done"
avm_diff = "not-applicable"
perf = "done"

[feature.proof]
tests = [
    "xtask/src/perf.rs",
    "tests/perf/baseline.toml",
]
commands = [
    "cargo xtask perf",
    "cargo xtask perf --bless",
]
fixtures = [
    "tests/perf/baseline.toml",
    "tests/conformance/vectors/valid/syn-gdf-intra-128x128.ivf",
    "tests/conformance/vectors/valid/syn-warp-inter-128x128.ivf",
    ".github/workflows/ci.yml",
]
diagnostics = []

[[feature]]
id = "INFRA-DECODE-FRAME-PIPELINING"
name = "Decode frame-delay finish-phase pipelining"
category = "infrastructure"
kind = "infrastructure"
spec_sections = ["7.2"]
sources = [
    "docs/spec/av2/1.0.0/07-decoding-process.md",
]
crate = "splot-decode"
module = "crates/splot-decode/src/pipeline/inflight.rs"
change_id = "decode-frame-pipelining"
tracking_issue = ""
owner = "decoder"
risk = "medium"
notes = "Frame-delay-deep pipelining of the per-frame finish phases on top of INFRA-DECODE-PARALLEL-STAGES. The plan driver moves frames through a bounded in-flight ring: each walked frame's shared § 7.2 in-loop filter chain and frame freeze run as one worker-pool task while the driver walks the next frame. Reference gating never blocks a worker: row parsing proceeds while referenced frames are still filtering, and a frame's precompute/commit work is enqueued only once every pixel-reference slot has settled (deferred rows flush in parse order; TIP/bridge synthesis, the serial prepass, and the multi-tile path take a blocking driver gate before their pixel reads). Driver waits pool-assist by running installed-pool jobs instead of parking. The depth resolves once per DecodeContext from DecodeRuntimeConfig::frame_delay: `auto` (the default, exposed as `splot decode --frame-delay`) is the resolved worker-thread count, a fixed depth is clamped to that count, and a resolved depth of one or an off-pool run takes the inline serial path unchanged. Emission, show-existing-frame handling, the output-frame limit, and EOF all harvest every outstanding finish, and failures surface lowest-decode-index-first, so a pipelined run reproduces the serial decode's diagnostic. Bit-exact: decoded-frame hash digests are identical across `--threads` 1/2/4/10 and across `--frame-delay` 1/2/4/auto on the canonical 30-frame 1080p10 performance stream, and the full 237-vector conformance corpus decodes byte-identical against the serial path at five thread-and-delay configurations. Paired interleaved medians on that stream (seven rounds, M2 Max): ten threads 0.43 s at `--frame-delay 1` versus 0.40 s at auto, four threads 0.65 s versus 0.63 s, single-thread serial 1.11 s unchanged; per-frame instrumentation attributes the remaining wall to the serial walk chain (parse 5.0 ms, ordered-commit chain 3.7 ms per frame) and the finish latency (7.8 ms). Measured no-go analyses (recorded in the change PR): relocating the commit chain into the finish phase trades walk time for reference latency roughly one-to-one because 91 percent of frames consume the immediately preceding frame's temporal motion field, so further scaling needs row-granular reference progress or intra-in-inter wavefront reconstruction rather than deeper frame pipelining; `perf` stays partial while those follow-ups are open."
replaces = []

[feature.status]
mapped = "done"
types = "not-applicable"
parse = "not-applicable"
validate = "not-applicable"
write = "not-applicable"
encode = "not-applicable"
decode_check = "done"
tests = "done"
avm_diff = "not-applicable"
perf = "partial"

[feature.proof]
tests = [
    "crates/splot-decode/tests/frame_pipelining.rs",
    "crates/splot-decode/src/prediction/inter/block/row_gate_tests.rs::a_tip_batch_waits_for_its_first_candidate_over_the_whole_rectangle",
    "crates/splot-cli/tests/decode_cli.rs::decode_hash_json_success_hashes_are_frame_delay_deterministic",
    "crates/splot-cli/tests/decode_raw_cli.rs::decode_raw_outputs_are_frame_delay_deterministic",
    "crates/splot-cli/tests/decode_y4m_cli.rs::decode_y4m_outputs_are_frame_delay_deterministic",
]
commands = [
    "cargo test -p splot-decode --test frame_pipelining --locked",
    "cargo test -p splot-cli --locked",
    "cargo build --release --locked",
    "./target/release/splot decode --threads 4 --frame-delay 1 --output-format hash --limit=30 $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "./target/release/splot decode --threads 4 --frame-delay 4 --output-format hash --limit=30 $SPLOT_LOCAL_DECODER_MISSION_IVF",
    "./target/release/splot decode --threads 10 --frame-delay auto --output-format hash --limit=30 $SPLOT_LOCAL_DECODER_MISSION_IVF",
]
fixtures = []
diagnostics = []
