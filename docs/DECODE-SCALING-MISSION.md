# Decode Scaling Mission

Feature IDs: `INFRA-DECODE-PARALLEL-STAGES`,
`INFRA-DECODE-FRAME-PIPELINING`

The live implementation checklist is
[`DECODE-SCALING-OPEN-TASKS.md`](DECODE-SCALING-OPEN-TASKS.md). Add discoveries
there immediately; keep measurements and completed or rejected evidence here.

## Goal

On `/Users/bartosztomczyk/Documents/SplotLabs/test.ivf`, decode 30 frames at
1, 2, 4, 8, and 10 threads faster than dav2d, with the 10-thread splot wall
time at most 50% of dav2d's and no 1-thread regression.

Retention policy (maintainer, 2026-07-31): individual candidates are no longer
gated at 10%. A candidate is retained when its direction is consistently
positive across alternating pairs, 1 thread does not regress, 2/4 threads do
not materially regress, output is byte-exact, and complexity is proportionate.
Retained wins stack; each survivor is re-measured independently and in
combination after rebases and dropped only on neutral or negative
confirmation. Architectural barrier removals and scalar kernel wins are both
in scope, including safe `std::simd` vectorization; `unsafe_code = forbid`
stands, and third-party decoders remain engineering inspiration only.

## Benchmark contract

Splot decoder-only timing:

```sh
SPLOT_DECODE_DISCARD_HASH=1 ./target/release/splot decode \
  --quiet --output-format hash --limit=30 \
  /Users/bartosztomczyk/Documents/SplotLabs/test.ivf \
  --threads=10 --frame-delay=10
```

Dav2d timing:

```sh
/Users/bartosztomczyk/Devel/dav2d/build/tools/dav2d \
  --demuxer ivf \
  -i /Users/bartosztomczyk/Documents/SplotLabs/test.ivf \
  -o /dev/null --threads 10 -l 30 --framedelay 10
```

Use rebuilt release binaries, warmups, alternating paired samples, and an idle
machine. Prove decoded output separately from timing. A deliverable candidate
must pass exact output comparison, the required corpus/oracle checks,
`cargo xtask check-duplication`, `cargo xtask ci`, and the 1/2/4/8/10 paired
timing matrix.

## Confirmed baseline

Measured 2026-07-31 on exact `5d78ba974` (pre-SCALE-013 base):

| Threads | splot wall | dav2d wall | splot cores busy | dav2d cores busy |
|---:|---:|---:|---:|---:|
| 1 | 1.04 s | 1.08 s | 1.00 | 1.00 |
| 2 | 0.80 s | 0.56 s | 1.33 | 2.00 |
| 4 | 0.33 s | 0.29 s | 3.50 | 3.90 |
| 8 | 0.24 s | 0.16 s | — | — |
| 10 | 0.24 s | 0.155 s | 5.6 | 8.6 |

Splot's total CPU time is at or below dav2d's at every thread count (2T: 1.05 s
versus 1.10 s), so the remaining gap is parallel occupancy plus per-kernel DSP,
not excess scalar work. Steady state is worse than 30-frame walls suggest: the
30/60/120-frame sweep gives splot 7.8/7.5/6.4 ms per frame versus dav2d
5.0/4.3/3.6. After SCALE-013 the 10-thread wall is 0.2247 s.

## Delivery discipline

Only one implementation candidate is active at a time. For each candidate:

1. Start from the current `origin/main`.
2. State the bottleneck, evidence, expected ceiling, and acceptance threshold.
3. Implement the smallest architecture change that tests the hypothesis.
4. Check exact output before timing.
5. Run the full paired thread matrix.
6. Reject and fully revert a regression or immaterial result.
7. For a material result, run `rust-simplify`, correctness gates, commit, open a
   PR, merge it, refresh `origin/main`, and only then start the next candidate.

Every newly discovered bottleneck or rejected experiment must be recorded in
the open-task checklist before work moves on. Completed and rejected evidence
is retained in the task table below.

## Task ledger

| ID | Status | Finding / task | Evidence and next action |
|---|---|---|---|
| SCALE-001 | Confirmed | dav2d uses a global queue over frame contexts and three row-progress passes: entropy, MV resolution, and reconstruction/filter progress. | With the canonical command, dav2d creates 2/4/8/10 persistent worker threads and the same number of frame contexts; the caller remains a separate admission thread. One tile task advances one superblock row, publishes, and continues or requeues. Use this as the reference model. |
| SCALE-002 | Rejected | Enable split entropy/reconstruction at two workers. | Lowering the four-worker bound to two improved 2T by 17.0% in a 9-pair diagnostic run and was byte-identical at 1/2/4/8/10T, but the full gate proved that the split path raises peak live reference storage from 110,592 to 122,880 bytes at both 2T and 3T. The source change was reverted and no code PR was opened. Superseded by SCALE-022: the serial-peak condition was unachievable by construction, and main already paid the higher peak at four or more workers where the three-thread test never looked. |
| SCALE-003 | Rejected standalone | Queue entropy work from multiple frame contexts instead of synchronously parsing one whole frame on the driver. | A three-context POC derived pending-slot geometry from the header, provisionally refreshed references with terminal CDF/CCSO handles, and scheduled entropy under exact primary/blend/CCSO dependencies. It was output-identical in focused 1/4/10T checks and frame-delay 1/2/3/10 tests, but an 11-pair 10T screen moved the median only from 294.8 ms to 286.5 ms, 2.8%; ten entropy contexts regressed. SCALE-004 did not unlock more value, so revert rather than merge this large foundation. Reintroduce pending entropy products only as part of a row-progress design that materially shortens the dependency chain. |
| SCALE-004 | Rejected | Replace the all-at-once reconstruction task dump with a bounded dav2d-style wavefront that advances and republishes one unit at a time. | An exact POC removed mutex-held commit replay with an owned baton, admitted four-unit precomputes lazily under a global pool-width budget, charged permits only after row conditions became ready, and prioritized entropy/commit before speculation. Baton ownership alone was approximately 0.8% slower. A nine-batch global window regressed approximately 2.5%; 18- and 27-batch windows only converged to neutral/slightly worse. The existing precompute/shadow-surface plus serial replay split is the wrong unit: bounding it removes useful work without advancing reference/filter publication. Revert and pursue SCALE-008's direct resumable row owner instead of tuning this scheduler. |
| SCALE-005 | Rejected standalone | Remove the whole-reference motion-field barrier with row/band publication. | A byte-exact POC published immutable full-width 64-pixel source bands, projected only the current band from the exact named references, and advanced resolve through one ordered continuation. Its raw 30-frame output matched main at SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`. In 11 alternating pairs, 1T moved from 0.962320 s to 0.968369 s (paired +0.38%), 8T from 0.264656 s to 0.258346 s (-2.94%), and 10T from 0.267150 s to 0.261461 s (-1.92%). One 10T trace shortened `pipeline_inflight` from 276.671 ms to 253.575 ms, but `resolve_row` rose from 18.427 ms to 27.258 ms and `mode_record` from 15.049 ms to 24.437 ms while filter/reconstruction work remained whole-frame. Motion-band publication is real and correct but insufficient standalone. Reintroduce it only inside an integrated row DAG that also publishes filter and reference rows; do not merge approximately 1,600 lines of infrastructure for a 2–3% gain. |
| SCALE-006 | Rejected standalone | Start filter progress before whole-frame reconstruction commit completes. | An exact POC introduced canonical 64-luma-row postfilter owners, early deblock/transform-skip records, segmented immutable filter sources, two-band horizontal deblock, and explicit `ReconReady -> DeblockColsDone -> DeblockRowsDone -> FilterStripeDone` admission continuations. Its raw 30-frame output matched main at SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`. In 11 alternating pairs, 1T moved from 0.937173 s to 0.945433 s (paired +0.28%), 8T from 0.265516 s to 0.266778 s (+0.48%), and 10T from 0.269261 s to 0.263486 s (-1.82%). One 10T trace shortened `pipeline_inflight` from 269.829 ms to 254.573 ms and total from 272.848 ms to 257.318 ms, but the paired result is below the 10% architectural acceptance bar. Filter-row eligibility moved correctly, but the adjacent entropy, motion, canonical reconstruction, and reference-publication gates remained whole-frame. Fully revert the standalone implementation and reintroduce it only in SCALE-011. |
| SCALE-007 | Investigated; deprioritized standalone | Partition reference sample ownership to eliminate the shared frame-progress `RwLock`. | The 594 ms sample contains 55 worker-ms in `RwLock::lock_contended`, an optimistic 9.3% wall ceiling, versus approximately 2,543 worker-ms idle. Of 56 contended call sites, 53 are reference readers and 3 are filter writers. Remove the convoy as part of immutable band ownership, but do not expect it to close the architecture gap alone. |
| SCALE-008 | Rejected standalone | Shorten or remove the serial pixel commit/intra reconstruction spine. | The approximately 313 ms to 25 ms `commit_inter` drop is a hard path switch: the 1/2T fused path replays 7,676 inter commands, while the 4/10T prepass leaves only 2,100. A byte-identical by-value tile-superblock-row owner correctly removed the scheduled shadow surfaces, publish copies, completion fanout, and commit mutex, but it also serialized the old motion derivation and replay within each frame. In 11 alternating paired samples, 1T was neutral (0.979014 s baseline, 0.979486 s candidate), while 8T regressed from 0.265387 s to 0.480559 s (paired median +80.98%) and 10T from 0.267624 s to 0.487116 s (+82.35%). Canonical row ownership must return only as part of a motion-band producer/consumer and filter/reference-row publication design that replaces the lost useful intra-frame fanout; do not repeat it standalone. |
| SCALE-009 | Investigated; deprioritized standalone | Remove process-global recycler contention from hot paths. | Individual recycler locks are too small for standalone PRs. Coefficient recycling has an optimistic 4.5% wall ceiling; filter source and residual pools are below 1%, while the inter scratch pool has no contended stack. Make worker-local scratch and frame-context-owned row/coefficient/filter arenas an acceptance requirement of SCALE-003/004; fold motion-field partitioning into SCALE-005. |
| SCALE-010 | Open | Reduce scalar CPU work after the scaling barriers move. | The 0.076443 s target is below ideal 10-way scaling of the current 0.996527 s 1T result. Reprofile the new architecture and require a combined scalar reduction of roughly 23.3% or more without SIMD or assembly. |
| SCALE-011 | Completed | Build one cohesive dav2d-style row DAG across entropy, motion, canonical reconstruction, filters, and reference publication. | Merged in `#1140`. The integrated F1+B3+D/E candidate publishes source motion bands, projects and resolves dependent bands, reconstructs bounded ReferenceOnly work directly into canonical row-band ownership, advances deblock/filter/reference watermarks, and admits dependent rows across frame contexts. The exact 30-frame raw output is 186,624,000 bytes with SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945` at 1T and 10T. The final post-CI alternating matrix measured 0.996527 s versus exact base 0.996898 s at 1T (+0.04%), 0.241250 s versus 0.272401 s at 8T (+12.91%), and 0.242316 s versus 0.275443 s at 10T (+13.67%). Serial settled motion fields retain monolithic storage instead of paying for unused band copies. Parse-time unresolved-leaf containment, `rust-simplify`, the 239-fixture decoder oracle, 245-vector conformance corpus, duplication gate, and `cargo xtask ci` pass. Dav2d remains faster at 10T (0.152885 s), so the final 50%-faster target remains open. A GeneralIntra band-target extension was byte-exact but regressed parallel performance and was fully reverted. |
| SCALE-011-K10.7 | Rejected; no PR | Persistent row cursors, horizontal leaves, and row-scoped reference leases. | Segmented storage, permanent halos, reference copies, and nested joins regressed 8T/10T by about 51%. Do not carry that ownership model forward. |
| SCALE-011-K10.8 | Rejected; no PR | Add a vertical readiness graph over the packed/contiguous path. | A whole-frame workspace claim collapsed effective 10T occupancy from 5.67 to 2.41 cores and regressed wall time by 123%. Do not retry whole-frame claims or lock-order/context-depth patches. |
| SCALE-011-K10.9 | Rejected; no PR | Use hybrid row bands for the remaining current-dependent frames. | Segmented deblock was 2.21x slower, generalized CfL was 11.71x slower, and 8T/10T regressed about 30%. Do not retry segmented whole-frame kernels. |
| SCALE-011-K10.10 | Rejected; no PR | Run leaf-DAG row continuations over contiguous band interiors. | The byte-exact/full-CI candidate added 6.94% instructions through 135-way joins, patches, shadows, and band initialization; 8T/10T regressed about 6%. Do not retry leaf/SB granularity while observable progress remains row-wide. |
| SCALE-011-K10.11 | Rejected; no PR | Use a scoped zero-copy horizontal row wavefront. | Direct writes saved compute, but 225 nested scopes fenced four chunks at a time; 10T occupancy fell from 5.69 to 4.97 cores and wall time regressed 12.7%. Reconstruction ownership variants are evidence-closed under safe Rust plus one contiguous mutable workspace. |
| SCALE-011-K10.12 | Rejected; no PR | Build one fused owned `PostFilterGraph` that reduces filter work and removes progressive-publication lock inversion. | The integrated candidate was byte-exact at 1T/10T, and its typed LR/GDF leaves, exact halos, nonblocking joins, immutable publication, and failure settlement were correct. Five-pair 10T isolation screens rejected every shape: 256-pixel slabs were +51.29% versus exact base, 512-pixel slabs +48.42%, slab expansion disabled +45.37%, and full-width publication with expansion disabled +46.16%. A safe four-segment reference view restored cross-stripe reads but moved hot TIP/reference kernels off contiguous fast paths; the exact integrated graph regressed +54.94% (0.377700 s versus 0.246013 s), with TIP motion-grid attribution rising from about 88 ms to 301 ms. Do not retry immutable segmented reference storage or globally scheduled x-slabs. |
| SCALE-011-K10.13 | Rejected; no PR | Persist the existing typed scheduled-reconstruction context across frames, install its MC/TIP buffers once per admitted row, and move coefficient-quant storage into the same row/task lifetime without changing contiguous reference or filter storage. | The integrated candidate reused worker-bounded `TileDecodeScratch` contexts through typed admission slots, amortized MC/TIP TLS installation over each row, and carried coefficient-quant free lists through scheduled parse and ordered replay while preserving the global fallback. Its 30-frame raw output was byte-identical at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`). Five alternating pairs improved 8T by only 1.91% (0.237016 s versus 0.243152 s) and 10T by 2.50% (0.236977 s versus 0.243055 s); dav2d remained 0.160695/0.155624 s. This confirms task-context ownership is sound but allocator/recycler overhead is not the remaining scaling barrier. Fully revert the runtime candidate and do not pursue standalone recycler/TLS/context-pool edits. |
| SCALE-011-K10.14 | Rejected; no PR | Fuse multi-class Wiener-NS luma classification and filtering into a scalar rolling row pass while preserving the existing normative math, source storage, and full-width stripe task. | The six-feature-row scalar classifier and fail-atomic fused filter were byte-exact at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and matched the old composition in focused u8/u16, partial-cell, stride, and 2/4/8/16-class tests. Five alternating pairs nevertheless regressed 8T by 3.07% (0.255134 s versus 0.246928 s) and 10T by 1.66% (0.250111 s versus 0.245015 s). Attribution explains the loss: rolling removed the hot classification bucket (56.160 ms to 7.328 ms, leaving only PC-Wiener calls), but scalar classified filtering rose from 74.037 ms to 190.727 ms and aggregate LR from 178.704 ms to 241.881 ms. The current full-grid layout repays its materialization through faster bulk feature and filter traversal. Fully revert the candidate; do not retry scalar row-at-a-time Wiener classification without evidence for a faster scalar feature kernel. |
| SCALE-011-K10.15 | Rejected; no PR | Fuse TIP motion refinement and final prediction into one owned command so refined cells, scaling/bounds, temporal records, and output publication share one traversal. | The candidate carried the derived plan and compact grid across the scheduler seam, produced final luma cell-by-cell into failure-atomic recycled `u16` storage while refinement state was hot, completed chroma later, and published once. It was byte-exact at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and passed focused 8/10-bit 4:2:0/4:2:2/4:4:4 partial-cell, stored-MV, and failure-before-publication tests. Five alternating pairs nevertheless regressed 8T by 1.59% (0.248627 s versus 0.245406 s) and 10T by 0.39% (0.246134 s versus 0.244425 s). Attribution showed motion-grid work rising from 79.207 ms to 120.650 ms while TIP prediction fell from 118.241 ms to 84.094 ms: the work moved between phases, but cache-hot cell fusion did not beat the existing bulk plane traversal. Fully revert; do not retry per-cell final TIP MC or owned luma sidecars without evidence for a faster scalar final-MC kernel. |
| SCALE-011-K10.16 | Rejected; no PR | Identify and remove scheduler/driver overhead after every isolated scalar-kernel family failed the at-least-10% candidate ceiling. | Exact-main sampling closed the isolated kernels and exposed occupancy as the dominant gap: Splot measured 0.25 s real, 1.43 s CPU, 19.90 billion instructions, and 4.53 billion cycles versus dav2d at 0.15 s real, 1.34 s CPU, 22.46 billion instructions, and 4.19 billion cycles, or about 5.7 versus 8.9 busy cores. Timeline tracing found 2,807 top-level admission jobs but only 11.64 ms of strict single-job time; three reconstruction contexts stayed active for 155.9 ms. Raising reconstruction depth to ten regressed 10T by 1.73%, and frame-delay four/five improved only about 2%. An exact external-caller driver freed the tenth Rayon worker but lost installed-worker semantics and regressed about 149%; donating its waits back through the pool still regressed about 160%. Exact cooperative global runners then replaced per-job spawns with inline lowest-ready-key execution, but three alternating 10T pairs regressed by 7.48% (0.268773 s versus 0.248583 s). All runtime and diagnostic edits were reverted. Do not retry depth, caller-thread, or admission-runner changes without a design that preserves nested pool execution and demonstrates a different critical-path reduction. |
| SCALE-011-K10.17 | Rejected; no PR | Restore a distinct caller-side driver while preserving installed-pool semantics and the requested number of compute workers. | The exact candidate kept the caller outside Rayon, submitted ready tasks through `in_place_scope_fifo`, and explicitly re-entered the owned pool around every direct whole-frame walk so nested parallel helpers retained the ten-worker registry. This recovered the invalid K10.16 shape from about 0.62 s to 0.29 s, but three alternating 10T pairs still regressed by 18.46% (0.288872 s versus 0.246190 s). Re-entering the pool once for each complete dependency wait preserved the old work-first assist behavior but still regressed 18.34% (0.290500 s versus 0.245486 s). Both 1T/10T outputs were exact before timing; all runtime edits were reverted. Caller separation is not a standalone win under the current mixed driver/direct-work API. |
| SCALE-011-K10.18 | Rejected; no PR | Decouple independent temporal-band projection from the frame-depth and ordered neighbor-grid resolve batons. | The phase-one candidate submitted all nine projections under their exact source-band conditions and retained one ordered neighbor-grid resolve baton. It was byte-exact at 1T/10T but improved the three-pair 10T screen by only 1.46% (0.243915 s versus 0.245933 s). Phase two moved frame preparation and projection ahead of frame N−3's reconstruction gate, applied that gate only to resolve index zero, and replaced the ordered temporal-scratch baton with a non-semantic recycler. It remained exact but regressed 10T by 2.28% (0.245045 s versus 0.242078 s). All runtime edits were reverted. Independent projection has enough worker-time but is not the missing wall-time critical path; do not retry projection order or scratch recycling alone. |
| SCALE-011-K10.19 | Rejected; no PR | Replace the whole-frame depth-three reconstruction gate with a bounded row-progress credit wavefront. | The candidate kept preparation behind N−3's first complete-row credit, then admitted each batch only after N−3 committed the same normalized touched-row fraction. It preserved every semantic condition, per-frame order, failure settlement, contiguous storage, and literal 1T behavior; focused partial-row, terminal, cross-resolution, and 9-band/34-batch-shape tests passed. Its 1T/10T raw output was byte-identical (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`). The three-pair 10T screen nevertheless regressed by 0.59% (0.240336 s versus 0.239840 s); dav2d was 0.154996 s. Fully revert. The measured whole-frame N−3 wait was not removable wall time once real row work and its working set were released; do not retry frame-depth gates or cross-frame row credits. |
| SCALE-011-K10.20 | Rejected; no PR | Find the next materially distinct scheduler-continuation, working-set, or combined scalar-work candidate after the false-dependency family closed. | Trace decomposition closed continuation-only work: the next commit was already precomputed for only 14.620 ms of aggregate adjacent gaps across 758 transitions, terminal finish dispatch added 0.067 ms, and canonical-band gaps included semantic reference/resolve waits. A cache-only 24 ms saving would require about 0.435 billion cycles at the observed occupancy, more than Splot's entire 0.34-billion-cycle gap to dav2d; the only topology large enough is the rejected persistent-runner/context family. After excluding K10.13's coefficient-quant work, the remaining scalar ledger has only an optimistic 5–12% arithmetic range before overlap. Do not implement same-frame fallthrough, slab allocation, completion coalescing, or a blind cross-domain scalar bundle. |
| SCALE-011-K10.21 | Rejected; no PR | Reduce actual entropy-symbol compute rather than exposing another scheduling edge. | Exact 1 ms sampling attributes about 18 worker-ms to `read_symbol_u16`, 12 ms to CDF adaptation, 8 ms to selector/trace dispatch, and 5 ms to literals: only 43 ms combined inside the 116.763 ms entropy phase. Production already uses trusted CDF validation. A dav2d-style binary-CDF specialization is sound, but even an implausible 50% reduction of every symbol primitive is 21.5 worker-ms before cross-frame overlap; binary-only work is materially smaller. Keep fixed-arity symbol decoding as a small scalar-ledger item, not a K10 candidate. |
| SCALE-011-K10.22 | Rejected; no PR | Reduce the larger coefficient/residual pipeline rather than the arithmetic-decoder primitive. | The large outer stacks are inclusive: staged nonzero decode contains coefficient-state construction, and normative EOB/base/sign/quant passes do not duplicate coefficient decoding. The only large exclusive materialization is dense coefficient-state construction (61 top-of-stack worker samples, with another 14 in quant recycling). K10.13 already removed that pool ownership/search in its integrated row context and improved 10T by only 2.50%, about 6 ms. Dequantization and scan generation contribute only 15 and 12 top samples and overlap reconstruction. A touched-index handoff would need a new sparse representation across decode, reconstruction, and CCTX while replacing the current flat dequant path, without a credible route to the missing 24 ms. Keep precomputed scans, DC-only inverse transforms, and sparse dequant as small-ledger ideas; do not implement them as K10.22. |
| SCALE-011-K10.23 | Rejected; no PR | Reduce the larger inter-prediction and motion-compensation compute family. | About 630 exclusive worker samples belong to MC, but they are overwhelmingly required convolution and copy arithmetic rather than removable materialization. Warp's 114-sample prepared pass and 57-sample output pass are the normative horizontal and vertical eight-tap filters; only the already-ledgered 2–4% direct single-reference clip/pack/write remains removable. Translational compound already fuses the second predictor and uniform blend into output, with direct full-pixel, horizontal, and small two-axis dispatch. Its remaining named buckets are kernels: 64 samples in two-axis compound, 42 in copy-plus-average, 28 in generic single-reference subpel, 25/21 in vertical compound/single, 20 in single copy, 19 each in full-pixel/horizontal compound, and 18 in TIP overlap. At the measured 5.7-core occupancy, 24 ms wall requires roughly 137 avoidable worker-ms; no such redundant subset exists. Dav2d uses the same scalar prep/blend and horizontal/vertical warp structure; its remaining advantage is optimized DSP. Keep direct single-reference warp output and broader fused two-axis coverage in the small ledger, not as K10.23. |
| SCALE-011-K10.24 | Rejected; no PR | Reduce actual post-filter kernel work after graph, storage, and rolling-traversal candidates failed. | Exact top-of-stack sampling attributes about 380 worker samples to CDEF, 280 to Wiener-NS/LR, 142 to GDF, and 190 to deblock. Their large buckets are normative kernels: CDEF luma filtering is 151 samples, chroma-pair filtering 74, and direction search 45 while gather is only 15; Wiener luma is 115 and cached PC classification 98 while LR source materialization/write are 11/10; GDF's 75/48-sample width kernels are tap/class math; deblock's 68-sample edge kernel and roughly 84-sample walkers dominate its 16-sample grid build. Existing code already shares the CDEF luma pad between direction and filtering, pairs chroma planes, caches PC features, rolls GDF gradients, and uses direct strided deblock. Dav2d's advantage is DSP plus cross-stage rolling execution. A compact prederived deblock-edge descriptor is the strongest structural remainder at an estimated 40–90 worker-ms, hard ceiling below 120 before its build cost, so it stays in the small ledger. |
| SCALE-011-K10.25 | Rejected; no PR | Preserve ready-heap priority after admission without moving execution out of Rayon. | Exact-trace inference bounded known-ready commit delays below the gate, so a fresh exact-main diagnostic added env-gated per-job frame/phase/unit plus ready-heap-push, scheduler-pop, Rayon-entry, and wrapper-exit timestamps. The diagnostic binary was byte-exact at 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`). Five 10T traces captured 2,772 jobs each: direct priority-inversion intervals had a 29.53 ms median union and a pool-width-cap simulation retained 27.40 ms median potential. The exact POC then kept excess ready jobs in the ordered heap, issued at most the installed pool width, and used an unwind-safe wrapper permit to refill the lowest key on completion, without changing dependencies or Rayon execution. Its three-pair 10T screen improved only 1.91% median (0.240802 s versus 0.245491 s), with paired results -6.38%, +1.71%, and -1.91%; dav2d was 0.157779 s. Queue-age union was largely latency hidden under useful work, not removable wall time. Fully revert runtime and telemetry code; do not retry issue caps or urgent-overflow variants. |
| SCALE-011-K10.26 | Rejected; no PR | Source-attribute the global memory-copy bucket before attempting another storage change. | All 198 exclusive `_platform_memmove` samples are accounted for: CDEF/filter ready-window copies contribute 56, LR/GDF materialization/publication 36, canonical owned-rectangle commit 34, MC/prediction workspace writes 23, coefficient/residual movement 16, frame allocation/output 4, and other metadata growth 29. The largest coherent family is 56 samples; even all filter copies combined are only 92 before subtracting required deblock/filter overlap and contiguous publication. Eliminating canonical commit replays K10.8/K10.11 ownership, borrowing deblocked rows serializes filters, and K10.12's typed filter arenas/immutable slots regressed 45–55%. Keep rolling CDEF output, direct LR stripe writes, exact-capacity metadata vectors, and coefficient-container movement as separate small-ledger ideas; no family reaches the 137-worker-ms gate. |
| SCALE-011-K10.27 | Rejected; no PR | Reduce motion-metadata computation rather than its scheduling or publication order. | Non-overlapping exact-sample attribution gives 209 worker-ms to temporal projection including its trajectory children, 147 to trajectory work alone, 129 to all neighbour-grid construction/publication, and 88 to motion-field unit folding including 53 mutex-wait samples. Projection, intersection, TIP preparation, and temporal-cell writes are normative AV2 work. Dav2d's compact trajectory maps match the already-ledgered 2–4% trajectory-layout simplification; its two-superblock-row neighbour ring cannot delete Splot's two-phase flags/motion readiness, per-position warp/TIP sub-MVs, or parse-ahead flag-log replay. Even deleting all neighbour initialization and publication is only 22.6 ms at 5.7 cores, and exclusive unit segments can remove at most the 53 lock-wait samples while retaining field writes. Keep compact owner indices/SB-row storage and exclusive motion-band segments as small-ledger ideas; no cohesive safely avoidable subset reaches 137 worker-ms. |
| SCALE-011-K10.28 | Rejected; no PR | Attribute blocking mutex waits before attempting another ownership change. | Of 178 exclusive `__psynch_mutexwait` samples, 18 are Rayon's sleep/wake machinery and project-attributed sites total 160. Coefficient-quant allocation/recycling is the largest family at 74, but K10.13 already moved it into row/task ownership and improved 10T only 2.50% (about 6 ms). Motion folding contributes 53, already bounded by K10.27; admission contributes 13, of which 7 are Rayon wake locking, and K10.25's bounded issue queue improved only 1.91%. Filter scratch contributes 11, residual pools 4, frame precompute 4, and tile conditions 1. No family reaches 137 worker-ms or demonstrates a 24 ms critical-path wait; the aggregate crosses the gate only by combining unrelated locks. |
| SCALE-011-K10.29 | Rejected; no PR | Resolve the 110-sample linker-deduplicated symbol bucket into real source families. | Reconstructing exclusive call-tree self time by immediate project caller accounts for all 110 project samples: 98 are inside compound-subpel intermediate output, 6 inside deferred motion derivation, 4 inside temporal resolve, and 2 inside Wiener prepared-class extension. One additional identically labelled occurrence is actually `libsystem_malloc`, explaining the raw 111 total. The 98-sample majority is required compound-MC intermediate work already included in K10.23's roughly 630-sample MC family; the remaining owners are individually tiny and already covered by K10.27/K10.24. No hidden cohesive source family reaches 137 worker-ms. |
| SCALE-011-K10.30 | Rejected; no PR | Attribute frame, row-band, and scratch allocation/initialization before proposing buffer reuse. | Exclusive constructor/validation buckets total 60 worker samples (`CurrentFramePlane::new` 28, `OwnedFrameRect::new` 20, `DecodedFrame::try_new` 12), all `libsystem_malloc` self time totals 43, and zero-initialization totals 59 (`memset` 42 plus `bzero` 17). The impossible cross-lifetime sum is 162, but no compatible family approaches 137: transient reconstruction planes already use a retained buffer pool; filtered output becomes live reference storage; owned row bands are canonical scheduler storage covered by the rejected direct-row/segmented designs; `try_new` enforces immutable-frame invariants; and zero-fill is fragmented, with LR materialization the largest owner at only 10. Keep exact-capacity row vectors and output-plane recycling after final reference release as small-ledger ideas; no arena has a 24 ms ceiling. |
| SCALE-011-K10.31 | Rejected; no PR | Distinguish removable pipeline completion stalls from unavoidable worker-idle parks. | Every one of the 477 sampled `InflightRing::harvest_oldest` observations is at the capacity-reserve call before admitting another frame, not terminal `harvest_all`: 241 samples execute an arbitrary queued job through `wait_with_pool_assist`, while 230 reach the condition-variable park only after Rayon's installed pool reports no runnable job. Entropy reserve-pressure promotion has the same split, with 37 assisted executions and 45 idle parks. These are waits on producers already running or dependency-gated work, not a driver sleeping over queued work. Removing the bounded park only spins; deeper frame admission, caller separation/pool re-entry, continuation runners, row credits, and ready-queue issue caps were already measured negative or below the gate in K10.16–K10.20 and K10.25. No distinct design creates a 24 ms runnable critical path. |
| SCALE-011-K10.32 | Rejected; no PR | Determine whether reference-sample lock contention hides a removable full-frame gate after progressive row publication. | All 75 exclusive `semaphore_wait_trap` samples belong to `FrameProgress::workspace`: 69 reader acquisitions (55 directly under `RefFrameSlot::hold_samples`, 10 inlined block-reference holds, and 4 TIP/reference holds) and only 6 writer acquisitions (4 terminal backlog drains and 2 freezes). Normal stripe publication uses `try_write` and cannot park; only the final backlog-copy/freeze/slot-settlement handoff blocks. Row admission is already exact and progressive, and the 75 worker samples include concurrent readers waiting on one writer, so they neither reach the 137-sample gate nor demonstrate 24 ms of serialized wall time. Dav2d writes disjoint rows directly into its final picture and publishes atomic row progress; reproducing that ownership in safe Rust is the immutable/segmented reference family rejected by K10.12, or adds another copy. |
| SCALE-011-K10.33 | Rejected; no PR | Exhaustively classify the remaining sampled exclusive self-time after the scheduler, synchronization, storage, entropy, coefficient, MC, motion, filter, and allocation families were closed. | All 130 top-of-stack leaves of at least five samples partition exactly into 6,013 samples: K10.21–K10.32 already cover 5,823 across 111 leaves. The remaining 190 samples split into normative general-intra prediction 51, sample validation 41, intra metadata 41, partition/frontier traversal 30, runtime steals/clock reads 22, and recon-row bookkeeping 5. No residual family reaches 137 samples; even impossible deletion of the largest is only about 8.9 ms at 5.7 active cores. Dav2d's leading intra difference is AArch64 DSP, outside the scalar target. Keep these as small-ledger items rather than bundling unrelated domains. |
| SCALE-011-K10.34 | Rejected; no PR | Determine whether ordered commit contains a still-unexposed parallelism opportunity after exclusive self-time is fully attributed. | Fresh 10T counters exhaust replayed reconstruction: ordered general intra is 73.642 worker-ms, inter 31.795, IntraBC 1.541, and replay bookkeeping 0.144. The sampled commit subtree contains 220 intra, 148 deblock, 98 inter, 47 owned-surface publication, 30 filter-window copy, 24 metadata/replay, and 7 residual samples. The existing prepass already takes the maximal safe leading reference-only/contained run; later writes cannot move ahead of an intervening current-dependent block without changing mixed-region chroma residual bases. Applying the sampled MC share to all remaining inter gives only a 24.94 worker-ms prediction-only ceiling before eligibility, side buffers, copies, and overlap. K10.9–K10.11 measured the required band/leaf/scoped ownership negative, and dav2d likewise advances coarse tile SB rows in direct mutable frame storage. |
| SCALE-011-K10.35 | Rejected; no PR | Measure whether portable scalar Rust is leaving a material backend scheduling/code-layout gain on Apple Silicon before changing more source architecture. | The exact-source `-C target-cpu=native` Apple M2 build produced byte-identical 1T/10T raw output, but five-pair screens regressed 8T by 1.071% (0.247375 s versus 0.244559 s) and improved 10T by only 0.271% (0.244506 s versus 0.242773 s); three 1T pairs regressed 0.550% (1.020669 s versus 1.016043 s). Dav2d measured 0.161590/0.157243/1.038208 s at 8T/10T/1T respectively. Host-native scheduling/code layout is neutral and Splot already narrowly wins this stream at 1T; keep generic AArch64 code generation. |
| SCALE-011-K10.36 | Rejected; no PR | Keep the pipeline driver on the caller while preserving one installed compute worker per requested thread. | The exact POC used Rayon's current-thread registry with exposed width/frame delay still `N`, caller plus `N` executors internally, literal 1T, and configured-width resource accounting. Focused parallel/context/pipeline tests passed and 1T/10T raw output was exact. Five-pair screens improved 8T by only 0.369% and regressed 10T by 0.766%; it remained 52.83%/57.40% slower than dav2d. Rayon also permanently registers/leaks the joined caller, forcing later reusable-context calls to fall back. The sleeping external caller and missing nominal worker lane are not the scaling barrier; all source edits were reverted. |
| SCALE-011-K10.37 | Rejected; no PR | Fuse single-reference block-warp rounding, clipping, and sample packing into the existing final vertical pass. | Both implementations passed focused u8/u16, identity/affine, interior/edge parity and produced exact 1T/10T raw output. A shared row sink generated four full generic warp kernels and regressed 1T/10T. A dedicated clipped vertical pass restored the original compound kernel and initially improved five-pair medians by 0.53%/0.68%/1.79% at 1T/8T/10T, but the required 11-pair 10T confirmation reversed to a 1.163% regression with 8 of 11 candidate runs slower. All source/tests were reverted; do not retain a noisy second traversal deletion. |
| SCALE-011-K10.38 | Rejected; no PR | Prederive compact deblock-edge descriptors to remove repeated metadata work from the horizontal and vertical sample walks. | The apparent 40–90-worker-ms descriptor opportunity is a subset of the existing 190-sample deblock family, only about 7–16 ms optimistic wall time at 5.7 active cores before construction cost. Each oriented edge is consumed once, so a descriptor pass moves adjacency, transform, sub-PU, strength, and applicability work earlier rather than amortizing it. A worst-case 1080p edge list adds about 3.1 MB/frame even at an unrealistic eight bytes per entry, or 4.7–6.2 MB at 12–16 bytes, plus another traversal and cache traffic. Preserve the current compact SIMD-scanned candidate masks; retain only zero-storage exact-mask or tile-boundary-bit specializations as micro-ledger ideas. |
| SCALE-011-K10.39 | Rejected; no PR | Remove repeated active-bit-depth sample validation from decoder-owned output and filter paths. | The full validation family is only 41 worker samples: 31 in scheduled inter workspace output and 10 in Wiener-NS sources, an impossible 2.9% wall ceiling. Public workspace checks must remain because storage width does not prove the active 8/10/12-bit range, and tests require range diagnostics plus fail-atomic writes. A private clipped-MC direct-write proof could target the 31-sample subset, while an established prevalidated LR window could target 10, but neither is a qualifying candidate and mixing them crosses domains. |
| SCALE-011-K10.40 | Rejected; no PR | Replace runtime coefficient scan generation with immutable static scan slices. | Ordinary/FSC coefficient parsing and secondary IST share a strict 12-worker-ms ceiling, about 2.1 ms wall before unavoidable scan consumption. A correct centralized design would touch four or five production files and add about 16 KiB of read-only data; dequant, primary inverse transform, and CCTX do not recompute scans. Retain static-slice centralization only for a later combined ledger if a fresh profile still exposes this family. |
| SCALE-011-K10.41 | Rejected; no PR | Skip default-valued intra-context publication for ordinary non-intra leaves. | The candidate deleted redundant ordinary non-intra MRL, palette, and Y-mode writes while preserving active values and IntraBC `DC_PRED`. Focused publication tests passed and 1T/10T raw output was exact. A five-pair screen appeared to improve 1T/8T/10T by 0.70%/2.37%/1.31%, but the host had a renderer using about one core. In a cleaner 11-pair 10T confirmation every candidate pair was neutral or slower, with a 0.709% median regression. All source/tests were reverted; do not retain the lazy-default deletion. |
| SCALE-011-K10.42 | Rejected; no PR | Bypass redundant active-bit-depth rescans for proven-clipped scheduled MC output. | The private helper reused geometry-checked contiguous u16 Frame/Row/OwnedRect access for compound and warp publication, preserved checked fallbacks, passed focused MC/workspace/duplication checks, and produced exact 1T/10T output. Five-pair medians improved 1T by 1.601% but only 0.131% at 8T and regressed 10T by 0.243%. The range rescan is not a stable multithread win; all source/tests were reverted. |
| SCALE-011-K10.43 | Rejected; no PR | Replace the inverse block-size linear search in partition eligibility with an exact O(1) geometry mapping. | The log-geometry mapping matched all 29 generated block sizes and exhaustive valid/invalid dimensions, passed partition/duplication checks, and produced exact 1T/10T output. Five-pair medians regressed 1T by 0.131% and 8T by 0.924% while improving 10T only 0.796%, with scattered opposing pairs. All source/tests were reverted; the retained loop is not a stable wall-time cost. |
| SCALE-011-K10.44 | Rejected; no PR | Use existing deblock candidate-mask bits as a zero-storage luma edge fast path. | Candidate masks are selection supersets, not final-edge proofs: grid construction permits base-record overwrites while OR-ing old/new boundaries, explicit sub-PU marks whole footprints, default sub-PU has no bit, and chroma unions overlay/subsampling candidates. A defensible no-sub-PU luma predicate would require a new non-overlap invariant and prove only one transform-origin comparison, which address attribution gives about one worker sample. Close without implementation; do not add construction state or runtime branches for this ceiling. |
| SCALE-011-K10.45 | Rejected; no PR | Reuse exact CDEF tap aliases inside the dominant scalar filter kernels. | CDEF already hoists tap offsets, row starts, strength adjustments, and direction reuse. Odd directions have one secondary first-distance offset equal to a primary first-distance offset, but only on both-strength blocks. Even an impossible all-odd ceiling is below 37.5 samples; a realistic half-odd ceiling is below 18.75 before constrain/weight/store work. Keep as a lower-priority ledger idea; do not implement ahead of the larger GDF reuse candidate. |
| SCALE-011-K10.46 | Rejected; no PR | Reuse overlapping neighbor windows across the two-row GDF width kernels. | A complete exact chain representation required 348 lines and was reverted before testing. A compact x=0 vertical-chain fallback saved 10 theoretical loads, but AArch64 codegen worsened: mixed width-8 grew 2,820→2,976 bytes with a 240→384-byte frame and more vector loads/stores; uniform width-16 grew and its frame expanded from 336 bytes to roughly 832 bytes; uniform width-8 also grew. The fallback was rejected before exact-output/timing and fully reverted. Do not trade simple reload loops for carried-vector state that increases spills. |
| SCALE-011-K10.47 | Rejected; no PR | Remove redundant exact work from the remaining general-intra scalar kernels. | Smooth, directional, and angular IBP already hoist weights/references; IBP DC's extra traversal is public fail-atomic validation. CfL alone fills a prediction vector with constant DC and immediately rereads/overwrites it with the final clipped value, but this belongs to an eight-sample leaf with an impossible 1.4 ms wall ceiling. Retain direct CfL construction only as a later combined-ledger idea. |
| SCALE-011-K10.48 | Rejected; no PR | Reuse overlapping source rows inside the bulk Wiener-NS luma kernel. | The current kernel is spill-free, shares symmetric pairs, removes zero coefficients, merges class runs, and uses direct valid-u16 lane groups. Two-row fusion could remove 20 of 66 source loads with a generous 34.8-worker-sample ceiling, but a width-32 row already needs eight i32 accumulators; two rows require 16 before tap temporaries and would force narrower lanes or spills. Close without implementation; do not replay rolling K10.14 or hand-expand a multirow sparse convolution. |
| SCALE-011-K10.49 | Rejected; no PR | Measure profile-guided whole-program scalar code generation on the exact decode workload. | Matching LLVM 22.1.8 tools trained 1/2/4/8/10-thread paths and produced a byte-exact PGO binary. It improved 1T by 1.821% but regressed every high-thread pair: 8T by 29.794% median and 10T by 33.802%. The optimized build also reported 4,769 missing-profile-function notices, including 213 in `splot-decode`. No source changed; do not use this workload profile for concurrent decode. |
| SCALE-011-K10.50 | Rejected; no PR | Isolate fat cross-crate LTO with default release codegen units. | The exact fat-LTO binary was 15.6% smaller and improved 1T/8T by 1.00%/2.38%, but regressed 10T by 0.19% and remained 56.83% slower than dav2d there. Clean build cost was 62.38 seconds. No source changed; keep the default release profile. |
| SCALE-011-K10.51 | Rejected; no PR | Remove disabled timing/diagnostic instrumentation overhead from normal decode. | About 217,155 disabled hooks retain flag and `Option` branches but execute no clocks or atomics; K10.33's clock samples came from allocator VM reclamation. The only concrete leaks are roughly 31 formatting allocations, at most 4,226 relaxed first-commit loads, and one 70-counter snapshot per decode. Hoisting all hooks needs pervasive run-mode plumbing or dual-specialized decode, while the local cleanup is far below the gate. Close without implementation. |
| SCALE-011-K10.52 | Rejected; no PR | Add local row-band parallelism inside each retained full-width CDEF stripe task. | CDEF averages only 0.291 ms across 544 stripe calls. An ideal fixed-timeline replay gives 11.68/18.68/22.46 ms savings for 2/4/8-way fanout; even eight-way is only 9.3% of decode before 3,808 child tasks and 544 joins. The ownership-safe row-band shape also replays pre-`369e1bb96` staged CDEF bands and K10.11-style fine nested fences. Close without implementation. |
| SCALE-011-K10.53 | Rejected; no PR | Parallelize only GDF's hot output rows inside the existing full-width stripe task. | The exact candidate split 56-row stripes into 20/20/16 and 64-row stripes into four 16-row children after unchanged source/class construction, with serial 1T and joined failure settlement. Focused parity and raw output passed, but nested Rayon overhead outweighed concurrency: 8T regressed 0.992%, 10T regressed 1.377%, and 1T was neutral. All source/tests were reverted. |
| SCALE-011-K10.54 | Rejected; no PR | Defer deblock-record grid construction out of ordered commit. | Records are already created during parse, detached per row, flattened in semantic order, and bulk-built before scheduled reconstruction. The apparent 148-sample commit bucket contains 126 actual deblock filtering/walk samples, two band-view samples, and 20 unrelated jobs stolen during a join; grid construction contributes zero there. Deferral deletes no ordered work and delays the first final-row/filter-ready frontier. Close without implementation. |
| SCALE-011-K10.55 | Rejected; no PR | Remove redundant primary/secondary inverse-transform and residual materialization. | Exact attribution gives only 79 worker samples to the entire transform-block application subtree: 32 primary inverse, 15 dequant, 29 residual, and three bookkeeping; secondary IST had zero samples. Existing code skips zero rows/coefficients, uses lane column passes, reuses scratch, writes direct rows, and has an inter DC shortcut. The largest remaining residual add/clip traversal is 18 samples. Close without implementation. |
| SCALE-011-K10.56 | Rejected; no PR | Compact internal trajectory MVs from two i32 components to two i16 components. | The private four-byte field was test-clean, byte-exact at 1T/10T, and compiled to direct halfword loads/stores without extra spills. Paired medians were only 0.791% faster at 8T and 0.925% faster at 10T with mixed direction, while 1T was neutral/slightly slower by 0.025%. This misses both the architecture gate and the small-win requirement for repeatable positive direction without 1T regression. All source/tests were reverted. |
| SCALE-011-K10.57 | Rejected; no PR | Compact or split the retained projected temporal-motion cells used by TIP and motion-field consumers. | The 16-byte cell can be losslessly reduced only to 12 bytes because TIP projection reaches ±65535; replacing `valid` plus `i32 ref_offset` with one encoded `i8` saves 126.6 KiB per field and 379.7 KiB across the three retained scratch fields. Even assuming runtime scales with all removed bytes gives only ~1.55% at 1T and 3.3% at 4T, while a 12-byte stride worsens AArch64 indexing. Close without implementation. |
| SCALE-011-K10.58 | Completed | Re-rank the remaining exact 10-thread profile after the K10.12–K10.57 closures. | Resumed with a fresh two-stage diagnostics-only instrumentation pass on exact main. First, a settle census over the scheduled row-reference gate measured zero settle-collapsed batches out of 954 in all three 10T runs, closing the settle-bounding hypothesis. Second, a frame-lifecycle timeline decomposed the steady state: pool utilization is 54% (1,255 worker-ms against a 231 ms wall), scheduler cost is 0.029 ms per commit link, and the 7.26 ms/frame steady pace is set by two nearly equal recurrences. The recon-lane recurrence L/3 = 7.11 ms: admission fires a median 0.04 ms after frame N−3's lane release, and depth is capped by `ReconAdmissionLane::new(ring.capacity().min(3))`, which `--frame-delay` never widens. The publication recurrence W = 7.48 ms: a frame's reference-row watermark first rises only after its commit spine advances 5.35 ms and the stripe-0 final-filter job runs, while consumers need those rows 5.62 ms after their own admission; this lands as the spine's 5.14 ms precompute wait (row-bounded batches wait 6.78 ms/batch on high-dependency frames versus 0.66 ms on low, r = 0.79). Entropy is exonerated in the tail — each frame's parse ends a mean 41.7 ms before its recon admission, so the 52 ms of parse-chain gaps measured in the first stage are driver back-pressure, not an upstream constraint. The ordered commit spine spans 14.36 ms of the 21.32 ms lane occupancy: 8.25 ms execution across 34 links, 5.14 ms precompute wait, 1.00 ms scheduler. Depth-alone experiments (A3, K10.16) were neutral because W remained binding. Successor: SCALE-013. |
| SCALE-013 | Completed | Publish filter stripes one superblock row earlier by replacing the whole-superblock-row deblock back-off with the exact six-mode-info-row reconstruction read lead. | Committed in `d1eb9ad0e`. `safe_deblock_mi_end` previously returned `(completed_rows - 1) * sb_h4`, so the first final-filter stripe waited for two complete superblock rows (30 of 135 units) and the first reference-row publication landed a median 13.1 ms after admission. Ordinary current-frame prediction reads only one row above a superblock boundary (luma forces reference line 0, chroma has no MRL, cross-component references clamp to the superblock's first row); the binding writer is the vertical deblock pass spanning two mode-info rows on subsampled chroma, giving an exact six-row lead. A five-row lead diverges — derivation and falsification agree. First publication drops to 6.9 ms. Byte-exact 1T/10T (186,624,000 bytes, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`); corpus oracle and full `cargo xtask ci` green. Eleven alternating pairs: 10T +5.64% (0.238086 s to 0.224667 s, 11/11 positive), 8T +5.52% (0.240687 s to 0.227410 s, 11/11), 4T +1.36%, 1T neutral, 2T −0.07% (noise). Lane-depth 4/5 on top was byte-exact, passed the reference-storage peak gate, but showed no consistent direction (+0.53% median, 4/7 positive) and was dropped; `ring.capacity().min(3)` stands. Performance caveat: stripe readiness now lands on exact equality at several superblock rows, so future changes to the stripe window margin, deblock pass reach, or pass-0 lane count can silently forfeit the win without breaking exactness. |
| SCALE-014 | Completed | Narrow the subpel MC intermediate arithmetic to 16-bit lanes where the normative ranges allow. | Committed in `de4d4d6df`. § 7.13.3.16 fixes `InterRound0 = 3` with 8/10-bit conformance (§ 6 Table 6.3), so the horizontal intermediate lies in `[-7161, 23529]` and fits i16; the vertical accumulator stays i32 (widening multiply-accumulate). The fused compound scratch, compound horizontal kernel, bilinear pair, clipped-edge/compound/copy variants, and TIP overlap followed; `ACTIVE_TAP_SPANS` pruning retained; warp untouched. A new extreme-contrast test drives both proven bounds against an i64 oracle across all 256 phase pairs. Byte-exact at 1T/10T; 239-fixture decoder oracle green. Paired medians versus the stacked base: 1T -0.64% (9/11), 2T -1.13% (7/11), 4T -0.60% (3/5), 8T -0.55% (10/15), 10T -0.85% (15/21) — consistently positive, retained under the stacking policy. Honest magnitude: ~1%, not the estimated 4-8%, because a fresh 1T sample profile attributes only ~13% of CPU to all subpel MC kernels; wienerns, CDEF, warp, pc_wiener, find_mv_stack, and GDF are each comparable or larger. The 1T scalar queue must be re-ranked against that profile. |
| SCALE-022 | Completed | Enable the scheduled split parse/reconstruction path at two and three workers. | Committed in `851bf8816`; the candidate is one character (`splittable_inter_frame` width bound 4 to 2) plus the retargeted storage test. Eleven alternating pairs: 2T +20.07% (11/11 positive, 0.782585 s to 0.625525 s), 3T +39.39% (5/5), 1T/4T/8T/10T noise; 2T occupancy 1.35 to 1.80 cores for 7% more total CPU; 2T goes from 1.4x to ~1.15x dav2d and 3T to ~1.08x. Byte-exact at 1/2/3/10T; full `cargo xtask ci` green. Storage finding that unblocked SCALE-002's rejection: the split path's peak live reference storage is inherent (a frame evicted from the reference slots can still be a selected reference of a not-yet-run reconstruction because refresh is recorded from parse products) and is bounded by serial peak plus one decoded frame per resolved frame delay — proportional to pipeline depth, not worker count, exactly tight at (2 workers, delay 2) and (3, 3), and already paid by main at four or more workers where the old three-thread gate never looked. That gate asserted the bound, not an invariant; `pipelined_hash_decode_bounds_reference_storage_by_the_resolved_frame_delay` now asserts the depth-proportional cap at 2/3/8 workers with delays 2 and 3. An early sub-finding was corrected during landing: first-breach values had suggested a constant plus-one-frame peak; true peak probing (24 repeats per configuration) established the depth-proportional form. |
| SCALE-016 | Completed | Filter LR/PC-Wiener blocks directly into the post-LR stripe and delete the per-block staging traffic. | Committed in `aaab561b3`. The post-LR stripe is destination-only until GDF reads it, so LR kernels write their rectangles through `rect_mut` with the stripe stride, removing the per-block zero-fill, the `write_lr_block` copy, and PC-Wiener's private staging vector (~4.2 ms of a 1008 ms 1T run; `filter_lr_stripe` 115.6 to 111.3 ms). GDF's dual pre-LR/post-LR source requirement keeps both stripe planes structurally necessary, and instrumented ceilings for the surviving bulk copies (CDEF deblocked-to-stripe 3.73 ms, post-LR seed copy 2.05 ms) close rolling-CDEF and copy-coverage variants as disproportionate. Byte-exact at 1T/10T; oracle and full `cargo xtask ci` green. Paired medians versus the stacked base: 1T +0.48%, 2T +0.82%, 4T +0.00%, 8T +0.27%, 10T +0.96%, no level negative. |
| SCALE-023 | Completed | Admit a fourth reconstruction frame context after the stacked tree made the lane recurrence strictly binding. | Committed in `541eb1ffc`. A re-decomposition on the stacked tree first showed SCALE-013 delivered its W reduction cleanly (first publication 13.1 to 5.8 ms median; W 7.48 to 4.76) while the lane edge stayed exactly tight, so depth again had headroom: `min(3)` to `min(4)` at the recon admission lane measured +1.506% at 10T (9/11) and +0.814% at 8T (9/11), 4T +0.64%, 1T/2T provably identical code path (ring clamps), byte-exact, depth-proportional storage test green, full `cargo xtask ci` green. Depth 5 was dropped: −1.395% head-to-head against depth 4 (1/5 positive). Instrumented depth-4 trace: admit-to-pre0 waiting doubles while real residency grows only 16.18 to 17.81 ms, so L/D is near-invariant (6.75 to 6.62) — extra lanes add no ready work; the win came from collapsing the publication slack (2.57 to 0.21 ms), making the pace publication-bound. This bounds every lane-yielding scheme: SCALE-021 is closed as covered by the depth sweep until S falls. |
| SCALE-028 | Committed; 10T/8T/2T clear the bar | Take the § 7.17 deblock frontier and its stripe-window extraction off the ordered commit baton and run them on a per-frame ordered frontier chain. | Committed in `20f25c390` (seal), `5a99a068d` (chain) and `006c2603b` (recycled copy). Slice 1 seals each completed superblock row into a per-frame filter copy so the chain owns the pixels it filters; slice 2 hands each sealed row to a `frontier(c)` chain that advances deblock, takes the stripes, and whose terminal link runs deblock finish, record restore, terminal stripes and the finish submission, while the terminal commit still sets `recon_done` and recycles the spine workspace. Byte-exact at 1/2/4/8/10T (186,624,000 B, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`); focused suites, 9/9 `frame_pipelining` including the untouched depth-proportional storage gate, `check-duplication` at budget, bare `cargo xtask ci` exit 0. Paired medians against frozen `0200bf08f` with same-source A/A nulls: 10T **−3.385%** (10/11) against null −0.572%, 8T **−1.160%** (10/11) against null −0.894%, 2T **−2.987%** (5/5) against null −0.057%, 4T +0.497% (1/5) against null +0.210%, 1T −0.279% (3/3, and the split path needs two workers so 1T runs unchanged code). Pooled over three matrices per level the picture is stable: 10T ≈ −3.0% (26/27 pairs positive), 8T ≈ −1.5% (30/33). **The 8T result is below the 2%-beyond-null retention bar this candidate was set**; 10T and 2T clear it, and no level regresses beyond null noise, so retention is a maintainer call under the 2026-07-31 stacking policy — `git reset --hard 0200bf08f` drops both commits. Slice 2b, `006c2603b`, removes the copy's construction cost: every sealed row is fully overwritten, so the copy is built over recycled plane storage with no fill, and the retained plane pool now hands a plane a buffer it already fits instead of the last one pushed — taking a chroma buffer for a luma plane reallocated it and left the next chroma plane the oversized one, which is why the copy almost never found reusable storage. Over a 30-frame run at ten threads the copy takes pooled storage for 63 of its 75 planes instead of 19, plane fill falls 34.9 to 27.2 ms and plane allocation 3.1 to 0.6 ms summed over the workers. Marginal against frozen `291bdad00` over four 11-pair matrices: 10T **−0.87%** (31/44 pairs) against nulls −0.233/+1.063/−0.042%, 8T **−0.70%** (29/44) against nulls +0.331/+0.092%, 1T −0.774% (3/3), 2T −0.759% (4/5), 4T a wash (−0.359% over nine pairs against a −0.507% null). Cumulative against `0200bf08f`: 10T −2.934% (10/11), 8T −2.113% (8/11), 2T −2.101% (5/5), 1T −0.659% (3/3), 4T +0.971% (1/5), so **8T now clears the 2% bar** the chain alone missed. Slice 3, relaxing the frontier for the sealed path from the SCALE-013 six-row read lead to deblock's own reach (`F = 32c − 4`, refusing `32c − 3`), was implemented, measured and reverted: it is byte-exact and the derivation holds, but it releases no stripe earlier and measures null (10T −0.717/+0.169/−0.780%, 16/33 pairs; 8T −0.252/+0.447/+0.004%, 17/33; nulls +0.541/−0.237%). The mechanism is arithmetic: a stripe is ready when `final_luma_rows` reaches its end plus a `16 << subsampling_y` halo, stripe ends sit 64 luma rows apart, and the two leads differ by 8. Under the six-row lead the frontier already lands exactly on a stripe boundary (`128c − 40` equals the `88 + 64s` requirement at `s = 2c − 2`), so both leads make stripes `0..=2c − 2` ready after `c` superblock rows — verified over 128/64-superblock, 4:2:0/4:4:4 and 1080p/720p geometries. **The six-row lead costs the frontier nothing and is not a lever.** Two mechanism findings. First, the design's intended shape — seal into `OwnedFrameBands` and reuse `advance_bands` — is dead on arrival: the segmented deblock kernel costs 2.8x the contiguous one (`deblock_plane_pass` 106.0 to 241.7 ms over the same 1266 calls, 10T +10.2% over five pairs), the same 2.2x SCALE-011-K10.9 measured. Sealing into a second contiguous `CurrentFrameWorkspace` keeps the fast kernel and costs only 4.5–5.3 ms of copy plus 8.4 ms of workspace allocation per 30-frame run. Second, the chain must filter its three planes in sequence: a nested Rayon section inside the frontier link executes another queued job on the same worker, and a sampled hang proved that job can be the very commit waiting for the storage the link holds. Slice 4 re-ran the SCALE-023 recon-lane depth sweep on the shortened chain and rejected it. `ReconAdmissionLane::new(ring.capacity().min(4))` to `min(5)` is byte-exact at 10T (186,624,000 B, SHA-256 `48f0dc14…`) and 9/9 on `frame_pipelining` with the depth-proportional reference-storage gate untouched and green, but against frozen `006c2603b` it measures 10T −0.298% (6/11) and +0.514% (4/11) against a −0.944% A/A null, and 8T −0.306% (8/11) against a −0.457% null — inside or behind the null at both levels, nothing like SCALE-023's +1.506%/+0.814%. Depth 6 was therefore not built, and no other level can move: `ring.capacity()` is the resolved frame delay, so at 1/2/4 threads `min(5)` and `min(4)` clamp to the same depth and run identical code. The prediction that a shorter per-frame commit chain re-opens depth does not hold; the lane edge is not what binds after the frontier split. Slice 4 also skipped the `OwnedFrameBands`-style `release_through` of consumed sealed rows rather than building it, on three grounds. The construction cost it would target is already gone: slice 2b builds the copy over pooled plane storage with no fill (6-buffer process pool, 63 of 75 planes recycled, 0.6 ms of allocation per 30-frame run). The 4.5–5.3 ms that remains is the row copy itself, and releasing a row after the frontier consumes it does not remove the copy that put it there. And the footprint it would save is both small and structurally unreachable — at most four in-flight sealed copies of 6.22 MB against a 601 MB peak RSS at ten threads, and a contiguous plane allocation cannot free its own prefix, so a ~3-superblock-row cap is a rolling banded buffer: exactly the segmented shape this row already measured at 2.8x on the deblock kernel. |
| SCALE-029 | Rejected; no source change | Publish reference-row watermarks as the § 7.21 guided detail filter finalizes rows inside a filter stripe, instead of only at stripe end. | Step-1 diagnostics on exact `f1a0ac1ae` logged, per pending frame, every `FrameProgress::row_condition` registration with the watermark it saw, every stripe publication, the guided-detail start and end of each stripe chain, and an eight-row progress marker inside both § 7.21 row loops; three ten-thread 30-frame runs were reconstructed offline. Consumers do wait: 22 to 36 row-gate registrations per steady frame block on a threshold above the published watermark, for 72.4 to 80.3 ms/frame summed over blocked consumers. But the part of that wait **sub-stripe publication can recover** is only **0.546 / 0.553 / 0.604 ms per steady frame** summed over every blocked consumer, and **0.285 / 0.288 / 0.303 ms per steady frame** as the union of the intervals in which any consumer was blocked purely by stripe-end granularity — under the 1 ms/frame kill line in all three runs, and about a fifth of the 1.5 ms/frame implement line. Two mechanisms cap it. First, § 7.21 runs on only 7 of 28 steady frames (136 of 544 stripes; `gdf_config` yields nothing on the rest and `apply_stripe` returns in about 200 nanoseconds), so on 21 of 28 frames the last writer of a luma row is § 7.17 loop restoration and guided-detail row granularity recovers exactly zero; where it does run it is worth 2.18–2.42 ms/frame summed and 1.14–1.21 ms union, which the 25% duty cycle amortizes away. Second, even there the window is bounded by one stripe's guided-detail pass — 0.85–0.98 ms measured, matching `filter_gdf_stripe` at 119.6 ms over its 136 active calls — because CDEF, CCSO and loop restoration are whole-stripe passes that must all finish before the stripe's first row can be finalized. The premise that the filter stripe publishes late is right; the premise that stripe-end **granularity** is what makes it late is wrong. The same reconstruction located the term that does dominate, recorded as SCALE-030: publication lagging the stripe chain that produced it, worth 9.0–9.6 ms/frame summed over consumers and 2.36–2.67 ms/frame as an interval union. All diagnostics were reverted; the worktree is byte-identical to `f1a0ac1ae`, so no exactness or timing matrix was run. |
| SCALE-030 | Committed in `c46a24085`; retention call open | Publish a filter stripe from the reader whose prefix borrow made its `try_write` fail, instead of leaving it for the next stripe or the terminal drain. | **Mechanism, measured on exact `f1a0ac1ae` with an env-gated timestamp on every queued stripe over three 10T 30-frame runs (544 stripes each).** Before: median publication lag 16–19 us, but **27.8 / 29.4 / 27.8%** of stripes deferred past 100 us, p90 **1192 / 1243 / 1047 us**, p99 **3024 / 3801 / 4071 us**, max 5089 / 7189 / 6226 us, **169.6 / 194.4 / 174.0 ms** summed, and **55 / 44 / 41** stripes landing only at the terminal `BeforeFreeze` drain. The cause is retry cadence, not lock occupancy: 73% of stripes win `try_write` on the first attempt, so the lock is free most of the time — but a stripe that loses it has no further retry until the *next* stripe finishes, roughly 1 ms later, which is exactly the p90. The filter side cannot fix that; it offers only 17–26 retry points per frame. **Design.** The backlog moves out of `OwnedFilterSetup::filtered` into `FrameProgress` as `StripeCopy` closures, and `PublishedFrame`'s `Drop` releases the shared borrow and then calls `drain_pending`. Readers take the prefix once per predicted block — thousands of times per frame — so the retry cadence becomes the consumer cadence, and each attempt fires at the instant a blocking borrow ends. **The non-blocking rationale is preserved literally**: every attempt on the hot path is still `try_write`, so no stripe and no reader ever queues behind a writer, and the cross-frame cycle the borrow rules warn about (two readers each holding the other frame's prefix) stays impossible because neither ever waits. The one blocking write is the terminal drain, which already blocked and runs only after the phase is over. Exactly-once and fail-atomicity are unchanged: the queue is emptied by `mem::take` under the exclusive lock the copies need, so a stripe is taken by one drain and lands once; a failed copy abandons the rest of its batch, publishes no watermark for itself or anything behind it, and records its diagnostic for the next `publish_stripe` or the terminal drain to return. **After, same three runs:** **15.6 / 19.3 / 20.0%** deferred, p90 **211 / 305 / 279 us**, p99 **1666 / 1327 / 1447 us**, **52.6 / 55.0 / 51.8 ms** summed, **6 / 5 / 2** stripes at the terminal drain. Split by drainer: **72.6 / 75.9 / 74.8%** of stripes still land on their own worker at median 0 us, **26.3 / 23.2 / 24.8%** land on a reader releasing the prefix at median 96–122 us, and 0.4–1.1% reach the freeze. **Timing.** Byte-exact at 1/2/4/8/10T (186,624,000 B, SHA-256 `48f0dc14…`). Paired medians against frozen `f1a0ac1ae` (same code as `89d0eb15b`), 11 pairs at 8/10T against same-source A/A nulls, 5 at 2/4T, 3 at 1T: **10T -3.309%** (9/11 faster, null **-0.090%** 6/11), **8T -2.567%** (11/11, null **-0.158%** 6/11), 4T **-0.716%** (5/5), 2T **-0.160%** (4/5), 1T **-0.239%** (3/3). Cumulative against `0200bf08f`: 10T **-6.456%** (10/11), 8T **-4.044%** (11/11), 2T **-2.938%** (5/5), 1T **-0.841%** (3/3), 4T **-0.362%** (4/5) — the +0.971% 4T cost SCALE-028 carried is gone. Suites: `frame_pipelining` 9/9 with the reference-storage gate untouched, `frame_progress` 10/10, decode `filters` 211/211, `scheduled` 4/4, decode and parallel `admission` 25/25 and 9/9; `cargo fmt --check`, `clippy -D warnings`, `check-duplication` at budget (2619), bare `cargo xtask ci` exit 0. **Not exhausted.** 15.6–20.0% of stripes still wait, because a reader release only frees the lock when it is the last reader; with ten workers reading a pending reference that is often false. The remaining lever is the hold itself — `RefFrameSlot::hold_samples` takes one shared borrow per predicted block and keeps it across the whole of that block's motion compensation — not another drain trigger. SCALE-031 measured that lever and closed it. |
| SCALE-031 | Rejected; no source change | Shorten the per-block reference-sample hold, or defer arriving readers, so a queued filter stripe finds the progressive workspace free instead of waiting to be the last reader out. | **Why the family reopened.** K10.32 closed progressive-workspace lock contention on *blocking*: all 75 `semaphore_wait_trap` samples belonged to that one `RwLock`, 69 were reader acquisitions, and nonterminal publication used `try_write` and could not park. That reasoning is untouched. SCALE-030 changed what a *hold duration* costs rather than what a wait costs: reader release became the publication trigger, so the time a block spends inside `RefFrameSlot::hold_samples` now decides when a queued stripe lands, and the hold has a measured consumer for the first time. This row measures that consumer and closes it. **Step-1 mechanism on exact `ddb14def1`**, with env-gated per-hold, per-arrival, per-stripe and per-row-gate timestamps, three 10T and three 8T 30-frame runs. The premise reproduces: 544 stripes per run, publication mode own 396–408 / reader 131–141 / freeze 2–12 at 10T, median lag 0 us, p90 **161 / 248 / 289 us**, p99 **1355 / 1709 / 1904 us**, **14.2 / 17.8 / 18.4%** deferred past 100 us, **37.1 / 58.8 / 62.4 ms** summed; 8T p90 181–192 us, p99 958–1184 us, 14.7–16.2% deferred, 37.6–40.6 ms summed. **Hold attribution**, site-tagged over one 10T run: 4,823 filtering holds, about 150 per frame, 187.7 ms held in total, mean 38.9 us — TIP batched output 935 holds / 72.4 ms / 77.4 us mean, TIP motion grid 986 / 53.3 / 54.0, pure inter MC 1,213 / 40.3 / 33.2, deferred compound grid 932 / 9.8 / 10.5, placed inter MC 346 / 4.7 / 13.5, § 7.13.3.25 BAWP 24 / 0.12 / 5.2. **§ 7.13.5 TIP owns 67% of all hold time**, and the mean hold is 39 us rather than one small block's convolution. **Occupancy.** Only 6.1–7.3% of a pending frame's span has any reader at 10T (7.8–8.4% at 8T) — but conditioned on a queued stripe it is effectively total, because a stripe only stays queued when its own immediate `try_write` already lost: of 803–974 reader arrivals that occur while a stripe is queued, **11–16 (1.3–1.8%) find the lock free**, and 88.4–92.8% of the 907–1,250 releases that see a queued stripe still leave peers behind. **Ceiling.** Instrumenting every `FrameProgress::row_condition` registration (691–717 per run, about 23 per steady frame, matching SCALE-029's 22–36) against the perfect-publication counterfactual — a stripe visible the instant its copy is queued — the recoverable consumer wait is **2.63 / 3.31 / 3.69 ms/frame summed and 0.64 / 0.68 / 0.86 ms/frame as an interval union** at 10T, and **1.08 / 1.14 / 1.22 ms/frame summed and 0.27 / 0.28 / 0.34 ms/frame union** at 8T. Both levels are under SCALE-029's 1 ms/frame kill line, and the metric is calibrated by SCALE-030 on this same path and stream: it removed 2.36–2.67 ms/frame of the same union and realized 3.3%, or 0.257 ms/frame of a 7.0 ms/frame pace, about 10% realization. Applying that gives at most **−0.9 to −1.2% at 10T and −0.4% at 8T with a cost-free mechanism**, against a 2%-beyond-null retention bar and a host A/A null band of about ±0.9% at 10T. 8T misses the bar by roughly five times before any mechanism is paid for. **The three candidate designs close individually.** Narrowing the hold to a bounded per-block window copy has to happen inside the two TIP kernels that own two thirds of the hold time, and it moves those convolutions off the contiguous reference reads: K10.23 (MC is required arithmetic) and K10.12/K10.12C (segmented or scratch reference views measured 2.2–2.8x on the hot kernels) both apply, for a sub-1% prize. Batching holds per four-superblock precompute is wrong twice over — acquire/release count is not the constraint, since only 1.3–1.8% of arrivals find a free lock and extra retry points therefore publish almost nothing, while a longer hold raises occupancy in exactly the windows where a stripe is queued and removes the last-reader releases that publish 24–26% of stripes today. Publication-priority handoff splits into a free variant and a paying one: draining once on arrival can publish only those 11–16 arrivals in 900, and a deferral that actually waits must wait the residual life of a pending window, which is heavy-tailed (p90 161–289 us, p99 1.4–1.9 ms) and sampled length-biased by about 900 arrivals per run, so a bounded spin captures only the median-20-us windows that already cost nothing while paying on every arrival. **Thread coverage.** 1T builds no `FrameProgress` at all — no pending slot, no hold and no queued stripe in a whole 30-frame run — and 2T reaches it with 4 holds and zero arrivals-with-pending, so the term exists only at 4T (343 arrivals, 3 free), 8T (525–678, 11–15 free) and 10T. All diagnostics were reverted; the worktree is byte-identical to `ddb14def1`, so no exactness or timing matrix was run. |
| SCALE-032 | Investigated; no source change | Explain why splot's CPU matches dav2d's at `--limit=30` but exceeds it by 20% at `--limit=120`. | **Frame counts are equal**, so the comparison is sound: both decoders emit exactly 30/60/120 output frames (splot `frame.output_index` runs 0..N−1, dav2d reports `Decoded N/N`). Splot decodes 32 coded frames for 30 outputs and 121 for 120 — one hidden key frame, plus a one-frame overshoot at limit 30 that counts *against* splot at the short limit. **The datum reproduces and keeps going.** 10T CPU (user+sys, `/usr/bin/time -l`, median of 3 alternating pairs) splot versus dav2d is 1.400/1.340 s = **1.045x** at limit 30, 2.600/2.260 = **1.150x** at 60, 4.670/3.830 = **1.219x** at 120, 9.040/7.280 = **1.242x** at 240. **Nothing accumulates.** The marginal cost of one steady frame is flat across every window: 40.00 versus 30.67 ms CPU (**1.304x**) over frames 30–60, 34.50 versus 26.17 (**1.318x**) over 60–120, and 36.29 versus 28.67 (**1.266x**) over 120–240. The aggregate walk is pure mix: `(1.400 + 0.03633(L−30)) / (1.340 + 0.02767(L−30))` predicts 1.147 at limit 60 and 1.219 at limit 120 against measured 1.150 and 1.219, asymptote **1.313**. The 30-frame benchmark is dominated by an intra-heavy prologue costing splot 46.7 ms/frame against dav2d's 44.7; **the mission's headline 30-frame CPU parity understates the steady-state CPU gap by about seven times**, and the "+20% at 120" is that mix converging, not a late-frame defect. **It is threading, not content or state.** At 1 thread the same steady frames cost splot 24.00 ms CPU against dav2d's 22.78 (**1.054x**), and splot executes **fewer** instructions per steady frame (430.5 M versus 453.3 M). The whole gap opens under threading: at 10T instructions are at parity (463.2 versus 454.6 M/frame, +1.9%) while cycles are **113.8 versus 86.3 M/frame (+31.8%)**, IPC 4.07 versus 5.27. Going 1T to 10T, splot adds 7.6% instructions and **34.7% cycles**; dav2d adds 0.3% and 9.4%. Average clock is identical for both at each width (3.53 GHz at 1T, 3.13 GHz at 10T), so core-type mix is controlled and the term is stalled cycles, not frequency. Sys time is a minority: marginal sys is 2.0 ms/frame for splot against 0.44 for dav2d, 18% of the 8.66 ms/frame CPU gap. **It is not working-set footprint.** The 10T frame-delay sweep moves peak RSS from 240 MB to 710 MB while marginal cycles/frame stay 114.8 / 114.5 / 114.6 at delay 2/4/10 (delay 1 is *worse* at 124.4). Splot's RSS does grow with run length (590/669/729/802/837 MB at 30/60/120/240/360 frames against dav2d's 366/413/423/429/439) but that growth buys no cost. **It is not contention inside one decode.** Holding 8 busy cores fixed and regrouping the workers changes almost nothing: marginal cycles/frame are 106.4/108.7 as 1 process x 8 threads, 108.5/104.8 as 2 x 4, 103.8/104.6 as 4 x 2 — so deleting every lock, queue and shared structure between workers of one decode is worth **at most about 3%**. dav2d is flat the same way (81.6/81.2 at both 1 x 8 and 2 x 4). **The step is the split parse/reconstruct path itself.** At identical 8-core hardware pressure, 8 independent 1-thread processes (fused walk, `splittable_inter_frame` requires `current_pool_width() >= 2`, `crates/splot-decode/src/prediction/inter/frame_walk.rs:274`) cost **92.2** M cycles/frame, while 4 independent 2-thread processes (split path) cost **104.2** — **+13.0%, about 12 M cycles/frame**, paid at the first extra worker and nearly independent of worker count thereafter. Independent-process runs also bound the pure memory-system term: 8 concurrent 1-thread decodes cost splot +8.0% and dav2d +8.1% over solo, so shared-hardware pressure is equal for both and small. dav2d's own threading is nearly free — 78.9 M cycles/frame at 1 thread to 81.4 at 8 threads in one process, **+3.2%**. **What the split path makes slower.** With `SPLOT_DECODE_TIMING` at 2 workers versus 1 (its overhead is +0.8% cycles at both 1T and 10T, so the ratios are not an instrumentation artifact), the streaming kernels are indifferent — `filter_lr_stripe` 181.4 to 184.6 us/stripe, `filter_cdef_stripe` 195.0 to 197.3, `wiener_ns_luma` 18.43 to 18.61, `pc_wiener_classify` 17.34 to 17.72, `filter_gdf_stripe` 45.98 to 46.69, `inter_tip_prediction` 60.02 to 61.47, all +1 to +2% — while the entropy and intra path inflates on identical item counts: `block` 3.13 to 4.28 us (+37%), `intra_leaf` 3.12 to 4.50 (+44%), `coeff` 4.97 to 7.29 (+47%), `commit_intra` 4.43 to 5.09 (+15%). At 10 workers the same asymmetry widens to `block` x2.16, `intra_leaf` x2.45, `coeff` x1.95 against filters x1.22–1.26. The split path also multiplies units for the same pixels: `deblock_plane_pass` runs 39.4 times per frame instead of 4.8 (+17% total time) and `mode_record` writes 1,480 records per frame instead of 987. Honest accounting gap: the instrumented kernels explain about 1.3 ms/frame of the +4.1 ms/frame that 2 workers add, and the remaining ~2.8 ms/frame sits in the uninstrumented prepass and publication work that replaced the fused `commit_inter` (7.62 to 0.81 ms/frame, 288.8 to 83.7 items). A 4T-versus-10T sample profile shows no leaf moving more than 0.55 percentage points of busy time, so the extra cycles are spread across the whole kernel set rather than concentrated in one contended site; the blocking symbols do grow (`swtch_pri` 47 to 1,715 samples, `__psynch_mutexwait` 234 to 1,777) but total under 4.4% of busy samples. **The wall gap decomposes multiplicatively.** Steady-state 10T occupancy is 6.96 cores for splot against 8.59 for dav2d, so the marginal wall ratio is CPU 1.313 x occupancy 1.234 = **1.62**, matching the measured 6.29/3.38 = 1.861 (30–60), 4.66/2.96 = 1.575 (60–120) and 4.22/3.09 = 1.369 (120–240) as the pipeline fills. Per-frame CPU is therefore the larger of the two factors and has never been this mission's target. **Ceiling.** Closing the 12.0 M cycles/frame split-path step is 10.5% of splot's 113.8 M at 10T: steady CPU/frame 36.33 to 32.5 ms and, at unchanged 6.96-core occupancy, wall 5.22 to 4.67 ms/frame. Closing splot's whole parallel penalty down to dav2d's proportional one (113.8 to 92.4) is −18.8% cycles and 5.22 to 4.24 ms/frame. Conversely the coordination bound above caps every remaining scheduler, publication and lock candidate at about 3% of per-frame **CPU** — SCALE-030 already took 3.3% of wall from that family, and it did so by removing idle, so the bound constrains CPU-side candidates and not further occupancy work. **Side finding, recorded because it blocks verification:** raw and Y4M output of this stream fail past ~35 output frames. Retained output frames count against `max_reference_store_bytes` (268,435,456; `crates/splot-decode/src/limits.rs`), and `actual` reports 273,715,200 — exactly 44 frames of 6,220,800 bytes — identically at `--frame-delay` 2 and 10, so it is output retention and not pipeline depth. `--limit=35` succeeds; 40, 42, 43, 60 and 120 all raise `decode/resource-limit`. Only the discard-hash benchmark path runs beyond 35 frames, so today no byte-exactness check longer than 35 frames is possible. All measurements used existing env-gated instrumentation; no source changed and the worktree is byte-identical to `abd883e4f`. |
| SCALE-033 | Investigated; rejected; no source change | Attribute the split parse/reconstruct path's +12 M cycles/frame precisely, and remove the removable part. | **The hardware counters settle the shape before any phase timer touches it.** Marginal steady-frame cost (`--limit=120` minus `--limit=60`, over 60 frames, `/usr/bin/time -l`, median of 3, frozen `8ea93605f`) is 23.33 / 26.50 / 28.00 / 32.17 / 35.50 ms CPU at 1/2/4/8/10 threads, on 412.4 / 436.3 / 435.3 / 440.4 / 444.9 M instructions and 79.3 / 90.4 / 93.4 / 103.0 / 110.9 M cycles, IPC 5.20 / 4.83 / 4.66 / 4.28 / 4.01. **Instructions step once and then stop**: the second worker adds 23.9 M/frame (+5.8%), and going 2 to 10 workers adds only 8.6 M more (+2.0%) while cycles add 20.5 M (+22.7%). Priced at the single-thread IPC of 5.20, the whole 1T-to-10T instruction growth of 32.5 M/frame buys at most **6.25 of the 31.6 M cycles/frame** the width costs, so **at least 80% of the 10-thread penalty is stall on constant work**; at 2 threads the same arithmetic gives 4.6 of 11.1 M cycles, 41% work and 59% stall. Extra work is therefore fully paid at the first extra worker, exactly as SCALE-032 predicted, and it is not what grows with width. **Attribution.** Env-gated `SPLOT_DECODE_TIMING` phases were placed at the lowest points both walks share — `PendingInter::derive_motion` and `PendingInter::reconstruct_from_motion`, `crates/splot-decode/src/prediction/inter/block/deferred_recon.rs:206` and `:280` — so the fused and split paths are timed on identical item counts, plus a scope on every split-only step. Medians of three `--limit=30` runs, ms summed over workers for the whole run; the instrumentation costs +2.1% CPU at 1T, +1.4% at 2T, +0.4% at 8T and +1.4% at 10T, so none of the ratios is an artifact. Against the CPU each width adds over 1T (140 / 330 / 470 ms per run at 2/8/10 threads): **(a) work the fused walk never does** 57.1 / 96.7 / 129.8 ms (41% / 29% / 28%); **(b) identical work running slower** 17.7 / 164.5 / 253.2 ms (13% / 50% / 54%); **(c) duplicated publication bookkeeping** 9.9 / 14.2 / 15.0 ms (7% / 4% / 3%); uninstrumented scheduler, pool and driver residual 55 / 55 / 72 ms (40% / 17% / 15%), so the 2-thread shares are lower bounds while the 8- and 10-thread ones are firm. **(a), itemised** (ms/run at 2T / 8T / 10T): the per-superblock rect publish `OwnedFramePlaneRect::publish_into` (`crates/splot-recon/src/workspace/owned_rect.rs:93`) 14.5 / 19.1 / 21.7 over 3,645 rects; the sealed filter copy `ScheduledTileRecon::seal_committed_rows` 6.8 / 11.5 / 23.4; the whole of `prepare_scheduled_tile` less its duplicated grid 33.0 / 39.3 / 41.3, of which the rects' allocation and zero fill are 6.6 / 9.1 / 9.1, the owned deblock records 7.6 / 10.9 / 10.9 and the second whole-frame workspace 5.3 / 2.8 / 2.6; the banded deblock cascade, which runs `deblock_plane_pass` 1,270 times per run instead of 136 over the same pixels, +9.5 / +21.0 / +33.9; stripe window and publish +2.7 / +9.0 / +10.8; frame prologue +1.8 / +7.9 / +9.9; less the fused path's own deblock grid and overlay work, −9.1, and its terminal freeze, −3.6. **Top mechanism, named: the split path materialises each frame's pixels three times** — into a per-superblock `OwnedFrameRect`, then into the frame workspace, then into the sealed filter copy. Rect allocation, rect publish, seal copy and seal workspace together are 34.1 / 42.5 / 48.8 ms per run = 1.07 / 1.33 / 1.53 ms per coded frame ≈ **3.6 M cycles/frame at 2 threads**, and a 6-second `sample` of a `--limit=400` run puts `_platform_memmove` at 6.82% of busy CPU at 2T against 3.23% at 1T, with `publish_into` its largest single caller (96 of 483 samples). **(b), on identical item counts** (us per item, 1T → 2T → 8T → 10T): `block` over 18,046 leaves 2.71 → 3.35 → 3.73 → 3.65 (x1.35 at 10T); `coeff` over 1,512 x1.34 / x1.57 / x1.49; `intra_leaf` over 10,311 x1.30 / x1.38 / x1.34; `commit_intra` over 10,311 x1.10 / x1.60 / x1.66; `motion_derive` over 7,676 x1.02 / x1.24 / x1.46; `reconstruct_from_motion` over 7,676 x1.01 / x1.15 / x1.30; `filter_cdef_stripe` over 544 x1.01 / x1.14 / x1.25; `filter_lr_stripe` x1.02 / x1.12 / x1.19; `filter_gdf_stripe` x1.01 / x1.09 / x1.14; and `deblock_plane_pass` over the identical 1,270 banded passes x1.13 / x1.27 from 2T to 10T. **The inter reconstruction core is thread-indifferent at two workers** (+2% motion, +1% prediction) and only degrades with width, while the entropy parse degrades immediately. **(c) is small, and SCALE-032's lead was a red herring.** The 14,203 replayed `record_flags` calls (`crates/splot-decode/src/prediction/inter/find_mv_stack/neighbour_grid.rs:298`) cost 2.8 / 3.7 / 4.0 ms per run — 0.09 ms/frame at 2T — so the `mode_record` 987-to-1,480 inflation is worth about 2% of the step and is not a lever. The larger half of (c) is the second `NeighbourMvGrid` that `prepare_scheduled_tile` allocates per frame beside the parse pass's own, 6.5 / 9.6 / 10.0 ms per run. **SCALE-032's unattributed 2.8 ms/frame is now attributed, and it is not extra work.** `commit_inter` 310.4 ms/run at 1T becomes `prepass_row` 200.8 plus `prepass_motion_pass` 83.0 plus `commit_inter` 22.8 = 306.6 at 2T, on identical 7,676 motion derivations and 7,676 predictions: the reconstruction moved, it did not grow. **Why nothing was implemented.** The only (a) subset above the 3 M cycles/frame implementation line is the triple materialisation, and removing it means handing the prepass borrowed rects of the frame workspace instead of owned ones. `ReadyReconSurface<'static, T>` is owned precisely because the workspace is moved into `ScheduledCommit` for the frame's lifetime while the ordered commit writes into it concurrently, so that is a restructuring of scheduled-reconstruction surface ownership, not a smallest reduction. Every individually small candidate measured under the line: rect allocation 0.8 M cycles/frame, second neighbour grid 0.7, sealed-copy workspace 0.6. Two sub-candidates were measured and disproven outright — the banded temporal projection is a wash against the fused whole-field one (leaves 67.9 ms/run fused against 68.7 banded, `proj_scan` 56.0 against 56.4), and raising `MAX_RETAINED_RECON_PLANE_BUFFERS` from 6 to 32 is neutral (`sched_seal` 5.9 to 6.3 ms/run), so the sealed workspace's cost is its fill and not a pool miss. Skipping the publish for units the prepass left untouched was also bounded and rejected: 3,392 of 4,189 units precompute every entry, only 546 precompute none, so the copy is carrying real pixels. **Verification.** No candidate; the worktree is byte-identical to `8ea93605f` and the rebuilt release binary hashes identically to the frozen base (`a8830a49a544…`). Output was verified byte-exact anyway at 1/2/8/10 threads: raw 30 frames, 186,624,000 B, SHA-256 `48f0dc140be5…`. A/A null over eleven alternating pairs is −0.139% at 10T (4/11) and +0.207% at 8T (6/11), so the noise floor is about ±0.2 to 0.4%. |
| SCALE-034 | Investigated; closed | Discriminate host core heterogeneity from per-worker working-set pressure in the 10-thread CPU penalty, and decide whether a layout candidate survives. | **Width sweep.** Marginal steady frame (`--limit=120` minus `--limit=60` over 60 frames, `/usr/bin/time -l`, median of 3, rebuilt release binaries, host Apple M2 Max, 8 performance cores plus 4 efficiency cores). splot cycles/frame 79.6 / 90.4 / 93.7 / 98.1 / 102.1 / 110.5 M at 1/2/4/6/8/10 threads on 412.4 / 436.2 / 436.9 / 437.2 / 440.2 / 445.1 M instructions, IPC 5.18 / 4.82 / 4.66 / 4.46 / 4.31 / 4.03, CPU 22.83 / 26.50 / 28.00 / 30.17 / 31.83 / 35.33 ms, occupancy 1.01 / 1.87 / 3.73 / 5.32 / 6.82 / 7.57 cores, peak RSS 141 / 282 / 438 / 551 / 654 / 727 MB. dav2d on the same host and build: 73.8 / 75.0 / 74.9 / 75.2 / 77.0 / 82.2 M cycles on 424.9 / 424.6 / 424.6 / 423.6 / 424.7 / 428.6 M instructions, occupancy to 9.35, RSS 68 / 135 / 210 / 284 / 356 / 423 MB. **Cycle inflation is already present and monotone well inside the performance-core count**: splot is +13.6 / +17.7 / +23.3 / +28.2 / +38.9% over its own 1T against dav2d's +1.6 / +1.4 / +1.9 / +4.3 / +11.3%, and dav2d's 1T-to-8T cost of +4.3% confirms SCALE-032's +3.2% on this host and build. Per added worker splot's slope is +10.84 M at the split-path switch, then a flat +1.62 / +2.23 / +1.96 M across 2-to-4, 4-to-6 and 6-to-8, and +4.23 M for 8-to-10 — about twice the sub-eight slope, so the boundary step is real but is not where most of the penalty is made. **Performance-core lever.** macOS exposes no performance-core affinity, but `taskpolicy -b` confines a process to the efficiency cluster, and a 1T decode there retires identical instructions (32.33 against 32.27 G) for 12.686 G cycles against 6.105 G, IPC 5.29 to 2.55: **an efficiency core costs 2.08x the cycles for the same work**. Saturating all four efficiency cores with background-priority spinners while splot runs at ordinary priority approximates a performance-core-only run, and moves splot only 98.3 to 97.9 M cycles/frame at 6T (−0.4%) and 102.6 to 101.5 at 8T (−1.1%). **At eight workers splot is already effectively performance-core-only**, so its entire +28.2% inflation there is internal to the performance cluster. **Hardware pressure against software sharing — the decisive control.** N independent single-thread decodes hold the same cores busy with zero shared software state, in separate address spaces. Marginal cycles/frame at eight busy cores: splot solo 1T 79.6, eight independent 1T processes 88.6 (**+11.3%, hardware**), one 8-thread process 103.1 (**+16.5% beyond that, software**); dav2d 73.8, 78.2 (+5.9%), 76.9 (−1.6%, its threading is free). At ten busy cores, where both shapes carry the identical core mix so heterogeneity cancels on both sides: solo 79.6, ten independent 1T processes 95.1 (+19.5%), one 10-thread process 111.0 (+16.7% beyond). **The software term is 14.5 M cycles/frame at eight workers and 15.9 at ten — it does not grow with width**, so the 8-to-10 step in the plain sweep is host topology, not further sharing. Splitting it by instructions (412.5 to 445.3 M/frame at ten, priced at the concurrent-hardware IPC of 4.34) gives 7.6 M cycles of extra work and **8.4 M cycles of stall on constant work**, against 8.45 by the same arithmetic at eight workers. **Decomposition of the 31.4 M cycles/frame 1T-to-10T penalty**: host topology and memory-system pressure at ten busy cores **15.5 M (49%)**, the split path's extra work **7.6 M (24%)** — already named and closed by SCALE-033 as the triple pixel materialisation — and stall on constant work inside one address space **8.4 M (27%)**. splot pays roughly twice dav2d's hardware term at eight cores (+11.3% against +5.9%), tracking its 1.7 to 2.0x larger footprint at every width. **Where the stall lands.** Per-item phase cost with `SPLOT_DECODE_TIMING` at `--limit=30`, comparing one N-thread process against N independent 1-thread processes so hardware pressure cancels, on identical item counts. The hardware column is uniform across every phase alike (+1 to +4% at two busy cores, +5 to +6% at four, +8 to +13% at six, +11 to +19% at eight) — streaming kernels and metadata phases pay the same, which is the signature of memory-system pressure rather than a shared structure. The software column is not uniform: **every streaming filter and motion-compensation kernel is flat at every width** — at eight workers `filter_cdef_stripe` +0.2%, `filter_lr_stripe` +1.2%, `filter_gdf_stripe` +0.7%, `wiener_ns_luma` −0.5%, `pc_wiener_classify` −0.0%, `inter_tip_prediction` +2.9% — while the entropy parse and intra reconstruction carry all of it: `block` +19.4 / +17.4 / +17.4 / +19.0%, `coeff` +33.3 / +33.8 / +35.1 / +42.4%, `intra_leaf` +23.2 / +19.2 / +20.7 / +18.5%, `commit_intra` +5.0 / +19.8 / +20.1 / +33.0% at 2/4/6/8 workers. `block`, `coeff` and `intra_leaf` step at the first extra worker and then stay flat, so they are a property of the split path rather than of width; **`commit_intra` is the only member that grows with worker count**. Summed over identical-item phases the software term is 44.1 worker-ms per 30-frame run at eight workers, 88% of it in those four: `commit_intra` 16.7, `block` 10.8, `intra_leaf` 6.3, `coeff` 4.1. **Instrumentation hazard checked and cleared.** `PHASE_COUNTERS` (`crates/splot-decode/src/timing.rs:192`) is one shared atomic pair per phase, and a nested scope's two read-modify-writes land inside the enclosing scope's measured interval, which would fake exactly this result. Enabling the instrumentation costs +1.32% cycles in the eight-process shape against +1.14% in the eight-thread shape, so the counters are not contending and the attribution stands. **The shared structures, read in source.** The CDF bank is not shared and is not the target: `TileCdfWorkUnitBoundary` (`crates/splot-decode/src/bitstream/tile_payload/cdf.rs:306`) clones a private 47 KiB `TileCdfSubset` per tile through `tile_copy` (`cdf.rs:267`) and keeps only an immutable `Arc` frame bank; every neighbour-context array is per-tile as well. What the entropy and intra paths do share across workers is process-global pooling: `static TRANSFORM_COEFF_QUANT_BUFFERS: Mutex<Vec<Vec<i32>>>` (`crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs:49`) is acquired twice per transform block behind a best-fit scan of up to 2,560 entries, and its buffers migrate structurally from the parse worker to the commit worker because `into_quant` (`coeff_state.rs:241`) moves them into the `ReconCommand` while `Drop for LumaCoeffBlock` (`coeff_state.rs:138`) frees them from the other core; `static PARSED_RESIDUAL_PLANES` (`crates/splot-decode/src/residual/pipeline/plane_execution.rs:91`) adds one or two acquisitions per general-intra leaf; and the ordered commit containing `commit_intra` (`crates/splot-decode/src/prediction/inter/block/pixel_commit.rs:135`, inside `replay_recon_row` at `:60`) runs on one worker at a time but not the same one, mutating the single per-tile `TileDecodeScratch.ordered` (`crates/splot-decode/src/prediction/inter/block/tile.rs:898`, used at `:1218`) drawn from a pool that never pins a scratch to a worker (`InterReconScratchPool::with_scratch`, `tile.rs:874`). **Verdict.** The addressable term is 8.4 M cycles/frame, **7.6% of the 111.0 M cycles/frame at ten threads**, below the 10 M line this row was set and flat from eight to ten workers. Its largest single member, `commit_intra`, is 38% of it — about 3.2 M cycles/frame, 2.9% of ten-thread per-frame CPU — level with SCALE-033's 3 M cycles/frame implementation line and only if the saving converts to wall at unchanged occupancy. **The CPU-side lever is closed at about 7.6% of per-frame CPU**: 49% of the penalty is host topology no decoder layout can reach, 24% is the split path's extra work already closed behind a surface-ownership restructuring, and the remainder spreads over four phases with no member large enough to carry a candidate on its own. Reopen only for a design that pins the ordered commit and its scratch to the worker that produced the row, and only if a fresh sweep puts the software stall term back above 10 M cycles/frame. **Verification.** No candidate and no source change; all measurements used shipped env-gated instrumentation and the worktree is byte-identical to `6d459c440`. |
| SCALE-035 | Rejected; no source change | Let the parallel prepass reconstruct in place in the frame workspace, deleting the per-superblock `OwnedFrameRect` plus `publish_into` plus `seal_committed_rows` triple materialisation. | **The prize is bounded at 3.4% of steady-frame CPU before any mechanism is paid for.** Exact worker time at `--limit=120` (121 coded frames, 10 threads, median of 3, phase counters): `OwnedFramePlaneRect::publish_into` is 101.3 ms over 16,200 rects, 0.84 ms per coded frame, and the sealed filter copy is 50.8 ms, about 0.42 ms per frame, against a marginal steady frame of **35.83 ms CPU** (`--limit=120` minus `--limit=60`, user+sys, `/usr/bin/time -l`, median of 3, 10 threads). **Ownership finding: in place is not expressible here, and the batch geometry the task assumed does not hold.** `superblock_row_batches` (`crates/splot-decode/src/prediction/inter/block/tile/admission.rs:945`) only stops a batch from *crossing* a superblock row; a 1920-wide frame has 15 units per row and four 4-unit batches inside it, so batches within one row are column ranges of the *same* luma rows and a row-range split is not disjoint. The disjoint claim therefore has to be a 2-D rect partition, which is exactly `CurrentFrameWorkspace::rect_surfaces` (`crates/splot-recon/src/workspace.rs:1346`) — and it borrows `&mut self` for the lifetime of every rect, while the ordered commit needs `&mut CurrentFrameWorkspace` concurrently for `replay_recon_row` (`crates/splot-decode/src/prediction/inter/block/pixel_commit.rs:60`), whose general-intra and IntraBC reconstruction read neighbours across superblock boundaries. Holding both is a self-referential borrow; the only ways out are the already-closed shapes — segmented storage for the commit's own kernels (K10.9: 11.71x CfL, 2.2x deblock), a scope that fences the prepass (K10.11), or overlapping halos (K10.7). The per-block variant collapses to the same thing: a "direct" write still needs a second concurrent `&mut` into one contiguous workspace. **POC, byte-exact, measured, reverted.** The nearest safe approximation was built: the prepass publishes its own batch's rects on the worker that produced them, under a non-blocking `try_lock` of the commit mutex, with the ordered commit publishing whatever it found busy, gated off for IntraBC frames. Raw output was byte-exact at 1/2/4/8/10T (186,624,000 B, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`). Mechanism, per 30-frame run: rects published early **3,088 / 2,705 / 2,287 of 3,645** at 2/8/10 threads (84.7 / 74.2 / 62.7%), and the ordered spine's publish fell from **14.9 / 19.8 / 22.1 ms to 2.3 / 4.6 / 8.0**, moving 0.41–0.49 ms per frame off the ordered chain at unchanged total work. It did not convert. Two 11-pair matrices against frozen `5c732817d` with same-source A/A nulls: 10T −0.070% (5/11) then +0.292% (6/11) against a null of −0.195%; 8T −0.189% (4/11) then −0.734% (4/11) against +0.182%; 4T −0.151% (1/5) then −0.370% (2/11) against +0.016% and +0.149%; 3T −0.248% (2/5); 2T +1.123% (4/5) then +0.231% (6/11) against +0.409% and +0.452%; 1T −0.129% (2/3) on provably identical code, since 1 thread never builds a `ScheduledFrame`. Pooled: 10T +0.11% over 22 pairs (11/22 positive), 8T −0.46% (8/22), 4T −0.32% (3/16). **Nothing is consistently positive and 4T is consistently negative.** This corroborates SCALE-024 from the other side: the spine's own execution is not the constraint — it already waits 6.2 ms/frame on its own row-gated precompute — so copy work moved onto the precompute workers lands *on* the constraint, and at two to four workers there is no spare lane at all. **Decisive control: the scheduled path already contains a zero-copy in-place shape, and it is worth exactly zero.** `supports_owned_bands` (`admission.rs:872`) lets an eligible frame reconstruct straight into `OwnedFrameRowBand` storage with no rect, no `publish_into` and no seal, its § 7.17 frontier deblocking those bands. It takes 4 of 31 coded frames at `--limit=30` and 21 of 121 at `--limit=120`; the rest are rejected as `current_frame`. A one-line diagnostic that forces every frame onto the copying path is byte-exact and prices the trade with the identical binary, phase counters at `--limit=120`, 10 threads, median of 3: publish 83.7 to 101.3 ms (+17.5), seal 44.7 to 50.8 (+6.1), `deblock_plane_pass` 268.7 to 244.7 (**−24.0**) over the identical 4,812 calls. **In place saves 23.6 ms of copying and pays 24.0 ms of segmented deblock: a net 0.4 ms over a 120-frame run.** Its wall A/B is null too (11 pairs, same binary, env-toggled: 10T +0.684%, 6/11, against a −0.258% null; 8T +0.040%, 5/11, against +0.014%), and marginal steady-frame CPU is 35.83 ms/frame banded against 36.50 forced-legacy, inside that metric's own noise at 3 reps. **Sub-candidates bounded and closed.** Coalescing the per-superblock rect into one rect per batch: publish moves 796 MB in 101.3 ms (7.9 GB/s) while the seal moves 752 MB of the same storage in 50.8 ms (14.8 GB/s), so the entire gap between a 256-byte strided row copy and a full-width contiguous one is 47.5 ms per 120-frame run, 0.40 ms/frame, **1.1% of steady-frame CPU and only for infinite coalescing** — a full-width rect needs one batch per superblock row, which is SCALE-011-D6's rejected whole-row tasking; four-superblock coalescing captures part of that for about 0.55%, under the resolution of the paired matrix. Skipping the rect's zero fill with a recycled buffer is worth about 9 ms per 30-frame run but has no airtight proof: `publish_into` copies the whole rect including the suffix the prepass's prefix rule left untouched, so it would rest on "every sample of every superblock is written by the prepass or the replay" — true of a correct decoder, but with a nondeterministic failure mode, unlike `CurrentFrameWorkspace::new_recycled`'s "seals whole rows upward and reads only below them". **Verification.** No source change survives; the worktree is byte-identical to `5c732817d` and the rebuilt release binary hashes identically to the frozen base (`d65e831c364a…`). |
| SCALE-036 | Rejected; fully reverted | Replace process-global pooling in the entropy and intra paths SCALE-034 named with ownership that fits the measured buffer lifetimes. | **Per-pool measurement first**, on exact `5c732817d` with env-gated take/recycle timers, per-worker attribution and pool-length sampling, one 30-frame run per width. Only one of the three named pools pays. `TRANSFORM_COEFF_QUANT_BUFFERS` (`crates/splot-decode/src/bitstream/tile_payload/coeff_state.rs:49`) runs 13,793 take/recycle pairs for **4.34 worker-ms at 1T, 33.29 at 8T and 27.64 at 10T** — an **eight-fold inflation of identical operation counts**, the SCALE-034 signature exactly. The mechanism is the best-fit scan, not the lock alone: the pool saturates its 2,560-buffer cap under the split path where 1T reaches only 409, so the take scan averages **537 entries at 8T and 317 at 10T** (7.41 M and 4.37 M entries per run) and the full-pool eviction pass runs on every recycle once saturated. `PARSED_RESIDUAL_PLANES` (`residual/pipeline/plane_execution.rs:91`) costs **3.5 worker-ms per run** over 20,622 pairs and the inter scratch pool (`prediction/inter/block/tile.rs:874`) **0.4 ms** over 1,035 — two orders under the line. The "rotating unpinned" ordered-commit scratch **is not rotating**: over 4,226 links the worker changes only 934 times at 8T and 891 at 10T, so the commit already keeps its scratch on one core for 79% of the chain and there is no migration to remove. **The task's proposed shape is refuted by its own measurement.** Worker-local free lists cannot serve the quant pool because takes and recycles are structurally on different workers — the parse worker builds the buffer, `into_quant` (`coeff_state.rs:241`) moves it into the `ReconCommand`, and `Drop for LumaCoeffBlock` (`coeff_state.rs:138`) frees it on the commit worker. At 10T one worker takes 6,594 buffers and recycles 126 while another takes none and recycles 1,769; per-worker lists would drain on the taking side and overflow on the recycling side. **What was built** kept the pool shared and made both ends O(1): buckets by `floor(log2(capacity))`, take from the first bucket whose buffers all hold `len`, recycle into the buffer's own bucket, at most 11 buckets fixed by `MAX_RETAINED_COEFF_BUFFER_CAPACITY`. Selection order, the 2,560-buffer retention cap and the keep-the-larger eviction policy were preserved, and buffer choice is allocation policy only because `zero_buffer` clears and resizes whatever it returns. Pool cost after: **0.55+0.79 worker-ms at 1T, 0.87+1.76 at 8T, 1.03+2.24 at 10T**. It was byte-exact at 1/2/4/8/10T (186,624,000 B, SHA-256 `48f0dc140be565069838bcf7141aba3c80cefaa14400284840b2e1475a3be945`) and fast: paired medians against frozen `5c732817d`, **10T -3.413%** (11/11, A/A null -0.491%), **8T -2.657%** (10/11, null -0.448%), 4T -0.992% (5/5), 2T -2.021% (5/5), 1T -0.166% (2/3), reproduced within noise by a second matrix on an earlier build (10T -3.158% 11/11, 8T -2.973% 11/11); marginal steady-frame CPU **36.25 to 35.50 ms/frame at 10T and 32.67 to 31.50 at 8T** over 10 alternating reps. `fmt`, `clippy -D warnings`, `check-duplication` (2619) and `check-comment-density` (248) all pass. **It was rejected on the reference-storage gate.** `pipelined_hash_decode_bounds_reference_storage_by_the_resolved_frame_delay` fails intermittently at two workers: **8 failures in 80 runs against 60 of 60 clean on exact base**, always at resolved frame delay 2, always `actual 147456` against a 135168 ceiling — serial peak plus **three** decoded frames where the gate allows two. **The mechanism is the parse lead, and a control proves it.** Rebuilding the identical bucketed pool with a `black_box` scan restoring the old take-side cost inside the lock — same structure, same lifetimes, same eviction, only the parse-side saving removed — passes the gate **40 of 40** and is still byte-exact. So no lifetime, ownership or retention change causes the extra frame: accelerating the entropy parse relative to reconstruction does, by letting parse products refresh reference slots further ahead of the reconstructions that still select them. **This bounds a whole class of future candidates.** SCALE-022 landed the depth-proportional bound as "exactly tight at (2 workers, delay 2)", and it is: the configuration has no headroom, so any parse-side speedup at two workers is rejected by an empirical bound rather than by a structural invariant. **The parse-neutral half is worth having and is priced**: the restored-scan control, whose only remaining saving is the commit-side eviction pass, measures **10T -1.351%** (11/11), **8T -2.230%** (10/11) and **2T -3.408%** (5/5) against the same frozen base while passing the storage gate 40/40 — roughly 40% of the 10T win and most of the 8T win, from the end that never accelerates parse. A shippable version of it needs O(1) eviction over the existing flat pool without touching the best-fit take scan, which the bucket structure cannot express. **Verification.** Fully reverted: the worktree is byte-identical to `5c732817d` and the rebuilt release binary hashes identically to the frozen base (`d65e831c364a…`). Suites run green on the candidate before the gate failure was found: `frame_pipelining` 8/9 (that one gate), decode and parallel `admission` 25/25 and 9/9, `scheduled` 4/4, `coeff_state` 26/26, decode `residual` 123/123. Peak RSS on the candidate was unchanged at 1T over 120 frames (135 MB) and at 10T over 30 frames (560 against 561 MB), and rose 682 to 694 MB (+1.8%) at 10T over 120 frames — pipeline fill, consistent with the same lead the gate caught. |
| SCALE-012 | Open correctness task | Make deferred finish reporting happen-before in-flight harvest. | `PendingFinish::run_finish` in `crates/splot-decode/src/pipeline/inflight.rs` currently lets `FrameSlotWriter::complete` (or its failure Drop) settle the slot before writing `FinishOutcome.records` or `FinishOutcome.error`; `InflightRing::harvest_oldest` waits only for that slot and can therefore wake, observe an empty outcome, and lose recyclable records or the real filter diagnostic. Add a separate one-shot finish-report completion owned by `PendingFinish` and `InflightEntry`; publish it only after the outcome write, and make harvest wait for the report before consuming the outcome and slot. Cover success, failure, and exactly-once settlement in `crates/splot-decode/src/pipeline/inflight_tests.rs`. Keep this separate from SCALE-011 filter-seam work. |

## Rejected experiments

- Reconstruction lane width 4 and width 10.
- Split entropy/reconstruction at 2 workers without frame-context storage
  ownership; it improved 2T by 17% but exceeded the serial reference-store
  peak.
- Scheduler-owned whole-frame entropy with three pending contexts; exact, but
  only 2.8% faster at 10T and neutral at 1T.
- Owned four-unit commit baton with lazy global reconstruction windows of 9,
  18, and 27 batches; exact, but slower or neutral because it did not advance
  reference/filter publication.
- LIFO in place of FIFO task scopes.
- Batch size 1.
- Commit fusion with blocking and try-lock ownership.
- Commit-gated bounded precompute window.
- Removing the temporal scratch chain.
- Exact whole-field motion dependency selection.
- Motion-band publication plus ordered band resolve without filter/reference-row
  publication; exact, but only 1.9% faster at 10T and 2.9% at 8T.
- Canonical reconstruction bands plus scheduled deblock/filter stripes without
  the adjacent entropy, motion, and reference-row stages; exact, but only 1.8%
  faster at 10T and neutral/slower at 1T and 8T.
- Removing motion-field folding/locking as a ceiling probe.
- The earlier cross-stage deblock slab scheduler.

These results reject the specific implementations, not the architectural tasks.
Do not repeat them without a materially different ownership model and a stated
reason the prior failure no longer applies.
