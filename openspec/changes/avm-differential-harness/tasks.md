# Tasks

> Status: **proposed**. None started.

## Implementation

- [ ] Define the local AVM checkout/corpus discovery (no vendoring).
- [ ] Implement `avm encode` → `splot validate` comparison in `xtask`.
- [ ] Document the reproduction command in `docs/CONFORMANCE.md`.

## Tests and proof

- [ ] Record the reproduction command in the `CONF-AVM-DIFF-HARNESS` row.

## Checks

- [ ] `cargo xtask ci`
